// Auto-generated by tools/codegen/generate_natives.py.
// Do not edit manually.
#![allow(clippy::too_many_arguments)]
#![allow(clippy::missing_safety_doc)]

use crate::native::{self, NativeArg, NativeError, NativeString};
use crate::types::*;

// ============================================================================
// AICOVERPOINT
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_enter_cover(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4972A022AE6DAFA1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_exit_cover(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2BC4A6D92D140112, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x140_b3_cb1_d424_a945(ped: Ped, weapon_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x140B3CB1D424A945, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_c7_a9_c2_c953128_fe(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3C7A9C2C953128FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x53_e4_d0_c079_ca6855(handle: ScrHandle) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(handle)];
    unsafe { native::invoke(0x53E4D0C079CA6855, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x64340_dc208_d671_d5(cover_layer: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(cover_layer)];
    unsafe { native::invoke(0x64340DC208D671D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_a1_fdcf35_eaa140_f(cover_layer: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(cover_layer)];
    unsafe { native::invoke(0x7A1FDCF35EAA140F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x957_d7_e750216_d74_b(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x957D7E750216D74B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eba51_a294_c73292_e(args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0xEBA51A294C73292E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_cover_blocking_area(args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0x733077295AB51304, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_scripted_cover_point(data: &mut Any) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::in_out(data)];
    unsafe { native::invoke(0x975BD6351648935F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_load_cover_anims_loaded(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8CBE916CFC64AD5C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_cover_point_exist(handle: ScrHandle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(handle)];
    unsafe { native::invoke(0xC276FE69DDA22BAD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_cover_point_state_from_ped(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5F5B1B7E8E8F94C6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_flinch_cover_anim(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2A31D13C5F021D0D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_running_cover_anims(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1A7A802B2301EDC0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_ai_seek_cover_to_cover_point(args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0x89783FDDF079C88D, &arguments) }.map(|_| ())
}

// ============================================================================
// AITRANSPORT
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_transport_config_flag(transport_entity: Entity, flag_id: i32, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::int(flag_id), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xF382C92CCC1CCDBC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_entering_transport(ped: Ped, transport_entity: Entity, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(transport_entity), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x619E63980BFC0096, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_exiting_transport(ped: Ped, transport_entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(transport_entity)];
    unsafe { native::invoke(0x660639BC60157048, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_off_transport_seat(ped: Ped, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flags)];
    unsafe { native::invoke(0x8886D83A430537FD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_on_transport_seat(ped: Ped, transport_entity: Entity, seat: i32, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(transport_entity), NativeArg::int(seat), NativeArg::int(flags)];
    unsafe { native::invoke(0xE588B5A8A005CB5E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_transport_accessible_seat_flags(transport_entity: Entity, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::int(flags)];
    unsafe { native::invoke(0xDD0660C997DE94FD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_transport_config_flag(transport_entity: Entity, flag_id: i32, value: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::int(flag_id), NativeArg::boolean(value)];
    unsafe { native::invoke(0xBA8818212633500A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_enter_transport(args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0xAEE3ADD08829CB6F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_exit_transport(args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0xC273A5B8488F7838, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_b6_c9_a43_f7_d9109_b(transport_vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(transport_vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x4B6C9A43F7D9109B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_all_seat_preference_slots(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5639FBEA922788DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_transport_exit_blend_ratio(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF8C20282B237E3F7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_in_transport_seat(transport_entity: Entity, seat_index: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::int(seat_index)];
    unsafe { native::invoke(0xFFEC4B0A1A3ED515, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_transport_usage_flags(transport_entity: Entity, flags: &mut i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::in_out(flags)];
    unsafe { native::invoke(0xE195C5A82156321D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_on_transport_entity(ped: Ped, transport_entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(transport_entity)];
    unsafe { native::invoke(0x159EF5B6EDCE00E8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_on_transport_seat(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xDC44F405A6B98D03, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_transport_seat_free(transport_entity: Entity, seat_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::int(seat_index)];
    unsafe { native::invoke(0x43FF27FC1829C202, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_transport_seat_occupied(transport_entity: Entity, seat_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::int(seat_index)];
    unsafe { native::invoke(0x2E2E06023D07631E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ai_can_use_transport(transport_entity: Entity, state: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::boolean(state)];
    unsafe { native::invoke(0x67F7CEAC2391E114, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_use_transport_seat_preference(ped: Ped, transport_entity: Entity, preference_slot: i32, p3: i32, seat_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(transport_entity), NativeArg::int(preference_slot), NativeArg::int(p3), NativeArg::int(seat_index)];
    unsafe { native::invoke(0xB7079F4C72896756, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_transport_exclusive_driver(transport_entity: Entity, ped: Ped, seat_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::int(ped), NativeArg::int(seat_index)];
    unsafe { native::invoke(0x4248AB2EEB3C75AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_transport_exit_blend_ratio(ped: Ped, ratio: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(ratio)];
    unsafe { native::invoke(0x8C8371EDFAF014A0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_transport_priority_seat(transport_entity: Entity, seat_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::int(seat_index)];
    unsafe { native::invoke(0x13F138225C202F66, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_transport_usage_flags(transport_entity: Entity, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(transport_entity), NativeArg::int(flags)];
    unsafe { native::invoke(0xE2487779957FE897, &arguments) }.map(|_| ())
}

// ============================================================================
// ANIMSCENE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn abort_anim_scene(anim_scene: AnimScene, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x718CF1328D20C2B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_anim_scene_to_entity(anim_scene: AnimScene, entity: Entity, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::int(entity), NativeArg::int(p2)];
    unsafe { native::invoke(0xDC418495DBA327A1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_anim_scene_to_entity_preserving_location(anim_scene: AnimScene, entity: Entity, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::int(entity), NativeArg::int(p2)];
    unsafe { native::invoke(0x1C0B105C3F30B88D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn block_anim_scene_fading_next_frame(p0: bool, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x1B70811D3BF75DB9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn check_ownership_of_anim_scene(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x661B8683611B9B97, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn could_anim_scene_entity_reach_exit_next_frame(anim_scene: AnimScene, entity_name: &str, p2: NativeArg, p3: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(entity_name), p2, p3];
    unsafe { native::invoke(0x73616E64696C616E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_anim_scene(anim_scene: AnimScene) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x6843A1AA3A336DFF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_anim_scene_preserving_location(anim_scene: AnimScene) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xA2507C4948C83D2E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_anim_scene_exist(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x25557E324489393C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn fade_anim_scene_audio_in(anim_scene: AnimScene, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::float(p1)];
    unsafe { native::invoke(0xA41351EA2A18A0AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn fade_anim_scene_audio_out(anim_scene: AnimScene, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::float(p1)];
    unsafe { native::invoke(0x323E3AD772BA5D57, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_bool(anim_scene: AnimScene, name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(name)];
    unsafe { native::invoke(0x07A6F6447ECA9B64, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_current_active_camera_count(anim_scene: AnimScene) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x4822A65D5AF64E69, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_entity_location_data(anim_scene: AnimScene, entity_name: &str, matrix: &mut Vector3, p3: bool, playback_list_name: &str, p5: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(entity_name), NativeArg::in_out(matrix), NativeArg::boolean(p3), NativeArg::string(playback_list_name), NativeArg::int(p5)];
    unsafe { native::invoke(0x8398438D8F14F56D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_float(anim_scene: AnimScene, name: &str) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(name)];
    unsafe { native::invoke(0xCC24CB07F60B496E, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_int(anim_scene: AnimScene, name: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(name)];
    unsafe { native::invoke(0x2B7277484CC095FD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_origin(anim_scene: AnimScene, position: &mut Vector3, rotation: &mut Vector3, order: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::in_out(position), NativeArg::in_out(rotation), NativeArg::int(order)];
    unsafe { native::invoke(0xADF1D53F3B1FE0A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_phase(anim_scene: AnimScene) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x3FBC3F51BF12DFBF, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_anim_scene_exited(anim_scene: AnimScene, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xF94692EB9DC15D74, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_exited_anim_scene(anim_scene: AnimScene, entity_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(entity_name)];
    unsafe { native::invoke(0xB89FCFF19DAFFF28, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_exiting_this_frame(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xCDC5512A407CF08D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_finished(anim_scene: AnimScene, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xD8254CB2C586412B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_in_section(anim_scene: AnimScene, section_name: &str, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(section_name), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x8D81E7824B7753F7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_loaded(anim_scene: AnimScene, p1: bool, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x477122B8D05E7968, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_metadata_loaded(anim_scene: AnimScene, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x95531A4A20CCE7BC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_running(anim_scene: AnimScene, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xCBFC7725DE6CE2E0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_exiting_anim_scene_this_frame(anim_scene: AnimScene, entity_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(entity_name)];
    unsafe { native::invoke(0x005E6F28DD7ED58D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_playing_anim_scene(entity: Entity, anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x3AB6C7B0BB0DF4B1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn load_anim_scene(anim_scene: AnimScene) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xAF068580194D9DC7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_anim_scene_entity(anim_scene: AnimScene, entity_name: &str, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(entity_name), NativeArg::int(entity)];
    unsafe { native::invoke(0x2BF96692C67F3E53, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_anim_scene_play_list(anim_scene: AnimScene, playlist_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(playlist_name)];
    unsafe { native::invoke(0xDF7B5144E25CD3FE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_anim_scene(anim_scene: AnimScene, playback_list_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(playback_list_name)];
    unsafe { native::invoke(0x8FDF221F13537936, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn resume_anim_scene_from_last_checkpoint(anim_scene: AnimScene) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x8E1BA705F63C1925, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_scene_bool(anim_scene: AnimScene, name: &str, value: bool, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(name), NativeArg::boolean(value), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x519E96C2C68B404B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_scene_entity(anim_scene: AnimScene, entity_name: &str, entity: Entity, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(entity_name), NativeArg::int(entity), NativeArg::int(flags)];
    unsafe { native::invoke(0x8B720AD451CA2AB3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_scene_float(anim_scene: AnimScene, name: &str, value: f32, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(name), NativeArg::float(value), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x6BC5104E68CBEFE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_scene_int(anim_scene: AnimScene, name: &str, value: i32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(name), NativeArg::int(value), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x3A379D2166CF5B92, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_scene_origin(anim_scene: AnimScene, pos_x: f32, pos_y: f32, pos_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, order: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::int(order)];
    unsafe { native::invoke(0x020894BF17A02EF2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_scene_paused(anim_scene: AnimScene, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xD6824B7D24DC0CE0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_scene_playback_list(anim_scene: AnimScene, playback_list_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(playback_list_name)];
    unsafe { native::invoke(0xAB5E7CAB074D6B84, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_scene_play_list(anim_scene: AnimScene, playlist_name: &str, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(playlist_name), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x15598CFB25F3DC7E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_scene_rate(anim_scene: AnimScene, rate: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::float(rate)];
    unsafe { native::invoke(0x75820B801CFF262A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_anim_scene(anim_scene: AnimScene) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xF4D94AF761768700, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn take_ownership_of_anim_scene(anim_scene: AnimScene) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xF7A4C571E572D237, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn trigger_anim_scene_skip(anim_scene: AnimScene) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x4B85B3CF9197AEDF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn was_anim_scene_skipped(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xEF324E9550A394D5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1407_f5115_fb9583_e(anim_scene: AnimScene, p1: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(p1)];
    unsafe { native::invoke(0x1407F5115FB9583E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_ad896_bf43619551() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1AD896BF43619551, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_c5_d33_a4293_e6_dde(anim_scene: AnimScene, phase_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(phase_name)];
    unsafe { native::invoke(0x1C5D33A4293E6DDE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_db524750_dc41_ed4() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2DB524750DC41ED4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3641_fcd53_e59_b335(mgm_handle: i32, ped: Ped, secondary_voice_string: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(mgm_handle), NativeArg::int(ped), NativeArg::string(secondary_voice_string)];
    unsafe { native::invoke(0x3641FCD53E59B335, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_b393716_c3_fd8237(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3B393716C3FD8237, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_b85_b3_cf91972222(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x4B85B3CF91972222, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_d7_bfda2290_b4_e39(p0: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x5D7BFDA2290B4E39, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x61_b2_aaef645_ddaf0(mgm_event_handle: i32, p1: &str, seat_id: i32, p3: i32, p4: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(mgm_event_handle), NativeArg::string(p1), NativeArg::int(seat_id), NativeArg::int(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x61B2AAEF645DDAF0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x73616_e64696_c132_e(anim_scene: AnimScene, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x73616E64696C132E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_aae3_c1148_a09_bca(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x9AAE3C1148A09BCA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a96619_fe85159_ed2(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xA96619FE85159ED2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ae6_de22_de0_ed4554(mgm_handle: i32, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(mgm_handle), NativeArg::int(ped)];
    unsafe { native::invoke(0xAE6DE22DE0ED4554, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b1_a196_bafe650402(mgm_handle: i32, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(mgm_handle), NativeArg::int(ped)];
    unsafe { native::invoke(0xB1A196BAFE650402, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c1193521_e3_b9_fadd(entity: Entity, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC1193521E3B9FADD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cdcd7_b2_d49_aee73_a(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xCDCD7B2D49AEE73A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d70_c7_a30412_f8_fa0(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xD70C7A30412F8FA0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e12_d7_b4_b959644_cd() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE12D7B4B959644CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ea41_d44_a8_d42057_b() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEA41D44A8D42057B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_anim_scene_was_skipped(anim_scene: AnimScene) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x8A8208AE92BF87A5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_breakout_archetype(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBC781D24AA11F179, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_anim_scene(anim_dict: &str, flags: i32, playback_list_name: &str, p3: bool, p4: bool) -> Result<AnimScene, NativeError> {
    let arguments = [NativeArg::string(anim_dict), NativeArg::int(flags), NativeArg::string(playback_list_name), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x1FCA98E33C1437B3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_mgm_system(mgm_filename: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(mgm_filename)];
    unsafe { native::invoke(0xA1300DE03E5D1973, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_anim_scene(anim_scene: AnimScene) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x84EEDB2C6E650000, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_mgm_system(mgm_handle: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(mgm_handle)];
    unsafe { native::invoke(0x53CB3970BA02E3CC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_anim_scene_ownership_of_entity_exist(anim_scene: AnimScene, entity_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(entity_name)];
    unsafe { native::invoke(0x9D1ECA9337BE9FC3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_anim_scene_play_list_exist(anim_scene: AnimScene, playback_list_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(playback_list_name)];
    unsafe { native::invoke(0xA9016536015DE29D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_entity_with_id_exist_in_anim_scene(anim_scene: AnimScene, entity_id: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(entity_id)];
    unsafe { native::invoke(0x6F1F0B17109309DA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_dict(anim_scene: AnimScene) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xAE5ADA4FE3E21ADC, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_duration(anim_scene: AnimScene) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x49F1D143ADE32656, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_object(anim_scene: AnimScene, name: &str, is_network: bool) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(name), NativeArg::boolean(is_network)];
    unsafe { native::invoke(0xFB5674687A1B2814, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_ped(anim_scene: AnimScene, name: &str, is_network: bool) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(name), NativeArg::boolean(is_network)];
    unsafe { native::invoke(0xE5822422197BBBA3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_playback_list_phase_audio_load_stress(anim_scene: AnimScene, phase_name: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(phase_name)];
    unsafe { native::invoke(0x9E036D5204FFBBC8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_rate(anim_scene: AnimScene) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x43C21623E42B821B, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_time(anim_scene: AnimScene) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x61BE7D6186260002, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_scene_vehicle(anim_scene: AnimScene, name: &str, is_network: bool) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(name), NativeArg::boolean(is_network)];
    unsafe { native::invoke(0x430EE0A19BC5A287, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_entered_anim_scene(anim_scene: AnimScene, entity_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(entity_name)];
    unsafe { native::invoke(0x337F1CC8EE895601, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_aborted(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x34A0671BE613D3D0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_loading(anim_scene: AnimScene, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x59606519FF9D3EC2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_metadata_asset_in_range_loading(anim_scene: AnimScene, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xF8D1D2DAB6007EEF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_paused(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x4B4038796F0D6566, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_playback_list_phase_active(anim_scene: AnimScene, phase_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(phase_name)];
    unsafe { native::invoke(0x1F0E401031E20146, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_playback_list_phase_loaded(anim_scene: AnimScene, phase_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(phase_name)];
    unsafe { native::invoke(0x23E33CB9F4A3F547, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_playback_list_phase_loading(anim_scene: AnimScene, phase_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(phase_name)];
    unsafe { native::invoke(0x0DF57F86FE71DBE5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_skippable(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x4CDFFE3189EBDBD0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_mgm_system_loaded(mgm_filename: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(mgm_filename)];
    unsafe { native::invoke(0xFDFC14799373283F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn load_mgm_assets(mgm_filename: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(mgm_filename)];
    unsafe { native::invoke(0xB727A847862CB00A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pause_script_threads(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x37C1257849DEF24A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_anim_scene_play_list(anim_scene: AnimScene, playlist_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene), NativeArg::string(playlist_name)];
    unsafe { native::invoke(0xAE6ADA8FE7E84ACC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_photo_mode_defreeze() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x41AFA5F228B0B6B0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_photo_mode_freeze() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7C709C01D43D94CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_breakout_archetype(ped: Ped, archetype: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(archetype)];
    unsafe { native::invoke(0x99B2A2E3655DEAF1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mgm_event(mgm_event_handle: i32, p1: &str, seat_id: NativeArg, p3: i32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(mgm_event_handle), NativeArg::string(p1), seat_id, NativeArg::int(p3), NativeArg::float(p4)];
    unsafe { native::invoke(0x07706C4CC9C6CC9E, &arguments) }.map(|_| ())
}

// ============================================================================
// ATTRIBUTE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_attribute_points(ped: Ped, attribute_index: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index), NativeArg::int(p2)];
    unsafe { native::invoke(0x75415EE0CB583760, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_attribute_overpower(ped: Ped, attribute_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0xF8DAC3D85636C241, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_attribute_overpower(ped: Ped, attribute_index: i32, value: f32, make_sound: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index), NativeArg::float(value), NativeArg::boolean(make_sound)];
    unsafe { native::invoke(0xF6A7C08DF2E28B28, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_attribute_base_rank(ped: Ped, attribute_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0x147149F2E909323C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_attribute_bonus_rank(ped: Ped, core_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(core_index)];
    unsafe { native::invoke(0x0EFA71F4B4330E04, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_attribute_points(ped: Ped, attribute_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0x219DA04BAA9CB065, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_attribute_rank(ped: Ped, attribute_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0xA4C8E23E29040DE0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_default_attribute_points_needed_for_rank(model_hash: Hash, attribute_index: i32, rank: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::int(attribute_index), NativeArg::int(rank)];
    unsafe { native::invoke(0x94A7F191DB49A44D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_default_attribute_rank(model_hash: Hash, attribute_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0x958DD43D41F89A47, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_default_max_attribute_rank(model_hash: Hash, attribute_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0x7C059C55AD940CB4, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_attribute_points(ped: Ped, attribute_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0x223BF310F854871C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_attribute_rank(ped: Ped, attribute_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0x704674A0535A471D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_attribute_base_rank(ped: Ped, attribute_index: i32, new_value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index), NativeArg::int(new_value)];
    unsafe { native::invoke(0x5DA12E025D47D4E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_attribute_bonus_rank(ped: Ped, attribute_index: i32, new_value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index), NativeArg::int(new_value)];
    unsafe { native::invoke(0x920F9488BD115EFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_attribute_points(ped: Ped, attribute_index: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index), NativeArg::int(p2)];
    unsafe { native::invoke(0x09A59688C26D88DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_item_preview() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD962F8579D702DB5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_attribute_core_overpower(ped: Ped, core_index: i32, value: f32, make_sound: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(core_index), NativeArg::float(value), NativeArg::boolean(make_sound)];
    unsafe { native::invoke(0x4AF5A4C7B9157D14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_attribute_core_overpower_seconds_left(ped: Ped, core_index: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(core_index)];
    unsafe { native::invoke(0xB429F58803D285B1, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_attribute_core_value(ped: Ped, core_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(core_index)];
    unsafe { native::invoke(0x36731AC041289BB1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_attribute_overpower_seconds_left(ped: Ped, attribute_index: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0x4C9F782180712742, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_attribute_core_overpowered(ped: Ped, core_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(core_index)];
    unsafe { native::invoke(0x200373A8DF081F22, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_attribute_overpowered(ped: Ped, attribute_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0x103C2F885ABEB00B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_attribute_core_value(ped: Ped, core_index: i32, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(core_index), NativeArg::int(value)];
    unsafe { native::invoke(0xC6258F41D86676E0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_status_effect_core_icon(status_effect_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(status_effect_type)];
    unsafe { native::invoke(0xA4D3A1C008F250DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_status_effect_periodic_icon(status_effect_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(status_effect_type)];
    unsafe { native::invoke(0xFB6E111908502871, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_item_preview(p0: NativeArg, p1: i32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(p1)];
    unsafe { native::invoke(0x7E2C766ADB2C5F1A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_status_effect_periodic_icon(status_effect_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(status_effect_type)];
    unsafe { native::invoke(0x3FC4C027FD0936F4, &arguments) }.map(|_| ())
}

// ============================================================================
// AUDIO
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_entity_to_audio_mix_group(entity: Entity, group_name: &str, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(group_name), NativeArg::float(p2)];
    unsafe { native::invoke(0x153973AB99FE8980, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ped_to_conversation(convo_root: &str, ped: Ped, character_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(convo_root), NativeArg::int(ped), NativeArg::string(character_name)];
    unsafe { native::invoke(0x95D9F4BC443956E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn audio_is_music_playing() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x845FFC3A4FEEFA3E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn audio_trigger_explosion(name: &str, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x374F0E716BFCDE82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cancel_music_event(event_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(event_name)];
    unsafe { native::invoke(0x5B17A90291133DA5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ambient_zone_list_state(ambient_zone: &str, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(ambient_zone), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x120C48C614909FA4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ambient_zone_state(zone_name: &str, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(zone_name), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x218DD44AAAC964FF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_conversation_history() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x33D51F801CB16E4F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_new_scripted_conversation(convo_root: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(convo_root)];
    unsafe { native::invoke(0xD2C91A0B572AAE56, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_ped_pain_audio(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xA9A41C1E940FB0E8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_context_exist_for_this_ped(ped: Ped, speech_name: &str, unk: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(speech_name), NativeArg::boolean(unk)];
    unsafe { native::invoke(0x49B99BF3FDA89A7A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_ped_panic_walla() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x062D5EAD4DA2FA6A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_use_audio_game_object(vehicle: Vehicle, audio_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::string(audio_name)];
    unsafe { native::invoke(0x4F0C413926060B38, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_scripted_conversation_line(p0: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x480357EE890C295A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_music_playtime() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE7A0D23DC414507B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_sound_id() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x430386FE9BF80B45, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ambient_speech_disabled(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x932C2D096A2C3FFF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ambient_speech_playing(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9072C8B49907BFAD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_animal_vocalization_playing(ped_handle: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped_handle)];
    unsafe { native::invoke(0xC265DF9FB44A9FBD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_speech_playing(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x729072355FA39EC9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_audio_scene_active(scene: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(scene)];
    unsafe { native::invoke(0xB65B60556E2A9225, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_horn_active(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x9D6BFC12B05C6121, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_current_conversation(p0: &str, ped: Ped, p2: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::int(ped), p2];
    unsafe { native::invoke(0x049E937F18F4020C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scripted_conversation_loaded(convo_root: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(convo_root)];
    unsafe { native::invoke(0xDF0D54BE7A776737, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scripted_conversation_playing(p0: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x1ECC76792F661CF5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scripted_speech_playing(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCC9AA18DCC7084F4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_stream_playing(stream_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(stream_id)];
    unsafe { native::invoke(0xD11FA52EB849D978, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn load_stream(stream_name: &str, sound_set: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(stream_name), NativeArg::string(sound_set)];
    unsafe { native::invoke(0x1F1F957154EC51DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pause_scripted_conversation(p0: &str, p1: bool, p2: bool, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x8530AD776CD72B12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_ambient_speech_from_position_native(x: f32, y: f32, z: f32, params: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(params)];
    unsafe { native::invoke(0xED640017ED337E45, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_animal_vocalization(ped: Ped, vocalization_name: &str, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(vocalization_name), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xEE066C7006C49C0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_end_credits_music(play: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(play)];
    unsafe { native::invoke(0xCD536C4D33DCC900, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_pain(ped: Ped, pain_id: i32, p2: f32, p3: bool, is_network: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(pain_id), NativeArg::float(p2), NativeArg::boolean(p3), NativeArg::boolean(is_network)];
    unsafe { native::invoke(0xBC9AE166038A5CEC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_ped_ambient_speech_native(speaker: Ped, params: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(speaker), NativeArg::in_out(params)];
    unsafe { native::invoke(0x8E04FEDD28D42462, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_sound(audio_name: &str, audio_ref: &str, p2: bool, p3: NativeArg, p4: bool, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(audio_name), NativeArg::string(audio_ref), NativeArg::boolean(p2), p3, NativeArg::boolean(p4), p5];
    unsafe { native::invoke(0x7FF4944CC209192D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_sound_from_entity(audio_name: &str, entity: Entity, audio_ref: &str, is_network: bool, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(audio_name), NativeArg::int(entity), NativeArg::string(audio_ref), NativeArg::boolean(is_network), p4, p5];
    unsafe { native::invoke(0x6FB1DA3CA9DA7D90, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_sound_frontend(audio_name: &str, audio_ref: &str, p2: bool, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(audio_name), NativeArg::string(audio_ref), NativeArg::boolean(p2), p3];
    unsafe { native::invoke(0x67C540AA08E4A6F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_stream_from_ped(ped: Ped, stream_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(stream_id)];
    unsafe { native::invoke(0x89049DD63C08B5D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_stream_from_position(x: f32, y: f32, z: f32, stream_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(stream_id)];
    unsafe { native::invoke(0x21442F412E8DE56B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_stream_frontend(stream_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(stream_id)];
    unsafe { native::invoke(0x58FCE43488F9F5F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn preload_script_conversation(convo_root: &str, p1: bool, p2: bool, clone: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(convo_root), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(clone)];
    unsafe { native::invoke(0x3B3CAD6166916D87, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn prepare_music_event(event_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(event_name)];
    unsafe { native::invoke(0x1E5185B72EF5158A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn prepare_sound(sound_name: &str, soundset_name: &str, sound_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(sound_name), NativeArg::string(soundset_name), NativeArg::int(sound_id)];
    unsafe { native::invoke(0xE368E8422C860BA7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn prepare_soundset(soundset_name: &str, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(soundset_name), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xD9130842D7226045, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn prepare_sound_with_entity(sound_name: &str, entity: Entity, soundset_name: &str, sound_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(sound_name), NativeArg::int(entity), NativeArg::string(soundset_name), NativeArg::int(sound_id)];
    unsafe { native::invoke(0x4AD019591E94C064, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_script_with_audio(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xC6ED9D5092438D91, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_named_script_audio_bank(audio_bank: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(audio_bank)];
    unsafe { native::invoke(0x77ED170667F50170, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_script_audio_bank() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7A2D8AD0A9EB9C3F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_sound_id(sound_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(sound_id)];
    unsafe { native::invoke(0x353FC880830B88FA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_entity_from_audio_mix_group(entity: Entity, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(p1)];
    unsafe { native::invoke(0x18EB48CFC41F2EA0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_portal_settings_override(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0xB4BBFD9CD8B3922B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_script_audio_bank(audio_bank: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(audio_bank)];
    unsafe { native::invoke(0x2F844A8B08D76685, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn restart_scripted_conversation(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x9AEB285D1818C9AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_voice_name(ped: Ped, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(name)];
    unsafe { native::invoke(0x6C8065A3B780185B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_zone_list_state(ambient_zone: &str, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(ambient_zone), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x9748FA4DE50CCE3E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_zone_list_state_persistent(ambient_zone: &str, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(ambient_zone), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xF3638DAE8C4045E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_zone_state(zone_name: &str, is_enabled: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(zone_name), NativeArg::boolean(is_enabled), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xBDA07E5950085E46, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_zone_state_persistent(ambient_zone: &str, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(ambient_zone), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x1D6650420CEC9D3B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_animal_mood(animal: Ped, mood: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::int(mood)];
    unsafe { native::invoke(0xCC97B29285B1DC3B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_audio_flag(flag_name: &str, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(flag_name), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xB9EFD5C25018725A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_audio_online_transition_stage(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x9B1FC259187C97C0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_audio_scene_variable(scene: &str, variable: &str, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(scene), NativeArg::string(variable), NativeArg::float(value)];
    unsafe { native::invoke(0xEF21A9EF089A2668, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_audio_vehicle_priority(vehicle: Vehicle, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), p1];
    unsafe { native::invoke(0xE5564483E407F914, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gps_active(active: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(active)];
    unsafe { native::invoke(0x3BD3F52BA9B1E4E8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_horn_enabled(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x76D683C108594D0E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_is_scripted_speech_disabled(ped: Ped, disabled: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(disabled)];
    unsafe { native::invoke(0xB2DE3AEBE31150E2, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_interior_walla_density(p0: f32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x8BF907833BE275DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_is_drunk(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x95D2D383D5396B8A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_walla_density(p0: f32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x149AEE66F0CB3A99, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_portal_settings_override(p0: &str, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0x044DBAD7A7FA2BE5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_static_emitter_enabled(emitter_name: &str, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(emitter_name), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x399D2D3B33F1B8EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn skip_to_next_scripted_conversation_line(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x9663FE6B7A61EB00, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_audio_scene(scene: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(scene)];
    unsafe { native::invoke(0x013A80FC08F6E4F2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_preloaded_conversation(convo_root: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(convo_root)];
    unsafe { native::invoke(0x23641AFE870AF385, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_script_conversation(convo_root: &str, p1: bool, p2: bool, clone: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(convo_root), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(clone)];
    unsafe { native::invoke(0x6B17C62C9635D2DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_audio_scene(scene: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(scene)];
    unsafe { native::invoke(0xDFE8422B3B94E688, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_audio_scenes() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBAC7FC81A75EC1A1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_current_playing_ambient_speech(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0xB8BEC0CA6F0EDB0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_current_playing_speech(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0x79D2F0E66F81D90D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_ped_speaking(ped: Ped, shaking: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(shaking)];
    unsafe { native::invoke(0x9D64D7405520E3D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_scripted_conversation(p0: &str, p1: bool, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xD79DEEFB53455EBA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_stream(stream_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(stream_id)];
    unsafe { native::invoke(0xA4718A1419D18151, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn trigger_music_event(event_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(event_name)];
    unsafe { native::invoke(0x706D57B0F50DA710, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unregister_script_with_audio() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA8638BE228D4751A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn use_footstep_script_sweeteners(ped: Ped, p1: bool, hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::hash(hash)];
    unsafe { native::invoke(0xBF4DC1784BE94DFA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x017492_b2201_e3428(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x017492B2201E3428, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x018_abe833_ca64_d2_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x018ABE833CA64D2A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x06_c5_df5_ee444_bc6_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x06C5DF5EE444BC6B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x078_f77_fd1_a43_eab3(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x078F77FD1A43EAB3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_cb3_d1919_e8_d7_cba(convo_root: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(convo_root)];
    unsafe { native::invoke(0x0CB3D1919E8D7CBA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_d7_fd6_a55_fd63_aef(speech_event_type: i32, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(speech_event_type), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x0D7FD6A55FD63AEF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_faf7171_bf613_b80(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0FAF7171BF613B80, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x131_ec9247_e7_a2903(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x131EC9247E7A2903, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x138_adb94_f8_b90616() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x138ADB94F8B90616, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x139_a4_b9_df2_d26_cbf(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x139A4B9DF2D26CBF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x152_ed1_b56_e8_f1_f50(p0: &str, current_scripted_convo_line: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::int(current_scripted_convo_line)];
    unsafe { native::invoke(0x152ED1B56E8F1F50, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_e6_f9_a9_fe1_a99_f36(aud_speech_event: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(aud_speech_event)];
    unsafe { native::invoke(0x1E6F9A9FE1A99F36, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x254_b0241_e964_b450(p0: &str, current_scripted_convo_line: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::int(current_scripted_convo_line)];
    unsafe { native::invoke(0x254B0241E964B450, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x259_acc5_b52_a2_b2_d9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x259ACC5B52A2B2D9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2651_ddc0_ea269073(rope_id: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::float(p1)];
    unsafe { native::invoke(0x2651DDC0EA269073, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x295859_eb18_f48_d82(p0: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x295859EB18F48D82, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_b101_ad9_f651243_a() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2B101AD9F651243A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_b9_c37_c01_bf25_edb(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2B9C37C01BF25EDB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_fff4_a78384_affdf(entity: Entity) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x2FFF4A78384AFFDF, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x341_cdd17_efc2472_e(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x341CDD17EFC2472E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x35_b8_c070_e0_c16_e2_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x35B8C070E0C16E2F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x380_a2_e353_ad30917(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x380A2E353AD30917, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_a00_d87_b20_a2_a5_e4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3A00D87B20A2A5E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_a3_be6_b920525237(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3A3BE6B920525237, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_d0_bbccf401_b5_fdb() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3D0BBCCF401B5FDB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_e93_dddcbb6111_e4(p0: &str, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x3E93DDDCBB6111E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_e98_ac9_d8_c56_c62_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3E98AC9D8C56C62C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x40_ca665_ab9_d8_d505(convo_root: &str, single_line_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(convo_root), NativeArg::int(single_line_index)];
    unsafe { native::invoke(0x40CA665AB9D8D505, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x43037_abfe214_a851() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x43037ABFE214A851, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x448_f2647_dd6_f2_e27(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x448F2647DD6F2E27, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x44_a5_eef54_f62_e823(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x44A5EEF54F62E823, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_a98_e228_a936_dbcc(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4A98E228A936DBCC, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_be3_ec91_c01_f0_fe8() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4BE3EC91C01F0FE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x569_abc36_e28_ddeaa() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x569ABC36E28DDEAA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x580_d71_dfe0088_e34(audio_name: &str, audio_ref: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(audio_name), NativeArg::string(audio_ref)];
    unsafe { native::invoke(0x580D71DFE0088E34, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_a13586_a9447931_f(p0: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x5A13586A9447931F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_ae0_cb5_f35_f034_fd(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0x5AE0CB5F35F034FD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_bc885_ebd75_faa7_d(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x5BC885EBD75FAA7D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_e3_ccf03995388_b5(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x5E3CCF03995388B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x62377977_e4_f08668(entity: Entity) -> Result<AnimScene, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x62377977E4F08668, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x64_b956_f4_e761_df5_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x64B956F4E761DF5C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x660_a8_f876_df1_d4_f8(speech_event_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(speech_event_type)];
    unsafe { native::invoke(0x660A8F876DF1D4F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6652_b0_c8_f3_d414_d0(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6652B0C8F3D414D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_ab944_df68_b512_d3(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6AB944DF68B512D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_b7_a88_a61_b41_e589(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6B7A88A61B41E589, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_da15746_d5_cc1_a92(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x6DA15746D5CC1A92, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7455_cd705_f7_e933_e() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7455CD705F7E933E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7678_fe0455_ed1145(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x7678FE0455ED1145, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x79_f9_c57_b8_d0_dfe90(convo_root: &str, anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(convo_root), NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x79F9C57B8D0DFE90, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_e176_c676_f8652_a9(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7E176C676F8652A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x821_c32_c728_b24477(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x821C32C728B24477, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x839_c9_f124_be74_d94(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x839C9F124BE74D94, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x847748_ae5_d7_b1071(p0: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x847748AE5D7B1071, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x864_a842_b86993851(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x864A842B86993851, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x886657_c5_b3_d8_ede3(entity: Entity) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x886657C5B3D8EDE3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_d29_fdf565_ded9_ae(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x8D29FDF565DED9AE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_e901_b65206_c2_d3_e(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8E901B65206C2D3E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x935_dbd96_d4_a3_da1_f(p0: &str, current_scripted_convo_line: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::int(current_scripted_convo_line)];
    unsafe { native::invoke(0x935DBD96D4A3DA1F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_d6_dec9791_a4_e501(ped: Ped, speech_name: &str, p2: bool, p3: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(speech_name), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x9D6DEC9791A4E501, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_eb779765_e68_c52_e(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9EB779765E68C52E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2323_a2_eae32_a290(listening_to_ped: Ped, ped: Ped, listener_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(listening_to_ped), NativeArg::int(ped), NativeArg::string(listener_name)];
    unsafe { native::invoke(0xA2323A2EAE32A290, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2_b851605748_ad0_e() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA2B851605748AD0E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a6847_bba4_fcdd13_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA6847BBA4FCDD13F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a6_a3_a3_f96_b8_b030_e() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA6A3A3F96B8B030E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_abdb4863_d3_d72021(entity: Entity, p1: NativeArg, p2: NativeArg, p3: f32, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), p1, p2, NativeArg::float(p3), p4];
    unsafe { native::invoke(0xABDB4863D3D72021, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b93_a769_b8_b726950(ped: Ped, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0xB93A769B8B726950, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bc07_ca8_fd710_e7_fd(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xBC07CA8FD710E7FD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_be28_db99556_ff8_d9(entity: Entity) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xBE28DB99556FF8D9, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c369_e2234_e34_a0_ca(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC369E2234E34A0CA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c4_cfce4_c656_ef480(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC4CFCE4C656EF480, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c68_c02_de259_c927_c(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC68C02DE259C927C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c886_cd666_add42_e1(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC886CD666ADD42E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cbf2_bebb468_a34_f3(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCBF2BEBB468A34F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cfad2_c8_cd1054523(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xCFAD2C8CD1054523, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d05_a460328560477(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD05A460328560477, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d0730_c1_fa40348_d9(convo_root: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(convo_root)];
    unsafe { native::invoke(0xD0730C1FA40348D9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d47_d47_efbf103_fb8(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD47D47EFBF103FB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d733528_b6_c35647_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD733528B6C35647A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dad6_cd07_caa4_f382() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDAD6CD07CAA4F382, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc2_f83_a0612_ca34_d(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDC2F83A0612CA34D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc93_f0948_f2_c28_f4(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDC93F0948F2C28F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_df947_fe0_d551684_e(ped: Ped, p1: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(p1)];
    unsafe { native::invoke(0xDF947FE0D551684E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e600_f61_f54_a444_a6() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE600F61F54A444A6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e7_e6_cb8_b713_ed190() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE7E6CB8B713ED190, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e891504_b2_f0_e2_dba(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE891504B2F0E2DBA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e9694_b2_d6_cb87_b06(entity: Entity, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), p1];
    unsafe { native::invoke(0xE9694B2D6CB87B06, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ea546_c31_fd45_f8_cd(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xEA546C31FD45F8CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eb4_d592620_b8_c209(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xEB4D592620B8C209, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f092_b6030_d6_fd49_c(rope_id: i32, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::string(name)];
    unsafe { native::invoke(0xF092B6030D6FD49C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f0_ee69_f500952_fa5(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF0EE69F500952FA5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f232_c2_c546_ac16_d0(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0xF232C2C546AC16D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f336_e9_f989_b3518_f(p0: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0xF336E9F989B3518F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f64034_d533_ce8_aac(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xF64034D533CE8AAC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fcdec42_b1_c78_b7_f8(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFCDEC42B1C78B7F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fd461_d0_aba5559_b1(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFD461D0ABA5559B1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fe5_c6177064_bd390(p0: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xFE5C6177064BD390, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ffe9_c53_deea3_db0_b(p0: NativeArg, p1: NativeArg, x: f32, y: f32, z: f32, is_srl_loaded: bool, p6: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(is_srl_loaded), p6];
    unsafe { native::invoke(0xFFE9C53DEEA3DB0B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn block_speech_context(context: &str, block: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(context), NativeArg::boolean(block)];
    unsafe { native::invoke(0x6378A235374B852F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_conversation_history_for_scripted_conversation(convo_root: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(convo_root)];
    unsafe { native::invoke(0xEF51242E35242B47, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_new_scripted_ped_ambient_speech(speaker: Ped, params: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(speaker), NativeArg::in_out(params)];
    unsafe { native::invoke(0x72E4D1C4639BC465, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_audio_mix_group(entity: Entity) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x8B25A18E390F75BF, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_last_played_speech_for_ped(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6BFFB7C276866996, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_loaded_stream_id_from_creation(stream_name: &str, sound_set: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(stream_name), NativeArg::string(sound_set)];
    unsafe { native::invoke(0x0556C784FA056628, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_song_index_host(ped: Ped) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2DBBF0C5E19383EE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_sound_audio_name_finished(audio_name: &str, soundset_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(audio_name), NativeArg::string(soundset_name)];
    unsafe { native::invoke(0x714A0EA7DE1167BE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_sound_id_finished(sound_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(sound_id)];
    unsafe { native::invoke(0x84848E1C0FC67DBB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_conversation_playing(p0: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xA2CAC9DEF0195E6F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_any_conversation(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x54B187F111D9C6F8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scripted_audio_custom(item: Hash, sound_set: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::hash(sound_set)];
    unsafe { native::invoke(0x6DF942C4179BE5AB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scripted_conversation_created(convo_root: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(convo_root)];
    unsafe { native::invoke(0xD89504D9D7D5057D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scripted_conversion_ongoing(p0: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0xF01C570E0A0A1E67, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_animal_vocalization_pheromone_vial_response(ped: Ped, p1: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x0E53530D9B2DB01D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_sound_from_entity_with_set(sound_id: i32, sound_name: &str, entity: Entity, soundset_name: &str, p4: bool, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(sound_id), NativeArg::string(sound_name), NativeArg::int(entity), NativeArg::string(soundset_name), NativeArg::boolean(p4), p5];
    unsafe { native::invoke(0xF1C5310FEAA36B48, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_sound_from_item(item: Hash, sound_set: Hash, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::hash(sound_set), p2];
    unsafe { native::invoke(0xE8EAFF7B41EDD291, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_sound_from_position(audio_name: &str, x: f32, y: f32, z: f32, audio_ref: &str, is_network: bool, p6: NativeArg, p7: bool, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(audio_name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::string(audio_ref), NativeArg::boolean(is_network), p6, NativeArg::boolean(p7), p8];
    unsafe { native::invoke(0xCCE219C922737BFA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_sound_from_position_with_id(sound_id: i32, sound_name: &str, x: f32, y: f32, z: f32, soundset_name: &str, p6: bool, p7: i32, p8: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(sound_id), NativeArg::string(sound_name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::string(soundset_name), NativeArg::boolean(p6), NativeArg::int(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0xDCF5BA95BBF0FABA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_sound_from_scripted_ped_ambient_speech(scripted_speech: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(scripted_speech)];
    unsafe { native::invoke(0xB18FEC133C7C6C69, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_sound_frontend_with_sound_id(sound_id: i32, name: &str, sound_set: &str, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(sound_id), NativeArg::string(name), NativeArg::string(sound_set), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xCE5D0FFE83939AF1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_shard_sounds(sound_name: &str, soundset_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(sound_name), NativeArg::string(soundset_name)];
    unsafe { native::invoke(0x9D746964E0CF2C5F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_soundset(soundset_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(soundset_name)];
    unsafe { native::invoke(0x531A78D6BF27014B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_zone_position(ambient_zone: &str, x: f32, y: f32, z: f32, heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(ambient_zone), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading)];
    unsafe { native::invoke(0x3743CE6948194349, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_audio_sceneset(audio_name: &str, sceneset: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(audio_name), NativeArg::string(sceneset)];
    unsafe { native::invoke(0xAC84686C06184B0D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_sound_relationship_on_ped(ped: Ped, p1: &str, p2: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0x2E31ACA7477CF00F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_variable_on_sound_with_id(sound_id: i32, variable_name: &str, variable_value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(sound_id), NativeArg::string(variable_name), NativeArg::float(variable_value)];
    unsafe { native::invoke(0x503703EC1781B7D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_variable_on_sound_with_name(variable_name: &str, variable_value: f32, audio_name: &str, audio_ref: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(variable_name), NativeArg::float(variable_value), NativeArg::string(audio_name), NativeArg::string(audio_ref)];
    unsafe { native::invoke(0x9821B68CD3E05F2B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vofx_ped_voice(ped: Ped, voice: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(voice)];
    unsafe { native::invoke(0x2703EFB583F0949A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_whistle_config_for_ped(ped: Ped, whistle_config: &str, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(whistle_config), NativeArg::float(value)];
    unsafe { native::invoke(0x9963681A8BC69BF3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_audio_sceneset(audio_name: &str, sceneset: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(audio_name), NativeArg::string(sceneset)];
    unsafe { native::invoke(0x6339C1EA3979B5F7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_all_scripted_audio_sounds() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2E399EAFBEEA74D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_all_scripted_conversions(p0: bool, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x36559148B78853B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_audio_sceneset(sceneset: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(sceneset)];
    unsafe { native::invoke(0x9428447DED71FC7E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_sound_with_id(sound_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(sound_id)];
    unsafe { native::invoke(0x3210BCB36AF7621B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_sound_with_name(audio_name: &str, audio_ref: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(audio_name), NativeArg::string(audio_ref)];
    unsafe { native::invoke(0x0F2A2175734926D8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn trigger_music_event_with_hash(event_name: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(event_name)];
    unsafe { native::invoke(0x05D6195FB4D428F4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unload_speech_context(speech_context: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(speech_context)];
    unsafe { native::invoke(0x87E6302FC61208CC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_sound_position(sound_id: i32, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(sound_id), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x0286617C8FC50A53, &arguments) }.map(|_| ())
}

// ============================================================================
// BOUNTY
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_get_bounty_on_player(gamer_handle: &mut Any, bounty_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::in_out(bounty_data)];
    unsafe { native::invoke(0x4EF23E04A0C8FF51, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_get_cooldown_collection(p0: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0x8FAF4D262FABA99C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_get_legendary_target(p0: NativeArg, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [p0, NativeArg::in_out(p1)];
    unsafe { native::invoke(0x85E4D7B225A30ED1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_get_wanted_poster_slot(p0: Hash, p1: Hash, p2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::hash(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0xB395A44A0C7CA615, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_begin_legendary_mission(out_rpc_guid: &mut Any, p1: i32, p2: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xFC81D7C7A151CFAA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_begin_legendary_mission_for_posse(out_rpc_guid: &mut Any, p1: i32, p2: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x48E4E23F1739E197, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x27_d3_a0_e1_fe090_a43(p0: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0x27D3A0E1FE090A43, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x81847_c2134039_bdc(p0: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0x81847C2134039BDC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x86_ec5_f83867_c4_b70(p0: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0x86EC5F83867C4B70, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d6_a67_e2_ff373_d0_e3(p0: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xD6A67E2FF373D0E3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f8_bcc5_eca33_ac9_c1() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF8BCC5ECA33AC9C1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_cancel_legendary_mission() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2BA1BCC99826CDA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_cancel_wanted_poster() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6A9DF0FCD0C87FF9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_clear_being_bounty_hunter() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA59D1997ECD99F0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_clear_being_target() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x932DB3C05A7465D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_is_request_pending(rpc_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(rpc_guid)];
    unsafe { native::invoke(0x03B61CD51097DE60, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_become_target_of_character_bounty_hunt(out_rpc_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid)];
    unsafe { native::invoke(0xB096547D61868254, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_begin_wanted_poster(out_rpc_guid: &mut Any, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::int(p1)];
    unsafe { native::invoke(0xFFA13742E43507E3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_bribe_jail_guard(out_rpc_guid: &mut Any, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::int(p1)];
    unsafe { native::invoke(0x28717806D3BDD0D0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_claim_character_bounty(out_rpc_guid: &mut Any, p1: i32, p2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::int(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0xA9C3B0F746375162, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_complete_legendary_mission(out_rpc_guid: &mut Any, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xA7309AC0DCF6D950, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_complete_split_wanted_poster(out_rpc_guid: &mut Any, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xFBD137BF0EC50FC9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_complete_wanted_poster(out_rpc_guid: &mut Any, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x727AB6F008BB9F29, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_escaped_character_bounty_hunt(out_rpc_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid)];
    unsafe { native::invoke(0x12E981D53B07BF48, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_pay_off_bounty(out_rpc_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid)];
    unsafe { native::invoke(0x537CE992BD2D7BCB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_pay_off_bounty_ex(out_rpc_guid: &mut Any, p1: Hash, cost_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::hash(p1), NativeArg::hash(cost_type)];
    unsafe { native::invoke(0x587BCEC31D64F382, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_posse_leader_claim_character_bounty(out_rpc_guid: &mut Any, p1: i32, p2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::int(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x5B53CA0E2AC3FF45, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_posse_leader_escaped_character_bounty_hunt(out_rpc_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid)];
    unsafe { native::invoke(0x2D874BA20E8E1F20, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_posse_member_claim_character_bounty_share(out_rpc_guid: &mut Any, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x22D3A61CE053270C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_posse_member_escaped_character_bounty_hunt(out_rpc_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid)];
    unsafe { native::invoke(0x8521C2E235558278, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_self_report_crime(out_rpc_guid: &mut Any, crime_type: Hash, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid), NativeArg::hash(crime_type), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x188B748861B5BA17, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_self_report_killed_by_bounty_hunter(out_rpc_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid)];
    unsafe { native::invoke(0xB462D69D406A2602, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bounty_request_served_full_jail_sentence(out_rpc_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_rpc_guid)];
    unsafe { native::invoke(0x3F73AED12A5EF0FF, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// BRAIN
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_script_brain_set(brain_set: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(brain_set)];
    unsafe { native::invoke(0x3F44EA613A5B2676, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_script_brain_set(brain_set: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(brain_set)];
    unsafe { native::invoke(0x1CF6E5C6750EADBD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reactivate_all_object_brains_that_are_waiting_till_out_of_range() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA32B0B05EFF75730, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reactivate_named_object_brains_waiting_till_out_of_range(script_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(script_name)];
    unsafe { native::invoke(0x74C333E34DF74E8A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_object_script_brain(script_name: &str, model_hash: Hash, p2: i32, activation_range: f32, p4: i32, p5: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(script_name), NativeArg::hash(model_hash), NativeArg::int(p2), NativeArg::float(activation_range), NativeArg::int(p4), NativeArg::int(p5)];
    unsafe { native::invoke(0x16AF9B4EEAC3B305, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_aa5_ea1_edfb25786(flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(flag)];
    unsafe { native::invoke(0x4AA5EA1EDFB25786, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a6_ac35_db4_a7957_a8(flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(flag)];
    unsafe { native::invoke(0xA6AC35DB4A7957A8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_script_brain_entity() -> Result<Entity, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6818D1A194E29983, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_script_brain_entity(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x38F1E09224EECA09, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_preloaded_script_brain(entity: Entity, script_name: &str, script_stack_size: i32, p3: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(script_name), NativeArg::int(script_stack_size), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x4E4507CC5E4DB869, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_script_brain(entity: Entity, script_name: &str, p2: i32, p3: &mut Any, p4: i32, p5: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(script_name), NativeArg::int(p2), NativeArg::in_out(p3), NativeArg::int(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x6F62FAE266DCFC81, &arguments) }.map(|result| result.as_i32())
}

// ============================================================================
// BUILTIN
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ceil(value: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0x11E019C8F43ACC8A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cos(value: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0xD0FFB162F40A139C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn floor(value: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0xF34EE736CF047844, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn log10(value: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0xE816E655DE37FE20, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pow(base: f32, exponent: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(base), NativeArg::float(exponent)];
    unsafe { native::invoke(0xE3621CC40F31FE2E, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn round(value: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0xF2DB717A73826179, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn settimera(value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(value)];
    unsafe { native::invoke(0xC1B1E9A034A63A62, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn settimerb(value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(value)];
    unsafe { native::invoke(0x5AE11BC36633DE4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_this_thread_priority(priority: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(priority)];
    unsafe { native::invoke(0x42B65DEEF2EDF2A1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn shift_left(value: i32, bit_shift: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(value), NativeArg::int(bit_shift)];
    unsafe { native::invoke(0xEDD95A39E5544DE8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn shift_right(value: i32, bit_shift: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(value), NativeArg::int(bit_shift)];
    unsafe { native::invoke(0x97EF1E5BCE9DC075, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sin(value: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0x0BADBFA3B172435F, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sqrt(value: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0x71D93B57D07F9804, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn timera() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x83666F9FB8FEBD4B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn timerb() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC9D9444186B5A374, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn timestep() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0000000050597EE2, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn to_float(value: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(value)];
    unsafe { native::invoke(0xBBDA792448DB5A89, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn vdist(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2)];
    unsafe { native::invoke(0x2A488C176D52CCA5, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn vdist2(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2)];
    unsafe { native::invoke(0xB7A628320EFF8E47, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn vmag(x: f32, y: f32, z: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x652D2EEEF1D3E62C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn vmag2(x: f32, y: f32, z: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xA8CEACB4F35AE058, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn wait(ms: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ms)];
    unsafe { native::invoke(0x4EDE34FBADD967A6, &arguments) }.map(|_| ())
}

// ============================================================================
// CAM
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_cam_spline_node(camera: Cam, x: f32, y: f32, z: f32, x_rot: f32, y_rot: f32, z_rot: f32, length: i32, p8: i32, p9: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(camera), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::int(length), NativeArg::int(p8), NativeArg::int(p9)];
    unsafe { native::invoke(0xF1F57F9D230F9CD1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn allow_motion_blur_decay(cam: Cam, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x42ED56B02E05D109, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_cam_to_entity(cam: Cam, entity: Entity, x_offset: f32, y_offset: f32, z_offset: f32, is_relative: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::int(entity), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::boolean(is_relative)];
    unsafe { native::invoke(0xFDC0DF7F6FB0A592, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_cam_to_ped_bone(cam: Cam, ped: Ped, bone_index: i32, x: f32, y: f32, z: f32, heading: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::int(ped), NativeArg::int(bone_index), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(heading)];
    unsafe { native::invoke(0xDFC1E4A44C0324CA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cinematic_location_override_target_entity_this_update(name: &str, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::int(entity)];
    unsafe { native::invoke(0x0B0F914459731F60, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cinematic_location_stop_scripted_shot_event(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x6D4D25C2137FF511, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cinematic_location_trigger_scripted_shot_event(dictionary: &str, shot_name: &str, camera_name: &str, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(dictionary), NativeArg::string(shot_name), NativeArg::string(camera_name), p3];
    unsafe { native::invoke(0x02389579A53C3276, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_cam(cam_name: &str, p1: bool) -> Result<Cam, NativeError> {
    let arguments = [NativeArg::string(cam_name), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xE72CDBA7F0A02DD6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_camera(cam_hash: Hash, p1: bool) -> Result<Cam, NativeError> {
    let arguments = [NativeArg::hash(cam_hash), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x57CDF879EA466C46, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_camera_with_params(cam_hash: Hash, pos_x: f32, pos_y: f32, pos_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, fov: f32, p8: bool, p9: NativeArg) -> Result<Cam, NativeError> {
    let arguments = [NativeArg::hash(cam_hash), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(fov), NativeArg::boolean(p8), p9];
    unsafe { native::invoke(0x98B99B9F27E2D60B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_cam_with_params(cam_name: &str, pos_x: f32, pos_y: f32, pos_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, fov: f32, p8: bool, p9: i32) -> Result<Cam, NativeError> {
    let arguments = [NativeArg::string(cam_name), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(fov), NativeArg::boolean(p8), NativeArg::int(p9)];
    unsafe { native::invoke(0x40C23491CE83708E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn destroy_all_cams(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x163600D6E136C9F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn destroy_cam(cam: Cam, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x4E67E0B6D7FD5145, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_cam(cam: Cam) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0x05B41DDBEB559556, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_cam_collision_for_object(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x7E3F546ACFE6C8D9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_cinematic_bonnet_camera_this_update() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA5929C2E57AC90D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_first_person_flash_effect_this_update() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x77D65669A05D1A1A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_on_foot_first_person_view_this_update() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9C473089A934C930, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_cam_exist(cam: Cam) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0x153AD457764FD704, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn do_screen_fade_in(duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(duration)];
    unsafe { native::invoke(0x6A053CF596F67DF7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn do_screen_fade_out(duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(duration)];
    unsafe { native::invoke(0x40C719A5E410B9E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_cinematic_rendering_this_update(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x702B75DC9D3EDE56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_cam_coord(cam: Cam) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0x6B12F11C2A9F0344, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_cam_fov(cam: Cam) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0x8101D32A0A6B0F60, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_cam_rot(cam: Cam, rotation_order: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::int(rotation_order)];
    unsafe { native::invoke(0x9BF96B57254E7889, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_cam_spline_phase(cam: Cam) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0x095EDCD24D90033A, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_final_rendered_cam_coord() -> Result<Vector3, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5352E025EC2B416F, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_final_rendered_cam_fov() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x04AF77971E508F6A, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_final_rendered_cam_rot(rotation_order: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(rotation_order)];
    unsafe { native::invoke(0x602685BD85DD26CA, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_first_person_aim_cam_zoom_factor() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB4132CA1B0EE1365, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_gameplay_cam_coord() -> Result<Vector3, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x595320200B98596E, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_gameplay_cam_fov() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF6A96E5ACEEC6E50, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_gameplay_cam_relative_heading() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC4ABF536048998AA, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_gameplay_cam_relative_pitch() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x99AADEBBA803F827, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_gameplay_cam_rot(rotation_order: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(rotation_order)];
    unsafe { native::invoke(0x0252D2B5582957A6, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_letter_box_ratio() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA2B1C7EF759A63CE, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rendering_cam() -> Result<Cam, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x03A8931ECC8015D6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_letter_box() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x81DCFD13CF39920E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn invalidate_cinematic_vehicle_idle_mode() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x634F4A0562CF19B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_aim_cam_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x698F456FB909E077, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_cam_active(cam: Cam) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0x63EFCC7E1810B8E6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_cam_interpolating(cam: Cam) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0x578F8F1CAA17BD2B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_cam_rendering(cam: Cam) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0x4415F8A6C536D39F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_cam_shaking(cam: Cam) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0x2EEB402BD7320159, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_cinematic_cam_rendering() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBF7C780731AADBF8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_death_fail_camera_running() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x139EFB0A71DD9011, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_first_person_aim_cam_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF63134C54B6EC212, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_first_person_camera_active(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xA24C1D341C6E0D53, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_follow_vehicle_cam_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA40C2F51FB589E9A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_gameplay_cam_looking_behind() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8FE0D24FFD04D5A2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_gameplay_cam_rendering() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8660EA714834E412, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_gameplay_cam_shaking() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEA4C5F4AA0A4DBEF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_gameplay_hint_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2E04AB5FEE042D4A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_interpolating_from_script_cams() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x251241CAEC707106, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_interpolating_to_script_cams() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x43AB9D5A7D415478, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_screen_faded_in() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x37F9A426FBCF4AF2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_screen_faded_out() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF5472C80DF2FF847, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_screen_fading_in() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0CECCC63FFA2EF24, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_screen_fading_out() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x02F39BEFE7B88D00, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_sphere_visible(x: f32, y: f32, z: f32, radius: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0x2E941B5FFA2989C6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_cam_anim(cam: Cam, anim_name: &str, anim_dictionary: &str, x: f32, y: f32, z: f32, x_rot: f32, y_rot: f32, z_rot: f32, anim_flags: i32, rot_order: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::string(anim_name), NativeArg::string(anim_dictionary), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::int(anim_flags), NativeArg::int(rot_order)];
    unsafe { native::invoke(0xA263DDF694D563F6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn point_cam_at_coord(cam: Cam, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x948B39341C3A40C2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn point_cam_at_entity(cam: Cam, entity: Entity, p2: f32, p3: f32, p4: f32, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::int(entity), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xFC2867E6074D3A61, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn render_script_cams(render: bool, ease: bool, ease_time: i32, p3: bool, p4: bool, rendering_flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(render), NativeArg::boolean(ease), NativeArg::int(ease_time), NativeArg::boolean(p3), NativeArg::boolean(p4), NativeArg::int(rendering_flags)];
    unsafe { native::invoke(0x33281167E4942E4F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_active(cam: Cam, active: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::boolean(active)];
    unsafe { native::invoke(0x87295BCA613800C8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_active_with_interp(cam_to: Cam, cam_from: Cam, duration: i32, ease_location: i32, ease_rotation: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam_to), NativeArg::int(cam_from), NativeArg::int(duration), NativeArg::int(ease_location), NativeArg::int(ease_rotation)];
    unsafe { native::invoke(0x8B15AE2987C1AC8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_affects_aiming(cam: Cam, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x3CB9E8BDE5E76F33, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_controls_mini_map_heading(cam: Cam, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x1B8F3CE5A6001298, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_coord(cam: Cam, pos_x: f32, pos_y: f32, pos_z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z)];
    unsafe { native::invoke(0xF9EE7D419EE49DE6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_far_clip(cam: Cam, far_clip: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(far_clip)];
    unsafe { native::invoke(0x5E32817BF6302111, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_fov(cam: Cam, field_of_view: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(field_of_view)];
    unsafe { native::invoke(0x27666E5988D9D429, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_motion_blur_strength(cam: Cam, strength: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(strength)];
    unsafe { native::invoke(0x45FD891364181F9E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_near_clip(cam: Cam, near_clip: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(near_clip)];
    unsafe { native::invoke(0xA924028272A61364, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_params(cam: Cam, pos_x: f32, pos_y: f32, pos_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, field_of_view: f32, p8: NativeArg, graph_type1: i32, graph_type2: i32, rotation_order: i32, p12: NativeArg, p13: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(field_of_view), p8, NativeArg::int(graph_type1), NativeArg::int(graph_type2), NativeArg::int(rotation_order), p12, p13];
    unsafe { native::invoke(0xA47BBFFFB83D4D0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_rot(cam: Cam, rot_x: f32, rot_y: f32, rot_z: f32, rotation_order: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::int(rotation_order)];
    unsafe { native::invoke(0x63DFA6810AD78719, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_spline_duration(cam: Cam, time_duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::int(time_duration)];
    unsafe { native::invoke(0xFF6311652CA91015, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_spline_phase(cam: Cam, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(p1)];
    unsafe { native::invoke(0xF1898A68E7C15636, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_spline_smoothing_style(cam: Cam, smoothing_style: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::int(smoothing_style)];
    unsafe { native::invoke(0x84B3645618E726B0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cinematic_button_active(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xB90411F480457A6C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cinematic_mode_active(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xCE7A90B160F75046, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_first_person_aim_cam_relative_heading_limits_this_update(p0: f32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x05BD5E4088B30A66, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_first_person_aim_cam_relative_pitch_limits_this_update(p0: f32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x715B7F5E8BED32A2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_follow_ped_this_update(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x82E41D6ADE924FCA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_ignore_entity_collision_this_update(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xD904F75DBD7AB865, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_max_motion_blur_strength_this_update(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x8459B3E64257B21D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_relative_heading(heading: f32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(heading), NativeArg::float(p1)];
    unsafe { native::invoke(0x5D1EB123EAC5D071, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_relative_pitch(x: f32, value2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(value2)];
    unsafe { native::invoke(0xFB760AF4F537B8BF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_shake_amplitude(amplitude: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(amplitude)];
    unsafe { native::invoke(0x570E35F5C4A44838, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_coord_hint(x: f32, y: f32, z: f32, duration: i32, blend_out_duration: i32, blend_in_duration: i32, p6: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(duration), NativeArg::int(blend_out_duration), NativeArg::int(blend_in_duration), NativeArg::hash(p6)];
    unsafe { native::invoke(0xFA33B8C69A4A6A0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_entity_hint(entity: Entity, x_offset: f32, y_offset: f32, z_offset: f32, p4: bool, p5: i32, p6: i32, p7: i32, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::boolean(p4), NativeArg::int(p5), NativeArg::int(p6), NativeArg::int(p7), p8];
    unsafe { native::invoke(0xD1F7F32640ADFD12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_hint_base_orbit_pitch_offset(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x421192A2DA48FD01, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_hint_camera_relative_side_offset(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0xF86B6F93727C59C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_hint_camera_relative_vertical_offset(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x29E74F819150CC32, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_hint_follow_distance_scalar(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0xDDDC54181868F81F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_hint_fov(fov: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(fov)];
    unsafe { native::invoke(0x661E58BC6F00A49A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_object_hint(p0: NativeArg, p1: f32, p2: f32, p3: f32, p4: bool, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::boolean(p4), p5, p6, p7];
    unsafe { native::invoke(0xC40551D65F2BF297, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_ped_hint(p0: Ped, x1: f32, y1: f32, z1: f32, p4: bool, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::boolean(p4), p5, p6, p7];
    unsafe { native::invoke(0x90FB951648851733, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_vehicle_hint(p0: NativeArg, p1: f32, p2: f32, p3: f32, p4: bool, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::boolean(p4), p5, p6, p7];
    unsafe { native::invoke(0xE2B2BB7DAC280515, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_in_vehicle_cam_state_this_update(vehicle: Vehicle, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(p1)];
    unsafe { native::invoke(0xFA1D5E8D1C3CCD67, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scripted_camera_is_first_person_this_frame(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x1DD95A8D6B24A0C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_third_person_cam_orbit_distance_limits_this_update(p0: f32, distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(distance)];
    unsafe { native::invoke(0x2126C740A4AC370B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_third_person_cam_relative_heading_limits_this_update(minimum: f32, maximum: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(minimum), NativeArg::float(maximum)];
    unsafe { native::invoke(0x14F3947318CA8AD2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_third_person_cam_relative_pitch_limits_this_update(minimum: f32, maximum: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(minimum), NativeArg::float(maximum)];
    unsafe { native::invoke(0x326C7AA308F3DF6A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_widescreen_borders(p0: bool, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::int(p1)];
    unsafe { native::invoke(0xD7F4D54CF80AFA34, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn shake_cam(cam: Cam, type_: &str, amplitude: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::string(type_), NativeArg::float(amplitude)];
    unsafe { native::invoke(0xF9A7BCF5D050D4E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn shake_gameplay_cam(shake_name: &str, intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(shake_name), NativeArg::float(intensity)];
    unsafe { native::invoke(0xD9B31B4650520529, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_cam_pointing(cam: Cam) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam)];
    unsafe { native::invoke(0xCA1B30A3357C71F1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_cam_shaking(cam: Cam, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xB78CC4B4706614B0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_code_gameplay_hint(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x93759A83D0D844E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_gameplay_cam_shaking(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xE0DE43D290FB65F9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_gameplay_hint(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x1BCEC33D54CFCA8A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_rendering_script_cams_using_catch_up(render: bool, distance: f32, blend_back_smoothing_type: i32, p3: bool, p4: bool, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(render), NativeArg::float(distance), NativeArg::int(blend_back_smoothing_type), NativeArg::boolean(p3), NativeArg::boolean(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x8C7C7FF7CF0E5153, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0060_b31968_e60_e41(shake_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(shake_name)];
    unsafe { native::invoke(0x0060B31968E60E41, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x04084490_cc302_cfb() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x04084490CC302CFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x06557_f6_d96_c86881() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x06557F6D96C86881, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0961_b089947_ba6_d0(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0961B089947BA6D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_f1_ffef5_d54_ae832() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0F1FFEF5D54AE832, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_ff7125_f07_deb84_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0FF7125F07DEB84F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1204_eb53_a5_fbc63_d() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1204EB53A5FBC63D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x14_c4_a49_e36_c29_e49() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x14C4A49E36C29E49, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x16_e9_abdd34_ddd931() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x16E9ABDD34DDD931, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1811_a02277_a9_e49_d() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1811A02277A9E49D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x18_c3_dfac458783_bb() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x18C3DFAC458783BB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x190_f7_da1_ac09_a8_ef() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x190F7DA1AC09A8EF, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_d931_b7_cc0_ee3956(dictionary: &str, shot_name: &str, camera_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(dictionary), NativeArg::string(shot_name), NativeArg::string(camera_name)];
    unsafe { native::invoke(0x1D931B7CC0EE3956, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_d9_f72_dd4_fd9_a9_d7(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1D9F72DD4FD9A9D7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_f6_ebd94680252_ce(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x1F6EBD94680252CE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_fc6_c727_d30_ffdde(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1FC6C727D30FFDDE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x29_e6655_df3590_b0_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x29E6655DF3590B0D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_ab7_c81_b3_f70570_c() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2AB7C81B3F70570C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_dd3149_dc34_a3_f4_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2DD3149DC34A3F4C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x39073_da4_eddbc91_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x39073DA4EDDBC91D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_b8_e3_ad9677_ce12_b(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x3B8E3AD9677CE12B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_c486_e334520579_d() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3C486E334520579D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_c8_f74_e8_fe751614() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3C8F74E8FE751614, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4138_ee36_bc3_dc0_a7(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x4138EE36BC3DC0A7, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x41_e452_a3_c580_d1_a7() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x41E452A3C580D1A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x450769_c833_d58844() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x450769C833D58844, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x465_f04_f68_ad38197(dictionary: &str, shot_name: &str, duration: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::string(dictionary), NativeArg::string(shot_name), NativeArg::int(duration)];
    unsafe { native::invoke(0x465F04F68AD38197, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_d2_f46_d1_b28_d90_fb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x4D2F46D1B28D90FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5060_fa977_cea4455() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5060FA977CEA4455, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_b637_d6_f3_b67716_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5B637D6F3B67716A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6072_b7420_a83_a03_f() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6072B7420A83A03F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x632_be8_d84846_fa56() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x632BE8D84846FA56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x63_e5841_a9264_d016(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x63E5841A9264D016, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x641092322_a8852_ab() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x641092322A8852AB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6519238858_af5479(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6519238858AF5479, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_cab0_ba160_b168_d2() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6CAB0BA160B168D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_dfd37_e586_d4_f44_f() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6DFD37E586D4F44F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x70_a6658_d476_c6187() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x70A6658D476C6187, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x71_d71_e08_a7_ed5_bd7(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x71D71E08A7ED5BD7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x728491_fb3_dffef99(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x728491FB3DFFEF99, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x73_ff6_be63_dc18819() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x73FF6BE63DC18819, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x796085220_adcc847() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x796085220ADCC847, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_ce9_dc58_e3_e4755_f() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7CE9DC58E3E4755F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_e40_a01_b11398_fcb() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7E40A01B11398FCB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x80_d7_a3_e39_b120_bc4() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x80D7A3E39B120BC4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8505_e05_fc8822843(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x8505E05FC8822843, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x88544_c0_e3291_dcae(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x88544C0E3291DCAE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_b1_a5_fe7_e41_e52_b2() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8B1A5FE7E41E52B2, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_e036_b41_c37_d0_e5_f(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x8E036B41C37D0E5F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x975_f6_ebb62632_fe3() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x975F6EBB62632FE3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_ac65_a36_d3_c0_c189(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9AC65A36D3C0C189, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a54_d643_d0773_eb65(dictionary: &str, shot_name: &str, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(dictionary), NativeArg::string(shot_name), NativeArg::int(duration)];
    unsafe { native::invoke(0xA54D643D0773EB65, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a8_ba2_e0204_d8486_f() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA8BA2E0204D8486F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ac77757_c05_de9_e5_a(camera_dictionary: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(camera_dictionary)];
    unsafe { native::invoke(0xAC77757C05DE9E5A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b6_a80_e1_e3_a5444_f1() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB6A80E1E3A5444F1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b85_c13_e0_bf1_f2_a1_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xB85C13E0BF1F2A1C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c205_b3_c54_c6_a4_e37(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC205B3C54C6A4E37, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c252_c0_cc969_af79_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC252C0CC969AF79A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c285_fd21294_a1_c49(camera_dictionary: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(camera_dictionary)];
    unsafe { native::invoke(0xC285FD21294A1C49, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c3742_f1_fdf0_a6824() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC3742F1FDF0A6824, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c3_aebb276825_a359(dictionary: &str, shot_name: &str, duration: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(dictionary), NativeArg::string(shot_name), NativeArg::int(duration)];
    unsafe { native::invoke(0xC3AEBB276825A359, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c3_e9_e5_d4_f413_b773(shake_name: &str, intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(shake_name), NativeArg::float(intensity)];
    unsafe { native::invoke(0xC3E9E5D4F413B773, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cf69_ea05_cd9_c33_c9() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCF69EA05CD9C33C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_db382_fe20_c2_da222(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDB382FE20C2DA222, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc62_cd70658_e7_a02() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDC62CD70658E7A02, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_df7_f5_be9150_e47_e4(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDF7F5BE9150E47E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e28_f73212_a813_e82(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xE28F73212A813E82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e2_bb2_d6_a9_fe2_ecde(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE2BB2D6A9FE2ECDE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e6_f364_de6_c2_fdefe() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE6F364DE6C2FDEFE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ea113_bf9_b0_c0_c5_d7(dictionary: &str, shot_name: &str, duration: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::string(dictionary), NativeArg::string(shot_name), NativeArg::int(duration)];
    unsafe { native::invoke(0xEA113BF9B0C0C5D7, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ef9_a3132_a0_aa6_b19() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEF9A3132A0AA6B19, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f1_a6_feedf3776_ef9() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF1A6FEEDF3776EF9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f48664_e9_c83825_e3(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xF48664E9C83825E3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f824530_b612_fe0_ce() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF824530B612FE0CE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fc3_f638_be2_b6_bb02() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFC3F638BE2B6BB02, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_feb8646818294_c75(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFEB8646818294C75, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fefddc6_e8_fdf8_a75(shake_name: &str, intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(shake_name), NativeArg::float(intensity)];
    unsafe { native::invoke(0xFEFDDC6E8FDF8A75, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cam_create(camera_dictionary: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(camera_dictionary)];
    unsafe { native::invoke(0xB8B207C34285E978, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cam_create_2(camera_dictionary: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(camera_dictionary)];
    unsafe { native::invoke(0x7B0279170961A73F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cam_destroy(camera_dictionary: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(camera_dictionary)];
    unsafe { native::invoke(0x0A5A4F1979ABB40E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cinematic_location_set_location_and_rotation(name: &str, x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z)];
    unsafe { native::invoke(0x0E94C95EC3185FA9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cinematic_location_trigger_scripted_shot_event_2(dictionary: &str, shot_name: &str, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(dictionary), NativeArg::string(shot_name), NativeArg::int(duration)];
    unsafe { native::invoke(0xBC016635D6A73B31, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_kill_cam(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2F994CC29CAA9D22, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_cinematic_mode_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8910C24B7E0046EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_on_foot_first_person_view_this_update_2() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x05AB44D906738426, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_cinematic_death_cam_on_ped(target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(target_ped)];
    unsafe { native::invoke(0xE3639DB78B3B5400, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_first_person_cam_this_frame() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x90DA5BA5C2635416, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_letter_box_this_update() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC64ABC0676AF262B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_third_person_cam_far_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1CFB749AD4317BDE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_third_person_cam_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8370D34BD2E60B73, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_third_person_close_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x718C6ECF5E8CBDD4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn freeze_gameplay_cam_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x027CAB2C3AF27010, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_photo_mode_dof() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4653A741D17F2CD0, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_photo_mode_focal_length() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2533BAFFBE737E54, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_photo_mode_focus_distance() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x18FC740FFDCD7454, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_anim_scene_cam_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x20389408F0E93B9A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_camera_available(camera_dictionary: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(camera_dictionary)];
    unsafe { native::invoke(0x927B810E43E99932, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_cam_data_dict_loaded(camera_dictionary: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(camera_dictionary)];
    unsafe { native::invoke(0xDD0B7C5AE58F721D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_cam_photofx_running() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA14D5FE82BCB1D9E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_cinematic_cam_location_loaded(s_location_dict_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(s_location_dict_name)];
    unsafe { native::invoke(0xAA235E2F2C09E952, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_cinematic_cam_location_loaded_2(location_dict_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(location_dict_name)];
    unsafe { native::invoke(0x595550376B7EA230, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_in_cinematic_mode() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x74F1D22EFA71FAB8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_in_full_first_person_mode() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD1BA66940E94C547, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn load_camera_data_dict(camera_dictionary: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(camera_dictionary)];
    unsafe { native::invoke(0x6A4D224FC7643941, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn load_cinematic_cam_location(location_dict_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(location_dict_name)];
    unsafe { native::invoke(0x1B3C2D961F5FC0E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pause_camera_focus(cam: Cam, pause: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::boolean(pause)];
    unsafe { native::invoke(0x9F97E85EC142255E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reactivate_ped_headshot_execute_slowcam(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x986F7A51EE3E1F92, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_letter_box_now(p0: bool, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x69D65E89FFD72313, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_letter_box_overtime(p0: i32, p1: i32, p2: bool, p3: i32, p4: bool, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1), NativeArg::boolean(p2), NativeArg::int(p3), NativeArg::boolean(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xE296208C273BD7F0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_dof_params(cam: Cam, args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::in_out(args)];
    unsafe { native::invoke(0xE4B7945EF4F1BFB2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cam_focus_distance(cam: Cam, distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cam), NativeArg::float(distance)];
    unsafe { native::invoke(0x11F32BB61B756732, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_initial_heading(cam_initial_heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(cam_initial_heading)];
    unsafe { native::invoke(0x6C1053C433A573CF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_initial_pitch(cam_initial_pitch: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(cam_initial_pitch)];
    unsafe { native::invoke(0x449995EA846D3FC2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_initial_zoom(cam_initial_zoom: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(cam_initial_zoom)];
    unsafe { native::invoke(0xBCDA0BA8762FACB9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gameplay_cam_params_this_update(speed: f32, respect_horizontal_offset: bool, horizontal_offset: f32, respect_distance: bool, distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(speed), NativeArg::boolean(respect_horizontal_offset), NativeArg::float(horizontal_offset), NativeArg::boolean(respect_distance), NativeArg::float(distance)];
    unsafe { native::invoke(0x066167C63111D8CF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_start_cinematic_death_cam(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x6E969927CF632608, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_camera_orbit(p0: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0x65B205BF30C13DDB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_gameplay_cam_shaking_with_name(shake_name: &str, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(shake_name), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x4285804FD65D8066, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn trigger_mission_failed_cam() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9A92C06ACBAF9731, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unload_camera_data_dict(camera_dictionary: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(camera_dictionary)];
    unsafe { native::invoke(0x798BE43C9393632B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unload_cinematic_camera_location(dictionary_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(dictionary_name)];
    unsafe { native::invoke(0x2412216FCC7B4E3E, &arguments) }.map(|_| ())
}

// ============================================================================
// CLOCK
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_to_clock_time(hours: i32, minutes: i32, seconds: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(hours), NativeArg::int(minutes), NativeArg::int(seconds)];
    unsafe { native::invoke(0xAB7C251C7701D336, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn advance_clock_time_to(hour: i32, minute: i32, second: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(hour), NativeArg::int(minute), NativeArg::int(second)];
    unsafe { native::invoke(0x0184750AE88D0B1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_clock_day_of_month() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDF2FD796C54480A5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_clock_day_of_week() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4DD02D4C7FB30076, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_clock_hours() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC82CF208C2B19199, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_clock_minutes() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4E162231B823DBBF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_clock_month() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2D44E8FC79EAB1AC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_clock_seconds() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB6101ABE62B5F080, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_clock_year() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE136DCA28C4A48BA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_milliseconds_per_game_minute() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE4CB8D126501EC52, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_posix_time(year: &mut i32, month: &mut i32, day: &mut i32, hour: &mut i32, minute: &mut i32, second: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(year), NativeArg::in_out(month), NativeArg::in_out(day), NativeArg::in_out(hour), NativeArg::in_out(minute), NativeArg::in_out(second)];
    unsafe { native::invoke(0x90338AD4A784E455, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pause_clock(toggle: bool, unused: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle), unused];
    unsafe { native::invoke(0x4D1A590C92BF377E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_clock_date(day: i32, month: i32, year: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(day), NativeArg::int(month), NativeArg::int(year)];
    unsafe { native::invoke(0x02AD3092562941E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_clock_time(hour: i32, minute: i32, second: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(hour), NativeArg::int(minute), NativeArg::int(second)];
    unsafe { native::invoke(0x3A52C59FFB2DEED8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_time_to_date_time(in_date_time: &mut Any, time_to_add: &mut Any, out_date_time: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(in_date_time), NativeArg::in_out(time_to_add), NativeArg::in_out(out_date_time)];
    unsafe { native::invoke(0x28EEACE9B43D9597, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_posix_time_struct(out_time: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(out_time)];
    unsafe { native::invoke(0x86A68E84E5884951, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_seconds_since_base_year() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x78FD8BE812E436B2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pause_clock_this_frame(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x568D998A9FF96774, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_milliseconds_per_game_minute(ms: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ms)];
    unsafe { native::invoke(0x04EEDB3848DACF68, &arguments) }.map(|_| ())
}

// ============================================================================
// COLLECTION
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_category_set_has_received_list(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x0B6D275D2F242E17, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_get_category_item_set_buy_award(collectable_category: Hash, p1: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::hash(p1)];
    unsafe { native::invoke(0xCC644BC1DD655269, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x33825_a7388_a6_b9_f6(collectable_category: Hash, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0x33825A7388A6B9F6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_fd91_f1_a148_a0468(collectable_category: Hash, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0x3FD91F1A148A0468, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x61_befba3_ce7_a3_bc8(collectable_category: Hash, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0x61BEFBA3CE7A3BC8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_bab7_aced1017204(collectable_category: Hash, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0x6BAB7ACED1017204, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x755901_c7598_b97_bc(collectable_category: Hash, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0x755901C7598B97BC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x775_fa1_fc87666847(collectable_category: Hash, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0x775FA1FC87666847, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x93_f2_e7_b5_db85657_b(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x93F2E7B5DB85657B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_adee485726025_d4(collectable_category: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(collectable_category)];
    unsafe { native::invoke(0x9ADEE485726025D4, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b9020_ec89_c07_df04(collectable_category: Hash, p1: i32, index: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1), NativeArg::int(index)];
    unsafe { native::invoke(0xB9020EC89C07DF04, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c3_ca424_e1_f12_ed0_c(collectable_category: Hash, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0xC3CA424E1F12ED0C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c4_ac39719_c1_bb559(collectable_category: Hash, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), p1];
    unsafe { native::invoke(0xC4AC39719C1BB559, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d1806_fb3_eded6_d11(collectable_category: Hash, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0xD1806FB3EDED6D11, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d297_f68928_a58130(collectable_category: Hash, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0xD297F68928A58130, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fc832_b06127_d8_e99(collectable_category: Hash, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::int(p1)];
    unsafe { native::invoke(0xFC832B06127D8E99, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_category_get_num_collectables(collectable_category: Hash, collectable_subcategory: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::hash(collectable_subcategory)];
    unsafe { native::invoke(0x62CAB7DB62EAD434, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_category_get_num_found(collectable_category: Hash, collectable_subcategory: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::hash(collectable_subcategory)];
    unsafe { native::invoke(0x5461C821D00FE15A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_category_get_num_turned_in(collectable_category: Hash, collectable_subcategory: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::hash(collectable_subcategory)];
    unsafe { native::invoke(0x3A65F4844913A047, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_category_get_toast_texture_dictionary(collectable_category: Hash, collectable_subcategory: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::hash(collectable_subcategory)];
    unsafe { native::invoke(0x13AAECDA43318BFE, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_category_get_toast_texture_name(collectable_category: Hash, collectable_subcategory: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(collectable_category), NativeArg::hash(collectable_subcategory)];
    unsafe { native::invoke(0xD52D20B0C76BB26D, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_get_category(collectable_item: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(collectable_item)];
    unsafe { native::invoke(0x725D52F21A5E9EF6, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_get_collectable_item_hash(index: i32, collectable_category: Hash, collectable_subcategory: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(index), NativeArg::hash(collectable_category), NativeArg::hash(collectable_subcategory)];
    unsafe { native::invoke(0x126CBEBBA46693CF, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_get_ipl(collectable_item: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(collectable_item)];
    unsafe { native::invoke(0x922A79CD4A033B8B, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_get_num_found(collectable_item: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(collectable_item)];
    unsafe { native::invoke(0xF83D3DDA4D3C8169, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_get_num_turned_in(collectable_item: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(collectable_item)];
    unsafe { native::invoke(0x9A03F22AD446EEAC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_get_placement_location(collectable_item: Hash) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::hash(collectable_item)];
    unsafe { native::invoke(0x1F1DD794908C2BFA, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_get_subcategory(collectable_item: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(collectable_item)];
    unsafe { native::invoke(0x6052B4DE6657684F, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_increment_num_found(collectable_item: Hash, amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(collectable_item), NativeArg::int(amount)];
    unsafe { native::invoke(0x3EA62E56F386C997, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_increment_num_turned_in(collectable_item: Hash, amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(collectable_item), NativeArg::int(amount)];
    unsafe { native::invoke(0x398FAB9C96A81924, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn collectable_set_item_hash_discovered(collectable_item: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(collectable_item)];
    unsafe { native::invoke(0xEC3959E9950BF56B, &arguments) }.map(|result| result.as_u32())
}

// ============================================================================
// COMPANION
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_a8_fd91_ede7_b328_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0A8FD91EDE7B328A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_c6_a00_dae896614_c(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0C6A00DAE896614C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_de02_da3_c0_f66955(ped: Ped, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0x0DE02DA3C0F66955, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2917_e634206_b9_e17(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x2917E634206B9E17, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_caad93_fa5_b9579_a(volume: Volume, p1: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x3CAAD93FA5B9579A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x61_bda07407754_a5_c(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x61BDA07407754A5C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x722_fbe08_ef5_b87_bd(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x722FBE08EF5B87BD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7274_f84_b1501_b523(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7274F84B1501B523, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_fb98_b719_aa0075_a(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x8FB98B719AA0075A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x991_e3346_d788_f20_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x991E3346D788F20F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_c902084_f48_d2_e6_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9C902084F48D2E6C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a079_ff7_cfb9_ac8_bd(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA079FF7CFB9AC8BD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bf6583_e926_d13890(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xBF6583E926D13890, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ce27824_b5968_b79_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCE27824B5968B79A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d428_c3_f92_fc3_f6_f8(ped: Ped, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(p1)];
    unsafe { native::invoke(0xD428C3F92FC3F6F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d55_a871_e1_ce3481_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xD55A871E1CE3481B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d730281_e496621_fb(ped: Ped, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0xD730281E496621FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d747979_c053_efa7_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD747979C053EFA7A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f06_cbb8_ccca823_c0(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xF06CBB8CCCA823C0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activate_companion_analysis(group_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0xCBD9EC60495C728C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_companion_flag(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xDEB369F6AD168C58, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn deactivate_companion_analysis(group_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0x72B7F65F11FC8896, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_companion_activity(group_id: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0xB7E0590C86E1711F, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_companion_flag(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x1740E3DEE0AE4D27, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_companion_activity(group_id: i32, activity: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::hash(activity)];
    unsafe { native::invoke(0x0F1CD8CA9E65D5F6, &arguments) }.map(|_| ())
}

// ============================================================================
// COMPAPP
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x29_c733459_a9011_eb(p0: Hash, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0x29C733459A9011EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x74_bcceb233_ad95_b2(p0: Hash, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::hash(p1)];
    unsafe { native::invoke(0x74BCCEB233AD95B2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_af1_bb4504_ea5_ed9() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7AF1BB4504EA5ED9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b6_fd96420_c0126_a1(p0: Hash, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xB6FD96420C0126A1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ccb4635_a071_fb62_d() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCCB4635A071FB62D, &arguments) }.map(|_| ())
}

// ============================================================================
// COMPENDIUM
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_animal_get_sample_inventory_item(compendium_entry: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(compendium_entry)];
    unsafe { native::invoke(0x4E4ACAE1C671A9DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_animal_has_sample(compendium_entry: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(compendium_entry)];
    unsafe { native::invoke(0x6FC24625E4FCAC27, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_animal_has_stamp(compendium_entry: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(compendium_entry)];
    unsafe { native::invoke(0xBCF569FC32FFF456, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_animal_observed_by_stat_name(animal_type: Hash, disable_compendium_toast: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(animal_type), NativeArg::boolean(disable_compendium_toast)];
    unsafe { native::invoke(0x725D52F26A5E9E10, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_animal_set_discovered(compendium_entry: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(compendium_entry)];
    unsafe { native::invoke(0x67F35C7C9F2BDCFE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_fish_caught(ped: Ped, category: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(category)];
    unsafe { native::invoke(0x725D52F21A5E9E00, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_fish_get_lure_suitability_by_stat_item(animal_type: Hash, bait_type: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(animal_type), NativeArg::hash(bait_type)];
    unsafe { native::invoke(0x725D52F21A5E9E81, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_gang_ambush_survived(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x725D52F21A5E9E04, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_gang_bounty_captured(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x725D52F21A5E9E06, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_gang_camp_found(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x725D52F21A5E9E03, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_gang_encountered(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x725D52F21A5E9E05, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_gang_hideout_found(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x725D52F21A5E9E08, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_gang_member_killed(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x725D52F21A5E9E07, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_entry_by_index_in_subcategory(category: Hash, subcategory: Hash, count: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::hash(subcategory), NativeArg::int(count)];
    unsafe { native::invoke(0x5CEB63B2E3D9895F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_entry_by_ped_index(category: Hash, ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::int(ped)];
    unsafe { native::invoke(0x1CFA0219D8E1CF25, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_entry_by_stat_item(category: Hash, animal_type: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::hash(animal_type)];
    unsafe { native::invoke(0x66EC938394D76C85, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_map_discoverable_from_stat_item(animal_stat_item: Hash, x: f32, y: f32, z: f32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(animal_stat_item), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x729D54121A5E9E20, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_num_entries_in_subcategory(category: Hash, subcategory: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::hash(subcategory)];
    unsafe { native::invoke(0xF58A0C0E086E8E36, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_short_description_from_ped(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6C5E5D48E48B4C65, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_study_award_id(ped: Ped) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9F678782720349E4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_subcategory_ped_is_in(category: Hash, ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::int(ped)];
    unsafe { native::invoke(0x9B657550DF55EC96, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_subcategory_sample_toast_desc_complete(category: Hash, subcategory: Hash) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::hash(subcategory)];
    unsafe { native::invoke(0x59D4D68CDB82427C, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_subcategory_sample_toast_desc_progress(category: Hash, subcategory: Hash) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::hash(subcategory)];
    unsafe { native::invoke(0x82BFB5B367957699, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_subcategory_sample_toast_title(category: Hash, subcategory: Hash) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::hash(subcategory)];
    unsafe { native::invoke(0x5E50C67EB60951E6, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_subcategory_toast_app_id(category: Hash, subcategory: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::hash(subcategory)];
    unsafe { native::invoke(0x2BF30D9D4D680112, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_herb_picked(herb_type: Hash, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(herb_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x725D52F21A5E9E09, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_horse_bonding(ped: Ped, bonding_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(bonding_level)];
    unsafe { native::invoke(0x725D52F21A5E9E50, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_horse_observed(ped: Ped, disable_compendium_toast: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(disable_compendium_toast)];
    unsafe { native::invoke(0x725D58F2125E5E50, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_horse_wild_broken(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x725852D21A2E9E50, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_was_animal_observed(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x23B5E9C5160BC04F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x725_d52_f21_a5_e9_e22(category: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::hash(category)];
    unsafe { native::invoke(0x725D52F21A5E9E22, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x729_d52461_aea9_e22(category: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(category)];
    unsafe { native::invoke(0x729D52461AEA9E22, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_num_of_entries_in_category(category: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(category)];
    unsafe { native::invoke(0x729D52F61A5A9E22, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compendium_get_subcategory_hash_from_animal_type(category: Hash, animal_type: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::hash(animal_type)];
    unsafe { native::invoke(0xCD278B6BFBDBDC22, &arguments) }.map(|result| result.as_u32())
}

// ============================================================================
// CRASHLOG
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_fd3_ecf9_d0_c8655_f(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x0FD3ECF9D0C8655F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x23_ccab8_f40_b9_cbee(x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x23CCAB8F40B9CBEE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x33_c1_d63_e55_fa4284(p0: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x33C1D63E55FA4284, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_a66_f1963_b223_f61(p0: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x3A66F1963B223F61, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_e42_ca5_bcd45444_a() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4E42CA5BCD45444A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_c680_ff55617_f82_f() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7C680FF55617F82F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x87_f005_c969_ef1563(p0: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x87F005C969EF1563, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a67_f0_b039_d9_cd513(p0: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xA67F0B039D9CD513, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ca0_bac376_c541978(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0xCA0BAC376C541978, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d8_e3_d22_aa4_f0_e0_a5(p0: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0xD8E3D22AA4F0E0A5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_da05310_ea94_dc8_c6(p0: &str, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0xDA05310EA94DC8C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e72_e234_b30_da7_b7_a(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xE72E234B30DA7B7A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f0_d545_c1_eead614_a() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF0D545C1EEAD614A, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// CREW
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_get_local_memberships_count() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1F471B79ACC90BEF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_get_membership_desc(member_desc: &mut Any, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(member_desc), NativeArg::int(p1)];
    unsafe { native::invoke(0x48DE78AF2C8885B8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_is_emblem_ready(p0: NativeArg, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [p0, NativeArg::in_out(p1)];
    unsafe { native::invoke(0xA134777FF7F33331, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_player_get_desc(clan_desc: &mut Any, buffer_size: i32, gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(clan_desc), NativeArg::int(buffer_size), NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xEEE6EACBE8874FBA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_player_is_active(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xB124B57F571D8F18, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_release_emblem(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x113E6E3E50E286B0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_request_emblem(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x13518FF1C6B28938, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_service_is_valid() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x579CCED0265D4896, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_find_gamers_in_crew(crew_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(crew_id)];
    unsafe { native::invoke(0xE532D6811B3A4D2A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_primary_clan_data_clear() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9AA46BADAD0E27ED, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_primary_clan_data_new(p0: &mut Any, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xC080FF658B2E41DA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_primary_clan_data_pending() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB5074DB804E28CE7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_primary_clan_data_start(p0: &mut Any, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), p1];
    unsafe { native::invoke(0xCE86D8191B762107, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_primary_clan_data_success() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5B4F04F19376A0BA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x58_d378_af2_c8765_b7(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x58D378AF2C8765B7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_accept_clan_invite(crew_invite_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(crew_invite_index)];
    unsafe { native::invoke(0x8E2143144B8E188D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_invite_player(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC685B014CE3D988B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clan_set_active(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC080FF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

// ============================================================================
// DATABINDING
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_is_entry_valid(entry_id: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(entry_id)];
    unsafe { native::invoke(0x1E7130793AAAAB8D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_int(p0: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x570784D782597512, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_string_from_parent(p0: NativeArg, p1: &str, p2: &str) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0x4FF713B2F17A391E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x02_b21_b6_beedd83_cc(entry_id: Hash, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(entry_id), NativeArg::int(p1)];
    unsafe { native::invoke(0x02B21B6BEEDD83CC, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x05_ac9_e1_e02975_afb(p0: NativeArg, p1: &str, p2: f32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0x05AC9E1E02975AFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1919_d59_e60_fd516_e(p0: NativeArg, p1: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x1919D59E60FD516E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x294_af5323_f44_b053(p0: NativeArg, p1: &str, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1), p2];
    unsafe { native::invoke(0x294AF5323F44B053, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_bf0767_cf33_fcc88(entry_id: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(entry_id)];
    unsafe { native::invoke(0x3BF0767CF33FCC88, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x422179_c7_f6_ad9304(p0: NativeArg, gamer_handle: &mut Any) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x422179C7F6AD9304, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6329_c34_bee5_bff4_b(p0: NativeArg, p1: Hash) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1)];
    unsafe { native::invoke(0x6329C34BEE5BFF4B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_fc60_c94_c83_c5_cd7(p0: NativeArg, p1: Hash, p2: i32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x7FC60C94C83C5CD7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b138_ca787_f3_dd858(p0: NativeArg, p1: &str, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), p2];
    unsafe { native::invoke(0xB138CA787F3DD858, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bfc83_da249_befcc9(p0: NativeArg, p1: Hash, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(p1), p2];
    unsafe { native::invoke(0xBFC83DA249BEFCC9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c900_cec8_a172375_b(p0: NativeArg, p1: &str, p2: i32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xC900CEC8A172375B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d48993_a61938_c64_d(p0: NativeArg, p1: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0xD48993A61938C64D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e6_aab897120492_d6(p0: NativeArg, p1: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0xE6AAB897120492D6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e6_aab897120492_d7(p0: NativeArg, p1: &str, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), p2];
    unsafe { native::invoke(0xE6AAB897120492D7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f47_e33_f8_d2523825(p0: NativeArg, p1: i32) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::int(p1)];
    unsafe { native::invoke(0xF47E33F8D2523825, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_bool(p0: NativeArg, p1: &str, p2: bool) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x58BAA5F635DA2FF4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_bool_by_hash(p0: NativeArg, p1: Hash, p2: bool) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xBC95D3AE2ECA70D6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_bool_from_path(p0: &str, p1: &str, p2: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x37BB86A751148A6A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_container(entry_id: Hash, p1: &str) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(entry_id), NativeArg::string(p1)];
    unsafe { native::invoke(0xEB4F9A3537EEABCD, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_container_by_hash(p0: NativeArg, p1: Hash) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1)];
    unsafe { native::invoke(0x98BB14345BB68257, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_container_from_path(p0: &str, p1: &str) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0x0C827D175F1292F4, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_container_from_path_by_hash(p0: &str, p1: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::hash(p1)];
    unsafe { native::invoke(0xD7DB94AB78E8EBE4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_float(p0: NativeArg, p1: &str, p2: f32) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0x5154228273ADB9A6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_gang_id(p0: NativeArg, p1: &str, gang_id: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1), gang_id];
    unsafe { native::invoke(0x7D0F2014DB28DD00, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_hash(p0: NativeArg, p1: &str, p2: Hash) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0x8538F1205D60ECA6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_hash_by_hash(p0: NativeArg, p1: Hash, p2: Hash) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0x8E173DFB041993C6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_int(p0: NativeArg, p1: &str, p2: Hash) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0x307A3247C5457BDE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_int_by_hash(p0: NativeArg, p1: Hash, p2: i32) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x267F9527F4350ADE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_posse_id(p0: NativeArg, p1: &str, posse_id: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1), posse_id];
    unsafe { native::invoke(0x7D0F2014DB28DD01, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_string(p0: NativeArg, p1: &str, p2: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0x617FCA1C5652BBAD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_string_by_hash(p0: NativeArg, p1: Hash, p2: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0xEAD09E76E22630C3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_data_string_from_path(p0: &str, p1: &str, p2: &str) -> Result<Any, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0xA381DE86EE170C4A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_hash_array(p0: NativeArg, p1: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0x52F5F08278EA5D75, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_string_array(p0: NativeArg, p1: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0x1B23E0627BDBFE85, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_ui_item_list(p0: NativeArg, p1: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0xFE74FA57E0CE6824, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_ui_item_list_by_hash(p0: NativeArg, p1: Hash) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1)];
    unsafe { native::invoke(0x3C7799283325181B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_add_ui_item_list_from_path(p0: &str, p1: &str) -> Result<Any, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0xDB5B9A474148F699, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_clear_binding_array(entry_id: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(entry_id)];
    unsafe { native::invoke(0xA1F15C1D03DF802D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_get_array_count(entry_id: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(entry_id)];
    unsafe { native::invoke(0xD23F5DE04FE717E2, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_get_data_container_from_child_index(entry_id: Hash, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(entry_id), NativeArg::int(p1)];
    unsafe { native::invoke(0x0C827D175F1292F3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_get_data_container_from_path(p0: &str) -> Result<Any, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x0C827D175F1292F2, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_get_item_context_by_index(p0: NativeArg, index: i32) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::int(index)];
    unsafe { native::invoke(0xE96D7F9FEFCC105F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_insert_ui_item_to_list_from_context_hash_alias(p0: NativeArg, index: i32, p2: Hash, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(index), NativeArg::hash(p2), p3];
    unsafe { native::invoke(0xEE97A05C05F16E41, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_insert_ui_item_to_list_from_context_string_alias(p0: NativeArg, index: i32, p2: &str, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(index), NativeArg::string(p2), p3];
    unsafe { native::invoke(0x5859E970794D92F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_insert_ui_item_to_list_from_path_string_alias(p0: NativeArg, p1: NativeArg, p2: &str, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, NativeArg::string(p2), p3];
    unsafe { native::invoke(0x5740774F608E4FC8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_data_bool(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5EEFBD4B6D7CD6EB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_data_bool_from_parent(p0: NativeArg, p1: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0xA8EDE09FE07BD77F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_data_bool_from_parent_by_hash(p0: NativeArg, p1: Hash) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1)];
    unsafe { native::invoke(0x4CDC3FDDFAE07EB3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_data_hash_string_from_parent(p0: NativeArg, p1: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0x9B535990B01B62DE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_data_hash_string_from_parent_by_hash(p0: NativeArg, p1: Hash) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1)];
    unsafe { native::invoke(0x1F43BC25A119B252, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_data_int_from_parent(p0: NativeArg, p1: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0xFFC566A4801F6B40, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_data_int_from_parent_by_hash(p0: NativeArg, p1: Hash) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::hash(p1)];
    unsafe { native::invoke(0xB5F668B648EC0970, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_data_string(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3D290B5FFA7C5151, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_data_string_from_parent(p0: NativeArg, p1: &str) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0x6323AD277C4A2AFB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_float(entry_id: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::hash(entry_id)];
    unsafe { native::invoke(0x5FE444EB67C70AD4, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_read_hash(entry_id: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(entry_id)];
    unsafe { native::invoke(0x81D7183E7A8ECA72, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_remove_binding_array_item_by_data_context_id(p0: NativeArg, entry_id: Hash) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(entry_id)];
    unsafe { native::invoke(0xF68B1726EAF7B285, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_remove_data_entry(entry_id: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(entry_id)];
    unsafe { native::invoke(0x0AE9938D0541F2DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_remove_ui_item_from_list_by_index(entry_id: Hash, index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(entry_id), NativeArg::int(index)];
    unsafe { native::invoke(0x6318FB3BE37E11B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_set_templated_ui_item_hash_alias(p0: NativeArg, p1: i32, p2: Hash) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0x0AE7138D0541F2DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_set_templated_ui_item_list_size(p0: NativeArg, p1: i32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(p1)];
    unsafe { native::invoke(0xFE74FA57E0CE6825, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_bool(p0: NativeArg, p1: bool) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::boolean(p1)];
    unsafe { native::invoke(0xAB888B4B91046770, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_bool_from_parent(p0: NativeArg, p1: &str, p2: bool) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xBDFE546E4C2D0E21, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_float(p0: NativeArg, p1: f32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::float(p1)];
    unsafe { native::invoke(0xDF504BECEB15DA93, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_gang_id(p0: NativeArg, p1: &str, gang_id: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), gang_id];
    unsafe { native::invoke(0xC70041408E16BE2D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_hash_string(p0: NativeArg, p1: Hash) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(p1)];
    unsafe { native::invoke(0xACDEF586BD71B1FD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_hash_string_from_parent(p0: NativeArg, p1: &str, p2: Hash) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0x0971F04E1EAA7AE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_hash_string_from_parent_by_hash(p0: NativeArg, p1: Hash, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(p1), p2];
    unsafe { native::invoke(0x20209529689E0953, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_int(p0: NativeArg, p1: i32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(p1)];
    unsafe { native::invoke(0x335C3F6B3766B8D9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_int_from_parent(p0: NativeArg, p1: &str, p2: i32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x9EFA98238BA08FC4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_int_from_parent_by_hash(p0: NativeArg, p1: Hash, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(p1), p2];
    unsafe { native::invoke(0x9D6E10A41D6ED6EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_posse_id(p0: NativeArg, p1: &str, posse_id: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1), posse_id];
    unsafe { native::invoke(0xC70041408E16BE2E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_script_variables(p0: i32, p1: i32, extra: &[NativeArg]) -> Result<(), NativeError> {
    let mut arguments = Vec::with_capacity(2 + extra.len());
    arguments.push(NativeArg::int(p0));
    arguments.push(NativeArg::int(p1));
    arguments.extend_from_slice(extra);
    unsafe { native::invoke(0xAB888B4B91046771, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_data_string(p0: NativeArg, p1: &str) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::string(p1)];
    unsafe { native::invoke(0xE1BD342F2872AEE9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn databinding_write_string_from_hash(p0: NativeArg, p1: Hash, p2: &str) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0xA3BD6FF95E713EE5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn virtual_collection_exists(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x37963B56755BFB35, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn virtual_collection_item_add(p0: NativeArg, index: i32, p2: Hash, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(index), NativeArg::hash(p2), p3];
    unsafe { native::invoke(0x6DCBF187221CF73D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn virtual_collection_reset(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x09D95666ED2B5F60, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn virtual_collection_set_interest_index(p0: NativeArg, interest_index: i32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(interest_index)];
    unsafe { native::invoke(0x49A8447533308BCF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn virtual_collection_set_size(p0: NativeArg, size: i32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(size)];
    unsafe { native::invoke(0x9DCE9B01A93B58BC, &arguments) }.map(|_| ())
}

// ============================================================================
// DATAFILE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dataarray_get_bool(array_data: &mut Any, array_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(array_data), NativeArg::int(array_index)];
    unsafe { native::invoke(0xAB1231D2DE52F2D3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dataarray_get_count(array_data: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(array_data)];
    unsafe { native::invoke(0x6A885BF69239E539, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dataarray_get_dict(array_data: &mut Any, array_index: i32) -> Result<u64, NativeError> {
    let arguments = [NativeArg::in_out(array_data), NativeArg::int(array_index)];
    unsafe { native::invoke(0xA010655985853485, &arguments) }.map(|result| result.as_u64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dataarray_get_float(array_data: &mut Any, array_index: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::in_out(array_data), NativeArg::int(array_index)];
    unsafe { native::invoke(0xA9D003CF419CB81E, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dataarray_get_int(array_data: &mut Any, array_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(array_data), NativeArg::int(array_index)];
    unsafe { native::invoke(0x96DEA500B6EBBE53, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dataarray_get_string(array_data: &mut Any, array_index: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::in_out(array_data), NativeArg::int(array_index)];
    unsafe { native::invoke(0xB6790A8FF80F889F, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dataarray_get_type(array_data: &mut Any, array_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(array_data), NativeArg::int(array_index)];
    unsafe { native::invoke(0x151DAFE6B3B9888F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dataarray_get_vector(array_data: &mut Any, array_index: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::in_out(array_data), NativeArg::int(array_index)];
    unsafe { native::invoke(0x850DA2750DA14E9A, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_get_array(object_data: &mut Any, key: &str) -> Result<u64, NativeError> {
    let arguments = [NativeArg::in_out(object_data), NativeArg::string(key)];
    unsafe { native::invoke(0x1B5447CF18544B18, &arguments) }.map(|result| result.as_u64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_get_bool(object_data: &mut Any, key: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(object_data), NativeArg::string(key)];
    unsafe { native::invoke(0x175E915A486EE548, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_get_dict(object_data: &mut Any, key: &str) -> Result<u64, NativeError> {
    let arguments = [NativeArg::in_out(object_data), NativeArg::string(key)];
    unsafe { native::invoke(0x4D7A30130F46AC9C, &arguments) }.map(|result| result.as_u64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_get_float(object_data: &mut Any, key: &str) -> Result<f32, NativeError> {
    let arguments = [NativeArg::in_out(object_data), NativeArg::string(key)];
    unsafe { native::invoke(0x814643ECA258ADF5, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_get_int(object_data: &mut Any, key: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(object_data), NativeArg::string(key)];
    unsafe { native::invoke(0x9D896A3B87D96E2B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_get_string(object_data: &mut Any, key: &str) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::in_out(object_data), NativeArg::string(key)];
    unsafe { native::invoke(0xE37B38C0B4E95DFA, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_get_type(object_data: &mut Any, key: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(object_data), NativeArg::string(key)];
    unsafe { native::invoke(0x92E11E3CA4C7CDF0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_get_vector(object_data: &mut Any, key: &str) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::in_out(object_data), NativeArg::string(key)];
    unsafe { native::invoke(0xE459C941431E0FFA, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_is_array_valid(file_dict: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(file_dict)];
    unsafe { native::invoke(0xB04B69CF277D15C0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_is_dict_valid(file_dict: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(file_dict)];
    unsafe { native::invoke(0x4607D57C5F7D332A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datadict_set_int(object_data: &mut Any, key: &str, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(object_data), NativeArg::string(key), NativeArg::int(value)];
    unsafe { native::invoke(0x26FDF5E99AA2F3E9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datafile_create(index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0x56B7291FB953DD51, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datafile_delete(index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0x9FB90EEDEA9F2D5C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datafile_delete_requested_file(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x604B8ED1A482F9DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datafile_get_file_dict(index: i32) -> Result<u64, NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0xBBD8CF823CAE557C, &arguments) }.map(|result| result.as_u64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datafile_has_loaded_file_data(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x17279C820464CEE0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datafile_has_valid_file_data(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE60100389E50EADE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datafile_select_active_file(p0: NativeArg, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x46102A0989AD80B5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datafile_ugc_select_data(ugc_request_id: NativeArg, index: i32, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [ugc_request_id, NativeArg::int(index), p2];
    unsafe { native::invoke(0x790EC421078F5C4E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn datafile_watch_request_id(id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0xA5834834CA8FD7FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_is_file_loaded(file_handle: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(file_handle)];
    unsafe { native::invoke(0x603AC35FD4602C76, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_is_file_valid(file_handle: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(file_handle)];
    unsafe { native::invoke(0x7907969497EA92F5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_rq_fillout_hash(p0: &mut Hash, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xFBFF3FF2F5E80C0B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_rq_fillout_node(p0: &mut i32, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x83C3ED532B6E5D07, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_rq_fillout_string_127(p0: &str, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x951327435DC5164B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc2_set_player_data(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xE79C70E77E0973C7, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_c65_cc931_c0_f946_f(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x1C65CC931C0F946F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x277251_c161_b4_c3_f4(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x277251C161B4C3F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3168_ba5_d6_dece323() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3168BA5D6DECE323, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_f9_e3_ed7617123_ac(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x4F9E3ED7617123AC, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7681_b677400_c7071(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x7681B677400C7071, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_f130129_ebc31_b34(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x9F130129EBC31B34, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bc0_df006_a4952_c68(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xBC0DF006A4952C68, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ca56_dd6_ab7_a39_f64(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCA56DD6AB7A39F64, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e13634_bb6_baf0734(p0: i32, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1)];
    unsafe { native::invoke(0xE13634BB6BAF0734, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_get_bool(p0: &mut i32, p1: &mut Any, p2: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0xA63CD20F19B961AB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_get_entries(p0: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0xED4413CEE1BF142C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_get_file(p0: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0x91DED5DD64BB2691, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_get_float(p0: &mut Any, p1: &mut Any, p2: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0xB2B42607F7867576, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_get_int(p0: &mut Any, p1: &mut Any, p2: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0x52FC26D2D2FC2987, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_get_num_children(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6BEB168D5195E7AB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_get_section(p0: &mut Hash, p1: &mut Any, section: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::hash(section)];
    unsafe { native::invoke(0x44B3A36933AC009C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_load_file_hash(p0: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0xD97D8D905F1562F2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_register_query(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xAE156A747C39A741, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_rq_fillout_bool(p0: &mut i32, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x0D9138F3F8261DF7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_rq_fillout_float(p0: &mut f32, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x7F034FC3E891B57A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_rq_fillout_int(p0: &mut i32, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xEF44ACC657352A35, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_rq_fillout_string_63(p0: &str, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x08EAF8E9F2EB7B2E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_rq_fillout_vector(p0: &mut Vector3, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x06FBF89B12DA279C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_rq_get_num_nodes(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDF01B1F7A886B42D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn parseddata_unload_file(file_handle: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(file_handle)];
    unsafe { native::invoke(0x129567F0C05F81B9, &arguments) }.map(|_| ())
}

// ============================================================================
// DEBUG
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_game_version_name() -> Result<NativeString, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x05A5F662AD35C573, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a8_d970_d8_a72640_a6() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA8D970D8A72640A6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_acf9_cb705_befa8_cb() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xACF9CB705BEFA8CB, &arguments) }.map(|result| result.as_i64())
}

// ============================================================================
// DECORATOR
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_exist_on(entity: Entity, property_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name)];
    unsafe { native::invoke(0xD9D1CDBF3464DCDF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_get_bool(entity: Entity, property_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name)];
    unsafe { native::invoke(0xDEF3F1B071ABB197, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_get_float(entity: Entity, property_name: &str) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name)];
    unsafe { native::invoke(0xE5FF70CD842CA9D4, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_get_int(entity: Entity, property_name: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name)];
    unsafe { native::invoke(0x44DB62727762FD9B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_is_registered_as_type(property_name: &str, type_: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(property_name), NativeArg::int(type_)];
    unsafe { native::invoke(0x72355278C069F272, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_register(property_name: &str, type_: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(property_name), NativeArg::int(type_)];
    unsafe { native::invoke(0x0B253D644E3C36B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_remove(entity: Entity, property_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name)];
    unsafe { native::invoke(0x2BA7F5877A088A1D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_remove_all(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x88942780E0ADEA42, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_set_bool(entity: Entity, property_name: &str, value: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name), NativeArg::boolean(value)];
    unsafe { native::invoke(0xFE26E4609B1C3772, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_set_float(entity: Entity, property_name: &str, value: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name), NativeArg::float(value)];
    unsafe { native::invoke(0x238F8B0C1C7FE834, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_set_int(entity: Entity, property_name: &str, value: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name), NativeArg::int(value)];
    unsafe { native::invoke(0xE88F4D7F52A6090F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_set_string(entity: Entity, property_name: &str, value: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name), NativeArg::string(value)];
    unsafe { native::invoke(0x0671C1A3FF7AFDFC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_get_player_index(entity: Entity, property_name: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name)];
    unsafe { native::invoke(0xB1682B2443F0540B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_register_networked(property_name: &str, type_: i32, is_networked: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(property_name), NativeArg::int(type_), NativeArg::boolean(is_networked)];
    unsafe { native::invoke(0x4587374F88B7F6C2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn decor_set_player_index(entity: Entity, property_name: &str, value: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(property_name), NativeArg::int(value)];
    unsafe { native::invoke(0x4BDC83150D43772D, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// DLC
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_loading_screen_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x71D4BF5890659B0C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_dlc_present(dlc_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(dlc_hash)];
    unsafe { native::invoke(0x2763DC12BBE2BB6F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_special_edition_cash_camp_bonus_enabled() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1DB9D61E505AE3FC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_special_edition_core_stats_bonus_enabled() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA16B4FBA7887D7BA, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// ENTITY
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn apply_force_to_entity(entity: Entity, force_flags: i32, x: f32, y: f32, z: f32, off_x: f32, off_y: f32, off_z: f32, bone_index: i32, is_direction_rel: bool, ignore_up_vec: bool, is_force_rel: bool, p12: bool, p13: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(force_flags), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(off_x), NativeArg::float(off_y), NativeArg::float(off_z), NativeArg::int(bone_index), NativeArg::boolean(is_direction_rel), NativeArg::boolean(ignore_up_vec), NativeArg::boolean(is_force_rel), NativeArg::boolean(p12), NativeArg::boolean(p13)];
    unsafe { native::invoke(0xF15E8F5D333F09C4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn apply_force_to_entity_center_of_mass(entity: Entity, force_type: i32, x: f32, y: f32, z: f32, component: i32, is_direction_rel: bool, is_force_rel: bool, p8: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(force_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(component), NativeArg::boolean(is_direction_rel), NativeArg::boolean(is_force_rel), NativeArg::boolean(p8)];
    unsafe { native::invoke(0x31DA7CEC5334DB37, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_entity_to_entity(entity1: Entity, entity2: Entity, bone_index: i32, x_pos: f32, y_pos: f32, z_pos: f32, x_rot: f32, y_rot: f32, z_rot: f32, p9: bool, use_soft_pinning: bool, collision: bool, is_ped: bool, vertex_index: i32, fixed_rot: bool, p15: bool, p16: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::int(bone_index), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::boolean(p9), NativeArg::boolean(use_soft_pinning), NativeArg::boolean(collision), NativeArg::boolean(is_ped), NativeArg::int(vertex_index), NativeArg::boolean(fixed_rot), NativeArg::boolean(p15), NativeArg::boolean(p16)];
    unsafe { native::invoke(0x6B9BBD38AB0796DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_entity_to_entity_physically(entity1: Entity, entity2: Entity, p2: i32, bone_index: i32, offset_x: f32, offset_y: f32, offset_z: f32, p7: f32, p8: f32, p9: f32, p10: f32, p11: f32, p12: f32, p13: f32, p14: bool, p15: bool, p16: bool, p17: bool, p18: i32, p19: bool, p20: f32, p21: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::int(p2), NativeArg::int(bone_index), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(p7), NativeArg::float(p8), NativeArg::float(p9), NativeArg::float(p10), NativeArg::float(p11), NativeArg::float(p12), NativeArg::float(p13), NativeArg::boolean(p14), NativeArg::boolean(p15), NativeArg::boolean(p16), NativeArg::boolean(p17), NativeArg::int(p18), NativeArg::boolean(p19), NativeArg::float(p20), NativeArg::float(p21)];
    unsafe { native::invoke(0xB629A43CA1643481, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_entity_last_damage_entity(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xBB19AC7D4DCEFD0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_forced_object(x: f32, y: f32, z: f32, p3: NativeArg, model_hash: Hash, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), p3, NativeArg::hash(model_hash), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x0961A905AFBC34C7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_model_hide(x: f32, y: f32, z: f32, radius: f32, model: Hash, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::hash(model), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x069848B3FB3C4426, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_model_hide_excluding_script_objects(x: f32, y: f32, z: f32, radius: f32, model: Hash, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::hash(model), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xD136090A9AAAB17D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_model_swap(x: f32, y: f32, z: f32, radius: f32, original_model: Hash, new_model: Hash, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::hash(original_model), NativeArg::hash(new_model), NativeArg::boolean(p6)];
    unsafe { native::invoke(0x10B2218320B6F5AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_entity(entity: &mut Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(entity)];
    unsafe { native::invoke(0x4CD38C78BD19A497, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_entity(entity: Entity, p1: bool, collision: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1), NativeArg::boolean(collision)];
    unsafe { native::invoke(0x64CDE9D6BF8ECAD3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_entity_belong_to_this_script(entity: Entity, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x622B1980CBE13332, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_entity_exist(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xD42BD6EB2E0F1677, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_entity_have_drawable(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x20487F0DA9AF164A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_entity_have_physics(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xA512B3F1B2A0B51C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn find_anim_event_phase(anim_dictionary: &str, anim_name: &str, p2: &str, p3: &mut Any, p4: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(anim_dictionary), NativeArg::string(anim_name), NativeArg::string(p2), NativeArg::in_out(p3), NativeArg::in_out(p4)];
    unsafe { native::invoke(0x42718CC559BD7776, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_entity_ai_and_animation_update(entity: Entity, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x4C9E96473D4F1A88, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn freeze_entity_position(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x7D9EFB7AD6B19754, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_duration(anim_dict: &str, anim_name: &str) -> Result<f32, NativeError> {
    let arguments = [NativeArg::string(anim_dict), NativeArg::string(anim_name)];
    unsafe { native::invoke(0x9FFAF4940A54CC09, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_carriable_entity_state(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x61914209C36EFDDB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_alpha(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x1BB501624FAF2BEA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_attached_to(entity: Entity) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x56D713888A566481, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_bone_index_by_name(entity: Entity, bone_name: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(bone_name)];
    unsafe { native::invoke(0xBACA8FE9C76C124E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_collision_disabled(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xAA2FADD30F45A9DA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_coords(entity: Entity, alive: bool, real_coords: bool) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(alive), NativeArg::boolean(real_coords)];
    unsafe { native::invoke(0xA86D5F069399F44D, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_forward_vector(entity: Entity) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x2412D9C05BB09B97, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_forward_x(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xDB0954E9960F6457, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_forward_y(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x9A5C073ECBDA7EE7, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_heading(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xC230DD956E2F5507, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_health(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x82368787EA73C0F7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_height(entity: Entity, x: f32, y: f32, z: f32, at_top: bool, in_world_coords: bool) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(at_top), NativeArg::boolean(in_world_coords)];
    unsafe { native::invoke(0x296DEBC84474B375, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_height_above_ground(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x0D3B5BAEA08F63E9, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_lod_dist(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xDF240D0C2A948683, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_matrix(entity: Entity, right_vector: &mut Vector3, forward_vector: &mut Vector3, up_vector: &mut Vector3, position: &mut Vector3) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::in_out(right_vector), NativeArg::in_out(forward_vector), NativeArg::in_out(up_vector), NativeArg::in_out(position)];
    unsafe { native::invoke(0x3A9B1120AF13FBF2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_max_health(entity: Entity, p1: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x15D757606D170C3C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_model(entity: Entity) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xDA76A9F39210D365, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_pitch(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xEF355ABEFF7F5005, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_population_type(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xADE28862B6D7B85B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_roll(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xBF966536FA8B6879, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_rotation(entity: Entity, rotation_order: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(rotation_order)];
    unsafe { native::invoke(0xE09CAF86C32CB48F, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_speed(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xFB6BA510A533DF81, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_speed_vector(entity: Entity, relative: bool) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(relative)];
    unsafe { native::invoke(0xF2DB09816A419DC5, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_submerged_level(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x4A77C3F73FD9E831, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_type(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x97F696ACA466B4E0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_upright_value(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x56398BE65160C3BE, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_velocity(entity: Entity, p1: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1)];
    unsafe { native::invoke(0x4805D2B1D8CF94A9, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_animal(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x9A100F1CF4546629, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_matching_entities(volume: Volume, item_set: ItemSet, entity_type: i32, p3: NativeArg, p4: Hash, p5: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(item_set), NativeArg::int(entity_type), p3, NativeArg::hash(p4), NativeArg::string(p5)];
    unsafe { native::invoke(0x84CCF9A12942C83D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_nearest_participant_to_entity(entity: Entity) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x6888A43C35A5F630, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_nearest_player_to_entity(entity: Entity, player_ped_to_ignore: Ped, flags: i32) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(player_ped_to_ignore), NativeArg::int(flags)];
    unsafe { native::invoke(0x990E294FC387FB88, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_nearest_player_to_entity_on_team(entity: Entity, team: i32, player_ped_to_ignore: Ped, flags: i32) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(team), NativeArg::int(player_ped_to_ignore), NativeArg::int(flags)];
    unsafe { native::invoke(0xB2C30C3B4AFF718C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_object_index_from_entity_index(entity: Entity) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x280BBE5601EAA983, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_offset_from_entity_given_world_coords(entity: Entity, pos_x: f32, pos_y: f32, pos_z: f32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z)];
    unsafe { native::invoke(0x497C6B1A2C9AE69C, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_offset_from_entity_in_world_coords(entity: Entity, offset_x: f32, offset_y: f32, offset_z: f32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z)];
    unsafe { native::invoke(0x1899F328B0E12848, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_index_from_entity_index(entity: Entity) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x0F16D042BD640EA3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_index_from_entity_index(entity: Entity) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xDF1E5AAC561AFC59, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_world_position_of_entity_bone(entity: Entity, bone_index: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(bone_index)];
    unsafe { native::invoke(0x82CFA50E34681CA5, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_anim_event_fired(entity: Entity, action_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(action_hash)];
    unsafe { native::invoke(0x5851CC48405F4A07, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_collision_loaded_around_entity(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xBEB1600952B9CF5C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_collision_loaded_around_position(x_pos: f32, y_pos: f32, z_pos: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos)];
    unsafe { native::invoke(0x6BFBDC46139C45AB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_anim_finished(entity: Entity, anim_dict: &str, anim_name: &str, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(anim_dict), NativeArg::string(anim_name), NativeArg::int(p3)];
    unsafe { native::invoke(0xAEB40615337EF1E3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_been_damaged_by_any_object(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x73BB763880CD23A6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_been_damaged_by_any_ped(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x9934E9C42D52D87E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_been_damaged_by_any_vehicle(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x695D7C26DE65C423, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_been_damaged_by_entity(entity1: Entity, entity2: Entity, p2: bool, p3: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x7B6E7BEC1143AC86, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_clear_los_to_coord(entity: Entity, x: f32, y: f32, z: f32, flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(flags)];
    unsafe { native::invoke(0x0C9DBF48C6BA6E4C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_clear_los_to_entity(entity1: Entity, entity2: Entity, trace_type: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::int(trace_type)];
    unsafe { native::invoke(0xFCDFF7B72D23A1AC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_clear_los_to_entity_in_front(entity1: Entity, entity2: Entity, trace_type: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::int(trace_type)];
    unsafe { native::invoke(0xE88F19660651D566, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_collided_with_anything(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xDF18751EC74F90FF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_an_entity(handle: ScrHandle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(handle)];
    unsafe { native::invoke(0x27CFF3E5A286D3DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_an_object(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x0A27A546A375FDEF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_attached(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xEE6AD63ABF59C0B7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_attached_to_any_object(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x306C1F6178F01AB3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_attached_to_any_ped(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xC841153DED2CA89A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_attached_to_any_vehicle(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x12DF6E0D2E736749, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_attached_to_entity(from: Entity, to: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(from), NativeArg::int(to)];
    unsafe { native::invoke(0x154A3C529497053E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_at_coord(entity: Entity, x_pos: f32, y_pos: f32, z_pos: f32, x_size: f32, y_size: f32, z_size: f32, p7: bool, p8: bool, p9: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::float(x_size), NativeArg::float(y_size), NativeArg::float(z_size), NativeArg::boolean(p7), NativeArg::boolean(p8), NativeArg::int(p9)];
    unsafe { native::invoke(0x5E58342602E94718, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_at_entity(entity1: Entity, entity2: Entity, x_size: f32, y_size: f32, z_size: f32, p5: bool, p6: bool, p7: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::float(x_size), NativeArg::float(y_size), NativeArg::float(z_size), NativeArg::boolean(p5), NativeArg::boolean(p6), NativeArg::int(p7)];
    unsafe { native::invoke(0xC057F02B837A27F6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_a_mission_entity(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x138190F64DB4BBD1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_a_ped(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xCF8176912DDA4EA5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_a_vehicle(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xC3D96AF45FCCEC4C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_dead(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x7D5B1F88E7504BBA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_in_air(entity: Entity, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), p1];
    unsafe { native::invoke(0x886E37EC497200B6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_in_angled_area(entity: Entity, origin_x: f32, origin_y: f32, origin_z: f32, edge_x: f32, edge_y: f32, edge_z: f32, angle: f32, p8: bool, p9: bool, p10: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(origin_x), NativeArg::float(origin_y), NativeArg::float(origin_z), NativeArg::float(edge_x), NativeArg::float(edge_y), NativeArg::float(edge_z), NativeArg::float(angle), NativeArg::boolean(p8), NativeArg::boolean(p9), p10];
    unsafe { native::invoke(0xD3151E53134595E5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_in_area(entity: Entity, x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, p7: bool, p8: bool, p9: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::boolean(p7), NativeArg::boolean(p8), p9];
    unsafe { native::invoke(0x0C2634C40A16193E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_in_volume(entity: Entity, volume: ScrHandle, p2: bool, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(volume), NativeArg::boolean(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x5A5526BC09C06623, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_in_water(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xDDE5C125AC446723, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_occluded(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x140188E884645624, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_on_screen(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x613C15D5D8DB781F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_playing_anim(entity: Entity, anim_dict: &str, anim_name: &str, anim_type: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(anim_dict), NativeArg::string(anim_name), NativeArg::int(anim_type)];
    unsafe { native::invoke(0xDEE49D5CA6C49148, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_static(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x86468ADFA0F6B861, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_touching_entity(entity: Entity, target_entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(target_entity)];
    unsafe { native::invoke(0x9A2304A64C3C8423, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_touching_model(entity: Entity, model_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(model_hash)];
    unsafe { native::invoke(0x2AE3EBC8DEB9768B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_upright(entity: Entity, angle: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(angle)];
    unsafe { native::invoke(0xF6F6AFD8D4FB2658, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_upsidedown(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x109DE3DA41AAD94A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_visible(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xFFC96ECB7FA404CA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_visible_to_script(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xF213C724E77F321A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_waiting_for_world_collision(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x5E1CC2E8DC3111DD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_map_entity_pinned(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1FF441D7954F8709, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pin_closest_map_entity(model_hash: Hash, x: f32, y: f32, z: f32, flags: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(flags)];
    unsafe { native::invoke(0x6F3068258A499E52, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn place_entity_on_ground_properly(entity: Entity, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x9587913B9E772D29, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_entity_anim(entity: Entity, anim_name: &str, anim_dict: &str, p3: f32, loop_: bool, stay_in_anim: bool, p6: bool, delta: f32, bitset: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(anim_name), NativeArg::string(anim_dict), NativeArg::float(p3), NativeArg::boolean(loop_), NativeArg::boolean(stay_in_anim), NativeArg::boolean(p6), NativeArg::float(delta), bitset];
    unsafe { native::invoke(0xDC6D22FAB76D4874, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_forced_object(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x553FA683F2BCD814, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_model_hide(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x3F38A98576F6213A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_model_swap(x: f32, y: f32, z: f32, radius: f32, original_model: Hash, new_model: Hash, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::hash(original_model), NativeArg::hash(new_model), NativeArg::boolean(p6)];
    unsafe { native::invoke(0x824E1C26A14CB817, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_entity_alpha(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x744B9EF44779D9AB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn script_override_entity_loot_table_permanent(entity: Entity, loot_table: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(loot_table)];
    unsafe { native::invoke(0x8C03CD6B5E0E85E8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_can_auto_vault_on_entity(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x80646744FA88F9D7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_can_climb_on_entity(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x24AED2A608F93C4C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_alpha(entity: Entity, alpha_level: i32, skin: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(alpha_level), NativeArg::boolean(skin)];
    unsafe { native::invoke(0x0DF7692B1D9E7BA7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_always_prerender(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xACAD101E1FB66689, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_as_mission_entity(entity: Entity, script_host_object: bool, grab_from_other_script: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(script_host_object), NativeArg::boolean(grab_from_other_script)];
    unsafe { native::invoke(0xDC19C288082E586E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_as_no_longer_needed(entity: &mut Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(entity)];
    unsafe { native::invoke(0x4971D2F8162B9674, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_can_be_damaged(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x0D06D522B90E861F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_can_be_damaged_by_relationship_group(entity: Entity, b_can_be_damaged: bool, rel_group: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(b_can_be_damaged), NativeArg::hash(rel_group)];
    unsafe { native::invoke(0x0EF1AFB18649E015, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_can_be_targeted_without_los(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6D09F32E284D0FB7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_collision(entity: Entity, toggle: bool, keep_physics: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle), NativeArg::boolean(keep_physics)];
    unsafe { native::invoke(0xF66F820909453B8C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_completely_disable_collision(entity: Entity, toggle: bool, keep_physics: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle), NativeArg::boolean(keep_physics)];
    unsafe { native::invoke(0xE0580EC84813875A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_coords(entity: Entity, x_pos: f32, y_pos: f32, z_pos: f32, x_axis: bool, y_axis: bool, z_axis: bool, clear_area: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis), NativeArg::boolean(clear_area)];
    unsafe { native::invoke(0x06843DA7060A026B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_coords_no_offset(entity: Entity, x_pos: f32, y_pos: f32, z_pos: f32, x_axis: bool, y_axis: bool, z_axis: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis)];
    unsafe { native::invoke(0x239A3351AC1DA385, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_dynamic(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xFBFC4473F66CE344, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_has_gravity(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x0CEDB728A1083FA7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_heading(entity: Entity, heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(heading)];
    unsafe { native::invoke(0xCF2B9C0645C4651B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_health(entity: Entity, health_amount: i32, entity_killed_by: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(health_amount), NativeArg::int(entity_killed_by)];
    unsafe { native::invoke(0xAC2767ED8BDFAB15, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_invincible(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xA5C38736C426FCB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_is_target_priority(entity: Entity, p1: bool, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0x0A5D170C44CB2189, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_load_collision_flag(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x9B9EE31AED48072E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_lod_dist(entity: Entity, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(value)];
    unsafe { native::invoke(0x5FB407F0A7C877BF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_max_health(entity: Entity, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(value)];
    unsafe { native::invoke(0x166E7CF68597D8B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_motion_blur(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x516C6ABD18322B63, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_noweapondecals(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC64E597783BE9A1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_no_collision_entity(entity1: Entity, entity2: Entity, this_frame_only: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::boolean(this_frame_only)];
    unsafe { native::invoke(0xE037BF068223C38D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_only_damaged_by_player(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x473598683095D430, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_only_damaged_by_relationship_group(entity: Entity, p1: bool, relationship_group: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1), NativeArg::hash(relationship_group)];
    unsafe { native::invoke(0x6C1F6AA2F0ADD104, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_proofs(entity: Entity, proofs_bitset: i32, special_flag: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(proofs_bitset), NativeArg::boolean(special_flag)];
    unsafe { native::invoke(0xFAEE099C6F890BB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_quaternion(entity: Entity, x: f32, y: f32, z: f32, w: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(w)];
    unsafe { native::invoke(0x100E7007D13E3687, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_render_scorched(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x85B8A7534E44BC23, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_requires_more_expensive_river_check(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x850C940EE3E7B8B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_rotation(entity: Entity, pitch: f32, roll: f32, yaw: f32, rotation_order: i32, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(pitch), NativeArg::float(roll), NativeArg::float(yaw), NativeArg::int(rotation_order), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x9CC8314DFEDE441E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_should_freeze_waiting_on_collision(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x740CB4F3F602C9F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_velocity(entity: Entity, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x1C99BB7B6E96D16F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_visible(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x1794B4FCC84D812F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_as_no_longer_needed(object: &mut Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(object)];
    unsafe { native::invoke(0x3AE22DEB5BA5A3E6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_as_no_longer_needed(ped: &mut Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(ped)];
    unsafe { native::invoke(0x2595DD4236549CE3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_as_no_longer_needed(vehicle: &mut Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(vehicle)];
    unsafe { native::invoke(0x629BFA74418D6239, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_entity_anim(entity: Entity, animation: &str, anim_group: &str, p3: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(animation), NativeArg::string(anim_group), NativeArg::float(p3)];
    unsafe { native::invoke(0x786591D986DE9159, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn would_entity_be_occluded(entity_model_hash: Hash, x: f32, y: f32, z: f32, p4: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(entity_model_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x3546FAB293FF2981, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x002_aac783_ed323_ed(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x002AAC783ED323ED, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x007_aac783_ed323_ed(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x007AAC783ED323ED, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0939_e773925_c4719() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0939E773925C4719, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_ccefc6_c2_c95_da2_a(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x0CCEFC6C2C95DA2A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_db41_d59_e0_f1502_b(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0DB41D59E0F1502B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_fd7_d7_c232876_e72(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0FD7D7C232876E72, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x120376_c23_f019_c6_c(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x120376C23F019C6C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x16908_e859_c3_ab698(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x16908E859C3AB698, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x188736456_d1_dede6(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x188736456D1DEDE6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x20_faee47427_a4497() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x20FAEE47427A4497, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_a77_ef9_bec8518_f4(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2A77EF9BEC8518F4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_d40_bcbfe9305_dea(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x2D40BCBFE9305DEA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x350_e9211074955_af(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x350E9211074955AF, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x371_d179701_d9_c082(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x371D179701D9C082, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x37_b01666_bae8_f7_ef(entity: Entity) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x37B01666BAE8F7EF, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x37_ceb637_ba3_b1_a47(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x37CEB637BA3B1A47, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x383_f64263_f946_e45(p0: &mut i32, entity: Entity, p2: i32, ped: Ped, p4: NativeArg, p5: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::int(entity), NativeArg::int(p2), NativeArg::int(ped), p4, NativeArg::int(p5)];
    unsafe { native::invoke(0x383F64263F946E45, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_ab3_a77672_f6473_f(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Vector3, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x3AB3A77672F6473F, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_ec28_da1_ffac9_ddd(entity1: Entity, entity2: Entity, p2: NativeArg, p3: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), p2, p3];
    unsafe { native::invoke(0x3EC28DA1FFAC9DDD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_f08_c6163_a4_ab1_d6(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3F08C6163A4AB1D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x56_e0735_d6273_b227(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x56E0735D6273B227, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5744562_e973_e33_cd(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x5744562E973E33CD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5826_efd6_d73_c4_de5(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x5826EFD6D73C4DE5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x582_f73_acfe969571(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x582F73ACFE969571, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_e214112806591_ea(entity: Entity, bone_index: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(bone_index)];
    unsafe { native::invoke(0x5E214112806591EA, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c31_b06_e91518269(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6C31B06E91518269, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_d58167_f62238284(vehicle: Vehicle) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x6D58167F62238284, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_a49_d40_de437_bc8_d(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x7A49D40DE437BC8D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_f20092547_b4_ddea(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7F20092547B4DDEA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x80_fdeb3_a9_e9_aa578(entity: Entity, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x80FDEB3A9E9AA578, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x898586729_db5221_d(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x898586729DB5221D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_e10_df0_ffa63_fb65(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x8E10DF0FFA63FB65, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_e46_e18_aa828334_f(entity: Entity, anim_dict: &str, anim_clip: &str) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(anim_dict), NativeArg::string(anim_clip)];
    unsafe { native::invoke(0x8E46E18AA828334F, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x978_aa2323_ed32209(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x978AA2323ED32209, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_c6906_ef8_cb20_c5_f(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x9C6906EF8CB20C5F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a48_e4801_debdf7_e4(entity: Entity, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xA48E4801DEBDF7E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a9_e6_d8_f2_ddfc4_db9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA9E6D8F2DDFC4DB9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_aaacb74442_c1_bed3(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xAAACB74442C1BED3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_af72_ec7_e1_b54539_b(entity: Entity) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xAF72EC7E1B54539B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_af7_f3099_b9_feb535(entity: Entity, p1: f32, p2: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0xAF7F3099B9FEB535, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b16_c780_c51_e51_e2_b(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xB16C780C51E51E2B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b38_a29_ccd5447783(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xB38A29CCD5447783, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ba2_a089_e60_ed1163(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xBA2A089E60ED1163, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bd94_cecfb2_d65119(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xBD94CECFB2D65119, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c0_edef16_d90661_ee(entity: Entity, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(p1)];
    unsafe { native::invoke(0xC0EDEF16D90661EE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c2_e71_d7_e0_a7_b4_c89(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC2E71D7E0A7B4C89, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c3_abcfbc7_d74_afa5(ped: Ped, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xC3ABCFBC7D74AFA5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c6_a1_a3_d63_f122_de7(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC6A1A3D63F122DE7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c76_e94_a78127412_b(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xC76E94A78127412B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cdb682_bb47_c02_f0_a(entity: Entity, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(p1)];
    unsafe { native::invoke(0xCDB682BB47C02F0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d21_c7418_c590_bb40(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD21C7418C590BB40, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d45_bb89_b53_fc0_cfd(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0xD45BB89B53FC0CFD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d4636_c2_edb0_dea8_a(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD4636C2EDB0DEA8A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d46_bf94_c4_c66_fab0(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xD46BF94C4C66FAB0, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dd03_fc2089_ad093_c(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xDD03FC2089AD093C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_df8_e49_ea89_a01_db1(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xDF8E49EA89A01DB1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dfc2_b226_d56_d85_f6(p0: NativeArg, p1: NativeArg) -> Result<f32, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDFC2B226D56D85F6, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e19035_eb65_ab2932(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE19035EB65AB2932, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e31_fc20319874_cb3(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xE31FC20319874CB3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e75_eea8_db59_a9_f39(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xE75EEA8DB59A9F39, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e9_e7_a0_bac7_f57746(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE9E7A0BAC7F57746, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eab3_d91_d30_a344_f1(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xEAB3D91D30A344F1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ef259_aa1_e097_e0_ad(entity: Entity, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), p1];
    unsafe { native::invoke(0xEF259AA1E097E0AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f41_e2979_d5_bc5370(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF41E2979D5BC5370, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f59_fde7_b4_d31_a630(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF59FDE7B4D31A630, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ff83_af534156_b399(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFF83AF534156B399, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ff9965_c47_fa404_da(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xFF9965C47FA404DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_entity_tracking_trails(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x1AD922AB5038DEF3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_entity_to_coords_physically(entity: Entity, p1: i32, x: f32, y: f32, z: f32, offset_x: f32, offset_y: f32, offset_z: f32, timer: f32, p9: bool, p10: i32, p11: i32, p12: i32, p13: i32, p14: i32, p15: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(timer), NativeArg::boolean(p9), NativeArg::int(p10), NativeArg::int(p11), NativeArg::int(p12), NativeArg::int(p13), NativeArg::int(p14), NativeArg::int(p15)];
    unsafe { native::invoke(0x445D7D8EA66E373E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn change_entity_health(entity: Entity, amount: f32, entity2: Entity, weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(amount), NativeArg::int(entity2), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x835F131E7DC8F97A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_footpath_trail(p0: NativeArg, waypoint_record: &str, b_use_snow_offset: bool, p3: f32, p4: f32, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, b_init: bool) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::string(waypoint_record), NativeArg::boolean(b_use_snow_offset), NativeArg::float(p3), NativeArg::float(p4), p5, p6, p7, p8, p9, p10, NativeArg::boolean(b_init)];
    unsafe { native::invoke(0x29BA9F78321E5A6C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_carriable(entity: &mut Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(entity)];
    unsafe { native::invoke(0x0D0DB2B6AF19A987, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_entity_2(entity: &mut Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(entity)];
    unsafe { native::invoke(0x5E94EA09E7207C16, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_thread_own_this_entity(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x88AD6CC10D8D35B2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn find_entity_looting_ped(entity: Entity) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xEF2D9ED7CE684F08, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_train_wagon_population(train_wagon: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train_wagon), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x119A5714578F4E05, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_carriable_from_entity(entity: Entity) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x31FEF6A20F00B963, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entities_in_volume(volume: Volume, item_set: ItemSet, entity_type: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(item_set), NativeArg::int(entity_type)];
    unsafe { native::invoke(0x886171A12F400B89, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entities_near_point(x: f32, y: f32, z: f32, radius: f32, item_set: ItemSet, p5: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(item_set), NativeArg::int(p5)];
    unsafe { native::invoke(0x59B57C4B06531E1E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_anim_current_time(entity: Entity, anim_dict: &str, anim_name: &str) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(anim_dict), NativeArg::string(anim_name)];
    unsafe { native::invoke(0x627520389E288A73, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_by_doorhash(door_hash: Hash, p1: i32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::hash(door_hash), NativeArg::int(p1)];
    unsafe { native::invoke(0xF7424890E4A094C0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_can_be_damaged(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x75DF9E73F2F005FD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_carrying_flag(entity: Entity, flag_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(flag_id)];
    unsafe { native::invoke(0x808077647856DE62, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_carry_config(entity: Entity) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x0FD25587BB306C86, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_forward_vector_yx(entity: Entity) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x935A30AA88FB1014, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_health_float(entity: Entity) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x96C638784DB4C815, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_proofs(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x6CF0DAD7FA1088EA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_script(entity: Entity, arg_struct: &mut Any) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::in_out(arg_struct)];
    unsafe { native::invoke(0x2A08A32B6D49906F, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_threat_tier(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xE12F56CB25D9CE23, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_world_position_of_dimensions(entity: Entity, minimum: &mut Vector3, maximum: &mut Vector3) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::in_out(minimum), NativeArg::in_out(maximum)];
    unsafe { native::invoke(0xF3FDA9A617A15145, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_bird(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xC346A546612C49A9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_carriable_pelt(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x255B6DB4E3AD3C3E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_predator(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x5594AFE9DE0C01B7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_optimal_carry_config(entity: Entity, index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(index)];
    unsafe { native::invoke(0x34F008A7E48C496B, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_animal_type(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x964000D355219FC0, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_pinned_map_entity(p0: NativeArg) -> Result<Entity, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x4735E2A4BB83D9DA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_script_override_entity_loot_table_permanent(entity: Entity, loot_table: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::in_out(loot_table)];
    unsafe { native::invoke(0x1E804EA9B12030A4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_carriable_model(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x5AFFA9DDC87846F8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_frozen(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x083D497D57B7400F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_fully_looted(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x8DE41E9902E85756, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_on_train_track(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x857ACB0AB4BD0D55, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_owned_by_persistence_system(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xA7E51B53309EAC97, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_playing_any_anim(entity: Entity, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1)];
    unsafe { native::invoke(0x0B7CB1300CBFE19C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_underwater(entity: Entity, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xD4E5C1E93C466127, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_tracked_entity_visible(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xC8CCDB712FBCBA92, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pause_entity_tracking(entity: Entity, pause: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(pause)];
    unsafe { native::invoke(0x36EB4D34D4A092C5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_entity_loot_list(mount: Ped, arg_struct: &mut Any, visiblelootslotrequest_type: Hash, flag: i32, p4: i32, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::in_out(arg_struct), NativeArg::hash(visiblelootslotrequest_type), NativeArg::int(flag), NativeArg::int(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xA88E215CEB0435C0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn search_building_pool_for_entity_with_this_model(model_hash: Hash) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::hash(model_hash)];
    unsafe { native::invoke(0x66B2B83B94B22458, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_anim_current_time(entity: Entity, anim_dict: &str, anim_name: &str, time: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(anim_dict), NativeArg::string(anim_name), NativeArg::float(time)];
    unsafe { native::invoke(0x11CDABDC7783B2BC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_anim_speed(entity: Entity, anim_dict: &str, anim_name: &str, speed_multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(anim_dict), NativeArg::string(anim_name), NativeArg::float(speed_multiplier)];
    unsafe { native::invoke(0xEAA885BA3CEA4E4A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_carcass_type(entity: Entity, type_: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(type_)];
    unsafe { native::invoke(0x399657ED871B3A6C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_carrying_flag(entity: Entity, flag_id: i32, value: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(flag_id), NativeArg::boolean(value)];
    unsafe { native::invoke(0x18FF3110CF47115D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_coords_and_heading(entity: Entity, x_pos: f32, y_pos: f32, z_pos: f32, heading: f32, x_axis: bool, y_axis: bool, z_axis: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::float(heading), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis)];
    unsafe { native::invoke(0x203BEFFDBE12E96A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_coords_and_heading_no_offset(entity: Entity, x_pos: f32, y_pos: f32, z_pos: f32, heading: f32, p5: bool, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::float(heading), NativeArg::boolean(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0x0918E3565C20F03C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_custom_pickup_radius(entity: Entity, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(radius)];
    unsafe { native::invoke(0x482D17E45665DA44, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_fade_in(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xA91E6CF94404E8C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_fully_looted(entity: Entity, looted: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(looted)];
    unsafe { native::invoke(0x6BCF5F3D8FFE988D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_lights_enabled(entity: Entity, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0xEBDC12861D079ABA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_threat_tier(entity: Entity, tier: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(tier), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x4B436BAC8CBE9B07, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_material_fill_level_for_entity(entity: Entity, expression_type: i32, dof_name: &str, fill_state: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(expression_type), NativeArg::string(dof_name), NativeArg::float(fill_state)];
    unsafe { native::invoke(0x669655FFB29EF1A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unpin_map_entity(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xD2B9C78537ED5759, &arguments) }.map(|_| ())
}

// ============================================================================
// EVENT
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_shocking_event_at_position(event_type: Hash, x: f32, y: f32, z: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: i32, p10: i32) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::hash(event_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), NativeArg::int(p9), NativeArg::int(p10)];
    unsafe { native::invoke(0xD9F8455409B525E9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_shocking_event_for_entity(event_type: Hash, entity: Entity, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: bool, p9: bool, p10: i32, p11: i32) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::hash(event_type), NativeArg::int(entity), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::boolean(p8), NativeArg::boolean(p9), NativeArg::int(p10), NativeArg::int(p11)];
    unsafe { native::invoke(0x7FD8F3BE76F89422, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_shocking_event_in_sphere(event_type: Hash, x: f32, y: f32, z: f32, radius: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(event_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0x9DB47E16060D6354, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_all_shocking_events(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xD47A168C2AB90DC4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_all_shocking_events_of_type(event_type: Hash, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(event_type), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x118873DD538490B4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_shocking_event(event: ScrHandle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(event)];
    unsafe { native::invoke(0xE8BB3CC253A34559, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_shocking_event_spawn_blocking_areas() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDB249021652420C5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_decision_maker(ped: Ped, name: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(name)];
    unsafe { native::invoke(0x8AE2F981CDDB8FA4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_decision_maker_to_default(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6B9C5C38838FB6E6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn suppress_shocking_events_next_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x84994FAD4E4E4E69, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x18_e93_ebfc1_fcfa48(volume: Volume, p1: bool, p2: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x18E93EBFC1FCFA48, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_a5_c5_d350068_a673(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x1A5C5D350068A673, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_d1_b448_d719415_ab(ped: Ped) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1D1B448D719415AB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x26054_eb81_ac0893_b(object: Object) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x26054EB81AC0893B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_dd42_fad06_e6_f19_e(object: Object, p1: bool, p2: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x2DD42FAD06E6F19E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x36_d0_f2_ba2_c0_d9_bde(entity: Entity, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1)];
    unsafe { native::invoke(0x36D0F2BA2C0D9BDE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4465_c3_d1475_bd3_fd(model: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x4465C3D1475BD3FD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_b2_b1_a891_d437_ca7(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x4B2B1A891D437CA7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x56_b3410626_a473_e7(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x56B3410626A473E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_c511_e91738_a0828(ped1: Ped, ped2: Ped, p2: i32, p3: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2), NativeArg::int(p2), NativeArg::hash(p3)];
    unsafe { native::invoke(0x7C511E91738A0828, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x83_d43_f0_fd5276_e4_d(entity: Entity, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1)];
    unsafe { native::invoke(0x83D43F0FD5276E4D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9520175_b35_e2268_d(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x9520175B35E2268D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a86_b0_ee9_b39_d15_d6(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0xA86B0EE9B39D15D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ad17_a18215_dd23_d6(entity: Entity, p1: i32, p2: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xAD17A18215DD23D6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b6_f4825153920582() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB6F4825153920582, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e28_d7_fc9_fd32_abeb(entity: Entity, event_type: Hash, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(event_type), NativeArg::int(p2)];
    unsafe { native::invoke(0xE28D7FC9FD32ABEB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e2_c2_fbb7825_ffc66() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE2C2FBB7825FFC66, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_model_to_event_monitor(model: Hash, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x608AD36A644A97FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_shocking_event(args: &mut Any) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0xCA1315C33B9A2847, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn event_flush_all_event_trackers(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xAD8F2424C6E1E3A8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn event_get_recent_event(entity: Entity, p1: i32, p2: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x796EECFF0C6D39BE, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn event_get_source_entity_from_event(entity: Entity, event_type: Hash, p2: i32, p3: i32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(event_type), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x822A001BCEA5BD81, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn event_get_target_entity_from_event(entity: Entity, event_type: Hash, p2: i32, p3: i32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(event_type), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x38497F139981C5C9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn event_get_time_since_event(entity: Entity, event_type: Hash, p2: i32, p3: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(event_type), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0xC6A7DC546E94FED5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_event_tracker_active(event_name: &str, shocking_event: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(event_name), NativeArg::hash(shocking_event)];
    unsafe { native::invoke(0x797B3D4D92E56094, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_all_shocking_events_in_area(x: f32, y: f32, z: f32, radius: f32, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xB4C71BA9CAB097BD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_all_shocking_events_of_type_in_area(event_type: Hash, x: f32, y: f32, z: f32, radius: f32, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(event_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x6A648D42BF271DC7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_event_tracker_for_ped(ped: Ped, event_name: &str, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(event_name), NativeArg::int(p2)];
    unsafe { native::invoke(0xBB1E41DD3D3C6250, &arguments) }.map(|_| ())
}

// ============================================================================
// FIRE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_explosion(x: f32, y: f32, z: f32, explosion_type: i32, damage_scale: f32, is_audible: bool, is_invisible: bool, camera_shake: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(explosion_type), NativeArg::float(damage_scale), NativeArg::boolean(is_audible), NativeArg::boolean(is_invisible), NativeArg::float(camera_shake)];
    unsafe { native::invoke(0x7D6F58F69DA92530, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_explosion_with_user_vfx(x: f32, y: f32, z: f32, explosion_type: i32, explosion_fx: Hash, damage_scale: f32, is_audible: bool, is_invisible: bool, camera_shake: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(explosion_type), NativeArg::hash(explosion_fx), NativeArg::float(damage_scale), NativeArg::boolean(is_audible), NativeArg::boolean(is_invisible), NativeArg::float(camera_shake)];
    unsafe { native::invoke(0x53BA259F3A67A99E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_owned_explosion(ped: Ped, x: f32, y: f32, z: f32, explosion_type: i32, damage_scale: f32, is_audible: bool, is_invisible: bool, camera_shake: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(explosion_type), NativeArg::float(damage_scale), NativeArg::boolean(is_audible), NativeArg::boolean(is_invisible), NativeArg::float(camera_shake)];
    unsafe { native::invoke(0xD84A917A64D4D016, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_closest_fire_pos(out_position: &mut Vector3, x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_position), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xB646FB657F448261, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_number_of_fires_in_range(x: f32, y: f32, z: f32, radius: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0xF9617BC6FAE61E08, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_owner_of_explosion_in_angled_area(explosion_type: i32, x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, radius: f32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(explosion_type), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::float(radius)];
    unsafe { native::invoke(0x8002DDAB58594D78, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_on_fire(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x1BD7C371CE257C3E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_explosion_active_in_area(explosion_type: i32, x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(explosion_type), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2)];
    unsafe { native::invoke(0xD96E82AEBFFAAFF0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_explosion_in_angled_area(explosion_type: i32, x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, angle: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(explosion_type), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::float(angle)];
    unsafe { native::invoke(0x5AE661ECD18524C9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_explosion_in_area(explosion_type: i32, x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(explosion_type), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2)];
    unsafe { native::invoke(0x8391BA4313A25AD3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_explosion_in_sphere(explosion_type: i32, x: f32, y: f32, z: f32, radius: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(explosion_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0xD62DD846D82CBB90, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_script_fire(fire_handle: FireId) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(fire_handle)];
    unsafe { native::invoke(0x790125C36E194069, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_entity_fire(entity: Entity, intensity: f32, bone_index: i32, fire_flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(intensity), NativeArg::int(bone_index), NativeArg::int(fire_flags)];
    unsafe { native::invoke(0xC4DC7418A44D6822, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_script_fire(x: f32, y: f32, z: f32, p3: i32, p4: f32, p5: bool, soundset_name: &str, p7: f32, p8: i32) -> Result<FireId, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(p3), NativeArg::float(p4), NativeArg::boolean(p5), NativeArg::string(soundset_name), NativeArg::float(p7), NativeArg::int(p8)];
    unsafe { native::invoke(0x6B83617E04503888, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_entity_fire(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8390751DC40C1E98, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_fire_in_range(x: f32, y: f32, z: f32, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0xDB38F247BD421708, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x24_db6_b9_f2_b719043(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x24DB6B9F2B719043, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x41_b87_a6495_ee13_dd(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9];
    unsafe { native::invoke(0x41B87A6495EE13DD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x68_f6_a75_fdf5_a70_d6(x: f32, y: f32, z: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3)];
    unsafe { native::invoke(0x68F6A75FDF5A70D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x754937_c28271_bc65(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x754937C28271BC65, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_explosion_with_damage_causer(entity: Entity, p1: i32, x: f32, y: f32, z: f32, explosion_type: i32, damage_scale: f32, is_audible: bool, is_invisible: bool, camera_shake: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(explosion_type), NativeArg::float(damage_scale), NativeArg::boolean(is_audible), NativeArg::boolean(is_invisible), NativeArg::float(camera_shake)];
    unsafe { native::invoke(0xB7DF150605EEDC9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_explosion_with_user_vfx_and_damage_causer(entity: Entity, p1: bool, x: f32, y: f32, z: f32, explosion_type: i32, explosion_fx: Hash, damage_scale: f32, is_audible: bool, is_invisible: bool, camera_shake: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(explosion_type), NativeArg::hash(explosion_fx), NativeArg::float(damage_scale), NativeArg::boolean(is_audible), NativeArg::boolean(is_invisible), NativeArg::float(camera_shake)];
    unsafe { native::invoke(0x34AE85C7CA4857AA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_closest_fire_pos_in_volume(out_position: &mut Vector3, pos_x: f32, pos_y: f32, pos_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_position), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z)];
    unsafe { native::invoke(0x559FC1D310813031, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_being_damaged_by_fire(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xA4454592DCF7C992, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_consumed_by_fire(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xCDC25355C0D65963, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_explosion_in_volume(explosion_type: i32, volume: Volume) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(explosion_type), NativeArg::int(volume)];
    unsafe { native::invoke(0xE24822A4CFC9107A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_shocking_event_active(ped: Ped, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xAB7993BA61A4674F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_fire_in_box(pos_x: f32, pos_y: f32, pos_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z)];
    unsafe { native::invoke(0xB7C7BDC375AEA9A4, &arguments) }.map(|_| ())
}

// ============================================================================
// FLOCK
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_animal_tuning_bool_param(animal: Ped, index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::int(index)];
    unsafe { native::invoke(0x1C1993824A396603, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_animal_tuning_float_param(animal: Ped, index: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::int(index)];
    unsafe { native::invoke(0x4BC3ECFDA0297E27, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_species_tuning_float_param(p0: Hash, p1: i32, p2: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xE108489621422F91, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_animal_tuning_bool_param(animal: Ped, index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::int(index)];
    unsafe { native::invoke(0x96AA1304D30E6BC3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_animal_tuning_float_param(animal: Ped, index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::int(index)];
    unsafe { native::invoke(0xE776A195488FC520, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_animal_tuning_bool_param(animal: Ped, index: i32, value: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::int(index), NativeArg::boolean(value)];
    unsafe { native::invoke(0x9FF1E042FA597187, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_animal_tuning_float_param(animal: Ped, index: i32, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::int(index), NativeArg::float(value)];
    unsafe { native::invoke(0xCBDA22C87977244F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_species_tuning_bool_param(p0: Hash, p1: i32, p2: i32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::int(p1), NativeArg::int(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x6D1D94C2459B42EE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_species_tuning_float_param(p0: Hash, p1: i32, p2: i32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::int(p1), NativeArg::int(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0x963240B6C252BA49, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0816_c31480764_ab0(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x0816C31480764AB0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x09_ee00_b8_f858_e0_be(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x09EE00B8F858E0BE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1520626_ffafffa8_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x1520626FFAFFFA8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x17_e3_e5_c46_eccd308(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x17E3E5C46ECCD308, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x19870_c40_c7_ee15_be(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x19870C40C7EE15BE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_da6_cb02071055_d5(p0: NativeArg) -> Result<Vector3, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1DA6CB02071055D5, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_df3_d457_d86_f8_e57(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x2DF3D457D86F8E57, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x34_b9_c4_d86_df2_c2_f3(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x34B9C4D86DF2C2F3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x36486_af7_da93_a464(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x36486AF7DA93A464, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x53187_e563_f938_e76(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x53187E563F938E76, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c57_bea886_a20_c6_b(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6C57BEA886A20C6B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x706_b434_fefad6_a24(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x706B434FEFAD6A24, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8049_b17_bec937662(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x8049B17BEC937662, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a881_f5_c77_a560906(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA881F5C77A560906, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c3_d581_a34_bc0_a1_f0(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC3D581A34BC0A1F0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c72_ce37081_dae625(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xC72CE37081DAE625, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c95611869_e14_f8_af(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xC95611869E14F8AF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cc6_b5_aafc87_bfc7_b(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xCC6B5AAFC87BFC7B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d95_f04_a4_e73_be85_e(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD95F04A4E73BE85E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e36_d2_cb540597_ef7(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0xE36D2CB540597EF7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e93415_b3307208_e5(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8];
    unsafe { native::invoke(0xE93415B3307208E5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f2_cca7_b68_cfab2_b9(species: Hash, x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, x3: f32, y3: f32, z3: f32, p10: f32, p11: f32, p12: f32, p13: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(species), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::float(x3), NativeArg::float(y3), NativeArg::float(z3), NativeArg::float(p10), NativeArg::float(p11), NativeArg::float(p12), NativeArg::float(p13)];
    unsafe { native::invoke(0xF2CCA7B68CFAB2B9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa821997794_f48_e7(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xFA821997794F48E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fb16_f08_f47_b83_b4_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xFB16F08F47B83B4C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fdb008_b3_bcf5992_f(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xFDB008B3BCF5992F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ff1_e339_ce40_eaaaf(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFF1E339CE40EAAAF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ped_to_flock(p0: NativeArg, ped: Ped) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(ped)];
    unsafe { native::invoke(0x933E5D31A7D13069, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_herd(herd_handle: ScrHandle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(herd_handle)];
    unsafe { native::invoke(0x67A43EA3F6FE0076, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_herd() -> Result<ScrHandle, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCB4EF7EDAE2E16F1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_herd(herd_handle: ScrHandle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(herd_handle)];
    unsafe { native::invoke(0xE0961AED72642B80, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_animal_is_wild(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3B005FF0538ED2A9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_animal_rarity(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF8B48A361DC388AE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_herd_valid(herd_handle: ScrHandle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(herd_handle)];
    unsafe { native::invoke(0x8D913E493BAFE0A3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_herd(herd_handle: ScrHandle, ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(herd_handle), NativeArg::int(ped)];
    unsafe { native::invoke(0x9E13ACC38BA8F9C3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_herd_ped(herd_handle: ScrHandle, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(herd_handle), NativeArg::int(ped)];
    unsafe { native::invoke(0x408D1149C5E39C1E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_animal_is_wild(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xAEB97D84CDF3C00B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_animal_rarity(ped: Ped, rarity_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(rarity_level)];
    unsafe { native::invoke(0x8B6F0F59B1B99801, &arguments) }.map(|_| ())
}

// ============================================================================
// GANG
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gang_id(player: Player) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x901E0DC25080C8B9, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gang_leader(gang_id: NativeArg) -> Result<Player, NativeError> {
    let arguments = [gang_id];
    unsafe { native::invoke(0x4BE6C13A45CCA8EC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_num_gang_members(gang_id: NativeArg) -> Result<i32, NativeError> {
    let arguments = [gang_id];
    unsafe { native::invoke(0x149A2751AB66AC02, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_gang_active(gang_id: NativeArg) -> Result<bool, NativeError> {
    let arguments = [gang_id];
    unsafe { native::invoke(0x0F99F6436528A089, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_gang_id_valid(gang_id: NativeArg) -> Result<bool, NativeError> {
    let arguments = [gang_id];
    unsafe { native::invoke(0xD6F6ACF4392187FB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_gang_in_session(gang_id: NativeArg) -> Result<bool, NativeError> {
    let arguments = [gang_id];
    unsafe { native::invoke(0x93A91A351A07360E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_gang_leader(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x424B17A7DC5C90BC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_in_same_gang(player1: Player, player2: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player1), NativeArg::int(player2)];
    unsafe { native::invoke(0x3F59FE6F37869576, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_e5_c9_fb9_ed5_dff1_c(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0E5C9FB9ED5DFF1C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_f11702_ddbd915_c6(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x1F11702DDBD915C6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_f7_eb8_b6_f6_afe79_c(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2F7EB8B6F6AFE79C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_adc71_a66356_d706() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3ADC71A66356D706, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x48_d82_c83987_e18_e4(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x48D82C83987E18E4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x51_c5_ef47086_aa0_d7() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x51C5EF47086AA0D7, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x53_a94294_fddcf98_c(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x53A94294FDDCF98C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6102830_f764_b3_de1(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x6102830F764B3DE1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x644_e02_f24_f9_d4_e98(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x644E02F24F9D4E98, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7933754_f260_b428_a(player: Player) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x7933754F260B428A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_baa30_c9_bbe8_aee7(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x7BAA30C9BBE8AEE7, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a9_ceae8_d6637_fbad(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA9CEAE8D6637FBAD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ad22_ab64_fa428_df3(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xAD22AB64FA428DF3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_afd3599_a3_cc5637_d() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAFD3599A3CC5637D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b22_b1_d9_f74095382(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xB22B1D9F74095382, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b38_c256498748413() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB38C256498748413, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c81_a9_e2_c8_efd28_d5(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC81A9E2C8EFD28D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_da801_f7_f6_a5278_d3(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xDA801F7F6A5278D3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e4_c64_cd37_cb176_aa(p0: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xE4C64CD37CB176AA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ee4_f20004_d0288_b7() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEE4F20004D0288B7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa7_c5_b7_e087_a4_ceb() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFA7C5B7E087A4CEB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gang_leader_handle(gang_id: NativeArg, gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [gang_id, NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xCE88A261DCBBA0D9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gang_members(gang_id: NativeArg, member_handles: &mut Any) -> Result<i32, NativeError> {
    let arguments = [gang_id, NativeArg::in_out(member_handles)];
    unsafe { native::invoke(0xD1BF325C8252A982, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gang_privacy() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9970AE8C3D706139, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gang_size(gang_id: NativeArg) -> Result<i32, NativeError> {
    let arguments = [gang_id];
    unsafe { native::invoke(0x853B0FA4D8732C57, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_gang_member(gang_id: NativeArg, player: Player) -> Result<bool, NativeError> {
    let arguments = [gang_id, NativeArg::int(player)];
    unsafe { native::invoke(0x9BE7DCB22D32CCBE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_gang_open(gang_id: NativeArg) -> Result<bool, NativeError> {
    let arguments = [gang_id];
    unsafe { native::invoke(0xFCF96CCBD81B24C8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_in_my_gang(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x81FB74C83C2ED69F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_kick_gang_member(player: Player, ban_time_seconds: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ban_time_seconds)];
    unsafe { native::invoke(0xCD9E2D9BC52FD80F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_leave_gang(disband: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(disband)];
    unsafe { native::invoke(0x0A04A07BC3074EDB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_request_gang_join(gang_id: NativeArg) -> Result<bool, NativeError> {
    let arguments = [gang_id];
    unsafe { native::invoke(0xC0474C8BCF6787AD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_gang_privacy(privacy_type: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(privacy_type)];
    unsafe { native::invoke(0xC5BF29F4035277C2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_gang_size(size: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(size)];
    unsafe { native::invoke(0x833D8268D51B4522, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_start_gang(open_status: bool, camp_size: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(open_status), NativeArg::int(camp_size)];
    unsafe { native::invoke(0xD1A226F2E05E58FC, &arguments) }.map(|_| ())
}

// ============================================================================
// GOOGLE_ANALYTICS
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn google_analytics_end_event() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x87BBCC4360A9BDE3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn google_analytics_pop_page(page_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(page_name)];
    unsafe { native::invoke(0xC6DE040378364798, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn google_analytics_push_page(page_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(page_name)];
    unsafe { native::invoke(0xD43A616AE3AC4EF6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn google_analytics_start_event(event_category: &str, event_action: &str, event_label: &str, event_value: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(event_category), NativeArg::string(event_action), NativeArg::string(event_label), NativeArg::int(event_value)];
    unsafe { native::invoke(0x1C54F031D7C0F7AC, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// GRAPHICS
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_decal(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg, p15: NativeArg, p16: NativeArg, p17: NativeArg, p18: NativeArg, p19: NativeArg, p20: NativeArg, p21: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13, p14, p15, p16, p17, p18, p19, p20, p21];
    unsafe { native::invoke(0x57CB267624EF85C0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_petrol_trail_decal_info(x: f32, y: f32, z: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3)];
    unsafe { native::invoke(0x73354FB6D03D2E8A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_veg_modifier_sphere(x: f32, y: f32, z: f32, radius: f32, mod_type: i32, flags: i32, p6: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(mod_type), NativeArg::int(flags), NativeArg::int(p6)];
    unsafe { native::invoke(0xFA50F79257745E74, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn allow_pickup_light_sync(pickup_object: Object, allow: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pickup_object), NativeArg::boolean(allow)];
    unsafe { native::invoke(0x7C348310A6E2FB91, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_has_event_triggered_by_stackhash(effect_name_hash: Hash, event_type: i32, b_peek_only: bool, b_is_registered: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash), NativeArg::int(event_type), NativeArg::boolean(b_peek_only), NativeArg::in_out(b_is_registered)];
    unsafe { native::invoke(0x9AB192A9EF980EED, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_is_preloading_by_stackhash(effect_name_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash)];
    unsafe { native::invoke(0x59EA80079B86D8C7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_is_running(effect_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0x4A123E85D7C4CA0B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_play(effect_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0x4102732DF6B4005F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_stop(effect_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0xB4FD7446BAB2F394, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_stop_all() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x66560A0D4C64FD21, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_tv_audio_to_entity(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x40866A418EB8EFDE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn begin_create_low_quality_copy_of_photo(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x494A9874F17A7D50, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn begin_take_high_quality_photo() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA15BFFC0A01B34E1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn block_pickup_placement_light(pickup: Pickup, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pickup), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x0552AA3FFC5B87AA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cascade_shadows_clear_shadow_sample_type() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF7C29D7C12C36F03, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cascade_shadows_enable_entity_tracker(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x8FBFD2AEB196B369, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cascade_shadows_set_cascade_bounds(p0: NativeArg, p1: bool, p2: f32, p3: f32, p4: f32, p5: f32, p6: bool, p7: f32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::boolean(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::boolean(p6), NativeArg::float(p7)];
    unsafe { native::invoke(0xD9EDB2E4512D563E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cascade_shadows_set_shadow_sample_type(type_: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(type_)];
    unsafe { native::invoke(0xCE4774E0F9AD48D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_timecycle_modifier() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0E3F4AF2D63491FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_checkpoint_with_namehash(type_hash: Hash, pos_x1: f32, pos_y1: f32, pos_z1: f32, pos_x2: f32, pos_y2: f32, pos_z2: f32, radius: f32, red: i32, green: i32, blue: i32, alpha: i32, reserved: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(type_hash), NativeArg::float(pos_x1), NativeArg::float(pos_y1), NativeArg::float(pos_z1), NativeArg::float(pos_x2), NativeArg::float(pos_y2), NativeArg::float(pos_z2), NativeArg::float(radius), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue), NativeArg::int(alpha), NativeArg::int(reserved)];
    unsafe { native::invoke(0x175668836B44CBB0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_tracked_point() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFB405CB357C69CB9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_checkpoint(checkpoint: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(checkpoint)];
    unsafe { native::invoke(0x0DED5B0C8EBAAE12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn destroy_tracked_point(point: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(point)];
    unsafe { native::invoke(0x37A59922109F8F1C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_entitymask() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5C9978A2A3DC3D0D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_hdtex_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x98A7CD5EA379A854, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_particle_fx_looped_exist(ptfx_handle: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ptfx_handle)];
    unsafe { native::invoke(0x9DD5AFF561E88F2A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn draw_light_with_range(pos_x: f32, pos_y: f32, pos_z: f32, color_r: i32, color_g: i32, color_b: i32, range: f32, intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::int(color_r), NativeArg::int(color_g), NativeArg::int(color_b), NativeArg::float(range), NativeArg::float(intensity)];
    unsafe { native::invoke(0xD2D9E04C0DF927F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn draw_low_quality_photo_to_phone(p0: bool, photo_rotation: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::int(photo_rotation)];
    unsafe { native::invoke(0xF1142E5D64B47802, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn draw_rect(x: f32, y: f32, width: f32, height: f32, red: i32, green: i32, blue: i32, alpha: i32, p8: bool, p9: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(width), NativeArg::float(height), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue), NativeArg::int(alpha), NativeArg::boolean(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x405224591DF02025, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn draw_sprite(texture_dict: &str, texture_name: &str, screen_x: f32, screen_y: f32, width: f32, height: f32, heading: f32, red: i32, green: i32, blue: i32, alpha: i32, p11: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(texture_dict), NativeArg::string(texture_name), NativeArg::float(screen_x), NativeArg::float(screen_y), NativeArg::float(width), NativeArg::float(height), NativeArg::float(heading), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue), NativeArg::int(alpha), NativeArg::boolean(p11)];
    unsafe { native::invoke(0xC9884ECADE94CB34, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn draw_tv_channel(x_pos: f32, y_pos: f32, x_scale: f32, y_scale: f32, rotation: f32, red: i32, green: i32, blue: i32, alpha: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(x_scale), NativeArg::float(y_scale), NativeArg::float(rotation), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue), NativeArg::int(alpha)];
    unsafe { native::invoke(0xC0A145540254A840, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_entitymask() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFAAD23DE7A54FC14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_moon_cycle_override(strength: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(strength)];
    unsafe { native::invoke(0x6FE93BCC7BF12B63, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_movie_subtitles(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6FC9B065229C0787, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn end_petrol_trail_decals() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0E126AAE933F3B56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn free_memory_for_high_quality_photo() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD45547D8396F002A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn free_memory_for_low_quality_photo() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x614682E715ADBAAC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn free_memory_for_mission_creator_photo() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7DFF8F94937D2659, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_screen_coord_from_world_coord(world_x: f32, world_y: f32, world_z: f32, screen_x: &mut f32, screen_y: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(world_x), NativeArg::float(world_y), NativeArg::float(world_z), NativeArg::in_out(screen_x), NativeArg::in_out(screen_y)];
    unsafe { native::invoke(0xCB50D7AFCC8B0EC6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_screen_resolution(x: &mut i32, y: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(x), NativeArg::in_out(y)];
    unsafe { native::invoke(0x66773C92835D0909, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_status_of_create_low_quality_copy_of_photo(p0: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x13430D3D5A45F14B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_status_of_load_mission_creator_photo(content_id: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(content_id)];
    unsafe { native::invoke(0xC71B50AE58D07369, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_status_of_save_high_quality_photo() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD6663EC374092383, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_status_of_sorted_list_operation() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB28894CD7408BD0C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_status_of_take_high_quality_photo() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4A3DA74C3CCB1725, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_timecycle_modifier_index() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA705394293E2B3D3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_timecycle_transition_modifier_index() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2DA67BA3C8A6755D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_toggle_paused_renderphases_status() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x86ED21BDB2791CE8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_tv_channel() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF90FBFD68F3C59AE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_decal_alive(decal: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(decal)];
    unsafe { native::invoke(0x3E4B4E5CF5D3EEB5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_photo_frame() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x86076AE35CBBE55F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_tracked_point_visible(point: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(point)];
    unsafe { native::invoke(0xCBB056BA159FB48D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_tvshow_currently_playing(video_cliphash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(video_cliphash)];
    unsafe { native::invoke(0x4D562223E0EB65F3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn load_mission_creator_photo(p0: &mut Any, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), p1, p2, p3];
    unsafe { native::invoke(0x84F0BA7462FF8D58, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pedshot_is_available() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAF6E67D073D2DCE2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn queue_operation_to_create_sorted_list_of_photos() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA42EDF1E88734A7E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_decal(decal: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(decal)];
    unsafe { native::invoke(0x49A720552EB0BB88, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_decals_from_object(obj: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(obj)];
    unsafe { native::invoke(0xFB8972BAE0013140, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_decals_in_range(x: f32, y: f32, z: f32, range: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(range)];
    unsafe { native::invoke(0x86DE59FA02902B40, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_grass_cull_sphere(handle: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(handle)];
    unsafe { native::invoke(0xAE7BF7CA9E4BA48D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_particle_fx(ptfx_handle: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ptfx_handle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x459598F579C98929, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_particle_fx_from_entity(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x92884B4A49D81325, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_particle_fx_in_range(x: f32, y: f32, z: f32, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0x87B5905ECA623B68, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_veg_modifier_sphere(veg_modifier_handle: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(veg_modifier_handle), NativeArg::int(p1)];
    unsafe { native::invoke(0x9CF1836C03FB67A2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_adaptation(unk: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(unk)];
    unsafe { native::invoke(0x297B72E2AF094742, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_particle_fx_override(name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x274B3DABF7E72DEF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_paused_renderphases() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCCD9AAD85E1B559E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn save_high_quality_photo(unused: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(unused)];
    unsafe { native::invoke(0x57639FD876B68A91, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_artificial_lights_state(state: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(state)];
    unsafe { native::invoke(0xB2797619A7C7747B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_checkpoint_rgba(checkpoint: i32, red: i32, green: i32, blue: i32, alpha: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(checkpoint), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue), NativeArg::int(alpha)];
    unsafe { native::invoke(0xCAAFC225E33B1D15, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_checkpoint_rgba2(checkpoint: i32, red: i32, green: i32, blue: i32, alpha: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(checkpoint), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue), NativeArg::int(alpha)];
    unsafe { native::invoke(0x99AFF17222D4DEB4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_petrol_decals_igniting_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x53ED07BF368EDA59, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_grass_cull_sphere(x: f32, y: f32, z: f32, p3: f32, p4: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3), NativeArg::int(p4)];
    unsafe { native::invoke(0x27219300C36A8D40, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_hidof_override(p0: bool, p1: bool, p2: f32, p3: f32, p4: f32, p5: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::boolean(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5)];
    unsafe { native::invoke(0xCC23AA1A7CBFE840, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_ambient_colour(entity: Entity, p1: &str, r: f32, g: f32, b: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(p1), NativeArg::float(r), NativeArg::float(g), NativeArg::float(b)];
    unsafe { native::invoke(0x3C61B52B00848C26, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_bullet_impact_lodrange_scale(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x8DCCC98DC0DBF9E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_bullet_impact_scale(scale: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(scale)];
    unsafe { native::invoke(0xA53C8D7D0F8C74D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_foot_lodrange_scale(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x2A1625858887D4E6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_looped_alpha(ptfx_handle: i32, alpha: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ptfx_handle), NativeArg::float(alpha)];
    unsafe { native::invoke(0x88786E76234F7054, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_looped_colour(ptfx_handle: i32, r: f32, g: f32, b: f32, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ptfx_handle), NativeArg::float(r), NativeArg::float(g), NativeArg::float(b), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x239879FC61C610CC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_looped_evolution(ptfx_handle: i32, property_name: &str, amount: f32, no_network: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ptfx_handle), NativeArg::string(property_name), NativeArg::float(amount), NativeArg::boolean(no_network)];
    unsafe { native::invoke(0x3674F389B0FACD80, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_looped_far_clip_dist(ptfx_handle: i32, range: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ptfx_handle), NativeArg::float(range)];
    unsafe { native::invoke(0x9B04D471DA0AD7AA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_looped_offsets(ptfx_handle: i32, x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ptfx_handle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z)];
    unsafe { native::invoke(0xD3A4A95FC94FE83B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_looped_scale(ptfx_handle: i32, scale: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ptfx_handle), NativeArg::float(scale)];
    unsafe { native::invoke(0x1A9E1C0D98D093B7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_non_looped_alpha(alpha: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(alpha)];
    unsafe { native::invoke(0xE8A35938A7026CEA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_non_looped_colour(r: f32, g: f32, b: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(r), NativeArg::float(g), NativeArg::float(b)];
    unsafe { native::invoke(0x60B85BED6577A35B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_override(old_asset: &str, new_asset: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(old_asset), NativeArg::string(new_asset)];
    unsafe { native::invoke(0xBE711A169E9C7E95, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_light(object: Object, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x7DFB49BCDB73089A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_script_gfx_draw_behind_pausemenu(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x906B86E6D7896B9E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_script_gfx_draw_order(draw_order: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(draw_order)];
    unsafe { native::invoke(0xCFCC78391C8B3814, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_timecycle_modifier(modifier_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(modifier_name)];
    unsafe { native::invoke(0xFA08722A5EA82DA7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_timecycle_modifier_strength(strength: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(strength)];
    unsafe { native::invoke(0xFDB74C9CC54C3F37, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_tracked_point_info(point: i32, x: f32, y: f32, z: f32, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(point), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0xF6FDA3D4404D4F2C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_transition_out_of_timecycle_modifier(strength: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(strength)];
    unsafe { native::invoke(0xBB6C707F20D955D4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_transition_timecycle_modifier(modifier_name: &str, transition_blend: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(modifier_name), NativeArg::float(transition_blend)];
    unsafe { native::invoke(0xFF927A09F481D80C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_tv_audio_frontend(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x64437C98FCC5F291, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_tv_channel(channel: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(channel)];
    unsafe { native::invoke(0x593FAF7FC9401A56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_tv_channel_playlist(tv_channel: i32, playlist_name: &str, restart: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(tv_channel), NativeArg::string(playlist_name), NativeArg::boolean(restart)];
    unsafe { native::invoke(0xDEC6B25F5DC8925B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_tv_volume(volume: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(volume)];
    unsafe { native::invoke(0x73A97068787D7231, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_networked_particle_fx_looped_on_entity(effect_name: &str, entity: Entity, x_offset: f32, y_offset: f32, z_offset: f32, x_rot: f32, y_rot: f32, z_rot: f32, scale: f32, x_axis: bool, y_axis: bool, z_axis: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(entity), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::float(scale), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis)];
    unsafe { native::invoke(0x8F90AB32E1944BDE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_networked_particle_fx_looped_on_entity_bone(effect_name: &str, entity: Entity, x_offset: f32, y_offset: f32, z_offset: f32, x_rot: f32, y_rot: f32, z_rot: f32, bone_index: i32, scale: f32, x_axis: bool, y_axis: bool, z_axis: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(entity), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::int(bone_index), NativeArg::float(scale), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis)];
    unsafe { native::invoke(0x9C56621462FFE7A6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_networked_particle_fx_non_looped_at_coord(effect_name: &str, x_pos: f32, y_pos: f32, z_pos: f32, x_rot: f32, y_rot: f32, z_rot: f32, scale: f32, x_axis: bool, y_axis: bool, z_axis: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::float(scale), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis)];
    unsafe { native::invoke(0xFB97618457994A62, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_networked_particle_fx_non_looped_on_entity(effect_name: &str, entity: Entity, offset_x: f32, offset_y: f32, offset_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale: f32, axis_x: bool, axis_y: bool, axis_z: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(entity), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale), NativeArg::boolean(axis_x), NativeArg::boolean(axis_y), NativeArg::boolean(axis_z)];
    unsafe { native::invoke(0xE6CFE43937061143, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_particle_fx_looped_at_coord(effect_name: &str, x: f32, y: f32, z: f32, x_rot: f32, y_rot: f32, z_rot: f32, scale: f32, x_axis: bool, y_axis: bool, z_axis: bool, p11: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::float(scale), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis), NativeArg::boolean(p11)];
    unsafe { native::invoke(0xBA32867E86125D3A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_particle_fx_looped_on_entity(effect_name: &str, entity: Entity, x_offset: f32, y_offset: f32, z_offset: f32, x_rot: f32, y_rot: f32, z_rot: f32, scale: f32, x_axis: bool, y_axis: bool, z_axis: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(entity), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::float(scale), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis)];
    unsafe { native::invoke(0xBD41E1440CE39800, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_particle_fx_looped_on_entity_bone(effect_name: &str, entity: Entity, x_offset: f32, y_offset: f32, z_offset: f32, x_rot: f32, y_rot: f32, z_rot: f32, bone_index: i32, scale: f32, x_axis: bool, y_axis: bool, z_axis: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(entity), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::int(bone_index), NativeArg::float(scale), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis)];
    unsafe { native::invoke(0xD3BA6EC7F2FBD5E9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_particle_fx_looped_on_ped_bone(effect_name: &str, ped: Ped, x_offset: f32, y_offset: f32, z_offset: f32, x_rot: f32, y_rot: f32, z_rot: f32, bone_index: i32, scale: f32, x_axis: bool, y_axis: bool, z_axis: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(ped), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::int(bone_index), NativeArg::float(scale), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis)];
    unsafe { native::invoke(0xE689C1B1432BB8AF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_particle_fx_non_looped_at_coord(effect_name: &str, x_pos: f32, y_pos: f32, z_pos: f32, x_rot: f32, y_rot: f32, z_rot: f32, scale: f32, x_axis: bool, y_axis: bool, z_axis: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::float(scale), NativeArg::boolean(x_axis), NativeArg::boolean(y_axis), NativeArg::boolean(z_axis)];
    unsafe { native::invoke(0x2E80BF72EF7C87AC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_particle_fx_non_looped_on_entity(effect_name: &str, entity: Entity, offset_x: f32, offset_y: f32, offset_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale: f32, axis_x: bool, axis_y: bool, axis_z: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(entity), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale), NativeArg::boolean(axis_x), NativeArg::boolean(axis_y), NativeArg::boolean(axis_z)];
    unsafe { native::invoke(0xFF4C64C513388C12, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_particle_fx_non_looped_on_ped_bone(effect_name: &str, ped: Ped, offset_x: f32, offset_y: f32, offset_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, bone_index: i32, scale: f32, axis_x: bool, axis_y: bool, axis_z: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(ped), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::int(bone_index), NativeArg::float(scale), NativeArg::boolean(axis_x), NativeArg::boolean(axis_y), NativeArg::boolean(axis_z)];
    unsafe { native::invoke(0x3FAA72BD940C3AC0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_petrol_trail_decals(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x46F246D6504F0031, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_particle_fx_looped(ptfx_handle: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ptfx_handle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x22970F3A088B133B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn toggle_paused_renderphases(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xEF9E1C45732F55FA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_lights_on_entity(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xBDBACB52A03CC760, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn use_particle_fx_asset(fx_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(fx_name)];
    unsafe { native::invoke(0xA10DB07FC234DD12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x085_c5_b61_a0114_f32(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x085C5B61A0114F32, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_d5_b19_c34068_fee7(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0D5B19C34068FEE7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1460_b644397453_eb() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1460B644397453EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x171_c18_e994_c1_a395(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x171C18E994C1A395, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_a9_f09_ab458_d49_c6(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x1A9F09AB458D49C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_c6306_e5_bc25_c29_c() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1C6306E5BC25C29C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_ff8731_be1_dfc0_c0(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x1FF8731BE1DFC0C0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x21_f00_e08_cbb5_f37_b(component: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(component)];
    unsafe { native::invoke(0x21F00E08CBB5F37B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x26_dd2_fb0_a88_cc412(effect_name: &str, effect_name2: &str, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::string(effect_name2), p2, p3];
    unsafe { native::invoke(0x26DD2FB0A88CC412, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x285438_c26_c732_f9_d() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x285438C26C732F9D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x32_de2_bffda43_e62_a() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x32DE2BFFDA43E62A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x38_d9_d50_f2085_e9_b3(effect_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash)];
    unsafe { native::invoke(0x38D9D50F2085E9B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_da7_a10583_a4_bec0() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3DA7A10583A4BEC0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x402_e1_a61_d2587_fcd(p0: NativeArg, x: f32, y: f32, z: f32, p4: f32, p5: f32, heading: f32) -> Result<bool, NativeError> {
    let arguments = [p0, NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(heading)];
    unsafe { native::invoke(0x402E1A61D2587FCD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4046493_d2_eeaca0_e() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4046493D2EEACA0E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x41_f88_a85_a579_a61_d(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x41F88A85A579A61D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x453_d16_d41_fc51_d3_e(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x453D16D41FC51D3E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x48_fe0_db54045_b975(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x48FE0DB54045B975, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_bd66_b4_e3427689_b(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x4BD66B4E3427689B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_fb67_d172_c4476_f3(entity: Entity, p1: &str, p2: &str, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(p1), NativeArg::string(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0x4FB67D172C4476F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x503941_f65_dba24_ec(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x503941F65DBA24EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x519928_df02_eb5101(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x519928DF02EB5101, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_ac6_e0_fa028369_de() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5AC6E0FA028369DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_c674_eb487891_f6_b() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5C674EB487891F6B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_c9_c3_a466_b3296_a8(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5C9C3A466B3296A8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x67_b0778_c62_e74423(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x67B0778C62E74423, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c03118_e9_e5_c1_a14(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6C03118E9E5C1A14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x71845905_bccde781(effect_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash)];
    unsafe { native::invoke(0x71845905BCCDE781, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x735762_e8_d7573_e42(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x735762E8D7573E42, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x812_c1563185_c6_fb2() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x812C1563185C6FB2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x815653_a42_c5_abe76() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x815653A42C5ABE76, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8996_fa6_ad9_fe4_e90(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x8996FA6AD9FE4E90, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x910_e260_aead855_de() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x910E260AEAD855DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x94_b261_f1_f35293_e1(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x94B261F1F35293E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x981_c7_d863980_fa51() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x981C7D863980FA51, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_d1_b0_b5066205692() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9D1B0B5066205692, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_f158_a49_b0_d84_c3_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9F158A49B0D84C3C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_f6_d859_c80708_b26(p0: bool, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x9F6D859C80708B26, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a04_ef43030593_abc(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA04EF43030593ABC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a0_f4_d12_d6042_f6_d5(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA0F4D12D6042F6D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a15_ccab8_ad038291(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xA15CCAB8AD038291, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a1_a86055792_fb249(persona_photo_local_cache_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(persona_photo_local_cache_type)];
    unsafe { native::invoke(0xA1A86055792FB249, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a201_a3_d0_ac087_c37(effect_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0xA201A3D0AC087C37, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a21_af60_c9_f99_ccc5() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA21AF60C9F99CCC5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b032_c085_d9_a03907() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB032C085D9A03907, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b958_d97_a0_dfaa0_c2(effect_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0xB958D97A0DFAA0C2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c06_f2_f45_a73_eabcd(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xC06F2F45A73EABCD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c28_f62_ac9774_fc1_b() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC28F62AC9774FC1B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c37792_a3_f9_c90771() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC37792A3F9C90771, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c489_fe31_ac726512(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC489FE31AC726512, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c76_fc4_c2_fc5_f4405(effect_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash)];
    unsafe { native::invoke(0xC76FC4C2FC5F4405, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cc3_b787_e73_e64160(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xCC3B787E73E64160, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d1472_aff30_c103_d6(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0xD1472AFF30C103D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d543487_a1_f12828_f(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xD543487A1F12828F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d9_bc98_b55_bcfaa9_b(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD9BC98B55BCFAA9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e63_d68_f455_ca0_b47(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xE63D68F455CA0B47, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e75_cddebf618_c8_ff(effect_name_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash)];
    unsafe { native::invoke(0xE75CDDEBF618C8FF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eb48_ce48_eec41_fd4(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xEB48CE48EEC41FD4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec3_d8_c228_fe553_d7(p0: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xEC3D8C228FE553D7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec3_f7_f24_eeeb3_ba3() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEC3F7F24EEEB3BA3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f2_f543_d48_f319_a3_a() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF2F543D48F319A3A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f5793_bb386_e1_ff9_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF5793BB386E1FF9C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fb680_a9_b33_d0_edbe(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xFB680A9B33D0EDBE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fbf161_fcfec8589_e(effect_name: &str, p1: i32, p2: bool, p3: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(p1), NativeArg::boolean(p2), NativeArg::in_out(p3)];
    unsafe { native::invoke(0xFBF161FCFEC8589E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fc9_b53_c072_f418_e0() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFC9B53C072F418E0, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fd05_b1_dde83749_fa(p0: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0xFD05B1DDE83749FA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ff584_f097_c17_fa8_f() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFF584F097C17FA8F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ff8018_c778349234(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xFF8018C778349234, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_blood_pool(x: f32, y: f32, z: f32, unused: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(unused)];
    unsafe { native::invoke(0xFA2ECC78A6014D4F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_blood_pools_for_ped(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xDFCE8CE9F3EBE93F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_blood_pools_for_ped_with_params(ped: Ped, p1: f32, size: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(size), NativeArg::float(p3)];
    unsafe { native::invoke(0xC349EE1E6EFA494B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_blood_pool_2(x: f32, y: f32, z: f32, p3: f32, size: f32, p5: f32, permanent: bool, p7: f32, p8: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3), NativeArg::float(size), NativeArg::float(p5), NativeArg::boolean(permanent), NativeArg::float(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0xF708298675ABDC6A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_blood_trail_point(x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xDD9DC1AB63D513CE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_blood_trail_splat(x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xF5E45CB1CF965D2D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_entity_to_entity_mask(entity: Entity, mask: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(mask)];
    unsafe { native::invoke(0xC6F81FCD15350323, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_entity_to_entity_mask_with_intensity(entity: Entity, mask: i32, intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(mask), NativeArg::float(intensity)];
    unsafe { native::invoke(0x958DEBD9353C0935, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_veg_modifier_zone(volume: Volume, p1: i32, flags: i32, p3: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(p1), NativeArg::int(flags), NativeArg::int(p3)];
    unsafe { native::invoke(0xBD3324281E8B9933, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_clear_effect(effect_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0xC5CB91D65852ED7E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_get_stackhash(effect_name: &str) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0x842CCC9491FFCD9B, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_has_loaded(effect_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0xBF2DD155B2ADCD0A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_is_stackhash_playing(effect_name_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash)];
    unsafe { native::invoke(0xEEF83A759AE06A27, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_is_tag_playing(effect_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0x2D4F9C852CE8A253, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_play_tag(effect_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash)];
    unsafe { native::invoke(0x9B8D5D4CB8AF58B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_play_timed(effect_name: &str, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(duration)];
    unsafe { native::invoke(0x3A9A281FF71249E9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_preload_postfx(effect_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0x5199405EABFBD7F0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_preload_postfx_by_stackhash(effect_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash)];
    unsafe { native::invoke(0xF3E039322BFBD4D8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_set_postfx_color(effect_name: &str, p1: i32, red: i32, green: i32, blue: i32, alpha: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(p1), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue), NativeArg::int(alpha)];
    unsafe { native::invoke(0x63011D0C7C6519E0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_set_potency(effect_name: &str, p1: i32, potency: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(p1), NativeArg::float(potency)];
    unsafe { native::invoke(0xF972F0AB16DC5260, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_set_strength(effect_name: &str, strength: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::float(strength)];
    unsafe { native::invoke(0xCAB4DD2D5B2B7246, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_set_to_unload(effect_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0x37D7BDBA89F13959, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_stop_stackhash_postfx(effect_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(effect_name_hash)];
    unsafe { native::invoke(0xEDA5CBECF56E1386, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn animpostfx_stop_tag(effect_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(effect_name)];
    unsafe { native::invoke(0xAD74C22A541AB987, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn block_pickup_object_light(pickup_object: Object, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pickup_object), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x50C14328119E1DD1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blood_trail_for_waypoint(waypoint_recording: &str, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(waypoint_recording), NativeArg::float(p1)];
    unsafe { native::invoke(0xB9C92616929CC25D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn change_photo_mode_contrast(value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0x62B9F9A1272AED80, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn change_photo_mode_exposure(value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0xC8D0611D9A0CF5D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_swatch_texture_dict(slots: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(slots)];
    unsafe { native::invoke(0x3D084D5568FB4028, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn destroy_swatch_texture_dict() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDAD7FB8402651654, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_far_artificial_lights(disable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(disable)];
    unsafe { native::invoke(0xCD284E2F6AC27EE9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_static_veg_modifier(p0: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0xDD0BC0EDCB2162F6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_checkpoint_have_fx(checkpoint: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(checkpoint)];
    unsafe { native::invoke(0x4C11CCACB7C02B6E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn draw_marker(type_: Hash, pos_x: f32, pos_y: f32, pos_z: f32, dir_x: f32, dir_y: f32, dir_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32, red: i32, green: i32, blue: i32, alpha: i32, bob_up_and_down: bool, face_camera: bool, p19: i32, rotate: bool, texture_dict: &str, texture_name: &str, draw_on_ents: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(type_), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(dir_x), NativeArg::float(dir_y), NativeArg::float(dir_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue), NativeArg::int(alpha), NativeArg::boolean(bob_up_and_down), NativeArg::boolean(face_camera), NativeArg::int(p19), NativeArg::boolean(rotate), NativeArg::string(texture_dict), NativeArg::string(texture_name), NativeArg::boolean(draw_on_ents)];
    unsafe { native::invoke(0x2A32FAA57B937173, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_static_veg_modifier(p0: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0xDFEA23EC90113657, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn generate_swatch_texture(slot_id: i32, component_hash: Hash, metaped_type: i32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(slot_id), NativeArg::hash(component_hash), NativeArg::int(metaped_type), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x160921255327C591, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn generate_swatch_texture_directly(slot: i32, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(slot), p1];
    unsafe { native::invoke(0x646ED1A1D28487DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_number_of_local_photos() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x78C56B8A7B1D000C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_mask_layers(entity: Entity, layer0: &mut f32, layer1: &mut f32, layer2: &mut f32, layer3: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::in_out(layer0), NativeArg::in_out(layer1), NativeArg::in_out(layer2), NativeArg::in_out(layer3)];
    unsafe { native::invoke(0xE8A8378BF651079C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_number_of_local_photos() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8E587FCD30E05592, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_modified_visibility_distance() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x25CA89B2A39DCC69, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_photo_mode_contrast() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x98F4154989B81EC6, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_photo_mode_exposure() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x06C0D8BB6B04A709, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_proxy_interior_index(interior_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(interior_id)];
    unsafe { native::invoke(0x5D1C5D8E62E8EE1C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_proxy_interior_index_artificial_lights_enabled(proxy_interior_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(proxy_interior_index)];
    unsafe { native::invoke(0x113857D66A9CABE6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_static_veg_modifier_enabled(p0: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0xDE9BAD3292AA6D5E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_texture_in_dict(txd_hash: Hash, dict: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(txd_hash), NativeArg::hash(dict)];
    unsafe { native::invoke(0xA2A51869BDED733B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_tracked_point_valid(point: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(point)];
    unsafe { native::invoke(0xF2FDDCC8C6BAE1B3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn num_pixels_visible_at_tracked_point(i_tracked_point: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(i_tracked_point)];
    unsafe { native::invoke(0xDFE332A5DA6FE7C9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pedshot_finish_cleanup_data() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC2B8164C3BE871A4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pedshot_generate_persona_photo(texture: &str, ped: Ped, player_slot: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(texture), NativeArg::int(ped), NativeArg::int(player_slot)];
    unsafe { native::invoke(0xD9C24F53631F2372, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pedshot_init_cleanup_data() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x55285F885F662169, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pedshot_previous_persona_photo_data_cleanup() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3E2FDDBE435A8787, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pedshot_set_persona_photo_type(persona_photo_local_cache_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(persona_photo_local_cache_type)];
    unsafe { native::invoke(0x196D3ACBEBA4A44B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_entity_from_entity_mask(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x56A786E87FF53478, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_entity_aura() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAF4D239B8903FCBE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cloud_height(height: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(height)];
    unsafe { native::invoke(0xC332C91388F5580B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cloud_layer(x: f32, y: f32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::int(p2)];
    unsafe { native::invoke(0xB8C984C0D47F4F07, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cloud_noise(x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xFE7966DF01452F32, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cloud_position(x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x10C1767B93257480, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_district_photo_taken_stat(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x9937FACBBF267244, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_aura(p0: f32, p1: f32, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0x249CD6B7285536F2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_mask_layers(entity: Entity, layer0: &mut f32, layer1: &mut f32, layer2: &mut f32, layer3: &mut f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::in_out(layer0), NativeArg::in_out(layer1), NativeArg::in_out(layer2), NativeArg::in_out(layer3)];
    unsafe { native::invoke(0xE92012611461A42A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_render_guarma_ship(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC38B4952B728397A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_lights_color_for_entity(entity: Entity, red: i32, green: i32, blue: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue)];
    unsafe { native::invoke(0x6EC2A67962296F49, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_lights_intensity_for_entity(entity: Entity, intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(intensity)];
    unsafe { native::invoke(0x07C0F87AAC57F2E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_lights_type_for_entity(entity: Entity, type_: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(type_)];
    unsafe { native::invoke(0xAB72C67163DC4DB4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_looped_update_distant_smoke(ptfx_handle: i32, scalar: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ptfx_handle), NativeArg::float(scalar)];
    unsafe { native::invoke(0x9DDC222D85D5AF2A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_particle_fx_non_looped_emitter_scale(p0: f32, p1: f32, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0x56C392C2BD78B024, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pearlescent_fx_enabled(object: Object, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x72E30372E7CC4415, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_photo_in_photomode_stat(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xFA91736933AB3D93, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_photo_mode_exposure_locked(locked: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(locked)];
    unsafe { native::invoke(0x5CD6A2CCE5087161, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_photo_overlay_effect_stat(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x8B3296278328B5EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_photo_self_stat(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x2705D18C11B61046, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_photo_studio_stat(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x8E6AFF353C09652E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_appear_in_photo(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x75D568607909333E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_posse_id_for_photo(posse_id: NativeArg) -> Result<(), NativeError> {
    let arguments = [posse_id];
    unsafe { native::invoke(0x564837D4A9EDE296, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_proxy_interior_index_artificial_lights_state(proxy_interior_index: i32, state: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(proxy_interior_index), NativeArg::boolean(state)];
    unsafe { native::invoke(0xBFCB17895BB99E4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_region_photo_taken_stat(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0xD1031B83AC093BC7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_sniper_glints_enabled(enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x6E8EB45A4F4460EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_snow_coverage_type(type_: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(type_)];
    unsafe { native::invoke(0xF02A9C330BBFC5C7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_state_photo_taken_stat(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x8952E857696B8A79, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_particle_fx_non_looped_on_ped_bone_2(effect_name: &str, ped: Ped, offset_x: f32, offset_y: f32, offset_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, bone_index: i32, scale: f32, axis_x: bool, axis_y: bool, axis_z: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(effect_name), NativeArg::int(ped), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::int(bone_index), NativeArg::float(scale), NativeArg::boolean(axis_x), NativeArg::boolean(axis_y), NativeArg::boolean(axis_z)];
    unsafe { native::invoke(0xC695870B8A149B96, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_photo_mode_exposure() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9229ED770975BD9E, &arguments) }.map(|_| ())
}

// ============================================================================
// HUD
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn allow_pause_when_not_in_state_of_play_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x30996422DF1EE561, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn busyspinner_is_on() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x823BF7B1DF613A21, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn busyspinner_off() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x58F441B90EA84D06, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_all_help_messages() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x916ED8321F087059, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_fake_mp_gamer_tag(ped: Ped, username: &str, pointed_clan_tag: bool, is_rockstar_clan: bool, clan_tag: &str, clan_flag: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(username), NativeArg::boolean(pointed_clan_tag), NativeArg::boolean(is_rockstar_clan), NativeArg::string(clan_tag), NativeArg::int(clan_flag)];
    unsafe { native::invoke(0x53CB4B502E1C57EA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_frontend_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x56CE42A528156A67, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn display_hud(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xD63FE3AF9FB3D53F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_text_label_exist(label: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(label)];
    unsafe { native::invoke(0x73C258C68D6F55B6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_character_from_audio_conversation_filename(text: &str, position: i32, length: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::string(text), NativeArg::int(position), NativeArg::int(length)];
    unsafe { native::invoke(0x9D7E12EC6A1EE4E5, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_filename_for_audio_conversation(label_name: &str) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::string(label_name)];
    unsafe { native::invoke(0xCFEDCCAD3C5BA90D, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_hud_screen_position_from_world_position(world_x: f32, world_y: f32, world_z: f32, screen_x: &mut f32, screen_y: &mut f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(world_x), NativeArg::float(world_y), NativeArg::float(world_z), NativeArg::in_out(screen_x), NativeArg::in_out(screen_y)];
    unsafe { native::invoke(0xB39C81628EF10B42, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_length_of_literal_string(string: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(string)];
    unsafe { native::invoke(0x481FBF588B0B76DB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_length_of_literal_string_in_bytes(string: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(string)];
    unsafe { native::invoke(0xDC5AD6B7AB8184F5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_named_rendertarget_render_id(name: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0xB6762A85EE29AA60, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_string_from_hash_key(label_hash: Hash) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(label_hash)];
    unsafe { native::invoke(0xBD5DD5EAE2B6CE14, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hide_hud_and_radar_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x36CDD81627A6FCD2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hide_loading_on_fade_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEA600AABAF4B9084, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_hud_hidden() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x71B72B478F8189DC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_mp_gamer_tag_active(gamer_tag_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id)];
    unsafe { native::invoke(0x6E1C31E14C7A5F97, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_named_rendertarget_linked(model_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model_hash)];
    unsafe { native::invoke(0x707032835FF09AE7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_named_rendertarget_registered(name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x3EE32F7964C40FE6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_pause_menu_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x535384D6067BA42E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_radar_hidden() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1B82FD5FFA4D666E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_radar_hidden_by_script() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x66F35DD9D2B58579, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_radar_preference_switched_on() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x81E47F0EE1F2B21E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_subtitle_preference_switched_on() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7C4AC9573587F2DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn link_named_rendertarget(model_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model_hash)];
    unsafe { native::invoke(0x2F506B8556242DDB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_named_rendertarget(name: &str, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x98AF2BB6F62BD588, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_named_rendertarget(name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x0E692EE61761361F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_mp_gamer_tag(gamer_tag_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id)];
    unsafe { native::invoke(0x839BFD7D7E49FE09, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_frontend_active(active: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(active)];
    unsafe { native::invoke(0xCE47C21C0687EBC2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mission_name(p0: bool, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::string(name)];
    unsafe { native::invoke(0x402669A4BDAA72DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mission_name_for_ugc_mission(p0: bool, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::string(name)];
    unsafe { native::invoke(0xD98630CE73C61E98, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mp_gamer_tag_big_text(gamer_tag_id: i32, string: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::string(string)];
    unsafe { native::invoke(0xA0D7CE5F83259663, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mp_gamer_tag_name(gamer_tag_id: i32, string: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::string(string)];
    unsafe { native::invoke(0xEA6F4B8D4B4B5B3E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_text_render_id(render_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(render_id)];
    unsafe { native::invoke(0xE550CDE128D56757, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn text_block_is_loaded(text_block: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(text_block)];
    unsafe { native::invoke(0xD0976CC34002DB57, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn text_block_request(text_block: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(text_block)];
    unsafe { native::invoke(0xF66090013DE648D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_get_scene_uiobject(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xBE1067CD1C9570F6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_movieview_set_render_target(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x51DE09A2196BD951, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_is_control_action_active(control_action: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(control_action)];
    unsafe { native::invoke(0x1BE19185B8AFE299, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_request_scene(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB6857100F8FD433C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x052_d4_ac0922_af91_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x052D4AC0922AF91A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x066725_a9_d52_b3641() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x066725A9D52B3641, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x100157_d6_d7_fe32_ca(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x100157D6D7FE32CA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x160825_dadf1_b04_b3() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x160825DADF1B04B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x28_ae29_d909_c8_fdce(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x28AE29D909C8FDCE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_f7_bb105144_acf30() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2F7BB105144ACF30, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_fe4_fb41_ef7_d2196(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3FE4FB41EF7D2196, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x53_ce46_c01_a089_da1(prompt: Prompt, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x53CE46C01A089DA1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5651516_d947_abc53() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5651516D947ABC53, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_a59_d44189_af2_bc5(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8A59D44189AF2BC5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_b55_b324_a9123_f6_b(group_id: i32, volume: Volume, p2: &str, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::int(volume), NativeArg::string(p2), p3, p4, p5];
    unsafe { native::invoke(0x8B55B324A9123F6B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x958278_b97_c4_affd8(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x958278B97C4AFFD8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_d37_eb5003_e0_f2_cf(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9D37EB5003E0F2CF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bfff81_e12_a745_a5_f() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBFFF81E12A745A5F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d6_bd313_cfa41_e57_a(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD6BD313CFA41E57A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f1_e6979_c0_b779985(uiscene: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(uiscene)];
    unsafe { native::invoke(0xF1E6979C0B779985, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn busyspinner_set_text(text: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(text)];
    unsafe { native::invoke(0x7F78CD75CC4539E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_mp_gamer_tag(player: Player, username: &str, pointed_clan_tag: bool, is_rockstar_clan: bool, clan_tag: &str, clan_flag: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::string(username), NativeArg::boolean(pointed_clan_tag), NativeArg::boolean(is_rockstar_clan), NativeArg::string(clan_tag), NativeArg::int(clan_flag)];
    unsafe { native::invoke(0xD877AF112AD2B41B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_mp_gamer_tag_on_entity(entity: Entity, text: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(text)];
    unsafe { native::invoke(0xE961BF23EAB76B12, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_hud_context(component: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(component)];
    unsafe { native::invoke(0x8BC7C1F929D07BF3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_reduced_menu_time_scale() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC5C7A2F6567FCCBC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn display_text(text: &str, x_pos: f32, y_pos: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(text), NativeArg::float(x_pos), NativeArg::float(y_pos)];
    unsafe { native::invoke(0xD79334A4BB99BAD1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_text_block_exist(text_database: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(text_database)];
    unsafe { native::invoke(0x2C729F2B94CEA911, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_hud_context(component: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(component)];
    unsafe { native::invoke(0x4CC5F2FC1332577F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_hud_context_this_frame(component: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(component)];
    unsafe { native::invoke(0xC9CAEAEEC1256E54, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_reduced_menu_time_scale() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x26F6BBEA2CE3E3DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_color_from_name(color_name_hash: Hash, red: &mut i32, green: &mut i32, blue: &mut i32, alpha: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(color_name_hash), NativeArg::in_out(red), NativeArg::in_out(green), NativeArg::in_out(blue), NativeArg::in_out(alpha)];
    unsafe { native::invoke(0xB981DD2DFAF9B1C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_hud_visibility_slot_state(hud_slot: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(hud_slot)];
    unsafe { native::invoke(0x7EC0D68233E391AC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_label_text_2(label: &str) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::string(label)];
    unsafe { native::invoke(0x3429670F9B9EF2D3, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_text_substring_2(text: &str, length: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::string(text), NativeArg::int(length)];
    unsafe { native::invoke(0xD8402B858F4DDD88, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_text_substring_3(text: &str, begin: i32, length: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::string(text), NativeArg::int(begin), NativeArg::int(length)];
    unsafe { native::invoke(0x806862E5D266CF38, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hud_check_closest_horse(p0: bool) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x0501D52D24EA8934, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hud_get_inventory_wheel_currently_highlighted() -> Result<Hash, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9C409BBC492CB5B1, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hud_hide_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBF4F34A85CA2970D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_mp_gamer_tag_active_on_entity(gamer_tag_id: i32, entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::int(entity)];
    unsafe { native::invoke(0x502E1591A504F843, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn journal_can_write_entry(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCF782691D91F270E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn journal_clear_all_progress() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF402978DE6F88D6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn journal_get_entry_at_index(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3D16ABD7A1FD8C96, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn journal_get_entry_count() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE65B5DE53351BE22, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn journal_get_entry_info(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x5514C3E60673530F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn journal_get_grime_at_index(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCB5945E1B855852F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn journal_get_texture_with_layout(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x62CC549B3B8EA2AA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn journal_mark_read(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE4509BABE59BD24E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn journal_write_entry(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6DFDD665E416B093, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn mp_gamer_tag_disable_revive_top_icon(gamer_tag_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id)];
    unsafe { native::invoke(0x1F9A64C2804B3471, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn mp_gamer_tag_enable_revive_top_icon(gamer_tag_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id)];
    unsafe { native::invoke(0xFFF6579CF0139FCE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_current_ugc_mission_description(active: bool, string1: &str, string2: &str, string3: &str, string4: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(active), NativeArg::string(string1), NativeArg::string(string2), NativeArg::string(string3), NativeArg::string(string4)];
    unsafe { native::invoke(0xCE0D2F5586627CCE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mp_gamer_tag_colour(gamer_tag_id: i32, colour: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::hash(colour)];
    unsafe { native::invoke(0x84BD27DDF9575816, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mp_gamer_tag_name_posse(gamer_tag_id: i32, text: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::string(text)];
    unsafe { native::invoke(0x1EA716E0628A6F44, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mp_gamer_tag_secondary_icon(gamer_tag_id: i32, icon: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::hash(icon)];
    unsafe { native::invoke(0x95384C6CE1526EFF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mp_gamer_tag_top_icon(gamer_tag_id: i32, icon: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::hash(icon)];
    unsafe { native::invoke(0x5F57522BC1EB9D9D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mp_gamer_tag_type(gamer_tag_id: i32, type_: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::hash(type_)];
    unsafe { native::invoke(0x25B9C78A25105C35, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mp_gamer_tag_unk_allow_localized(gamer_tag_id: i32, allow: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::boolean(allow)];
    unsafe { native::invoke(0xEF7AB1A0E8C86170, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mp_gamer_tag_visibility(gamer_tag_id: i32, visibility: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(gamer_tag_id), NativeArg::int(visibility)];
    unsafe { native::invoke(0x93171DDDAB274EB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_text_color(r: i32, g: i32, b: i32, a: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(r), NativeArg::int(g), NativeArg::int(b), NativeArg::int(a)];
    unsafe { native::invoke(0x50A41AD966910F03, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn show_horse_cores(state: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(state)];
    unsafe { native::invoke(0xD4EE21B7CC7FD350, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn show_player_cores(state: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(state)];
    unsafe { native::invoke(0x50C803A4CD5932C5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn text_block_delete(text_block: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(text_block)];
    unsafe { native::invoke(0xAA03F130A637D923, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn text_block_is_streamed(text_block: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(text_block)];
    unsafe { native::invoke(0x3CF96E16265B7DC8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_add_group_link(p0: NativeArg, prompt: Prompt, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(prompt), p2];
    unsafe { native::invoke(0x684C96CC7C66E8EF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_add_group_return_link(p0: NativeArg, prompt: Prompt) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(prompt)];
    unsafe { native::invoke(0x837972ED28159536, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_clear_horizontal_orientation(id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0x6095358C4142932A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_clear_prompt_priority_preference() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x51259AE5C72D4A1B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_context_set_point(prompt: Prompt, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xAE84C5EE2C384FB3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_context_set_radius(prompt: Prompt, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(radius)];
    unsafe { native::invoke(0x0C718001B77CA468, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_context_set_volume(prompt: Prompt, volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(volume)];
    unsafe { native::invoke(0x4D107406667423BE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_create(input_hash: Hash, label_name: &str, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: i32) -> Result<Prompt, NativeError> {
    let arguments = [NativeArg::hash(input_hash), NativeArg::string(label_name), p2, p3, p4, NativeArg::int(p5)];
    unsafe { native::invoke(0x29FA7910726C3889, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_delete(prompt: Prompt) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x00EDE88D4D13CF59, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_disable_prompts_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF1622CE88A1946FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_disable_prompt_type_this_frame(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xFC094EF26DD153FA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_does_ambient_group_exist(hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xEB550B927B34A1BB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_enable_prompt_type_this_frame(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x06565032897BA861, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_filter_clear() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6A2F820452017EA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_get_group_active_page(hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xC1FCC36C3F7286C8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_get_group_id_for_scenario_point(p0: NativeArg, p1: i32) -> Result<i32, NativeError> {
    let arguments = [p0, NativeArg::int(p1)];
    unsafe { native::invoke(0xCB73D7521E7103F0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_get_group_id_for_target_entity(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xB796970BD125FCE8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_get_mash_mode_progress(prompt: Prompt) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x8A9585293863B8A5, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_get_progress(prompt: Prompt) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x81801291806DBC50, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_get_urgent_pulsing_enabled(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x1FBA0DABECDDB52B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_hold_auto_fill_mode(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x8010BEBD0D5ED5BC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_hold_mode(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xB60C9F9ED47ABB76, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_hold_mode_completed(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xE0F65F0640EF0617, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_manual_mash_mode(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xA6C6A4ADB3BAC409, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_mash_mode(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xCD072523791DDC1B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_mash_mode_completed(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x845CE958416DC473, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_mash_mode_failed(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x25B18E530CF39D6F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_mash_mode_just_pressed(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xB0E8599243B3F568, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_pressed_timed_mode_completed(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x3CE854D250A88DAF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_pressed_timed_mode_failed(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x1A17B9ECFF617562, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_has_standard_mode_completed(prompt: Prompt, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(p1)];
    unsafe { native::invoke(0xC92AC953F0A982AE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_is_active(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x546E342E01DE71CF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_is_enabled(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x0D00EDDFB58B7F28, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_is_hold_mode_running(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xC7D70EAEF92EFF48, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_is_just_pressed(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x2787CC611D3FACC5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_is_just_released(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x635CC82FA297A827, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_is_pressed(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x21E60E230086697F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_is_released(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xAFC887BA7A7756D6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_is_valid(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x347469FBDD1589A9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_register_begin() -> Result<Prompt, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x04F97DE45A519419, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_register_end(prompt: Prompt) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xF7AA2696A22AD8B9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_remove_group(prompt: Prompt, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), p1];
    unsafe { native::invoke(0x4E52C800A28F7BE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_restart_modes(prompt: Prompt) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xDC6C55DFA2C24EE5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_active_group_this_frame(hash: Hash, name: &str, tab_amount: i32, tab_default_index: i32, p4: i32, prompt: Prompt) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(hash), NativeArg::string(name), NativeArg::int(tab_amount), NativeArg::int(tab_default_index), NativeArg::int(p4), NativeArg::int(prompt)];
    unsafe { native::invoke(0xC65A45D4453C2627, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_allowed_action(prompt: Prompt, action: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::hash(action)];
    unsafe { native::invoke(0x565C1CE183CB0EAF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_ambient_group_this_frame(entity: Entity, p1: f32, p2: i32, p3: i32, p4: Hash, name: &str, p6: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(p1), NativeArg::int(p2), NativeArg::int(p3), NativeArg::hash(p4), NativeArg::string(name), NativeArg::int(p6)];
    unsafe { native::invoke(0x315C81D760609108, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_attribute(prompt: Prompt, attribute: i32, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(attribute), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x560E76D5E2E1803F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_beat_mode(prompt: Prompt, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xF957A1654C6322FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_beat_mode_grayed_out(prompt: Prompt, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), p1];
    unsafe { native::invoke(0xB487A4936FBF40AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_control_action(prompt: Prompt, action: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::hash(action)];
    unsafe { native::invoke(0xB5352B7494A08258, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_enabled(prompt: Prompt, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x8A0FB4D03A630D21, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_group(prompt: Prompt, group_id: i32, tab_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(group_id), NativeArg::int(tab_index)];
    unsafe { native::invoke(0x2F11D3A254169EA4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_hold_auto_fill_mode(prompt: Prompt, auto_fill_time_ms: i32, hold_time_ms: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(auto_fill_time_ms), NativeArg::int(hold_time_ms)];
    unsafe { native::invoke(0x3CE932E737C145D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_hold_auto_fill_with_decay_mode(prompt: Prompt, auto_fill_time_ms: i32, hold_time_ms: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(auto_fill_time_ms), NativeArg::int(hold_time_ms)];
    unsafe { native::invoke(0xA3F2149AA24F3D8E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_hold_indefinitely_mode(prompt: Prompt) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0xEA5CCF4EEB2F82D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_hold_mode(prompt: Prompt, hold_time_ms: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(hold_time_ms)];
    unsafe { native::invoke(0x94073D5CA3F16B7B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_manual_resolved(prompt: Prompt, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), p1];
    unsafe { native::invoke(0xA520C7B05FA4EB2A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_auto_fill_mode(prompt: Prompt, auto_fill_time_ms: i32, mashes: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(auto_fill_time_ms), NativeArg::int(mashes)];
    unsafe { native::invoke(0x6C39587D7CC66801, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_indefinitely_mode(prompt: Prompt) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x7B66E89312727274, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_manual_can_fail_mode(prompt: Prompt, p1: f32, p2: f32, p3: f32, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), p4];
    unsafe { native::invoke(0x179DCF71F705DA20, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_manual_mode(prompt: Prompt, p1: f32, p2: f32, p3: f32, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), p4];
    unsafe { native::invoke(0x32DF729D8BD3C1C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_manual_mode_decay_speed(prompt: Prompt, speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(speed)];
    unsafe { native::invoke(0x7D393C247FB9B431, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_manual_mode_increase_per_press(prompt: Prompt, rate: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(rate)];
    unsafe { native::invoke(0xA0D1D79C6036A855, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_manual_mode_pressed_growth_speed(prompt: Prompt, speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(speed)];
    unsafe { native::invoke(0x56DBB26F98582C29, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_mode(prompt: Prompt, mashes: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(mashes)];
    unsafe { native::invoke(0xDF6423BF071C7F71, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_with_resistance_can_fail_mode(prompt: Prompt, mashes: i32, decrease_speed: f32, start_progress: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(mashes), NativeArg::float(decrease_speed), NativeArg::float(start_progress)];
    unsafe { native::invoke(0xDC0CB602DEADBA53, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_mash_with_resistance_mode(prompt: Prompt, mashes: i32, p2: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(mashes), NativeArg::float(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0xCD1BDFF15EFA79F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_ordering_as_input_type(prompt: Prompt, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), p1];
    unsafe { native::invoke(0x2F385ECC5200938D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_pressed_timed_mode(prompt: Prompt, depletion_time_ms: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(depletion_time_ms)];
    unsafe { native::invoke(0x1473D3AF51D54276, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_priority(prompt: Prompt, priority: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(priority)];
    unsafe { native::invoke(0xCA24F528D0D16289, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_prompt_priority_preference(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x530A428705BE5DEF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_register_horizontal_orientation() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD9459157EB22C895, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_rotate_mode(prompt: Prompt, p1: f32, counterclockwise: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(p1), NativeArg::boolean(counterclockwise)];
    unsafe { native::invoke(0x7ABE7095FB3D2581, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_spinner_position(prompt: Prompt, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), p1];
    unsafe { native::invoke(0x832CB510DE546282, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_spinner_speed(prompt: Prompt, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), p1];
    unsafe { native::invoke(0xAC6586A7FDCD4B68, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_standardized_hold_mode(prompt: Prompt, hold_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::hash(hold_type)];
    unsafe { native::invoke(0x74C7D7B72ED0D3CF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_standard_mode(prompt: Prompt, release_mode: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::boolean(release_mode)];
    unsafe { native::invoke(0xCC6656799977741B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_tag(prompt: Prompt, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), p1];
    unsafe { native::invoke(0xDEC85C174751292B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_target_mode(prompt: Prompt, p1: f32, p2: f32, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(p1), NativeArg::float(p2), p3];
    unsafe { native::invoke(0x5F6503D9CD2754EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_target_mode_progress(prompt: Prompt, progress: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(progress)];
    unsafe { native::invoke(0x00123054BEC8A30F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_target_mode_target(prompt: Prompt, p1: f32, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::float(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0x5E019C45DD3B6A14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_text(prompt: Prompt, text: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::string(text)];
    unsafe { native::invoke(0x5DD02A8318420DD7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_transport_mode(prompt: Prompt, mode: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(mode)];
    unsafe { native::invoke(0x876E4A35C73A6655, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_type(prompt: Prompt, type_: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::int(type_)];
    unsafe { native::invoke(0xF4A5C4509BF923B1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_urgent_pulsing_enabled(prompt: Prompt, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC5F428EE08FA7F2C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_set_visible(prompt: Prompt, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prompt), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x71215ACCFDE075EE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_prompt_was_beat_mode_pressed_in_time_window(prompt: Prompt) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prompt)];
    unsafe { native::invoke(0x1FE4788AB1430C55, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// IK
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_b9_f7_a01_ec50448_d(ped: Ped, args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(args)];
    unsafe { native::invoke(0x0B9F7A01EC50448D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6098139150_dcc745(ped: Ped, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x6098139150DCC745, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x873_c792_e07_a32_c8_b(ped1: Ped, ped2: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2)];
    unsafe { native::invoke(0x873C792E07A32C8B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inverse_kinematics_request_look_at(ped: Ped, args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(args)];
    unsafe { native::invoke(0x66F9EB44342BB4C5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inverse_kinematics_set_disabled_for_ped(ped: Ped, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x0EABF182FBB63D72, &arguments) }.map(|_| ())
}

// ============================================================================
// INTERACTION
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mouse_cursor_style(sprite_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(sprite_id)];
    unsafe { native::invoke(0x7F5858AAB5A58CCE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mouse_cursor_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF12E4CCAF249DC10, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pointer_is_being_moved() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2B8B605F2A9E64BF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pointer_is_left_button_held() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x61CAE9D1FD055E44, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pointer_is_left_button_just_released() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF7F51A57349739F2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_allow_first_person_mouse_camera_movement() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0546B117BB17548B, &arguments) }.map(|_| ())
}

// ============================================================================
// INTERIOR
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activate_interior_entity_set(interior: Interior, entity_set_name: &str, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(interior), NativeArg::string(entity_set_name), NativeArg::int(p2)];
    unsafe { native::invoke(0x174D0AAB11CED739, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_room_for_entity(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xA1762D5BBFCA13A8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_room_for_game_viewport() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x951A049765E0D450, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn deactivate_interior_entity_set(interior: Interior, entity_set_name: &str, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(interior), NativeArg::string(entity_set_name), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x33B81A2C07A51FFF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_interior(interior: Interior, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(interior), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x3C2B92A1A07D4FCE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_room_for_entity(entity: Entity, interior: Interior, room_hash_key: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(interior), NativeArg::hash(room_hash_key)];
    unsafe { native::invoke(0xBC29A9894C976945, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_room_for_game_viewport(interior_id: i32, room_hash_key: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(interior_id), NativeArg::hash(room_hash_key)];
    unsafe { native::invoke(0x115B4AA8FB28AB43, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_interior_at_coords(x: f32, y: f32, z: f32) -> Result<Interior, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xCDD36C9E5C469070, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_interior_at_coords_with_type(x: f32, y: f32, z: f32, interior_type: &str) -> Result<Interior, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::string(interior_type)];
    unsafe { native::invoke(0xAAD6170AA33B13C0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_interior_at_coords_with_typehash(x: f32, y: f32, z: f32, type_hash: Hash) -> Result<Interior, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::hash(type_hash)];
    unsafe { native::invoke(0x3543AEA1816D1D2B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_interior_from_collision(x: f32, y: f32, z: f32) -> Result<Interior, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x5054D1A5218FA696, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_interior_from_entity(entity: Entity) -> Result<Interior, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xB417689857646F61, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_interior_from_primary_view() -> Result<Interior, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBC8A281FF125C655, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_interior_location_and_namehash(interior: Interior, position: &mut Vector3, name_hash: &mut Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(interior), NativeArg::in_out(position), NativeArg::in_out(name_hash)];
    unsafe { native::invoke(0x8451E87D3C2B0286, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_key_for_entity_in_room(entity: Entity) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x27D7B6F79E1F4603, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_room_key_from_entity(entity: Entity) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x076E46E0EB52AFC6, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_collision_marked_outside(x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xF291396B517E25B2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_interior_entity_set_active(interior: Interior, entity_set_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(interior), NativeArg::string(entity_set_name)];
    unsafe { native::invoke(0x32810CA2125F5842, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_interior_ready(interior: Interior) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(interior)];
    unsafe { native::invoke(0x941560D2D45DBFC8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_interior_scene() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4200F14D6F840A9A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_valid_interior(interior: Interior) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(interior)];
    unsafe { native::invoke(0x017C1B3159F79F6C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pin_interior_in_memory(interior: Interior) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(interior)];
    unsafe { native::invoke(0xBD3D33EABF680168, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn retain_entity_in_interior(entity: Entity, interior: Interior) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(interior)];
    unsafe { native::invoke(0x5BD616735F16BF5C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_interior_in_use(interior: Interior) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(interior)];
    unsafe { native::invoke(0xB5EF6FEF2DC9EBED, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unpin_interior(interior: Interior) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(interior)];
    unsafe { native::invoke(0x07FD1A0B814F6055, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2533_f2_ab0_eb9_c6_f9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x2533F2AB0EB9C6F9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fe2_b3_d5500_b1_b2_e4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFE2B3D5500B1B2E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_interior_minimap_hash(interior: Interior) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(interior)];
    unsafe { native::invoke(0x3039BE60B3749716, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_interior_position(interior: Interior) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(interior)];
    unsafe { native::invoke(0x2C9746D0CA15BE1C, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_interior_entity_set_valid(interior: Interior, entity_set_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(interior), NativeArg::string(entity_set_name)];
    unsafe { native::invoke(0xD56FF170710FC826, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// INVENTORY
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_copy_mp_inventory_to_mission_inventory(p0: bool, p1: bool, b_copy_satchel_items: bool, b_copy_emotes: bool, b_copy_horse: bool, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::boolean(p1), NativeArg::boolean(b_copy_satchel_items), NativeArg::boolean(b_copy_emotes), NativeArg::boolean(b_copy_horse), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x644CCB76A76CFBD6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_disable_mission_inventory_pickups() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE1F389F03DC83673, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_children_in_slot_count(inventory_id: i32, guid: &mut Any, slot_id: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::hash(slot_id)];
    unsafe { native::invoke(0x033EE4B89F3AC545, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_guid_from_itemid(inventory_id: i32, guid: &mut Any, p2: Hash, slot_id: Hash, out_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::hash(p2), NativeArg::hash(slot_id), NativeArg::in_out(out_guid)];
    unsafe { native::invoke(0x886DFD3E185C8A89, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item(inventory_id: i32, in_data: &mut Any, out_data: &mut Any, p3: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(in_data), NativeArg::in_out(out_data), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x9700E8EFC4AB9089, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0349404_a22736740(p0: bool, inventory_id: i32, guid: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::int(inventory_id), NativeArg::in_out(guid)];
    unsafe { native::invoke(0x0349404A22736740, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x46_db71883_ee9_d5_af(data: NativeArg, stats: &str, guid: &mut Any, ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [data, NativeArg::string(stats), NativeArg::in_out(guid), NativeArg::int(ped)];
    unsafe { native::invoke(0x46DB71883EE9D5AF, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6862_e4_d93_f64_cf01(inventory_id: i32, guid: &mut Any, p2: Hash, p3: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::hash(p2), NativeArg::in_out(p3)];
    unsafe { native::invoke(0x6862E4D93F64CF01, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6968_ce7_ac32_f6788(inventory_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id)];
    unsafe { native::invoke(0x6968CE7AC32F6788, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x75_cfac49301_e134_f(databinding_entry_id: Hash, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(databinding_entry_id), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x75CFAC49301E134F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x951847_cef3_d829_ff(p0: NativeArg, out_guid: &mut Any, ped: Ped) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::in_out(out_guid), NativeArg::int(ped)];
    unsafe { native::invoke(0x951847CEF3D829FF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_b4_e793_b1_cb6550_a() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9B4E793B1CB6550A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_e58207_b194488_ac(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x9E58207B194488AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b1_dd74_a1_f5536622(inventory_id: i32, item_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(item_guid)];
    unsafe { native::invoke(0xB1DD74A1F5536622, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d08685_ba892_dbfab(inventory_id: i32, guid: &mut Any, p2: &mut i32, p3: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::in_out(p2), NativeArg::in_out(p3)];
    unsafe { native::invoke(0xD08685BA892DBFAB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e1_f45_a67_a9_f0_dcbc(inventory_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id)];
    unsafe { native::invoke(0xE1F45A67A9F0DCBC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_default_item_slot_info(item: Hash, p1: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::hash(p1)];
    unsafe { native::invoke(0x6452B1D357D81742, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_item_role_max_level_count(inventory_id: i32, e_role_max_level: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::hash(e_role_max_level)];
    unsafe { native::invoke(0xADDD1E7C0ECF7D95, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_item_slot_max_count(provision: Hash, slot_id: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(provision), NativeArg::hash(slot_id)];
    unsafe { native::invoke(0xE80E50BEE276A54A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_add_item_with_guid(inventory_id: i32, guid1: &mut Any, guid2: &mut Any, item: Hash, inventory_item_slot: Hash, p5: i32, add_reason: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid1), NativeArg::in_out(guid2), NativeArg::hash(item), NativeArg::hash(inventory_item_slot), NativeArg::int(p5), NativeArg::hash(add_reason)];
    unsafe { native::invoke(0xCB5D11F9508A928D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_apply_weapon_stats_to_entry(databinding_entry_id: Hash, p1: bool, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(databinding_entry_id), NativeArg::boolean(p1), NativeArg::int(ped)];
    unsafe { native::invoke(0x75CFAC49301E134E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_are_local_changes_allowed(inventory_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id)];
    unsafe { native::invoke(0x0FBBFFC891A97C81, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_compare_guids(guid1: &mut Any, guid2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(guid1), NativeArg::in_out(guid2)];
    unsafe { native::invoke(0x4C543D5DFCD2DAFD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_copy_item_to_inventory(inventory_id: i32, inventory_id_cloned: i32, p2: &mut Any, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::int(inventory_id_cloned), NativeArg::in_out(p2), p3];
    unsafe { native::invoke(0xC04F47D488EF9EBA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_copy_item_to_mission_inventory(guid: &mut Any, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(guid), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x3112ADB9D5F3426B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_create_catalog_item_inspection_effects_entry(data: i32, name: &str, p2: bool, p3: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(data), NativeArg::string(name), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x9D21B185ABC2DBC4, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_create_catalog_item_inspection_stats_entry(data: i32, name: &str, p2: i32, p3: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(data), NativeArg::string(name), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x9D21B185ABC2DBC5, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_create_item_collection(inventory_id: i32, filter_name: &str, slot_id: Hash, size: &mut i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::string(filter_name), NativeArg::hash(slot_id), NativeArg::in_out(size)];
    unsafe { native::invoke(0x80D78BDC9D88EF07, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_create_item_collection_2(collection_size: &mut i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(collection_size)];
    unsafe { native::invoke(0x97A3646645727F42, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_create_item_collection_with_filter(inventory_id: i32, filter: &mut Any, num_in_collection: &mut i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(filter), NativeArg::in_out(num_in_collection)];
    unsafe { native::invoke(0x640F890C3E5A3FFD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_create_sorted_collection(inventory_id: i32, p1: i32, size: &mut i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::int(p1), NativeArg::in_out(size)];
    unsafe { native::invoke(0xBB7F968675B34B0C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_disable_item(inventory_id: i32, item: Hash, gtx_reason: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::hash(item), NativeArg::hash(gtx_reason)];
    unsafe { native::invoke(0x766315A564594401, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_disable_weapons(inventory_id: i32, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id), p1];
    unsafe { native::invoke(0xE3A46370F70F3607, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_does_item_own_equipment(inventory_id: i32, guid: &mut Any, item: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::hash(item)];
    unsafe { native::invoke(0x88B58B83A43A8CAB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_enable_item(inventory_id: i32, item: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::hash(item)];
    unsafe { native::invoke(0x6A564540FAC12211, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_enable_weapons(inventory_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id)];
    unsafe { native::invoke(0xD5D72F1624F3BA7C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_equip_item_with_guid(inventory_id: i32, guid: &mut Any, b_equipped: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::boolean(b_equipped)];
    unsafe { native::invoke(0x734311E2852760D0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_fits_slot_id(item: Hash, slot_id: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::hash(slot_id)];
    unsafe { native::invoke(0x780C5B9AE2819807, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_children_count(inventory_id: i32, parent_guid: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(parent_guid)];
    unsafe { native::invoke(0xE843D21A8E2498AA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_full_inventory_item_data(inventory_id: i32, guid: &mut Any, p2: &mut Any, p3: i32, p4: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::in_out(p2), NativeArg::int(p3), NativeArg::int(p4)];
    unsafe { native::invoke(0x025A1B1FB03FBF61, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_id_from_ped(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x13D234A2A3F66E63, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_child(inventory_id: i32, parent_guid: &mut Any, child_index: NativeArg, out_inventory_item: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(parent_guid), child_index, NativeArg::in_out(out_inventory_item)];
    unsafe { native::invoke(0xCD9A485F2B383B44, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_compatible_slots(item: Hash, out_slot_ids: &mut i32, max_results: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::in_out(out_slot_ids), NativeArg::int(max_results)];
    unsafe { native::invoke(0x9AC53CB6907B4428, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_count_with_guid(inventory_id: i32, guid: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xC97E0D2302382211, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_count_with_itemid(inventory_id: i32, e_inventory_item: Hash, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::hash(e_inventory_item), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xE787F05DFC977BDE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_description_hash(item: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(item)];
    unsafe { native::invoke(0xA4550FE9C512E3DD, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_equipped_in_slot(inventory_id: i32, guid: &mut Any, slot_id: Hash, p3: i32, p4: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::hash(slot_id), NativeArg::int(p3), NativeArg::in_out(p4)];
    unsafe { native::invoke(0xBE012571B25F5ACA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_equipped_in_slot_by_ref(inventory_id: i32, guid: &mut Any, slot_id: Hash, out_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::hash(slot_id), NativeArg::in_out(out_guid)];
    unsafe { native::invoke(0x22E590F108289A9D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_fit_slot(p0: Hash, p1: &mut Any, p2: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::in_out(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xB991FE166FAF84FD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_hidden(inventory_id: i32, guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid)];
    unsafe { native::invoke(0xF9933164965533B7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_inspection_info(item: Hash, info: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::in_out(info)];
    unsafe { native::invoke(0x0C093C1787F18519, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_in_use(inventory_id: i32, guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid)];
    unsafe { native::invoke(0x70E3A884ED000A01, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_is_animal_pelt(item: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(item)];
    unsafe { native::invoke(0x4AEF1FB5B9011D75, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_last_creation(inventory_id: i32, item: Hash, year: &mut i32, month: &mut i32, day: &mut i32, hour: &mut i32, minute: &mut i32, second: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::hash(item), NativeArg::in_out(year), NativeArg::in_out(month), NativeArg::in_out(day), NativeArg::in_out(hour), NativeArg::in_out(minute), NativeArg::in_out(second)];
    unsafe { native::invoke(0x112BCA290D2EB53C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_sound(item: Hash, sound_type: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::int(sound_type)];
    unsafe { native::invoke(0x2E1CDC1FF3B8473E, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_inventory_item_weapon_copy_id(inventory_id: i32, guid: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid)];
    unsafe { native::invoke(0xAB5F12746A099A0E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_is_inventory_item_sound_valid(item: Hash, sound_type: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::int(sound_type)];
    unsafe { native::invoke(0x2BAE4880DCDD560B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_item_expiry_time(item_guid: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(item_guid)];
    unsafe { native::invoke(0x4A606C17276E1BCC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_get_item_from_collection_index(collection_id: i32, item_index: i32, item_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(collection_id), NativeArg::int(item_index), NativeArg::in_out(item_data)];
    unsafe { native::invoke(0x82FA24C3D3FCD9B7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_is_guid_valid(guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(guid)];
    unsafe { native::invoke(0xB881CA836CC4B6D4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_is_inventory_item_equipped(inventory_id: i32, item: Hash, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::hash(item), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x3D10D7179D7034AF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_is_inventory_item_flag_enabled(item: Hash, flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::int(flag)];
    unsafe { native::invoke(0x245D07651B1D183B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_is_item_expired(item_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(item_guid)];
    unsafe { native::invoke(0x0137C77A2EC64536, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_is_player_inventory_mirroring_transactions() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFC7563F482781A3D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_is_using_backup_inventory() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7C7E4AB748EA3B07, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_move_inventory_item(inventory_id: i32, guid1: &mut Any, guid2: &mut Any, slot_id: Hash, quantity: i32, out_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid1), NativeArg::in_out(guid2), NativeArg::hash(slot_id), NativeArg::int(quantity), NativeArg::in_out(out_guid)];
    unsafe { native::invoke(0xDCCAA7C3BFD88862, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_release_item_collection(collection_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(collection_id)];
    unsafe { native::invoke(0x42A2F33A1942E865, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_remove_inventory_items(inventory_id: i32, remove_reason: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::hash(remove_reason)];
    unsafe { native::invoke(0x5D6182F3BCE1333B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_remove_inventory_item_with_guid(inventory_id: i32, guid: &mut Any, quantity: i32, remove_reason: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::int(quantity), NativeArg::hash(remove_reason)];
    unsafe { native::invoke(0x3E4E811480B3AE79, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_remove_inventory_item_with_itemid(inventory_id: i32, item: Hash, quantity: i32, remove_reason: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::hash(item), NativeArg::int(quantity), NativeArg::hash(remove_reason)];
    unsafe { native::invoke(0xB4158C8C9A3B5DCE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_set_inventory_item_hidden(inventory_id: i32, guid: &mut Any, hidden: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::boolean(hidden)];
    unsafe { native::invoke(0x9A113C660AEA3832, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_set_inventory_item_hidden_2(inventory_id: i32, guid: &mut Any, hidden: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::boolean(hidden)];
    unsafe { native::invoke(0xD740F11FBC8AEF43, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_set_inventory_item_inspection_enabled(inventory_id: i32, p1: &mut Any, enabled: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(p1), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x227522FD59DDB7E8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_set_inventory_item_in_use(inventory_id: i32, guid: &mut Any, in_use: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::boolean(in_use)];
    unsafe { native::invoke(0x65A5F70F4A292EBE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_set_inventory_item_weather_effectiveness(inventory_id: i32, guid: &mut Any, weather_effectiveness: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid), NativeArg::int(weather_effectiveness)];
    unsafe { native::invoke(0x6D2F987736A42D4C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_swap_inventory_item(inventory_id: i32, guid1: &mut Any, guid2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid1), NativeArg::in_out(guid2)];
    unsafe { native::invoke(0xF2753D691BCDA314, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_update_inventory_item(inventory_id: i32, guid1: &mut Any, guid2: &mut Any, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::in_out(guid1), NativeArg::in_out(guid2), NativeArg::int(p3)];
    unsafe { native::invoke(0xD80A8854DB5CFBA5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_use_backup_inventory(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xE36D4A38D28D9CFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_use_mission_inventory(enable: bool, mirror_transactions: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(enable), NativeArg::boolean(mirror_transactions)];
    unsafe { native::invoke(0xA6AA9F56BC6CFF58, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inventory_use_satchel_item(inventory_id: i32, e_inventory_item: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(inventory_id), NativeArg::hash(e_inventory_item), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x46743BBFEDBC859E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_carriable_carry_action_prompt_override(data: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(data)];
    unsafe { native::invoke(0xF666EF30F4F0AC4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_item_prompt_info_request(p0: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0xFD41D1D4350F6413, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_use_mission_inventory(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x597F571DDEE3FFAC, &arguments) }.map(|_| ())
}

// ============================================================================
// ITEMDATABASE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_item_by_name(key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x2A610BEE7D341CC4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_item_effect_id_info(key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xCF2D360D27FD1ABF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_item_info(key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xFE90ABBCBFDC13B2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_is_buyable_award_valid(award: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award)];
    unsafe { native::invoke(0x4CE753203FA42214, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x17721003_a66_c72_bf(shop_type: Hash, key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(shop_type), NativeArg::hash(key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x17721003A66C72BF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x388088_bff3681189(bundle: Hash, costtype: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(bundle), NativeArg::hash(costtype)];
    unsafe { native::invoke(0x388088BFF3681189, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_a0_b667_abff87_f6_e(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x3A0B667ABFF87F6E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x48229_ce0_c7938237(award: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(award)];
    unsafe { native::invoke(0x48229CE0C7938237, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x537_a0555_f62_ca01_a(key: Hash, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::int(p1)];
    unsafe { native::invoke(0x537A0555F62CA01A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x799_fcd53358_ed5_fa(bundle: NativeArg, p1: NativeArg) -> Result<i32, NativeError> {
    let arguments = [bundle, p1];
    unsafe { native::invoke(0x799FCD53358ED5FA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_a35_a72_a692_be9_db(p0: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7A35A72A692BE9DB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8870895_ba5_ed9385(key: Hash, tag_type: Hash, out_data: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::hash(tag_type), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x8870895BA5ED9385, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_d029948_ca29409_b(award: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x8D029948CA29409B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_aa29_a5_f13_b2_c20_b2(p0: NativeArg, p1: Hash) -> Result<Hash, NativeError> {
    let arguments = [p0, NativeArg::hash(p1)];
    unsafe { native::invoke(0xAA29A5F13B2C20B2, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c4146375_d8_a0_b374(bundle: NativeArg, p1: NativeArg, index: i32, p3: NativeArg) -> Result<bool, NativeError> {
    let arguments = [bundle, p1, NativeArg::int(index), p3];
    unsafe { native::invoke(0xC4146375D8A0B374, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_can_equip_item_on_category(key: Hash, category: Hash, slot_id: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::hash(category), NativeArg::hash(slot_id)];
    unsafe { native::invoke(0x856FF92C57742AE5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_create_item_collection(data: &mut Any, size: &mut i32, comparison_type: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(data), NativeArg::in_out(size), NativeArg::int(comparison_type)];
    unsafe { native::invoke(0x71EFA7999AE79408, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_does_bundle_have_tag(bundle: Hash, tag: Hash, tag_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(bundle), NativeArg::hash(tag), NativeArg::hash(tag_type)];
    unsafe { native::invoke(0x99C6EA66DFE73757, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_does_item_have_tag(item: Hash, tag: Hash, tag_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::hash(tag), NativeArg::hash(tag_type)];
    unsafe { native::invoke(0xFF5FB5605AD56856, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_acquire_cost(key: Hash, costtype: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::hash(costtype), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x74F7928816E4E181, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_award_acquire_cost(award: Hash, costtype: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award), NativeArg::hash(costtype), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xF27F01BBF5ACD3F3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_award_item_info(award: Hash, index: i32, out_data: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x121D2005DD64496B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_bundle(bundle: Hash, costtype: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(bundle), NativeArg::hash(costtype), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xB542632693D53408, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_bundle_ui_data(bundle: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(bundle), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x74C3B1093728D263, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_buy_award_acquire_costs(award: Hash, out_data: &mut Any, out_unk: &mut i32, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award), NativeArg::in_out(out_data), NativeArg::in_out(out_unk), NativeArg::int(p3)];
    unsafe { native::invoke(0xB52E20F6767A09A2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_buy_award_ui_data(award: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xF8D09EF8CE61D7BF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_item(key: Hash, costtype: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::hash(costtype), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xAD73B614DF26CF8A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_item_effect_ids(key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x9379BE60DC55BBE6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_modifier(key: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x60614A0AB580A2B5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_price_modifier_by_key(key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x40C5D95818823C94, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_satchel_data(key: Hash, out_satchel_item_size: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::in_out(out_satchel_item_size)];
    unsafe { native::invoke(0x4776EFD78F75C23F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_sell_price(key: Hash, sell_type: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::hash(sell_type), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x7A62A2EEDE1C3766, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_tag_data(key: Hash, out_data: &mut Any, out_index: &mut i32, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::in_out(out_data), NativeArg::in_out(out_index), NativeArg::int(p3)];
    unsafe { native::invoke(0x5A11D6EEA17165B0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_fillout_ui_data(key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xB86F7CC2DC67AC60, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_acquire_cost(key: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x6772A83C67A25775, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_acquire_costs_count(key: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(key)];
    unsafe { native::invoke(0x01FDDAD392D04144, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_acquire_costs_count_from_cost_type(key: Hash, costtype: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::hash(costtype)];
    unsafe { native::invoke(0xDEE7B3C76ED664BE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_award_acquire_cost(award: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x1FC25AEB5F76B38D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_award_acquire_cost_count(key: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(key)];
    unsafe { native::invoke(0x12DF9C58201DD19A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_award_acquire_cost_count_from_cost_type(award: Hash, costtype: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(award), NativeArg::hash(costtype)];
    unsafe { native::invoke(0xF540239F9937033B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_award_cost_modifiers(award: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xE81D0378A384E755, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_award_info(award: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xD076DB9B96FAADF1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_award_item_count(award: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(award)];
    unsafe { native::invoke(0x3FAA928A79591761, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_bundle_acquire_cost_modifiers(bundle: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(bundle), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xA97EE5E4589FCF5A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_bundle_id(bundle: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(bundle)];
    unsafe { native::invoke(0x891A45960B6B768A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_bundle_item_count(bundle_id: i32, data: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(bundle_id), NativeArg::in_out(data)];
    unsafe { native::invoke(0x3332695B01015DF9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_bundle_item_info(bundle_id: i32, data: &mut Any, index: i32, out_bundle: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(bundle_id), NativeArg::in_out(data), NativeArg::int(index), NativeArg::in_out(out_bundle)];
    unsafe { native::invoke(0x5D48A77E4B668B57, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_collection_size(collection_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(collection_id)];
    unsafe { native::invoke(0xD389A2549C4EFB30, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_component_item(collection_id: i32, index: i32, out_key: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(collection_id), NativeArg::int(index), NativeArg::in_out(out_key)];
    unsafe { native::invoke(0x8750F69A720C2E41, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_fits_slot_count(category: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(category)];
    unsafe { native::invoke(0x2970D1D6BFCF9B46, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_fits_slot_info(category: Hash, index: i32, out_slot_id: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::int(index), NativeArg::in_out(out_slot_id)];
    unsafe { native::invoke(0x77210C146CED5261, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_has_slot_count(category: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(category)];
    unsafe { native::invoke(0x44915068579D7710, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_has_slot_info(category: Hash, index: i32, out_slot_id: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::int(index), NativeArg::in_out(out_slot_id)];
    unsafe { native::invoke(0x8A9BD0DB7E8376CF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_item_pathset(key: Hash, default_pathset: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::hash(default_pathset)];
    unsafe { native::invoke(0xF4452CE83118C738, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_item_price_modifiers(key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x4EB37AAB79AB0C48, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_item_tag_type(item: Hash, tag: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(item), NativeArg::hash(tag)];
    unsafe { native::invoke(0x6111B8F9413F413A, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_modified_price(key: Hash, index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::int(index)];
    unsafe { native::invoke(0xCB92EC9C004732B4, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_number_of_modified_prices(key: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(key)];
    unsafe { native::invoke(0x5AAAF40E9B224F5E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_number_of_modifiers(key: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(key)];
    unsafe { native::invoke(0x1289D8315235856D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_priority_access_award(award: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(award)];
    unsafe { native::invoke(0xEF254F1A4C08B7E6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_inventories_items_count(shop_type: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(shop_type)];
    unsafe { native::invoke(0xC568B1A0F17C7025, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_inventories_item_info(shop_type: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(shop_type), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x4A79B41B4EB91F4E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_inventories_item_info_by_key(shop_type: Hash, key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(shop_type), NativeArg::hash(key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xCFB06801F5099B25, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_inventories_requirement_group_info(shop_type: Hash, key: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(shop_type), NativeArg::hash(key), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x76C752D788A76813, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_inventories_requirement_info(shop_type: Hash, key: Hash, group_index: i32, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(shop_type), NativeArg::hash(key), NativeArg::int(group_index), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xE0EA5C031AE5539F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_layout_info(layout: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(layout), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x66A6D76B6BB999B4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_layout_menu_info_by_id(layout: Hash, menu: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(layout), NativeArg::hash(menu), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xD66114469978B55B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_layout_menu_info_by_index(layout: Hash, menu: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(layout), NativeArg::hash(menu), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xF04247092F193B75, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_layout_menu_page_key(layout: Hash, menu: Hash, index: i32, out_page_key: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(layout), NativeArg::hash(menu), NativeArg::int(index), NativeArg::in_out(out_page_key)];
    unsafe { native::invoke(0x9A60570657A7B635, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_layout_page_info_by_index(layout: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(layout), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xDBEADA0DF5F9AB9F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_layout_page_info_by_key(layout: Hash, page_key: Hash, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(layout), NativeArg::hash(page_key), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xB347C100DF0C9B7F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_layout_page_item_key(layout: Hash, page_key: Hash, index: i32, out_item_key: &mut Hash, out_menu_id: &mut i32, out_layout: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(layout), NativeArg::hash(page_key), NativeArg::int(index), NativeArg::in_out(out_item_key), NativeArg::in_out(out_menu_id), NativeArg::in_out(out_layout)];
    unsafe { native::invoke(0xF32BEF578B3DBAE8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_get_shop_layout_root_menu_info(layout: Hash, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(layout), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x86FCB565CCA0CFA7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_is_bundle_valid(bundle: Hash, mode: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(bundle), NativeArg::int(mode)];
    unsafe { native::invoke(0x4308812A6E9CA62E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_is_intrinsic_item(key: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key)];
    unsafe { native::invoke(0x337F88E3A063995E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_is_key_valid(key: Hash, mode: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key), NativeArg::int(mode)];
    unsafe { native::invoke(0x6D5D51B188333FD1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_is_overpowered_item(key: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(key)];
    unsafe { native::invoke(0x337F88E3A063995F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_is_shop_key_valid(shop_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(shop_type)];
    unsafe { native::invoke(0x00B9507D8E1D8716, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_is_shop_layout_key_valid(layout: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(layout)];
    unsafe { native::invoke(0x3AFE5182C45A84F6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_localization_get_num_label_types(p0: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCEC6A41E8910486A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_localization_get_num_values(p0: NativeArg, p1: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x49885D82A13EEAEA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_localization_get_type(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCABF5D41D0073D4A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_localization_get_value(p0: NativeArg, label: Hash, p2: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0, NativeArg::hash(label), p2];
    unsafe { native::invoke(0x9AE5610FDCED6EA7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn itemdatabase_release_item_collection(collection_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(collection_id)];
    unsafe { native::invoke(0xCBB7B6EDFA933ADE, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// ITEMSET
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_to_itemset(entity: Entity, itemset: ItemSet) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(itemset)];
    unsafe { native::invoke(0xABE74510883C7950, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clean_itemset(itemset: ItemSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(itemset)];
    unsafe { native::invoke(0x85F3A86CA9021FB0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_itemset(p0: bool) -> Result<ItemSet, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xA1AF16083320065A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn destroy_itemset(itemset: ItemSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(itemset)];
    unsafe { native::invoke(0x712BC69F10549B92, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_indexed_item_in_itemset(index: i32, itemset: ItemSet) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::int(index), NativeArg::int(itemset)];
    unsafe { native::invoke(0x275A2E2C0FAB7612, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_indexed_scenario_point_index_in_itemset(index: i32, itemset: ItemSet) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(index), NativeArg::int(itemset)];
    unsafe { native::invoke(0x9FC3CDB5CE815901, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_itemset_size(itemset: ItemSet) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(itemset)];
    unsafe { native::invoke(0x55F2E375AC6018A9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_in_itemset(entity: Entity, itemset: ItemSet) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(itemset)];
    unsafe { native::invoke(0xD1503C2EE2FE688C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_itemset_valid(itemset: ItemSet) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(itemset)];
    unsafe { native::invoke(0xD30765D153EF5C76, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_from_itemset(entity: Entity, itemset: ItemSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(itemset)];
    unsafe { native::invoke(0xC5BAA432B429DC24, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_itemset(itemset: ItemSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(itemset)];
    unsafe { native::invoke(0x20A4BF0E09BEE146, &arguments) }.map(|_| ())
}

// ============================================================================
// LAW
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_bounty(player: Player, item_value_amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(item_value_amount)];
    unsafe { native::invoke(0x0E3BDEED21BEB945, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_witnesses_active(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x69E181772886F48B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_bounty(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xC76F252371150D9A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_player_past_crimes(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xBCC6DC59E32A2BDC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_wanted_score(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x062B4A4A3396351D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_bounty(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x54310AAB97B92816, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_registered_crime(player: Player, index: i32, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(index), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x532C5FDDB986EE5C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_wanted_score(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xDD5FD601481F648B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_law_incident_active(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xAD401C63158ACBAA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn num_crimes_suppressed(player: Player, crime_type: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(crime_type)];
    unsafe { native::invoke(0xC08E804C91F47C80, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_wanted_for_new_incident(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x2728C77FBC4B9796, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_bounty(player: Player, amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(amount)];
    unsafe { native::invoke(0x093A9D1F72DF0D19, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_disturbance_crimes(player: Player, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xDE5FAA741A781F73, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_law_sense_range_modifier(player: Player, range: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(range)];
    unsafe { native::invoke(0xFEC85339AACA2A35, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_arrested_in_region(player: Player, law_region_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(law_region_hash)];
    unsafe { native::invoke(0xE0FA74AA3CCE650B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_turned_in_bounty_in_region(player: Player, law_region_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(law_region_hash)];
    unsafe { native::invoke(0x73BAD7B2F2DB50DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_postpone_disturbance_crimes_during_combat(player: Player, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x362086B911657B1A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_wanted_score(player: Player, intensity: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(intensity)];
    unsafe { native::invoke(0xA80FF73F772ACF6A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn suppress_crime_this_frame(player: Player, crime_type: Hash, p2: i32, p3: i32, p4: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(crime_type), NativeArg::int(p2), NativeArg::int(p3), NativeArg::int(p4)];
    unsafe { native::invoke(0x785177E4D57D7389, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x00_db0_bc05_e3_faa4_e(ped: Ped, bitset: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(bitset)];
    unsafe { native::invoke(0x00DB0BC05E3FAA4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x018_f30_d762_e62_df8(ped: Ped, p1: &mut Any) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x018F30D762E62DF8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x07_e8_b8_b20570271_c(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x07E8B8B20570271C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_bdfebcf40_a5_f7_e3(crime_type: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(crime_type)];
    unsafe { native::invoke(0x0BDFEBCF40A5F7E3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_c392_db374655176(x: f32, y: f32, z: f32, p3: f32, item_set: ItemSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3), NativeArg::int(item_set)];
    unsafe { native::invoke(0x0C392DB374655176, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_dbaca9_c38_c9_a686(name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x0DBACA9C38C9A686, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_eaf918_f751_f27_ba(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0EAF918F751F27BA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_f230_de0_ddbe3649(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0F230DE0DDBE3649, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x15_abd5004_cad2_d99(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x15ABD5004CAD2D99, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2001687_f9562_fd9_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2001687F9562FD9D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x21213_b833_ef4_dae7(player: Player, ped: Ped, out_coords: &mut Vector3) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::in_out(out_coords)];
    unsafe { native::invoke(0x21213B833EF4DAE7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x22741652985_c84_d0(player: Player, law_region_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(law_region_hash)];
    unsafe { native::invoke(0x22741652985C84D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x26934083_d3_f2579_c(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x26934083D3F2579C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x292_ad61_a33_a7_a485() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x292AD61A33A7A485, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x29_cd4896_ecb66_c12() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x29CD4896ECB66C12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x318_f0_f9_a4426_cfa2(ped: Ped, p1: &mut Any) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x318F0F9A4426CFA2, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x331_d349_e0380_b097(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x331D349E0380B097, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3738_b784_ddd35_cc6(player: Player, p1: i32, p2: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x3738B784DDD35CC6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3852237_a3_d9_df145(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x3852237A3D9DF145, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x390710_d2_dafa6_bff(player: Player, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x390710D2DAFA6BFF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_d2674828_a4_e6_b3_c() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3D2674828A4E6B3C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x40851_bcc33_acd9_ab(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x40851BCC33ACD9AB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x522_f74636_df10201(player: Player, item_set: ItemSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(item_set)];
    unsafe { native::invoke(0x522F74636DF10201, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_e6_f375_ca101_c108(player: Player, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x5E6F375CA101C108, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x61_b98367_d93_f012_f(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x61B98367D93F012F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_abc50979655_bee7(player: Player, crime_type: &mut Hash, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(crime_type), p2];
    unsafe { native::invoke(0x6ABC50979655BEE7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7351_da734_f989_f4_e(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x7351DA734F989F4E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7803436_e68_c32_b26() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7803436E68C32B26, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_ef2_a2_fe38_d74456(flag: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(flag), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x7EF2A2FE38D74456, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_fc667_f6_ddfbcdcc(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x7FC667F6DDFBCDCC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x82_f11_e1296996574(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x82F11E1296996574, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x856_ce8_fde2416602(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x856CE8FDE2416602, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x89_e005_b1662_f6_e48(player: Player, p1: i32, p2: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x89E005B1662F6E48, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x95878_b13_e272_ef1_f(entity: Entity, ped: Ped, p2: bool, x: f32, y: f32, z: f32, crime_type: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(ped), NativeArg::boolean(p2), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::hash(crime_type)];
    unsafe { native::invoke(0x95878B13E272EF1F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9772395_cc73_e8_d1_f(ped: Ped, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(name)];
    unsafe { native::invoke(0x9772395CC73E8D1F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x987_be590_fb9_d41_e5(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x987BE590FB9D41E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9945_a3_e2528_a02_e8(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x9945A3E2528A02E8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_b4_c564_bfa7_cff37(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x9B4C564BFA7CFF37, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_c5_bd8_c562565_ce6(crime_type: &mut Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(crime_type)];
    unsafe { native::invoke(0x9C5BD8C562565CE6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_c8_a2_bf37_e966464(player: Player, item_set: ItemSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(item_set)];
    unsafe { native::invoke(0x9C8A2BF37E966464, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_d5_c9_a5_a3321_b128(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x9D5C9A5A3321B128, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_ef07_cfbb19_a9733() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9EF07CFBB19A9733, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b527099_d1_e1_eed49(player: Player, p1: i32, crime_type: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1), NativeArg::in_out(crime_type)];
    unsafe { native::invoke(0xB527099D1E1EED49, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bd944_a3_d36_e992_de() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBD944A3D36E992DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c0_df161950_fb101_e(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC0DF161950FB101E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c310239_acccf5579() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC310239ACCCF5579, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c5_eb2755_fa25_f1_e9(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xC5EB2755FA25F1E9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c687_a23_e166_dcf68(p0: &mut Any) -> Result<Any, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0xC687A23E166DCF68, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c7_dc5_a0_a7_df608_cb(flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flag)];
    unsafe { native::invoke(0xC7DC5A0A7DF608CB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cbfb4951_f2_e3934_c(player: Player, data: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(data)];
    unsafe { native::invoke(0xCBFB4951F2E3934C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d6_c0_a8_c7_c0_b2_f82_c(player: Player, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xD6C0A8C7C0B2F82C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d7494_ded50_c6_ef52(player: Player, crime_type: Hash, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(crime_type), NativeArg::int(p2)];
    unsafe { native::invoke(0xD7494DED50C6EF52, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_da1_a9_adc4_e3_d4_b16(item_set: ItemSet, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(item_set), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xDA1A9ADC4E3D4B16, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_daefdfdb2_aeece37(crime_type: Hash, p1: NativeArg) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(crime_type), p1];
    unsafe { native::invoke(0xDAEFDFDB2AEECE37, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dcf12_b89624_aac96(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xDCF12B89624AAC96, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dea083_c16_bb91345() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDEA083C16BB91345, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e083_beda81709891(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xE083BEDA81709891, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e4_d6_e45_f491_a66_cb(player: Player, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1)];
    unsafe { native::invoke(0xE4D6E45F491A66CB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e94_b5_e938619712_e() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE94B5E938619712E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e9_ac8466_abe484_bb(p0: bool, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), p1];
    unsafe { native::invoke(0xE9AC8466ABE484BB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e9_eb79_cbf9_c0_f58_a(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xE9EB79CBF9C0F58A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_edfc6_c1_fd1_c964_f5(player: Player, crime_type: Hash, bounty: i32, p3: f32, p4: f32, p5: bool, p6: f32, p7: f32, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(crime_type), NativeArg::int(bounty), NativeArg::float(p3), NativeArg::float(p4), NativeArg::boolean(p5), NativeArg::float(p6), NativeArg::float(p7), p8];
    unsafe { native::invoke(0xEDFC6C1FD1C964F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f46108_c50_a22_b029() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF46108C50A22B029, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f611_de44_aeb36_a1_d(crime_type: Hash, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(crime_type), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xF611DE44AEB36A1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ffebe5_aa96_bc2_e4_e(ped: Ped, crime_type: Hash, p2: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(crime_type), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xFFEBE5AA96BC2E4E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_witness_response(player: Player, crime_type: Hash, ped_group: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(crime_type), NativeArg::int(ped_group)];
    unsafe { native::invoke(0x10827B5A0AAC56A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_any_law_peds_investigating() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xECE3C34B270428D5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_investigators_active(player: Player, are_investigators_active: bool, p2: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(are_investigators_active), p2];
    unsafe { native::invoke(0xF0FBFB9AB15F7734, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_law_peds_enabled_for_train() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA22C46F16359471C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_witnesses_pending(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0BB6DE7D23C60626, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_guard_zone(name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x8F9DE75680275C9F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_guard_zone_for_entity(guard_zone_name: &str, entity: Entity, x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(guard_zone_name), NativeArg::int(entity), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x0D4B77E862475ED3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_law_dispatch_response_for_coords(x: f32, y: f32, z: f32, dispatch_response_hash: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::hash(dispatch_response_hash)];
    unsafe { native::invoke(0x75CBF20BA47E4F89, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_guard_zone(name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x26D558692B25DD95, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_dispatch_law(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC805EB785824F712, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_dispatch_law_2(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x710448D44A64C213, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_law_on_local_player_immediately() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x956510F8C36B5C64, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_bounty_hunter_global_cooldown(p0: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x76CF93D4B416B288, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_crime_bounty_amount_by_type(crime_type: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(crime_type)];
    unsafe { native::invoke(0x35E5E21F9159849C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_crime_dispatch_type_for_player(player: Player) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x148E7AC8141C9E64, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_hud_player_crime_type(player: Player) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x259CE340A8738814, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_time_since_last_seen_by_law(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x717DA2281DF90855, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_guard_ped_investigating(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD743C4293F47AFAD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn law_witness_response_task(ped_group1: Ped, ped: Ped, ped_group2: Ped, x: f32, y: f32, z: f32, crime_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped_group1), NativeArg::int(ped), NativeArg::int(ped_group2), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::hash(crime_type)];
    unsafe { native::invoke(0xF0B67BAD53C35BD9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pause_bounty_hunter_cooldown(p0: Hash, p1: bool, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::boolean(p1), p2];
    unsafe { native::invoke(0xC61EDEBF16CD9668, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_guard_zone(name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x67EBDD958835956C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn report_crime(player: Player, crime_type: Hash, bounty: i32, entity: Entity, is_known_suspect: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(crime_type), NativeArg::int(bounty), NativeArg::int(entity), NativeArg::boolean(is_known_suspect)];
    unsafe { native::invoke(0xF60386770878A98F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn report_player_law_dispatch_response_override(player: Player, dispatch_response_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(dispatch_response_hash)];
    unsafe { native::invoke(0x9C4352134B2835FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_allow_disabled_law_responses(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x4B52BF96E225D230, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_bounty_hunter_global_cooldown(p0: Hash, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::int(p1)];
    unsafe { native::invoke(0xF19706B1F8FFA88F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_bounty_hunter_pursuit_cleared() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x55F37F5F3F2475E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cant_lose_law_this_response(enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(enabled)];
    unsafe { native::invoke(0xDDCE8E960D1DE240, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_custom_law_dispatch_response(dispatch_response_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(dispatch_response_hash)];
    unsafe { native::invoke(0x009CF9A29972C298, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_dispatch_multiplier_override(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0x002BABE0B7D53136, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_guard_zone_position(name: &str, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x7E7BF59F89FC6C6D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_guard_zone_position_2(name: &str, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x2F9005E2EA4E5EE4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_guard_zone_volume_registration_end(name: &str, volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::int(volume)];
    unsafe { native::invoke(0xA8A74AA79FB67159, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_guard_zone_volume_registration_start(name: &str, volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::int(volume)];
    unsafe { native::invoke(0x8C598A930F471938, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_guard_zone_volume_restricted(name: &str, volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::int(volume)];
    unsafe { native::invoke(0x35815F372D43E1E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_guard_zone_volume_threat(name: &str, volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::int(volume)];
    unsafe { native::invoke(0xA1B0E6301E2E02A6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_guard_zone_volume_warning(name: &str, volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::int(volume)];
    unsafe { native::invoke(0xAD3E07C37A7C1ADC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_law_disabled(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x8DE82BC774F3B862, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_law_rbs_volume(player: Player, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(p1)];
    unsafe { native::invoke(0x9BBDCB8DF789EBC1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_law_region(player: Player, law_region_hash: Hash, state_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(law_region_hash), NativeArg::hash(state_hash)];
    unsafe { native::invoke(0x4752F68EB7F2D280, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_law_behaviour(ped: Ped, behaviour: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(behaviour)];
    unsafe { native::invoke(0x819ADD5EF1742F47, &arguments) }.map(|_| ())
}

// ============================================================================
// LOCALIZATION
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_language() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDB917DA5C6835FCC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn localization_get_system_date_type() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x76E30B799EBEEA0F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn localization_get_system_language() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3C1A05F86AE6ACB5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_current_language_support_condensed_style() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x45D50415E4D885FF, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// MAP
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_point_to_gps_multi_route(x: f32, y: f32, z: f32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x64C59DD6834FA942, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn allow_sonar_blips(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6E6E64788C07D2E0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_add_for_coords(blip_hash: Hash, x: f32, y: f32, z: f32) -> Result<Blip, NativeError> {
    let arguments = [NativeArg::hash(blip_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x554D9D53F696D002, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_add_for_entity(blip_hash: Hash, entity: Entity) -> Result<Blip, NativeError> {
    let arguments = [NativeArg::hash(blip_hash), NativeArg::int(entity)];
    unsafe { native::invoke(0x23F74C2FDA6E7C61, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_add_for_pickup_placement(blip_hash: Hash, pickup: Pickup) -> Result<Blip, NativeError> {
    let arguments = [NativeArg::hash(blip_hash), NativeArg::int(pickup)];
    unsafe { native::invoke(0xA486008892065FB9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_add_for_radius(blip_hash: Hash, x: f32, y: f32, z: f32, radius: f32) -> Result<Blip, NativeError> {
    let arguments = [NativeArg::hash(blip_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0x45F13B7E0A15C880, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_add_modifier(blip: Blip, modifier_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::hash(modifier_hash)];
    unsafe { native::invoke(0x662D364ABF16DE2F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_remove_modifier(blip: Blip, modifier_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::hash(modifier_hash)];
    unsafe { native::invoke(0xB059D7BD3D78C16F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_gps_custom_route() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1EAA5674B4D181C5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_gps_flags() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4D3771237C79FF41, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_gps_multi_route() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9E0AB9AAEE87CE28, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_gps_player_waypoint() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x08FDC6F796E350D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn display_radar(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x1B3DA717B9AFF828, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_blip_exist(blip: Blip) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(blip)];
    unsafe { native::invoke(0xCD82FA174080B3B1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_sonar_blips_this_frame() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEE1C7BA69BB74B08, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_blip_coords(blip: Blip) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(blip)];
    unsafe { native::invoke(0x201C319797BDA603, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_blip_from_entity(entity: Entity) -> Result<Blip, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x6D2C41A8BD6D6FD0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_main_player_blip_id() -> Result<Blip, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5CD2889B2B381D45, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_blip_on_minimap(blip: Blip) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(blip)];
    unsafe { native::invoke(0x46534526B9CD2D17, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_waypoint_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x202B1BBFC6AB5EE4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn lock_minimap_angle(angle: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(angle)];
    unsafe { native::invoke(0x0BFD145EF819FB3A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_blip(blip: &mut Blip) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(blip)];
    unsafe { native::invoke(0xF2C3C9DA47AAA54A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_minimap_fow(hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xEB3CB3386C775D72, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_coords(blip: Blip, pos_x: f32, pos_y: f32, pos_z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z)];
    unsafe { native::invoke(0x4FF674F5E23D49CE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_flashes(blip: Blip, p1: &mut i32, p2: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::in_out(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x0DF2B55F717DDB10, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_flash_timer(blip: Blip, blip_type: i32, blip_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::int(blip_type), NativeArg::hash(blip_hash)];
    unsafe { native::invoke(0x02FF4CF43B7209D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_name_from_text_file(blip: Blip, text_label: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::string(text_label)];
    unsafe { native::invoke(0x0A062D6D7C0B2C2C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_name_to_player_name(blip: Blip, player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::int(player)];
    unsafe { native::invoke(0x093DD5A31BC2B459, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_rotation(blip: Blip, rotation: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::int(rotation)];
    unsafe { native::invoke(0x6049966A94FBE706, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_scale(blip: Blip, scale: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::float(scale)];
    unsafe { native::invoke(0xD38744167B2FA257, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_sprite(blip: Blip, hash: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::hash(hash), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x74F74D3207ED525C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gps_custom_route_render(p0: bool, p1: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xF6CEF599FC470B33, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gps_flags(p0: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x5DE61C90DDECFA2D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gps_multi_route_render(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x4426D65E029A4DC0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_minimap_fow_reveal_coordinate(x: f32, y: f32, z: f32, p3: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::hash(p3)];
    unsafe { native::invoke(0x73348402566ECB6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_minimap_fow_reveal_volume(volume: Volume, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::hash(p1)];
    unsafe { native::invoke(0x63CBBD6CA6F321F9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_minimap_hide_fow(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x4B8F743A4A6D2FF8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_radar_as_exterior_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA8EBBAE986FB5457, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_radar_zoom(zoom_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(zoom_level)];
    unsafe { native::invoke(0xCAF6489DA2C8DD9E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_waypoint_off() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFA8C41E8020D3439, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_gps_multi_route(color_name_hash: Hash, on_foot: bool, in_vehicle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(color_name_hash), NativeArg::boolean(on_foot), NativeArg::boolean(in_vehicle)];
    unsafe { native::invoke(0x3D3D15AF7BCAAF83, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn trigger_sonar_blip(type_hash: Hash, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(type_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x72DD432F3CDFC0EE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_minimap_angle() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5373DE8E179BC2A0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d3_f58_e9316_b7_fc2_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD3F58E9316B7FC2A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn abandon_blip(blip: Blip) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip)];
    unsafe { native::invoke(0xDEEDE7C41742E011, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_prop_to_minimap(minimap_prop: Hash, x: f32, y: f32, rotation: f32, p4: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(minimap_prop), NativeArg::float(x), NativeArg::float(y), NativeArg::float(rotation), NativeArg::int(p4)];
    unsafe { native::invoke(0x1392105DA88BBFFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_add_for_area(blip_hash: Hash, x: f32, y: f32, z: f32, scale_x: f32, scale_y: f32, scale_z: f32, p7: i32) -> Result<Blip, NativeError> {
    let arguments = [NativeArg::hash(blip_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z), NativeArg::int(p7)];
    unsafe { native::invoke(0xEC174ADBCB611ECC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_add_for_style(style_hash: Hash) -> Result<Blip, NativeError> {
    let arguments = [NativeArg::hash(style_hash)];
    unsafe { native::invoke(0x3E593DF9C2962EC6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_add_for_volume(blip_hash: Hash, volume: Volume) -> Result<Blip, NativeError> {
    let arguments = [NativeArg::hash(blip_hash), NativeArg::int(volume)];
    unsafe { native::invoke(0xA6EF0C54A3443E70, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_add_style(blip: Blip, style_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::hash(style_hash)];
    unsafe { native::invoke(0xBD62D98799A3DAF0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn blip_set_style(blip: Blip, style_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::hash(style_hash)];
    unsafe { native::invoke(0xEDD964B7984AC291, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_blip(blip: Blip) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(blip)];
    unsafe { native::invoke(0x01B928CA2E198B01, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_blip_icon_from_lockon_entity_prompt(entity: Entity, blip: Blip) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(blip)];
    unsafe { native::invoke(0x44813684F72B563C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_pausemap_coords() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7C9F4CDF402CA82A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_entity_have_blip(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x9FA00E2FC134A9D0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn find_closest_gps_position(x: f32, y: f32, z: f32, out_position: &mut Vector3) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(out_position)];
    unsafe { native::invoke(0x3FDA2B79AEEE351C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_waypoint_coords() -> Result<Vector3, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x29B30D07C3F7873B, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_waypoint_position(x: &mut f32, y: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(x), NativeArg::in_out(y)];
    unsafe { native::invoke(0xF08E42BFA46BDFF8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hide_active_points_of_interest() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA1B4052C2A3DCC1E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_blip_attached_to_any_entity(blip: Blip) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(blip)];
    unsafe { native::invoke(0xE9F676788F8D5E1E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_display_blip_icon_on_lockon_entity_prompt(entity: Entity, blip: Blip) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(blip)];
    unsafe { native::invoke(0x3CB8859F04763C78, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_path_for_gps_on_road() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF47A1EB2A538A3A3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn map_disable_region_blip(region_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(region_hash)];
    unsafe { native::invoke(0x6786D7AFAC3162B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn map_discovery_set_enabled(discovery_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(discovery_hash)];
    unsafe { native::invoke(0xDA98246C7A3C2189, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn map_discover_region(discovery_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(discovery_hash)];
    unsafe { native::invoke(0xD8C7162AB2E2AF45, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn map_enable_region_blip(region_hash: Hash, style_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(region_hash), NativeArg::hash(style_hash)];
    unsafe { native::invoke(0x563FCB6620523917, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn map_is_discovery_active(discovery_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(discovery_hash)];
    unsafe { native::invoke(0x3F81EA4275D39D6F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn map_is_region_highlighted_with_style(region_hash: Hash, style_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(region_hash), NativeArg::hash(style_hash)];
    unsafe { native::invoke(0xE38450DBCBC70E3D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_prop_from_minimap(minimap_prop: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(minimap_prop)];
    unsafe { native::invoke(0xE057FEA9A22EB3EE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reveal_minimap_fow(hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xF8096DF9B87246E3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_frozen(blip: Blip) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip)];
    unsafe { native::invoke(0x250C75EB1728CC0D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blip_name(blip: Blip, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(blip), NativeArg::string(name)];
    unsafe { native::invoke(0x9CB1A1623062F402, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_display_blip_icon_for_entity_prompt_removed(entity: Entity, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1)];
    unsafe { native::invoke(0xBB68D4D3CA3DE402, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_display_blip_icon_for_entity_prompt_without_lockon(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x1726963E6049DB53, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_display_blip_icon_for_entity_prompt_with_lockon(entity: Entity, blip_icon: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(blip_icon)];
    unsafe { native::invoke(0x7563CBCA99253D1A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_display_blip_icon_to_lockon_entity_prompt(entity: Entity, blip: Blip) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(blip)];
    unsafe { native::invoke(0x97F6F158CC5B5CA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_fow_update_player_override(toggle: bool, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle), NativeArg::hash(p1)];
    unsafe { native::invoke(0x63E7279D04160477, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_minimap_fow_override_reveal_scale(scale: f32, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(scale), NativeArg::hash(p1)];
    unsafe { native::invoke(0xE5A7F70B7C0F3271, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_minimap_fow_should_update(toggle: bool, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle), NativeArg::hash(p1)];
    unsafe { native::invoke(0x632AA10BF7EA53D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_minimap_zone(zone: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(zone)];
    unsafe { native::invoke(0xA657EC9DBC6CC900, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pausemap_coords_with_radius(x: f32, y: f32, z: f32, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0xE0884C184728C75B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_radar_config_type(config_hash: Hash, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(config_hash), NativeArg::hash(p1)];
    unsafe { native::invoke(0x9C113883487FD53C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn show_active_points_of_interest() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3FBB838AEA30C1D8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_gps_custom_route_from_waypoint_recording_route(waypoint_recording: &str, point: i32, num_points: i32, color_name_hash: Hash, p4: bool, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(waypoint_recording), NativeArg::int(point), NativeArg::int(num_points), NativeArg::hash(color_name_hash), NativeArg::boolean(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x6B44F13D888F770D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn trigger_sonar_blip_on_entity(type_hash: Hash, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(type_hash), NativeArg::int(entity)];
    unsafe { native::invoke(0x0C7A2289A5C4D7C9, &arguments) }.map(|_| ())
}

// ============================================================================
// MINIGAME
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x012027_c28_f421_f46(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x012027C28F421F46, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0876326238914_a3_f() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0876326238914A3F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x10342_cc82_e8356_e9(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x10342CC82E8356E9, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x15_e90_b6_a993017_aa() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x15E90B6A993017AA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x18_a0_d48_df9211_c07() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x18A0D48DF9211C07, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x32_a7_c216344_d623_b(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x32A7C216344D623B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x39654_e1_f68_b78287() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x39654E1F68B78287, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x398066_f893149856(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x398066F893149856, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_ae451860_f03_ca8_a(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3AE451860F03CA8A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_b31732_fade5_baf3() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3B31732FADE5BAF3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_df7_ee3_a76185108() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3DF7EE3A76185108, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_eecaadab0_d9_fe29() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3EECAADAB0D9FE29, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_f4_fd4_bed07_ab8_c4(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3F4FD4BED07AB8C4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_ffe60_dd8_a936551(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3FFE60DD8A936551, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x455_ecca0715_c507_f() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x455ECCA0715C507F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x578907_f59_ba01_b6_d(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x578907F59BA01B6D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x580_f34_c726387226(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x580F34C726387226, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x58521_e6_dcde97_d74(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x58521E6DCDE97D74, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x644439_b5387_ee57_e(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x644439B5387EE57E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6480723_d3_be535_b6(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6480723D3BE535B6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x910_b088_e51_a511_ac() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x910B088E51A511AC, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_dd95_b405_ab4983_e(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9DD95B405AB4983E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2_db3_c6270_c122_e3(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA2DB3C6270C122E3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bea7_d3_cb47_e1479_c() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBEA7D3CB47E1479C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d39_d32_eb3_b52_dd83(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD39D32EB3B52DD83, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_df728_c5_ae137_fc14(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xDF728C5AE137FC14, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e1_f365_c4_c8_f259_d8(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xE1F365C4C8F259D8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e53_a308_ac35877_a8() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE53A308AC35877A8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec819_d612038_ef4_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0xEC819D612038EF4B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f6_de98516_fd3_ac9_b() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF6DE98516FD3AC9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dominoes_buy_in(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x399E6CD12FC8CA89, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dominoes_place_domino(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB79A29B33BF29BA5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn dominoes_request_valid_placements(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE26AEE7E67D9E21D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn minigame_get_next_event(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDF728C5AE137FC13, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn minigame_get_next_event_type() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x578907F59BA01B6C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn minigame_is_connected_to_server(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2A0C4736AC5AF0CE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn minigame_is_request_pending(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9105A4A2556FA937, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn minigame_is_seat_occupied(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x8593A8CB0ED2C3B4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn minigame_leave_table(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF5446E47941E654C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn minigame_pop_next_event() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x833E03BAEBADC4B0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn minigame_request_seat_at_table(data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(data)];
    unsafe { native::invoke(0xF6AC6085D8D6C004, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn poker_buy_in(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB4D610EA5A1FDE74, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn poker_call(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8DED681B161EBD78, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn poker_check(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x49A045628D9B1B86, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn poker_fold(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3DFAB7D9BB45B5BE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn poker_get_game_settings_for_id(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2D20E12E1990D584, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn poker_raise(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xECCF45A79A17BB96, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn poker_reveal(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2F2131DB0A8B02DC, &arguments) }.map(|result| result.as_i64())
}

// ============================================================================
// MISC
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn absf(value: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0x134549B388167CBF, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn absi(value: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(value)];
    unsafe { native::invoke(0x0C214D5B8A38C828, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn acos(p0: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x586690F0176DC575, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn action_manager_enable_action(hash: Hash, enable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(hash), NativeArg::boolean(enable)];
    unsafe { native::invoke(0x7ACF124C12A2B045, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn action_manager_is_action_enabled(hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xFD0759658268FD8E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activity_feed_action_start_with_command_line(p0: &str, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0x91D657230BC208D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activity_feed_action_start_with_command_line_add(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x1694A053DFB61A34, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activity_feed_add_substring_to_caption(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x9935F76407C32539, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activity_feed_create(p0: &str, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0xCC7FC854B956A128, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activity_feed_post() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB16FC7B364D86585, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_pop_multiplier_area(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, ped_density: f32, traffic_density: f32, p8: bool, p9: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::float(ped_density), NativeArg::float(traffic_density), NativeArg::boolean(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x5EBDA1A3B8CB5EF7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_tactical_nav_mesh_point(x: f32, y: f32, z: f32, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(p3)];
    unsafe { native::invoke(0xE4EE55E63FA9AF45, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_strings_equal(string1: &str, string2: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(string1), NativeArg::string(string2)];
    unsafe { native::invoke(0xD3852F22AB713A1F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn asin(p0: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x6E3C15D296C15583, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn atan(p0: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x503054DED0B78027, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn atan2(p0: f32, p1: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x965B220A066E3F07, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn block_dispatch_service_resource_creation(dispatch_service: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(dispatch_service), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x66947E61A44DE2C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cancel_onscreen_keyboard() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x58A39BE597CE99CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_angled_area_of_vehicles(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0xA4D83115C1E02F8A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_area(x: f32, y: f32, z: f32, radius: f32, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(flag)];
    unsafe { native::invoke(0x3B882A96EA77D5B1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_bit(address: &mut i32, offset: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(address), NativeArg::int(offset)];
    unsafe { native::invoke(0x7D1D4A3602B6AD4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_override_weather() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x80A398F16FFE3CC3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_tactical_nav_mesh_points() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD93B6516C6878267, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_weather_type_persist() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD85DFE5C131E4AE9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compare_strings(str1: &str, str2: &str, match_case: bool, max_length: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(str1), NativeArg::string(str2), NativeArg::boolean(match_case), NativeArg::int(max_length)];
    unsafe { native::invoke(0xBFBB74A15EFC149B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn copy_script_struct(dst: &mut Any, src: &mut Any, size: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(dst), NativeArg::in_out(src), NativeArg::int(size)];
    unsafe { native::invoke(0xF7AC7DC0DEE7C9BE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_incident(dispatch_service: i32, x: f32, y: f32, z: f32, num_units: i32, radius: f32, out_incident_id: &mut i32, p7: NativeArg, p8: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(dispatch_service), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(num_units), NativeArg::float(radius), NativeArg::in_out(out_incident_id), p7, p8];
    unsafe { native::invoke(0x3F892CAF67444AE7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_incident(incident_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(incident_id)];
    unsafe { native::invoke(0x5CFD0F0D6AAE0AEE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_looting_composite_lootable_this_frame(composite_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(composite_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x40D72189F46D2E15, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn display_onscreen_keyboard(text_type: i32, window_title: &str, p2: &str, default_text: &str, default_concat1: &str, default_concat2: &str, default_concat3: &str, max_input_length: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(text_type), NativeArg::string(window_title), NativeArg::string(p2), NativeArg::string(default_text), NativeArg::string(default_concat1), NativeArg::string(default_concat2), NativeArg::string(default_concat3), NativeArg::int(max_input_length)];
    unsafe { native::invoke(0x044131118D8DB3CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_pop_multiplier_area_exist(id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0x03BA619C81A646B3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_dispatch_service(dispatch_service: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(dispatch_service), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x50E52637EF70EF77, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn fire_single_bullet(args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0xCBC9A21F6A2A679C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_lightning_flash() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x369DB5B2510FA080, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn game_framework_manager_init(transition_mode: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(transition_mode)];
    unsafe { native::invoke(0x4CABE596D632E4B0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_angle_between_2_d_vectors(x1: f32, y1: f32, x2: f32, y2: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(x2), NativeArg::float(y2)];
    unsafe { native::invoke(0xD0DFE1C486097BBB, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_benchmark_iterations() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x22FC52CF470CC98D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_benchmark_pass() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9297DACF3A2CDFF7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_bits_in_range(var: i32, range_start: i32, range_end: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(var), NativeArg::int(range_start), NativeArg::int(range_end)];
    unsafe { native::invoke(0x68E1352AF48F905D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_closest_point_on_line(p0: f32, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: bool) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x83ACC65D9ACEC5EF, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_coords_of_projectile_type_within_distance(ped: Ped, weapon_hash: Hash, distance: f32, out_coords: &mut Vector3, p4: bool, must_be_owned_by_this_ped: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::float(distance), NativeArg::in_out(out_coords), NativeArg::boolean(p4), NativeArg::boolean(must_be_owned_by_this_ped)];
    unsafe { native::invoke(0xD73C960A681052DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_curr_weather_state(weather_type1: &mut Hash, weather_type2: &mut Hash, percent_weather2: &mut f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(weather_type1), NativeArg::in_out(weather_type2), NativeArg::in_out(percent_weather2)];
    unsafe { native::invoke(0x0AC679B2342F14F2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_distance_between_coords(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, use_z: bool) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::boolean(use_z)];
    unsafe { native::invoke(0x0BE7F4E3CDBAFB28, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_frame_count() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x77DFA958FCF100C1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_frame_time() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5E72022914CE3C38, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_game_timer() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4F67E8ECA7D3F667, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ground_z_and_normal_for_3_d_coord(x: f32, y: f32, z: f32, ground_z: &mut f32, normal: &mut Vector3) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(ground_z), NativeArg::in_out(normal)];
    unsafe { native::invoke(0x2A29CA9A6319E6AB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ground_z_for_3_d_coord(x: f32, y: f32, z: f32, ground_z: &mut f32, p4: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(ground_z), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x24FA4267BB8D2431, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_hash_key(string: &str) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::string(string)];
    unsafe { native::invoke(0xFD340785ADF8CFB7, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_heading_from_vector_2_d(dx: f32, dy: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(dx), NativeArg::float(dy)];
    unsafe { native::invoke(0x38D5202FF9271C62, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_line_plane_intersection(p0: f32, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: f32, p10: f32, p11: f32, p12: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), NativeArg::float(p9), NativeArg::float(p10), NativeArg::float(p11), NativeArg::in_out(p12)];
    unsafe { native::invoke(0xAB6A04CEC428258B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_mission_flag() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB15CD1CF58771DE1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_model_dimensions(model_hash: Hash, minimum: &mut Vector3, maximum: &mut Vector3) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::in_out(minimum), NativeArg::in_out(maximum)];
    unsafe { native::invoke(0xDCB8DDD5D054A7E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_number_of_free_stacks_of_this_size(stack_size: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(stack_size)];
    unsafe { native::invoke(0x40DC2907A9697EF7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_number_of_microseconds_since_last_call() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB0CE5E5ED8BB3581, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_onscreen_keyboard_result() -> Result<NativeString, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAFB4CF58A4A292B1, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_projectile_of_projectile_type_within_distance(ped: Ped, weapon_hash: Hash, distance: f32, out_coords: &mut Vector3, out_projectile: &mut Object, p5: bool, must_be_owned_by_this_ped: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::float(distance), NativeArg::in_out(out_coords), NativeArg::in_out(out_projectile), NativeArg::boolean(p5), NativeArg::boolean(must_be_owned_by_this_ped)];
    unsafe { native::invoke(0x9578986A6105A6AD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rain_level() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x931B5F4CC130224B, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_random_event_flag() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x924D54E5698AE3E0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_random_float_in_range(start_range: f32, end_range: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(start_range), NativeArg::float(end_range)];
    unsafe { native::invoke(0xE29F927A961F8AAA, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_random_int_in_range(start_range: i32, end_range: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(start_range), NativeArg::int(end_range)];
    unsafe { native::invoke(0xD53343AA4FB7DD28, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_real_world_time() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2E036F0480B8BF02, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_script_time_within_frame_in_microseconds() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x63219768C586667C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_snow_level() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1E5D727041BE1709, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_system_time() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBE7F225417E35A7C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_system_time_step() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3F3172FEAE3AFE1C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_wind_direction() -> Result<Vector3, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF703E82F3FE14A5F, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_wind_speed() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFFB7E74E041150A4, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_bullet_impacted_in_area(x: f32, y: f32, z: f32, p3: f32, p4: bool, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3), NativeArg::boolean(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xC153E5BCCF411814, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_bullet_impacted_in_box(p0: f32, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: bool, p7: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::boolean(p6), NativeArg::boolean(p7)];
    unsafe { native::invoke(0x3B6A4C05FB2B33AC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ignore_next_restart(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6C9FF40FF1B69F8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn inform_code_of_content_id_of_current_ugc_mission(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x708DF841B8F27AA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_bit_set(address: i32, offset: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(address), NativeArg::int(offset)];
    unsafe { native::invoke(0x4ED6CFDFE8D4131A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_bullet_in_angled_area(p0: f32, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::boolean(p7)];
    unsafe { native::invoke(0x9D09D8493747CF02, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_bullet_in_area(p0: f32, p1: f32, p2: f32, p3: f32, p4: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xC652FD308772D79E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_bullet_in_box(p0: f32, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0xC128137C52152741, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_durango_version() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD1CCC2A2639D325F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_game_session_state_machine_idle() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF9E7DBB39080640B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_incident_valid(incident_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(incident_id)];
    unsafe { native::invoke(0x39F2B1BAD412246A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_mag_demo_1_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5FC9357C26DAEFCE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_minigame_in_progress() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF4D8BCD052E7EA1B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_orbis_version() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x88CFAE250D3E0C71, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_pc_version() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB0FB6CFAA5A1C833, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_position_occupied(x: f32, y: f32, z: f32, range: f32, p4: bool, p5: bool, p6: bool, p7: bool, p8: bool, p9: NativeArg, p10: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(range), NativeArg::boolean(p4), NativeArg::boolean(p5), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::boolean(p8), p9, NativeArg::boolean(p10)];
    unsafe { native::invoke(0x825CA3ED43831015, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_projectile_in_area(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, owned_by_player: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::boolean(owned_by_player)];
    unsafe { native::invoke(0x05B0061EFDFC8941, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_projectile_type_in_angled_area(p0: f32, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: NativeArg, p8: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), p7, NativeArg::boolean(p8)];
    unsafe { native::invoke(0x928431F4133CD3D4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_projectile_type_in_area(x_min: f32, y_min: f32, z_min: f32, x_max: f32, y_max: f32, z_max: f32, weapon_type: Hash, is_player: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x_min), NativeArg::float(y_min), NativeArg::float(z_min), NativeArg::float(x_max), NativeArg::float(y_max), NativeArg::float(z_max), NativeArg::hash(weapon_type), NativeArg::boolean(is_player)];
    unsafe { native::invoke(0x04965FB9E14235C7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_projectile_type_within_distance(p0: f32, p1: f32, p2: f32, p3: NativeArg, p4: f32, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), p3, NativeArg::float(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xF51C9BAAD9ED64C4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_stadia_version() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x268AB8420A9E4ED7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_string_null(string: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(string)];
    unsafe { native::invoke(0x602102324604D96B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_string_null_or_empty(string: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(string)];
    unsafe { native::invoke(0x2CF12F9ACF18F048, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_string_null_or_empty_or_spaces(string: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(string)];
    unsafe { native::invoke(0x375F5870A7B8BEC1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_script_is_safe_for_network_game() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3D0EAC6385DD6100, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn next_onscreen_keyboard_result_will_display_using_these_fonts(font_bit_field: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(font_bit_field)];
    unsafe { native::invoke(0x5CB71EAA1429A358, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn override_save_house(override_: bool, x: f32, y: f32, z: f32, heading: f32, is_autosave: bool, return_coords: &mut Vector3, return_heading: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(override_), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::boolean(is_autosave), NativeArg::in_out(return_coords), NativeArg::in_out(return_heading)];
    unsafe { native::invoke(0xB2C69E11A37B5AF0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pause_death_arrest_restart(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x66AB6B6C7E72F393, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn populate_now() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEA6DC3A8ADD2005F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_interaction_lockon_prompt(entity: Entity, text: &str, radius: f32, p3: f32, flag: i32, p5: f32, p6: f32, prompt: Prompt, p8: bool, p9: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(text), NativeArg::float(radius), NativeArg::float(p3), NativeArg::int(flag), NativeArg::float(p5), NativeArg::float(p6), NativeArg::int(prompt), NativeArg::boolean(p8), NativeArg::int(p9)];
    unsafe { native::invoke(0x870708A6E147A9AD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_dispatch_spawn_blocking_area(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x49F751F6868DDC5B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_pop_multiplier_area(id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x88CB484364EFB37A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_dispatch_ideal_spawn_distance() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC7817264BC4B6377, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_end_user_benchmark() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xECBABD0307FB216F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_script_time_within_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1411A7CBC3A6EB7B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_wanted_response_num_peds_to_spawn() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEF42F56F69877125, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn save_end_user_benchmark() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF4743E2ECC02B3DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn script_race_get_player_split_time(p0: NativeArg, p1: &mut Any, p2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [p0, NativeArg::in_out(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x769E848C66E3C2BB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn script_race_init(num_checkpoints: i32, num_laps: i32, num_players: i32, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(num_checkpoints), NativeArg::int(num_laps), NativeArg::int(num_players), p3];
    unsafe { native::invoke(0x8AE059F47158417E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn script_race_player_hit_checkpoint(part: i32, checkpoint: i32, lap: i32, time: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(part), NativeArg::int(checkpoint), NativeArg::int(lap), NativeArg::int(time)];
    unsafe { native::invoke(0xBA62B4D80FA66BD6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn script_race_shutdown() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x334CE0DA4FAF330C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_bit(address: &mut i32, offset: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(address), NativeArg::int(offset)];
    unsafe { native::invoke(0xF73FBE4845C43B5B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_bits_in_range(var: &mut i32, range_start: i32, range_end: i32, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(var), NativeArg::int(range_start), NativeArg::int(range_end), NativeArg::int(p3)];
    unsafe { native::invoke(0x324DC1CEF57F31E6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_cheat_active(cheat_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cheat_id)];
    unsafe { native::invoke(0xD4958E8CF0DE0DD0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_credits_active(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xD37BECF862DA726F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_curr_weather_state(weather_type1: Hash, weather_type2: Hash, percent_weather2: f32, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(weather_type1), NativeArg::hash(weather_type2), NativeArg::float(percent_weather2), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0xFA3E3CA8A1DE6D5D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_dispatch_ideal_spawn_distance(f_ideal_spawn_distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(f_ideal_spawn_distance)];
    unsafe { native::invoke(0xEAB6823B82FBD283, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_fade_in_after_death_arrest(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xDF3B5846DE5904AF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_fade_in_after_load(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xAC806C4CAB973517, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_game_paused(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xFAEC088D28B1DE4A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mission_flag(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x36694B456BE80D0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_decomposed(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x674B90BE1115846D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_rain(intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(intensity)];
    unsafe { native::invoke(0x193DFC0526830FD6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_random_event_flag(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xB1ADCCC4150C6473, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_random_seed(seed: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(seed)];
    unsafe { native::invoke(0x5CD7A49104AFCB6B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_random_weather_type(p0: bool, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x6E5A7FBEECAB3C72, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_super_jump_this_frame(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xB3E9BE963F10C445, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_this_script_can_be_paused(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x3215376E79F6EA18, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_this_script_can_remove_blips_created_by_any_script(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x8ABD939C2E5D00ED, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_time_scale(time_scale: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(time_scale)];
    unsafe { native::invoke(0x9682AF6050854856, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weather_type(weather_type: Hash, p1: bool, p2: bool, transition: bool, transition_time: f32, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(weather_type), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(transition), NativeArg::float(transition_time), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x59174F1AFE095B5A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_wind_direction(direction: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(direction)];
    unsafe { native::invoke(0xB56C4F5F57A45600, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_wind_speed(speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(speed)];
    unsafe { native::invoke(0xD00C2D82DC04A99F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn shoot_single_bullet_between_coords(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, damage: i32, p7: bool, weapon_hash: Hash, owner_ped: Ped, is_audible: bool, is_invisible: bool, speed: f32, p13: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::int(damage), NativeArg::boolean(p7), NativeArg::hash(weapon_hash), NativeArg::int(owner_ped), NativeArg::boolean(is_audible), NativeArg::boolean(is_invisible), NativeArg::float(speed), NativeArg::boolean(p13)];
    unsafe { native::invoke(0x867654CBC7606F2C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn should_use_metric_measurements() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4FB556ACEFA93098, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_end_user_benchmark() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x29D1F6DF864A094E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_current_loading_progress_timer() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA565FAC215CBC77D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_end_user_benchmark() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB89AEC71AFF2B599, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn string_to_int(string: &str, out_integer: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(string), NativeArg::in_out(out_integer)];
    unsafe { native::invoke(0xF2DD2298B3AF23E2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn tan(p0: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x8C13DB96497B7ABF, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_started_end_user_benchmark() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4FFA0386A6216113, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unregister_interaction_lockon_prompt(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xE98D55C5983F2509, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_onscreen_keyboard() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x37DF360F235A3893, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn var_string(flags: i32, extra: &[NativeArg]) -> Result<NativeString, NativeError> {
    let mut arguments = Vec::with_capacity(1 + extra.len());
    arguments.push(NativeArg::int(flags));
    arguments.extend_from_slice(extra);
    unsafe { native::invoke(0xFA925AC00EB830B9, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn water_override_set_oceanwavemaxamplitude(max_amplitude: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(max_amplitude)];
    unsafe { native::invoke(0xF06C5B66DE20B2B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn water_override_set_shorewaveamplitude(amplitude: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(amplitude)];
    unsafe { native::invoke(0x55123D5A7D9D3C42, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0358_b8_a41916_c613(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x0358B8A41916C613, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0730_e518486_deec3(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0730E518486DEEC3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_a487_cc74_a517_fb5(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0A487CC74A517FB5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_d0_ae5081_f88_cfe1(p0: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x0D0AE5081F88CFE1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1096603_b519_c905_f(name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x1096603B519C905F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x154340_e87_d8_cc178(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x154340E87D8CC178, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x183672_fe838_a661_b(data: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(data)];
    unsafe { native::invoke(0x183672FE838A661B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x243_cede8_f916_b994() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x243CEDE8F916B994, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2916_b30_dc6_c41179(weather_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(weather_type)];
    unsafe { native::invoke(0x2916B30DC6C41179, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x33982467_b1_e349_ef(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x33982467B1E349EF, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x35165_c658077_cd0_b() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x35165C658077CD0B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x38_c0_c9_cae1544500(p0: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x38C0C9CAE1544500, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x38_c2_bf94_d15_f464_d(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x38C2BF94D15F464D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_a87_fda8_f1_b6_cdfb(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x3A87FDA8F1B6CDFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_c3_c7_b1_b5_ec08764() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3C3C7B1B5EC08764, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4647842_fe8_f31_c1_e(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x4647842FE8F31C1E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x49_c44_fe78_a135_a1_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x49C44FE78A135A1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x49_f3241_c28_ebbfbc(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x49F3241C28EBBFBC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_b0501_a468_b749_f8() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4B0501A468B749F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_b101_dbcc9482_f2_d(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4B101DBCC9482F2D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_d5_c9_cc7_e7_e23_e09() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4D5C9CC7E7E23E09, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x553_d67295_ddd2309(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x553D67295DDD2309, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5801_be2_df2_af07_ec(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5801BE2DF2AF07EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_b4_a8121_a47_d844_d(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5B4A8121A47D844D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x627_b68_d9_ce6_ee8_de(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x627B68D9CE6EE8DE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x68319452_c5064_aba(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x68319452C5064ABA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_bcf7_b5_cd338281_a(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x6BCF7B5CD338281A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_bed40493_a1_afdb8(p0: &mut Any, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x6BED40493A1AFDB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c7_b68_d3_ce60_e8_de(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6C7B68D3CE60E8DE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_f02_b5_e50511721_e(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6F02B5E50511721E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x745808_bb01_cec6_b9(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x745808BB01CEC6B9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x74_aca66484_cebaf0(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x74ACA66484CEBAF0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_a76104_cc2_cc69_e8(entity: Entity, p1: i32, p2: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x7A76104CC2CC69E8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_cf96_f1250_ef3221(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7CF96F1250EF3221, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_fa58_ced69405_f9_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x7FA58CED69405F9A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8314_fc2013_ece2_da(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x8314FC2013ECE2DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_bb99_b85444544_d9(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8BB99B85444544D9, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_c0_f6_a3_d7236_deeb(entity: Entity, string: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(string)];
    unsafe { native::invoke(0x8C0F6A3D7236DEEB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_db104_ccebcd58_c5(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8DB104CCEBCD58C5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x94_fcadcf9_f0_c368_e(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x94FCADCF9F0C368E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x96282005_c5_c6801_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x96282005C5C6801F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x970339_efa4_fde518(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x970339EFA4FDE518, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_a252_aa23_d7098_f2() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9A252AA23D7098F2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_bf2_c0_c568_c61641(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9BF2C0C568C61641, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a08111_b053_d84_b4_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA08111B053D84B4D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a3_a8926951471_c82() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA3A8926951471C82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a9342743_b634_a462(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA9342743B634A462, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ab26_deee120_fd3_fd(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xAB26DEEE120FD3FD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ad44856_a1_cd29635(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xAD44856A1CD29635, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_af3_a84_c7_de6_a1_dc5(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xAF3A84C7DE6A1DC5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_af530_e56505_d1_bd6(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xAF530E56505D1BD6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b08_c4_fa25_bc29_db9(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xB08C4FA25BC29DB9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b1_f6665_aa54_dcd5_c(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xB1F6665AA54DCD5C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b711_eb4_bc8_d06013() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB711EB4BC8D06013, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bb282_cf5_d2333_fb8(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xBB282CF5D2333FB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cc1_baf72_d571_db8_d(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xCC1BAF72D571DB8D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cc3_edc5614_b03_f61(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xCC3EDC5614B03F61, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d3_f943_b88_f55376_a(weather_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(weather_type)];
    unsafe { native::invoke(0xD3F943B88F55376A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_da4_d8_eb04_e8_e2928(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDA4D8EB04E8E2928, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dbda48_ec456_ed908() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDBDA48EC456ED908, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc057_b86_fc157031() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDC057B86FC157031, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc416_ca762_bc4_f43(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xDC416CA762BC4F43, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_de2_c3_b74_d2_b3705_c(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDE2C3B74D2B3705C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eb946_b9_e579729_ad(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0xEB946B9E579729AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f569_e33_fb72_ed28_e() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF569E33FB72ED28E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f63_fa29_d4_a9_aca86(entity: Entity, string: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(string)];
    unsafe { native::invoke(0xF63FA29D4A9ACA86, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f650_dcf5_d6_f312_c1(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF650DCF5D6F312C1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f81_c53561_d15_f330() -> Result<NativeString, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF81C53561D15F330, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fc6_ecb9170145_ece() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFC6ECB9170145ECE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ff252_e2_bafb7330_f(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xFF252E2BAFB7330F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_dispatch_spawn_blocking_area(volume: Volume) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0xA2D5A26208421426, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_pop_multiplier_volume(volume: Volume, ped_density: f32, vehicle_density: f32, p3: bool, p4: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::float(ped_density), NativeArg::float(vehicle_density), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x3233C4EC0514C7EC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_all_bit_flags(bit_flags: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(bit_flags)];
    unsafe { native::invoke(0xD2D74F89DF844A50, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_bit_flag(bit_flags: &mut Any, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(bit_flags), NativeArg::int(flag)];
    unsafe { native::invoke(0xB909149F2BB5F6DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_volume_area(volume: Volume, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(flag)];
    unsafe { native::invoke(0x2FCD528A397E5C88, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_weather_type_persist_overtime(milliseconds: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(milliseconds)];
    unsafe { native::invoke(0xCE7690C0A0D1C36D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_weather_variation(weather_type: &str, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(weather_type), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x0E71C80FA4EC8147, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn count_bit_flags(bit_flags: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(bit_flags)];
    unsafe { native::invoke(0xE704838F36F93B7B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_ai_memory(args: &mut Any, ai_memory_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args), NativeArg::int(ai_memory_type)];
    unsafe { native::invoke(0x88BC5F4AEF77FC4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_color_string(rgb: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::int(rgb)];
    unsafe { native::invoke(0xBCC2CFADEA1AEA6C, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_incident_with_entities(dispatch_service: i32, x: f32, y: f32, z: f32, item_set: ItemSet, radius: f32, out_incident_id: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(dispatch_service), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(item_set), NativeArg::float(radius), NativeArg::in_out(out_incident_id)];
    unsafe { native::invoke(0xAB3D3F45436DB1D8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_looting_composite_pickable_this_frame(composite_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(composite_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x082C043C7AFC3747, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_item_have_valid_base(item: ScrHandle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(item)];
    unsafe { native::invoke(0xBDC6E364C9C78178, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_pop_multiplier_area_exist_for_volume(volume: Volume) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x39D6DACE323A20B6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_string_exist_in_string(string1: &str, string2: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(string1), NativeArg::string(string2)];
    unsafe { native::invoke(0x9382D5D43D2AA6FF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_lightning_flash_at_coords(x: f32, y: f32, z: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3)];
    unsafe { native::invoke(0x67943537D179597C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn game_framework_manager_get_mode() -> Result<Hash, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFAED234C7F53ABEB, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn game_framework_manager_shutdown() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAFF2FD8ADD927585, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ai_ped_does_have_event_memory(args: &mut Any, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(args), NativeArg::int(p1)];
    unsafe { native::invoke(0xFDF38E2B711BF78E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_easing_curve_value(t: f32, b: f32, d: f32, easing_curve_type: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(t), NativeArg::float(b), NativeArg::float(d), NativeArg::int(easing_curve_type)];
    unsafe { native::invoke(0xEF50E344A8F93784, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_from_item(item: ScrHandle) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(item)];
    unsafe { native::invoke(0xEE04C0AFD4EFAF0E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_forced_weather(weather: &mut Hash, p1: &mut Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(weather), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xDD560ABEF5D3784C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_game_timer_non_scaled_clipped() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x483B8C542103AD72, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ground_z_and_material_for_3_d_coord(x: f32, y: f32, z: f32, flags: i32, out_ground_z: &mut f32, out_material_hash: &mut Hash, out_flags: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(flags), NativeArg::in_out(out_ground_z), NativeArg::in_out(out_material_hash), NativeArg::in_out(out_flags)];
    unsafe { native::invoke(0xBBE5B63EFFB08E68, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_item_type(handle: ScrHandle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(handle)];
    unsafe { native::invoke(0xDC8D2FF478DF9553, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_looting_event_has_fired(ped: Ped, event_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(event_name)];
    unsafe { native::invoke(0xF9B91C5129EABC08, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_num_instructions() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC43CD2668B204419, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_next_weather_type_hash_name() -> Result<Hash, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x51021D36F62AAA83, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_number_of_instructions() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x72904D3D62AF5839, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_object_from_indexed_item(item: ScrHandle) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(item)];
    unsafe { native::invoke(0x18013392501CE5DC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_from_indexed_item(item: ScrHandle) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(item)];
    unsafe { native::invoke(0x3FFB15534067DCD4, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_prev_weather_type_hash_name() -> Result<Hash, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4BEB42AEBCA732E9, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_random_weather_type() -> Result<Hash, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1359C181BC625503, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_random_weather_type_index() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7F4CE164D9A11DFE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_status_of_savegame_operation(p0: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x1B065A2BF7953815, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_string_from_bool(value: bool) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::boolean(value)];
    unsafe { native::invoke(0xF216F74101968DB0, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_string_from_float(value: f32, digits: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::float(value), NativeArg::int(digits)];
    unsafe { native::invoke(0x2B6846401D68E563, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_string_from_vector(x: f32, y: f32, z: f32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x6C4DBF553885F9EB, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_temperature_at_coords(x: f32, y: f32, z: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xB98B78C3768AF6E0, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_from_indexed_item(item: ScrHandle) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(item)];
    unsafe { native::invoke(0xE578C8AE173719B3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_volume_from_indexed_item(item: ScrHandle) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::int(item)];
    unsafe { native::invoke(0xF18AF483DF70BBDE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn int_to_string(value: i32, format: &str, buffer: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(value), NativeArg::string(format), NativeArg::string(buffer)];
    unsafe { native::invoke(0xCF11C0CEB40C401B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_bit_flag_set(bit_flags: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(bit_flags)];
    unsafe { native::invoke(0x80E9C316EF84DD81, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_base_a_cover_point(handle: ScrHandle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(handle)];
    unsafe { native::invoke(0xFEC1D4B5C82C176F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_base_a_persistent_character(handle: ScrHandle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(handle)];
    unsafe { native::invoke(0x716F17F8A0419F95, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_bit_flag_set(bit_flags: &mut Any, flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(bit_flags), NativeArg::int(flag)];
    unsafe { native::invoke(0x8F4F050054005C27, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_global_block_valid(index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0xACB7E1418A8B6E32, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_mission_creator_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF236C84C6ADFCB2F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_decomposed(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5170DDA6D63ACAAA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_owning_standalone_sp() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x36040772DF5E59A0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn loot_tables_get_info(ped: Ped, p1: bool, p2: bool, loot_table_key: Hash, p4: &mut Any, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::hash(loot_table_key), NativeArg::in_out(p4), p5];
    unsafe { native::invoke(0x48E4D50F87A96AA5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn queue_savegame_operation(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x279B0696DA4657EB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn read_int_as_float(value: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(value)];
    unsafe { native::invoke(0xD2C9126410DFA1B2, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_pop_multiplier_area_for_volume(volume: Volume, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(p1)];
    unsafe { native::invoke(0xBD090F5B1DB82189, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_dispatch_max_spawn_distance() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x54EC7B6BC72BAD69, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_dispatch_min_spawn_distance() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x96498D922D8D0D0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ai_memory_reactions_enabled(enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x6AC4AF46A6B8DFB2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_bit_flag(bit_flags: &mut Any, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(bit_flags), NativeArg::int(flag)];
    unsafe { native::invoke(0xE84AAC1B22A73E99, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_dispatch_max_spawn_distance(max_spawn_distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(max_spawn_distance)];
    unsafe { native::invoke(0x89314FB3463E28DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_dispatch_min_spawn_distance(min_spawn_distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(min_spawn_distance)];
    unsafe { native::invoke(0x27A1B170AA8AF84C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_game_logic_paused() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x550F05CFFBD63C8C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_global_block_is_loaded(index: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(index), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE97240065406CB80, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_incident_unk(incident_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(incident_id)];
    unsafe { native::invoke(0x9617B6E5F6537B63, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_loot_pelt_satchel_item(ped: Ped, item: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), item];
    unsafe { native::invoke(0x9B47971234169990, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_override_weather(weather_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(weather_type)];
    unsafe { native::invoke(0xBE83CAE8ED77A94F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_snow_level(level: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(level)];
    unsafe { native::invoke(0xF6BEE7E80EC5CA40, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weather_type_2(weather_type: Hash, p1: i32, p2: i32, p3: i32, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(weather_type), NativeArg::int(p1), NativeArg::int(p2), NativeArg::int(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x2C6A07AF9AEDABD8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weather_type_frozen(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xD74ACDF7DB8114AF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weather_variation(weather_type: &str, variation: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(weather_type), NativeArg::string(variation)];
    unsafe { native::invoke(0x3373779BAF7CAF48, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn should_use_24_hour_clock() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0177CF20345F44DD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn should_use_metric_measurements_2() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x58BCDC75BA52110A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn should_use_metric_temperature() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFF4AAF3275BAAB4F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn should_use_metric_weight() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8F24157FEDB85EA2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn string_split_and_count_segments(input_string: &str, delimiters: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(input_string), NativeArg::string(delimiters)];
    unsafe { native::invoke(0x94E8CA3DEE952789, &arguments) }.map(|result| result.as_i32())
}

// ============================================================================
// MISSIONDATA
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_get_catagory(mission_id: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(mission_id)];
    unsafe { native::invoke(0x57E798B65C45EE17, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_get_high_score(mission_id: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(mission_id)];
    unsafe { native::invoke(0x9AABABF8313C3516, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_get_rating(mission_id: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(mission_id)];
    unsafe { native::invoke(0x57E798B54C45EE1A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_get_replay_state(p0: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x8C32D86E9556ED86, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_get_texture_name(mission_id: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(mission_id)];
    unsafe { native::invoke(0x57E798B56C45EE15, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_get_texture_txd(mission_id: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(mission_id)];
    unsafe { native::invoke(0x57E798B57C45EE16, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_is_required_story_mission(mission_id: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(mission_id)];
    unsafe { native::invoke(0xE824CE7D13FCB35E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_is_valid(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE54DC27571D5EDC5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_set_high_score(mission_id: Hash, score: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(mission_id), NativeArg::int(score)];
    unsafe { native::invoke(0x3A04F0169DA87A9D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_set_rating_scores(mission_id: Hash, bronze_score: i32, silver_score: i32, gold_score: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(mission_id), NativeArg::int(bronze_score), NativeArg::int(silver_score), NativeArg::int(gold_score)];
    unsafe { native::invoke(0x12F65317708749A5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_set_replay_locked_for_category(category: Hash, locked: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(category), NativeArg::boolean(locked)];
    unsafe { native::invoke(0x957A830C9B4B99EA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_was_completed(mission_id: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(mission_id)];
    unsafe { native::invoke(0xE54DC27571D5EDC4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_is_replay_category_locked(category: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(category)];
    unsafe { native::invoke(0xE145864DECC34219, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_set_mission_rating(mission_id: Hash, rating: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(mission_id), NativeArg::int(rating)];
    unsafe { native::invoke(0xE824CE7D13FCB300, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_set_replay_state_locked(mission_id: Hash, replay_state: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(mission_id), NativeArg::int(replay_state)];
    unsafe { native::invoke(0xE4E2C581F127A11C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_timecycle_box_delete() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7F89E15A8FB8DE97, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_timecycle_box_exists() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7E8F86A4FA33033C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn missiondata_timecycle_box_set_modifier(timecycle_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(timecycle_name)];
    unsafe { native::invoke(0x25855B1574BF8CD5, &arguments) }.map(|_| ())
}

// ============================================================================
// MONEY
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x07_ad9_e43_fd478527(p0: NativeArg, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x07AD9E43FD478527, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a46_fd001_d1_be896_c() -> Result<NativeString, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA46FD001D1BE896C, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn money_decrement_cash_balance(amount: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0x466BC8769CF26A7A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn money_get_cash_balance() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0C02DABFA3B98176, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn money_increment_cash_balance(amount: i32, add_reason: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(amount), NativeArg::hash(add_reason)];
    unsafe { native::invoke(0xBC3422DC91667621, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_cash_balance() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8A67120DBC299525, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_string_cash_balance() -> Result<NativeString, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x282D36FF103D78DF, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_money_balance_not_less_than(cash_balance: i32, gold_bar_balance: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(cash_balance), NativeArg::int(gold_bar_balance)];
    unsafe { native::invoke(0xAEC5F0119867E457, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// NETSHOPPING
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_init_session_status(p0: &mut i32, p1: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xC019112F8995DC1C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_is_connection_faulted() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6CE9FB6332B5E46E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x38640_a8_c2_def011_b(p0: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x38640A8C2DEF011B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_fa09_dd57_b93_c0_de(p0: Hash, p1: i32, p2: i32, p3: NativeArg, p4: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::int(p1), NativeArg::int(p2), p3, NativeArg::int(p4)];
    unsafe { native::invoke(0x3FA09DD57B93C0DE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x92_a32_ba29622763_f(id: i32, index: i32, p2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id), NativeArg::int(index), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x92A32BA29622763F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a0_b7094629724974(p0: Hash, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0), p1];
    unsafe { native::invoke(0xA0B7094629724974, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a3_b8_d31_c13_cb4239(p0: i32, p1: Hash, p2: &mut Any, p3: i32, p4: &mut Any, p5: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1), NativeArg::in_out(p2), NativeArg::int(p3), NativeArg::in_out(p4), NativeArg::int(p5)];
    unsafe { native::invoke(0xA3B8D31C13CB4239, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b6_f4557060_ef0_fb4(p0: i32, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1)];
    unsafe { native::invoke(0xB6F4557060EF0FB4, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ce54_c9_abe6_fbc6_db(p0: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0xCE54C9ABE6FBC6DB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d1555_fbc96_c88444(p0: Hash, p1: i32, p2: i32, p3: NativeArg, p4: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::int(p1), NativeArg::int(p2), p3, NativeArg::int(p4)];
    unsafe { native::invoke(0xD1555FBC96C88444, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_init_session_is_faulted() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD1CE92D1D9BE170A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_is_session_ready() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFCC24220FDDAC929, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_add_award(id: i32, hash: Hash, p2: &mut Any, p3: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id), NativeArg::hash(hash), NativeArg::in_out(p2), NativeArg::in_out(p3)];
    unsafe { native::invoke(0x52BDE32F21BA3B6D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_checkout(id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0x592BC00BF6629BE7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_checkout_status(id: i32, status: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id), NativeArg::in_out(status)];
    unsafe { native::invoke(0x26C008791D066F37, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_delete(id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0x59EF5D516E2D96B9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_fire_and_forget_item(action_hash: Hash, id: &mut i32, item: &mut Any, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(action_hash), NativeArg::in_out(id), NativeArg::in_out(item), NativeArg::int(p3)];
    unsafe { native::invoke(0xFFEA09CCEC4AF32F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_get_action(id: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0xBD2D520C51CCFF52, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_get_basket_is_valid(id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0x52A226ADF4A270D2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_get_item_info(id: i32, index: i32, item_info: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id), NativeArg::int(index), NativeArg::in_out(item_info)];
    unsafe { native::invoke(0x7616B5F0895C2D99, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_get_num_of_items(id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0xCF2D04D076847478, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_response_get_item_info(id: i32, index: i32, item_info: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id), NativeArg::int(index), NativeArg::in_out(item_info)];
    unsafe { native::invoke(0x98412398BBE73F61, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_start(id: &mut i32, type_: Hash, action_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(id), NativeArg::hash(type_), NativeArg::hash(action_hash)];
    unsafe { native::invoke(0xF039EC27F4490E96, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cashinventory_transaction_validate_item(p0: Hash, p1: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x6C9F12700BCE69F4, &arguments) }.map(|result| result.as_i32())
}

// ============================================================================
// NETWORK
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activate_damage_tracker_on_network_id(net_id: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(net_id), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xD45B1FFCCD52FF19, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_register_mission_entities(ped_amt: i32, vehicle_amt: i32, object_amt: i32, pickup_amt: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped_amt), NativeArg::int(vehicle_amt), NativeArg::int(object_amt), NativeArg::int(pickup_amt)];
    unsafe { native::invoke(0x69778E7564BADE6D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_register_mission_objects(amount: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0x800DD4721A8B008B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_register_mission_peds(amount: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0xBCBF4FEF9FA5D781, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_register_mission_pickups(amount: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0xF0460C7BF80011EA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_register_mission_vehicles(amount: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0x7277F1F2E085EE74, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_service_event_arguments() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x966DD84FB6A46017, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cloud_did_request_succeed(id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0x3A3D5568AF297CD5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cloud_has_request_completed(id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(id)];
    unsafe { native::invoke(0x4C61B39930D045DA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn convert_posix_time(posix_time: i32, time_structure: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(posix_time), NativeArg::in_out(time_structure)];
    unsafe { native::invoke(0xAC97AF97FA68E5D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_cloud_time_as_int() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9A73240B49945C76, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_launch_param_value(param_name: &str) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::string(param_name)];
    unsafe { native::invoke(0x65E65CA6A0FE59D4, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_num_network_objects() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC7BE335216B5EC7C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_num_network_peds() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0C1F7D49C39D2289, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_num_network_pickups() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA72835064DD63E4C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_num_network_vehicles() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0AFCE529F69B21FF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_network_time() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7A5487FE9FAA6B48, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_network_time_accurate() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x89023FBBF9200E9F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_created_mission_objects(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x12B6281B6C6706C0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_created_mission_peds(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xCB215C4B56A7FAE7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_created_mission_vehicles(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x0CD9AB83489430EA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_reserved_mission_objects(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xAA81B5F10BC43AC2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_reserved_mission_peds(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x1F13D5AE5CB17E17, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_reserved_mission_vehicles(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xCF3A965906452031, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_reserved_mission_entities_in_area(x: f32, y: f32, z: f32, p3: bool, peds: &mut i32, vehicles: &mut i32, objects: &mut i32, pickups: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(p3), NativeArg::in_out(peds), NativeArg::in_out(vehicles), NativeArg::in_out(objects), NativeArg::in_out(pickups)];
    unsafe { native::invoke(0x5E71E72A94985214, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_status_of_texture_download(texture_download_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(texture_download_id)];
    unsafe { native::invoke(0x8BD6C6DEA20E82C6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_time_difference(time_a: i32, time_b: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(time_a), NativeArg::int(time_b)];
    unsafe { native::invoke(0xA2C6FC031D46FFF0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_time_offset(time_a: i32, time_b: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(time_a), NativeArg::int(time_b)];
    unsafe { native::invoke(0x017008CCDAD48503, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_unique_int_for_player(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x07F723401B9D921C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_network_time_started() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x46718ACEEDEAFC84, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_damage_tracker_active_on_network_id(net_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0x6E192E33AD436366, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_a_ghost(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x21D04D7BC538C146, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_network_id_owned_by_participant(net_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0xA1607996431332DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_object_reassignment_in_progress() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8FE9EB11EC9CC23A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_sphere_visible_to_another_machine(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xD82CF8E64C8729D8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_sphere_visible_to_player(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xDC3A310219E5DA62, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_time_less_than(time_a: i32, time_b: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(time_a), NativeArg::int(time_b)];
    unsafe { native::invoke(0xCB2CF5148012C8D0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_time_more_than(time_a: i32, time_b: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(time_a), NativeArg::int(time_b)];
    unsafe { native::invoke(0xDE350F8651E4346C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn keep_network_id_in_fast_instance(net_id: i32, p1: bool, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(net_id), NativeArg::boolean(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xE1BC73D6815BA361, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_accept_rs_invite(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xB2CEA5105AAC8DDE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_access_tunable_bool(tunable_context: Hash, tunable_name: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(tunable_context), NativeArg::hash(tunable_name)];
    unsafe { native::invoke(0xAA6A47A573ABB75A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_access_tunable_int(tunable_context: Hash, tunable_name: Hash, value: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(tunable_context), NativeArg::hash(tunable_name), NativeArg::in_out(value)];
    unsafe { native::invoke(0x8BE1146DFD5D4468, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_action_platform_invite() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3B82ACC3F4B6240C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_activity_reset_to_idle() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3FE141FDB990E3D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_activity_set_current(net_playlist_activity: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(net_playlist_activity)];
    unsafe { native::invoke(0x9ADAC065D9F6706F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_add_friend(gamer_handle: &mut Any, message: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::string(message)];
    unsafe { native::invoke(0x8E02D73914064223, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_allow_all_entity_fading_for_instances(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x4B05B97BA46F419D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_allow_entity_fading_for_instances(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xF3354D6CA46F419D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_allow_remote_attachment_modification(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x267C78C60E806B9A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_are_handles_the_same(gamer_handle1: &mut Any, gamer_handle2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle1), NativeArg::in_out(gamer_handle2)];
    unsafe { native::invoke(0x57DBA049E110F217, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_are_players_in_same_tutorial_session(player: Player, index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(index)];
    unsafe { native::invoke(0x9DE986FC9A87C474, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_can_split_session(p0: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0xE404BFF0ABA23CDC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_finish_instance() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBB51299166B844F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_is_allowed_to_merge() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAADED99A6B268A27, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_is_object_creation_paused() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0E2C3AEE6CE603B7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_split_session(players_to_take: i32, max_instance_players: i32, session_flags: i32, bucket_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(players_to_take), NativeArg::int(max_instance_players), NativeArg::int(session_flags), NativeArg::int(bucket_id)];
    unsafe { native::invoke(0xC223D299C670413D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_award_has_reached_maxclaim(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xFBE782B3165AC8EC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_can_access_multiplayer(loading_state: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(loading_state)];
    unsafe { native::invoke(0xAF50DA1A3F8B1BA4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_can_refresh_friend_page() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1AF5E28E64A76A9F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_can_session_end() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4EEBC3694E49C572, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_can_view_gamer_user_content(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x246545C37C27A717, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_check_access_and_alert_if_fail() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2A8112A974DE1EF6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_check_communication_privileges(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x83F28CE49FBBFFBA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_check_user_content_privileges(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x595F028698072DD9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clear_clock_time_override() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD972DF67326F966E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clear_found_gamers() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6D14CCEE1B40381A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clear_get_gamer_status() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x86E0660E4F5C956D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clear_platform_invite() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA4484173759749B1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_conceal_player(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xBBDF066252829606, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_did_find_gamers_succeed() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF9B83B77929D8863, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_did_get_gamer_status_succeed() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5AE17C6B0134B7F1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_disable_leave_remote_ped_behind(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC505036A35AFD01B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_disable_proximity_migration(net_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0x407091CF6037118E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_disable_realtime_multiplayer() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x236905C700FDB54D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_displaynames_from_handles_start(p0: &mut Any, p1: NativeArg) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), p1];
    unsafe { native::invoke(0xD66C9E72B3CC4982, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_does_network_id_exist(net_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0x38CE16C96BD11344, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_does_tunable_exist(tunable_context: Hash, tunable_name: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(tunable_context), NativeArg::hash(tunable_name)];
    unsafe { native::invoke(0x85E5F8B9B898B20A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_dump_net_if_config() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAEDF1BC1C133D6E3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_end_tutorial_session() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD0AFAFF5A51D72F7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_assisted_damage_of_entity(player: Player, entity: Entity, p2: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(entity), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x4CACA84440FA26F6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_average_latency(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xD414BE129BB81B32, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_average_packet_loss(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x350C23949E43686C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_average_ping(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0E3A041ED6AC2B45, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_destroyer_of_network_id(net_id: i32, weapon_hash: &mut Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(net_id), NativeArg::in_out(weapon_hash)];
    unsafe { native::invoke(0x7A1ADEEF01740A24, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_displaynames_from_handles(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x58CC181719256197, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_entity_from_network_id(net_id: i32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0xCE4E5D9B0A4FF560, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_entity_is_networked(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xC7827959479DCC78, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_entity_killer_of_player(player: Player, weapon_hash: &mut Hash) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(weapon_hash)];
    unsafe { native::invoke(0x42B2DAA6B596F5F8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gamertag_from_handle(gamer_handle: &mut Any) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x426141162EBE5CDB, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_game_mode() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x225640E09EFFDC3F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_global_clock(hour: &mut i32, minute: &mut i32, second: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(hour), NativeArg::in_out(minute), NativeArg::in_out(second)];
    unsafe { native::invoke(0x11A7ADCD629E170F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_global_multiplayer_clock(hours: &mut i32, minutes: &mut i32, seconds: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(hours), NativeArg::in_out(minutes), NativeArg::in_out(seconds)];
    unsafe { native::invoke(0x6D03BFBD643B2A02, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_highest_reliable_resend_count(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x52C1EADAF7B10302, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_host_of_script(script_name: &str, p1: i32, p2: i32) -> Result<Player, NativeError> {
    let arguments = [NativeArg::string(script_name), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x1D6A14F1F9A736FC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_host_of_this_script() -> Result<Player, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC7B4D79B01FA7A5C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_host_of_thread(thread_id: i32) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(thread_id)];
    unsafe { native::invoke(0xB4A25351D79B444C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_instance_id_of_this_script() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x638A3A81733086DB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_local_handle(gamer_handle: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xE86051786B66CD8E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_max_num_participants() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA6C90FBC38E395EE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_network_id_from_entity(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xA11700682F3AD45C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_network_id_from_rope_id(rope_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x42871327315EDAE8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_net_statistics_info() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6FD992C4A1C1B986, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_np_unavailable_reason() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x74FB3E29E6D10FA9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_num_connected_players() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA4A79DD2D9600654, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_num_participants() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x18D0456E86604654, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_num_script_participants(script_name: &str, instance_id: i32, position: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(script_name), NativeArg::int(instance_id), NativeArg::hash(position)];
    unsafe { native::invoke(0x3658E8CD94FC121A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_num_unacked_reliables(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xFF8FCF9FFC458A1C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_participant_index(index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0x1B84DF6AF2A46938, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_player_from_gamer_handle(gamer_handle: &mut Any) -> Result<Player, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xCE5F689CF5A0A49D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_player_index(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x24FB80D107371267, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_player_index_from_ped(ped: Ped) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6C0E2E0125610278, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_promotion_dlg_seen_count() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2FB53C631A49BE92, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_random_int_ranged(range_start: i32, range_end: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(range_start), NativeArg::int(range_end)];
    unsafe { native::invoke(0xE30CF56F1EFA5F43, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_recent_gamer_names(p0: i32, p1: i32, out_data: &mut Any, data_size: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1), NativeArg::in_out(out_data), NativeArg::int(data_size)];
    unsafe { native::invoke(0xFEFCC345CE357453, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_rope_id_from_network_id(net_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0xEB1A4DD8352EC828, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_script_status() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x57D158647A6BFABF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_this_script_is_network_script() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2910669969E9535E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_timeout_time() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5ED0356A0CE3A34F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_total_num_friends() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDB7ABDD203FA3704, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_total_num_players() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCF61D4B4702EE9EB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_tunable_cloud_crc() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x10BD227A753B0D84, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_unreliable_resend_count(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x3765C3A3E8192E10, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_handle_from_friend(friend_index: i32, gamer_handle: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(friend_index), NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xD45CB817D7E177D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_handle_from_player(player: Player, gamer_handle: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x388EB2B86C73B6B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_hash_from_player_handle(player: Player) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xBC1D768F2F5D6C05, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_control_of_entity(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x01BF60A500E28887, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_control_of_network_id(net_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0x4D36070FE0215186, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_control_of_pickup(pickup: Pickup) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(pickup)];
    unsafe { native::invoke(0x5BC9495F0B3B6FA6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_control_of_pickup_placement(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x51EABCF2786515AB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_entity_been_registered_with_this_thread(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xB07D3185E11657A5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_pending_invite_failure() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD0498AD30E16B6BD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_received_host_broadcast_data() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5D10B3795F3FC886, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_ros_privilege(index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0xA699957E60D80214, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_social_club_account() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x67A5589628E0CFF6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_valid_ros_credentials() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x85443FF4C328F53B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_have_online_privileges() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x25CB5A9F37BFD063, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_have_ros_banned_priv() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8020A73847E0CA7D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_aim_cam_active(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x8E7CE19219669AEB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_clock_time_overridden() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD7C95D322FF57522, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_cloud_available() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9A4CF4F48AD77302, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_connected_via_relay(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x16D3D49902F697BB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_custom_upsell_enabled() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x78A9535AF83715C6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_feature_supported(feature_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(feature_id)];
    unsafe { native::invoke(0x9C725D149622BFDE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_finding_gamers() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDDDF64C91BFCF0AA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_friend(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x1A24A179F9B31654, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_gamer_in_my_session(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x0F10B05DDF8D16E9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_game_in_progress() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x10FAB35428CCC9D7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_handle_valid(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x6F79B93B0A8E4133, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_host() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8DB296B814EDDA07, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_host_of_this_script() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x83CD99A1E6061AB5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_in_mp_cutscene() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6CC27C9FA2040220, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_in_platform_party() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2FC5650B0271CB57, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_in_platform_party_chat() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFD8B834A8BA05048, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_in_session() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCA97246103B63917, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_in_spectator_mode() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x048746E388762E11, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_in_tutorial_session() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xADA24309FE08DACF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_participant_active(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x6FF8FF40B6357D45, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_pending_friend(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x0BE73DA6984A6E33, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_platform_invite_pending() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFC4165C9165C166F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_player_active(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xB8DFD30D6973E135, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_player_a_participant(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x3CA58F6CB7CBD784, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_player_a_participant_on_script(p0: Player, p1: &mut Any, p2: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::in_out(p1), p2];
    unsafe { native::invoke(0x1AD5B71586B94820, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_player_concealed(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x919B3C98ED8292F9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_player_connected(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x93DC1BE4E1ABE9D1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_player_index_valid(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x255A5EF65EDA9167, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_player_in_mp_cutscene(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x63F9EE203C3619F2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_promotion_enabled() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8FF6059DA26E688A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_resetting_population() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1BB50CD340A996E6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_script_active(script_name: &str, p1: i32, p2: bool, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(script_name), NativeArg::int(p1), NativeArg::boolean(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x9D40DF90FAD26098, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_script_active_by_hash(script_hash: Hash, p1: i32, p2: bool, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(script_hash), NativeArg::int(p1), NativeArg::boolean(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x1B89BC43B6E69107, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_session_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD83C2B94E7508980, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_session_started() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9DE624D2FC4B603F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_signed_online() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1077788E268557C2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_tunable_cloud_request_pending() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0467C11ED88B7D28, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_tutorial_session_change_pending() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x35F0B98A8387274D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_prevent_script_host_migration() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2302C0264EA58D31, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_refresh_current_friend_page() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1F51F367B710A832, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_register_entity_as_networked(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x06FAACD625D80CAA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_register_host_broadcast_variables(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x3E9B2F01C50DF595, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_register_player_broadcast_variables(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x3364AA97340CA215, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_request_cloud_tunables() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x42FB3B532D526E6C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_request_control_of_entity(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xB69317BF5E782347, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_request_control_of_network_id(net_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0xA670B3662FAFFBD0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_request_control_of_pickup_placement(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x56ED2C48558DAB78, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_request_join(p0: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE483BB6BE686F632, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_request_recent_gamer_names(p0: i32, player_count: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(player_count)];
    unsafe { native::invoke(0x6D206D383BB5F6B1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_request_session_seamless(flags: i32, seamless_type: i32, session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags), NativeArg::int(seamless_type), NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0x04019AE4956D4393, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_reset_population(p0: bool, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::int(p1)];
    unsafe { native::invoke(0x101F538C25ABB39A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_resurrect_local_player(x: f32, y: f32, z: f32, heading: f32, p4: i32, p5: bool, p6: NativeArg, p7: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::int(p4), NativeArg::boolean(p5), p6, NativeArg::boolean(p7)];
    unsafe { native::invoke(0xEA23C49EAA83ACFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_seed_random_number_generator(seed: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(seed)];
    unsafe { native::invoke(0xF1B84178F8674195, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_get_session_flags() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x51F33DBC1A41CBFD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_get_session_type() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF0C0C94B404206FA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_is_any_request_in_progress() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBAFFDE5F953720D9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_is_private() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCEF70AA5B3F89BA1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_is_request_in_progress(session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0x8FB7C254CFCBF78E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_is_request_pending_transition(session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0xCCF878D50F8AB10D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_is_session_request_id_valid(session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0x2F54B146D3EDCE4D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_is_transitioning() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF2CBC969C4F090C7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_leave_session() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x17C21B7319A05047, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_left_queue_or_requested_session(session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0xECE6A0C1B59CD8BE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_remove_session_flags(flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags)];
    unsafe { native::invoke(0x78335E12DB0BF961, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_request_session_competitive(flags: i32, match_type: i32, user_hash: i32, p3: i32, session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags), NativeArg::int(match_type), NativeArg::int(user_hash), NativeArg::int(p3), NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0x309BBEBEA8A3986C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_request_session_private(flags: i32, num_players: i32, user_hash: i32, session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags), NativeArg::int(num_players), NativeArg::int(user_hash), NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0x39A8EF7AF29A192C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_request_session_seamless(flags: i32, seamless_type: i32, user_hash: i32, session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags), NativeArg::int(seamless_type), NativeArg::int(user_hash), NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0x2989E131FDE37E97, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_completed_mp_intro_flow_on_current_slot(completed: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(completed)];
    unsafe { native::invoke(0x2C5BD9A43987AA27, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_entity_only_exists_for_participants(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xF1CA12B18AEF5298, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_entity_remains_when_unnetworked(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xD785864798258032, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_friendly_fire_option(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xF808475FA571D823, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_in_mp_cutscene(p0: bool, p1: bool, p2: i32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::boolean(p1), NativeArg::int(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x9CA5DE655269FEC4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_in_spectator_mode(toggle: bool, player_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle), NativeArg::int(player_ped)];
    unsafe { native::invoke(0x423DE3854BB50894, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_local_player_invincible_time(time: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(time)];
    unsafe { native::invoke(0x2D95C7E2D7E07307, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_local_player_pending_fast_instance_id(instance_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(instance_id)];
    unsafe { native::invoke(0x007FF852DCF49DA4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_local_player_sync_look_at(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x524FF0AEFF9C3973, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_mission_finished() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3B3D11CD9FFCDFC9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_mp_mission_flag_on_current_slot(enabled: bool, flag_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(enabled), NativeArg::int(flag_index)];
    unsafe { native::invoke(0x86FD10251A7118A4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_player_is_passive(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x9C25E8EC4C535FBD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_recent_gamers_enabled(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x29FE035D35B8589C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_rich_presence(p0: i32, p1: &mut Any, p2: i32, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::in_out(p1), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x1DCCACDCFC569362, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_script_ready_for_events(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x7AC752103856FB20, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_this_script_is_network_script(max_num_mission_participants: i32, p1: bool, instance_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(max_num_mission_participants), NativeArg::boolean(p1), NativeArg::int(instance_id)];
    unsafe { native::invoke(0x1CA59E306ECB80A5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_should_show_promotion_dlg() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDA4B1A479C414FB2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_show_account_upgrade_ui() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x83FE8D7229593017, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_show_chat_restriction_msc(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x6BFF5F84102DF80A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_show_profile_ui(gamer_handle: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x859ED1CEA343FCA8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_show_psn_ugc_restriction() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5C497525F803486B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_set_flags(flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(flags)];
    unsafe { native::invoke(0xF94A0D5B254375DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_set_ground_to_root_offset(offset: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(offset)];
    unsafe { native::invoke(0x59577799F6AE2F34, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_set_tuning_float(p0: Hash, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x0608326F7B98C08D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_start_solo_tutorial_session() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x17E0198B3882C2CB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_start_user_content_permissions_check(gamer_handle: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xDEB2B99A1AF1A2A6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_trigger_damage_event_for_zero_damage(entity: Entity, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x0C8BC052AE87D744, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_try_access_tunable_bool_hash(tunable_context: Hash, tunable_name: Hash, default_value: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(tunable_context), NativeArg::hash(tunable_name), NativeArg::boolean(default_value)];
    unsafe { native::invoke(0xB2AD5D29A99D4B26, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_try_access_tunable_float_hash(tunable_context: Hash, tunable_name: Hash, default_value: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::hash(tunable_context), NativeArg::hash(tunable_name), NativeArg::float(default_value)];
    unsafe { native::invoke(0xA18393089C05E49C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_try_access_tunable_int_hash(tunable_context: Hash, tunable_name: Hash, default_value: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(tunable_context), NativeArg::hash(tunable_name), NativeArg::int(default_value)];
    unsafe { native::invoke(0xA25E006B36719774, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn net_to_ent(net_handle: i32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(net_handle)];
    unsafe { native::invoke(0xBFFEAB45A9A9094A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn net_to_obj(net_handle: i32) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(net_handle)];
    unsafe { native::invoke(0xD8515F5FEA14CB3F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn net_to_ped(net_handle: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(net_handle)];
    unsafe { native::invoke(0xBDCD95FC216A8B3E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn net_to_veh(net_handle: i32) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(net_handle)];
    unsafe { native::invoke(0x367B936610BA360C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn obj_to_net(object: Object) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x99BFDC94A603E541, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn participant_id() -> Result<Player, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x90986E8876CE0A83, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn participant_id_to_int() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x57A3BDDAD8E5AA0A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_to_net(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0EDEC3C276198689, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn prevent_migration_of_entities_in_fast_instance_for_local_player(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x89D803CD48622150, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn prevent_network_id_migration(net_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0x7182EDDA1EE7DB5A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reserve_network_client_mission_objects(amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0xE7DDA8BD3BCF751C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reserve_network_client_mission_peds(amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0x807E119F80231732, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reserve_network_mission_objects(amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0x4E5C93BD0C32FBF8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reserve_network_mission_peds(amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0xB60FEBA45333D36F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reserve_network_mission_pickups(amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0x4D40E7D749BC6E6D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reserve_network_mission_vehicles(amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(amount)];
    unsafe { native::invoke(0x76B02E21ED27A469, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_visible_in_cutscene(entity: Entity, p1: bool, p2: bool, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0xE0031D3C8F36AB82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_local_player_as_ghost(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x5FFE9B4144F9712F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_local_player_invisible_locally(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xE5F773C1A1D9D168, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_local_player_visible_in_cutscene(local: bool, remote: bool, instance_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(local), NativeArg::boolean(remote), NativeArg::int(instance_id)];
    unsafe { native::invoke(0xD1065D68947E7B6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_network_id_always_exists_for_player(net_id: i32, player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(net_id), NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xA8A024587329F36A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_network_id_exists_on_all_machines(net_id: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(net_id), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE05E81A888FA63C8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_network_id_stop_cloning(network_id: i32, b_stop_cloning: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(network_id), NativeArg::boolean(b_stop_cloning)];
    unsafe { native::invoke(0x9ED3108D6847760A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_network_id_visible_in_cutscene(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xA6928482543022B4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_invisible_locally(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x12B37D54667DB0B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_visible_locally(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xFAA10F1FAFB11AF2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn texture_download_get_name(texture_download_id: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::int(texture_download_id)];
    unsafe { native::invoke(0x3448505B6E35262D, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn texture_download_release(texture_download_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_download_id)];
    unsafe { native::invoke(0x487EB90B98E9FB19, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn texture_download_request(gamer_handle: &mut Any, file_path: &str, name: &str, p3: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::string(file_path), NativeArg::string(name), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x16160DA74A8E74A2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_clear_query_results(ugc_request_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ugc_request_id)];
    unsafe { native::invoke(0xE931354FEA710038, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_did_description_request_succeed(description: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(description)];
    unsafe { native::invoke(0x162C23CA83ED0A62, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_did_request_succeed(ugc_request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ugc_request_id)];
    unsafe { native::invoke(0x0B6009A90B8495F1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_get_cached_description(description: Hash, length: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(description), NativeArg::int(length)];
    unsafe { native::invoke(0x40F7E66472DF3E5C, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_has_description_request_finished(description: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(description)];
    unsafe { native::invoke(0xEBFA8D50ADDC54C4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_has_request_finished(ugc_request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ugc_request_id)];
    unsafe { native::invoke(0xA9EB4D606076615D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_is_description_request_in_progress(description: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(description)];
    unsafe { native::invoke(0x2D5DC831176D0114, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_is_language_supported(language_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(language_id)];
    unsafe { native::invoke(0xF53E48461B71EECB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_is_request_pending(ugc_request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ugc_request_id)];
    unsafe { native::invoke(0xF4AC4FA844FD559A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_content_has_player_record(p0: NativeArg, index: i32) -> Result<bool, NativeError> {
    let arguments = [p0, NativeArg::int(index)];
    unsafe { native::invoke(0xF794765390A6DCA5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_content_num(ugc_request_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ugc_request_id)];
    unsafe { native::invoke(0x76160E0396142765, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_was_force_cancelled(ugc_request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ugc_request_id)];
    unsafe { native::invoke(0xF8F0705E77A0E705, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_release_all_cached_descriptions() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x68103E2247887242, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_release_cached_description(description: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(description)];
    unsafe { native::invoke(0x5A34CD9C3C5BEC44, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_request_cached_description(description: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(description)];
    unsafe { native::invoke(0x5E0165278F6339EE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_request_content_data_from_params(content_type_name: &str, content_id: &str, file_id: i32, file_version: i32, language_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(content_type_name), NativeArg::string(content_id), NativeArg::int(file_id), NativeArg::int(file_version), NativeArg::int(language_id)];
    unsafe { native::invoke(0x7FD2990AF016795E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_set_query_data_from_offline(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xF98DDE0A8ED09323, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_texture_download_request(p0: &mut Any, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: &mut Any, p5: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), p1, p2, p3, NativeArg::in_out(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x308F96458B7087CC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn veh_to_net(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xB4C94523F023419C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x02_b3_cdd652_b3_cdd6() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x02B3CDD652B3CDD6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x02_c4_c6_c2900_d84_df(player: Player, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), p1];
    unsafe { native::invoke(0x02C4C6C2900D84DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x039_ad6_b57_d5179_ff() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x039AD6B57D5179FF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x039_b692_b3318_fab6(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x039B692B3318FAB6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x062842_d61_d0_d53_fd() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x062842D61D0D53FD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_b6_b4507_ac5_ea8_b8() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0B6B4507AC5EA8B8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_bf90_cbb6_b72977_b() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0BF90CBB6B72977B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_cc28_c08613_ba9_e5(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x0CC28C08613BA9E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_d183_d8490_ee4366(p0: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1)];
    unsafe { native::invoke(0x0D183D8490EE4366, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_e54_d4_da6018_ff8_e() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0E54D4DA6018FF8E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x106_cbdd5077_dede1(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x106CBDD5077DEDE1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x13_f592_fc3_bf0_ea84(volume: Volume, p1: bool, original_weight: f32, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(p1), NativeArg::float(original_weight), p3, p4];
    unsafe { native::invoke(0x13F592FC3BF0EA84, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1413_b6_bf27_ab7_a95() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1413B6BF27AB7A95, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x160_f0_ce6_d76_a39_c9() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x160F0CE6D76A39C9, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x16_efb123_c4451032(p0: i32, gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x16EFB123C4451032, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x18_b94666_cf610_aeb() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x18B94666CF610AEB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x19447_fcae97704_dc(ctx: Hash, ec: i32, ex: bool, ro: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(ctx), NativeArg::int(ec), NativeArg::boolean(ex), NativeArg::boolean(ro)];
    unsafe { native::invoke(0x19447FCAE97704DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x19_b52_c20_b5_c4757_c() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x19B52C20B5C4757C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_e4_e097_d71_d449_fb(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x1E4E097D71D449FB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x232_e1_eb23_cdb313_c() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x232E1EB23CDB313C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x236321_f1178_a5446(player: Player, ped: Ped, p2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x236321F1178A5446, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2686_bd9566_b65_eda(x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x2686BD9566B65EDA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x26_a867_c0_b7_a456_d1(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x26A867C0B7A456D1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x271_f95_e55_c663_b8_b(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x271F95E55C663B8B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x273_e04_a3_a7_ad1_f2_d() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x273E04A3A7AD1F2D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x27_b1_ae4_d8_c652_f08(p0: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x27B1AE4D8C652F08, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_c4_e98_dda475364_f(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x2C4E98DDA475364F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_cd41_ac000_e6_f611() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2CD41AC000E6F611, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3034_c77_c79_a58880(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x3034C77C79A58880, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x316_fd416_c432_c761() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x316FD416C432C761, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x335_af56613_ca0_f49(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x335AF56613CA0F49, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x34_bc1_e79546_ba543(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x34BC1E79546BA543, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_aa0_cdc63696166_d(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x3AA0CDC63696166D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_cbd6565_d9_c3_b133(p0: i32, p1: i32, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0x3CBD6565D9C3B133, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_e4_a16_bc669_e71_b3() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3E4A16BC669E71B3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_e74_a687_a73979_c6(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x3E74A687A73979C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_e8_cce6769_db5_f34(p0: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x3E8CCE6769DB5F34, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_f0_abae38_a0515_ad(p0: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1)];
    unsafe { native::invoke(0x3F0ABAE38A0515AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_f2_ee18_a3_e294801(p0: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x3F2EE18A3E294801, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x405_ddefb1_f531_b18(volume: Volume, p1: bool, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(p1), p2, p3];
    unsafe { native::invoke(0x405DDEFB1F531B18, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x40_fedb13870042_f1() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x40FEDB13870042F1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x41452_e8_a3_b9_c0_c4_b() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x41452E8A3B9C0C4B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x422_f9_d6_d6_c7_bc290(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x422F9D6D6C7BC290, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x43_cf999205084_b4_b() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x43CF999205084B4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4538_ee7_c321590_bc(network_id: i32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(network_id)];
    unsafe { native::invoke(0x4538EE7C321590BC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x455156_f47_dc6_b78_c(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x455156F47DC6B78C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4835413_ea6_f9_c9_cd(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x4835413EA6F9C9CD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x49_cf17_a564918_e8_d() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x49CF17A564918E8D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5133_cf81924_f1129() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5133CF81924F1129, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x51951_de06_c0_d1_c40(player: Player, type_: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(type_)];
    unsafe { native::invoke(0x51951DE06C0D1C40, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x564552_c6_af1_eeab1() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x564552C6AF1EEAB1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5759160_ac17_c13_ce(gamer_handle: &mut Any, message: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::string(message)];
    unsafe { native::invoke(0x5759160AC17C13CE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_a91_bcef74944_e93(player: Player, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(p1)];
    unsafe { native::invoke(0x5A91BCEF74944E93, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_b9_c6_ac118_fd4774() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5B9C6AC118FD4774, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_cb8_b0_c846_d0_f30_b(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5CB8B0C846D0F30B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_cd3_aad8_ff9_ed121(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5CD3AAD8FF9ED121, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_d3_c528_b7_a7_df836(nsctf: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(nsctf)];
    unsafe { native::invoke(0x5D3C528B7A7DF836, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_ed39_da62_beb1330(p0: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x5ED39DA62BEB1330, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_f0_e99071582_deca(p0: NativeArg, index: i32, p2: i32) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::int(index), NativeArg::int(p2)];
    unsafe { native::invoke(0x5F0E99071582DECA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_f328_fc909_f0_e0_ff(p0: i32, p1: i32, p2: i32, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x5F328FC909F0E0FF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x603469298_a4308_af(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x603469298A4308AF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x61_bfbaa795_e712_ad() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x61BFBAA795E712AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x64_a36_ba85_ce01_a81(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x64A36BA85CE01A81, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x67_ccdf74_c4_df7169() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x67CCDF74C4DF7169, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x691_e4_de5309_eaefc(p0: NativeArg, p1: &mut Any) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::in_out(p1)];
    unsafe { native::invoke(0x691E4DE5309EAEFC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c27442_a225_a241_a(p0: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x6C27442A225A241A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c7_e04_e9_de451789() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6C7E04E9DE451789, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_cee2_e30021_daec6() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6CEE2E30021DAEC6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_cf82_a7_f65_a5_ad5_f(ped: Ped, p1: &mut Any) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x6CF82A7F65A5AD5F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x704_f92_b3_af20_d857(setting: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(setting)];
    unsafe { native::invoke(0x704F92B3AF20D857, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x71_fa2_d1880_c48032(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x71FA2D1880C48032, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x744_bfbb0_ca908161(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x744BFBB0CA908161, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x75_fc34_a2_ba345_bd1(entity: Entity, player: Player, p2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(player), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x75FC34A2BA345BD1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7673_c0_d2_c5_cdac55() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7673C0D2C5CDAC55, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x77_b299_e8799_b1332(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x77B299E8799B1332, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x780_a13_f780_a13_f1_b(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x780A13F780A13F1B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x78271_bc02_ae9_af83(p0: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x78271BC02AE9AF83, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_a8_e8_df782_b47_eb0(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x7A8E8DF782B47EB0, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_b3_ff2_d193628126(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x7B3FF2D193628126, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_bca0_a3972708436(out_data: &mut Any, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(out_data), NativeArg::int(p1)];
    unsafe { native::invoke(0x7BCA0A3972708436, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_e300_b5_b86_ab1_d1_a(p0: &mut Any, p1: i32, p2: i32, p3: i32, p4: i32, p5: i32, p6: i32, p7: i32, p8: i32, p9: i32, p10: i32, p11: i32, p12: i32, p13: i32, p14: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::int(p1), NativeArg::int(p2), NativeArg::int(p3), NativeArg::int(p4), NativeArg::int(p5), NativeArg::int(p6), NativeArg::int(p7), NativeArg::int(p8), NativeArg::int(p9), NativeArg::int(p10), NativeArg::int(p11), NativeArg::int(p12), NativeArg::int(p13), NativeArg::int(p14)];
    unsafe { native::invoke(0x7E300B5B86AB1D1A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x814729078_aed6_d30() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x814729078AED6D30, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x862_c5040_f4888741(player1: Player, player2: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player1), NativeArg::int(player2)];
    unsafe { native::invoke(0x862C5040F4888741, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x880_a7202301_e282_b(p0: &mut Any, p1: &mut Any, x: f32, y: f32, z: f32, p5: f32, p6: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p5), p6];
    unsafe { native::invoke(0x880A7202301E282B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x894_b5_ecab45_d2342(net_handle: i32, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(net_handle), p1];
    unsafe { native::invoke(0x894B5ECAB45D2342, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x917_ad74_bdcf8_b6_e9(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x917AD74BDCF8B6E9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x923346025512_dfb7(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x923346025512DFB7, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x950_acd8_f05_b7_b9_df(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x950ACD8F05B7B9DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x979765465_a6_f25_fc(entity: Entity, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x979765465A6F25FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x97_bce4_c4_b3191228() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x97BCE4C4B3191228, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x981146_e5_c9_ce9250(invite_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(invite_index)];
    unsafe { native::invoke(0x981146E5C9CE9250, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x982_d7_ad755_b8_f62_c(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x982D7AD755B8F62C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_b39_b0555_cc692_b5() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9B39B0555CC692B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_e5_a47744_c0_f0376(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x9E5A47744C0F0376, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2837_a5_e21_fb5_a58(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA2837A5E21FB5A58, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a47_d48_d06_aa5_a188() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA47D48D06AA5A188, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a63_e4_f050_f20021_f() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA63E4F050F20021F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a6_f1_baabff6_ad7_b9(p0: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0xA6F1BAABFF6AD7B9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a7670_f7991099680(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xA7670F7991099680, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a94_ece191_d90637_a() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA94ECE191D90637A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a95470_da137587_f5(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xA95470DA137587F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_acc44768_af229042() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xACC44768AF229042, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_afa14_f98327791_ce(session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0xAFA14F98327791CE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b131_e686_bd97_b3_f8() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB131E686BD97B3F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b389289_f031_f059_a() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB389289F031F059A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_baf7_e2979442_b29_f(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xBAF7E2979442B29F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bb1_ec8_c2_eef33_baa(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xBB1EC8C2EEF33BAA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bb697756309_d77_ee(p0: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xBB697756309D77EE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bc7_d36946_d19_e60_e(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xBC7D36946D19E60E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bf8276_e51761_f9_da() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBF8276E51761F9DA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c028_b3_f52_c707_c49(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC028B3F52C707C49, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c0_cffda87_c2_c163_d(p0: i32, p1: NativeArg, p2: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(p0), p1, NativeArg::int(p2)];
    unsafe { native::invoke(0xC0CFFDA87C2C163D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c1968045_eeb563_b7(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xC1968045EEB563B7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c8_b6_d18_e22484643() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC8B6D18E22484643, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c964_fcd3_d1720697() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC964FCD3D1720697, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ca58_d4_fd20_d70_f24(p0: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCA58D4FD20D70F24, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cc4_e72_c339461_ed1() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCC4E72C339461ED1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cd53_e6_cbf609_c012(ugc_request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ugc_request_id)];
    unsafe { native::invoke(0xCD53E6CBF609C012, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cf23_ab5_bd47_b384_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCF23AB5BD47B384D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d1_ffb246_f4_e088_ac(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xD1FFB246F4E088AC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d39_a72_ae5_ebd57_e5() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD39A72AE5EBD57E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d3_a3_c8_b9_f3_bdef81() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD3A3C8B9F3BDEF81, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d3_b6_ebc6_c3_d77_d44(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xD3B6EBC6C3D77D44, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d4022_c7286_b0_dfa2(p0: &str, p1: i32, p2: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xD4022C7286B0DFA2, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d42_c543_f73233041(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xD42C543F73233041, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d637_d327080_cd86_e(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xD637D327080CD86E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d78_a26024_bb13_e08(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xD78A26024BB13E08, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d7_bad4062074_b9_c1(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD7BAD4062074B9C1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d7_d0_df27_cb1765_b5(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xD7D0DF27CB1765B5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_da1_bfed8582_f61_f0() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDA1BFED8582F61F0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dbdf80673_bba3_d65(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xDBDF80673BBA3D65, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc6_ad5_c046_f33_ab4(p0: bool, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xDC6AD5C046F33AB4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dca4_a74135_e1_dea5(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDCA4A74135E1DEA5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e10_f2_d7715_ababec(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE10F2D7715ABABEC, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e31_a04513237_dc89(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xE31A04513237DC89, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e39600_e50_d608693(p0: NativeArg, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE39600E50D608693, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e3_ab5_eefcb6671_a2(setting: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(setting)];
    unsafe { native::invoke(0xE3AB5EEFCB6671A2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e5634491_a58_c2703(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0xE5634491A58C2703, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e59_f4924_bd3_a718_d(p0: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xE59F4924BD3A718D, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e5_ff65_cff5160752() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE5FF65CFF5160752, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e79_ba3_bc265895_da(p0: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xE79BA3BC265895DA, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e8_e633215471_bb5_d(p0: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE8E633215471BB5D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec089_f84_a9_c16_c62() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEC089F84A9C16C62, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f23_a6_d6_c11_d8_ec15(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xF23A6D6C11D8EC15, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f260_af6_f43953316(handle: ScrHandle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(handle)];
    unsafe { native::invoke(0xF260AF6F43953316, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f302_ab9_d978352_ee(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xF302AB9D978352EE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f342_f6_bd0_a8287_d5(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF342F6BD0A8287D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f8_dc69_dc1_ad19072(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xF8DC69DC1AD19072, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fb3205788_f8_afa3_f() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFB3205788F8AFA3F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fc6_fcf4_c03_f1_bbf6() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFC6FCF4C03F1BBF6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fd8112109_a96877_c() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFD8112109A96877C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fe53_b1_f8_d43_f19_bf(player1: Player, player2: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player1), NativeArg::int(player2)];
    unsafe { native::invoke(0xFE53B1F8D43F19BF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ff36_f36_b07_e69059(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xFF36F36B07E69059, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn anim_scene_to_net(anim_scene: AnimScene) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xE0D73CDDEA79DDCD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_launch_param(param_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(param_name)];
    unsafe { native::invoke(0x782C94DB6469634D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn commerce_store_is_enabled() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDBC754CB6CCB9378, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn commerce_store_is_open() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCE5E79D9E303628E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_launch_param_exists(param_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(param_name)];
    unsafe { native::invoke(0x02E97CE283648CD9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_launch_param_string() -> Result<NativeString, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC59AB6A04333C502, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_created_mission_pickups(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xD2BA051B94CA9BCC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_peer_negotiation_responses() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4FE932E84FE932E8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_reserved_mission_pickups(p0: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x62BE3ECC79FBD004, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_waypoint_is_active(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xDCC4B7F7112E8AB7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_reserved_mission_entities_for_thread(thread_id: i32, ped_max: &mut i32, vehicle_max: &mut i32, unk_max: &mut i32, ped_min: &mut i32, vehicle_min: &mut i32, unk_min: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(thread_id), NativeArg::in_out(ped_max), NativeArg::in_out(vehicle_max), NativeArg::in_out(unk_max), NativeArg::in_out(ped_min), NativeArg::in_out(vehicle_min), NativeArg::in_out(unk_min)];
    unsafe { native::invoke(0x99AAC89C510DEB0D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_social_matchmaking_allowed() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD0541EF28E9C4783, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn local_player_pedshot_texture_download_request(player_slot: i32, persona_photo_local_cache_type: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player_slot), NativeArg::int(persona_photo_local_cache_type)];
    unsafe { native::invoke(0x6E2FD8CF7EB10E53, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn mugshot_texture_download_request(gamer_handle: &mut Any, p1: i32, name: &str, p3: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::int(p1), NativeArg::string(name), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x9B5DB6CEAFAA10BB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_add_player_to_recent_gamers_list(player: Player, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1)];
    unsafe { native::invoke(0x157D8F3DE12B307F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_alert(ctx: Hash, lh: Hash, ec: i32, h: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(ctx), NativeArg::hash(lh), NativeArg::int(ec), NativeArg::int(h)];
    unsafe { native::invoke(0x1BAA028F52EED310, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_are_online_notifications_shown_in_story_mode() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF5C5929E07512F80, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_are_players_in_same_platform_party(gamer_handle1: &mut Any, gamer_handle2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle1), NativeArg::in_out(gamer_handle2)];
    unsafe { native::invoke(0x11820D1AE80DEA39, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_is_auto_warp_disabled() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE258570E0C116A66, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_is_instanced_session() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x277865A734918AE6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_is_processing_session_split() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA021095C983F20D8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_set_allowed_to_merge(toggle: bool, p1: &mut Any, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle), NativeArg::in_out(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x63246A24F5747510, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_set_allowed_to_split(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x0A428058079EE65C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_set_auto_warp_enabled(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x4440FEE3EFE78F54, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_auto_session_split_session_successful() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6D87BA8EF15226CD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_can_add_friend(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x99ABE9BF9DADA162, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_can_receive_invite_from_handle(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xF23D6475640D29EB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clear_clock_override_overtime(milliseconds: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(milliseconds)];
    unsafe { native::invoke(0x65F040D91001ED4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clock_time_override(hour: i32, minute: i32, second: i32, transition_time: i32, pause_clock: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(hour), NativeArg::int(minute), NativeArg::int(second), NativeArg::int(transition_time), NativeArg::boolean(pause_clock)];
    unsafe { native::invoke(0x669E223E64B1903C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_clock_time_override_2(hour: i32, minute: i32, second: i32, transition_time: i32, pause_clock: bool, clockwise: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(hour), NativeArg::int(minute), NativeArg::int(second), NativeArg::int(transition_time), NativeArg::boolean(pause_clock), NativeArg::boolean(clockwise)];
    unsafe { native::invoke(0xE28C13ECC36FF14E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_debug_request_entity_position(p0: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0xFA38B52F91B59075, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_did_recent_gamer_names_request_succeed() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x12AEB56B489415C5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_current_friend_page_data(p0: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0xA3EEC0A5AFF3FC5B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_display_name_from_handle(gamer_handle: &mut Any, display_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::string(display_name)];
    unsafe { native::invoke(0x7FEE4F07C54B6B3C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gamertag_from_friend(gamer_handle: &mut Any) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x5659D87BE674AB17, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gamer_session_from_handle(data: &mut Any, count: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(data), NativeArg::int(count)];
    unsafe { native::invoke(0xFBDFE1C1356E12E8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_gamer_status(gamer_handle: &mut Any, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::int(p1)];
    unsafe { native::invoke(0xDDAEB478E58F8DEA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_global_entity_flags(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xDD7806FD0543BC3D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_instance_id_of_thread(thread_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(thread_id)];
    unsafe { native::invoke(0xFB9ECED5B68F3B78, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_num_recent_gamers() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x37A834AEC6A4F74A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_platform_invite_id() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9BCF28FB5D65A9BE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_player_fast_instance_id(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xD9267375834C5EAB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_player_owner_of_network_id(net_id: i32) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0xA6C0787443C9583E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_rank() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x32C90CDFAF40514C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_ros_title_name() -> Result<NativeString, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAC6153A0722F524C, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_session_host() -> Result<Player, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8DC9AA3B508B1A85, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_size_of_host_broadcast_data_storage(p0: &mut i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0xBA24095EA96DFE17, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_size_of_player_broadcast_data_storage(p0: &mut i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0x690806BC83BC8CA2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_get_xp() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDB438CC9BC6F4022, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_completed_mp_intro_flow_on_current_slot() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDD73C9838CE7181D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_control_of_anim_scene(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0x26A5C12FACFF8724, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_current_get_gamer_status_started() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x25189F9908E9CD65, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_friend_handle_in_same_title(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x665161D250850A9F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_friend_handle_online(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xE348D1404BD80146, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_in_session_lobby() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC5196C42DE19F646, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_player_in_spectator_mode(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x5B709519997ECF0F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_previous_upload_pending() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA21E3BAD0A42D199, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_recent_gamer_names_request_in_progress() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4664D213A0CCAF40, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_thread_active(thread_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(thread_id)];
    unsafe { native::invoke(0x31DAD2CD6D49546E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_is_tracked_player_visible(player: Player, tracked_player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(tracked_player)];
    unsafe { native::invoke(0xE525878A35B9EEBD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_persona_photo_write_local(texture: &str, player_slot: i32, p2: i32, persona_photo_local_cache_type: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(texture), NativeArg::int(player_slot), NativeArg::int(p2), NativeArg::int(persona_photo_local_cache_type)];
    unsafe { native::invoke(0x2A48D9567940598F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_persona_photo_write_sc_profile(texture: &str, persona_photo_type: i32, format_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(texture), NativeArg::int(persona_photo_type), NativeArg::int(format_index)];
    unsafe { native::invoke(0xB72999D3120599DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_remove_friend(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x55F618F68AB854D3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_request_control_of_anim_scene(anim_scene: AnimScene) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(anim_scene)];
    unsafe { native::invoke(0xAAA92B631B13F614, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_resurrect_local_player_2(args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0x4154B7D8C75E5DCF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_send_session_invite(gamer_handle: &mut Any, content_id: &str, data: &mut Any, data_size: i32, p4: i32, flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::string(content_id), NativeArg::in_out(data), NativeArg::int(data_size), NativeArg::int(p4), NativeArg::int(flags)];
    unsafe { native::invoke(0xE47001B7CB8B98AE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_add_session_flags(flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags)];
    unsafe { native::invoke(0xE546BDA1B3E288EE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_are_session_ids_equal(session_id1: &mut Any, session_id2: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(session_id1), NativeArg::in_out(session_id2)];
    unsafe { native::invoke(0x4DEC5000F7B508F0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_cancel_request(session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0xE72E5C1289BD1F40, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_get_session_id(session_id: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(session_id)];
    unsafe { native::invoke(0xE9B356C330C0A806, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_get_session_request_result(session_request_id: &mut Any, p1: &mut i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(session_request_id), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x0DD051B1BF4B8BD6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_is_nsrr_success(session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0x0F44A5C78D114922, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_is_request_in_progress_by_queue_group(queue_group: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(queue_group)];
    unsafe { native::invoke(0x9E762A595CF88E4A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_playlist_get_upcoming_content() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8F9DB6CD03B42B58, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_playlist_go_to_next_content() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBDE605F925B07127, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_remove_player_flags(flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags)];
    unsafe { native::invoke(0x3215BBE34D3418C5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_request_session_nominated(flags: i32, user_hash: i32, p2: i32, session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags), NativeArg::int(user_hash), NativeArg::int(p2), NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0x4F4672457FF597D1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_request_session_on_call(flags: i32, category: i32, p2: &mut Any, user_hash: i32, session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags), NativeArg::int(category), NativeArg::in_out(p2), NativeArg::int(user_hash), NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0x23D9C1F2E4098EDC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_set_player_flags(flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(flags)];
    unsafe { native::invoke(0x0AE241A4A9ADEEEC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_shutdown() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFD4272A137703449, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_session_transition_to_session(session_request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(session_request_id)];
    unsafe { native::invoke(0xF20B18A330E6DB5C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_set_in_static_spectator_mode(toggle: bool, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xFBF1ECFB39A77B5F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_add_exclusion_volume(volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0xEEB7818B1D307212, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_add_property_preference(config_property: i32, include: bool, weight: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(config_property), NativeArg::boolean(include), NativeArg::float(weight)];
    unsafe { native::invoke(0xEB6027FD1B4600D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_add_property_scripted(config_property: i32, include: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(config_property), NativeArg::boolean(include)];
    unsafe { native::invoke(0x44D59EC597BBF348, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_add_spawn_point(x: f32, y: f32, z: f32, heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading)];
    unsafe { native::invoke(0xFD1AC0B3858F224C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_remove_exclusion_volume(volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0xA35E7BF20FA269E0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_search_in_progress() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x89EC2FC89ECB1005, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_set_cancel_search() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x765E60A1DCB8B1CE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_spawn_config_set_level_water_depth(water_depth_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(water_depth_level)];
    unsafe { native::invoke(0xBDCC671B911040F9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn net_to_anim_scene(net_id: i32) -> Result<AnimScene, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0xD7F6781A0ABAF6FB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn net_to_propset(net_id: i32) -> Result<PropSet, NativeError> {
    let arguments = [NativeArg::int(net_id)];
    unsafe { native::invoke(0xD08066E00D26C448, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pedmugshot_get_status() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCBAC13F065C47596, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pedmugshot_request_send() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFBC30B70B3CDB87E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn pedmugshot_take() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCD954F330693F5F2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn propset_to_net(prop_set: PropSet) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(prop_set)];
    unsafe { native::invoke(0x74F99EF7EF503398, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn report_player(player: Player, report_type: i32, description: &str, horse_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(report_type), NativeArg::string(description), NativeArg::string(horse_name)];
    unsafe { native::invoke(0xA197C35F73AC0F12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_pedshot_texture_local_backup_download(player: i32, persona_photo_local_cache_type: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(persona_photo_local_cache_type)];
    unsafe { native::invoke(0x356F9FB0698C1FEB, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_pedshot_texture_local_download(gamer_handle: &mut Any, p1: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::int(p1)];
    unsafe { native::invoke(0xCAF4CA2F87779F8F, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_pedshot_texture_multiplayer_download(gamer_handle: &mut Any, p1: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::int(p1)];
    unsafe { native::invoke(0xB5C4B18B12A2AF23, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_door_networked(door_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0x51D99497ABF3F451, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_door_unnetworked(p0: NativeArg, toggle: bool) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC1E1A3D5ED7617B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_entity_ghosted_to_local_player(entity: Entity, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xEE5AE9956743BA20, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_launch_param_string(params: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(params)];
    unsafe { native::invoke(0xDFFC15AA63D04AAB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_launch_param_value(param_name: &str, value: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(param_name), NativeArg::string(value)];
    unsafe { native::invoke(0x668AF6E4933AC13F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_local_player_damage_multiplier_for_player(player: Player, damage_multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(damage_multiplier)];
    unsafe { native::invoke(0xD041A32992A55F84, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_network_respot_timer(entity: Entity, timer: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(timer), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x442B4347B6EC36E8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_visibility_to_local_player_disabled(player: Player, disabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(disabled)];
    unsafe { native::invoke(0xDCA6ABDB9288FBE4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_social_matchmaking_allowed(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x777D0571A466B520, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn texture_download_release_by_name(name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x7A17B7981560FFA5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn texture_download_texture_name_is_valid(name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0xE2C3CEC3C0903A00, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_has_privilege() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6506BFA755FB209C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_is_book_marked(content_id: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(content_id)];
    unsafe { native::invoke(0xE42D1042F09865FE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_by_category(category_type: i32, p1: i32, max_get: i32, content_type_name: &str, p4: i32, p5: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(category_type), NativeArg::int(p1), NativeArg::int(max_get), NativeArg::string(content_type_name), NativeArg::int(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x8C109958C9BB559D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_by_content_id(content_id: &str, latest_version: bool, content_type_name: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(content_id), NativeArg::boolean(latest_version), NativeArg::string(content_type_name)];
    unsafe { native::invoke(0x69D22E183580113F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_by_content_type(p0: i32, max_get: i32, content_type_name: &str, p3: i32, p4: i32, p5: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(max_get), NativeArg::string(content_type_name), NativeArg::int(p3), NativeArg::int(p4), NativeArg::int(p5)];
    unsafe { native::invoke(0xF40EF49B3099E98E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_book_marked(p0: NativeArg, index: i32) -> Result<bool, NativeError> {
    let arguments = [p0, NativeArg::int(index)];
    unsafe { native::invoke(0x98539FC453AEA639, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_creator_handle(p0: NativeArg, index: i32, gamer_handle: &mut Any) -> Result<Any, NativeError> {
    let arguments = [p0, NativeArg::int(index), NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0xADB56322EEDFBDC9, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_creator_photo(p0: NativeArg, p1: i32, p2: NativeArg) -> Result<NativeString, NativeError> {
    let arguments = [p0, NativeArg::int(p1), p2];
    unsafe { native::invoke(0x409FE0CA6A4D1D49, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_date(p0: NativeArg, index: i32, p2: &mut Any) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(index), NativeArg::in_out(p2)];
    unsafe { native::invoke(0xE0CB4AB15CB32710, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_language(p0: NativeArg, index: i32) -> Result<i32, NativeError> {
    let arguments = [p0, NativeArg::int(index)];
    unsafe { native::invoke(0x97764E8AC6487A9A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_mission_desc_hash(p0: NativeArg, index: i32) -> Result<Hash, NativeError> {
    let arguments = [p0, NativeArg::int(index)];
    unsafe { native::invoke(0xA6BF569956C60A60, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_name(p0: NativeArg, index: i32) -> Result<NativeString, NativeError> {
    let arguments = [p0, NativeArg::int(index)];
    unsafe { native::invoke(0x2D053EA815702DD1, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_owner_id(p0: NativeArg, index: i32) -> Result<NativeString, NativeError> {
    let arguments = [p0, NativeArg::int(index)];
    unsafe { native::invoke(0xF9F0B3028431967B, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_playlist_name(p0: NativeArg, index: i32) -> Result<NativeString, NativeError> {
    let arguments = [p0, NativeArg::int(index)];
    unsafe { native::invoke(0xCAF50048C8D0FBA0, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_posix_published_date(p0: NativeArg, p1: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x104080CA9E519B00, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_posix_updated_date(p0: NativeArg, p1: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x21A99A72B00D8002, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_published(p0: NativeArg, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9993F1E11944A3DD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_rating(p0: NativeArg, index: i32, p2: i32) -> Result<f32, NativeError> {
    let arguments = [p0, NativeArg::int(index), NativeArg::int(p2)];
    unsafe { native::invoke(0x24CD8FAEA1368379, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_root_content_id(p0: NativeArg, index: i32) -> Result<NativeString, NativeError> {
    let arguments = [p0, NativeArg::int(index)];
    unsafe { native::invoke(0x566CEB0542EF5ECF, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ugc_query_get_version(p0: NativeArg, index: i32, p2: i32) -> Result<i32, NativeError> {
    let arguments = [p0, NativeArg::int(index), NativeArg::int(p2)];
    unsafe { native::invoke(0x63E9DCBC8B0931ED, &arguments) }.map(|result| result.as_i32())
}

// ============================================================================
// OBJECT
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn allow_damage_events_for_non_networked_objects(enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(enabled)];
    unsafe { native::invoke(0xE2B3B852B537C398, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_portable_pickup_to_ped(pickup_object: Object, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pickup_object), NativeArg::int(ped)];
    unsafe { native::invoke(0x8DC39368BDD57755, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn block_pickup_from_player_collection(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB8F5062070BB6DBD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn break_all_object_fragment_bones(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x8462BE2341A55B6F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn break_object_fragment_child(object: Object, p1: NativeArg, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), p1, NativeArg::boolean(p2)];
    unsafe { native::invoke(0xE7E4C198B0185900, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn convert_old_pickup_type_to_new(pickup_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(pickup_hash)];
    unsafe { native::invoke(0x5EAAD83F8CFB4575, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_ambient_pickup(pickup_hash: Hash, x: f32, y: f32, z: f32, flags: i32, amount: i32, custom_model: Hash, create_as_script_object: bool, script_host_object: bool, custom_ammo_type: i32, p10: f32) -> Result<Object, NativeError> {
    let arguments = [NativeArg::hash(pickup_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(flags), NativeArg::int(amount), NativeArg::hash(custom_model), NativeArg::boolean(create_as_script_object), NativeArg::boolean(script_host_object), NativeArg::int(custom_ammo_type), NativeArg::float(p10)];
    unsafe { native::invoke(0x673966A0C0FD7171, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_object(model_hash: Hash, x: f32, y: f32, z: f32, is_network: bool, b_script_host_obj: bool, dynamic: bool, p7: bool, p8: bool) -> Result<Object, NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(is_network), NativeArg::boolean(b_script_host_obj), NativeArg::boolean(dynamic), NativeArg::boolean(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0x509D5878EB39E842, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_object_no_offset(model_hash: Hash, x: f32, y: f32, z: f32, is_network: bool, b_script_host_obj: bool, dynamic: bool, p7: bool) -> Result<Object, NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(is_network), NativeArg::boolean(b_script_host_obj), NativeArg::boolean(dynamic), NativeArg::boolean(p7)];
    unsafe { native::invoke(0x9A294B2138ABB884, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_object_skeleton(object: Object) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0xB6CBD40F8EA69E8A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_pickup(pickup_hash: Hash, x: f32, y: f32, z: f32, flags: i32, p5: i32, p6: bool, model_hash: Hash, p8: i32, p9: f32, p10: NativeArg) -> Result<Pickup, NativeError> {
    let arguments = [NativeArg::hash(pickup_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(flags), NativeArg::int(p5), NativeArg::boolean(p6), NativeArg::hash(model_hash), NativeArg::int(p8), NativeArg::float(p9), p10];
    unsafe { native::invoke(0xFBA08C503DD5FA58, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_pickup_rotate(pickup_hash: Hash, pos_x: f32, pos_y: f32, pos_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, flags: i32, p8: i32, p9: i32, p10: bool, model_hash: Hash, p12: i32, p13: f32, p14: NativeArg) -> Result<Pickup, NativeError> {
    let arguments = [NativeArg::hash(pickup_hash), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::int(flags), NativeArg::int(p8), NativeArg::int(p9), NativeArg::boolean(p10), NativeArg::hash(model_hash), NativeArg::int(p12), NativeArg::float(p13), p14];
    unsafe { native::invoke(0x891804727E0A98B7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_portable_pickup(pickup_hash: Hash, x: f32, y: f32, z: f32, place_on_ground: bool, model_hash: Hash) -> Result<Object, NativeError> {
    let arguments = [NativeArg::hash(pickup_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(place_on_ground), NativeArg::hash(model_hash)];
    unsafe { native::invoke(0x2EAF1FDB2FB55698, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_object(object: &mut Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(object)];
    unsafe { native::invoke(0x931914268722C263, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_portable_pickup_from_ped(pickup_object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pickup_object)];
    unsafe { native::invoke(0xCF463D1E9A0AECB1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_object_of_type_exist_at_coords(x: f32, y: f32, z: f32, radius: f32, hash: Hash, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::hash(hash), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xBFA48E2FF417213F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_pickup_exist(pickup: Pickup) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(pickup)];
    unsafe { native::invoke(0xAFC1CA75AD4074D1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_pickup_object_exist(pickup_object: Object) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(pickup_object)];
    unsafe { native::invoke(0xD9EFB6DBF7DAAEA3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_pickup_of_type_exist_in_area(pickup_hash: Hash, x: f32, y: f32, z: f32, radius: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(pickup_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0xF9C36251F6E48E33, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_rayfire_map_object_exist(object: Object) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x52AF537A0C5B8AAD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_get_door_state(door_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0x160AA1B32F6139B8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_get_open_ratio(door_hash: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0x65499865FCA6E5EC, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_set_automatic_distance(door_hash: Hash, distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash), NativeArg::float(distance)];
    unsafe { native::invoke(0x9BA001CB45CBF627, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_set_automatic_rate(door_hash: Hash, rate: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash), NativeArg::float(rate)];
    unsafe { native::invoke(0x03C27E13B42A0E82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_set_door_state(door_hash: Hash, state: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash), NativeArg::int(state)];
    unsafe { native::invoke(0x6BAB9442830C7F53, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_set_open_ratio(door_hash: Hash, ajar: f32, force_update: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash), NativeArg::float(ajar), NativeArg::boolean(force_update)];
    unsafe { native::invoke(0xB6E6FBA95C7324AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn fix_object_fragment(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0xF9C1681347C8BD15, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_pickup_regenerate(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x758A5C1B3B1E1990, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_closest_object_of_type(x: f32, y: f32, z: f32, radius: f32, model_hash: Hash, mission_script_object: bool, script_host_object: bool, network_object: bool) -> Result<Object, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::hash(model_hash), NativeArg::boolean(mission_script_object), NativeArg::boolean(script_host_object), NativeArg::boolean(network_object)];
    unsafe { native::invoke(0xE143FA2249364369, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_object_fragment_damage_health(p0: NativeArg, p1: bool) -> Result<f32, NativeError> {
    let arguments = [p0, NativeArg::boolean(p1)];
    unsafe { native::invoke(0xB6FBFD079B8D0596, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_offset_from_coord_and_heading_in_world_coords(x_pos: f32, y_pos: f32, z_pos: f32, heading: f32, x_offset: f32, y_offset: f32, z_offset: f32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::float(heading), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset)];
    unsafe { native::invoke(0x163E252DE035A133, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_pickup_coords(pickup: Pickup) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(pickup)];
    unsafe { native::invoke(0x225B8B35C88029B3, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_pickup_object(pickup: Pickup) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(pickup)];
    unsafe { native::invoke(0x5099BC55630B25AE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rayfire_map_object(x: f32, y: f32, z: f32, radius: f32, name: &str) -> Result<Object, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::string(name)];
    unsafe { native::invoke(0xB48FCED898292E52, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rayfire_map_object_anim_phase(object: Object) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x260EE4FDBDF4DB01, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_safe_pickup_coords(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<Vector3, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x6E16BC2503FF1FF0, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_state_of_rayfire_map_object(object: Object) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x899BA936634A322E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_type_from_pickup_type(pickup_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(pickup_hash)];
    unsafe { native::invoke(0x08F96CA6C551AD51, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_closest_object_of_type_been_broken(p0: f32, p1: f32, p2: f32, p3: f32, model_hash: Hash, p5: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::hash(model_hash), p5];
    unsafe { native::invoke(0x761B0E69AC4D007E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_object_been_broken(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x8ABFB70C49CC43E2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_pickup_been_collected(pickup: Pickup) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(pickup)];
    unsafe { native::invoke(0x80EC48E6679313F9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_door_closed(door_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0xC531EE8A1145A149, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_door_registered_with_system(door_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0xC153C43EA202C8C1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_object_a_portable_pickup(object: Object) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x0378C08504160D0D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_object_visible(object: Object) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x8B32ACE6326A7546, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_point_in_angled_area(p0: f32, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: f32, p10: bool, p11: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), NativeArg::float(p9), NativeArg::boolean(p10), NativeArg::boolean(p11)];
    unsafe { native::invoke(0x2A70BAE8883E4C81, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn only_clean_up_object_when_out_of_range(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0xADBE4809F19F927A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn place_object_on_ground_properly(object: Object, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x58A850EAEE20FAA3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn prevent_collection_of_portable_pickup(object: Object, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x92AEFB5F6E294023, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_all_pickups_of_type(pickup_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(pickup_hash)];
    unsafe { native::invoke(0x27F9D613092159CF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_door_from_system(door_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0x464D8E1427156FE4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_pickup(pickup: Pickup) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pickup)];
    unsafe { native::invoke(0x3288D8ACAECD2AB2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_activate_object_physics_as_soon_as_it_is_unfrozen(object: Object, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x406137F8EF90EAF5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_custom_textures_on_object(object: Object, txd_hash: Hash, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::hash(txd_hash), p2, p3];
    unsafe { native::invoke(0xE124889AE0521FCF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_force_object_this_frame(x: f32, y: f32, z: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3)];
    unsafe { native::invoke(0xF538081986E49E9D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_local_player_can_collect_portable_pickups(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x78857FC65CADB909, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_local_player_permitted_to_collect_pickups_with_model(model_hash: Hash, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x88EAEC617CD26926, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_max_num_portable_pickups_carried_by_player(model_hash: Hash, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::int(p1)];
    unsafe { native::invoke(0x0BF3B3BD47D79C08, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_allow_low_lod_buoyancy(object: Object, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x4D89D607CB3DD1D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_physics_params(object: Object, weight: f32, p2: f32, p3: f32, p4: f32, p5: f32, gravity: f32, p7: f32, p8: f32, p9: f32, p10: f32, buoyancy: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::float(weight), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(gravity), NativeArg::float(p7), NativeArg::float(p8), NativeArg::float(p9), NativeArg::float(p10), NativeArg::float(buoyancy)];
    unsafe { native::invoke(0xF6DF6E90DE7DF90F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_takes_damage_from_colliding_with_buildings(object: Object, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0xEB6F1A9B5510A5D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_targettable(object: Object, targettable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(targettable)];
    unsafe { native::invoke(0x8A7391690F5AFD81, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_tint_index(object: Object, texture_variation: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::int(texture_variation)];
    unsafe { native::invoke(0x971DA0055324D033, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_do_not_auto_place_on_ground(pickup_object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pickup_object)];
    unsafe { native::invoke(0x634C19521485AB25, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_generation_range_multiplier(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0x318516E02DE3ECE2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_hidden_when_uncollectable(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x81218CE01B672219, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_not_lootable(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x92E87F60F21A0C3A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_particle_fx_highlight(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x1607C7D9B3021DF5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_particle_fx_spawn(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xEB9740A38FD6D634, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_regeneration_time(pickup: Pickup, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pickup), NativeArg::int(duration)];
    unsafe { native::invoke(0x78015C9B4B3ECC9D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_uncollectable(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x4A8CB328CD6F1C9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_state_of_rayfire_map_object(object: Object, state: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::int(state)];
    unsafe { native::invoke(0x5C29F698D404C5E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_team_pickup_object(object: Object, p1: NativeArg, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), p1, NativeArg::boolean(p2)];
    unsafe { native::invoke(0x53E0DF1A2A3CF0CA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn slide_object(object: Object, to_x: f32, to_y: f32, to_z: f32, speed_x: f32, speed_y: f32, speed_z: f32, collision: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::float(to_x), NativeArg::float(to_y), NativeArg::float(to_z), NativeArg::float(speed_x), NativeArg::float(speed_y), NativeArg::float(speed_z), NativeArg::boolean(collision)];
    unsafe { native::invoke(0x2FDFF4107B8C1147, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn suppress_pickup_reward_type(reward_type: i32, suppress: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(reward_type), NativeArg::boolean(suppress)];
    unsafe { native::invoke(0xF92099527DB8E2A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn track_object_visibility(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0xB252BC036B525623, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x08_c5825_a2932_ea7_b(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x08C5825A2932EA7B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0943113_e02322164(object: Object, p1: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::int(p1)];
    unsafe { native::invoke(0x0943113E02322164, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_c0_a373_d181_bf900(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0C0A373D181BF900, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_f5_e07_e14_a86_fafc(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x1F5E07E14A86FAFC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x20135_af9_c10_d2_a3_d(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x20135AF9C10D2A3D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x22031584496_cfb70(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x22031584496CFB70, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x235_c863_da77_bd88_d(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x235C863DA77BD88D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x250_ebb11_e81_a10_be(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x250EBB11E81A10BE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_bf1953_c0_c21_ac88(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2BF1953C0C21AC88, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_a77_dae8_b4_fd7586(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3A77DAE8B4FD7586, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_df1_a0_a58498_e209(object: Object, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x3DF1A0A58498E209, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_e2616_e7_ea539480(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3E2616E7EA539480, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x46_cbcf0_e98_a4_e156(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x46CBCF0E98A4E156, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x491439_aef410_a2_fc(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x491439AEF410A2FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_ae07_eba3462_c5_d5(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x4AE07EBA3462C5D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_d8611_dfe1126478(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x4D8611DFE1126478, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5230_bf34_eb0_ec645(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5230BF34EB0EC645, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x57_c242543_b7_b8_fb9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x57C242543B7B8FB9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x58_de624_fa7_fb0_e7_f(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x58DE624FA7FB0E7F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x614_d0_b4533_f842_d3(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x614D0B4533F842D3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6579860_a5558524_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6579860A5558524A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_e2_aa80_bb0_c03728(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6E2AA80BB0C03728, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_d4411_d6736_cd295(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x7D4411D6736CD295, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_f458_b543006_c8_fe(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x7F458B543006C8FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_fcd49388_bc9_b775(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x7FCD49388BC9B775, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_a74_a9_cadfa8_a598(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9A74A9CADFA8A598, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_f52_ad67_d1_a91_bad(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9F52AD67D1A91BAD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a93_f925_f1942_e434(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA93F925F1942E434, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_aaacf33_cbf9_b990_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xAAACF33CBF9B990A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_acd4_f9831_dfad7_f5(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xACD4F9831DFAD7F5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b3_b1546_d23_df8_de1(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xB3B1546D23DF8DE1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c07_b91_b996_c1_de89(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC07B91B996C1DE89, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cbfbd38_f2_e0_a263_b(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCBFBD38F2E0A263B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ceab54_f4632_c6_ef6(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCEAB54F4632C6EF6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d91_e55_b6_c005_eb09(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD91E55B6C005EB09, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_de116_ecffdd4_b997(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDE116ECFFDD4B997, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dfa1237_f5228263_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDFA1237F5228263F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e157_a8_a336_c7_f04_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE157A8A336C7F04A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eba314768_fb35_d58(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xEBA314768FB35D58, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f65_ede5_d02_a7_a760(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xF65EDE5D02A7A760, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f6_e88489_b4_e6_ebe5(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xF6E88489B4E6EBE5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa99_e8_e575_f2_fef8(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xFA99E8E575F2FEF8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_door_to_system_new(door_hash: Hash, p1: bool, p2: bool, p3: bool, thread_id: i32, p5: i32, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::int(thread_id), NativeArg::int(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0xD99229FE93B46286, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn damage_bone_on_prop(object: Object, bone: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::int(bone)];
    unsafe { native::invoke(0xE4EFB315BCD2A838, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_change_script_owner(door_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0x985767F5FA45BC44, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_force_shut(door_hash: Hash, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x276AAF0F1C7F2494, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_get_automatic_rate(door_hash: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0x8433E1954BE323FC, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_set_able_to_change_open_ratio_while_locked(door_hash: Hash, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x1F1FABFE9B2A1254, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn door_system_set_automatic_state(door_hash: Hash, disable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(door_hash), NativeArg::boolean(disable)];
    unsafe { native::invoke(0x1BC47A9DEDC8DF5D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ammo_type_from_pickup_type(pickup_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(pickup_hash)];
    unsafe { native::invoke(0x44B09A23D728045A, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_light_intensity_from_object(object: Object) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0xFA3B61EC249B4674, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_object_light_intensity(object: Object) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x3397CD4E0353DFBA, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hide_pickup_object(pickup_object: Object, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pickup_object), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x2777150CC7D9365E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_door_registered_with_network(door_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0xB5DED7B65C604FDF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_door_registered_with_owner(door_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(door_hash)];
    unsafe { native::invoke(0x4F89DAD4156BA145, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_pickup_type_valid(pickup_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(pickup_hash)];
    unsafe { native::invoke(0x007BD043587F7C82, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn make_item_carriable(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x1461DF6DB886BE3F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_object_velocity(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0xF40AB58D83C35027, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_pickup_lifetime(lifetime: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(lifetime)];
    unsafe { native::invoke(0xAC9AE68F0A463752, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_auto_jumpable_by_horse(object: Object, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x98D2D9C053A1F449, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_light_intensity_for_object(object: Object, light_intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::float(light_intensity)];
    unsafe { native::invoke(0xF49574E2332A8F06, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_light_scattering_disabled_for_object(object: Object, disable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(disable)];
    unsafe { native::invoke(0x04D1D4E411CE52D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_light_translucency_for_object(object: Object, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::float(value)];
    unsafe { native::invoke(0x63E39F09310F481F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_network_pickup_usable_for_player(player: Player, pickup_hash: Hash, is_usable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(pickup_hash), NativeArg::boolean(is_usable)];
    unsafe { native::invoke(0x94F3D956BFAEAE18, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_not_jumpable_by_horse(object: Object, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xE1C708BA4885796B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_break_scale(object: Object, scale: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::float(scale)];
    unsafe { native::invoke(0xFFB99FFD17F65889, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_burn_intensity(object: Object, intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::float(intensity)];
    unsafe { native::invoke(0xC8E21C1677DC5E6F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_burn_level(object: Object, burn_level: f32, affect_ash: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::float(burn_level), NativeArg::boolean(affect_ash)];
    unsafe { native::invoke(0x2797C633DCDBBAC5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_burn_opacity(object: Object, opacity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::float(opacity)];
    unsafe { native::invoke(0x7D7285EFEAB5AF15, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_burn_speed(object: Object, speed: f32, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::float(speed), NativeArg::float(p2)];
    unsafe { native::invoke(0x646564A3B7DF68F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_interaction_preset(object: Object, preset_flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::int(preset_flags)];
    unsafe { native::invoke(0xCAAF2BCCFEF37F77, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_kickable(object: Object, kickable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(kickable)];
    unsafe { native::invoke(0xB7017DA4D498269F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_prompt_name(object: Object, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::string(name)];
    unsafe { native::invoke(0xAEE6C800E124CFE1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_prompt_name_from_gxt_entry(object: Object, name: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::hash(name)];
    unsafe { native::invoke(0xD503D6F0986D58BC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_targettable_2(object: Object, targettable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(targettable)];
    unsafe { native::invoke(0x581EDBE56E8D62C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_object_targettable_focus(object: Object, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xA22712E8471AA08E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pickup_collectable_on_mount(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x00EE08603EADEE92, &arguments) }.map(|_| ())
}

// ============================================================================
// PAD
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_control_light_effect(control: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control)];
    unsafe { native::invoke(0xCB0360EFEFB2580D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_control_shake_suppressed_id(control: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control)];
    unsafe { native::invoke(0xA0CEFCEA390AAB9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_all_control_actions(control: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control)];
    unsafe { native::invoke(0x5F4B6931816E599B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_control_action(control: i32, action: Hash, disable_related_actions: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action), NativeArg::boolean(disable_related_actions)];
    unsafe { native::invoke(0xFE99B66D079CF6BC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_control_action(control: i32, action: Hash, enable_related_actions: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action), NativeArg::boolean(enable_related_actions)];
    unsafe { native::invoke(0x351220255D64C155, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_control_how_long_ago(control: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(control)];
    unsafe { native::invoke(0xD7D22F5592AED8BA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_control_normal(control: i32, action: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0xEC3C9B8D5327B563, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_control_unbound_normal(control: i32, action: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0x5B84D09CEC5209C5, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_control_value(control: i32, action: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0xD95E79E8686D2C27, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_disabled_control_normal(control: i32, action: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0x11E65974A982637C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_disabled_control_unbound_normal(control: i32, action: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0x4F8A26A890FD62FB, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn have_controls_changed(control: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control)];
    unsafe { native::invoke(0x6CD79468A1E595C6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_control_enabled(control: i32, action: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0x1CEA6BFDF248E5D9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_control_just_pressed(control: i32, action: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0x580417101DDB492F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_control_just_released(control: i32, action: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0x50F940259D3841E6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_control_pressed(control: i32, action: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0xF3A21BCD95725A4A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_control_released(control: i32, action: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0x648EE3E7F38877DD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_disabled_control_just_pressed(control: i32, action: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0x91AEF906BCA88877, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_disabled_control_just_released(control: i32, action: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0x305C8DCD79DA8B0F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_disabled_control_pressed(control: i32, action: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0xE2587F8CBBD87B1D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_look_inverted() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x77B612531280010D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_using_keyboard_and_mouse(control: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control)];
    unsafe { native::invoke(0xA571D46727E2B718, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_control_light_effect_color(control: i32, red: i32, green: i32, blue: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue)];
    unsafe { native::invoke(0x8290252FFF36ACB5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_control_light_effect_flashing_color(control: i32, red: i32, green: i32, blue: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue)];
    unsafe { native::invoke(0xA45884DB10EC7EE3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_control_shake(control: i32, duration: i32, frequency: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::int(duration), NativeArg::int(frequency)];
    unsafe { native::invoke(0x48B3886C1358D0D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_control_shake_suppressed_id(control: i32, unique_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::int(unique_id)];
    unsafe { native::invoke(0xF239400E16C23E08, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_control_trigger_shake(control: i32, left_duration: i32, left_frequency: i32, right_duration: i32, right_frequency: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::int(left_duration), NativeArg::int(left_frequency), NativeArg::int(right_duration), NativeArg::int(right_frequency)];
    unsafe { native::invoke(0x14D29BB12D47F68C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_control_value_next_frame(control: i32, action: Hash, value: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action), NativeArg::float(value)];
    unsafe { native::invoke(0xE8A25867FBA3B05E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_input_exclusive(control: i32, action: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(action)];
    unsafe { native::invoke(0xEDE476E5EE29EDB1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_control_shake(control: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control)];
    unsafe { native::invoke(0x38C16A305E8CDC8D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1252_c029_fc8_ebb4_d() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1252C029FC8EBB4D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x43_f35_ddb2905_d945(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x43F35DDB2905D945, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x52_c68_e92_d6_e23_add(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x52C68E92D6E23ADD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_f217_bc1190503_d8(rumble_curve: &str, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(rumble_curve), NativeArg::float(p1)];
    unsafe { native::invoke(0x5F217BC1190503D8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x709_ba8_c08_c5_c008_d() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x709BA8C08C5C008D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bd629_c1_c4_f501_c80(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xBD629C1C4F501C80, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_control_context(control: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(control)];
    unsafe { native::invoke(0xDDCEB0F26C89C00F, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_disabled_control_how_long_ago(control: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(control)];
    unsafe { native::invoke(0x771DFCB24D19C2F6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_control_action_valid(action: Hash, control: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(action), NativeArg::int(control)];
    unsafe { native::invoke(0xBC0884BC590951C7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_control_context(control: i32, context: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(control), NativeArg::hash(context)];
    unsafe { native::invoke(0x2804658EB7D8A50B, &arguments) }.map(|_| ())
}

// ============================================================================
// PATHFIND
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_navmesh_blocking_object(p0: f32, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: bool, p8: NativeArg) -> Result<Object, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::boolean(p7), p8];
    unsafe { native::invoke(0xFCD5C8E06E502F5A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_navmesh_required_region(x: f32, y: f32, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(radius)];
    unsafe { native::invoke(0x387EAD7EE42F6685, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_nodes_loaded_for_area(x1: f32, y1: f32, x2: f32, y2: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(x2), NativeArg::float(y2)];
    unsafe { native::invoke(0xF7B79A50B905A30D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_navmesh_blocking_object_exist(object: Object) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x0EAEB0DB4B132399, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_approx_floor_for_point(x: f32, y: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y)];
    unsafe { native::invoke(0x336511A34F2E5185, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_closest_road(x: f32, y: f32, z: f32, p3: f32, p4: i32, p5: &mut Vector3, p6: &mut Vector3, p7: &mut Any, p8: &mut Any, p9: &mut f32, p10: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3), NativeArg::int(p4), NativeArg::in_out(p5), NativeArg::in_out(p6), NativeArg::in_out(p7), NativeArg::in_out(p8), NativeArg::in_out(p9), NativeArg::boolean(p10)];
    unsafe { native::invoke(0x132F52BBA570FE92, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_closest_vehicle_node(x: f32, y: f32, z: f32, out_position: &mut Vector3, node_type: i32, p5: f32, p6: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(out_position), NativeArg::int(node_type), NativeArg::float(p5), NativeArg::float(p6)];
    unsafe { native::invoke(0x240A18690AE96513, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_closest_vehicle_node_with_heading(x: f32, y: f32, z: f32, out_position: &mut Vector3, out_heading: &mut f32, node_type: i32, p6: f32, p7: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(out_position), NativeArg::in_out(out_heading), NativeArg::int(node_type), NativeArg::float(p6), NativeArg::float(p7)];
    unsafe { native::invoke(0x23CFFD4CCB243354, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_gps_blip_route_found() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x869DAACBBE9FA006, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_gps_blip_route_length() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBBB45C3CF5C8AA85, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_nth_closest_vehicle_node(x: f32, y: f32, z: f32, nth_closest: i32, out_position: &mut Vector3, unknown1: i32, unknown2: f32, unknown3: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(nth_closest), NativeArg::in_out(out_position), NativeArg::int(unknown1), NativeArg::float(unknown2), unknown3];
    unsafe { native::invoke(0x5A6D8DF6FBC5D0C4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_nth_closest_vehicle_node_favour_direction(x: f32, y: f32, z: f32, desired_x: f32, desired_y: f32, desired_z: f32, nth_closest: i32, out_position: &mut Vector3, out_heading: &mut f32, nodetype: i32, p10: NativeArg, p11: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(desired_x), NativeArg::float(desired_y), NativeArg::float(desired_z), NativeArg::int(nth_closest), NativeArg::in_out(out_position), NativeArg::in_out(out_heading), NativeArg::int(nodetype), p10, p11];
    unsafe { native::invoke(0x2FAC235A6062F14A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_nth_closest_vehicle_node_id(x: f32, y: f32, z: f32, nth: i32, nodetype: i32, p5: f32, p6: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(nth), NativeArg::int(nodetype), NativeArg::float(p5), NativeArg::float(p6)];
    unsafe { native::invoke(0x116443008E5CEFC3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_nth_closest_vehicle_node_id_with_heading(x: f32, y: f32, z: f32, nth_closest: i32, return_heading: &mut f32, return_num_lanes: &mut i32, node_flags: i32, z_measure_mult: f32, z_tolerance: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(nth_closest), NativeArg::in_out(return_heading), NativeArg::in_out(return_num_lanes), NativeArg::int(node_flags), NativeArg::float(z_measure_mult), NativeArg::float(z_tolerance)];
    unsafe { native::invoke(0x4114EAA8A7F7766D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_nth_closest_vehicle_node_with_heading(x: f32, y: f32, z: f32, nth_closest: i32, out_position: &mut Vector3, heading: &mut f32, unknown1: &mut Any, unknown2: i32, unknown3: f32, unknown4: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(nth_closest), NativeArg::in_out(out_position), NativeArg::in_out(heading), NativeArg::in_out(unknown1), NativeArg::int(unknown2), NativeArg::float(unknown3), NativeArg::float(unknown4)];
    unsafe { native::invoke(0x591B40D4390DB54A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_navmeshes_existing_in_area(p0: f32, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5)];
    unsafe { native::invoke(0x01708E8DD3FF8C65, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_random_vehicle_node(x: f32, y: f32, z: f32, radius: f32, min_lanes: i32, avoid_dead_ends: bool, avoid_highways: bool, out_position: &mut Vector3, node_id: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(min_lanes), NativeArg::boolean(avoid_dead_ends), NativeArg::boolean(avoid_highways), NativeArg::in_out(out_position), NativeArg::in_out(node_id)];
    unsafe { native::invoke(0x93E0DB8440B73A7D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_safe_coord_for_ped(x: f32, y: f32, z: f32, on_ground: bool, out_position: &mut Vector3, flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(on_ground), NativeArg::in_out(out_position), NativeArg::int(flags)];
    unsafe { native::invoke(0xB61C8E878A4199CA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_node_is_switched_off(node_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(node_id)];
    unsafe { native::invoke(0x28533DBDDF7C2C97, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_node_position(node_id: i32, out_position: &mut Vector3) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(node_id), NativeArg::in_out(out_position)];
    unsafe { native::invoke(0x8E8D72FF24DEE1FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_navmesh_loaded_in_area(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2)];
    unsafe { native::invoke(0xF813C7E63F9062A5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_point_on_road(x: f32, y: f32, z: f32, vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(vehicle)];
    unsafe { native::invoke(0x125BF4ABFC536B09, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_node_id_valid(vehicle_node_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle_node_id)];
    unsafe { native::invoke(0x5829A02AF4F0B3CB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_request_path(ped: Ped, x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, bit_flag: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::int(bit_flag)];
    unsafe { native::invoke(0x348F211CA2404039, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_navmesh_blocking_object(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x46399A7895957C0E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_path_nodes_in_area_this_frame(x1: f32, y1: f32, x2: f32, y2: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(x2), NativeArg::float(y2)];
    unsafe { native::invoke(0x07FB139B592FA687, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_roads_in_volume(volume: Volume, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xD17672447692478E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_ped_range_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0x0B919E1FB47CC4E0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ignore_no_gps_flag(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x72751156E7678833, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_paths_back_to_original(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xE04B48F2CC926253, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_paths_in_area(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, unknown: bool, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::boolean(unknown), p7];
    unsafe { native::invoke(0x34F060F4BF92E018, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_roads_back_to_original(x_min: f32, y_min: f32, z_min: f32, x_max: f32, y_max: f32, z_max: f32, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x_min), NativeArg::float(y_min), NativeArg::float(z_min), NativeArg::float(x_max), NativeArg::float(y_max), NativeArg::float(z_max), p6, p7];
    unsafe { native::invoke(0x1EE7063B80FFC77C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_roads_back_to_original_in_angled_area(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8];
    unsafe { native::invoke(0x0027501B9F3B407E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_roads_in_angled_area(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10];
    unsafe { native::invoke(0x1A5AA1208AF5DB59, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_roads_in_area(x_min: f32, y_min: f32, z_min: f32, x_max: f32, y_max: f32, z_max: f32, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x_min), NativeArg::float(y_min), NativeArg::float(z_min), NativeArg::float(x_max), NativeArg::float(y_max), NativeArg::float(z_max), p6, p7, p8];
    unsafe { native::invoke(0xBF1A602B5BA52FEE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_roads_in_volume(volume: Volume, p1: bool, p2: bool, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xC1799FAFD2FDF52B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn simulated_route_get_eta(p0: NativeArg) -> Result<f32, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2DD5F78D73B24172, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn simulated_route_is_loaded(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x240915043CB799D7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn simulated_route_travel_to_point(p0: NativeArg, p1: f32, p2: f32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::float(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0xA1A3DE1C215C7394, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x264_e9_a5_cd78_c338_f(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x264E9A5CD78C338F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x34_c9_af25649172_d0(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x34C9AF25649172D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4358_bcf14_c91761_c(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9];
    unsafe { native::invoke(0x4358BCF14C91761C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_bdebea5702_b97_a9(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x4BDEBEA5702B97A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x54_f4_d7_b6670_fbb5_a(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x54F4D7B6670FBB5A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_a3_b54_addf5472_a3(p0: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x5A3B54ADDF5472A3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_a4_e1_a41_e3_a02_ad0(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x5A4E1A41E3A02AD0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x665_b21666351_cb37(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x665B21666351CB37, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c3_f12_eceb6_d2_e2_a(x_min: f32, y_min: f32, z_min: f32, x_max: f32, y_max: f32, z_max: f32, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x_min), NativeArg::float(y_min), NativeArg::float(z_min), NativeArg::float(x_max), NativeArg::float(y_max), NativeArg::float(z_max), p6, p7];
    unsafe { native::invoke(0x6C3F12ECEB6D2E2A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_dad6630_ae4_a74_cb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6DAD6630AE4A74CB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x869_a7015_bd4606_e9(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x869A7015BD4606E9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a33914_b00_ca55756(p0: &str, p1: i32) -> Result<u64, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::int(p1)];
    unsafe { native::invoke(0xA33914B00CA55756, &arguments) }.map(|result| result.as_u64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_afe2_ae66_f6251_c66(x_min: f32, y_min: f32, z_min: f32, x_max: f32, y_max: f32, z_max: f32, p6: i32, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x_min), NativeArg::float(y_min), NativeArg::float(z_min), NativeArg::float(x_max), NativeArg::float(y_max), NativeArg::float(z_max), NativeArg::int(p6), p7];
    unsafe { native::invoke(0xAFE2AE66F6251C66, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b03944057_fd735_ba(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xB03944057FD735BA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ca27_a86_caa4_e98_ed(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xCA27A86CAA4E98ED, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cf213_a5_fc3_abfc08(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xCF213A5FC3ABFC08, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e5_ef9_de716_ff737_e(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xE5EF9DE716FF737E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_efc535_c9_faf563_b3(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xEFC535C9FAF563B3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f2_a2177_ac848_b3_a8(volume: Volume, p1: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xF2A2177AC848B3A8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_navmesh_blocking_volume(volume: Volume, flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(flags)];
    unsafe { native::invoke(0x19C7567D2F2287D6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_navmesh_blocking_volume_exist(volume: Volume) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0xDE0EA444735C1368, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_spawn_data_for_road_node(node_id: i32, x: f32, y: f32, z: f32, out_coords: &mut Vector3, heading: &mut f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(node_id), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(out_coords), NativeArg::in_out(heading)];
    unsafe { native::invoke(0xA3791B915B8B84C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_activate_swap(name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x7C334FF4D9215912, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_assign_navmesh_to_vehicle(vehicle: Vehicle, nav_mesh_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::string(nav_mesh_name)];
    unsafe { native::invoke(0x44026E3DB3CED602, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_clear_requested_path(path: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(path)];
    unsafe { native::invoke(0x661BB1E1FF77742D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_deactivate_swap(name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x527B97C203BB8606, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_does_swap_exist(name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x495CFAB2924237C7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_is_swap_active(name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x5AC0944C156E5F44, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_requested_path_num_waypoints(path: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(path)];
    unsafe { native::invoke(0xD470725E0703D22F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_requested_path_query_status(path: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(path)];
    unsafe { native::invoke(0x3A0F82F6EE2291C8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_requested_path_waypoints_found(path: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(path)];
    unsafe { native::invoke(0x8800776E410EB669, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_requested_path_waypoints_terrain(path: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(path)];
    unsafe { native::invoke(0xF61CFEDEAB627BFA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn navmesh_requested_path_waypoint_by_index(path: i32, waypoint_index: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(path), NativeArg::int(waypoint_index)];
    unsafe { native::invoke(0x430F8319AE56C8A9, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_navmesh_blocking_volume(volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x2C87C3E1C7B96EE2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn simulated_route_create(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, p6: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::int(p6)];
    unsafe { native::invoke(0xFD5BB35AAB83FD48, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn simulated_route_delete(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x4907D0E4FB26EE65, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn simulated_route_exists(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x65A8196B8D7F5E0B, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// PED
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_armour_to_ped(ped: Ped, amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(amount)];
    unsafe { native::invoke(0x5BA652A0CD14DF2F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_custom_formation_location(group_id: i32, x: f32, y: f32, z: f32, position: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(position)];
    unsafe { native::invoke(0x4E23CD07BD161E06, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_formation_location(group_id: i32, p1: f32, p2: f32, p3: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0xB05945C1E9E60D91, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_relationship_group(name: &str, group_hash: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::in_out(group_hash)];
    unsafe { native::invoke(0xF372BC22FCB88606, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_scenario_blocking_area(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, p6: bool, blocking_flags: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::boolean(p6), NativeArg::int(blocking_flags)];
    unsafe { native::invoke(0x1B5C85C612E5256E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn apply_damage_to_ped(ped: Ped, damage_amount: i32, damage_armour: bool, bone_id: i32, ped_killer: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(damage_amount), NativeArg::boolean(damage_armour), NativeArg::int(bone_id), NativeArg::int(ped_killer)];
    unsafe { native::invoke(0x697157CED63F18D4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn apply_ped_blood_specific(ped: Ped, p1: NativeArg, p2: f32, p3: f32, p4: f32, p5: f32, p6: NativeArg, p7: f32, p8: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), p6, NativeArg::float(p7), NativeArg::in_out(p8)];
    unsafe { native::invoke(0xEF0D582CBF2D9B0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn apply_ped_damage_pack(ped: Ped, damage_pack: &str, damage: f32, mult: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(damage_pack), NativeArg::float(damage), NativeArg::float(mult)];
    unsafe { native::invoke(0x46DF918788CB093F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_knock_ped_off_vehicle(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x51AC07A44D4F5B8A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_ped_be_mounted(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2D64376CF437363E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_ped_in_combat_see_target(ped: Ped, target: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target)];
    unsafe { native::invoke(0xEAD42DE3610D0721, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_ped_ragdoll(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x128F79EDCECE4FD5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_ped_see_entity(ped: Ped, target_entity: Entity, p2: bool, do_foliage_check: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_entity), NativeArg::boolean(p2), NativeArg::boolean(do_foliage_check)];
    unsafe { native::invoke(0x7F9B9791D4CB71F6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_ped_see_ped_cached(ped: Ped, target_ped: Ped, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x9D9473CB82D83A30, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_facial_idle_anim_override(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x726256CC1EEB182F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_blood_damage(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8FE22675A5A45817, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_blood_damage_by_zone(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x56E3B78C5408D9F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_damage_decal_by_zone(ped: Ped, p1: i32, p2: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0x523C79AEEFCC4A2A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_decorations(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0E5173C163976E38, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_env_dirt(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6585D955A68452A5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_last_damage_bone(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8EF6B7AC68E2F01B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_non_creation_area() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2E05208086BA0651, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_wetness(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9C720776DAA43E7E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ragdoll_blocking_flags(ped: Ped, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flags)];
    unsafe { native::invoke(0xD86D101FCFD00A4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_relationship_between_groups(relationship: i32, group1: Hash, group2: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(relationship), NativeArg::hash(group1), NativeArg::hash(group2)];
    unsafe { native::invoke(0x5E29243FB56FC6D4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clone_ped(ped: Ped, is_network: bool, b_script_host_ped: bool, copy_head_blend_flag: bool) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(is_network), NativeArg::boolean(b_script_host_ped), NativeArg::boolean(copy_head_blend_flag)];
    unsafe { native::invoke(0xEF29A16337FACADB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clone_ped_to_target(ped: Ped, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped)];
    unsafe { native::invoke(0xE952D6431689AD9A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compute_satchel_item_for_ped_damage(p0: NativeArg, ped_attached: Ped, damage_cleanliness: i32) -> Result<bool, NativeError> {
    let arguments = [p0, NativeArg::int(ped_attached), NativeArg::int(damage_cleanliness)];
    unsafe { native::invoke(0x9E7738B291706746, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn count_peds_in_combat_with_target(ped: Ped, flag: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flag)];
    unsafe { native::invoke(0x5407B7288D0478B7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn count_peds_in_combat_with_target_within_radius(ped: Ped, x: f32, y: f32, z: f32, radius: f32, flag: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(flag)];
    unsafe { native::invoke(0x336B3D200AB007CB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_group(task_allocator: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(task_allocator)];
    unsafe { native::invoke(0x90370EBE0FEE1A3D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_ped(model_hash: Hash, x: f32, y: f32, z: f32, heading: f32, is_network: bool, b_script_host_ped: bool, p7: bool, p8: bool) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::boolean(is_network), NativeArg::boolean(b_script_host_ped), NativeArg::boolean(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0xD49F9B0955C367DE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_ped_inside_vehicle(vehicle: Vehicle, model_hash: Hash, seat_index: i32, p3: bool, p4: bool, p5: bool) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::hash(model_hash), NativeArg::int(seat_index), NativeArg::boolean(p3), NativeArg::boolean(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x7DD959874C1FD534, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_ped_on_mount(mount: Ped, model_hash: Hash, index: i32, p3: bool, p4: bool, p5: bool, p6: bool) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::hash(model_hash), NativeArg::int(index), NativeArg::boolean(p3), NativeArg::boolean(p4), NativeArg::boolean(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0xF89AA2BD01FC06B7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_ped(ped: &mut Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(ped)];
    unsafe { native::invoke(0xCC0EF140F99365C5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_carriable_entity(entity: Entity, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xED00D72F81CF7278, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_ped_injured_on_ground_behaviour(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x733C87D4CE22BEA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_group_exist(group_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0x7C6B0C22F9F40BBE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn explode_ped_head(ped: Ped, weapon_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x2D05CED3A38D0F3A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn fade_and_destroy_ped(ped: &mut Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(ped)];
    unsafe { native::invoke(0x7043D0681285BA2D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn find_all_attached_carriable_entities(ped: Ped, itemset: ItemSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(itemset)];
    unsafe { native::invoke(0xB5ACE8B23A438EC0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_all_heading_values_to_align(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xFF287323B0E2C69A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_ped_ai_and_animation_update(ped: Ped, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x2208438012482A1A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_ped_motion_state(ped: Ped, motion_state_hash: Hash, p2: bool, p3: i32, p4: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(motion_state_hash), NativeArg::boolean(p2), NativeArg::int(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xF28965D04F570DCA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_initial_offset_position(anim_dict: &str, anim_name: &str, x: f32, y: f32, z: f32, x_rot: f32, y_rot: f32, z_rot: f32, p8: f32, p9: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::string(anim_dict), NativeArg::string(anim_name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::float(p8), NativeArg::int(p9)];
    unsafe { native::invoke(0xBE22B26DD764C040, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_anim_initial_offset_rotation(anim_dict: &str, anim_name: &str, x: f32, y: f32, z: f32, x_rot: f32, y_rot: f32, z_rot: f32, p8: f32, p9: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::string(anim_dict), NativeArg::string(anim_name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::float(p8), NativeArg::int(p9)];
    unsafe { native::invoke(0x4B805E6046EE9E47, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_carried_attached_info_for_slot(out_data: &mut Any, ped: Ped, carriable_slot: i32, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(out_data), NativeArg::int(ped), NativeArg::int(carriable_slot), NativeArg::int(p3)];
    unsafe { native::invoke(0x608BC6A6AACD5036, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_closest_ped(x: f32, y: f32, z: f32, radius: f32, p4: bool, p5: bool, out_ped: &mut Ped, p7: bool, p8: bool, p9: bool, ped_type: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::boolean(p4), NativeArg::boolean(p5), NativeArg::in_out(out_ped), NativeArg::boolean(p7), NativeArg::boolean(p8), NativeArg::boolean(p9), NativeArg::int(ped_type)];
    unsafe { native::invoke(0xC33AB876A77F8164, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_combat_float(ped: Ped, combat_type: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(combat_type)];
    unsafe { native::invoke(0x52DFF8A10508090A, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_target_for_ped(ped: Ped) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xCD66FEA29400A0B5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_dead_ped_pickup_coords(ped: Ped, p1: f32, p2: f32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0xCD5003B097200F36, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_group_size(group_id: i32, has_leader: &mut i32, number_of_followers: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::in_out(has_leader), NativeArg::in_out(number_of_followers)];
    unsafe { native::invoke(0x8DE69FE35CA09A45, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_ped_responding_to_negative_interaction(ped: Ped, player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(player)];
    unsafe { native::invoke(0xA454D234E45BB6E5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_ped_responding_to_positive_interaction(ped: Ped, player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(player)];
    unsafe { native::invoke(0x9337183FDA2E9035, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_jack_target(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5486A79D9FBD342D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_looting_pickup_target_entity(ped: Ped) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x14169FA823679E41, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_melee_target_for_ped(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x18A3E9EE1297FD39, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_meta_ped_asset_guids(ped: Ped, index: i32, drawable: &mut Hash, albedo: &mut Hash, normal: &mut Hash, material: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(index), NativeArg::in_out(drawable), NativeArg::in_out(albedo), NativeArg::in_out(normal), NativeArg::in_out(material)];
    unsafe { native::invoke(0xA9C28516A6DC9D56, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_meta_ped_asset_tint(ped: Ped, index: i32, pallete: &mut Hash, tint0: &mut i32, tint1: &mut i32, tint2: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(index), NativeArg::in_out(pallete), NativeArg::in_out(tint0), NativeArg::in_out(tint1), NativeArg::in_out(tint2)];
    unsafe { native::invoke(0xE7998FEC53A33BBE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_mount(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xE7E11B8DCBED1058, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_meta_ped_outfits(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x10C70A515BC03707, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_peds_jacker(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9B128DC36C1E04CF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_accuracy(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x37F4AD56ECBC0CD6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_as_group_leader(group_id: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0x5CCE68DBD5FE93EC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_as_group_member(group_id: i32, member_number: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::int(member_number)];
    unsafe { native::invoke(0x51455483CF23ED97, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_blackboard_script_bool(ped: Ped, variable_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x4912DFE492DB98CD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_blackboard_script_float(ped: Ped, variable_name: &str) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0xA29FD00D45311EB7, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_blackboard_script_int(ped: Ped, variable_name: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0xB71B91B398F8F067, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_bone_coords(ped: Ped, bone_id: i32, offset_x: f32, offset_y: f32, offset_z: f32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(bone_id), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z)];
    unsafe { native::invoke(0x17C07FC640E86B4E, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_bone_index(ped: Ped, bone_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(bone_id)];
    unsafe { native::invoke(0x3F428D08BE5AAE31, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_cause_of_death(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x16FFE42AB2D2DC59, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_combat_movement(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xDEA92412FCAEB3F5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_config_flag(ped: Ped, flag_id: i32, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flag_id), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x7EE53118C892B513, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_crouch_movement(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD5FE956C70FF370B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_current_move_blend_ratio(ped: Ped, speed_x: &mut f32, speed_y: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(speed_x), NativeArg::in_out(speed_y)];
    unsafe { native::invoke(0xF60165E1D2C5370B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_defensive_area_position(ped: Ped, p1: bool) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x3C06B8786DD94CD1, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_grapple_state(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2311F15D971AA680, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_group_index(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF162E133B4E7A675, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_is_being_grappled(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3BDFCF25B58B0415, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_is_doing_combat_roll(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC48A9EB0D499B3E5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_is_grappling(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0E99E3BF11BB6367, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_last_damage_bone(ped: Ped, out_bone: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(out_bone)];
    unsafe { native::invoke(0xD75960F6BD9EA49C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_loot_status_mp(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC737697C41628340, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_max_health(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4700A416E8324EF3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_money(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3F69145BBA87BAE7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_motion_focus_entity(ped: Ped) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x243E1B4607040057, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_nearby_peds(ped: Ped, size_and_peds: &mut Any, ignored_ped_type: i32, p3: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(size_and_peds), NativeArg::int(ignored_ped_type), NativeArg::int(p3)];
    unsafe { native::invoke(0x23F8F5FC7E8C4A6B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_nearby_vehicles(ped: Ped, size_and_vehs: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(size_and_vehs)];
    unsafe { native::invoke(0xCFF869CBFA210D82, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_relationship_group_default_hash(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x42FDD0F017B1E38E, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_relationship_group_hash(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7DBDD04862D95F04, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_reset_flag(ped: Ped, flag_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flag_id)];
    unsafe { native::invoke(0xAF9E59B1B1FBF2A0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_source_of_death(ped: Ped) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x93C8B64DEB84728C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_stealth_movement(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7C2AC9CA66575FBF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_time_of_death(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1E98817B311AE98A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_to_player_weapon_damage_modifier(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x936E7CAD0AE2EE14, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_type(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xFF059E1E4C01E63C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_ped_is_following(ped: Ped) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6A3975DEA89F9A17, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_relationship_between_groups(group1: Hash, group2: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(group1), NativeArg::hash(group2)];
    unsafe { native::invoke(0x9E6B70061662AE5C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_relationship_between_peds(ped1: Ped, ped2: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2)];
    unsafe { native::invoke(0xEBA5AD3A0EAF7121, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_seat_ped_is_trying_to_enter(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6F4C85ACD641BCD2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_seat_ped_is_using(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4E76CB57222A00E5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_tracked_ped_pixelcount(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x511F1A683387C7E2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_ped_is_entering(ped: Ped) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF92691AED837A5FC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_ped_is_in(ped: Ped, last_vehicle: bool) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(last_vehicle)];
    unsafe { native::invoke(0x9A9112A0FE9A4713, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_ped_is_using(ped: Ped) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6094AD011A2EA87D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_ped_hash_scenario_prop(ped: Ped, object: Object, conditional_anim: &str, scenario_type: Hash, p4: Hash, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(object), NativeArg::string(conditional_anim), NativeArg::hash(scenario_type), NativeArg::hash(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x2B02DB082258625F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_motion_type_asset_loaded(name_hash: Hash, ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(name_hash), NativeArg::int(ped)];
    unsafe { native::invoke(0x854BC9B1A1CCD034, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn init_ped_default_health(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7DD7FB3480D8083E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn instantly_fill_ped_population() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4759CC730F947C81, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_animal_interaction_possible(ped: Ped, animal: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(animal)];
    unsafe { native::invoke(0xD543D3A8FDE4F185, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_hostile_ped_near_point(ped: Ped, x: f32, y: f32, z: f32, radius: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0x68772DB2B2526F9F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_ped_near_point(x: f32, y: f32, z: f32, radius: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0x083961498679DC9F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_ped_shooting_in_area(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, p6: bool, p7: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::boolean(p6), NativeArg::boolean(p7)];
    unsafe { native::invoke(0xA0D3D71EA1086C55, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_event_in_queue(ped: Ped, event_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(event_type)];
    unsafe { native::invoke(0xC8D523BF5BBD3808, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_group_locally_controlled(group_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0x909AD9E9A92A10DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_instantly_fill_ped_population_finished() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0EE3F0D7FECCC54F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_location_spawn_safe(ped: Ped, p1: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0xFB1E7998B8595825, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_aiming_from_cover(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3998B1276A3300E5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_a_player(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x12534C348C6CB68B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_being_dragged(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xEF3A8772F085B4AA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_being_hogtied(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD453BB601D4A606E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_being_jacked(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9A497FE2DF198913, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_being_stealth_killed(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x863B23EFDE9C5DF2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_being_stunned(ped: Ped, weapon_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_type)];
    unsafe { native::invoke(0x4FBACCE3B4138EE8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_carrying_something(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA911EE21EDF69DAF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_climbing(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x53E8CB4F48BFE623, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_dead_or_dying(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x3317DEDB88C95038, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_defensive_area_active(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xBA63D9FE45412247, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_diving(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5527B8246FEF9B11, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_entering_any_transport(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1D46B417F926D34D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_evasive_diving(ped: Ped, evading_entity: &mut Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(evading_entity)];
    unsafe { native::invoke(0x414641C26E105898, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_facing_ped(ped: Ped, other_ped: Ped, angle: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(other_ped), NativeArg::float(angle)];
    unsafe { native::invoke(0xD71649DB0A545AA3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_falling(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xFB92A102F1C4DFA3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_falling_over(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3E592D0486DEC0F6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_fatally_injured(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD839450756ED5A80, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_fleeing(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBBCCE00B381F8482, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_fully_on_mount(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x95CBC65780DE7EB1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_getting_into_a_vehicle(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBB062B2B5722478E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_going_into_cover(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9F65DBC537E59AD5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_group_member(ped: Ped, group_id: i32, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(group_id), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x9BB01E3834671191, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_hanging_on_to_vehicle(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1C86D8AEF8254B78, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_heading_towards_position(ped: Ped, x: f32, y: f32, z: f32, p4: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p4)];
    unsafe { native::invoke(0xFCF37A457CB96DC0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_headtracking_entity(ped: Ped, entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity)];
    unsafe { native::invoke(0x813A0A7C9D2E831F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_headtracking_ped(ped1: Ped, ped2: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2)];
    unsafe { native::invoke(0x5CD3CB88A7F8850D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_hogtied(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3AA24CCC0D451379, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_hogtying(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x42429C674B61238B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_human(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB980061DA992779D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_incapacitated(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB655DB7582AEC805, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_injured(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x84A2DD9AC37C35C1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_any_boat(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2E0E1C2B4F6CB339, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_any_heli(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x298B91AE825E5705, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_any_plane(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5FFF4CFC74D8FB80, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_any_taxi(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6E575D6A898AB852, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_any_train(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6F972C1AB75A1ED0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_any_vehicle(ped: Ped, at_get_in: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(at_get_in)];
    unsafe { native::invoke(0x997ABD671D25CA0B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_combat(ped: Ped, target: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target)];
    unsafe { native::invoke(0x4859F1FC66A6278E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_cover(ped: Ped, p1: bool, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x60DFD0691A170B88, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_cover_facing_left(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x845333B3150583AB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_flying_vehicle(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9134873537FA419C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_group(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5891CAC5D4ACFF74, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_melee_combat(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4E209B2C1EAD5159, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_model(ped: Ped, model_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(model_hash)];
    unsafe { native::invoke(0x796D90EFB19AA332, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_vehicle(ped: Ped, vehicle: Vehicle, at_get_in: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::boolean(at_get_in)];
    unsafe { native::invoke(0xA3EE4A07279BB9DB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_jacking(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4AE4FF911DFB61DA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_jumping(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xCEDABC5900A0BF97, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_lassoed(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9682F850056C9ADE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_male(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6D9F5FAA7488BA46, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_model(ped: Ped, model_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(model_hash)];
    unsafe { native::invoke(0xC9D55B1A358A5BF7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_on_foot(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x01FEE67DB37F59B2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_on_mount(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x460BC76A0E10655E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_on_specific_vehicle(ped: Ped, vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle)];
    unsafe { native::invoke(0xEC5F66E459AF3BB2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_on_vehicle(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x67722AEB798E5FAB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_opening_door(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x26AF0E8E30BD2A2C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_performing_melee_action(ped: Ped, p1: i32, p2: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0xDCCA191DF9980FD7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_planting_bomb(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC70B5FAE151982D8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_prone(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD6A86331A537A7B9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_ragdoll(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x47E4E977581C5B55, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_ready_to_render(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA0BC8FAED8CFEB3C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_reloading(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x24B100C68C645951, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_responding_to_event(ped: Ped, event_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(event_type)];
    unsafe { native::invoke(0x625B774D75C87068, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_responding_to_threat(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x77525BBF433F2CD6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_running_mobile_phone_task(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2AFE52F782F25775, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_running_ragdoll_task(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xE3B6097CC25AA69E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_shooting(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x34616828CD07F1A1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_sitting(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x84D0BF2B21862059, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_sitting_in_any_vehicle(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x826AA586EDB9FEF8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_sitting_in_vehicle(ped: Ped, vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle)];
    unsafe { native::invoke(0xA808AA1D79230FC2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_stopped(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x530944F6F4B8A214, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_swimming(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9DE327631295B4C2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_swimming_under_water(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC024869A53992F34, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_using_action_mode(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x00E73468D085F745, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_using_any_scenario(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x57AB4A3080F85143, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_using_scenario_hash(ped: Ped, scenario_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(scenario_hash)];
    unsafe { native::invoke(0x34D6AC1157C8226C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_using_this_scenario(ped: Ped, scenario: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(scenario)];
    unsafe { native::invoke(0x9C54041BB66BCF9E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_vaulting(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x117C70D1F5730B5E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_target_ped_in_perception_area(ped: Ped, target_ped: Ped, p2: f32, custom_distance: f32, p4: f32, p5: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::float(p2), NativeArg::float(custom_distance), NativeArg::float(p4), NativeArg::float(p5)];
    unsafe { native::invoke(0x06087579E7AA85A9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_tracked_ped_visible(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x91C8E617F64188AC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn knock_off_ped_prop(ped: Ped, p1: bool, p2: bool, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x6FD7816A36615F48, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn knock_ped_off_vehicle(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x45BBCBA77C29A841, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_cower_in_place(ped: Ped, ped2: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ped2)];
    unsafe { native::invoke(0xF6E1E9F47A7686F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_cower_move_to_point(ped: Ped, p1: f32, p2: f32, p3: f32, ped2: Ped, p5: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::int(ped2), NativeArg::float(p5)];
    unsafe { native::invoke(0x1E4C940233FC0C6F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_hated_targets_around_ped(ped: Ped, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(radius)];
    unsafe { native::invoke(0x9222F300BF8354FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_target(ped: Ped, target_ped: Ped, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x2F25D9AEFA34FBA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_ped_visibility_tracking(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3088634CF8C819CF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_group(group_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0x8EB2F69076AF7053, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_defensive_area(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x74D4E028107450A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_from_group(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xED74007FFB146BC2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_relationship_group(group_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(group_hash)];
    unsafe { native::invoke(0xB6BA2444AB393DA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_scenario_blocking_area(p0: NativeArg, p1: bool) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::boolean(p1)];
    unsafe { native::invoke(0x31D16B74C6E29D66, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_scenario_blocking_areas() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD37401D78A929A49, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_shop_item_from_ped_by_category(ped: Ped, component_category: Hash, p2: i32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component_category), NativeArg::int(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xDF631E4BCE1B1FC4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_tag_from_meta_ped(ped: Ped, component: Hash, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component), NativeArg::int(p2)];
    unsafe { native::invoke(0xD710A5007C2AC539, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ped_use_small_bbox_visibility_tracking(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x75BA1CB3B7D40CAF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ped_vehicle_visibility_tracking(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x2BC338A7B21F4608, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ped_visibility_tracking(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7D7A2E43E74E2EB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ai_weapon_damage_modifier() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEA16670E7BA4743C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_group_formation_default_spacing(group_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0x63DAB4CCB3273205, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_horse_avoidance_level_to_default(horse: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse)];
    unsafe { native::invoke(0x2A5AFD2B8381A6E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ped_in_vehicle_context(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x22EF8FF8778030EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ped_last_vehicle(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBB8DE8CF6A8DD8BB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ped_ragdoll_timer(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9FA4664CF62E47E8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ped_weapon_movement_clipset(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x97B0DB5B4AA74E77, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn resurrect_ped(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x71BC8E838B9C6035, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn revive_injured_ped(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8D8ACD8388CD99CE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ai_melee_weapon_damage_modifier(modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(modifier)];
    unsafe { native::invoke(0x66460DEDDD417254, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ai_weapon_damage_modifier(value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(value)];
    unsafe { native::invoke(0x1B1E2A40A65B8521, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blocking_of_non_temporary_events(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x9F8AA94D6D97DBF4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_blocking_of_non_temporary_events_for_ambient_peds_this_frame(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x9911F4A24485F653, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_combat_float(ped: Ped, combat_type: i32, new_value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(combat_type), NativeArg::float(new_value)];
    unsafe { native::invoke(0xFF41B4B141ED981C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_create_random_cops(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x102E68B2024D536D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_enable_bound_ankles(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC52E0F855C58FC2E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_enable_handcuffs(ped: Ped, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xDF1AF8B5D56542FA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_facial_idle_anim_override(ped: Ped, anim_name: &str, anim_dict: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(anim_name), NativeArg::string(anim_dict)];
    unsafe { native::invoke(0xFFC24B988B938B38, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_formation_positions_target_radius(group_id: i32, radius: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::float(radius)];
    unsafe { native::invoke(0x7CC7D3B7AF7FB71F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_group_formation(group_id: i32, formation_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::int(formation_type)];
    unsafe { native::invoke(0xCE2F5FC3AF7E8C1E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_group_formation_spacing(group_id: i32, p1: f32, p2: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0x1D9D45004C28C916, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_group_separation_range(group_id: i32, separation_range: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::float(separation_range)];
    unsafe { native::invoke(0x4102C7858CFEE4E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_horse_avoidance_level(horse: Ped, avoidance_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse), NativeArg::int(avoidance_level)];
    unsafe { native::invoke(0xDDCF6FEA5D7ACC17, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ik_target(ped: Ped, ik_index: i32, entity_look_at: Entity, bone_look_at: i32, offset_x: f32, offset_y: f32, offset_z: f32, p7: NativeArg, blend_in_duration: i32, blend_out_duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ik_index), NativeArg::int(entity_look_at), NativeArg::int(bone_look_at), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), p7, NativeArg::int(blend_in_duration), NativeArg::int(blend_out_duration)];
    unsafe { native::invoke(0xC32779C16FCEECD9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_looting_flag(ped: Ped, loot_flag: i32, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(loot_flag), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x6569F31A01B4C097, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pause_ped_writhe_bleedout(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x925A160133003AC6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_accuracy(ped: Ped, accuracy: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(accuracy)];
    unsafe { native::invoke(0x7AEFB85C1D49DEB6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_as_cop(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xBB03C38DD3FB7FFD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_as_group_leader(ped: Ped, group_id: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(group_id), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x2A7819605465FBCE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_as_group_member(ped: Ped, group_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(group_id)];
    unsafe { native::invoke(0x9F3480FE65DB31B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_arm_ik(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6C3B4D6D13B4C841, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_be_incapacitated(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x5240864E847C691C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_be_knocked_off_vehicle(ped: Ped, state: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(state)];
    unsafe { native::invoke(0x7A6535691B477C48, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_be_targetted(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x63F58F7C80513AAD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_be_targetted_by_player(ped: Ped, player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x66B57B72E0836A76, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_be_targetted_by_team(ped: Ped, team: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(team), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xBF1CA77833E58F2C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_head_ik(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC11C18092C5530DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_leg_ik(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x73518ECE2485412B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_play_ambient_anims(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6373D1349925A70E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_play_ambient_base_anims(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x0EB0585D15254740, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_play_gesture_anims(ped: Ped, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2];
    unsafe { native::invoke(0xBAF20C5432058024, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_ragdoll(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xB128377056A54E2A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_ragdoll_from_player_impact(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xDF993EE5E90ABA25, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_teleport_to_group_leader(ped_handle: Ped, group_id: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped_handle), NativeArg::int(group_id), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x2E2F4240B3F24647, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_torso_ik(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xF2B7106D37947CE0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_torso_react_ik(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xF5846EDB26A98A24, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_torso_vehicle_ik(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6647C5F6F5792496, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_use_auto_conversation_lookat(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xEC4686EC06434678, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_capsule(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x364DF566EC833DE2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_cloth_pin_frames(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x78C4E9961DB3EB5B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_combat_ability(ped: Ped, ability_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ability_level)];
    unsafe { native::invoke(0xC7622C0D36B2FDA8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_combat_attributes(ped: Ped, attribute_index: i32, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x9F7794730795E019, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_combat_movement(ped: Ped, combat_movement: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(combat_movement)];
    unsafe { native::invoke(0x4D9CA1009AFBD057, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_combat_range(ped: Ped, range: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(range)];
    unsafe { native::invoke(0x3C606747B23E497B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_config_flag(ped: Ped, flag_id: i32, value: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flag_id), NativeArg::boolean(value)];
    unsafe { native::invoke(0x1913FE4CBF41C463, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_defensive_area_direction(ped: Ped, p1: f32, p2: f32, p3: f32, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x413C6C763A4AFFAD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_defensive_area_volume(ped: Ped, volume: Volume, p2: bool, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(volume), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xFC3DB99C8144CD81, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_desired_heading(ped: Ped, heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(heading)];
    unsafe { native::invoke(0xAA5A7ECE2AA8FE70, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_firing_pattern(ped: Ped, pattern_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(pattern_hash)];
    unsafe { native::invoke(0x9AC577F5A12AD8A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_flee_attributes(ped: Ped, attribute_flags: i32, enable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_flags), NativeArg::boolean(enable)];
    unsafe { native::invoke(0x70A2D1137C8ED7C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_gesture_group(ped: Ped, gesture: &str, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(gesture), NativeArg::int(p2)];
    unsafe { native::invoke(0xDDF803377F94AAA8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_gravity(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x9FF447B6B6AD960A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_group_member_passenger_index(ped: Ped, index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(index)];
    unsafe { native::invoke(0x0BDDB8D9EC6BCF3C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_hearing_range(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x33A8F7F7D5F7F33C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_highly_perceptive(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x52D59AB61DDC05DD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_id_range(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0xF107E836A70DCE05, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_injured_on_ground_behaviour(ped: Ped, unk: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(unk)];
    unsafe { native::invoke(0xEC4B4B3B9908052A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_into_vehicle(ped: Ped, vehicle: Vehicle, seat_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::int(seat_index)];
    unsafe { native::invoke(0xF75B0D629E1C063D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_keep_task(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x971D38760FBC02EF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_lasso_hogtie_flag(ped: Ped, flag_id: i32, value: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flag_id), NativeArg::boolean(value)];
    unsafe { native::invoke(0xAE6004120C18DF97, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_leg_ik_mode(ped: Ped, mode: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(mode)];
    unsafe { native::invoke(0xC396F5B86FF9FEBD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_lod_multiplier(ped: Ped, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(multiplier)];
    unsafe { native::invoke(0xDC2C5C242AAC342B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_max_health(ped: Ped, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(value)];
    unsafe { native::invoke(0xF5F6378C4F3419D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_max_move_blend_ratio(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x433083750C5E064A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_max_time_in_water(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x43C851690662113D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_max_time_underwater(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x6BA428C528D9E522, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_min_move_blend_ratio(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x01A898D26E2333DD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_model_is_suppressed(model: Hash, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE163A4BCE4DE6F11, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_money(ped: Ped, amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(amount)];
    unsafe { native::invoke(0xA9C8960E8684C1B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_move_anims_blend_out(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9E8C908F41584ECD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_move_rate_override(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x085BF80FA50A39D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_name_debug(ped: Ped, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(name)];
    unsafe { native::invoke(0x98EFA132A4117BE1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_non_creation_area(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2)];
    unsafe { native::invoke(0xEE01041D559983EA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_onto_mount(ped: Ped, mount: Ped, seat_index: i32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(mount), NativeArg::int(seat_index), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x028F76B6E78246EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_owns_animal(ped: Ped, animal: Ped, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(animal), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x931B241409216C1F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_panic_exit_scenario(ped: Ped, x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xFE07FF6495D52E2A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_ragdoll_force_fall(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x01F6594B923B9251, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_ragdoll_on_collision(ped: Ped, toggle: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xF0A4F1BBF4FA7497, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_random_component_variation(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xC8A9481A01E63C28, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_relationship_group_default_hash(ped: Ped, hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(hash)];
    unsafe { native::invoke(0xADB3F206518799E8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_relationship_group_hash(ped: Ped, relationship_group: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(relationship_group)];
    unsafe { native::invoke(0xC80A74AC829DDD92, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_reset_flag(ped: Ped, flag_id: i32, do_reset: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flag_id), NativeArg::boolean(do_reset)];
    unsafe { native::invoke(0xC1E8A365BF3B29F2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_seeing_range(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0xF29CF591C4BF6CEE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_shoot_rate(ped: Ped, shoot_rate: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(shoot_rate)];
    unsafe { native::invoke(0x614DA022990752DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_should_play_combat_scenario_exit(ped: Ped, x: f32, y: f32, z: f32, look_intensity: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(look_intensity)];
    unsafe { native::invoke(0x802092B07E3B1EEA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_should_play_directed_normal_scenario_exit(ped: Ped, x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xEC6935EBE0847B90, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_should_play_emotional_scenario_exit(ped: Ped, x: f32, y: f32, z: f32, look_intensity: i32, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(look_intensity), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x62FDAD5E01D2DD47, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_should_play_flee_scenario_exit(ped: Ped, x: f32, y: f32, z: f32, look_intensity: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(look_intensity)];
    unsafe { native::invoke(0xEEED8FAFEC331A70, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_should_play_immediate_scenario_exit(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF1C03A5352243A30, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_should_play_normal_scenario_exit(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA3A9299C4F2ADB98, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_should_play_quick_scenario_exit(ped: Ped, x: f32, y: f32, z: f32, look_intensity: i32, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(look_intensity), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x463803429297117C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_sphere_defensive_area(ped: Ped, x: f32, y: f32, z: f32, radius: f32, p5: bool, p6: bool, p7: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::boolean(p5), NativeArg::boolean(p6), NativeArg::boolean(p7)];
    unsafe { native::invoke(0x9D3151A373974804, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_stealth_movement(ped: Ped, toggle: bool, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle), p2, p3];
    unsafe { native::invoke(0x88CBB5CEB96B7BD2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_sweat(ped: Ped, sweat: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(sweat)];
    unsafe { native::invoke(0x27B0405F59637D1F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_target_loss_response(ped: Ped, response_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(response_type)];
    unsafe { native::invoke(0x0703B9079823DA4A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_to_inform_respected_friends(ped: Ped, radius: f32, max_friends: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(radius), NativeArg::int(max_friends)];
    unsafe { native::invoke(0x112942C6E708F70B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_to_player_weapon_damage_modifier(ped: Ped, damage_modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(damage_modifier)];
    unsafe { native::invoke(0xD77AE48611B7B10A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_to_ragdoll(ped: Ped, time_min: i32, time_max: i32, ragdoll_type: i32, abort_if_injured: bool, abort_if_dead: bool, nm_task_message_parameter_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(time_min), NativeArg::int(time_max), NativeArg::int(ragdoll_type), NativeArg::boolean(abort_if_injured), NativeArg::boolean(abort_if_dead), NativeArg::string(nm_task_message_parameter_name)];
    unsafe { native::invoke(0xAE99FB955581844A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_to_ragdoll_with_fall(ped: Ped, time_min: i32, time_max: i32, ragdoll_type: i32, falldir_x: f32, falldir_y: f32, falldir_z: f32, p7: f32, p8: f32, p9: f32, p10: f32, p11: f32, p12: f32, p13: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(time_min), NativeArg::int(time_max), NativeArg::int(ragdoll_type), NativeArg::float(falldir_x), NativeArg::float(falldir_y), NativeArg::float(falldir_z), NativeArg::float(p7), NativeArg::float(p8), NativeArg::float(p9), NativeArg::float(p10), NativeArg::float(p11), NativeArg::float(p12), NativeArg::float(p13)];
    unsafe { native::invoke(0xD76632D99E4966C8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_using_action_mode(ped: Ped, b_action_mode_enabled: bool, p2: i32, action: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(b_action_mode_enabled), NativeArg::int(p2), NativeArg::string(action)];
    unsafe { native::invoke(0xD75ACCF5E0FB5367, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_visual_field_center_angle(ped: Ped, angle: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(angle)];
    unsafe { native::invoke(0x3B6405E8AB34A907, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_visual_field_max_angle(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x70793BDCA1E854D4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_visual_field_min_angle(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x2DB492222FB21E26, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_visual_field_peripheral_range(ped: Ped, range: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(range)];
    unsafe { native::invoke(0x9C74B0BC831B753A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_wetness_enabled_this_frame(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB5485E4907B53019, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_wetness_height(ped: Ped, height: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(height)];
    unsafe { native::invoke(0x44CB6447D2571AA0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pop_control_sphere_this_frame(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xD8C3BE3EE94CAF2D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ragdoll_blocking_flags(ped: Ped, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flags)];
    unsafe { native::invoke(0x26695EC767728D84, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_relationship_between_groups(relationship: i32, group1: Hash, group2: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(relationship), NativeArg::hash(group1), NativeArg::hash(group2)];
    unsafe { native::invoke(0xBF25EB89375A37AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_ped_density_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0x7A556143A1C03898, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_cancel_search() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFEE4A5459472A9F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_get_num_search_results() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA635C11B8C44AFC2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_get_search_result(random_int: i32, x: &mut f32, y: &mut Any, z: &mut f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(random_int), NativeArg::in_out(x), NativeArg::in_out(y), NativeArg::in_out(z)];
    unsafe { native::invoke(0x280C7E3AC7F56E90, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_get_search_result_flags(p0: NativeArg, p1: &mut Any) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::in_out(p1)];
    unsafe { native::invoke(0xB782F8238512BAD5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_is_search_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3C67506996001F5E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_is_search_complete() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA586FBEB32A53DBB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_is_search_failed() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF445DE8DA80A1792, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_start_search(x: f32, y: f32, z: f32, width: f32, p4: f32, spawnpoints_flag: i32, p6: f32, duration: i32, p8: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(width), NativeArg::float(p4), NativeArg::int(spawnpoints_flag), NativeArg::float(p6), NativeArg::int(duration), NativeArg::float(p8)];
    unsafe { native::invoke(0x2DF9038C90AD5264, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_start_search_in_angled_area(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, width: f32, spawnpoints_flag: i32, p8: f32, duration: i32, p10: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::float(width), NativeArg::int(spawnpoints_flag), NativeArg::float(p8), NativeArg::int(duration), NativeArg::float(p10)];
    unsafe { native::invoke(0xB2AFF10216DEFA2F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn special_function_do_not_use(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xF9ACF4A08098EA25, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn time_since_ped_last_shot(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x285D36C5C72B0569, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn toggle_scenario_ped_cower_in_place(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x9A77DFD295E29B09, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn was_ped_skeleton_updated(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x11B499C1E0FF8559, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x00_b380_ff2_df6_ab7_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x00B380FF2DF6AB7A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x024_ec9_b649111915(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x024EC9B649111915, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x02_e741_e19_e39628_c(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x02E741E19E39628C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0455546_f23_ff08_e4(group_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0x0455546F23FF08E4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x06_a10_b4_d7_f50_b0_c3(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x06A10B4D7F50B0C3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x070_a3841406_c43_d5(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x070A3841406C43D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x07_ea5_b053_fa60_ac7(group_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x07EA5B053FA60AC7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x09171_a6_f8_fde5_dc1(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x09171A6F8FDE5DC1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x095_c2277_fed731_db(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x095C2277FED731DB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x09_d7_afd3716_da8_e1(ped: Ped, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x09D7AFD3716DA8E1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_a4618_ffd517_e24_d(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0A4618FFD517E24D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_ada3_ec589_e1736_e() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0ADA3EC589E1736E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_b787_a37_eedd226_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0B787A37EEDD226F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_d3_b1568917_ebda0(ped: Ped, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x0D3B1568917EBDA0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_d497_aa69059_fe40(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0D497AA69059FE40, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_eef7_a81_c17679_db(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0EEF7A81C17679DB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_f967019_cc853_bcc(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0F967019CC853BCC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_fb1_ba7_ff73_b41_e1(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x0FB1BA7FF73B41E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_ffdf937_e5_c11382(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x0FFDF937E5C11382, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x101_b45_c5_f56_d970_f(p0: &mut Any, ped: Ped, damage_cleanliness: i32, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::int(ped), NativeArg::int(damage_cleanliness), NativeArg::int(p3)];
    unsafe { native::invoke(0x101B45C5F56D970F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x10_f96086123_b939_f(legendary_ped: Ped, prey_ped: Ped, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(legendary_ped), NativeArg::int(prey_ped), NativeArg::float(p2)];
    unsafe { native::invoke(0x10F96086123B939F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1148_f706_cf4_ebdda(ped: Ped, p1: Hash, p2: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x1148F706CF4EBDDA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1298_b3_d8_e4_c2409_f(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1298B3D8E4C2409F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x12_eb4_e31_f092_c9_b3(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x12EB4E31F092C9B3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x12_f2_d161_bf4031_fc(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x12F2D161BF4031FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x134775_b093_ad5_c38(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x134775B093AD5C38, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x154_b7_e841_ac7412_f(group_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x154B7E841AC7412F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x15_f4732_c357_b1_d6_d(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x15F4732C357B1D6D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x16802_c32_b2_fca06_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x16802C32B2FCA06B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x16_f798_a05_bb9_e3_b5(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x16F798A05BB9E3B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_d23_d3_f70606_d788(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x1D23D3F70606D788, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_d4636_c90_bbefacb(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x1D4636C90BBEFACB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_f44_b7_e283_c09_ede(ped: Ped, p1: f32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x1F44B7E283C09EDE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_f8215_d0_e446_f593(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x1F8215D0E446F593, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2371_c39_d4_f91_c288(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2371C39D4F91C288, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x23_bde06596_a22_cec(ped: Ped, p1: i32, p2: f32, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::float(p2), p3];
    unsafe { native::invoke(0x23BDE06596A22CEC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x242_edf85_d4_e87_b65(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x242EDF85D4E87B65, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x256_edd55_c6_be1482(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x256EDD55C6BE1482, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x273915_ce30780986(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x273915CE30780986, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x27_e8_a84_c12_b0_b7_d1(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x27E8A84C12B0B7D1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x28508173_c6_a7_cc18(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x28508173C6A7CC18, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x290_b2_e6_ccde532_e1(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x290B2E6CCDE532E1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x29924_eb8_ee9_db926(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x29924EB8EE9DB926, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x29_f3539189_d3_e277(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x29F3539189D3E277, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_ba9_d7_bf629_f920_c(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2BA9D7BF629F920C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_d976_dbdc731_df80(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2D976DBDC731DF80, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_dc0_e8_dcbd3546_e9(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2DC0E8DCBD3546E9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_dd4_e0_e26_dfad97_d(ped1: Ped, ped2: Ped, p2: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2), NativeArg::float(p2)];
    unsafe { native::invoke(0x2DD4E0E26DFAD97D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_e5_b5_d1_f1453_e08_e(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x2E5B5D1F1453E08E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_fa568_bfa725_f8_d6(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x2FA568BFA725F8D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x31_b2_e7_f2_e3_c58_b89(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x31B2E7F2E3C58B89, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x32417_cb860_a3_bdc4(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x32417CB860A3BDC4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x326_f7951_ef0_d7_f75(ped: Ped, event_type: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(event_type)];
    unsafe { native::invoke(0x326F7951EF0D7F75, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x329772_c47_dbb2_fbc(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x329772C47DBB2FBC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x32_ccad8_a981_b53_d3(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x32CCAD8A981B53D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x32_ceda9_a0_ab4_cef7(ped: Ped, p1: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x32CEDA9A0AB4CEF7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x34_b5_ceac180_a5_d6_e(ped: Ped, p1: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x34B5CEAC180A5D6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x34_c11114887150_fd(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x34C11114887150FD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x34_eddd59364_ad74_a(ped: Ped, p1: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x34EDDD59364AD74A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x354_ca4_dddeec397_a(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x354CA4DDDEEC397A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x370_a973252741_ac4(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x370A973252741AC4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_a5697_b80_fed5_ebe(ped: Ped, p1: f32, p2: f32, p3: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4)];
    unsafe { native::invoke(0x3A5697B80FED5EBE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_acce14_dfa6_ba8_c2(ped: Ped, p1: i32, x: f32, y: f32, z: f32, p5: f32, itemset: ItemSet) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p5), NativeArg::int(itemset)];
    unsafe { native::invoke(0x3ACCE14DFA6BA8C2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_aec4_a410_ecaf30_d(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3AEC4A410ECAF30D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_c529_a827998_f9_b3(ped: Ped, p1: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x3C529A827998F9B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_efed081_b4834_ba1(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3EFED081B4834BA1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_fcbb5_fcfd968698(drawable: Hash, albedo: Hash, normal: Hash, material: Hash, p4: NativeArg) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(drawable), NativeArg::hash(albedo), NativeArg::hash(normal), NativeArg::hash(material), p4];
    unsafe { native::invoke(0x3FCBB5FCFD968698, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_fdbb99_efd8_ce4_af(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x3FDBB99EFD8CE4AF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_fdcc1_f8_c17_e303_e(ped: Ped, p1: i32, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0x3FDCC1F8C17E303E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x405180_b14_da5_a935(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x405180B14DA5A935, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x40_c3524_d4_ed83554(group_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x40C3524D4ED83554, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x40_c9155_af8_bc13_f3(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x40C9155AF8BC13F3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x413697_ec260_aabbf(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x413697EC260AABBF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x41_c23_a8_e6_b344867(ped: Ped, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(p1)];
    unsafe { native::invoke(0x41C23A8E6B344867, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x45_fea6_d5539_bd474(ped: Ped, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(p1)];
    unsafe { native::invoke(0x45FEA6D5539BD474, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x49_dadfc4_cd808_b0_a(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x49DADFC4CD808B0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_b19_f171450_e0_d4_f(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4B19F171450E0D4F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_e68_c7_ef706_df35_d(ped: Ped, x: f32, y: f32, z: f32, p4: f32, relationship_group: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p4), NativeArg::hash(relationship_group)];
    unsafe { native::invoke(0x4E68C7EF706DF35D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_ec4_ea2_f72_b36358(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x4EC4EA2F72B36358, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_f27603_e44_a8_e4_c0(ped: Ped, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x4F27603E44A8E4C0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_f63433_ce3_c08230(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x4F63433CE3C08230, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5203038_ff8_bae577(ped: Ped, p1: i32, p2: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x5203038FF8BAE577, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x52250_b92_ea70_be3_d(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x52250B92EA70BE3D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x52_a24_d8_a1_da89658(ped: Ped, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x52A24D8A1DA89658, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x53_ba7_d96_b9_a421_d9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x53BA7D96B9A421D9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x54_d3_cd482742_c482(animal: Ped, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::float(p2)];
    unsafe { native::invoke(0x54D3CD482742C482, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x550_cb89_dd7_f4_fa3_d(ped1: Ped, ped2: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2)];
    unsafe { native::invoke(0x550CB89DD7F4FA3D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x55546004_a244302_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x55546004A244302A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x56076667_e7_c2_dcd6(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x56076667E7C2DCD6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x56_e4_bad93_d33453_c(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x56E4BAD93D33453C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x577_c60_ba06_d0_ea64(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x577C60BA06D0EA64, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x57_f35552_e771_be9_d(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x57F35552E771BE9D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_a1_a929_c8_b729_b4_a(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5A1A929C8B729B4A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_af24_ca9_c974_e51_a(ped1: Ped, ped2: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2)];
    unsafe { native::invoke(0x5AF24CA9C974E51A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_b73975_b4_f12_f7_f3(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x5B73975B4F12F7F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_bb04_bc74_a474_b47(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x5BB04BC74A474B47, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_bf0_b9_d9_a8_e227_a0(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5BF0B9D9A8E227A0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_c6_c7_c70_ca302801(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5C6C7C70CA302801, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_c90_e20_c25_e6_d83_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5C90E20C25E6D83C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_ca20_fbe49891_bbd(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x5CA20FBE49891BBD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_cb2_ebb467_be3_ed6(animal: Ped, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::float(p2)];
    unsafe { native::invoke(0x5CB2EBB467BE3ED6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_d4_cd22_a8_c82_a81_a(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x5D4CD22A8C82A81A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_da36_cccb63_c0895(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x5DA36CCCB63C0895, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_e9_faf6_c513347_b4(ped: Ped, event_type: Hash) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(event_type)];
    unsafe { native::invoke(0x5E9FAF6C513347B4, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_efa8_a3_d8_a60_d662(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x5EFA8A3D8A60D662, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_fcf25_d584065_bfd(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x5FCF25D584065BFD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x600_bbdd29820370_c(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x600BBDD29820370C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x604_e1010_e3162_e86(ped: Ped, prop_item_id: &str, p2: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(prop_item_id), NativeArg::string(p2)];
    unsafe { native::invoke(0x604E1010E3162E86, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x606_d529_dada3_c940(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0x606D529DADA3C940, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x62_fdf4_e678_e40_cc6(entity: Entity, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(entity), p1];
    unsafe { native::invoke(0x62FDF4E678E40CC6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x633_f83_b301_c87994(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x633F83B301C87994, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x642720_d8_d69328_b6(ped: Ped, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0x642720D8D69328B6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6507_ac3_bd7_c99009(x: f32, y: f32, z: f32, p3: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3)];
    unsafe { native::invoke(0x6507AC3BD7C99009, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6734_f0_a6_a52_c371_c(player: Player, horse_slot: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(horse_slot)];
    unsafe { native::invoke(0x6734F0A6A52C371C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_a190_b94_c2541_a99(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6A190B94C2541A99, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_a489892_e813951_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6A489892E813951A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_e8_b87139854022_d(ped: Ped, clipset: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(clipset)];
    unsafe { native::invoke(0x6E8B87139854022D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_f46_f8_acb44_c4_fc1(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6F46F8ACB44C4FC1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7020839_c7302_d8_ac(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7020839C7302D8AC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x704_c908_e9_c405136(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x704C908E9C405136, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x712_b2_c2_b2471_b493(ped: Ped, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0x712B2C2B2471B493, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x735662994_e60_a710(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x735662994E60A710, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7406_c71_f4_ac2_ffcc(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7406C71F4AC2FFCC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x758_f081_db204_ddde(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x758F081DB204DDDE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x75_a082563_b4452_e5(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x75A082563B4452E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x75_d3333409_cd33_ce(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x75D3333409CD33CE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x763_fa8_a9_d76_ee3_a7(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x763FA8A9D76EE3A7, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x77243_ed4_f7_caaa55(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x77243ED4F7CAAA55, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_abbd9_e449_e0_db00(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x7ABBD9E449E0DB00, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_b5_c293238_ee4_f20(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7B5C293238EE4F20, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_bb810_e8_b343_ac7_b(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7BB810E8B343AC7B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_c08_e7_cb8_d951_b70(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x7C08E7CB8D951B70, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_c10221_ce718_aa72(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x7C10221CE718AA72, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_e5185_b979706210(group_id: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::int(p1)];
    unsafe { native::invoke(0x7E5185B979706210, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_e8_f9949_b7_aabbf0(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x7E8F9949B7AABBF0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_edb3_c766_b0_d073_f(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7EDB3C766B0D073F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_ee3_a8660_f38797_e(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7EE3A8660F38797E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_f090958_ae95_b61_b(ped: Ped, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x7F090958AE95B61B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8101_ba1_c0_b462412(ped: Ped, rope_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(rope_id)];
    unsafe { native::invoke(0x8101BA1C0B462412, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x815_c0074_a1_bc0_d93(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x815C0074A1BC0D93, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x82_cb0_f3_f0_c7785_e5(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x82CB0F3F0C7785E5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x851966_e1_e35_af491(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x851966E1E35AF491, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x85_f500_f4_e24_ca43_e(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x85F500F4E24CA43E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x86_f0_b6730_c32_ac14(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x86F0B6730C32AC14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x86_fafc18_e3_d4380_c(group_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x86FAFC18E3D4380C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x878_e8104_fa27_cdae(vehicle: Vehicle, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::hash(p1)];
    unsafe { native::invoke(0x878E8104FA27CDAE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x87_c2724_a56_f66020(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x87C2724A56F66020, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8822_f124788_b8_d0_a(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x8822F124788B8D0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8822_f139408_b8_d0_a(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8822F139408B8D0A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x88_a95_bb640_fc186_f(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x88A95BB640FC186F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x88_b2026_a3_b0_be33_d(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x88B2026A3B0BE33D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x897934_e868_eddd6_c(ped: Ped, p1: i32, p2: f32, p3: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4)];
    unsafe { native::invoke(0x897934E868EDDD6C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x89816_b58_c3466262(ped: Ped) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x89816B58C3466262, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x899_dfa0009_ac93_de(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x899DFA0009AC93DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x89_e59_dbd15_e21177(group_id: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::int(p1)];
    unsafe { native::invoke(0x89E59DBD15E21177, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_af46_e5159_a5_b620(ped: Ped, speech_params: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(speech_params)];
    unsafe { native::invoke(0x8AF46E5159A5B620, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_af8_e647_d6_b2_a649(group_id: i32, ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::int(ped)];
    unsafe { native::invoke(0x8AF8E647D6B2A649, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_afccc0_f18_d70018(group_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x8AFCCC0F18D70018, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_ba0_c65_ac15_a7_d33(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x8BA0C65AC15A7D33, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_be24_d74_d74_c6_e9_b(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8BE24D74D74C6E9B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_cb2553_c559102_c1(ped: Ped, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x8CB2553C559102C1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_d9_db115_fba8_e23_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x8D9DB115FBA8E23D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x900_ca00_ce703_e1_e2(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x900CA00CE703E1E2, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9078_fb0557364099(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9078FB0557364099, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x913_d04_a5176_f84_c9(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x913D04A5176F84C9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9184788_bff1_edad7(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9184788BFF1EDAD7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x91_bab9_e064_f036_cd(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x91BAB9E064F036CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x92_a1_b55_a59720395(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x92A1B55A59720395, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x94132_d7_c8_d3575_c4(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x94132D7C8D3575C4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9629_faf6460_d35_cb(group: Hash, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(group), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x9629FAF6460D35CB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x96595_b36_d6_a2279_b(animal: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x96595B36D6A2279B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x966_de09688_a1_de39(group_id: i32, p1: f32, p2: f32, p3: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4)];
    unsafe { native::invoke(0x966DE09688A1DE39, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x96_c7_b659854_de629(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x96C7B659854DE629, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x97_a38_b65_ebda3_d50(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x97A38B65EBDA3D50, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x97_b06669_ac569003(ped1: Ped, ped2: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2)];
    unsafe { native::invoke(0x97B06669AC569003, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x97_c475212_b327666(group_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x97C475212B327666, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9851_de7_aec10_b4_e1(x: f32, y: f32, z: f32, p3: f32, p4: i32, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p3), NativeArg::int(p4), p5];
    unsafe { native::invoke(0x9851DE7AEC10B4E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x992187_d975635_df5(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x992187D975635DF5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x99_df2639_da76_c1_dc(ped1: Ped, ped2: Ped, p2: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2), NativeArg::int(p2)];
    unsafe { native::invoke(0x99DF2639DA76C1DC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_a4_ac116_cc1_eee14(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9A4AC116CC1EEE14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_ab33_cb5834885_b3(ped: Ped, p1: f32, p2: f32, p3: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4)];
    unsafe { native::invoke(0x9AB33CB5834885B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_b65444_c07_b782_bf(ped: Ped, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(p1)];
    unsafe { native::invoke(0x9B65444C07B782BF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_b9_b9_fa0_ea283_e3_d(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x9B9B9FA0EA283E3D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_bbeaf8_b0_c007_f1_e(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x9BBEAF8B0C007F1E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_d8_dfe2_de9_cb4_dfc(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9D8DFE2DE9CB4DFC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_e66708_b2_b41_f14_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9E66708B2B41F14A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_f0_f28_b42_c4_ee80_a(animal: Ped, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::float(p2)];
    unsafe { native::invoke(0x9F0F28B42C4EE80A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_f933_e0985_e12_c51(ped: Ped, p1: f32, p2: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0x9F933E0985E12C51, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a064_bbabb064446_f(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA064BBABB064446F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a180_fbd502_a03125(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xA180FBD502A03125, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a1_fbac56_d38563_e2(volume: Volume) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0xA1FBAC56D38563E2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2116_c1_e4_ed85_c24(ped: Ped, inverted: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(inverted)];
    unsafe { native::invoke(0xA2116C1E4ED85C24, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a218_d2_bbcaa7388_c(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA218D2BBCAA7388C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a274_f51_ef7_e34_b95(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA274F51EF7E34B95, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2_b8_e47442_c76_cec(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA2B8E47442C76CEC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2_f8_b3_b5_fedfc100(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA2F8B3B5FEDFC100, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a31_d350_d66_fa1855(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA31D350D66FA1855, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a405_bf9_f01960_c16(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA405BF9F01960C16, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a4_ac05_b1_a364_ebc5(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xA4AC05B1A364EBC5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a4_b6432_e3880_f2_f9(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA4B6432E3880F2F9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a6_d6_f03095_c88_f59(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA6D6F03095C88F59, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a7_a806677_f8_de138(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA7A806677F8DE138, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a7_dc9266_ed6_a4_e51(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA7DC9266ED6A4E51, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a8_a95_cecb1906_ea2(group_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xA8A95CECB1906EA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a90684_ed185_ccb4_b(animal: Ped, p1: bool, p2: f32, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(animal), NativeArg::boolean(p1), NativeArg::float(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0xA90684ED185CCB4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a967_d6_a8_ed2_d713_b(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xA967D6A8ED2D713B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_aa6_c49_ae90_a32299(ped: Ped, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0xAA6C49AE90A32299, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_aac0_ee3_b4999_abb5(ped: Ped, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped)];
    unsafe { native::invoke(0xAAC0EE3B4999ABB5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ad3330_e3_c3_e98007(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xAD3330E3C3E98007, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ae6_b68_a83_abbe7_c0(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xAE6B68A83ABBE7C0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_af041_c10756_c30_fb(ped: Ped, p1: bool, p2: bool, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xAF041C10756C30FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b05_cc690_cde8_a4_a9(group_id: i32, p1: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::float(p1)];
    unsafe { native::invoke(0xB05CC690CDE8A4A9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b06_f5_f1_def417216(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xB06F5F1DEF417216, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b4_b7_c92_fce7347_b7(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB4B7C92FCE7347B7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b65927_f861_e7_ae39(ped: Ped, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xB65927F861E7AE39, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b8_e2_d655_e1_d5_bd39(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xB8E2D655E1D5BD39, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b91_ab3_be7_f655_d49(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB91AB3BE7F655D49, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b9_bdfae609_dfb7_c5(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xB9BDFAE609DFB7C5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bb3_e5370_ebb6_be28(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xBB3E5370EBB6BE28, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bc1_dc48270468444(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xBC1DC48270468444, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bd0_e4_f52_f6_d95242(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBD0E4F52F6D95242, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bf567_df2_bef211_a6(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xBF567DF2BEF211A6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bfa6_b7731_c3_baf02() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBFA6B7731C3BAF02, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c17_a94_cc8_fc3_c61_a(entity: Entity, bone_id: i32, p2: f32, p3: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(bone_id), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4)];
    unsafe { native::invoke(0xC17A94CC8FC3C61A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c2722_b252_c79_e641(ped: Ped, p1: NativeArg, p2: NativeArg, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, NativeArg::boolean(p3)];
    unsafe { native::invoke(0xC2722B252C79E641, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c2_ef407645_beecdc(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC2EF407645BEECDC, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c48_af420371_c7407(ped: Ped, grapple: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(grapple)];
    unsafe { native::invoke(0xC48AF420371C7407, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c5_b78_e41_dcf8227_c(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC5B78E41DCF8227C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c6136_b40_fffb778_b(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xC6136B40FFFB778B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c6981_aff6_d2_a71_c2(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC6981AFF6D2A71C2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c6_c4_e15_cf7_d52_fea(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC6C4E15CF7D52FEA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c9151483_cc06_a414(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC9151483CC06A414, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c991_ef46_fe323867(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0xC991EF46FE323867, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c99_f104_bdf8_c7_f5_a(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC99F104BDF8C7F5A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ca95_c156_c14_b2054(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCA95C156C14B2054, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cac43_d060099_ea72(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xCAC43D060099EA72, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cb1_a3864_c524_f784(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCB1A3864C524F784, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cb86_d3_e3_e3708901(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xCB86D3E3E3708901, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cb8_f4_c9343_ebe240(ped: Ped, event_type: Hash, coords: &mut Vector3) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(event_type), NativeArg::in_out(coords)];
    unsafe { native::invoke(0xCB8F4C9343EBE240, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cbde59_c48_f2_b06_f5(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xCBDE59C48F2B06F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cd9_e5_f94_a2_f38683(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xCD9E5F94A2F38683, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cdfb8_c04_d4_c95_d9_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xCDFB8C04D4C95D9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cf0_b19806473_d324(ped: Ped, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xCF0B19806473D324, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d049920_cd29_f6_cc8(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xD049920CD29F6CC8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d049_fdaf089_fddb0(ped: Ped, p1: Hash, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0xD049FDAF089FDDB0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d103_f6_dbb5442_be8(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xD103F6DBB5442BE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d2_f0_fe8805_d91647(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD2F0FE8805D91647, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d355_e2_f1_bb41087_e(ped: Ped, p1: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0xD355E2F1BB41087E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d4_d403_ea031_f351_c(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD4D403EA031F351C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d55_db4466_d00_a258(legendary_animal: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(legendary_animal)];
    unsafe { native::invoke(0xD55DB4466D00A258, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d5_bd1_b5318_a81994(group_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xD5BD1B5318A81994, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d61_fcf9_fcfd515_b7(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xD61FCF9FCFD515B7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d7_d2_f45_c56_a4_f4_df(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xD7D2F45C56A4F4DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d8544_f6260_f5_f01_e(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xD8544F6260F5F01E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d8_ceeed54_c672_b5_d(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xD8CEEED54C672B5D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d97_bc27_ac039_f681(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xD97BC27AC039F681, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dd9540_e7_b1_c9714_f(ped: Ped, p1: Hash, r: f32, g: f32, b: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1), NativeArg::float(r), NativeArg::float(g), NativeArg::float(b)];
    unsafe { native::invoke(0xDD9540E7B1C9714F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ddfad4_deaa7_fa362(group_id: i32, p1: f32, p2: f32, p3: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4)];
    unsafe { native::invoke(0xDDFAD4DEAA7FA362, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dedbed3020_da49_dc(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDEDBED3020DA49DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dee8_d30_aa5_c2_e28_d(ped: Ped, p1: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xDEE8D30AA5C2E28D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e0_fe107_ab174_d64_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE0FE107AB174D64A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e1103300_f3456_de7(group_id: i32, p1: f32, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::float(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0xE1103300F3456DE7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e1_aadd0055_d76603(ped: Ped, entity: Entity, bone_index1: i32, bone_index2: i32, x: f32, y: f32, z: f32, p7: f32, p8: bool, p9: bool, p10: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), NativeArg::int(bone_index1), NativeArg::int(bone_index2), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p7), NativeArg::boolean(p8), NativeArg::boolean(p9), NativeArg::int(p10)];
    unsafe { native::invoke(0xE1AADD0055D76603, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e1_b3_be07_d3_aadded(ped: Ped, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xE1B3BE07D3AADDED, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e20027_b414_bfe6_c7(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE20027B414BFE6C7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e29_d8_cd66553_dbaa(horse: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse)];
    unsafe { native::invoke(0xE29D8CD66553DBAA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e37_acee15_ac50_c7_e(ped: Ped, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(p1)];
    unsafe { native::invoke(0xE37ACEE15AC50C7E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e4_c95_e0_ae31_c6512(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0xE4C95E0AE31C6512, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e4_ef4382_e22_c780_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE4EF4382E22C780C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e50_c9816_b3_f22_d8_b(ped: Ped, p1: Hash, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0xE50C9816B3F22D8B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e6_cb36_f43_a95_d75_f(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE6CB36F43A95D75F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e735_a7_da22_e88359(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE735A7DA22E88359, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e737_d5_f14304_a2_ec(ped: Ped, player: Player, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(player), NativeArg::int(p2)];
    unsafe { native::invoke(0xE737D5F14304A2EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e8_abe3_b73_fc7_fe17(ped: Ped, target_entity: Entity, prop_item_id: &str, conditional_anim_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_entity), NativeArg::string(prop_item_id), NativeArg::string(conditional_anim_name)];
    unsafe { native::invoke(0xE8ABE3B73FC7FE17, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e9_e06_ea514_a69061(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE9E06EA514A69061, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ea8763_e505_afd49_a(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xEA8763E505AFD49A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eb8886_e1065654_cd(ped: Ped, p1: i32, p2: &str, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::string(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0xEB8886E1065654CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ebaac9_a750_e7563_b(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xEBAAC9A750E7563B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ebd49472_bccf7642(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xEBD49472BCCF7642, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec60_d1_d225_bc50_aa(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0xEC60D1D225BC50AA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ed1_c764997_a86_d5_a(ped1: Ped, ped2: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2)];
    unsafe { native::invoke(0xED1C764997A86D5A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eedc9_b29314_b2733(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9];
    unsafe { native::invoke(0xEEDC9B29314B2733, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ef371232_bc6053_e1(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xEF371232BC6053E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f47_d54_b986_f0_a346(ped: Ped, dance_intensity: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(dance_intensity)];
    unsafe { native::invoke(0xF47D54B986F0A346, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f4860514_ad354226(shocking_event: ScrHandle, x: f32, y: f32, z: f32, p4: f32, p5: &mut i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(shocking_event), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p4), NativeArg::in_out(p5)];
    unsafe { native::invoke(0xF4860514AD354226, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f634_e2892220_ef34(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0xF634E2892220EF34, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f6_a8_c4_b4_a11_ae89_c(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xF6A8C4B4A11AE89C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f7327_acc7_a89_aef1(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xF7327ACC7A89AEF1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f917_f92_bf22_ecbab(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF917F92BF22ECBAB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f9331_b3_a314_eb49_d(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF9331B3A314EB49D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f9_cbd46433_e36713(ped: Ped, target_entity: Entity, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_entity), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), NativeArg::string(p9)];
    unsafe { native::invoke(0xF9CBD46433E36713, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa0_d206_b489_a6846(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xFA0D206B489A6846, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa742_b82_d093_d848(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xFA742B82D093D848, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa8_c10_dce0706_d43(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xFA8C10DCE0706D43, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fc23348_f0_f4_e245_f(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xFC23348F0F4E245F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fd3_c31_a2_e45671_e7(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xFD3C31A2E45671E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fd8_e853_f0_bc2_e942(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFD8E853F0BC2E942, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fea6126_c34_df2532(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xFEA6126C34DF2532, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ffa1594703_ed27_ca(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xFFA1594703ED27CA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ffde295662405_b25(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xFFDE295662405B25, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ped_stay_out_volume(ped: Ped, volume: Volume) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(volume)];
    unsafe { native::invoke(0xE9B168527B337BF0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ped_subscribe_to_legendary_blips(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xE37287EE358939C3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_scenario_blocking_volume(volume: Volume, p1: bool, flag: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(p1), NativeArg::int(flag)];
    unsafe { native::invoke(0x4C39C95AE5DB1329, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_scenario_transition(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6D07B371E9439019, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_texture_layer(texture_id: i32, albedo_hash: Hash, normal_hash: Hash, material_hash: Hash, blend_type: i32, tex_alpha: f32, sheet_grid_index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(texture_id), NativeArg::hash(albedo_hash), NativeArg::hash(normal_hash), NativeArg::hash(material_hash), NativeArg::int(blend_type), NativeArg::float(tex_alpha), NativeArg::int(sheet_grid_index)];
    unsafe { native::invoke(0x86BB5FF45F193A02, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn apply_ped_damage_pack_to_bone(ped: Ped, bone_id: i32, x_offset: f32, y_offset: f32, z_offset: f32, x_rot: f32, y_rot: f32, z_rot: f32, damage_pack: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(bone_id), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::string(damage_pack)];
    unsafe { native::invoke(0x58D32261AE0F0843, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn apply_ped_meta_ped_outfit(request_id: i32, ped: Ped, p2: bool, p3: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(request_id), NativeArg::int(ped), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x74F512E29CB717E2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn apply_shop_item_to_ped(ped: Ped, component_hash: Hash, immediately: bool, is_mp: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component_hash), NativeArg::boolean(immediately), NativeArg::boolean(is_mp), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xD3A7B003ED343FD9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn apply_texture_on_ped(ped: Ped, component_hash: Hash, texture_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component_hash), NativeArg::int(texture_id)];
    unsafe { native::invoke(0x0B46E25761519058, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_all_ambient_ped_reservations_ready() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5E420FF293EE5472, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_volume_to_entity(volume: Volume, entity: Entity, offset_x: f32, offset_y: f32, offset_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, p8: i32, p9: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(entity), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::int(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x7C00CFC48A782DC0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_ped_hear_target_ped(source: Ped, listener: Ped, include_noise_boost: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(source), NativeArg::int(listener), NativeArg::boolean(include_noise_boost)];
    unsafe { native::invoke(0x0EA9EACBA3B01601, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_ped_use_scenario_point(ped: Ped, scenario: i32, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(scenario), p2, p3, p4];
    unsafe { native::invoke(0xAB643407D0B26F07, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn change_ped_stamina(ped: Ped, amount: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(amount)];
    unsafe { native::invoke(0xC3D4B754C0E86B9E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_active_animal_owner(horse: Ped, clear: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse), NativeArg::boolean(clear)];
    unsafe { native::invoke(0xBCC76708E5677E1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_action_disable_flag(ped: Ped, action_disable_flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(action_disable_flag)];
    unsafe { native::invoke(0x949B2F9ED2917F5D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_blood_damage_facial(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x7F5D88333EE8A86F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_combat_style(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x78815FC52832B690, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_combat_style_mod(ped: Ped, combat_style_mod_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(combat_style_mod_hash)];
    unsafe { native::invoke(0x1FA132CBCD7CB239, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_desired_loco_for_model(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4FD80C3DD84B817B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_desired_loco_motion_type(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x58F7DB5BD8FA2288, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_grapple_flag(ped: Ped, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flag)];
    unsafe { native::invoke(0xEAE3B5B019C8D23F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_target_action_disable_flag(ped: Ped, action_disable_flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(action_disable_flag)];
    unsafe { native::invoke(0xBBF6D1D07C02D00A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_texture(texture_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id)];
    unsafe { native::invoke(0xB63B9178D0F58D82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_pelt_from_horse(horse: Ped, pelt_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse), NativeArg::int(pelt_id)];
    unsafe { native::invoke(0x627F7F3A0C4C51FF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compute_loot_for_ped_carcass(out_loot: &mut Any, model: Hash, damage_cleanliness: i32, skinning_quality: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(out_loot), NativeArg::hash(model), NativeArg::int(damage_cleanliness), NativeArg::int(skinning_quality)];
    unsafe { native::invoke(0xB29C553BA582D09E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compute_ped_move_blend_ratio_for_max_speed(ped: Ped, max_move_blend_ratio: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(max_move_blend_ratio)];
    unsafe { native::invoke(0x46BF2A810679D6E6, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compute_satchel_item_for_ped_carcass(out_inventory_item_array: &mut Any, ped: Ped, damage_cleanliness: i32, skinning_quality: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(out_inventory_item_array), NativeArg::int(ped), NativeArg::int(damage_cleanliness), NativeArg::int(skinning_quality)];
    unsafe { native::invoke(0x6B89FAA36FC909A3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn compute_speed_for_ped_move_blend_ratio(ped: Ped, speed: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(speed)];
    unsafe { native::invoke(0xCA95924C893A0C91, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_gravity_well(x_pos: f32, y_pos: f32, z_pos: f32, heading: f32, radius: f32, p5: f32, p6: f32, p7: f32, stop_at_destination: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::float(heading), NativeArg::float(radius), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::boolean(stop_at_destination)];
    unsafe { native::invoke(0x4F5EBE70081E5A20, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_meta_ped(request_id: i32, x: f32, y: f32, z: f32, heading: f32, p5: bool, p6: bool, p7: bool, p8: bool, p9: bool) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(request_id), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::boolean(p5), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::boolean(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x0BCD4091C8EABA42, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_meta_ped_asset(asset: Hash, pos_x: f32, pos_y: f32, pos_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, p7: bool, p8: bool, p9: bool) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::hash(asset), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::boolean(p7), NativeArg::boolean(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x9641A9A20310F6B8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_meta_ped_outfit_ped(request_id: i32, x: f32, y: f32, z: f32, heading: f32, p5: bool, p6: bool, p7: bool, p8: bool) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(request_id), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::boolean(p5), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0xEAF682A14F8E5F53, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_volume_from_entity(volume: Volume, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(entity)];
    unsafe { native::invoke(0x19C975B81BE53C28, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detect_player_shot_near_ped(player: Player, ped: Ped, shot_near_time_ms: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::int(shot_near_time_ms)];
    unsafe { native::invoke(0x9C81338B2E62CE0A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_all_look_at_requests(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xE1965A380342BE1F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_ambient_look_at_requests(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x80038740C96AD17F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_meta_ped_outfit_exist_for_ped_model(outfit: Hash, model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(outfit), NativeArg::hash(model)];
    unsafe { native::invoke(0xC0E880B7A441164D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_meta_ped_suboutfit_exist_for_ped_model(outfit: Hash, suboutfit: Hash, model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(outfit), NativeArg::hash(suboutfit), NativeArg::hash(model)];
    unsafe { native::invoke(0x4FF3C2B4E6A196C1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn equip_meta_ped_outfit(ped: Ped, hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(hash)];
    unsafe { native::invoke(0x1902C4CFCC5BE57C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn equip_meta_ped_outfit_extra(ped: Ped, component: i32, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(component), p2, p3];
    unsafe { native::invoke(0xA5BAE410B03E7371, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn equip_meta_ped_outfit_preset(ped: Ped, preset_id: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(preset_id), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x77FF8D35EEC6BBC4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn equip_meta_ped_suboutfit(ped: Ped, suboutfit: Hash, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(suboutfit), NativeArg::int(p2)];
    unsafe { native::invoke(0x66FF395445A88A6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn fake_set_ped_loco_injured(ped: Ped, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x8B3CB08158E98481, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_ped_death(ped: Ped, ped_killer: Ped, weapon: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ped_killer), NativeArg::hash(weapon)];
    unsafe { native::invoke(0x1CE875505D45338A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_accuracy_against_local_player_modifier(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xDC9273D95976BA22, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_active_animal_owner(animal: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(animal)];
    unsafe { native::invoke(0xF103823FFE72BB49, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_active_dynamic_scenario(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x569F1E1237508DEB, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_active_dynamic_scenario_2(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC22AA08A8ADB87D4, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_blocking_of_non_temporary_events(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x268B3AEBF032A88D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_carried_pelt_skins(mount: Ped, out_data: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x6F43C351A5D51E2F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_carrier_as_human(entity: Entity) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x79443D56C8DF45EE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_carrier_as_mount(entity: Entity) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xA033D7E4BBF9844D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_carrier_as_ped(entity: Entity) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x09B83E68DE004CD4, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_category_of_component_at_index(ped: Ped, component_index: i32, p2: NativeArg) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(component_index), p2];
    unsafe { native::invoke(0x9B90842304C938A7, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_char_expression(ped: Ped, index: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(index)];
    unsafe { native::invoke(0xFD1BA1EEF7985BB8, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_default_relationship_group_hash(model_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(model_hash)];
    unsafe { native::invoke(0x3CC4A718C258BDD0, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_first_entity_ped_is_carrying(ped: Ped) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD806CD2A4F2C2996, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_group_formation(group_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(group_id)];
    unsafe { native::invoke(0x13A1B061007C906B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_health_recharge_multiplier(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x95B8E397B8F4360F, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_horse_taming_state(horse: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(horse)];
    unsafe { native::invoke(0x454AD4DA6C41B5BD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_incapacitation_time_remaining(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x88D9D76D78065487, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_ped_being_robbed(ped: Ped, player: Player, true_until_player_pockets_item: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(player), NativeArg::boolean(true_until_player_pockets_item)];
    unsafe { native::invoke(0xE33F98BD76490ABC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_ped_command_hash_present(ped: Ped, command_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(command_hash)];
    unsafe { native::invoke(0x68821369A2CEADD5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_ped_in_dispute_with_ped(ped: Ped, ped_in_dispute_with: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ped_in_dispute_with)];
    unsafe { native::invoke(0x331550B212014B92, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_ped_motivation_state_enabled(ped: Ped, motivation_state: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(motivation_state)];
    unsafe { native::invoke(0x33FA048675821DA7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_lassoed_lassoer(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0C31C51168E80365, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_lassoer_of_ped(ped: Ped) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x833F0053340EF413, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_lasso_target(ped: Ped) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB65A4DAB460A19BD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_last_led_mount(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x693126B5D0457D0D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_last_mount(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4C8B59171957BCF7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_last_vehicle_draft_horse_was_attached_to(horse: Ped) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(horse)];
    unsafe { native::invoke(0x5064DB5083C29921, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_looting_flag(ped: Ped, loot_flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(loot_flag)];
    unsafe { native::invoke(0xE4C11F104620DDCE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_meta_ped_race(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB292203008EBBAAC, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_meta_ped_type(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xEC9A1261BF0CE510, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_components_in_ped(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x90403E8107B60E81, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_component_categories_in_ped(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA622E66EEE92A08D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_free_slots_in_ped_pool() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x313778EDCA9158E2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_reserved_ambient_peds_desired() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x62DE46F061CAA468, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_reserved_ambient_peds_ready() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5C16855277819BBF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_reserved_health(ped: Ped) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x16F2C8C084AB2092, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_reserved_stamina(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xFC3B580C4380B5B7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_peds_in_combat_with_target(ped: Ped, itemset: ItemSet, flag: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(itemset), NativeArg::int(flag)];
    unsafe { native::invoke(0x7BE607DAFF382FD2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_attitude(ped: Ped, player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(player)];
    unsafe { native::invoke(0x7CC2186C32D3540A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_blackboard_bool(ped: Ped, variable_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x498F2E77982D6945, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_blackboard_float(ped: Ped, variable_name: &str) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x56E58D4D118FB45E, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_blackboard_hash(ped: Ped, variable_name: &str) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0xBF5E791BBBF90A3C, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_brawling_style(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xEC6B59BE445FEC51, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_can_be_incapacitated_this_frame(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7A4E00364B5D727B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_combat_attribute(ped: Ped, attribute_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index)];
    unsafe { native::invoke(0xCC2B20596E29E4E3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_component_category_by_index(ped: Ped, index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(index)];
    unsafe { native::invoke(0xCCB97B51893C662F, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_damaged(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6CFC373008A1EDAF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_damage_cleanliness(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x88EFFED5FE8B0B4A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_defensive_volume(ped: Ped, p1: NativeArg) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0xEF2E6F870783369B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_dirt_level(ped: Ped, use_composite_layer: bool) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(use_composite_layer)];
    unsafe { native::invoke(0x0105FEE8F9091255, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_drunkness(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6FB76442469ABD68, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_grappler(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD0B7AEB56229D317, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_grapple_flag(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF3C873ED0C595109, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_grapple_style(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x753B15AD0FD6F3E3, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_has_interacted_with_player(target_ped: Ped, player: Player, flag: i32, duration_ms: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(target_ped), NativeArg::int(player), NativeArg::int(flag), NativeArg::int(duration_ms)];
    unsafe { native::invoke(0x947E43F544B6AB34, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_has_simple_player_memory_changed(ped: Ped, memory_type: i32, ms: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(memory_type), NativeArg::int(ms)];
    unsafe { native::invoke(0xC3995D396F1D97B6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_height(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1D491CCF7211FB74, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_id_range(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x31167ED4324B758D, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_incapacitation_health(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x89BFDF6D53145545, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_index_from_perschar_hash(pers_char_hash: Hash, p1: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::hash(pers_char_hash), NativeArg::int(p1)];
    unsafe { native::invoke(0xE76687023D8C8505, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_interaction_personality(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD7AD3C7EBAF88C92, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_lasso_hogtie_flag(ped: Ped, flag_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flag_id)];
    unsafe { native::invoke(0x2C76FA0E01681F8D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_last_dropped_hat(ped: Ped) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1F714E7A9DADFC42, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_lod_multiplier(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1B710E6F4AB69341, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_max_stamina(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xCB42AFE2B613EE55, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_melee_action_phase(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6127F25ED21C533C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_meta_outfit_hash(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x30569F348D126A5A, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_model_size_from_hash(model_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(model_hash)];
    unsafe { native::invoke(0xA65AA1ACE81E5A77, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_motivation(ped: Ped, motivation_state: i32, target_ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(motivation_state), NativeArg::int(target_ped)];
    unsafe { native::invoke(0x42688E94E96FD9B4, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_quality(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7BCC6087D130312A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_ragdoll_bone_index(ped: Ped, bone_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(bone_id)];
    unsafe { native::invoke(0xC5303F460A40D21D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_register_prop(ped: Ped, prop_name: &str, detach_prop: bool) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(prop_name), NativeArg::boolean(detach_prop)];
    unsafe { native::invoke(0x4D0D2E3D8BC000EB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_remaining_revival_time(ped: Ped, normalized: bool) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(normalized)];
    unsafe { native::invoke(0xEBE89623EB861271, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_speech_relationship(ped: Ped, p1: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0x7C8AA850617651D9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_stamina(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x775A1CA7893AA8B5, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_stamina_normalized(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x22F2A386D43048A9, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_tranquilizer(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x65C75FDCCAC86464, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_who_hogitied_this_ped(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3D9F958834AB9C30, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_pelt_from_horse(horse: Ped, index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(horse), NativeArg::int(index)];
    unsafe { native::invoke(0x0CEEB6F4780B1F2F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_current_animal_damage_modifier(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xEE2D5C819A65BF26, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_dismount_timestamp(mount: Ped, player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::int(player)];
    unsafe { native::invoke(0xE8D1CCB9375C101B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_ped_water_depth(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2942457417A5FD24, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rider_of_mount(mount: Ped, p1: bool) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xB676EFDA03DADA52, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_shop_item_base_layers(shop_item: Hash, p1: NativeArg, ped: Ped, metaped_type: i32, p4: bool, drawable: &mut Hash, albedo: &mut Hash, normal: &mut Hash, material: &mut Hash, p9: &mut Hash, p10: &mut Hash, p11: &mut Hash, p12: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(shop_item), p1, NativeArg::int(ped), NativeArg::int(metaped_type), NativeArg::boolean(p4), NativeArg::in_out(drawable), NativeArg::in_out(albedo), NativeArg::in_out(normal), NativeArg::in_out(material), NativeArg::in_out(p9), NativeArg::in_out(p10), NativeArg::in_out(p11), NativeArg::in_out(p12)];
    unsafe { native::invoke(0x63342C50EC115CE8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_shop_item_component_at_index(ped: Ped, index: i32, resolve_selection: bool, out_status_flag: &mut i32, out_wearable_state: &mut Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(index), NativeArg::boolean(resolve_selection), NativeArg::in_out(out_status_flag), NativeArg::in_out(out_wearable_state)];
    unsafe { native::invoke(0x77BA37622E22023B, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_shop_item_component_category(component_hash: Hash, metaped_type: i32, is_mp: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(component_hash), NativeArg::int(metaped_type), NativeArg::boolean(is_mp)];
    unsafe { native::invoke(0x5FF9A878C3D115B8, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_shop_item_hat_component(ped: Ped, metaped_type: i32, p2: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(metaped_type), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x7E02E4218D916B94, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_shop_item_num_wearable_states(component_hash: Hash, is_mp_female: bool, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(component_hash), NativeArg::boolean(is_mp_female), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xFFCC2DB2D9953401, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_shop_item_wearable_state_by_index(component_hash: Hash, wearable_state_index: i32, is_mp_female: bool, p3: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(component_hash), NativeArg::int(wearable_state_index), NativeArg::boolean(is_mp_female), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x6243635AF2F1B826, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_stamina_depletion_multiplier(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x825F6DD559A0895B, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_stamina_recharge_multiplier(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xE7687EB2F634ABF0, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_total_ped_damage_from_ai(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x92C8EACA29F6BED6, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_transport_ped_is_seated_on(ped: Ped) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x849BD6C6314793D0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_draft_horse_is_attached_to(horse: Ped) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(horse)];
    unsafe { native::invoke(0xE4770DA1B8FF4FD1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_ped_hash_command(ped: Ped, command_hash: Hash, activation_duration: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(command_hash), NativeArg::float(activation_duration)];
    unsafe { native::invoke(0xD65FDC686A031C83, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_ped_scenario_prop(ped: Ped, object: Object, conditional_anim: &str, p3: &str, p4: &str, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(object), NativeArg::string(conditional_anim), NativeArg::string(p3), NativeArg::string(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x3BBDD6143FF16F98, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_ped_scenario_prop_dynamic(ped: Ped, object: Object, p2: &str, p3: &str, p4: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(object), NativeArg::string(p2), NativeArg::string(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xA0774E388CE4A679, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_meta_ped_asset_loaded(request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(request_id)];
    unsafe { native::invoke(0xB0B2C6D170B0E8E5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_meta_ped_outfit_loaded(request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(request_id)];
    unsafe { native::invoke(0x610438375E5D1801, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_meta_ped_request_loaded(request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(request_id)];
    unsafe { native::invoke(0xC0940AC858C1E126, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_ped_been_shoved_recently(ped: Ped, ms: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ms)];
    unsafe { native::invoke(0x29FCE825613FEFCA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_ped_emotional_preset_loaded(ped: Ped, name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(name)];
    unsafe { native::invoke(0xDE3904B22695D9F9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_ped_shot_recently(ped: Ped, seconds: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(seconds)];
    unsafe { native::invoke(0xB7DBB2986B87E230, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_ped_taken_gore_damage(ped: Ped, limb: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(limb)];
    unsafe { native::invoke(0xBA208A8D6399A3AC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn horse_agitate(mount: Ped, kick_off_rider: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::boolean(kick_off_rider)];
    unsafe { native::invoke(0xBAE08F00021BFFB2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn incapacitated_revive(ped: Ped, ped2: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ped2)];
    unsafe { native::invoke(0xF6262491C7704A63, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_animal_controlled_by_a_player(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0E2F43516F998269, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_animal_interaction_running(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7FC84E85D98F063D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_animal_skinned(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x88A5564B19C15391, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_meta_ped_asset_valid(request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(request_id)];
    unsafe { native::invoke(0x93FFD92F05EC32FD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_meta_ped_fish(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x118D476A6F1A13F1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_meta_ped_outfit_equipped(ped: Ped, outfit: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(outfit)];
    unsafe { native::invoke(0x98082246107A6ACF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_meta_ped_outfit_request_valid(request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(request_id)];
    unsafe { native::invoke(0xB25E57FC8E37114D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_meta_ped_request_valid(request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(request_id)];
    unsafe { native::invoke(0x43E4DA469541A9C9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_meta_ped_using_component(ped: Ped, component: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component)];
    unsafe { native::invoke(0xFB4891BD7578CDC1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_mount_seat_free(mount: Ped, seat: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::int(seat)];
    unsafe { native::invoke(0xAAB0FE202E9FC9F0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_action_disable_flag_enabled(ped: Ped, action_disable_flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(action_disable_flag)];
    unsafe { native::invoke(0xB346C85D49CC998E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_child(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x137772000DAF42C5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_climbing_ladder(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x59643424B68D52B5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_cowering(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB086C8C0F5701D14, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_doing_scenario_transition(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC488B8C0E52560D8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_dragging(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x226CF9B159E38F42, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_drunk(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x50F124E6EF188B22, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_group_leader(ped: Ped, group_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(group_id)];
    unsafe { native::invoke(0x878B68960C1C2A35, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_intimidated(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x57779B55B83E2BEA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_investigating(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7583A9D35248B83F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_navigable_water(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xDC88D06719070C39, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_point(ped: Ped, x: f32, y: f32, z: f32, radius: f32, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x078076AB50FB117F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_leading_any_group(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x917760CFE7A0E0F1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_model_suppressed(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0xAA9F048DCF69B6DC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_queued_for_deletion(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8D9BFCE3352DE47F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_sliding(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD6740E14E4CEFC0B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_target_action_disable_flag_enabled(ped: Ped, action_disable_flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(action_disable_flag)];
    unsafe { native::invoke(0x02AA2096FE00F3E1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_using_action_mode_2(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xEBB208D6AE712C03, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_visibility_tracked(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5102307CE88798EB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scenario_blocking_area_valid(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x91A5F9CBEBB9D936, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_target(ped: Ped, target_ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped)];
    unsafe { native::invoke(0x6E5CBCB3941D7D08, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_texture_valid(texture_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(texture_id)];
    unsafe { native::invoke(0x31DC8D3F216D8509, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_this_model_a_horse(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x772A1969F649E902, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_tracked_ped_visibility_percentage_not_less_than(ped: Ped, percent: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(percent)];
    unsafe { native::invoke(0x164CECC59E70DF86, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_using_slipstream(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xAF61B3CD8C3B82C3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_apply_speech_relationship(ped: Ped, p1: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0x1E017404784AA6A3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_clear_loco_motion(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x935CF6E42BAF7F4D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_dueling_did_player_headshot_opponent(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBD6B242B8BD5543A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_emotional_preset_loco_motion(ped: Ped, preset_name: &str, target_ped: Ped, duration: i32, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(preset_name), NativeArg::int(target_ped), NativeArg::int(duration), NativeArg::int(flag)];
    unsafe { native::invoke(0xAAB050DA48B57978, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_remove_speech_relationship(ped: Ped, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0x2B4CE170DE09F346, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_set_simple_player_memory(ped: Ped, memory_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(memory_type)];
    unsafe { native::invoke(0xC494C76A34266E82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_was_killed_by_headshot(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x06FA94C835787C64, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn propitem_play_conditional_anim_with_object(ped: Ped, target_entity: Entity, prop_item_id: &str, conditional_anim_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_entity), NativeArg::string(prop_item_id), NativeArg::string(conditional_anim_name)];
    unsafe { native::invoke(0xCE7A6C1D5CDE1F9D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn refresh_carried_ped_for_ped(ped: Ped, carried_ped: &mut Ped, p2: i32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(carried_ped), NativeArg::int(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x6B67320E0D57856A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn refresh_carry_state_for_ped(ped: Ped, carry_type: i32, out_ents: &mut Any, out_ents_count: i32, filter_flags: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(carry_type), NativeArg::in_out(out_ents), NativeArg::int(out_ents_count), NativeArg::int(filter_flags)];
    unsafe { native::invoke(0x4642182A298187D0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn refresh_loot_state_for_ped(ped: Ped, p1: i32, loot_target: &mut Ped, p3: i32, p4: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::in_out(loot_target), NativeArg::int(p3), NativeArg::int(p4)];
    unsafe { native::invoke(0x5463C962BC7777C3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn refresh_meta_ped_shop_items(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x59BD177A1A48600A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_hated_targets_in_area(ped: Ped, x: f32, y: f32, z: f32, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0xD8736EFDA38EDC5C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_meta_ped_asset_request(request_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(request_id)];
    unsafe { native::invoke(0x13E7320C762F0477, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_meta_ped_outfit_request(request_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(request_id)];
    unsafe { native::invoke(0x4592B8B9B0EF5F48, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_meta_ped_request(request_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(request_id)];
    unsafe { native::invoke(0x3972F78A78B5D9DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_texture(texture_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id)];
    unsafe { native::invoke(0x6BEFAA907B076859, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_conditional_anim_propitem(ped: Ped, prop_item_id: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(prop_item_id)];
    unsafe { native::invoke(0x3A50753042B6891B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_gravity_well(handle: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(handle)];
    unsafe { native::invoke(0x87247BC60B60BED8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_motion_type_asset(name_hash: Hash, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(name_hash), NativeArg::int(ped)];
    unsafe { native::invoke(0xDE7B2B4144906CDF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_blackboard_bool(ped: Ped, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0xA6F67BEC53379A32, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_blackboard_float(ped: Ped, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x411189E51B8020BA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_blackboard_hash(ped: Ped, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x0E17378642156790, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_blackboard_int(ped: Ped, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x81B75428A7813E67, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_emotional_preset(ped: Ped, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(name)];
    unsafe { native::invoke(0xFC3BAB1801A8255A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_from_mount(ped: Ped, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x5337B721C51883A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_overlay(texture_id: i32, overlay_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id), NativeArg::int(overlay_id)];
    unsafe { native::invoke(0x96C349DE04C49011, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_stay_out_volume(ped: Ped, volume: Volume) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(volume)];
    unsafe { native::invoke(0x0CAB404CD2DB41F5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ped_subscribe_to_legendary_blips(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x011A42FD923D41CA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_shop_item_from_ped(ped: Ped, component_hash: Hash, p2: i32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component_hash), NativeArg::int(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x0D7FFA1B2F69ED82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_target(ped: Ped, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped)];
    unsafe { native::invoke(0x4707E9C23D8CA3FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_meta_ped(model: Hash, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(model), NativeArg::int(p1)];
    unsafe { native::invoke(0xF97C34C33487D569, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_meta_ped_asset_bundle(asset: Hash, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(asset), NativeArg::int(p1)];
    unsafe { native::invoke(0x91FE941F9FCFB702, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_meta_ped_component(meta_ped_type: i32, p1: NativeArg, p2: i32, p3: i32, p4: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(meta_ped_type), p1, NativeArg::int(p2), NativeArg::int(p3), NativeArg::int(p4)];
    unsafe { native::invoke(0xF6D9E1F3560CBF8E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_meta_ped_outfit(model: Hash, outfit: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(model), NativeArg::hash(outfit)];
    unsafe { native::invoke(0x13154A76CE0CF9AB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_motion_type_asset(name_hash: Hash, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(name_hash), NativeArg::int(ped)];
    unsafe { native::invoke(0xF7EA250B9A919E03, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ped_emotional_preset(ped: Ped, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(name)];
    unsafe { native::invoke(0x5C3C55EAAD19915F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ped_facial_mood_this_frame(ped: Ped, mood: Hash, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(mood), NativeArg::int(p2)];
    unsafe { native::invoke(0x8B3B71C80A29A4BB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ped_for_scenario_type(ped: Ped, object: Object, p2: &str, scenario_type: Hash, p4: &str, p5: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(object), NativeArg::string(p2), NativeArg::hash(scenario_type), NativeArg::string(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xBDED916A9F9B0604, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ped_getup_animation(ped: Ped, get_up_type: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(get_up_type)];
    unsafe { native::invoke(0xEAA8242C8479C27D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_prop_scenario_ped(ped: Ped, object: Object, p2: &str, p3: &str, p4: &str, p5: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(object), NativeArg::string(p2), NativeArg::string(p3), NativeArg::string(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xBEC65C6049B3219D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_texture(albedo_hash: Hash, normal_hash: Hash, material_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(albedo_hash), NativeArg::hash(normal_hash), NativeArg::hash(material_hash)];
    unsafe { native::invoke(0xC5E7204F322E49EB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reserve_ambient_peds(num_peds: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(num_peds)];
    unsafe { native::invoke(0xED9582B3DA8F02B4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reserve_ambient_peds_total(num_peds: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(num_peds)];
    unsafe { native::invoke(0xF008E0BA1FE1D644, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ped_components(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0BFA1BD465CDFEFD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ped_incapacitation_bleed_out_duration(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4B9668DB91DC39B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ped_ladder_movement_speed_modifier(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x801917E7D7BCE418, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ped_stamina(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x36513AFFC703C60D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_ped_texture(texture_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id)];
    unsafe { native::invoke(0x8472A1789478F82F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn restore_ped_stamina(ped: Ped, stamina: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(stamina)];
    unsafe { native::invoke(0x675680D089BFA21F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_accuracy_against_local_player_modifier(ped: Ped, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(modifier)];
    unsafe { native::invoke(0xC2266AA617668AD3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_active_meta_ped_components_updated(ped: Ped, is_mp: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(is_mp)];
    unsafe { native::invoke(0xAAB86462966168CE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_animal_density_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0xC0258742B034DFAF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_human_density_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0xBA0980B5C0A11924, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ambient_ped_density_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0xAB0D553FE20A6E25, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_char_expression(ped: Ped, index: i32, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(index), NativeArg::float(value)];
    unsafe { native::invoke(0x5653AB26C82938CF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_current_defense_against_players_modifier(horse: Ped, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse), NativeArg::float(modifier)];
    unsafe { native::invoke(0x069EDDF1FD4DEB0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_defense_modifier_for_ped(ped: Ped, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(modifier)];
    unsafe { native::invoke(0x9B6808EC46BE849B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_formation_auto_assign_position(group_id: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(group_id), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x478F6B9920446CE2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_health_recharge_multiplier(ped: Ped, multiplier: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(multiplier)];
    unsafe { native::invoke(0xDE1B1907A83A1550, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_horse_scripted_flag(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xB8AB265426CFE6DD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_interaction_lockon_flag(ped: Ped, player: Player, flag: i32, enable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(player), NativeArg::int(flag), NativeArg::boolean(enable)];
    unsafe { native::invoke(0xFECA2081F61ED2CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_meta_ped_tag(ped: Ped, drawable: Hash, albedo: Hash, normal: Hash, material: Hash, palette: Hash, tint0: i32, tint1: i32, tint2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(drawable), NativeArg::hash(albedo), NativeArg::hash(normal), NativeArg::hash(material), NativeArg::hash(palette), NativeArg::int(tint0), NativeArg::int(tint1), NativeArg::int(tint2)];
    unsafe { native::invoke(0xBC6DF00D7A4A6819, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_meta_ped_weariness(ped: Ped, weariness: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(weariness)];
    unsafe { native::invoke(0x314C5465195F3B30, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_min_ped_health_threshold(ped: Ped, health_amount: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(health_amount)];
    unsafe { native::invoke(0x7883AA809DF43D98, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mount_bonding_level(ped: Ped, bonding_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(bonding_level)];
    unsafe { native::invoke(0xA69899995997A63B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mount_security_enabled(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x11E6B9629C46D6EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_action_disable_flag(ped: Ped, action_disable_flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(action_disable_flag)];
    unsafe { native::invoke(0xB8DE69D9473B7593, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_activate_wound_effect(ped: Ped, p1: i32, bone_id: i32, move_wound_left_right: f32, blood_fountain_pressure: f32, yaw: f32, blood_fountain_direction: f32, blood_fountain_pulse: f32, p8: f32, p9: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::int(bone_id), NativeArg::float(move_wound_left_right), NativeArg::float(blood_fountain_pressure), NativeArg::float(yaw), NativeArg::float(blood_fountain_direction), NativeArg::float(blood_fountain_pulse), NativeArg::float(p8), NativeArg::float(p9)];
    unsafe { native::invoke(0xFFD54D9FE71B966A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_active_player_type(ped: Ped, player_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(player_type)];
    unsafe { native::invoke(0xB285AD0EC870B2DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_animal_detection_modifier(ped: Ped, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(modifier)];
    unsafe { native::invoke(0x43CA928E892CFDB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_beat_multiplier(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x6DBF2D78709AD70B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_blackboard_bool(ped: Ped, variable_name: &str, value: bool, remove_timer: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name), NativeArg::boolean(value), NativeArg::int(remove_timer)];
    unsafe { native::invoke(0xCB9401F918CB0F75, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_blackboard_float(ped: Ped, variable_name: &str, value: f32, remove_timer: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name), NativeArg::float(value), NativeArg::int(remove_timer)];
    unsafe { native::invoke(0x437C08DB4FEBE2BD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_blackboard_hash(ped: Ped, variable_name: &str, value: &str, remove_timer: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name), NativeArg::string(value), NativeArg::int(remove_timer)];
    unsafe { native::invoke(0xA762C9D6CF165E0D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_blackboard_int(ped: Ped, variable_name: &str, value: i32, remove_timer: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(variable_name), NativeArg::int(value), NativeArg::int(remove_timer)];
    unsafe { native::invoke(0x5F53010C4C3F6BAF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_bleedout_profile(ped: Ped, bleedout_profile: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(bleedout_profile)];
    unsafe { native::invoke(0x66C047719B0E80E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_brawling_style(ped: Ped, brawling_style: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(brawling_style)];
    unsafe { native::invoke(0x8BA83CC4288CD56D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_be_lassoed(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xFD6943B6DF77E449, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_can_unk_bodypart_ik(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xEE9DF765990E8D1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_combat_attribute_hash(ped: Ped, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(p1)];
    unsafe { native::invoke(0xBD75500141E4725C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_combat_behaviour(ped: Ped, behaviour: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(behaviour)];
    unsafe { native::invoke(0x9238A3D970BBB0A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_combat_style(ped: Ped, combat_style_hash: Hash, p2: i32, duration: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(combat_style_hash), NativeArg::int(p2), NativeArg::float(duration)];
    unsafe { native::invoke(0x8ACC0506743A8A5C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_combat_style_mod(ped: Ped, combat_style_mod_hash: Hash, duration: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(combat_style_mod_hash), NativeArg::float(duration)];
    unsafe { native::invoke(0x8B1E8E35A6E814EA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_crouch_movement(ped: Ped, state: bool, p2: i32, immediately: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(state), NativeArg::int(p2), NativeArg::boolean(immediately)];
    unsafe { native::invoke(0x7DE9692C6F64CFE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_cull_range(ped: Ped, p1: f32, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0x8AC1D721B2097B6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_damaged(ped: Ped, damaged: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(damaged)];
    unsafe { native::invoke(0xDACE03C65C6666DB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_damage_cleanliness(ped: Ped, damage_cleanliness: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(damage_cleanliness)];
    unsafe { native::invoke(0x7528720101A807A5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_defensive_area_to_angled_area(ped: Ped, x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, p7: NativeArg, p8: bool, p9: bool, entity: Entity, p11: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), p7, NativeArg::boolean(p8), NativeArg::boolean(p9), NativeArg::int(entity), NativeArg::boolean(p11)];
    unsafe { native::invoke(0xEB2BFE5D009F0331, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_defensive_sphere_attached_to_entity(ped: Ped, entity: Entity, x: f32, y: f32, z: f32, radius: f32, p6: i32, p7: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(p6), NativeArg::boolean(p7)];
    unsafe { native::invoke(0x1854217C640B39EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_desired_loco_for_model(ped: Ped, locomotion_archetype: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(locomotion_archetype)];
    unsafe { native::invoke(0x923583741DC87BCE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_desired_loco_motion_type(ped: Ped, loco_motion_type: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(loco_motion_type)];
    unsafe { native::invoke(0x89F5E7ADECCCB49C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_desires_group(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xBAD2A311667A50D7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_dirt_cleaned(ped: Ped, p1: f32, p2: i32, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::int(p2), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xE3144B932DFDFF65, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_disable_kick_move(ped: Ped, disable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(disable)];
    unsafe { native::invoke(0xADD31A5C7A5FAA73, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_drunkness(ped: Ped, enabled: bool, drunkness_level: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(enabled), NativeArg::float(drunkness_level)];
    unsafe { native::invoke(0x406CCF555B04FAD3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_firing_pattern_2(ped: Ped, pattern_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(pattern_hash)];
    unsafe { native::invoke(0x20E54854DEF6A54A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_firing_pattern_3(ped: Ped, pattern_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(pattern_hash)];
    unsafe { native::invoke(0x244E8C282188E40F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_formation_position(ped: Ped, position: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(position), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x0E9E95FDEDCC9D35, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_getup_animation(ped: Ped, anim_name: &str, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(anim_name), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x3AE3552E7C207CC5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_grapple_action(ped: Ped, grapple_action: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(grapple_action)];
    unsafe { native::invoke(0x8301D87B1B89E219, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_grapple_animation(ped: Ped, grapple_anim: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(grapple_anim)];
    unsafe { native::invoke(0x56E9C26CD29D1ED6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_grapple_effect_multiplier(ped: Ped, multiplier: f32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(multiplier)];
    unsafe { native::invoke(0x99A6E246C315BF60, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_grapple_flag(ped: Ped, flag: i32, enable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flag), NativeArg::boolean(enable)];
    unsafe { native::invoke(0x789DABD18E9024DB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_grapple_sequence(ped: Ped, grapple_sequence: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(grapple_sequence)];
    unsafe { native::invoke(0x604190F0CF0DF158, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_grapple_style(ped: Ped, style: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(style)];
    unsafe { native::invoke(0x630E7B01F091A197, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_headshot_damage_multiplier(ped: Ped, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(multiplier)];
    unsafe { native::invoke(0x2BA918C823B8BA56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_health_config(ped: Ped, config_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(config_hash)];
    unsafe { native::invoke(0xF6B82FCE03B43A37, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_immersion_flag(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x7FB0088E8769CDDB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_incapacitation_flags(ped: Ped, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flags)];
    unsafe { native::invoke(0xD67B6F3BCF81BA47, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_incapacitation_modifiers(ped: Ped, can_be_incapacitated: bool, threshold: i32, bleedout_time: i32, p4: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(can_be_incapacitated), NativeArg::int(threshold), NativeArg::int(bleedout_time), NativeArg::int(p4)];
    unsafe { native::invoke(0x39ED303390DDEAC7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_incapacitation_total_bleed_out_duration(ped: Ped, duration: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(duration)];
    unsafe { native::invoke(0x2890418B39BC8FFF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_interaction_negative_response(ped: Ped, speech: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(speech)];
    unsafe { native::invoke(0xA3C53CDE922BC78B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_interaction_personality(ped: Ped, personality: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(personality)];
    unsafe { native::invoke(0x24C82EF607105FAA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_interaction_positive_response(ped: Ped, speech: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(speech)];
    unsafe { native::invoke(0x20C5459379D75C1C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_knocked_by_one_hit(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x5BCF0B79D4F5DBA3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_ladder_movement_speed_modifier(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x05CE6AF4DF071D23, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_lights(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x13A210949FCBD92B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_melee_forced_duration(ped: Ped, duration_ms: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(duration_ms)];
    unsafe { native::invoke(0x6DB875AFC584FA32, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_motivation(ped: Ped, motivation_state: i32, threshold: f32, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(motivation_state), NativeArg::float(threshold), NativeArg::int(target_ped)];
    unsafe { native::invoke(0x06D26A96CA1BCA75, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_motivation_modifier(ped: Ped, motivation_state: i32, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(motivation_state), NativeArg::float(modifier)];
    unsafe { native::invoke(0xA1EB5D029E0191D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_motivation_state_override(ped: Ped, motivation_state: i32, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(motivation_state), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x2EB75FB86C41F026, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_personality(ped: Ped, personality: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(personality)];
    unsafe { native::invoke(0xB8B6430EAD2D2437, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_prompt_name(ped: Ped, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(name)];
    unsafe { native::invoke(0x4A48B6E03BABB4AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_prompt_name_2(ped: Ped, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(name)];
    unsafe { native::invoke(0x19B14E04B009E28B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_prompt_name_from_gxt_entry(ped: Ped, gxt_entry_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(gxt_entry_hash)];
    unsafe { native::invoke(0xFCA8FB9E15FA80D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_prompt_name_from_gxt_entry_2(ped: Ped, gxt_entry_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(gxt_entry_hash)];
    unsafe { native::invoke(0xC2745D9261664901, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_quality(ped: Ped, quality: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(quality)];
    unsafe { native::invoke(0xCE6B874286D640BB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_scale(ped: Ped, scale: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(scale)];
    unsafe { native::invoke(0x25ACFC650B65C538, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_scent(ped: Ped, scent: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(scent)];
    unsafe { native::invoke(0x01B21B81865E2A1F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_target_action_disable_flag(ped: Ped, action_disable_flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(action_disable_flag)];
    unsafe { native::invoke(0xC163DAC52AC975D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_to_be_removed(ped: Ped, p1: f32, p2: f32, p3: i32, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::int(p3), p4];
    unsafe { native::invoke(0x36E4B61DC56DE77C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_to_disable_ragdoll(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x221F4D9912B7FE86, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_trail_effect(ped: Ped, p1: bool, duration: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::float(duration)];
    unsafe { native::invoke(0xA5950E16B8F31052, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_use_horse_map_collision(ped: Ped, toggle: bool) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xEB72453B6F5B45B0, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_voice_volume(ped: Ped, volume: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(volume)];
    unsafe { native::invoke(0xD05AD61F242C626B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_wetness_amount(ped: Ped, amount: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(amount)];
    unsafe { native::invoke(0xF9CFF5BB70E8A2CB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_writhing_duration(ped: Ped, writhing_duration1: f32, writhing_duration2: f32, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(writhing_duration1), NativeArg::float(writhing_duration2), NativeArg::int(p3)];
    unsafe { native::invoke(0x4DB9D03AC4E1FA84, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pelt_for_horse(horse: Ped, pelt_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse), NativeArg::int(pelt_id)];
    unsafe { native::invoke(0xA73F50E8796150D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_pelt_for_horse_by_inventory_item(horse: Ped, inventory_item: Hash, albedo_hash: Hash, normal_hash: Hash, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse), NativeArg::hash(inventory_item), NativeArg::hash(albedo_hash), NativeArg::hash(normal_hash), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xC412AA1C73111FE0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_antagonize_disabled_for_ped(ped: Ped, player: Player, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(player), NativeArg::int(duration)];
    unsafe { native::invoke(0x5708EDD71B50C008, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_current_animal_damage_modifier(player: Player, modifier: f32, p2: i32, p3: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x9EFF3C91DF38304F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_dismount_timestamp(mount: Ped, player: Player, dismounted_timestamp: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::int(player), NativeArg::int(dismounted_timestamp)];
    unsafe { native::invoke(0xA691C10054275290, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_greet_disabled_for_ped(ped: Ped, player: Player, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(player), NativeArg::int(duration)];
    unsafe { native::invoke(0x19173C3F15367B54, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_random_outfit_variation(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x283978A15512B2FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_relationship_group_script_registered(group: Hash, unregister: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(group), NativeArg::boolean(unregister)];
    unsafe { native::invoke(0xDC91F22F09BC6C2F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_remove_ped_networked(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x39A2FC5AF55A52B1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_animal_density_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0xDB48E99F8E064E56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_human_density_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0x28CB6391ACEDD9DB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_ped_density_this_frame(config_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(config_hash)];
    unsafe { native::invoke(0x95423627A9CA598E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_ped_range_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0xA77FA7BE9312F8C0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_ped_volume_reference(volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x9E3842E5DAD69F80, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_staged_ped_flag(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x028E7B3BBA0BD2FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_stamina_depletion_multiplier(ped: Ped, multiplier: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(multiplier)];
    unsafe { native::invoke(0xEF5A3D2285D8924B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_stamina_recharge_multiplier(ped: Ped, multiplier: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(multiplier)];
    unsafe { native::invoke(0x345C9F993A8AB4A4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_tank_attribute_size(ped: Ped, attribute_index: i32, size: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attribute_index), NativeArg::float(size)];
    unsafe { native::invoke(0x7FF72DE061DF55E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_texture_layer_alpha(texture_id: i32, layer_id: i32, tex_alpha: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id), NativeArg::int(layer_id), NativeArg::float(tex_alpha)];
    unsafe { native::invoke(0x6C76BC24F8BB709A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_texture_layer_mod(texture_id: i32, layer_id: i32, mod_texture_hash: Hash, mod_alpha: f32, mod_channel: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id), NativeArg::int(layer_id), NativeArg::hash(mod_texture_hash), NativeArg::float(mod_alpha), NativeArg::int(mod_channel)];
    unsafe { native::invoke(0xF2EA041F1146D75B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_texture_layer_pallete(texture_id: i32, layer_id: i32, palette_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id), NativeArg::int(layer_id), NativeArg::hash(palette_hash)];
    unsafe { native::invoke(0x1ED8588524AC9BE1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_texture_layer_roughness(texture_id: i32, layer_id: i32, tex_rough: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id), NativeArg::int(layer_id), NativeArg::float(tex_rough)];
    unsafe { native::invoke(0x057C4F092E2298BE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_texture_layer_sheet_grid_index(texture_id: i32, layer_id: i32, sheet_grid_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id), NativeArg::int(layer_id), NativeArg::int(sheet_grid_index)];
    unsafe { native::invoke(0x3329AAE2882FC8E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_texture_layer_texture_map(texture_id: i32, layer_id: i32, albedo_hash: Hash, normal_hash: Hash, material_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id), NativeArg::int(layer_id), NativeArg::hash(albedo_hash), NativeArg::hash(normal_hash), NativeArg::hash(material_hash)];
    unsafe { native::invoke(0x253A63B5BADBC398, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_texture_layer_tint(texture_id: i32, layer_id: i32, tint0: i32, tint1: i32, tint2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id), NativeArg::int(layer_id), NativeArg::int(tint0), NativeArg::int(tint1), NativeArg::int(tint2)];
    unsafe { native::invoke(0x2DF59FFE6FFD6044, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_texture_outfit_tints(ped: Ped, component_category: Hash, palette: Hash, tint0: i32, tint1: i32, tint2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component_category), NativeArg::hash(palette), NativeArg::int(tint0), NativeArg::int(tint1), NativeArg::int(tint2)];
    unsafe { native::invoke(0x4EFC1F8FF1AD94DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_total_ped_damage_falloff_bonus(ped: Ped, bonus: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(bonus)];
    unsafe { native::invoke(0x932786CE3C76477C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_total_ped_damage_from_ai(ped: Ped, total_damage: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(total_damage)];
    unsafe { native::invoke(0x73B6F907B913C860, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn shoot_trigger_at_coords(ped: Ped, x: f32, y: f32, z: f32, p4: i32, p5: f32, p6: i32, p7: f32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(p4), NativeArg::float(p5), NativeArg::int(p6), NativeArg::float(p7)];
    unsafe { native::invoke(0x4C57F27D1554E6B0, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spawnpoints_start_search_with_volume(volume: Volume, spawnpoints_flag: i32, p2: f32, duration: i32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(spawnpoints_flag), NativeArg::float(p2), NativeArg::int(duration), NativeArg::float(p4)];
    unsafe { native::invoke(0x83ED1FC9DF3411F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn toggle_player_ped_flinch(ped: Ped, x: f32, y: f32, z: f32, scale: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(scale)];
    unsafe { native::invoke(0x09E378C52B1433B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unreserve_ambient_peds(num_peds: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(num_peds)];
    unsafe { native::invoke(0x7D4E70A67A651C71, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_animal_damage_modifier(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0F9E754EBE8FDBFA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_ped_texture(texture_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(texture_id)];
    unsafe { native::invoke(0x92DAABA2C1C10B0E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_ped_variation(ped: Ped, p1: bool, p2: bool, p3: bool, p4: bool, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xCC8CA3E88256E58F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_ped_wound_effect(ped: Ped, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(value)];
    unsafe { native::invoke(0x66B1CB778D911F49, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_shop_item_wearable_state(ped: Ped, component_hash: Hash, wearable_state: Hash, p3: i32, is_mp: bool, p5: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component_hash), NativeArg::hash(wearable_state), NativeArg::int(p3), NativeArg::boolean(is_mp), NativeArg::int(p5)];
    unsafe { native::invoke(0x66B957AAC2EAAEAB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn warp_ped_out_of_vehicle(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xE0B61ED8BB37712F, &arguments) }.map(|_| ())
}

// ============================================================================
// PERSCHAR
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x08_fc896_d2_cb31_fcc(p0: NativeArg, p1: bool) -> Result<Entity, NativeError> {
    let arguments = [p0, NativeArg::boolean(p1)];
    unsafe { native::invoke(0x08FC896D2CB31FCC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_b3_a99_ab6713_aa52(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0B3A99AB6713AA52, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x112_ddf56300_bc6_e5(pers_char_hash: Hash) -> Result<PersChar, NativeError> {
    let arguments = [NativeArg::hash(pers_char_hash)];
    unsafe { native::invoke(0x112DDF56300BC6E5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_e957_aa81_f2_c61_c9() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2E957AA81F2C61C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x406808610220405_b(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x406808610220405B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_afc7288_c77238_b3(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x4AFC7288C77238B3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_f81_ead1_de8_fa19_b(pers_char: PersChar) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0x4F81EAD1DE8FA19B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x535_a66_aad2_bf68_f9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x535A66AAD2BF68F9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x59_c7_ad6_fea2_ac449(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x59C7AD6FEA2AC449, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_ee6_fccc9_c832_ca2(p0: NativeArg) -> Result<Vector3, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5EE6FCCC9C832CA2, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x63_aa2_b8_eb087886_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x63AA2B8EB087886A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x669_c25840_c6_f7_ae2(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x669C25840C6F7AE2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6759_bee6762_e140_b(pers_char: PersChar) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0x6759BEE6762E140B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x69786495_c92_a3044(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x69786495C92A3044, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x70605812_abc9_ff0_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x70605812ABC9FF0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_ae4_efa464_dae42_d(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8AE4EFA464DAE42D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_bc555034_a5_a5_e8_c(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8BC555034A5A5E8C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x92690_b0822493_ce0() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x92690B0822493CE0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x94995829_ed15_a598(p0: NativeArg) -> Result<Vector3, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x94995829ED15A598, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_c7_f95946_e304778(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9C7F95946E304778, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2_b18_ff8_d39_f6_d87(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA2B18FF8D39F6D87, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a4_dcb3_f0_dd7488_bd(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xA4DCB3F0DD7488BD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a8120_ebeaf290_c7_a(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA8120EBEAF290C7A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a8_c406_c2_a56_edc16(pers_char: PersChar) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0xA8C406C2A56EDC16, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b173599_d61_faeb31() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB173599D61FAEB31, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b65_e7_f733956_cf25(pers_char: PersChar) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0xB65E7F733956CF25, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bb68908_cd11_aebdc(pers_char: PersChar) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0xBB68908CD11AEBDC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ceb40_b678_e403759(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCEB40B678E403759, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d4_b614179_bcd0654(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD4B614179BCD0654, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d95_d777_f828_b2_bbb(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD95D777F828B2BBB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e0_e65_e0_d261_f7507(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE0E65E0D261F7507, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e4_c51_a8_a3_bd1664_c(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE4C51A8A3BD1664C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec254_c2_c9_b0_f08_f1(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xEC254C2C9B0F08F1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_efc5_c6670_e0_b99_ba() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEFC5C6670E0B99BA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f8_de7154_f7_d1458_f(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF8DE7154F7D1458F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fcc6_db8_dbe709_bc8(pers_char: PersChar) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0xFCC6DB8DBE709BC8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_persistent_character(hash: Hash) -> Result<PersChar, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0x4F76E3676583D951, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_perschar(pers_char: PersChar) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0xFC77C5B44D5FF7C0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_despawn_perschar(pers_char: PersChar) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0x7B204F88F6C3D287, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_spawn_perschar(pers_char: PersChar, p1: bool) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(pers_char), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x0CADC3A977997472, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_perschar_index_from_ped_index(ped: Ped) -> Result<PersChar, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x32A1E3B83D501096, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_perschar_model_name(pers_char_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(pers_char_hash)];
    unsafe { native::invoke(0xA00DF706C60173D1, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_perschar_outfit(pers_char_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(pers_char_hash)];
    unsafe { native::invoke(0xDC9655D47DEC0353, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_perschar_ped_index(pers_char: PersChar) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0x31C70A716CAE1FEE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_persistent_character_dead(pers_char: PersChar) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0xEB98B38CA60742D7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_persistent_character_valid(pers_char: PersChar) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0x800DF3FC913355F3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_perschar_schedule(pers_char_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(pers_char_hash)];
    unsafe { native::invoke(0x8B44273A92CD406C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn retask_persistent_character(pers_char: PersChar) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0x631CD2D77FDC0316, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn revive_perschar(pers_char: PersChar) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(pers_char)];
    unsafe { native::invoke(0x49A8C2CD97815215, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_perschar_outfit(pers_char_hash: Hash, outfit: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(pers_char_hash), NativeArg::hash(outfit)];
    unsafe { native::invoke(0x2DF89CD2ED1D0BDE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_perschar_schedule(pers_char_hash: Hash, schedule: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(pers_char_hash), NativeArg::string(schedule)];
    unsafe { native::invoke(0x187D65F3AEC5D679, &arguments) }.map(|_| ())
}

// ============================================================================
// PERSISTENCE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn persistence_add_scenario_looted(scenario: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0x8245C1F3262F4AC2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn persistence_remove_all_entities_in_area(x: f32, y: f32, z: f32, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0x9D16896F0DBE78A2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x065887_b694359799(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x065887B694359799, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_f56_fb3_fdb4_eaf65(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1F56FB3FDB4EAF65, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x291_cc21_d1_fb6790_e(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x291CC21D1FB6790E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_e545965_df98_d476(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2E545965DF98D476, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_ca5_e58_c9731_a16_b(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3CA5E58C9731A16B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_a79220_f6_d38_d7_c3(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5A79220F6D38D7C3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x66_daa3_a9274_e8_e82() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x66DAA3A9274E8E82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_a1_bd123_e5_cdb6_e5() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7A1BD123E5CDB6E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_de104_bec243_a73_b(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x8DE104BEC243A73B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b03140014_aca6_c40(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB03140014ACA6C40, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ba2_c49_ea6_a8_d24_ff(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xBA2C49EA6A8D24FF, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cfda2518_f322_d836(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCFDA2518F322D836, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc0_a1_f0_ecec9_f0_c0(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDC0A1F0ECEC9F0C0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e225_cef1901_f6108(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE225CEF1901F6108, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f5622_fa6_acfca7_db(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xF5622FA6ACFCA7DB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fc9806_da9_a460093(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2)];
    unsafe { native::invoke(0xFC9806DA9A460093, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn persistence_is_scenario_marked_as_looted(scenario: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0xFB7CF1DE938A3E22, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn persistence_is_scenario_marked_as_looted_at_coords(x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xB6E1A185C2B9319A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn persistence_is_scenario_marked_as_looted_at_coords_with_model(x: f32, y: f32, z: f32, model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::hash(model)];
    unsafe { native::invoke(0x188313616D184213, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn persistence_refresh_town_volume(volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0xEFB5F34CC0953B27, &arguments) }.map(|_| ())
}

// ============================================================================
// PHYSICS
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activate_physics(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x710311ADF0E20730, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_rope(x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, length: f32, rope_type: i32, max_length: f32, min_length: f32, p10: f32, p11: bool, p12: bool, rigid: bool, p14: f32, break_when_shot: bool, unk_ptr: &mut Any, p17: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(length), NativeArg::int(rope_type), NativeArg::float(max_length), NativeArg::float(min_length), NativeArg::float(p10), NativeArg::boolean(p11), NativeArg::boolean(p12), NativeArg::boolean(rigid), NativeArg::float(p14), NativeArg::boolean(break_when_shot), NativeArg::in_out(unk_ptr), NativeArg::boolean(p17)];
    unsafe { native::invoke(0xE832D760399EB220, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_entities_to_rope(rope_id: i32, entity1: Entity, entity2: Entity, ent1_x: f32, ent1_y: f32, ent1_z: f32, ent2_x: f32, ent2_y: f32, ent2_z: f32, length: f32, always_zero1: i32, always_zero2: i32, bone_name1: &str, bone_name2: &str, p14: bool, bone_id1: i32, bone_id2: i32, always_zero3: i32, always_zero4: i32, p19: bool, p20: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::float(ent1_x), NativeArg::float(ent1_y), NativeArg::float(ent1_z), NativeArg::float(ent2_x), NativeArg::float(ent2_y), NativeArg::float(ent2_z), NativeArg::float(length), NativeArg::int(always_zero1), NativeArg::int(always_zero2), NativeArg::string(bone_name1), NativeArg::string(bone_name2), NativeArg::boolean(p14), NativeArg::int(bone_id1), NativeArg::int(bone_id2), NativeArg::int(always_zero3), NativeArg::int(always_zero4), NativeArg::boolean(p19), NativeArg::boolean(p20)];
    unsafe { native::invoke(0x3D95EC8B6D940AC3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn break_entity_glass(entity: Entity, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: NativeArg, p10: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), p9, NativeArg::boolean(p10)];
    unsafe { native::invoke(0x2E648D16F6E308F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_child_rope(rope_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0xAA5D6B1888E4DB20, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_rope(rope_id: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(rope_id)];
    unsafe { native::invoke(0x52B4829281364649, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_rope_from_entity(rope_id: i32, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::int(entity)];
    unsafe { native::invoke(0xBCF3026912A8647D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_rope_exist(rope_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0xFD5448BE3111ED96, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rope_last_vertex_coord(rope_id: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x21BB0FBD3E217C2D, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rope_vertex_coord(rope_id: i32, vertex: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::int(vertex)];
    unsafe { native::invoke(0xEA61CA8E80F09E4D, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rope_vertex_count(rope_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x3655F544CD30F0B5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn rope_draw_shadow_enabled(rope_id: &mut i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(rope_id), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xF159A63806BB5BA8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn rope_force_length(rope_id: i32, length: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::float(length)];
    unsafe { native::invoke(0xD009F759A723DB1B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn rope_set_update_order(rope_id: i32, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), p1];
    unsafe { native::invoke(0xDC57A637A20006ED, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_damping(entity: Entity, vertex: i32, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(vertex), NativeArg::float(value)];
    unsafe { native::invoke(0xEEA3B200A6FEB65B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_breaking(object: Object, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x5CEC1A84620E7D5B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_frag_damage(object: Object, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x01BA3AED21C16CFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_rope_unwinding_front(rope_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x538D1179EC1AA9A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_rope_winding(rope_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x1461C72C889E343E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_rope_unwinding_front(rope_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0xFFF3A50779EFBBB3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_rope_winding(rope_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0xCB2D4AB84A19AA7C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_cb16_d05_e03_fb525(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0CB16D05E03FB525, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_d97_da8_acb5_d2582(rope_id: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::int(p1)];
    unsafe { native::invoke(0x1D97DA8ACB5D2582, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_fc92_bdba1106_bd2(rope_id: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::float(p1)];
    unsafe { native::invoke(0x1FC92BDBA1106BD2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x21_d0890_d88_dfb0_b0(rope_id: i32, p1: bool, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: f32, p10: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::boolean(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), NativeArg::float(p9), NativeArg::int(p10)];
    unsafe { native::invoke(0x21D0890D88DFB0B0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x31160_ec47_e7_c9549(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x31160EC47E7C9549, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x32_f4_dbfdfcccc735(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x32F4DBFDFCCCC735, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3900491_c0_d61_ed4_b(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3900491C0D61ED4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x423_c6_b1_f3786_d28_b(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x423C6B1F3786D28B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x461_fcbdeb4_d06717(rope_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x461FCBDEB4D06717, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x483_d4_e917_b0_d35_a9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x483D4E917B0D35A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x522_fa3_f490_e2_f7_ac(rope_id: i32, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), p1, p2];
    unsafe { native::invoke(0x522FA3F490E2F7AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_a989_b7_ee3672_a56(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x5A989B7EE3672A56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_bd7457221_cc5_ff4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x5BD7457221CC5FF4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_e981_c764_df33117(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x5E981C764DF33117, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x69_c810_b72291_d831(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x69C810B72291D831, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_ea0_e93_cffa472_cc(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6EA0E93CFFA472CC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x751_df00_eeff122_e3(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x751DF00EEFF122E3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x76_bad9_d538_bca1_aa(rope_id: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::float(p1)];
    unsafe { native::invoke(0x76BAD9D538BCA1AA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x814_d453_fcfdf119_f(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x814D453FCFDF119F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_d59079_c37_c21_d78(rope_id: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::float(p1)];
    unsafe { native::invoke(0x8D59079C37C21D78, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_eedfd8921389928(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9];
    unsafe { native::invoke(0x8EEDFD8921389928, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_c24846_d0_a4_a2776(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9C24846D0A4A2776, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b40_ea9_e0_d2_e2_f7_f3(rope_id: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::float(p1)];
    unsafe { native::invoke(0xB40EA9E0D2E2F7F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b7469_cb9_ac3_c0_fd4(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0xB7469CB9AC3C0FD4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bb3_e9_b073_e66_c3_c9(rope_id: i32, p1: bool, p2: bool, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xBB3E9B073E66C3C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bdda142759307528(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xBDDA142759307528, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c64_e7_a62632_ad2_fe(rope_id: i32, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0xC64E7A62632AD2FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c89_e7410_a93_ac19_a(rope_id: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::float(p1)];
    unsafe { native::invoke(0xC89E7410A93AC19A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d699_e688_b49_c0_fd2(rope_id: i32, p1: f32, p2: f32, p3: f32, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xD699E688B49C0FD2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dede679_ed29_dd4_e7(rope_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xDEDE679ED29DD4E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e54_bf2_ce6_c7_d23_a9(rope_id: i32, p1: i32, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::int(p1), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xE54BF2CE6C7D23A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eaf529446488_eb18(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xEAF529446488EB18, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f1_ea2_a881_eb7_f2_cd(rope_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xF1EA2A881EB7F2CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f27_f1_a8_de4_f50_a1_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xF27F1A8DE4F50A1B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f8_ca39_d5_c0_d1_d9_a1(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xF8CA39D5C0D1D9A1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fb9153_a54_ac713_e8(rope_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xFB9153A54AC713E8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_rope_2(x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, length: f32, rope_type: i32, is_networked: bool, p9: i32, p10: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(length), NativeArg::int(rope_type), NativeArg::boolean(is_networked), NativeArg::int(p9), NativeArg::float(p10)];
    unsafe { native::invoke(0xE9C59F6809373A99, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_entites_to_rope_3(rope_id: i32, entity1: Entity, entity2: Entity, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: NativeArg, p10: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), p9, p10];
    unsafe { native::invoke(0xE9CD9A67834985A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_entities_to_rope_2(rope_id: i32, entity1: Entity, entity2: Entity, ent1_x: f32, ent1_y: f32, ent1_z: f32, ent2_x: f32, ent2_y: f32, ent2_z: f32, bone_name1: &str, bone_name2: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::float(ent1_x), NativeArg::float(ent1_y), NativeArg::float(ent1_z), NativeArg::float(ent2_x), NativeArg::float(ent2_y), NativeArg::float(ent2_z), NativeArg::string(bone_name1), NativeArg::string(bone_name2)];
    unsafe { native::invoke(0x462FF2A432733A44, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn break_rope(rope_id: &mut i32, rope_top: &mut i32, rope_bottom: &mut i32, offset_x: f32, offset_y: f32, offset_z: f32, p6: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(rope_id), NativeArg::in_out(rope_top), NativeArg::in_out(rope_bottom), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::int(p6)];
    unsafe { native::invoke(0x4CFA2B7FAE115ECB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_rope_winding_ability(rope_id: i32, p1: &str, rope_model_type: &str, length: f32, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::string(p1), NativeArg::string(rope_model_type), NativeArg::float(length), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x3C6490D940FF5D0B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hitch_horse(horse: Ped, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x06AADE17334F7A40, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_rope_attached_to_entity(rope_id: i32, entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(rope_id), NativeArg::int(entity)];
    unsafe { native::invoke(0x9B4F7E3E4F9C77B3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_rope_broken(rope_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x79C2BEC82CFD7F7F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_rope(rope_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x6076213101A47B3B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn rope_change_visibility(rope_id: &mut i32, visible: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(rope_id), NativeArg::boolean(visible)];
    unsafe { native::invoke(0x7A54D82227A139DB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn rope_get_breaker_of_rope(rope_id: i32) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0xEE360CFC80C8B2BC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn rope_get_forced_length(rope_id: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x3D69537039F8D824, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_rope_unwinding_back(rope_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x00F611A794A3C36E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_rope_unwinding_back(rope_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rope_id)];
    unsafe { native::invoke(0x10DAA76CB8A201A1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unhitch_horse(horse: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse)];
    unsafe { native::invoke(0x0348469DAA17576C, &arguments) }.map(|_| ())
}

// ============================================================================
// PLAYER
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn boost_player_horse_speed_for_time(player: Player, speed_boost: f32, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(speed_boost), NativeArg::int(duration)];
    unsafe { native::invoke(0x09C28F828EE674FA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_player_start_mission(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x2DF170B1185AF777, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_player_has_damaged_at_least_one_non_animal_ped(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0361096D6CE4372C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_player_has_damaged_at_least_one_ped(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x270B63A641BE32F2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_player_wanted_level(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x4E4B996C928C7AA6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_player_firing(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x2970929FD5F9FC89, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_custom_entity_tint(entity: Entity, red: i32, green: i32, blue: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue)];
    unsafe { native::invoke(0x62ED71E133B6C9F1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_cleanup(cleanup_flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(cleanup_flags)];
    unsafe { native::invoke(0x768C017FB878E4F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_cleanup_for_all_threads_with_this_name(name: &str, cleanup_flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::int(cleanup_flags)];
    unsafe { native::invoke(0xDAACAF8B687F2353, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_cleanup_for_thread_with_this_id(id: i32, cleanup_flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(id), NativeArg::int(cleanup_flags)];
    unsafe { native::invoke(0xF4C9512A2F0A3031, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_cause_of_most_recent_force_cleanup() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x84E8E29EBD4A46D2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_discoverable_name_hash_and_type_for_entity(entity: Entity, type_: &mut Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::in_out(type_)];
    unsafe { native::invoke(0x0139637A3BFF8B6D, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entity_player_is_free_aiming_at(player: Player, entity: &mut Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(entity)];
    unsafe { native::invoke(0xA6817C110B830EAD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_has_player_discovered_character_name_mp(discovery_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(discovery_hash)];
    unsafe { native::invoke(0x354F689C4FFAAB37, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_player_ui_prompt_active(player: Player, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1)];
    unsafe { native::invoke(0x51BEA356B1C60225, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_wanted_level() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD04CFAD1E2B7984A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_mount_owned_by_player(player: Player) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xF49F14462F0AE27C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_players_last_vehicle() -> Result<Vehicle, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2F96E7720B0B19EA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_current_stealth_noise(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xD7ECC25E176ECBA5, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_group(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x9BAB31815159ABCF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_index() -> Result<Player, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x47E385B0D957C8D4, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_interaction_target_entity(player: Player, out_entity: &mut Entity, p2: bool, p3: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(out_entity), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x3EE1F7A8C32F24E1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_invincible(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0CBBCB2CCFA7DC4E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_name(player: Player) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x7124FD9AC0E01BA0, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_ped(player: Player) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x275F255ED201B937, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_ped_script_index(player: Player) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x5C880F9056D784C8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_received_battle_event_recently(player: Player, p1: i32, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xFB6EB8785F808551, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_target_entity(player: Player, entity: &mut Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(entity)];
    unsafe { native::invoke(0xAE663DDD99C8A670, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_team(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xB464EB6A40C7975B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_wanted_level(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xABC532F9098BFD9D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_target_character_name_for_local_player(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x36E3D8B5A6552FE8, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_target_character_name_script_override_hash(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0335106F3ACABBED, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_target_character_name_script_override_raw_string(ped: Ped) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x755E08680F21EF30, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_wanted_level_radius(p0: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x80B00EB26D9521C7, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_wanted_level_threshold(wanted_level: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(wanted_level)];
    unsafe { native::invoke(0x1B1A3B358F7D8F07, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_force_cleanup_occurred(cleanup_flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(cleanup_flags)];
    unsafe { native::invoke(0xC11469DCA6FC3BB5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_player_been_spotted_in_stolen_vehicle(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xC932F57F31EA9152, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_player_damaged_at_least_one_non_animal_ped(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x16C8D205DD5A2E90, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_player_damaged_at_least_one_ped(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xDA4A4B9B96E20092, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn int_to_participantindex(value: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(value)];
    unsafe { native::invoke(0x58FF971FC8F2702C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn int_to_playerindex(value: i32) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(value)];
    unsafe { native::invoke(0x748B3A65C2604C33, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_being_arrested(player: Player, at_arresting: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(at_arresting)];
    unsafe { native::invoke(0xC8183AE963C58374, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_climbing(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xB8A70C22FD48197A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_control_on(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x7964097FCE4C244B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_dead(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x2E9C3FCB6798F397, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_free_aiming(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x936F967D4BE1CE9D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_free_aiming_at_entity(player: Player, entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(entity)];
    unsafe { native::invoke(0x8C67C11C68713D25, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_playing(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xBFFB35986CAAE58C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_ready_for_cutscene(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xAA67BCB0097F2FA3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_riding_train(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x2FB0ACADA6A238DD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_script_control_on(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xB78350754157C00F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_targetting_anything(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x4605C66E0F935F83, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_targetting_entity(player: Player, entity: Entity, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(entity), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x27F89FDC16688A7A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_teleport_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x085EEAEB8783FEB5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_wanted_level_greater(player: Player, wanted_level: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(wanted_level)];
    unsafe { native::invoke(0xE1C0AD4C24324C36, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_system_ui_being_displayed() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x908258B6209E71F7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_player_id_to_int() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8A9386F0749A17FA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn player_id() -> Result<Player, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x217E9DC48139933D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn player_ped_id() -> Result<Ped, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x096275889B8E0EE0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn report_police_spotted_player(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xCBCCF73FFA69CC6B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_law_response_delay_override() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5CE5CACC01D0F985, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_player_arrest_state(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x12917931C31F1750, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_player_input_gait(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x61A2EECAB274829B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_wanted_level_difficulty(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x062D14F18E8B0CAE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn restore_player_stamina(player: Player, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(p1)];
    unsafe { native::invoke(0xC41F4B6E23FE6A4A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_air_drag_multiplier_for_players_vehicle(player: Player, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(multiplier)];
    unsafe { native::invoke(0x5DA6500FE849DA16, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_neutral_random_peds_flee_this_frame(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x16752DAA7E6D3F72, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_random_peds_flee(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE705309B8C6445A4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_random_peds_flee_this_frame(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xD5C198A62F1DEB0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_everyone_ignore_player(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x34630A768925B852, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_law_response_delay_override(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0xD2DFC9CCA5596A11, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_lockon_to_friendly_players(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x4A056257802DD3E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_max_wanted_level(max_wanted_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(max_wanted_level)];
    unsafe { native::invoke(0x28A4BD2CEE236E19, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_min_time_before_horse_bucking(mount: Ped, i_min_buck_time: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::int(i_min_buck_time)];
    unsafe { native::invoke(0x506CE71FB6E8CF5E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_as_temp_player_horse(player: Player, horse: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(horse)];
    unsafe { native::invoke(0x227B06324234FB09, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_can_be_hassled_by_gangs(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC7FE774412046825, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_can_use_cover(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x5EDA520F7A3BAF4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_cloth_pin_frames(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xD0D9317DFEEF9A66, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_control(player: Player, toggle: bool, flags: i32, b_prevent_heading_change: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle), NativeArg::int(flags), NativeArg::boolean(b_prevent_heading_change)];
    unsafe { native::invoke(0x4D51E59243281D80, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_forced_aim(player: Player, toggle: bool, ped: Ped, p3: i32, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle), NativeArg::int(ped), NativeArg::int(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xD5FCC166AEB2FD0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_health_recharge_multiplier(player: Player, regen_rate: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(regen_rate)];
    unsafe { native::invoke(0x8899C244EBCF70DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_invincible(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xFEBEEBC9CBDF4B12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_lockon(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x462AA1973CBBA75E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_lockon_range_override(player: Player, range: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(range)];
    unsafe { native::invoke(0x3A3CD06597388322, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_may_not_enter_any_vehicle(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xBEC463B3A11C909E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_may_only_enter_this_vehicle(player: Player, vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(vehicle)];
    unsafe { native::invoke(0xDA35A134038557EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_melee_weapon_damage_modifier(player: Player, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier)];
    unsafe { native::invoke(0xE4CB5A3F18170381, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_model(player: Player, model_hash: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(model_hash), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xED40380076A31506, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_noise_multiplier(player: Player, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(multiplier)];
    unsafe { native::invoke(0xB5EC6BDAEBCA454C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_simulate_aiming(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE0447DEF81CCDFD2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_sneaking_noise_multiplier(player: Player, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(multiplier)];
    unsafe { native::invoke(0x4DE44FA389DCA565, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_stamina_recharge_multiplier(player: Player, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(multiplier)];
    unsafe { native::invoke(0xFECA17CF3343694B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_targeting_mode(target_mode: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(target_mode)];
    unsafe { native::invoke(0xD66A941F401E7302, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_team(player: Player, team: i32, b_restrict_to_this_script: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(team), NativeArg::boolean(b_restrict_to_this_script)];
    unsafe { native::invoke(0xE8DD8536F01DE600, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_wanted_level(player: Player, wanted_level: i32, disable_no_mission: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(wanted_level), NativeArg::boolean(disable_no_mission)];
    unsafe { native::invoke(0x384D4765395E006C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_weapon_damage_modifier(player: Player, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier)];
    unsafe { native::invoke(0x94D529F7B73D7A85, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_weapon_defense_modifier(player: Player, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier)];
    unsafe { native::invoke(0xD15CC2D493160BE3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_weapon_type_damage_modifier(player: Player, weapon_hash: Hash, damage_modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(weapon_hash), NativeArg::float(damage_modifier)];
    unsafe { native::invoke(0xD04AD186CE8BB129, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_police_radar_blips(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6FD7DD6B63F2820E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_swim_multiplier_for_player(player: Player, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(multiplier)];
    unsafe { native::invoke(0xBFCEABDE34DA5085, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_wanted_level_multiplier(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0xD7FA719CB54866C2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn simulate_player_input_gait(player: Player, speed: f32, duration: i32, heading: f32, p4: bool, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(speed), NativeArg::int(duration), NativeArg::float(heading), NativeArg::boolean(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xFA0C063C422C4355, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_player_teleport(player: Player, x: f32, y: f32, z: f32, heading: f32, p5: bool, p6: bool, p7: bool, p8: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::boolean(p5), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0xDF8822C55EDDA65B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_player_teleport() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0858B86146601BE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn suppress_witnesses_calling_police_this_frame(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x96722257E5381E00, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_player_teleport(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xC39DCE4672CBFBC1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_wanted_position_this_frame(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xD0B0B044112BF424, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x00_b156_afebcc5_ae0(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x00B156AFEBCC5AE0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x03_b4_b759_a8990505(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x03B4B759A8990505, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x06_e1_fb78_b1_e59_ca5(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x06E1FB78B1E59CA5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x086549_f3_b0381_cb1(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x086549F3B0381CB1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0869_d499_a7848309(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0x0869D499A7848309, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x08_e22898_a6_af4905(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x08E22898A6AF4905, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_b7803_f6_f7_bb43_e0() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0B7803F6F7BB43E0, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_f4_eaf69_da41_af43(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0F4EAF69DA41AF43, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_f9_cf06986300875(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0F9CF06986300875, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x107_f2_a66_e1_c4_c83_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x107F2A66E1C4C83A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x113_ef458_ab6_cda67(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x113EF458AB6CDA67, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x12_e09_e278_c6_c29_b7(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x12E09E278C6C29B7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x14_e57_f88_ba0_a07_fc(location: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(location)];
    unsafe { native::invoke(0x14E57F88BA0A07FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x19_b2_c7_a6_c34_fad54(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x19B2C7A6C34FAD54, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_ad8_ad999_c27_f44_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1AD8AD999C27F44A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_d256_eed194_f5_b58(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1D256EED194F5B58, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_e8099_f449_abb0_ba(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1E8099F449ABB0BA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_f488807_bc8_e0630(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x1F488807BC8E0630, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_fda57_e8908_f2609(player: Player, ped: Ped, use_steerassist: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::boolean(use_steerassist)];
    unsafe { native::invoke(0x1FDA57E8908F2609, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x21091_b4_beb6376_ee(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x21091B4BEB6376EE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x216_bc0_d3_d2_e413_d2(player: Player, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), p1];
    unsafe { native::invoke(0x216BC0D3D2E413D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x22_b3_cabeddb538_b2(player: Player, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(p1)];
    unsafe { native::invoke(0x22B3CABEDDB538B2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x263_d69767_f76059_c(player: Player, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1)];
    unsafe { native::invoke(0x263D69767F76059C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_bb8_d58_e88777499(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2BB8D58E88777499, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_beed53_b912537_d0(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x2BEED53B912537D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_c2_d287748_e8_e9_b7(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x2C2D287748E8E9B7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_e1_abe627_c95_ed9_b() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2E1ABE627C95ED9B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_e67707_bec52_ca4_b(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2E67707BEC52CA4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x310_ce349_e0_c0_ec4_b(player: Player, ped: Ped, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::int(p2)];
    unsafe { native::invoke(0x310CE349E0C0EC4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x325434_c68358_d282(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x325434C68358D282, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x35_a33783_ec3_c3448(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x35A33783EC3C3448, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x39_d8_d7082_bc34_b72(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x39D8D7082BC34B72, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_a8611_bd7_bde84_f7(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3A8611BD7BDE84F7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_acac8832_e77_bc93(player: Player, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x3ACAC8832E77BC93, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_ad212429_e095_efb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3AD212429E095EFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_b296934_db026873(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3B296934DB026873, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_bb84_f812_e052_c90(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3BB84F812E052C90, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_c4_ae8506638_c7_e2(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3C4AE8506638C7E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_d9_da5_c9_efd20_d88(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3D9DA5C9EFD20D88, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_daabe78_a23694_bc(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3DAABE78A23694BC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x45_ef176_b532_ca851(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x45EF176B532CA851, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x497_a18_f8_f88_aa9_d8() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x497A18F8F88AA9D8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_d1699543_b1_c023_c(player: Player, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(p1)];
    unsafe { native::invoke(0x4D1699543B1C023C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_dbc4873707_e8_fd6(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x4DBC4873707E8FD6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_ec8_be63_b8_a5_d4_ef(player: Player, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1)];
    unsafe { native::invoke(0x4EC8BE63B8A5D4EF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_f0_d2256_aae94_eda(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x4F0D2256AAE94EDA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x51139_d8_c17_b16_fbc(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x51139D8C17B16FBC, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x57028_fd99886_f6_f9() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x57028FD99886F6F9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x570_a13_a4_ca2799_bb(player: Player, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x570A13A4CA2799BB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x57_d9991_dc1334151(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x57D9991DC1334151, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x585_ce159_db46_fadb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x585CE159DB46FADB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_b7_b97_e99_f84138_b(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5B7B97E99F84138B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_c2_e5_e3_caeeb1_f58(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x5C2E5E3CAEEB1F58, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x621_d1_b289_caf5978(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x621D1B289CAF5978, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x628_e742_fe1_f79_c4_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x628E742FE1F79C4A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x65887_eac535_a0_b0_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x65887EAC535A0B0C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x67659_a8_f248_e0141(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x67659A8F248E0141, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6852288340_b43239(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6852288340B43239, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x694_ffa4308060_cd1(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x694FFA4308060CD1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c54_e69516_cc56_bd(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6C54E69516CC56BD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_edb5_d08_cb03_e763(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6EDB5D08CB03E763, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x73_eb2_ef2_e92_d23_bf() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x73EB2EF2E92D23BF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x747257807_b8721_ce(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x747257807B8721CE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x76_f7_e1_bcd623_a429(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x76F7E1BCD623A429, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x77_b0_b6_d17_a3_ac9_aa(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x77B0B6D17A3AC9AA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x77_e83_c315_a3_b31_ca(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x77E83C315A3B31CA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_ae93_c45_ec14_a166(player: Player, ped: &mut Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(ped)];
    unsafe { native::invoke(0x7AE93C45EC14A166, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x818241_b3_eda84191(player: Player, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x818241B3EDA84191, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x83_c989_d5_b5_b5_b466(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x83C989D5B5B5B466, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x84481018_e668_e1_b8(player: Player, ped: Ped, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), p2];
    unsafe { native::invoke(0x84481018E668E1B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8591_ee69_cc3_ed257(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x8591EE69CC3ED257, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8702_d9150_d9_fbb3_d(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8702D9150D9FBB3D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_f44_ebb3_ba8_f6_d44(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8F44EBB3BA8F6D44, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9044835_be9_d9_dbfe(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9044835BE9D9DBFE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9073_ec5456651_a90(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9073EC5456651A90, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x908_d4_b72854_c8_f62(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x908D4B72854C8F62, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x927861_b2_c08_dbea5(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x927861B2C08DBEA5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x929_ddd5538_f3_df1_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x929DDD5538F3DF1F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x93624_b36_e8851_b42(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x93624B36E8851B42, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9422743_a5_ba50_e10(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9422743A5BA50E10, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9461_a8_fab0378_e5_b(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9461A8FAB0378E5B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_afcf9_fe1884_bf62(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9AFCF9FE1884BF62, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_fc5_a003_fb76_edbd(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9FC5A003FB76EDBD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a28056_cd1_b04_b250(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10];
    unsafe { native::invoke(0xA28056CD1B04B250, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a342495_f93_b7_b838(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA342495F93B7B838, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a54000_d4_bfd90_bde(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA54000D4BFD90BDE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_aaed694_ce814817_f(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xAAED694CE814817F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b15_cd2_f9932_c9_ab5(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xB15CD2F9932C9AB5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b331_d8_a73_f9_d2_bdf(player: Player, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xB331D8A73F9D2BDF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ba5_ca1_feb5_de0_df6(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xBA5CA1FEB5DE0DF6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bb6_ea5_d59_e926095(category: i32, emote: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(category), NativeArg::hash(emote)];
    unsafe { native::invoke(0xBB6EA5D59E926095, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bd96185264_ddaaea(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xBD96185264DDAAEA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bed386157_f65942_c(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xBED386157F65942C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_befed69_ce8317_f91(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xBEFED69CE8317F91, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c177_c827_cefc0_aa4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC177C827CEFC0AA4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c4873_b053054_c04_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0xC4873B053054C04B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c58_ce6824_e604_dec(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC58CE6824E604DEC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c71_d07_c96946_e263(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC71D07C96946E263, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c74_eb3_f2_ec169_f6_b(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC74EB3F2EC169F6B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c900_a465364_a85_d6(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xC900A465364A85D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c93_a9_a45430_d484_e(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC93A9A45430D484E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ca59808_e51_fd67_c4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCA59808E51FD67C4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cb0_b9506_bc91_e441(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCB0B9506BC91E441, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cb61_a63_aa53_d7_d22(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCB61A63AA53D7D22, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cd7_ca3013_fd12749(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCD7CA3013FD12749, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cddd4_b74660_e2335(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xCDDD4B74660E2335, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cedc16930526_f728(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCEDC16930526F728, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cfb2_eed4_fcb7_bd77(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xCFB2EED4FCB7BD77, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d1_f6_b912785_bfd35(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD1F6B912785BFD35, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d288_e02_e364972_d2(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xD288E02E364972D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_da9_d7_be231_fe865_f(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xDA9D7BE231FE865F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dab6_a2_fc56_b7_de65(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDAB6A2FC56B7DE65, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dd33_a82352_c4652_f(player: Player, ped: Ped, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::int(p2)];
    unsafe { native::invoke(0xDD33A82352C4652F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e1_d356_f5_a66_d0_ffa(emote: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(emote)];
    unsafe { native::invoke(0xE1D356F5A66D0FFA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e50_a67_c33514_a390(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE50A67C33514A390, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e7_f8707269544_b29(player: Player, ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped)];
    unsafe { native::invoke(0xE7F8707269544B29, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e956_c2340_a76272_e(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE956C2340A76272E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eacebaae0_a33_fb3_f(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xEACEBAAE0A33FB3F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ebb6_e27_ac2_ff32_da(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xEBB6E27AC2FF32DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ebff94328_ff7_a18_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xEBFF94328FF7A18A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f21_c7_a3_f3_ffba629(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xF21C7A3F3FFBA629, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f4_cb347_d7_b5_eb0_fd() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF4CB347D7B5EB0FD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f993373285053_d77(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xF993373285053D77, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa437_fa0738_c370_c(player: Player, p1: f32, p2: i32, p3: i32, p4: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(p1), NativeArg::int(p2), NativeArg::int(p3), NativeArg::int(p4)];
    unsafe { native::invoke(0xFA437FA0738C370C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa7_daae3959_e6_c7_b(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFA7DAAE3959E6C7B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ambient_player_interactive_focus_preset(player: Player, ped: Ped, preset: &str, x: f32, y: f32, z: f32, target_entity: Entity, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::string(preset), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(target_entity), NativeArg::string(name)];
    unsafe { native::invoke(0x3946FC742AC305CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ambient_player_interactive_focus_preset_at_coords(player: Player, x1: f32, y1: f32, z1: f32, preset: &str, x2: f32, y2: f32, z2: f32, target_entity: Entity, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::string(preset), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::int(target_entity), NativeArg::string(name)];
    unsafe { native::invoke(0xD48227263E3D06AE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_player_as_follow_target(player: Player, ped: Ped, p2: f32, p3: f32, follow_mode: i32, follow_priority: i32, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::float(p2), NativeArg::float(p3), NativeArg::int(follow_mode), NativeArg::int(follow_priority), NativeArg::boolean(p6)];
    unsafe { native::invoke(0xAC22AA6DF4D1C1DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_bounty_target(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x8F2A81C09DA9124A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_deadeye_aura_effect_intensity(player: Player, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(flag)];
    unsafe { native::invoke(0x0E9057A9DA78D0F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_eagle_eye_trails_for_player(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xE5D3EB37ABC1EB03, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_player_interactive_focus_preset(player: Player, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::string(name)];
    unsafe { native::invoke(0xC67A4910425F11F1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_add_focus_skill(entity: Entity, enable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(enable)];
    unsafe { native::invoke(0xBC02B3D151D3859F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_are_all_trails_hidden(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xA62BBAAE67A05BB0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_can_player_focus_on_track(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x1DA5C5B0923E1B85, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_disable_tracking_trail(entity: Entity, trail: &str, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::string(trail), p2, p3];
    unsafe { native::invoke(0x40AB73092C95B5F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_get_tracked_ped_id(player: Player) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x3813E11A378958A5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_remove_particle_fx_from_entity(entity1: Entity, entity2: Entity, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::int(p2)];
    unsafe { native::invoke(0xDC5E09D012D759C4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_color(player: Player, p1: bool, p2: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x2C41D93F550D5E37, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_custom_distance(entity: Entity, distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(distance)];
    unsafe { native::invoke(0x907B16B3834C69E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_drain_rate_modifier(player: Player, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier)];
    unsafe { native::invoke(0xE0D6C2A146A5C993, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_focus_on_associated_clue_trail(player: Player, linked_waypoint_ped: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(linked_waypoint_ped)];
    unsafe { native::invoke(0x2AF423D6ECB2C485, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_hide_all_trails(player: Player, hide_trails: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(hide_trails)];
    unsafe { native::invoke(0x330CA55A3647FA1C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_particle_fx_to_entity(entity1: Entity, entity2: Entity, p2: i32, heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity1), NativeArg::int(entity2), NativeArg::int(p2), NativeArg::float(heading)];
    unsafe { native::invoke(0x6ECFC621A168424C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_plus_flag_disabled(player: Player, disabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(disabled)];
    unsafe { native::invoke(0xCE285A4413B00B7F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_range(player: Player, range: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(range)];
    unsafe { native::invoke(0x22C8B10802301381, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_tracking_upgrade(player: Player, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(p1)];
    unsafe { native::invoke(0xDFC85C5199045026, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn eagle_eye_set_tracking_upgrade_2(player: Player, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(p1)];
    unsafe { native::invoke(0x6FA957D1B55941C1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_custom_deadeye_ability(player: Player, enable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(enable)];
    unsafe { native::invoke(0x95EE1DEE1DCD9070, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_eagleeye(player: Player, enable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(enable)];
    unsafe { native::invoke(0xA63FCAD3A6FEC6D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_rest_scenario(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE5A3DD2FF84E1A4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn format_player_name_string(string: &str) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::string(string)];
    unsafe { native::invoke(0x5B6193813E03E4E9, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_active_horse_for_player(player: Player) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x46FA0AE18F4C7FA9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ai_player_defense_modifier_against_ai(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x2E78D822208E740A, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_constructed_discovered_character_name(p0: Hash, model: bool, outfit: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::boolean(model), NativeArg::boolean(outfit)];
    unsafe { native::invoke(0x8E84119A23C16623, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_deadeye_ability_depletion_delay(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xE92261BD28C0878F, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_deadeye_ability_level(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xCCE7C695C164C35F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_has_player_discovered_character_name_sp(player: Player, p1: i32, discovery_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1), NativeArg::hash(discovery_hash)];
    unsafe { native::invoke(0x0772F87D7B07719A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_deadeye_tagging_enabled(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x32348719DCED2969, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_deadeye_marks_on_ped(player: Player, ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped)];
    unsafe { native::invoke(0x27AD7162D3FED01E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_marked_deadeye_targets(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xCCD9B77F70D31C9D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_peds_in_combat_with_recently(player: Player, recently_ms: i32, out_array: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(recently_ms), NativeArg::in_out(out_array)];
    unsafe { native::invoke(0x1A6E84F13C952094, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_cached_dead_eye_amount(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xDF66A37936D5F3D9, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_dead_eye(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xA81D24AE0AF99A5E, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_dead_eye_meter_level(player: Player, p1: bool) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x3A6AE4EEE30370FE, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_health(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0317C947D062854E, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_health_recharge_multiplier(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x22CD23BB0C45E0CD, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_hunting_wagon(player: Player) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x5CA6BBD4A7D8145E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_interaction_aim_entity(player: Player, out_entity: &mut Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::in_out(out_entity)];
    unsafe { native::invoke(0xBEA3A6E5F5F79A6F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_max_dead_eye(player: Player, p1: NativeArg) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player), p1];
    unsafe { native::invoke(0x592F58BC4D2A2CF3, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_mood(player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x054473164C012699, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_mount_is_sprinting_on_road(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xE631EAF35828FA67, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_owner_of_mount(mount: Ped) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(mount)];
    unsafe { native::invoke(0xAD03B03737CE6810, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_owner_of_vehicle(vehicle: Vehicle) -> Result<Player, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x7C803BDC8343228D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_ped_2(player: Player) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x5EBE38A20BC51C27, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_required_dead_eye_amount(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x811A748B1BE231BA, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_reset_flag(player: Player, player_reset_flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(player_reset_flag)];
    unsafe { native::invoke(0xFE691E89C08937B6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_special_ability_multiplier(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xAB3773E7AA1E9DCC, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_stamina(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0FF421E467373FCF, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_stamina_depletion_multiplier(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x68A0389E0718AC8F, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_stamina_recharge_multiplier(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x617D3494AD58200F, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_ui_prompt_for_ped_is_enabled(player: Player, ped: Ped, prompt_type: i32, prompt_mode: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::int(prompt_type), NativeArg::int(prompt_mode)];
    unsafe { native::invoke(0xEA8F168A76A0B9BC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_ui_prompt_is_disabled(player: Player, prompt_type: i32, prompt_mode: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(prompt_type), NativeArg::int(prompt_mode)];
    unsafe { native::invoke(0x6614F9039BD31931, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_weapon_damage(player: Player, weapon_hash: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xFE0304050261442C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_saddle_horse_for_player(player: Player) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xB48050D326E9A2F3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_temp_player_horse(player: Player) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xD3F7445CEA2E5035, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_owned_by_player(player: Player) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xB9050A97594C8832, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_wanted_level_multiplier() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA82964B9D8D6A983, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_player_damaged_recently_attacked_ped(player: Player, recently_ms: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(recently_ms)];
    unsafe { native::invoke(0x72AD59F7B7FB6E24, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_deadeye_ability_locked(player: Player, ability_type: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ability_type)];
    unsafe { native::invoke(0x8A0643B0B4CA276B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_eagle_eye_registered_for_entity(player: Player, entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(entity)];
    unsafe { native::invoke(0x0E6846476906C9DD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_following_target(player: Player, ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped)];
    unsafe { native::invoke(0xE24C64D9ADED2EF5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_free_focusing(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x1A51BFE60708E482, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_in_scope(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x04D7F33640662FA2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_locked_on_entity_on_horse(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x2009F8AB7A5E9D6D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_secondary_special_ability_active(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x45AB66D02B601FA7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_secondary_special_ability_enabled(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xE022CC1B545F1D9F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_special_ability_active(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xB16223CB7DA965F0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_special_ability_enabled(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xDE6C85975F9D4894, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn modify_infinite_trail_vision(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x28A13BF6B05C3D83, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn modify_player_discovered_character_name_mp_set_undiscovered(discovery_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(discovery_hash)];
    unsafe { native::invoke(0xFB0E622B401884D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn modify_player_ui_prompt(player: Player, prompt_type: i32, prompt_mode: i32, disabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(prompt_type), NativeArg::int(prompt_mode), NativeArg::boolean(disabled)];
    unsafe { native::invoke(0x0751D461F06E41CE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn modify_player_ui_prompt_for_ped(player: Player, ped: Ped, prompt_type: i32, prompt_mode: i32, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped), NativeArg::int(prompt_type), NativeArg::int(prompt_mode), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0xA3DB37EDF9A74635, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn network_has_player_valid_ped(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0760D6F70EBCC05C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_eagle_eye_for_entity(player: Player, entity: Entity, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(entity), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x543DFE14BE720027, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_eagle_eye_trails_for_entity(player: Player, entity: Entity, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(entity), p2];
    unsafe { native::invoke(0xAC67098A1E54ABB0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_player_as_follow_target(player: Player, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ped)];
    unsafe { native::invoke(0x0C6B89876262A99D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_deadeye_aura_effect(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xE910932F4B30BE23, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn secondary_special_ability_set_active(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x1710BC33CFB83634, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn secondary_special_ability_set_disabled(player: Player, disabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(disabled)];
    unsafe { native::invoke(0x64FF4BF9AF59E139, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ai_player_defense_modifier_against_ai(player: Player, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier)];
    unsafe { native::invoke(0x914071FF93AF2692, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_bounty_target(player: Player, target: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(target)];
    unsafe { native::invoke(0x6ADF821FBF21920E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_bow_draw_reduction_time_in_deadeye(player: Player, draw_reduction_time: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(draw_reduction_time)];
    unsafe { native::invoke(0xBE0C524970892D41, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_bow_stamina_drain_speed(player: Player, stamina_drain: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(stamina_drain)];
    unsafe { native::invoke(0xFE7C9CF376D23342, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_damage_close_distance_bonus(player: Player, close_range_lower_bound: f32, close_range_upper_bound: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(close_range_lower_bound), NativeArg::float(close_range_upper_bound)];
    unsafe { native::invoke(0x7761A30432C91297, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_damage_close_distance_bonus_total(player: Player, close_damage_bonus: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(close_damage_bonus)];
    unsafe { native::invoke(0x5006C36652D6EC56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_damage_far_distance_bonus(player: Player, far_range_lower_bound: f32, far_range_upper_bound: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(far_range_lower_bound), NativeArg::float(far_range_upper_bound)];
    unsafe { native::invoke(0xED591CB17C8BA216, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_damage_far_distance_bonus_total(player: Player, far_damage_bonus: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(far_damage_bonus)];
    unsafe { native::invoke(0x1F0E3A4434565F8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_deadeye_ability_depletion_delay(player: Player, delay: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(delay)];
    unsafe { native::invoke(0x870634493CB4372C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_deadeye_ability_level(player: Player, level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(level)];
    unsafe { native::invoke(0xF0FE8E790BFEB5BB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_deadeye_ability_locked(player: Player, ability_type: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ability_type), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x2797B8D66DD0EBB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_deadeye_entity_aura_effect(player: Player, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(flag)];
    unsafe { native::invoke(0x2B12B6FC8B8772AB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_deadeye_entity_aura_effect_intensity(player: Player, p1: f32, p2: f32, p3: f32, aura_intensity: f32, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(aura_intensity), NativeArg::int(flag)];
    unsafe { native::invoke(0x131E294EF60160DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_deadeye_tagging_config(player: Player, filter: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(filter)];
    unsafe { native::invoke(0x83FCD6921FC8FD05, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_deadeye_tagging_enabled(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6B5DDFB967E5073D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_player_wanted_level(player: Player, disable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(disable)];
    unsafe { native::invoke(0x8674D138391FFB1B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_local_player_persona_ability_flag(flag_id: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(flag_id), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x7146CF430965927C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_lockon_focus_fire_vfx(player: Player, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::string(p1)];
    unsafe { native::invoke(0x5F8E0303C229C84B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_max_wanted_level_2(max_wanted_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(max_wanted_level)];
    unsafe { native::invoke(0xEA6DE0CD15AECBE2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mount_prompt_disabled(disabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(disabled)];
    unsafe { native::invoke(0x5B9813ECF7633FE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_active_player_horse(player: Player, horse: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(horse)];
    unsafe { native::invoke(0x8FBF9EDB378CCB8C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_as_saddle_horse_for_player(player: Player, mount: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(mount)];
    unsafe { native::invoke(0xD2CB0FB0FDCB473D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_aim_weapon(player: Player, weapon: Hash, weapon_draw_order: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(weapon), NativeArg::int(weapon_draw_order)];
    unsafe { native::invoke(0xCFFC3ECCD7A5CCEB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_can_mercy_kill(player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x39363DFD04E91496, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_can_pickup_ability(player: Player, is_visible: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(is_visible)];
    unsafe { native::invoke(0xD1A70C1E8D1031FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_can_pickup_hat(player: Player, enable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(enable)];
    unsafe { native::invoke(0xACA45DDCEF6071C4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_cooperate_prompt_this_frame(player: Player, target_ped: Ped, prompt_order: i32, unknown_flag: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(target_ped), NativeArg::int(prompt_order), NativeArg::boolean(unknown_flag)];
    unsafe { native::invoke(0xCBB54CC7FFFFAB86, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_damage_info_override(player: Player, damage_info: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::string(damage_info)];
    unsafe { native::invoke(0x78B3D19AF6391A55, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_dead_eye_aura_by_hash(player: Player, aura_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(aura_hash)];
    unsafe { native::invoke(0x768E81AE285A4B67, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_defense_modifier(player: Player, weapon_defense_mod: f32, melee_defense_mod: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(weapon_defense_mod), NativeArg::float(melee_defense_mod)];
    unsafe { native::invoke(0x497A6539BB0E8787, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_defense_type_modifier(player: Player, type_: i32, defense_modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(type_), NativeArg::float(defense_modifier)];
    unsafe { native::invoke(0x93F499CAE53FCD05, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_explosive_weapon_damage_modifier(player: Player, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier)];
    unsafe { native::invoke(0x2D3ACE3DE0A2B622, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_has_discovered_character_name_mp(discovery_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(discovery_hash)];
    unsafe { native::invoke(0x7C32191D9FB2BDEA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_has_discovered_character_name_sp(player: Player, p1: i32, discovery_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1), NativeArg::hash(discovery_hash)];
    unsafe { native::invoke(0x946D46CD6DFB9742, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_hat_access(player: Player, flag: i32, enable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(flag), NativeArg::boolean(enable)];
    unsafe { native::invoke(0xA0C683284DF027C7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_health_recharge_time_modifier(player: Player, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier)];
    unsafe { native::invoke(0x535ED4605F89AB6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_hunting_wagon(player: Player, wagon: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(wagon)];
    unsafe { native::invoke(0x6A4404BDFA62CE2C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_interaction_negative_response(player: Player, speech: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::string(speech)];
    unsafe { native::invoke(0x98CD760DE43B612E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_interaction_positive_response(player: Player, speech: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::string(speech)];
    unsafe { native::invoke(0xC6366A585659D15C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_in_vehicle_targeting_mode(target_mode: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(target_mode)];
    unsafe { native::invoke(0x19B4F71703902238, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_lasso_damage_per_second(player: Player, damage: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(damage)];
    unsafe { native::invoke(0x43F50A7CD2482156, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_local_accuracy_floor_modifier(player: Player, accuracy: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(accuracy)];
    unsafe { native::invoke(0x4EA69188FBCE6A7D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_manage_buff_super_jump(player: Player, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(p1)];
    unsafe { native::invoke(0x292F0B6EDC82E3A4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_max_ammo_override_for_ammo_type(player: Player, ammo_type: Hash, amount: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(ammo_type), NativeArg::int(amount)];
    unsafe { native::invoke(0xE133C1EC5300F740, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_mood(player: Player, mood: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(mood)];
    unsafe { native::invoke(0x39BED552DB46FFA9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_mount_state_active(player: Player, active: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(active)];
    unsafe { native::invoke(0xDF93973251FB2CA5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_owns_mount(player: Player, mount: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(mount)];
    unsafe { native::invoke(0xE6D4E435B56D5BD0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_owns_vehicle(player: Player, vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(vehicle)];
    unsafe { native::invoke(0xD0E02AA618020D17, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_prompt_leave_text(player: Player, prompt_text_key: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::string(prompt_text_key)];
    unsafe { native::invoke(0x06C3DB00B69D5435, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_prompt_melee_text(player: Player, prompt_text_key: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::string(prompt_text_key)];
    unsafe { native::invoke(0x0FAF95D71ED67ADE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_prompt_sit_text(player: Player, prompt_text_key: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::string(prompt_text_key)];
    unsafe { native::invoke(0x988C9045531B9FCE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_remote_accuracy_floor_modifier(player: Player, accuracy: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(accuracy)];
    unsafe { native::invoke(0xDEE80FEDFDD43C9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_reset_flag(player: Player, player_reset_flag: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(player_reset_flag), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x9F9A829C6751F3C7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_stamina_sprint_depletion_multiplier(player: Player, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(multiplier)];
    unsafe { native::invoke(0xBBADFB5E5E5766FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_total_accuracy_modifier(player: Player, accuracy: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(accuracy)];
    unsafe { native::invoke(0x967FF5BC0CFE6D26, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_trample_damage_modifier(player: Player, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier)];
    unsafe { native::invoke(0xAF341032E97FB061, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_weapon_group_as_instant_kill(player: Player, weapon_group: Hash, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(weapon_group), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x59F0AFF3E0A1B019, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_weapon_group_damage_modifier(player: Player, weapon_group: Hash, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(weapon_group), NativeArg::float(modifier)];
    unsafe { native::invoke(0xFC79DCC94D0A5897, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_received_damage_taken_on_horseback_modifier(player: Player, damage_decrease: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(damage_decrease)];
    unsafe { native::invoke(0xB427911EA6DFFEF3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_show_info_card(player: Player, showing_info_card: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(showing_info_card)];
    unsafe { native::invoke(0xDC68829BB3F37023, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_special_ability_activation_cost(player: Player, activation_cost: f32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(activation_cost), NativeArg::int(p2)];
    unsafe { native::invoke(0xAE4BCC79C587EBBF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_special_ability_disable_timer(player: Player, timer: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(timer)];
    unsafe { native::invoke(0xC0B1C05B313693D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_special_ability_duration_cost(player: Player, duration_cost: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(duration_cost)];
    unsafe { native::invoke(0xB783F75940B23014, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_special_ability_multiplier(player: Player, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(multiplier)];
    unsafe { native::invoke(0x5A498FCA232F71E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_special_ability_type(player: Player, type_: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(type_)];
    unsafe { native::invoke(0x00BA333DA05ADC23, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_used_item_effect(health: f32, stamina: f32, deadeye: f32, health_core: i32, stamina_core: i32, deadeye_core: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(health), NativeArg::float(stamina), NativeArg::float(deadeye), NativeArg::int(health_core), NativeArg::int(stamina_core), NativeArg::int(deadeye_core)];
    unsafe { native::invoke(0x0E1DB1F8F5B561DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weapon_degradation_modifier(player: Player, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(modifier)];
    unsafe { native::invoke(0x11A7FF918EF6BC66, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weapon_draw_speed(player: Player, weapon_hash: Hash, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::hash(weapon_hash), NativeArg::float(modifier)];
    unsafe { native::invoke(0x00EB5A760638DB55, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn special_ability_drain_by_amount(player: Player, amount: f32, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(amount), p2];
    unsafe { native::invoke(0x200114E99552462B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn special_ability_get_amount_cached(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x029884FB65821B07, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn special_ability_restore_by_amount(player: Player, amount: f32, p2: i32, p3: i32, p4: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(amount), NativeArg::int(p2), NativeArg::int(p3), NativeArg::int(p4)];
    unsafe { native::invoke(0x51345AE20F22C261, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn special_ability_restore_outer_ring(player: Player, amount: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::float(amount)];
    unsafe { native::invoke(0x2498035289B5688F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn special_ability_set_activate(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xBBA140062B15A8AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn special_ability_set_disabled(player: Player, disabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(disabled)];
    unsafe { native::invoke(0xAE637BB8EF017875, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn special_ability_set_eagle_eye_disabled(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xC0B21F235C02139C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn special_ability_start_restore(player: Player, ability_type: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(ability_type), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x1D77B47AFA584E90, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unregister_eagle_eye_for_entity(player: Player, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(entity)];
    unsafe { native::invoke(0x9DAE1380CC5C6451, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unregister_eagle_eye_trails_for_entity(player: Player, entity: Entity, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(entity), p2];
    unsafe { native::invoke(0x9A957912CE2EABD1, &arguments) }.map(|_| ())
}

// ============================================================================
// POPULATION
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_spawner_info_priority(p0: Hash, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::hash(p1)];
    unsafe { native::invoke(0x217A54DE2D200305, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_ambient_road_population(unk: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(unk)];
    unsafe { native::invoke(0xC6DCC2A3A8825C85, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_ambient_road_population() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBC90BDF4E5228EA1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_models_in_population_set(pop_set_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(pop_set_hash)];
    unsafe { native::invoke(0xA1E3171ED0E47564, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_model_name_in_population_set(pop_set_hash: Hash, index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(pop_set_hash), NativeArg::int(index)];
    unsafe { native::invoke(0x3EAFA1C533B7139E, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_random_model_from_population_set(pop_set_hash: Hash, flags: i32, p2: Hash, p3: bool, p4: bool, x: f32, y: f32, z: f32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(pop_set_hash), NativeArg::int(flags), NativeArg::hash(p2), NativeArg::boolean(p3), NativeArg::boolean(p4), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x6B12ED8C77E8567B, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_popzone_population_set(pop_zone: PopZone, population_set_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pop_zone), NativeArg::hash(population_set_hash)];
    unsafe { native::invoke(0x3E6A49D9B519E85C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_spawner_info_priority(p0: Hash, p1: Hash, priority: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::hash(p1), NativeArg::int(priority)];
    unsafe { native::invoke(0x60CDE717A6D47769, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x08892122769770_d5(pop_zone: PopZone, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pop_zone), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x08892122769770D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_f1861101_c9_a9944(pop_zone: PopZone, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pop_zone), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x0F1861101C9A9944, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2161278_c6322_f740(include_flags: i32, exclude_flags: i32, p2: i32, p3: Hash, p4: i32, volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(include_flags), NativeArg::int(exclude_flags), NativeArg::int(p2), NativeArg::hash(p3), NativeArg::int(p4), NativeArg::int(volume)];
    unsafe { native::invoke(0x2161278C6322F740, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x247_f86595_d396344(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x247F86595D396344, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2660_e7720_edc4_bd0(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x2660E7720EDC4BD0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x324_ab2_a68_ad8_aee5() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x324AB2A68AD8AEE5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x578_e2_fa64_e847_c60(pop_zone: PopZone, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pop_zone), NativeArg::int(p1)];
    unsafe { native::invoke(0x578E2FA64E847C60, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x638_fcfc6042_a9473(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x638FCFC6042A9473, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_e6_bc0_b94_f5928_f0(pop_zone: PopZone, p1: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pop_zone), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x7E6BC0B94F5928F0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_ec7_cd701_f872_f87(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x8EC7CD701F872F87, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c4533_e3_e87125_c9_e(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC4533E3E87125C9E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dbbf12_ea7_c1029_b2(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDBBF12EA7C1029B2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec116_edb683_ad479(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0xEC116EDB683AD479, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f45_e46_deecf7_df6_e(bit_flag: i32, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(bit_flag), p1, p2, p3, p4];
    unsafe { native::invoke(0xF45E46DEECF7DF6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ambient_avoidance_restriction(volume: Volume, include_flags: i32, exclude_flags: i32, p3: Hash, p4: Hash, p5: Hash, p6: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(include_flags), NativeArg::int(exclude_flags), NativeArg::hash(p3), NativeArg::hash(p4), NativeArg::hash(p5), NativeArg::int(p6)];
    unsafe { native::invoke(0xB56D41A694E42E86, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ambient_spawn_restriction(volume: Volume, include_flags: i32, exclude_flags: i32, p3: Hash, p4: Hash, p5: Hash, p6: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(include_flags), NativeArg::int(exclude_flags), NativeArg::hash(p3), NativeArg::hash(p4), NativeArg::hash(p5), NativeArg::int(p6)];
    unsafe { native::invoke(0x18262CAFEBB5FBE1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_popzone_from_volume(volume: Volume) -> Result<PopZone, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x9AC1C64FE46B6D09, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_script_popzone(pop_zone: PopZone) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(pop_zone)];
    unsafe { native::invoke(0xA6E6A66FC4CA4224, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_random_fish_type_for_location() -> Result<Hash, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x595478B3BBC3076D, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_popzone_valid(pop_zone: PopZone) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(pop_zone)];
    unsafe { native::invoke(0xA5BD585005EFCAD4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ambient_avoidance_restriction(volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x74C2B3DC0B294102, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ambient_spawn_restriction(volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0xA1CFB35069D23C23, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_should_ignore_avoidance_volumes(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xF74E134F40192884, &arguments) }.map(|_| ())
}

// ============================================================================
// POSSE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn posse_get_posse_membership_count() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC088FF658B2E51DA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c06_cff658_b2_e51_da(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xC06CFF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c07_cff658_b2_e51_da(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC07CFF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c084_ff658_b2_e52_da(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC084FF658B2E52DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c084_ff658_b2_e53_da() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC084FF658B2E53DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c084_ff658_b2_e54_da(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC084FF658B2E54DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c084_ff658_b2_e55_da(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC084FF658B2E55DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c084_ff658_b2_e61_da(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC084FF658B2E61DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c084_ff658_b2_e71_da(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xC084FF658B2E71DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c084_ff658_b2_e81_da(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xC084FF658B2E81DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c086_ff658_b2_e51_da(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC086FF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c086_ff658_b2_e51_db() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC086FF658B2E51DB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c087_ff658_b2_e51_da(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC087FF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c089_ff658_b2_e51_da(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC089FF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_aff658_b2_e51_da(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC08AFF658B2E51DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_aff658_b2_e51_db(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC08AFF658B2E51DB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_bff658_b2_e51_da(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC08BFF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_cff658_b2_e51_da(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC08CFF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_def658_b2_e51_da(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC08DEF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_dff658_b2_e51_da() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC08DFF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_dff658_b2_e51_db(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC08DFF658B2E51DB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_eff658_b2_e51_db(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC08EFF658B2E51DB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_fff658_b2_e51_da() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC08FFF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c08_fff658_b2_e51_db(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC08FFF658B2E51DB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c09_cff658_b2_e51_da(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xC09CFF658B2E51DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c184_ff658_b2_e55_da(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xC184FF658B2E55DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c284_ff658_b2_e55_da(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xC284FF658B2E55DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c394_ff658_b2_e55_da(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xC394FF658B2E55DA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c484_ff658_b2_e55_da(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC484FF658B2E55DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c584_ff658_b2_e55_da(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC584FF658B2E55DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c684_ff658_b2_e55_da(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC684FF658B2E55DA, &arguments) }.map(|_| ())
}

// ============================================================================
// PROPSET
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_prop_set_instance_attached_to_entity(hash: Hash, x: f32, y: f32, z: f32, entity: Entity, p5: f32, p6: bool, p7: i32, p8: bool) -> Result<PropSet, NativeError> {
    let arguments = [NativeArg::hash(hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(entity), NativeArg::float(p5), NativeArg::boolean(p6), NativeArg::int(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0x9609DBDDE18FAD8C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_prop_set_exist(prop_set: PropSet) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prop_set)];
    unsafe { native::invoke(0x7DDDCF815E650FF5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_prop_set_fully_loaded(prop_set: PropSet) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prop_set)];
    unsafe { native::invoke(0xF42DB680A8B2A4D9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x58_e0_b01_d45_ca7357(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x58E0B01D45CA7357, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_additional_prop_set_for_vehicle(vehicle: Vehicle, propset: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::hash(propset)];
    unsafe { native::invoke(0x75F90E4051CC084C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_light_prop_set_to_vehicle(vehicle: Vehicle, light_propset: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::hash(light_propset)];
    unsafe { native::invoke(0xC0F0417A90402742, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_prop_set_for_vehicle(vehicle: Vehicle, propset: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::hash(propset)];
    unsafe { native::invoke(0xD80FAF919A2E56EA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_prop_set(propset_type: Hash, x: f32, y: f32, z: f32, placement_type: i32, heading: f32, z_probe: f32, p7: bool, use_veg_mod: bool) -> Result<PropSet, NativeError> {
    let arguments = [NativeArg::hash(propset_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(placement_type), NativeArg::float(heading), NativeArg::float(z_probe), NativeArg::boolean(p7), NativeArg::boolean(use_veg_mod)];
    unsafe { native::invoke(0xE65C5CBA95F0E510, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_prop_set_2(propset_type: Hash, x: f32, y: f32, z: f32, placement_type: i32, heading: f32, z_probe: f32, p7: bool, use_veg_mod: bool) -> Result<PropSet, NativeError> {
    let arguments = [NativeArg::hash(propset_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(placement_type), NativeArg::float(heading), NativeArg::float(z_probe), NativeArg::boolean(p7), NativeArg::boolean(use_veg_mod)];
    unsafe { native::invoke(0x899C97A1CCE7D483, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_prop_set_instance_attached_to_entity_2(hash: Hash, x: f32, y: f32, z: f32, entity: Entity, p5: f32, p6: bool, p7: i32, p8: bool) -> Result<PropSet, NativeError> {
    let arguments = [NativeArg::hash(hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(entity), NativeArg::float(p5), NativeArg::boolean(p6), NativeArg::int(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0xACA7FB30269096D4, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_prop_set(prop_set: PropSet, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prop_set), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x58AC173A55D9D7B4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_prop_set_of_type_exist_near_coords(propset_hash: Hash, x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(propset_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x72068021F498E6E3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_vehicle_have_any_light_prop_set(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xC9B4B3A36F81FD75, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_vehicle_have_any_prop_set(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x53784CEA0159439B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_entities_from_prop_set(prop_set: PropSet, item_set: ItemSet, model: Hash, p3: bool, p4: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(prop_set), NativeArg::int(item_set), NativeArg::hash(model), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x738271B660FE0695, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_prop_set_at_coords(propset_hash: Hash, x: f32, y: f32, z: f32) -> Result<PropSet, NativeError> {
    let arguments = [NativeArg::hash(propset_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xC061E50F8D299F95, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_prop_set_model(prop_set: PropSet) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(prop_set)];
    unsafe { native::invoke(0xA6A9712955F53D9C, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_carriage_prop_set(train_carriage: Entity) -> Result<PropSet, NativeError> {
    let arguments = [NativeArg::int(train_carriage)];
    unsafe { native::invoke(0xCFC0BD09BB1B73FF, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_light_prop_set(vehicle: Vehicle) -> Result<PropSet, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xA079300AF757FB1A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_prop_set(vehicle: Vehicle) -> Result<PropSet, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xCE2ACD6F602803E5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_prop_set_hash(vehicle: Vehicle) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x36F69E7A22655653, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_prop_set_loaded(hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0x48A88FC684C55FDC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_prop_set_loaded_2(hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xD090ABEF4D6A7D96, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_vehicle_trailer_prop_set_loaded(vehicle: Vehicle, wagon_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(wagon_index)];
    unsafe { native::invoke(0x8F3333F0A6900B3C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_prop_set_visible(prop_set: PropSet) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(prop_set)];
    unsafe { native::invoke(0x0CE8AAFE9E433A23, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_light_prop_set_loaded(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x0790473EEE1977D3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_prop_set_loaded(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x155B2FBE72D7D1D0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_prop_set_loaded_additional(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x7264F9CA87A9830B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn move_propset_coords_and_heading(prop_set: PropSet, x: f32, y: f32, z: f32, on_ground_properly: bool, heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prop_set), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(on_ground_properly), NativeArg::float(heading)];
    unsafe { native::invoke(0xC4B67EF3FD65622D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_prop_set(hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xB1964A83B345B4AB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_vehicle_light_prop_sets(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xE31C0CB1C3186D40, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_vehicle_prop_sets(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x3BCF32FF37EA9F1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_prop_set(hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xF3DE57A46D5585E9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_prop_set_2(hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xE72F591958F3ACAB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_prop_set_as_no_longer_needed(prop_set: PropSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prop_set)];
    unsafe { native::invoke(0x909E3C7FAE539FB1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_prop_set_flag(prop_set: PropSet, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prop_set), NativeArg::int(flag)];
    unsafe { native::invoke(0xC1AB7EEFD3E6EE49, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_prop_set_visible(prop_set: PropSet, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(prop_set), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x9D096A5BD02F953E, &arguments) }.map(|_| ())
}

// ============================================================================
// QUEUE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn event_queue_is_empty(hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0x402B5D7D269FF796, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn event_queue_pop(hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0xD87DF294B049211D, &arguments) }.map(|_| ())
}

// ============================================================================
// RECORDING
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn replay_prevent_recording_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA8C44C13419634F2, &arguments) }.map(|_| ())
}

// ============================================================================
// REPLAY
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn close_video_editor(p0: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCEEC64BD27A59312, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_video_editor_running() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9EEB007317FA3B9C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn open_video_editor() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB3F2829907403C13, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn replay_system_has_requested_a_script_cleanup() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0F838D47DE58EDB2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scripts_have_cleaned_up_for_replay_system() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x57C6525034E76EB0, &arguments) }.map(|_| ())
}

// ============================================================================
// SAVE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_is_save_pending() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3CF46F55C6585590, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_save_mp(savegame_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(savegame_type)];
    unsafe { native::invoke(0x1840F3B30ED0105F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_save_sp(savegame_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(savegame_type)];
    unsafe { native::invoke(0x62C9EB51656D68CE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x443174_c20_b8_b9_e7_f(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x443174C20B8B9E7F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_fb5869_e2_b37_fc00() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4FB5869E2B37FC00, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x81_f4_e92_be3958364(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x81F4E92BE3958364, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_e8_ffb9_e4_ad051_d2(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x8E8FFB9E4AD051D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_bb83_c4_dd7_be0802(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x9BB83C4DD7BE0802, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a7_ecebafbaf997_a5(savegame_type: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(savegame_type)];
    unsafe { native::invoke(0xA7ECEBAFBAF997A5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a844_feb5_c22_c2_c74() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA844FEB5C22C2C74, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b00_ce33465_b5406_d(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB00CE33465B5406D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c0_abf784590798_a9(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC0ABF784590798A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e0_b45_e983_bfc0768() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE0B45E983BFC0768, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e8346_e62_fd7_fb962() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE8346E62FD7FB962, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ed4_b0_c1057892_b2_e(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xED4B0C1057892B2E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_get_bool(p0: &mut Any, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::string(variable_name)];
    unsafe { native::invoke(0xBB7F4273C186BC4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_get_float(p0: &mut Any, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x35DEFECAE36D4FAE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_get_int(p0: &mut Any, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x529B9CCD0972AF4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_get_int_2(p0: &mut Any, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x529B9CCD0972AF4D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_get_int_3(p0: &mut Any, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::string(variable_name)];
    unsafe { native::invoke(0xB25B5A375BE5BE26, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_get_text_label_23(p0: &mut Any, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x5A10D6506B2F2C63, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_get_text_label_31(p0: &mut Any, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x4845E7E7643A908C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_get_text_label_63(p0: &mut Any, variable_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::string(variable_name)];
    unsafe { native::invoke(0x186608A2AC6F9E88, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn savegame_has_save_failed() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1431540BCA1A1BD2, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// SCRIPTS
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn awards_get_result_item(rpc_guid: &mut Any, award_hash: Hash, item_index: i32, out_result_item: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(rpc_guid), NativeArg::hash(award_hash), NativeArg::int(item_index), NativeArg::in_out(out_result_item)];
    unsafe { native::invoke(0xAC8FAB22A914AE34, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bail_to_landing_page(bail_code: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(bail_code)];
    unsafe { native::invoke(0xBC2C927F5C264243, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bail_with_pass_through_params(params: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(params)];
    unsafe { native::invoke(0xE98204D3C25AE14C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_does_launch_param_exist(script_index: i32, p1: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(script_index), NativeArg::string(p1)];
    unsafe { native::invoke(0x4AE1DFF337A86FDE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_end_context(context_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(context_name)];
    unsafe { native::invoke(0x3ABF7BA1C3E2C8CF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_end_context_hash(context_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(context_hash)];
    unsafe { native::invoke(0x6D1431744182CDE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_get_launch_param_value(script_index: i32, p1: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(script_index), NativeArg::string(p1)];
    unsafe { native::invoke(0x55C40B7592BAD213, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_get_script_id_from_name_hash(p0: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x829CD22E043A2577, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_is_exitflag_set() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2238EC3EC631AB1F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_set_exitflag_response() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4858148E3B8A75D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_start_context(context_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(context_name)];
    unsafe { native::invoke(0x49BA5678BA040CA7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_start_context_hash(context_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(context_hash)];
    unsafe { native::invoke(0x2EB67D564DCC09D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn count_participant_bits(value: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(value)];
    unsafe { native::invoke(0x2F050A3FF8738245, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn count_player_bits(value: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(value)];
    unsafe { native::invoke(0x462C687BEA254BD9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_script_exist(script_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(script_name)];
    unsafe { native::invoke(0x552B171E3F69E5AE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_script_with_name_hash_exist(script_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(script_hash)];
    unsafe { native::invoke(0xA34E89749F628284, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_thread_exist(thread_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(thread_id)];
    unsafe { native::invoke(0xFF975BC4435A0FA3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_block_of_player_bits(value: &mut Any, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(value), NativeArg::int(p1)];
    unsafe { native::invoke(0xFA3B530A5CC693D5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_event_at_index(event_group: i32, event_index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(event_group), NativeArg::int(event_index)];
    unsafe { native::invoke(0xA85E614430EFF816, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_event_data(event_group: i32, event_index: i32, event_data: &mut Any, event_data_size: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(event_group), NativeArg::int(event_index), NativeArg::in_out(event_data), NativeArg::int(event_data_size)];
    unsafe { native::invoke(0x57EC5FA4D4D6AFCA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_event_exists(event_group: i32, event_type: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(event_group), NativeArg::hash(event_type)];
    unsafe { native::invoke(0xC9F59C0A710ECD34, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_hash_of_this_script_name() -> Result<Hash, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBC2C927F5C264960, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_id_of_this_thread() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x55525C346BEF6960, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_no_loading_screen() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x323DAF00687E0F28, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_number_of_events(event_group: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(event_group)];
    unsafe { native::invoke(0x5CE8DE5909565748, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_number_of_threads_running_the_script_with_this_hash(script_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(script_hash)];
    unsafe { native::invoke(0x8E34C953364A76DD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_thread_existence_details(thread_id: i32, thread_exists: &mut i32, has_script_handler: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(thread_id), NativeArg::in_out(thread_exists), NativeArg::in_out(has_script_handler)];
    unsafe { native::invoke(0xD92FA81B64920E85, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_script_loaded(script_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(script_name)];
    unsafe { native::invoke(0xE97BD36574F8B0A6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_script_with_name_hash_loaded(script_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(script_hash)];
    unsafe { native::invoke(0xA5D8E0C2F3C7EEBC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn have_all_child_scripts_terminated(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x380FFA15B72408FB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_loading_screen_visible() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB54ADBE65D528FCB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_thread_active(thread_id: i32, ignore_killed_state: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(thread_id), NativeArg::boolean(ignore_killed_state)];
    unsafe { native::invoke(0x46E9AE36D8FA6417, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_thread_exit_requested() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9E4EF615E307FBBE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_script(script_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(script_name)];
    unsafe { native::invoke(0x46ED607DDD40D7FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_script_with_name_hash(script_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(script_hash)];
    unsafe { native::invoke(0xF6B9CE3F8D5B9B74, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn script_thread_iterator_get_next_thread_id() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3CE3FB167E837D7C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn script_thread_iterator_reset() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x39382EB8DCD8684D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_block_of_player_bits(value: &mut Any, p1: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(value), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xC6DFB8C04C86D5A5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_event_flag_for_deletion(event_group: i32, event_index: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(event_group), NativeArg::int(event_index), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x4768D5252EAEB76F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_no_loading_screen(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x5CB83156AA038F95, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_script_as_no_longer_needed(script_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(script_name)];
    unsafe { native::invoke(0x0086D3067E1CFD1C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_script_with_name_hash_as_no_longer_needed(script_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(script_hash)];
    unsafe { native::invoke(0x50723A1567C8361E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn shutdown_loading_screen() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFC179D7E8886DADF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_new_script(script_name: &str, stack_size: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(script_name), NativeArg::int(stack_size)];
    unsafe { native::invoke(0xE81651AD79516E48, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_new_script_with_args(script_name: &str, args: &mut Any, arg_count: i32, stack_size: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(script_name), NativeArg::in_out(args), NativeArg::int(arg_count), NativeArg::int(stack_size)];
    unsafe { native::invoke(0xB8BA7F44DF1575E1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_new_script_with_name_hash(script_hash: Hash, stack_size: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(script_hash), NativeArg::int(stack_size)];
    unsafe { native::invoke(0xEB1C67C3A5333A92, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_new_script_with_name_hash_and_args(script_hash: Hash, args: &mut Any, arg_count: i32, stack_size: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(script_hash), NativeArg::in_out(args), NativeArg::int(arg_count), NativeArg::int(stack_size)];
    unsafe { native::invoke(0xC4BB298BD441BE78, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_displaying_mp_transition_loading_screens(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x778D4733E0F2F265, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn terminate_this_thread() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5E8B6D17FF91CD59, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn terminate_thread(thread_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(thread_id)];
    unsafe { native::invoke(0x87ED52AE40EA1A52, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn trigger_script_event(event_group: i32, event_data: &mut Any, event_data_size: i32, script_metadata_index: i32, player_bits: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(event_group), NativeArg::in_out(event_data), NativeArg::int(event_data_size), NativeArg::int(script_metadata_index), NativeArg::in_out(player_bits)];
    unsafe { native::invoke(0x5AE99C571D5BBE5D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_a79_c81_c418_f5_d38(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0A79C81C418F5D38, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x11_b0_a0_b282_fa9_b10(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x11B0A0B282FA9B10, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_bdb5_a07307_f6929(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x1BDB5A07307F6929, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_c5_eb3_c27_f7508_cb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x1C5EB3C27F7508CB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x29_fb4_ce89472_c3_cb(p0: NativeArg, p1: NativeArg, p2: f32, p3: f32, p4: &str, p5: &str, p6: &str, p7: i32) -> Result<(), NativeError> {
    let arguments = [p0, p1, NativeArg::float(p2), NativeArg::float(p3), NativeArg::string(p4), NativeArg::string(p5), NativeArg::string(p6), NativeArg::int(p7)];
    unsafe { native::invoke(0x29FB4CE89472C3CB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x42_a429_cdfed6_d99_d(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x42A429CDFED6D99D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5827_be85_a87_b073_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5827BE85A87B073D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x64_f765_d9_a1_f8_f02_c(p0: &mut Any, p1: &mut Any, p2: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x64F765D9A1F8F02C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_f700_a4_bf7_c3331_b(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x6F700A4BF7C3331B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x76_cbcd9_eadc00955() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x76CBCD9EADC00955, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a88_e1_d7_fa1_e20080(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA88E1D7FA1E20080, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e4_abe20_dce7_c7_cfe(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xE4ABE20DCE7C7CFE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e7282390542_f570_d(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE7282390542F570D, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f9_e951_a1_e5517_c06() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF9E951A1E5517C06, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ffddf802279_be128(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xFFDDF802279BE128, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn activate_goal_context(goal_context: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(goal_context)];
    unsafe { native::invoke(0x7D654266025E921B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn awards_get_unlock_claim_data(rpc_guid: &mut Any, award_hash: Hash, data_index: i32, out_unlock_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(rpc_guid), NativeArg::hash(award_hash), NativeArg::int(data_index), NativeArg::in_out(out_unlock_data)];
    unsafe { native::invoke(0xB9467E41DAB1CF2C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_reload_all_background_scripts() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xBE7D814CFA181B56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_all_player_bits(value: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(value)];
    unsafe { native::invoke(0xDE544B7EC0C187CC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_player_bit_at_index(value: &mut Any, bit_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(value), NativeArg::int(bit_index)];
    unsafe { native::invoke(0xD426E2E3288469D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn deactivate_goal_context(goal_context: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(goal_context)];
    unsafe { native::invoke(0x50B72A754EE64A71, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn display_loading_screens(p0: Hash, p1: Hash, p2: Hash, gamemode_name: &str, title: &str, subtitle: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::hash(p1), NativeArg::hash(p2), NativeArg::string(gamemode_name), NativeArg::string(title), NativeArg::string(subtitle)];
    unsafe { native::invoke(0x1E5B70E53DB661E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_compressed_global_block_buffer_exist(index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0x66EE5B93C308F734, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_global_block_can_be_accessed(index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0x42A7EB5C814C2DE0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_hash_of_thread(thread_id: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(thread_id)];
    unsafe { native::invoke(0x724CB89D35B283D0, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_thread_exit_reason() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x54AE4FDEEFEAB77E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_player_bit_set(player_bits: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(player_bits)];
    unsafe { native::invoke(0x179A6F0EE2E79026, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_background_script(thread_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(thread_id)];
    unsafe { native::invoke(0x20B7F69B40C6B755, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_goal_context_active(goal_context: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(goal_context)];
    unsafe { native::invoke(0x7409669C5ED50144, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_bit_set_at_index(value: &mut Any, bit_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(value), NativeArg::int(bit_index)];
    unsafe { native::invoke(0x72B2E00C9BAC6789, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_thread_exit_requested_for_thread_with_this_id(thread_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(thread_id)];
    unsafe { native::invoke(0x30BED53646C86D11, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn loot_get_loot_claim_data(rpc_guid: &mut Any, data_index: i32, out_loot_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(rpc_guid), NativeArg::int(data_index), NativeArg::in_out(out_loot_data)];
    unsafe { native::invoke(0xF1E9045F5AA9E428, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn loot_get_result_item(rpc_guid: &mut Any, item_index: i32, out_result_item: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(rpc_guid), NativeArg::int(item_index), NativeArg::in_out(out_result_item)];
    unsafe { native::invoke(0x4293B44A855F82CC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn net_rpc_guid_to_string(net_rpc_guid: &mut Any) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::in_out(net_rpc_guid)];
    unsafe { native::invoke(0xAC9FF854BD4BA9B5, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_thread_exit(thread_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(thread_id)];
    unsafe { native::invoke(0x7DE4643157AD646C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_thread_exit_for_all_threads_with_this_name(name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(name_hash)];
    unsafe { native::invoke(0x7423F7835770F619, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn restore_global_block(index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0xDC3914A99B4A5FDF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_global_blocks_have_been_loaded(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x11986B05885564D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_player_bits(value: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(value)];
    unsafe { native::invoke(0x20F4CB76689ACDBC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_global_block_can_be_accessed(index: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(index), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE66F392BFCE734AF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_bit_at_index(value: &mut Any, bit_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(value), NativeArg::int(bit_index)];
    unsafe { native::invoke(0x31010318BA9897AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn store_global_block(index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0xB952A3AC41D58F2F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn trigger_script_event_2(event_data: &mut Any, event_data_size: i32, script_metadata_index: i32, thread_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(event_data), NativeArg::int(event_data_size), NativeArg::int(script_metadata_index), NativeArg::int(thread_id)];
    unsafe { native::invoke(0x8B61C950A148FFA2, &arguments) }.map(|_| ())
}

// ============================================================================
// SHAPETEST
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_shape_test_result(shape_test_handle: ScrHandle, hit: &mut i32, end_coords: &mut Vector3, surface_normal: &mut Vector3, entity_hit: &mut Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(shape_test_handle), NativeArg::in_out(hit), NativeArg::in_out(end_coords), NativeArg::in_out(surface_normal), NativeArg::in_out(entity_hit)];
    unsafe { native::invoke(0xEDE8AC7C5108FB1D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_expensive_synchronous_shape_test_los_probe(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, flags: i32, entity_to_ignore: Entity, p8: i32) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::int(flags), NativeArg::int(entity_to_ignore), NativeArg::int(p8)];
    unsafe { native::invoke(0x377906D8A31E5586, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_shape_test_box(pos_x: f32, pos_y: f32, pos_z: f32, dimensions_x: f32, dimensions_y: f32, dimensions_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, rotation_order: i32, flags: i32, entity_to_ignore: Entity, options: i32) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(dimensions_x), NativeArg::float(dimensions_y), NativeArg::float(dimensions_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::int(rotation_order), NativeArg::int(flags), NativeArg::int(entity_to_ignore), NativeArg::int(options)];
    unsafe { native::invoke(0xFE466162C4401D18, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_shape_test_capsule(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, radius: f32, flags: i32, entity_to_ignore: Entity, p9: i32) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::float(radius), NativeArg::int(flags), NativeArg::int(entity_to_ignore), NativeArg::int(p9)];
    unsafe { native::invoke(0x28579D1B8F8AAC80, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_shape_test_los_probe(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, flags: i32, entity: Entity, p8: i32) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::int(flags), NativeArg::int(entity), NativeArg::int(p8)];
    unsafe { native::invoke(0x7EE9F5D83DD4F90E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_shape_test_mouse_cursor_los_probe(p_vec1: &mut Vector3, p_vec2: &mut Vector3, flag: i32, entity: Entity, flag2: i32) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::in_out(p_vec1), NativeArg::in_out(p_vec2), NativeArg::int(flag), NativeArg::int(entity), NativeArg::int(flag2)];
    unsafe { native::invoke(0x9839013D8B6014F1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_shape_test_swept_sphere(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, radius: f32, flags: i32, entity: Entity, p9: NativeArg) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::float(radius), NativeArg::int(flags), NativeArg::int(entity), p9];
    unsafe { native::invoke(0xAA5B7C8309F73230, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x04_aa59_ca40571_c2_e(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x04AA59CA40571C2E, &arguments) }.map(|result| result.as_i64())
}

// ============================================================================
// SOCIALCLUB
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_display_name(p0: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x89D9BDE7334B110F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_display_name_by_id(p0: i32, p1: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0x11EA52CAD1B55910, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_display_name_for_type(p0: &str, p1: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0x85EA0BEC7B1F7622, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_event_id() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD635DF6BAA5A6017, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_event_id_for_type(p0: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x03C03ABBABBEF752, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_extra_data_float(p0: &str, p1: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x060BBAD634C2B44B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_extra_data_float_by_id(p0: i32, p1: &str, p2: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::string(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x91C9E2A0F9DD6DD4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_extra_data_float_for_type(p0: &str, p1: &mut f32, p2: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::in_out(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0x1BDB56DB258F052D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_extra_data_int(p0: &str, p1: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xB4411D4D6B81438E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_extra_data_int_by_id(p0: i32, p1: &str, p2: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::string(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x7C981DE05A7403A0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_extra_data_int_for_type(p0: &str, p1: &mut i32, p2: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::in_out(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0x3519CC3525319A96, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_extra_data_string(p0: &str, p1: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0x9F6DCD0C939C71E9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_extra_data_string_by_id(p0: i32, p1: &str, p2: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::string(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0x049D2196D9D11184, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_get_extra_data_string_for_type(p0: &str, p1: &str, p2: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0xC8FC3B2432E8229D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_is_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCBF743C984695CF3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_is_active_by_id(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x62B384FEFDE06817, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_community_event_is_active_for_type(p0: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0x09937EB0CEBC2F9F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_inbox_get_message_is_read_at_index(msg_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(msg_index)];
    unsafe { native::invoke(0x74CF39E030A382C4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_inbox_get_message_type_at_index(msg_index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(msg_index)];
    unsafe { native::invoke(0xFF92537C4DDC1241, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_inbox_get_total_num_messages() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8EF0F633280C0663, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_inbox_message_get_data_int(p0: i32, context: &str, out: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::string(context), NativeArg::in_out(out)];
    unsafe { native::invoke(0x95BB39C4DA99F348, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_inbox_message_get_data_string(p0: i32, context: &str, out: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::string(context), NativeArg::string(out)];
    unsafe { native::invoke(0x66F77FD58506FF6B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_inbox_message_get_raw_type_at_index(p0: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x176D077685CD83E4, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_inbox_set_message_as_read_at_index(msg_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(msg_index)];
    unsafe { native::invoke(0x63CAC501FFF66DC4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_presence_attr_set_float(attr_hash: Hash, value: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(attr_hash), NativeArg::float(value)];
    unsafe { native::invoke(0xA31DAFCDC33775E9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_presence_attr_set_float_ex(attr_name: &str, value: f32, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(attr_name), NativeArg::float(value), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x00000000467F4CAA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_presence_attr_set_int_ex(attr_name: &str, value: i32, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(attr_name), NativeArg::int(value), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x0000000085488C49, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_presence_attr_set_string_ex(attr_name: &str, value: &str, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(attr_name), NativeArg::string(value), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x00000000EB2D93B3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_profanity_check_string(string: &str, token: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(string), NativeArg::in_out(token)];
    unsafe { native::invoke(0x9C74AC9D87B3FFF4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_profanity_get_check_is_pending(token: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(token)];
    unsafe { native::invoke(0x3A10BCD0C8AA0B82, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_profanity_get_check_is_valid(token: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(token)];
    unsafe { native::invoke(0x08C8052AF40C4247, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_profanity_get_string_passed(token: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(token)];
    unsafe { native::invoke(0xF302973BB8BE70E6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_profanity_get_string_status(token: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(token)];
    unsafe { native::invoke(0x0CF3BFB99EBBE5B1, &arguments) }.map(|result| result.as_i32())
}

// ============================================================================
// SOCIALCLUBFEED
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_feed_hub_has_new_data() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x068332D20CB6F897, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn sc_feed_submit_preset_message(type_: i32, sub_type: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(type_), NativeArg::int(sub_type)];
    unsafe { native::invoke(0xEFB64240F6B17817, &arguments) }.map(|result| result.as_i32())
}

// ============================================================================
// SPACTIONPROXY
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spactionproxy_get_next_pending_buy_action(data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(data)];
    unsafe { native::invoke(0x1F471B79ACC98BEF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spactionproxy_get_next_pending_crafting_action(data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(data)];
    unsafe { native::invoke(0x1F471B79ACC97BEF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spactionproxy_manager_is_failed() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1F471B79ACC91BEC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spactionproxy_manager_is_ready() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1F471B79ACC91BED, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spactionproxy_process_action(p0: NativeArg, p1: bool) -> Result<bool, NativeError> {
    let arguments = [p0, NativeArg::boolean(p1)];
    unsafe { native::invoke(0x1F471B79ACC94BEF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn spactionproxy_start_manager() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1F471B79ACC91BEE, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// STATS
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_achievement_get_progress_int(p0: Hash, p1: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::hash(p1)];
    unsafe { native::invoke(0x808712E428F697B8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_achievement_is_complete(p0: Hash, p1: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::hash(p1)];
    unsafe { native::invoke(0x77B97A827739D434, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_add_goal_progress_float(chal_hash: Hash, goal_hash: Hash, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::hash(goal_hash), NativeArg::float(value)];
    unsafe { native::invoke(0x86922D8C02FB7703, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_add_goal_progress_float_by_score_id(p0: Hash, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::float(value)];
    unsafe { native::invoke(0x86922D8C02FB7705, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_add_goal_progress_int(chal_hash: Hash, goal_hash: Hash, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::hash(goal_hash), NativeArg::int(value)];
    unsafe { native::invoke(0xDDBD560745B1EE9A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_add_goal_progress_int_by_score_id(p0: Hash, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::int(value)];
    unsafe { native::invoke(0xDDBD560745B1EE9C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_get_max_ranks(chal_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(chal_hash)];
    unsafe { native::invoke(0x58CB53DB63F84DEA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_get_num_ranks_completed(chal_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(chal_hash)];
    unsafe { native::invoke(0x58CB53DB63F84DE9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_is_goal_active(chal_hash: Hash, goal_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::hash(goal_hash)];
    unsafe { native::invoke(0x04DAC3929796EB87, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_mission_add_goal_progress_int(mission_hash: Hash, goal_hash: Hash, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(mission_hash), NativeArg::hash(goal_hash), NativeArg::int(value)];
    unsafe { native::invoke(0x97E18E7C098626DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_mission_get_num_goals(mission_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(mission_hash)];
    unsafe { native::invoke(0x0B0576DD3A75E58D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_mission_get_num_goals_complete(mission_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(mission_hash)];
    unsafe { native::invoke(0xA785A52B59B7E7B2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_mission_is_goal_complete(mission_hash: Hash, goal_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(mission_hash), NativeArg::hash(goal_hash)];
    unsafe { native::invoke(0xC0BB774787BBF301, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_net_start_chal(chal_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(chal_hash)];
    unsafe { native::invoke(0x4ABF7E4DB6279E8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_net_start_goal(chal_hash: Hash, goal_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::hash(goal_hash)];
    unsafe { native::invoke(0xC3FCB47344DCB638, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_net_stop_chal(chal_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(chal_hash)];
    unsafe { native::invoke(0x43B0163154A50C86, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_net_stop_goal(chal_hash: Hash, goal_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::hash(goal_hash)];
    unsafe { native::invoke(0x00CE6A93324A590B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_set_goal_disabled(chal_hash: Hash, goal_hash: Hash, disabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::hash(goal_hash), NativeArg::boolean(disabled)];
    unsafe { native::invoke(0xF63DF9EE16393343, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn chal_set_goal_progress_int(chal_hash: Hash, goal_hash: Hash, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::hash(goal_hash), NativeArg::int(value)];
    unsafe { native::invoke(0xDDBD560745B1EE9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn statstracker_deed_started(p0: Hash, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), p1];
    unsafe { native::invoke(0xB2A38826E5886E83, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn statstracker_is_initialized(p0: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x01F4D242765C6B24, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_add_bounty_target(unlock_hash: Hash, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(unlock_hash), NativeArg::int(ped)];
    unsafe { native::invoke(0x6B1044FDC2B09101, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_bounty_captured(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x262EF7CF49CF1EB9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_bounty_escaped(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB22F05732F72F70C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_get_bool(stat_id: &mut Any, value: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::in_out(value)];
    unsafe { native::invoke(0x11B5E6D2AE73F48F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_get_date(stat_id: &mut Any, date: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::in_out(date)];
    unsafe { native::invoke(0x8B0FACEFC36C824C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_get_float(stat_id: &mut Any, value: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::in_out(value)];
    unsafe { native::invoke(0xD7AE6C9C9C6AC54D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_get_int(stat_id: &mut Any, p1: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::in_out(p1)];
    unsafe { native::invoke(0x767FBC2AC802EF3E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_is_valid(stat_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id)];
    unsafe { native::invoke(0xC48FE1971C9743FF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_set_bool(stat_id: &mut Any, value: bool, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::boolean(value), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x3B5107353267D7A1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_set_date(stat_id: &mut Any, date: &mut Any, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::in_out(date), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x1FAE9B2FAA2DFE06, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_set_float(stat_id: &mut Any, value: f32, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::float(value), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x481BDF6A10C5EF68, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_set_gxt_label(stat_id: &mut Any, label: &str, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::string(label), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x05060A54834F2382, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_set_int(stat_id: &mut Any, value: i32, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::int(value), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xA4DDF5DF95E65EEE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_set_to_posse_id(stat_id: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(stat_id)];
    unsafe { native::invoke(0x34B22DE38477EDB4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_photograph_taken(itemset: ItemSet) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(itemset)];
    unsafe { native::invoke(0x4D31051A4CA83787, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_register_legendary_animal_deed(deed_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(deed_hash)];
    unsafe { native::invoke(0xCD0D69C65BB0E8B9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn weekly_collectible_get_item_in_set(chal_hash: Hash, set_index: i32, item_index: i32, p3: &mut Hash, p4: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::int(set_index), NativeArg::int(item_index), NativeArg::in_out(p3), NativeArg::in_out(p4)];
    unsafe { native::invoke(0xBA61BA6205A3F5A8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn weekly_collectible_get_item_set_buy_award(chal_hash: Hash, index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::int(index)];
    unsafe { native::invoke(0x610783F646894D25, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn weekly_collectible_get_item_set_label(chal_hash: Hash, index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::int(index)];
    unsafe { native::invoke(0xBFFA88522FF0F730, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn weekly_collectible_get_num_items_in_set(chal_hash: Hash, index: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::int(index)];
    unsafe { native::invoke(0x7D675C9DDDB365BE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn weekly_collectible_get_num_sets(chal_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(chal_hash)];
    unsafe { native::invoke(0x8F5317729F791D10, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x025_e98_e317652_cdd(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x025E98E317652CDD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_fee2561120_f3333(stat_id: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(stat_id)];
    unsafe { native::invoke(0x0FEE2561120F3333, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x218_f7710_a139_d012() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x218F7710A139D012, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x302_e71_c1_d9_ee75_b9(stat_id: &mut Any, p1: Hash, p2: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::hash(p1), NativeArg::in_out(p2)];
    unsafe { native::invoke(0x302E71C1D9EE75B9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x378_d3_b1_b11_d9385_b(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x378D3B1B11D9385B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_aeabae3_f3_c7600_c() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3AEABAE3F3C7600C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_eb2791_a1_fbc8_a42(stat_item: Hash, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(stat_item), NativeArg::int(p1)];
    unsafe { native::invoke(0x3EB2791A1FBC8A42, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_f6_fd87_d2030_adc6() -> Result<NativeString, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3F6FD87D2030ADC6, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_dac398297981_b87(p0: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x4DAC398297981B87, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_e463_a3_cdefffe96() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4E463A3CDEFFFE96, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_f2_d5_fa23_db992_de() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4F2D5FA23DB992DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_fcbcc0584_cd08_e9(p0: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x4FCBCC0584CD08E9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6123_e2832_c34243_d(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x6123E2832C34243D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8312_f09_c56149_a8_a(animal_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(animal_type)];
    unsafe { native::invoke(0x8312F09C56149A8A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_ba3_d7_b1_e83_ef803(p0: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x8BA3D7B1E83EF803, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_c889_e4_cbb4_b2356(p0: NativeArg, ped: Ped) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(ped)];
    unsafe { native::invoke(0x8C889E4CBB4B2356, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x91_a4_f58_e01_ed5_e4_c(stat_id: &mut Any, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::int(value)];
    unsafe { native::invoke(0x91A4F58E01ED5E4C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x99230691875_fc218(p0: NativeArg, p1: Hash, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(p1), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x99230691875FC218, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_d0_f5_d2_e1951_cd84() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x9D0F5D2E1951CD84, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2_e2_bea4_e83_f6270(p0: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0xA2E2BEA4E83F6270, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a59590050_f80_ff2_e(p0: NativeArg, p1: bool, p2: bool, p3: bool) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xA59590050F80FF2E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a596890_cf55_b5095(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xA596890CF55B5095, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b112_b9262_ec29_c20(p0: Hash, p1: i32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::int(p1)];
    unsafe { native::invoke(0xB112B9262EC29C20, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b5_e2_eda2135_e0_fa1(p0: Hash, p1: i32, schedule_location: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::int(p1), NativeArg::in_out(schedule_location)];
    unsafe { native::invoke(0xB5E2EDA2135E0FA1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_be66_b26_b6529_e943(unlock_hash: Hash, ped: Ped, animal_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(unlock_hash), NativeArg::int(ped), NativeArg::hash(animal_type)];
    unsafe { native::invoke(0xBE66B26B6529E943, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ca1_f0_b5103936891(p0: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0xCA1F0B5103936891, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ca41_e86545413_b5_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xCA41E86545413B5B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d64_dbc8_b0424135_f(ped: Ped, animal_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(animal_type)];
    unsafe { native::invoke(0xD64DBC8B0424135F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_da26263_c07_cce9_c2(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xDA26263C07CCE9C2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ddbd560745_b1_ee98(chal_hash: Hash, goal_hash: Hash, player: Player) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(chal_hash), NativeArg::hash(goal_hash), NativeArg::int(player)];
    unsafe { native::invoke(0xDDBD560745B1EE98, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_df95_df488_a645_ce7() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDF95DF488A645CE7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e141_f6_b40_b1_e3683(stat_id: &mut Any, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::float(value)];
    unsafe { native::invoke(0xE141F6B40B1E3683, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e5_a680_a5_d8_b1_f687(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0xE5A680A5D8B1F687, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f21_a5_d66874_fcedd(p0: NativeArg, p1: Hash, p2: Hash) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0xF21A5D66874FCEDD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f2_b5_abde09958689(unlock_hash: Hash, ped1: Ped, ped2: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(unlock_hash), NativeArg::int(ped1), NativeArg::int(ped2)];
    unsafe { native::invoke(0xF2B5ABDE09958689, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f8181_b5_ef156862_c(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF8181B5EF156862C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn statstracker_deed_status(deed_type: i32, deed_hash: Hash, mission_status: i32, data: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(deed_type), NativeArg::hash(deed_hash), NativeArg::int(mission_status), NativeArg::in_out(data)];
    unsafe { native::invoke(0xD5910ECF81A2278C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_add_animal_sample_target(animal_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(animal_type)];
    unsafe { native::invoke(0x90E9A5DADBABC918, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_calculate_cooldown(value: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(value)];
    unsafe { native::invoke(0x1E7384AB5D4F4581, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_carried_satchel_item_from_ped(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x831BF01C56149A8A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_donate_increment_item(item: i32, slot: i32, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(item), NativeArg::int(slot), p2, p3];
    unsafe { native::invoke(0x7C2ABF6E556B21FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_decrement_int(stat_id: &mut Any, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::int(value)];
    unsafe { native::invoke(0xBD861AE8A5181ED7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_increment_float(stat_id: &mut Any, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::float(value)];
    unsafe { native::invoke(0x4A47E38EA3D60939, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_id_increment_int(stat_id: &mut Any, value: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(stat_id), NativeArg::int(value)];
    unsafe { native::invoke(0x6A0184E904CDF25E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_item_fish_caught(fish: Ped, weight: f32, category: Hash, subcategory: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(fish), NativeArg::float(weight), NativeArg::hash(category), NativeArg::hash(subcategory)];
    unsafe { native::invoke(0xDA26263C87CCE9C1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stat_pheromone_cooldown_legendary_animal(entity: Entity, stat_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::in_out(stat_id)];
    unsafe { native::invoke(0x5420D398A42917FC, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// STREAMING
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn begin_srl() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0360710033BE60D9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_focus() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x86CCAF7CE493EFBE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_hd_area() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD83B22434E52728D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_anim_dict_exist(anim_dict: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(anim_dict)];
    unsafe { native::invoke(0x537F44CB0D7F150D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn end_srl() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1CE71FB33CA079FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_number_of_streaming_requests() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x30CCCC4D88E654CA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_population_budget_multiplier() -> Result<f32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8A3945405B31048F, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_anim_dict_loaded(anim_dict: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(anim_dict)];
    unsafe { native::invoke(0x27FF6FE8009B40CA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_clip_set_loaded(clip_set: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(clip_set)];
    unsafe { native::invoke(0x1F23D6B6DA1CC3B2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_collision_for_model_loaded(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x210A79C9EC89778F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_model_loaded(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x1283B8B89DD5D1B6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_move_network_def_loaded(name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x2C04D89A0FB4E244, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_named_ptfx_asset_loaded(fx_name_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(fx_name_hash)];
    unsafe { native::invoke(0x65BB72F29138F5D6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_ptfx_asset_loaded() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x13A3F30A9ED0BC31, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ipl_group_swap_cancel() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x31108BB5715D035F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ipl_group_swap_finish() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x040EE319EFD1D3B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ipl_group_swap_is_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFC464598F6EE97B0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ipl_group_swap_is_ready() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC2C05DEFE85A0B64, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ipl_group_swap_start(ipl_name1: &str, ipl_name2: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(ipl_name1), NativeArg::string(ipl_name2)];
    unsafe { native::invoke(0x20D504994FDC4412, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_focus(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xF87DE697E9A06EC6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ipl_active_by_hash(ipl_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(ipl_hash)];
    unsafe { native::invoke(0x93AC1B91CB6D9913, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ipl_active_hash(ipl_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(ipl_hash)];
    unsafe { native::invoke(0xD779B9B910BD3B7C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_load_scene_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCF45DF50C7775F2A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_load_scene_loaded() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0909F71B5C070797, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_model_a_ped(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0xC3F09DE9D6D17DDA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_model_a_vehicle(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x354F62672DE7DB0A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_model_in_cdimage(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0xD6F3B6D7716CFF8E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_model_valid(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x392C8D8E07B70EFC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_player_switch_in_progress() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xED20CB1F5297791D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_rendered_scene_loaded() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x45BF3A6239A576B7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_srl_loaded() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5C2C88512CF6DAFB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn load_scene_start(pos_x: f32, pos_y: f32, pos_z: f32, offset_x: f32, offset_y: f32, offset_z: f32, radius: f32, p7: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(radius), NativeArg::int(p7)];
    unsafe { native::invoke(0x387AD749E3B69B70, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn load_scene_start_sphere(x: f32, y: f32, z: f32, radius: f32, p4: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), p4];
    unsafe { native::invoke(0x513F8AA5BF2F17CF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn load_scene_stop() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5A8B01199C3E79C3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn prefetch_srl(srl: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(srl)];
    unsafe { native::invoke(0x354837E5A5BAA5AF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_anim_dict(anim_dict: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(anim_dict)];
    unsafe { native::invoke(0x4763145053A33D46, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_clip_set(clip_set: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(clip_set)];
    unsafe { native::invoke(0x817FA1B1EE7CD6F0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ipl_by_hash(ipl_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(ipl_hash)];
    unsafe { native::invoke(0x431E3AB760629B34, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ipl_hash(ipl_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(ipl_hash)];
    unsafe { native::invoke(0x5A3E5CF7B4014B96, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_move_network_def(name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x57A197AD83F66BBF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_named_ptfx_asset(fx_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(fx_name_hash)];
    unsafe { native::invoke(0xF20866829E1C81A2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ptfx_asset() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x042F9049EA419E86, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_additional_collision_at_coord(x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x83A8D71650D1894F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_anim_dict(anim_dict: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(anim_dict)];
    unsafe { native::invoke(0xA862A2AD321F94B4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_clip_set(clip_set: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(clip_set)];
    unsafe { native::invoke(0xEF7611B57A820126, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_collision_at_coord(x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x0A3720F162A033C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_collision_for_model(model: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0xF1767BE37F661551, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ipl_by_hash(ipl_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(ipl_hash)];
    unsafe { native::invoke(0x9E211A378F95C97C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ipl_hash(ipl_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(ipl_hash)];
    unsafe { native::invoke(0x59767C5A7A9AE6DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_model(model: Hash, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xFA28FE3A6246FC30, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_move_network_def(name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x2B6529C54D29037A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_named_ptfx_asset(fx_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(fx_name_hash)];
    unsafe { native::invoke(0xF2B2353BBC0D4E8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_ptfx_asset() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x001FF43843028E0C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_mapdata_culled(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x19ABCC581D28E6F9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_focus_entity(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x955AEDD58F4BD309, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_focus_pos_and_vel(x: f32, y: f32, z: f32, offset_x: f32, offset_y: f32, offset_z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z)];
    unsafe { native::invoke(0x25F6EF88664540E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_game_pauses_for_streaming(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xB3BC8250F4FE8B63, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_hd_area(x: f32, y: f32, z: f32, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0xB88B905AFA35CB4D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mapdatacullbox_enabled(name: &str, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(name), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x3CACC83F6FED837C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_model_as_no_longer_needed(model: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x4AD96EF928BD4F9A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_population_budget_multiplier(f_budget_multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(f_budget_multiplier)];
    unsafe { native::invoke(0x2F9AC754FE179D58, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scene_streaming_tracks_cam_pos_this_frame() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA03A6812529AD9C8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_srl_long_jump_mode(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x7C907E8A725E5FD2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_srl_readahead_times(p0: i32, p1: i32, p2: i32, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0xD346248C1DCE0D76, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_srl_time(p0: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0)];
    unsafe { native::invoke(0x18231AEF458BCFF2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x032_a14_d082_a9_b269(p0: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x032A14D082A9B269, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x03_ddbf2_d73799_f9_e(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x03DDBF2D73799F9E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x05_dd384_f39_de1_c8_c(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x05DD384F39DE1C8C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x071769_bcb24379_e5() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x071769BCB24379E5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x07559_b29950301_ff(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x07559B29950301FF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x09_fbf15_d73_efc900() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x09FBF15D73EFC900, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x198_b85_cc3_c7_a4593(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x198B85CC3C7A4593, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x27_af48_c62_b281341() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x27AF48C62B281341, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_a6_d1_daab9_ebb262(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2A6D1DAAB9EBB262, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_e24_c27_b112_b5_b12(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2E24C27B112B5B12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_f4_d53023_f826_ff0() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2F4D53023F826FF0, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5288_b7_f0690_f7_c1_f(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5288B7F0690F7C1F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x53764309_c4618087(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x53764309C4618087, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_d5_e2102_b174_b8_d2() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5D5E2102B174B8D2, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x62_d5_f0588915_b277() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x62D5F0588915B277, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x66_bc28_e50_e85270_e(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x66BC28E50E85270E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_a6_e79_fbe8678_c98() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6A6E79FBE8678C98, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_b8_c2_b846_c05_e5_ad() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7B8C2B846C05E5AD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x80_b3_e0597366_adf1() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x80B3E0597366ADF1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x85_b8_f04555_ab49_b8(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x85B8F04555AB49B8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_d56_bda343_d9519_f(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x8D56BDA343D9519F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x99_f92061_efe908_ba() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x99F92061EFE908BA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_f348_de670423460(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9F348DE670423460, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a0_ae7653_e8181725(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA0AE7653E8181725, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ae00387_e53_b1_e9_fc() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAE00387E53B1E9FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_afa87_a7_d41_ee346_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xAFA87A7D41EE346A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b223249_b7798_eeed(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xB223249B7798EEED, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b9_b9_e47_edb9_d63_db() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB9B9E47EDB9D63DB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_be8_daa9_d8_d01_da6_a(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xBE8DAA9D8D01DA6A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cc61_d8_d6_c19_d9_f14(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xCC61D8D6C19D9F14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d6_e39_dc5_d46_df4_ab(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xD6E39DC5D46DF4AB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d840_c130_d7_aacfa5(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xD840C130D7AACFA5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d9_f2_ff4_af394_d926() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xD9F2FF4AF394D926, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_da7_fdeff4_de86839() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDA7FDEFF4DE86839, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dabfe48_ba0_d457_aa() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDABFE48BA0D457AA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_deee1_f265_b7_ecef5() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDEEE1F265B7ECEF5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e5_b76_e5_b56_cd77_dd() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE5B76E5B56CD77DD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ef1_a8_a484118735_e() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEF1A8A484118735E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f01_d21_df39554115(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF01D21DF39554115, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f11_d7_cb962_fcd747(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF11D7CB962FCD747, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ipl_bounding_sphere(ipl_hash: Hash, position: &mut Vector3, radius: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(ipl_hash), NativeArg::in_out(position), NativeArg::in_out(radius)];
    unsafe { native::invoke(0x9C77964B0E07B633, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_collision_loaded_at_coord(x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xDA8B2EAF29E872E2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_scenario_type_loaded(scenario_type: Hash, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(scenario_type), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x9427C94D2E4094A4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_model_an_object(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x274EE1B90CFA669E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_position_inside_ipl_streaming_extents(ipl_hash: Hash, x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(ipl_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x73B40D97D7BAAD77, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_scenario_asset(scenario_type: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::hash(scenario_type)];
    unsafe { native::invoke(0x4EDDD9E9CA5AF985, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_clip_set_by_hash(clip_set_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(clip_set_hash)];
    unsafe { native::invoke(0xAC37644A538F7524, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_metadata_at_coord(x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xA8432A14D4DC2101, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_scenario_type(scenario_type: Hash, p1: i32, entity_model: Hash, conditional_anim: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(scenario_type), NativeArg::int(p1), NativeArg::hash(entity_model), NativeArg::hash(conditional_anim)];
    unsafe { native::invoke(0x19A6BE7D9C6884D3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_guarma_worldhorizon_active(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x74E2261D2A66849A, &arguments) }.map(|_| ())
}

// ============================================================================
// TASK
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_add_cover_blocking_area(player_x: f32, player_y: f32, player_z: f32, radius_x: f32, radius_y: f32, radius_z: f32, p6: bool, p7: bool, p8: bool, p9: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(player_x), NativeArg::float(player_y), NativeArg::float(player_z), NativeArg::float(radius_x), NativeArg::float(radius_y), NativeArg::float(radius_z), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::boolean(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x45C597097DD7CB81, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_cover_point(p0: f32, p1: f32, p2: f32, p3: f32, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: bool) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), p4, p5, p6, NativeArg::boolean(p7)];
    unsafe { native::invoke(0xD5C12A75C7B9497F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_flee_target_ped(ped: Ped, target_ped: Ped, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::float(p2)];
    unsafe { native::invoke(0x3923EC958249657D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_patrol_route_link(node1: i32, node2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(node1), NativeArg::int(node2)];
    unsafe { native::invoke(0x23083260DEC3A551, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_patrol_route_node(node_id: i32, scenario_name: &str, x: f32, y: f32, z: f32, look_pos_x: f32, look_pos_y: f32, look_pos_z: f32, duration: i32, p9: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(node_id), NativeArg::string(scenario_name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(look_pos_x), NativeArg::float(look_pos_y), NativeArg::float(look_pos_z), NativeArg::int(duration), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x8EDF950167586B7C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_composite_lootable_entity_def_assets_loaded(asset: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(asset)];
    unsafe { native::invoke(0x5E5D96BE25E9DF68, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn assisted_movement_is_route_loaded(route: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(route)];
    unsafe { native::invoke(0x60F9A4393A21F741, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn assisted_movement_remove_route(route: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(route)];
    unsafe { native::invoke(0x3548536485DD792B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn assisted_movement_set_route_properties(route: &str, props: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(route), NativeArg::int(props)];
    unsafe { native::invoke(0xD5002D78B7162E1B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_start_item_interaction(ped: Ped, item_hash: Hash, interaction_anim_hash: Hash, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(item_hash), NativeArg::hash(interaction_anim_hash), NativeArg::int(p3)];
    unsafe { native::invoke(0x2D19BC4DF626CBE7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_driveby_task_underneath_driving_task(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC35B5CDB2824CF69, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_secondary_task(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x176CECF6F920D707, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_tasks(ped: Ped, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xE1EF3C1216AFF2CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_tasks_immediately(ped: Ped, p1: bool, reset_crouch: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(reset_crouch)];
    unsafe { native::invoke(0xAAA34F8A7CB32098, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_sequence_task(task_sequence_id: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(task_sequence_id)];
    unsafe { native::invoke(0x3841422E9C488D8C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn close_patrol_route() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB043ECA801B8CBC1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn close_sequence_task(task_sequence_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(task_sequence_id)];
    unsafe { native::invoke(0x39E72BC99E6360CB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_patrol_route() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAF8A443CCC8018DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_scenario_point_hash(scenario_hash: Hash, x: f32, y: f32, z: f32, heading: f32, p5: NativeArg, p6: NativeArg, p7: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(scenario_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), p5, p6, NativeArg::boolean(p7)];
    unsafe { native::invoke(0x94B745CE41DB58A1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_scenario_point_hash_attached_to_entity(entity: Entity, scenario_hash: Hash, x: f32, y: f32, z: f32, heading: f32, p6: NativeArg, p7: NativeArg, p8: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(scenario_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), p6, p7, NativeArg::boolean(p8)];
    unsafe { native::invoke(0x794AB1379A74064D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_patrol_route(patrol_route: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(patrol_route)];
    unsafe { native::invoke(0x7767DD9D65E91319, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_scenario_exist_in_area(x: f32, y: f32, z: f32, radius: f32, p4: bool, p5: NativeArg, p6: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::boolean(p4), p5, NativeArg::boolean(p6)];
    unsafe { native::invoke(0x5A59271FFADD33C1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_scenario_group_exist(scenario_group: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(scenario_group)];
    unsafe { native::invoke(0xF9034C136C9E00D3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_scenario_of_type_exist_in_area_hash(x: f32, y: f32, z: f32, type_hash: Hash, radius: f32, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::hash(type_hash), NativeArg::float(radius), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x6EEAD6AF637DA752, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_scenario_point_exist(scenario: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0x841475AC96E794D1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_scripted_cover_point_exist_at_coords(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xA98B8E3C088E5A31, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn end_duel(ped: Ped, p1: bool, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::float(p2)];
    unsafe { native::invoke(0xEED08A3A98B847E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn find_scenario_of_type_hash(x_pos: f32, y_pos: f32, z_pos: f32, scenario_type: Hash, distance: f32, p5: NativeArg, p6: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::hash(scenario_type), NativeArg::float(distance), p5, NativeArg::boolean(p6)];
    unsafe { native::invoke(0xF533D68FF970D190, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_scenario_group_priority(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x444C910A5058E568, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_active_vehicle_mission_type(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x534AEBA6E5ED4CAB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_carriable_entity(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x0CCFE72B43C9CF96, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_ped_aiming_in_the_air(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8785E6E40C7A8819, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_task_active(ped: Ped, task_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(task_index)];
    unsafe { native::invoke(0xB0760331C7AA4155, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_waypoint_recording_loaded(waypoint_recording: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(waypoint_recording)];
    unsafe { native::invoke(0xCB4E8BE8A0063C5D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_item_interaction_item_id(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x804425C4BBD00883, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_item_interaction_prompt_progress(ped: Ped, input_context: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(input_context)];
    unsafe { native::invoke(0xBC864A70AD55E0C1, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_item_interaction_state(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6AA3DCA2C6F5EB6D, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_desired_move_blend_ratio(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8517D4A6CA8513ED, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_waypoint_distance(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xE6A877C64CAF1BC5, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_waypoint_override_speed(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD39A2F3E7FCAFF08, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_waypoint_progress(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2720AAA75001E094, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_prop_for_scenario_point(scenario_point: i32, name: &str) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(scenario_point), NativeArg::string(name)];
    unsafe { native::invoke(0x295514F198EFD0CA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ransack_scenario_point_ped_is_using(ped: Ped) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD04241BBF6D03A5E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_scenario_points_in_area(pos_x: f32, pos_y: f32, pos_z: f32, radius: f32, scenarios_in_radius: &mut Any, size: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(radius), NativeArg::in_out(scenarios_in_radius), NativeArg::int(size)];
    unsafe { native::invoke(0x345EC3B7EBDE1CB5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_scripted_cover_point_coords(coverpoint: ScrHandle) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(coverpoint)];
    unsafe { native::invoke(0x594A1028FC2A3E85, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_script_task_status(ped: Ped, task_hash: Hash, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(task_hash), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x77F1BEB8863288D5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_sequence_progress(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x00A9010CFE1E3533, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_task_move_network_event(ped: Ped, event_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(event_name)];
    unsafe { native::invoke(0xB4F47213DF45A64C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_task_move_network_state(ped: Ped) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x717E4D1F2048376D, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_waypoint_playback_override_speed(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3DC971EB22F73447, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_waypoint_progress(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x9824CFF8FC66E159, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_waypoint_target_point(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x416B62AC8B9E5BBD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_waypoint_distance_along_route(waypoint_recording: &str, p1: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::string(waypoint_recording), NativeArg::int(p1)];
    unsafe { native::invoke(0xA5B769058763E497, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_driveby_task_underneath_driving_task(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8785E6E40C7A8818, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_emote_task_running(ped: Ped, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0xCF9B71C0AF824036, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_mounted_weapon_task_underneath_driving_task(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA320EF046186FA3B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_move_blend_ratio_running(move_blend_ratio: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(move_blend_ratio)];
    unsafe { native::invoke(0xD4D8636C0199A939, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_move_blend_ratio_sprinting(move_blend_ratio: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(move_blend_ratio)];
    unsafe { native::invoke(0x24A2AD74FA9814E2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_move_blend_ratio_still(move_blend_ratio: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(move_blend_ratio)];
    unsafe { native::invoke(0x349CE7B56DAFD95C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_move_blend_ratio_walking(move_blend_ratio: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(move_blend_ratio)];
    unsafe { native::invoke(0xF133BBBE91E1691F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_active_in_scenario(ped: Ped, scenario: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(scenario)];
    unsafe { native::invoke(0xAA135F9482C82CC3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_being_arrested(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x90A09F3A45FED688, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_cuffed(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x74E559B3BC910685, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_exiting_scenario(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x0C3CB2E600C8977D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_getting_up(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2A74E1D5F2F00EEC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_hit_react(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF330A5C062B29BED, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_in_writhe(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xDEB6D52126E7D640, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_running(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC5286FFC176F28A2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_running_inspection_task(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x038B1F1674F0E242, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_running_task_item_interaction(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xEC7E480FF8BD0BED, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_scenario_react_looking(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x916B8E075ABC8B4E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_sprinting(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x57E457CD2C0FC168, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_still(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xAC29253EEF8F0180, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_walking(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xDE4C184B2B9B071A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scenario_group_enabled(scenario_group: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(scenario_group)];
    unsafe { native::invoke(0x367A09DED4E05B99, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scenario_occupied(p0: f32, p1: f32, p2: f32, p3: f32, p4: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x788756D73AC2E07C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scenario_type_enabled(scenario_type: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(scenario_type)];
    unsafe { native::invoke(0x3A815DB3EA088722, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_task_move_network_active(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x921CE12C489C4C41, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_task_move_network_ready_for_transition(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x30ED88D5E0C56A37, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_team_carriable_entity(p0: NativeArg, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x559A6F8C5133B4EE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_waypoint_playback_going_on_for_ped(ped: Ped, waypoint_recording: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(waypoint_recording)];
    unsafe { native::invoke(0xE03B3F2D3DC59B64, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_waypoint_playback_going_on_for_vehicle(p0: NativeArg, p1: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xF5134943EA29868C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn make_object_not_carriable(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x67BFCED22909834D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn open_patrol_route(patrol_route: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(patrol_route)];
    unsafe { native::invoke(0xA36BFB5EE89F3D82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn open_sequence_task(task_sequence_id: &mut i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(task_sequence_id)];
    unsafe { native::invoke(0xE8854A4326B9E12B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_has_use_scenario_task(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x295E3CCEC879CCD7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_anim_on_running_scenario(ped: Ped, anim_dict: &str, anim_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(anim_dict), NativeArg::string(anim_name)];
    unsafe { native::invoke(0x748040460F8DF5DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn play_entity_scripted_anim(entity: Entity, args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::in_out(args)];
    unsafe { native::invoke(0x77A1EEC547E7FCF1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn react_look_at(ped: Ped, target_ped: Ped, look_intensity: i32, exit_animation: i32, duration: f32, p5: i32, target_ped2: Ped, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::int(look_intensity), NativeArg::int(exit_animation), NativeArg::float(duration), NativeArg::int(p5), NativeArg::int(target_ped2), p7, p8];
    unsafe { native::invoke(0xE7FA07624574B79A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn react_look_at_end(ped: Ped, exit_animation: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(exit_animation), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x541E5B41DCA45828, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_all_cover_blocking_areas() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDB6708C0B46F56D8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_cover_point(coverpoint: ScrHandle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(coverpoint)];
    unsafe { native::invoke(0xAE287C923D891715, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_waypoint_recording(waypoint_recording: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(waypoint_recording)];
    unsafe { native::invoke(0xFF1B8B4AA1C25DC8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_task_move_network_state_transition(ped: Ped, name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(name)];
    unsafe { native::invoke(0xD01015C7316AE176, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_waypoint_recording(waypoint_recording: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(waypoint_recording)];
    unsafe { native::invoke(0x9EEFB62EB27B5792, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_scenario_groups_enabled() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDD902D0349AFAD3A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_scenario_types_enabled() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0D40EE2A7F2B2D6D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_filter(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x87B66D77D545DB66, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anim_rate(p0: NativeArg, p1: f32, p2: NativeArg, p3: bool) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::float(p1), p2, NativeArg::boolean(p3)];
    unsafe { native::invoke(0x032D49C5E359C847, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_driveby_task_target(shooting_ped: Ped, target_ped: Ped, target_vehicle: Vehicle, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(shooting_ped), NativeArg::int(target_ped), NativeArg::int(target_vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xE5B302114D8162EE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_drive_task_cruise_speed(driver: Ped, cruise_speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(driver), NativeArg::float(cruise_speed)];
    unsafe { native::invoke(0x5C9B84BD7D31D908, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_drive_task_max_cruise_speed(ped: Ped, max_cruise_speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(max_cruise_speed)];
    unsafe { native::invoke(0x404A5AA9B9F0B746, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_enable_speed_restrain_for_waypoint_recording_leader(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x295F03DC97BEEBC1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_enhanced_break_free(ped: Ped, p1: bool, clipset: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::string(clipset)];
    unsafe { native::invoke(0x1BF9D36A5EAFFBAE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_high_fall_task(ped: Ped, p1: i32, p2: i32, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x8C825BDC7741D37C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_desired_move_blend_ratio(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x1E982AC8716912C5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_avoid_fire(ped: Ped, avoid_fire: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(avoid_fire)];
    unsafe { native::invoke(0x4455517B28441E60, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_can_drop_from_height(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE361C5C71C431A4F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_can_use_climbovers(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x8E06A6FE76C9EFF4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_can_use_ladders(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x77A5B103C87F476E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_climb_cost_modifier(ped: Ped, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(modifier)];
    unsafe { native::invoke(0x88E32DB8C1A4AA4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_deep_snow_cost_modifier(ped: Ped, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(modifier)];
    unsafe { native::invoke(0xE8C296B75EACC357, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_foliage_cost_modifier(ped: Ped, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(modifier)];
    unsafe { native::invoke(0x3AD8EFF9703BE657, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_may_enter_water(ped: Ped, may_enter_water: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(may_enter_water)];
    unsafe { native::invoke(0xF35425A4204367EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_prefer_to_avoid_water(ped: Ped, avoid_water: bool, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(avoid_water), NativeArg::float(p2)];
    unsafe { native::invoke(0x38FE1EC73743793C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_waypoint_route_offset(ped: Ped, p1: f32, p2: f32, p3: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3)];
    unsafe { native::invoke(0xED98E10B0AFCE4B4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_group_enabled(scenario_group: &str, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(scenario_group), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x02C8E5B49848664E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_type_enabled(scenario_type: &str, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(scenario_type), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xEB47EC4E34FB7EE1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_sequence_to_repeat(task_sequence_id: i32, repeat_mode: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(task_sequence_id), NativeArg::int(repeat_mode)];
    unsafe { native::invoke(0x58C70CF3A41E4AE7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_task_move_network_signal_bool(ped: Ped, signal_name: &str, value: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(signal_name), NativeArg::boolean(value)];
    unsafe { native::invoke(0xB0A6CFD2C69C1088, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_task_move_network_signal_float(ped: Ped, signal_name: &str, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(signal_name), NativeArg::float(value)];
    unsafe { native::invoke(0xD5BB4025AE449A4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_team_carriable_entity(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x545BF19F86E80F11, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_up_speed_restrain_information_for_player_follower(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8];
    unsafe { native::invoke(0xB5C51DD544F14F58, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_task_item_interaction(ped: Ped, item_hash: Hash, interaction_anim_hash: Hash, p3: i32, flag: i32, p5: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(item_hash), NativeArg::hash(interaction_anim_hash), NativeArg::int(p3), NativeArg::int(flag), NativeArg::float(p5)];
    unsafe { native::invoke(0xAE72E7DF013AAA61, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_anim_playback(ped: Ped, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xEE08C992D238C5D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_anim_task(ped: Ped, anim_dictionary: &str, animation_name: &str, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(anim_dictionary), NativeArg::string(animation_name), NativeArg::float(p3)];
    unsafe { native::invoke(0x97FF36A1D40EA00A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_achieve_heading(ped: Ped, heading: f32, timeout: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(heading), NativeArg::int(timeout)];
    unsafe { native::invoke(0x93B93A37987F1F3D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_aim_at_coord(ped: Ped, x: f32, y: f32, z: f32, time: i32, p5: bool, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(time), NativeArg::boolean(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0x4AF1D73861212F52, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_aim_at_entity(ped: Ped, target_entity: Entity, time: i32, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_entity), NativeArg::int(time), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xCF7569BD0FB480A0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_aim_gun_at_coord(ped: Ped, x: f32, y: f32, z: f32, time: i32, p5: bool, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(time), NativeArg::boolean(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0x6671F3EEC681BDA1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_aim_gun_at_entity(ped: Ped, target_entity: Entity, duration: i32, p3: bool, p4: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_entity), NativeArg::int(duration), NativeArg::boolean(p3), NativeArg::int(p4)];
    unsafe { native::invoke(0x9B53BB6E8943AF53, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_ambient_animal_hunt(ped: Ped, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2];
    unsafe { native::invoke(0x4B39D8F9D0FE7749, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_ambient_animal_stalk(ped: Ped, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2];
    unsafe { native::invoke(0x37C13863ABA1B4A3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_animal_alerted(ped: Ped, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2];
    unsafe { native::invoke(0x979D93372FC8C565, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_animal_flee(ped: Ped, target_ped: Ped, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), p2];
    unsafe { native::invoke(0xA899B61C66F09134, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_animal_interaction(ped: Ped, target_ped: Ped, interaction_type: Hash, interaction_model: Hash, skip_idle_animation_clip: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::hash(interaction_type), NativeArg::hash(interaction_model), NativeArg::boolean(skip_idle_animation_clip)];
    unsafe { native::invoke(0xCD181A959CFDD7F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_animal_unalerted(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4];
    unsafe { native::invoke(0x21FDF9A25CFE1CE5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_animal_writhe(ped: Ped, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2];
    unsafe { native::invoke(0x8C038A39C4A4B6D6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_arrest_ped(ped: Ped, target: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target)];
    unsafe { native::invoke(0xF3B9A78A178572B1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_bark(ped: Ped, bark_at_target: Ped, mood: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(bark_at_target), NativeArg::hash(mood)];
    unsafe { native::invoke(0x83BFC1F836B2F3F2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_boat_mission(ped_driver: Ped, boat: Vehicle, p2: NativeArg, p3: NativeArg, x: f32, y: f32, z: f32, p7: NativeArg, max_speed: f32, driving_style: i32, p10: f32, p11: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped_driver), NativeArg::int(boat), p2, p3, NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), p7, NativeArg::float(max_speed), NativeArg::int(driving_style), NativeArg::float(p10), p11];
    unsafe { native::invoke(0x15C86013127CE63F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_break_vehicle_door_lock(ped: Ped, vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle)];
    unsafe { native::invoke(0xBB28D1BC9EA8A6A5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_carriable(entity: Entity, carry_config: Hash, carrier: Ped, carriable_slot: i32, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(carry_config), NativeArg::int(carrier), NativeArg::int(carriable_slot), NativeArg::int(flags)];
    unsafe { native::invoke(0xF0B4F759F35CC7F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_clear_defensive_area(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x95A6C46A31D1917D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_clear_look_at(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0F804F1DB19B9689, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_climb(ped: Ped, unused: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(unused)];
    unsafe { native::invoke(0x89D9FCC2435112F1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_climb_ladder(ped: Ped, p1: f32, p2: bool, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xB6C987F9285A3814, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_animal_charge_ped(ped: Ped, target_ped: Ped, p2: bool, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::boolean(p2), p3, p4, p5, p6];
    unsafe { native::invoke(0xEE3AA414CF99F368, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_animal_warn(ped: Ped, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2];
    unsafe { native::invoke(0xF960F3D57B660E96, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_hated_targets(ped: Ped, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(radius)];
    unsafe { native::invoke(0x8182B561A29BD597, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_hated_targets_around_ped(ped: Ped, radius: f32, flags: i32, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(radius), NativeArg::int(flags), p3];
    unsafe { native::invoke(0x7BF835BB9E2698C8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_hated_targets_around_ped_timed(ped: Ped, radius: f32, time: i32, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(radius), NativeArg::int(time), NativeArg::int(flags)];
    unsafe { native::invoke(0x2BBA30B854534A0C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_hated_targets_in_area(ped: Ped, x: f32, y: f32, z: f32, radius: f32, flags: i32, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(flags), p6];
    unsafe { native::invoke(0x4CF5F55DAC3280A0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_hated_targets_no_los_test(ped: Ped, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(radius)];
    unsafe { native::invoke(0xB5BC69D9C4060BC3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_ped(ped: Ped, target_ped: Ped, p2: i32, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0xF166E48407BAC484, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_ped_timed(ped: Ped, target_ped: Ped, p2: i32, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::int(p2), p3];
    unsafe { native::invoke(0x944F30DCB7096BDE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_companion_ambient(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0xE017CF6E2527FE4F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_confront(ped: Ped, target_ped: Ped, p2: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::int(p2)];
    unsafe { native::invoke(0x3A2A2071DF5CC569, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_cower(ped: Ped, duration: i32, ped_to_cower_from: Ped, p3: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(duration), NativeArg::int(ped_to_cower_from), NativeArg::string(p3)];
    unsafe { native::invoke(0x3EB1FE9E8E908E15, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_disembark_nearest_train_carriage(ped: Ped, p1: f32, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::int(flags)];
    unsafe { native::invoke(0x0A11F3BDEC03ED5F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_dismount_animal(rider: Ped, task_flag: i32, p2: NativeArg, p3: NativeArg, p4: NativeArg, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rider), NativeArg::int(task_flag), p2, p3, p4, NativeArg::int(target_ped)];
    unsafe { native::invoke(0x48E92D3DDE23C23A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_drive_by(driver_ped: Ped, target_ped: Ped, target_vehicle: Vehicle, target_x: f32, target_y: f32, target_z: f32, distance_to_shoot: f32, ped_accuracy: i32, p8: bool, firing_pattern: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(driver_ped), NativeArg::int(target_ped), NativeArg::int(target_vehicle), NativeArg::float(target_x), NativeArg::float(target_y), NativeArg::float(target_z), NativeArg::float(distance_to_shoot), NativeArg::int(ped_accuracy), NativeArg::boolean(p8), NativeArg::hash(firing_pattern)];
    unsafe { native::invoke(0x2F8AF0E82773A171, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_duck(ped: Ped, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0xA14B5FBF986BAC23, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_duel(ped: Ped, p1: NativeArg, p2: f32, entity: Entity, p4: f32, p5: i32, v_pos_opponent_x: f32, v_pos_opponent_y: f32, v_pos_opponent_z: f32, f_opponent_head: f32, p10: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, NativeArg::float(p2), NativeArg::int(entity), NativeArg::float(p4), NativeArg::int(p5), NativeArg::float(v_pos_opponent_x), NativeArg::float(v_pos_opponent_y), NativeArg::float(v_pos_opponent_z), NativeArg::float(f_opponent_head), NativeArg::int(p10)];
    unsafe { native::invoke(0x5D5B0D5BC3626E5A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_dump_carriable_from_parent(ped: Ped, ped2: Ped, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ped2), NativeArg::int(entity)];
    unsafe { native::invoke(0x17CA98707B15926A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_eat(ped: Ped, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2];
    unsafe { native::invoke(0xBD7949BD07299672, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_enter_anim_scene(ped: Ped, anim_scene: AnimScene, entity_name: &str, playback_list_name: &str, enter_speed: f32, b_auto_start: bool, flag: i32, p7: i32, p8: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(anim_scene), NativeArg::string(entity_name), NativeArg::string(playback_list_name), NativeArg::float(enter_speed), NativeArg::boolean(b_auto_start), NativeArg::int(flag), NativeArg::int(p7), NativeArg::float(p8)];
    unsafe { native::invoke(0xC2329B0206426644, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_enter_vehicle(ped: Ped, vehicle: Vehicle, timeout: i32, seat: i32, speed: f32, flag: i32, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::int(timeout), NativeArg::int(seat), NativeArg::float(speed), NativeArg::int(flag), p6];
    unsafe { native::invoke(0xC20E50AA46D09CA8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_evasive_anim(ped1: Ped, ped2: Ped, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::int(ped2), NativeArg::int(p2)];
    unsafe { native::invoke(0x5F22926E1BCE9B08, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_everyone_leave_vehicle_in_order(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x6F1C49F275BD25B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_extend_route(x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x1E7889778264843A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_flee_coord(ped: Ped, x: f32, y: f32, z: f32, flee_style: i32, p5: i32, p6: f32, duration: i32, p8: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(flee_style), NativeArg::int(p5), NativeArg::float(p6), NativeArg::int(duration), NativeArg::int(p8)];
    unsafe { native::invoke(0x58428248BF4B64E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_flee_coord_via(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11];
    unsafe { native::invoke(0x390E0B697D25EAF5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_flee_ped(ped: Ped, flee_from_target: Ped, flee_style: i32, flag: i32, p4: f32, p5: i32, p6: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flee_from_target), NativeArg::int(flee_style), NativeArg::int(flag), NativeArg::float(p4), NativeArg::int(p5), NativeArg::int(p6)];
    unsafe { native::invoke(0xFD45175A6DFD7CE9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_flee_ped_via(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9];
    unsafe { native::invoke(0x5802E0F910E4CF1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_flush_route() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x841142A1376E9006, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_flying_circle(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x72997893BFB8ECCC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_fly_away(ped: Ped, flee_from_target: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flee_from_target)];
    unsafe { native::invoke(0xE86A537B5A3C297C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_fly_to_coord(ped: Ped, travel_mbr: f32, x: f32, y: f32, z: f32, p5: bool, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(travel_mbr), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0xD6CFC2D59DA72042, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_and_converse_with_ped(ped: Ped, target_ped: Ped, p2: NativeArg, p3: NativeArg, p4: f32, p5: f32, p6: i32, p7: NativeArg, p8: NativeArg, p9: f32, p10: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), p2, p3, NativeArg::float(p4), NativeArg::float(p5), NativeArg::int(p6), p7, p8, NativeArg::float(p9), NativeArg::float(p10)];
    unsafe { native::invoke(0x489FFCCCE7392B55, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_entity_along_waypoint_recording_at_offset(ped0: Ped, ped1: Ped, waypoint_recording: &str, p3: f32, p4: f32, p5: i32, p6: i32, p7: i32, p8: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped0), NativeArg::int(ped1), NativeArg::string(waypoint_recording), NativeArg::float(p3), NativeArg::float(p4), NativeArg::int(p5), NativeArg::int(p6), NativeArg::int(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0x4D2B787BAE9AB760, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_entity_while_aiming_at_entity(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0x2D532EAA142CF83F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_nav_mesh_to_coord(ped: Ped, x: f32, y: f32, z: f32, speed_multiplier: f32, timeout: i32, stopping_range: f32, flags: i32, heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(speed_multiplier), NativeArg::int(timeout), NativeArg::float(stopping_range), NativeArg::int(flags), NativeArg::float(heading)];
    unsafe { native::invoke(0x15D3A79D4E44B913, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_nav_mesh_to_coord_advanced(ped: Ped, x: f32, y: f32, z: f32, speed_multiplier: f32, timeout: i32, stopping_range: f32, flags: i32, p8: f32, p9: f32, p10: f32, entity: Entity, unk: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(speed_multiplier), NativeArg::int(timeout), NativeArg::float(stopping_range), NativeArg::int(flags), NativeArg::float(p8), NativeArg::float(p9), NativeArg::float(p10), NativeArg::int(entity), NativeArg::float(unk)];
    unsafe { native::invoke(0x17F58B88D085DBAC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_pavement_to_coord(ped: Ped, args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(args)];
    unsafe { native::invoke(0x1B1475414E70DD8E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_point_route(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x0E14C5550DC3CD1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_to_offset_of_coord(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13, p14];
    unsafe { native::invoke(0x2E3676282C18A692, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_to_offset_of_entity(ped: Ped, entity: Entity, offset_x: f32, offset_y: f32, offset_z: f32, movement_speed: f32, timeout: i32, stopping_range: f32, persist_following: bool, p9: bool, walk_only: bool, p11: bool, p12: bool, p13: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(movement_speed), NativeArg::int(timeout), NativeArg::float(stopping_range), NativeArg::boolean(persist_following), NativeArg::boolean(p9), NativeArg::boolean(walk_only), NativeArg::boolean(p11), NativeArg::boolean(p12), NativeArg::boolean(p13)];
    unsafe { native::invoke(0x304AE42E357B8C7E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_waypoint_recording(ped: Ped, waypoint_recording: &str, start_index: i32, flags: i32, end_index: i32, patrol: bool, aim_weapon: bool, duration_ms: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(waypoint_recording), NativeArg::int(start_index), NativeArg::int(flags), NativeArg::int(end_index), NativeArg::boolean(patrol), NativeArg::boolean(aim_weapon), NativeArg::int(duration_ms)];
    unsafe { native::invoke(0x0759591819534F7B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_waypoint_recording_advanced(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0x0CFC13EBC19BCA52, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_follow_waypoint_recording_at_offset(ped: Ped, waypoint_recording: &str, p2: f32, p3: i32, p4: i32, p5: i32, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(waypoint_recording), NativeArg::float(p2), NativeArg::int(p3), NativeArg::int(p4), NativeArg::int(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0xBE9B0520BD7C445B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_force_motion_state(ped: Ped, motion_state_hash: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(motion_state_hash), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x4F056E1AFFEF17AB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_goto_entity_aiming(ped: Ped, target: Entity, distance_to_stop_at: f32, start_aiming_dist: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target), NativeArg::float(distance_to_stop_at), NativeArg::float(start_aiming_dist)];
    unsafe { native::invoke(0xA9DA48FAB8A76C12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_goto_entity_offset(ped: Ped, entity: Entity, p2: NativeArg, x: f32, y: f32, z: f32, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), p2, NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(duration)];
    unsafe { native::invoke(0xE39B4FF4FDEBDE27, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_goto_entity_offset_xy(ped: Ped, entity: Entity, duration: i32, target_radius: f32, x_offset: f32, y_offset: f32, move_blend_ratio: f32, offset_flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), NativeArg::int(duration), NativeArg::float(target_radius), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(move_blend_ratio), NativeArg::int(offset_flags)];
    unsafe { native::invoke(0x338E7EF52B6095A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_goto_entity_offset_xyz(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8];
    unsafe { native::invoke(0xFA6DA9D151769392, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_goto_entity_offset_xyz_aiming(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9];
    unsafe { native::invoke(0x41B0832CA96B5351, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_goto_entity_offset_xy_aiming(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8];
    unsafe { native::invoke(0x901BD69984400F62, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_straight_to_coord(ped: Ped, x: f32, y: f32, z: f32, move_blend_speed_y: f32, time_before_teleport: i32, final_heading: f32, target_radius: f32, p8: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(move_blend_speed_y), NativeArg::int(time_before_teleport), NativeArg::float(final_heading), NativeArg::float(target_radius), NativeArg::int(p8)];
    unsafe { native::invoke(0xD76B57B44F1E6F8B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_straight_to_coord_relative_to_entity(ped: Ped, entity: Entity, x_offset: f32, y_offset: f32, z_offset: f32, move_blend_ratio: i32, time_before_teleport: i32, p7: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::int(move_blend_ratio), NativeArg::int(time_before_teleport), NativeArg::int(p7)];
    unsafe { native::invoke(0x61E360B7E040D12E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_coord_and_aim_at_hated_entities_near_coord(ped: Ped, go_to_location_x: f32, go_to_location_y: f32, go_to_location_z: f32, focus_location_x: f32, focus_location_y: f32, focus_location_z: f32, speed: f32, shoot_at_enemies: bool, distance_to_stop_at: f32, no_roads_distance: f32, unk_true: bool, unk_flag: i32, aiming_flag: i32, firing_pattern: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(go_to_location_x), NativeArg::float(go_to_location_y), NativeArg::float(go_to_location_z), NativeArg::float(focus_location_x), NativeArg::float(focus_location_y), NativeArg::float(focus_location_z), NativeArg::float(speed), NativeArg::boolean(shoot_at_enemies), NativeArg::float(distance_to_stop_at), NativeArg::float(no_roads_distance), NativeArg::boolean(unk_true), NativeArg::int(unk_flag), NativeArg::int(aiming_flag), NativeArg::hash(firing_pattern)];
    unsafe { native::invoke(0xA55547801EB331FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_coord_and_aim_at_hated_entities_near_coord_using_combat_style(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13, p14];
    unsafe { native::invoke(0x87BD711FC31EA273, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_coord_any_means(ped: Ped, x: f32, y: f32, z: f32, speed: f32, entity: Entity, p6: bool, walking_style: i32, p8: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(speed), NativeArg::int(entity), NativeArg::boolean(p6), NativeArg::int(walking_style), NativeArg::float(p8)];
    unsafe { native::invoke(0x5BC448CB78FA3E88, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_coord_any_means_extra_params(ped: Ped, x: f32, y: f32, z: f32, speed: f32, p5: NativeArg, p6: bool, walking_style: i32, p8: f32, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(speed), p5, NativeArg::boolean(p6), NativeArg::int(walking_style), NativeArg::float(p8), p9, p10, p11, p12];
    unsafe { native::invoke(0x1DD45F9ECFDB1BC9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_coord_any_means_extra_params_with_cruise_speed(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13, p14];
    unsafe { native::invoke(0xB8ECD61F531A7B02, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_coord_while_aiming_at_coord(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg, p15: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13, p14, p15];
    unsafe { native::invoke(0x11315AB3385B8AC0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_coord_while_aiming_at_coord_using_combat_style(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg, p15: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13, p14, p15];
    unsafe { native::invoke(0x639C0425A0B4E77E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_coord_while_aiming_at_entity(ped1: Ped, x: f32, y: f32, z: f32, ped2: Ped, p5: f32, p6: NativeArg, p7: f32, p8: f32, p9: NativeArg, p10: NativeArg, p11: NativeArg, firing_pattern: Hash, p13: i32, p14: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped1), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(ped2), NativeArg::float(p5), p6, NativeArg::float(p7), NativeArg::float(p8), p9, p10, p11, NativeArg::hash(firing_pattern), NativeArg::int(p13), p14];
    unsafe { native::invoke(0xB2A16444EAD9AE47, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_coord_while_aiming_at_entity_using_combat_style(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13, p14];
    unsafe { native::invoke(0x78426D0982D083C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_entity(ped: Ped, target: Entity, duration: i32, distance: f32, speed: f32, p5: f32, p6: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target), NativeArg::int(duration), NativeArg::float(distance), NativeArg::float(speed), NativeArg::float(p5), NativeArg::int(p6)];
    unsafe { native::invoke(0x6A071245EB0D1882, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_entity_while_aiming_at_entity(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10];
    unsafe { native::invoke(0x97465886D35210E9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_entity_while_aiming_at_entity_using_combat_style(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10];
    unsafe { native::invoke(0xCEF0117C233026AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_go_to_whistle(ped: Ped, p1: Ped, whistle_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::int(whistle_type)];
    unsafe { native::invoke(0xBAD6545608CECA6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_grapple(ped: Ped, target_ped: Ped, grapple_style: Hash, p3: i32, p4: f32, p5: i32, p6: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::hash(grapple_style), NativeArg::int(p3), NativeArg::float(p4), NativeArg::int(p5), NativeArg::int(p6)];
    unsafe { native::invoke(0x779A2FFACEFAEA7B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_guard(ped: Ped, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2];
    unsafe { native::invoke(0xB9FB242EACCAF30F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_guard_assigned_defensive_area(ped: Ped, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), p6];
    unsafe { native::invoke(0xD2A207EEBDF9889B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_guard_current_position(ped: Ped, p1: f32, p2: f32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x4A58A47A72E3FCB4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_hands_up(ped: Ped, duration: i32, facing_ped: Ped, time_to_face_ped: i32, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(duration), NativeArg::int(facing_ped), NativeArg::int(time_to_face_ped), NativeArg::int(flags)];
    unsafe { native::invoke(0xF2EAB31979A7F910, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_hitch_animal(ped: Ped, scenario_point: i32, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(scenario_point), NativeArg::int(flag)];
    unsafe { native::invoke(0x9030AD4B6207BFE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_hogtieable(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6AFD8FE0D723328F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_hogtie_target_ped(ped: Ped, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped)];
    unsafe { native::invoke(0x27829AFD3E03AC1A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_horse_action(ped: Ped, action: i32, target_ped: Ped, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(action), NativeArg::int(target_ped), p3];
    unsafe { native::invoke(0xA09CFD29100F06C3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_investigate(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x5C8514540D27FBFB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_jump(ped: Ped, unused: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(unused)];
    unsafe { native::invoke(0x0AE4086104E067B1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_knocked_out(ped: Ped, p1: f32, permanently: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::boolean(permanently)];
    unsafe { native::invoke(0xF90427F00A495A28, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_knocked_out_and_hogtied(ped: Ped, ko_time_offset: f32, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(ko_time_offset), NativeArg::int(flags)];
    unsafe { native::invoke(0x42AC6401ABB8C7E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_lasso_ped(ped: Ped, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped)];
    unsafe { native::invoke(0xC716EB2BD16370A3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_lead_and_converse(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8];
    unsafe { native::invoke(0xAA19711D33C6708C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_lead_horse(ped: Ped, horse: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(horse)];
    unsafe { native::invoke(0x9A7A4A54596FE09D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_leave_any_vehicle(ped: Ped, p1: i32, task_flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::int(task_flag)];
    unsafe { native::invoke(0x504D54DF3F6F2247, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_leave_vehicle(ped: Ped, vehicle: Vehicle, flags: i32, unk_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::int(flags), NativeArg::int(unk_ped)];
    unsafe { native::invoke(0xD3DBCE61A490BE02, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_look_at_coord(ped: Ped, x: f32, y: f32, z: f32, duration: i32, flags: i32, p6: i32, p7: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(duration), NativeArg::int(flags), NativeArg::int(p6), NativeArg::boolean(p7)];
    unsafe { native::invoke(0x6FA46612594F7973, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_look_at_entity(ped: Ped, look_at_target: Entity, duration: i32, p3: i32, p4: i32, p5: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(look_at_target), NativeArg::int(duration), NativeArg::int(p3), NativeArg::int(p4), NativeArg::int(p5)];
    unsafe { native::invoke(0x69F4BE8C8CC4796C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_loot_entity(ped: Ped, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity)];
    unsafe { native::invoke(0x48FAE038401A2888, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_loot_nearest_entity(ped: Ped, x: f32, y: f32, z: f32, p4: i32, p5: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(p4), NativeArg::float(p5)];
    unsafe { native::invoke(0xCF1501CBC4059412, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_melee(ped: Ped, target_ped: Ped, p2: Hash, p3: NativeArg, p4: NativeArg, p5: f32, p6: NativeArg, p7: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::hash(p2), p3, p4, NativeArg::float(p5), p6, NativeArg::float(p7)];
    unsafe { native::invoke(0x482C99D0B38D1B0A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_mount_animal(ped: Ped, mount: Ped, timer: i32, seat_index: i32, ped_speed: f32, mount_style: i32, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(mount), NativeArg::int(timer), NativeArg::int(seat_index), NativeArg::float(ped_speed), NativeArg::int(mount_style), p6, p7];
    unsafe { native::invoke(0x92DB0739813C5186, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_move_be_in_formation(ped: Ped, p1: NativeArg, p2: f32, p3: f32, p4: f32, p5: f32, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), p6];
    unsafe { native::invoke(0x4AA5AA97C65E4A2F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_move_follow_road_using_navmesh(ped: Ped, move_blend_ratio: f32, x: f32, y: f32, z: f32, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(move_blend_ratio), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), p5];
    unsafe { native::invoke(0x79482C12482A860D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_move_in_traffic(ped: Ped, p1: f32, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), p2, p3];
    unsafe { native::invoke(0x8AA1593AEC087A29, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_move_in_traffic_away_from_entity(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4];
    unsafe { native::invoke(0x13DED0BC45600FE1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_move_in_traffic_to_destination(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0xDCA3A13F7A45338B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_move_network_advanced_by_name_with_init_params(ped: Ped, move_network_def_name: &str, task_data: &mut Any, x_pos: f32, y_pos: f32, z_pos: f32, x_rot: f32, y_rot: f32, z_rot: f32, p9: i32, p10: f32, p11: i32, p12: i32, flag: i32, p14: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(move_network_def_name), NativeArg::in_out(task_data), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::float(x_rot), NativeArg::float(y_rot), NativeArg::float(z_rot), NativeArg::int(p9), NativeArg::float(p10), NativeArg::int(p11), NativeArg::int(p12), NativeArg::int(flag), NativeArg::int(p14)];
    unsafe { native::invoke(0x7B6A04F98BBAFB2C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_move_network_advanced_by_name_with_init_params_attached(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg, p15: NativeArg, p16: NativeArg, p17: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13, p14, p15, p16, p17];
    unsafe { native::invoke(0xF92171093BCABED4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_move_network_by_name(ped: Ped, task: &str, multiplier: f32, p3: bool, anim_dict: &str, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(task), NativeArg::float(multiplier), NativeArg::boolean(p3), NativeArg::string(anim_dict), NativeArg::int(flags)];
    unsafe { native::invoke(0x2D537BA194896636, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_move_network_by_name_with_init_params(ped: Ped, move_network_def_name: &str, task_data: &mut Any, p3: f32, p4: bool, anim_dict: &str, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(move_network_def_name), NativeArg::in_out(task_data), NativeArg::float(p3), NativeArg::boolean(p4), NativeArg::string(anim_dict), NativeArg::int(flags)];
    unsafe { native::invoke(0x139805C2A67C4795, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_patrol(ped: Ped, patrol_route: &str, p2: NativeArg, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(patrol_route), p2, NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xBDA5DF49D080FE4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_pause(ped: Ped, ms: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ms)];
    unsafe { native::invoke(0xE73A266DB0CA9042, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_ped_slide_to_coord(ped: Ped, x: f32, y: f32, z: f32, heading: f32, p5: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::float(p5)];
    unsafe { native::invoke(0xD04FE6765D990A06, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_perform_sequence(ped: Ped, task_sequence_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(task_sequence_id)];
    unsafe { native::invoke(0x5ABA3986D90D8A3B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_perform_sequence_from_progress(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3];
    unsafe { native::invoke(0x89221B16730234F0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_persistent_character(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4391700CBD89C3D8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_pickup_carriable_entity(ped: Ped, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity)];
    unsafe { native::invoke(0x502EC17B1BED4BFA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_pick_up_weapon(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0x55B0ECFD98596624, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_place_carried_entity_at_coord(ped: Ped, entity: Entity, x: f32, y: f32, z: f32, p5: f32, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p5), NativeArg::int(flags)];
    unsafe { native::invoke(0xC7F0B43DCDC57E3D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_place_carried_entity_on_mount(ped: Ped, entity: Entity, mount: Ped, p3: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), NativeArg::int(mount), NativeArg::float(p3)];
    unsafe { native::invoke(0x6D3D87C57B3D52C7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_plant_bomb(ped: Ped, x: f32, y: f32, z: f32, heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading)];
    unsafe { native::invoke(0x965FEC691D55E9BF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_play_anim(ped: Ped, anim_dict: &str, anim_name: &str, speed: f32, speed_multiplier: f32, duration: i32, flags: i32, playback_rate: f32, p8: bool, ik_flags: i32, p10: bool, task_filter: &str, p12: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(anim_dict), NativeArg::string(anim_name), NativeArg::float(speed), NativeArg::float(speed_multiplier), NativeArg::int(duration), NativeArg::int(flags), NativeArg::float(playback_rate), NativeArg::boolean(p8), NativeArg::int(ik_flags), NativeArg::boolean(p10), NativeArg::string(task_filter), NativeArg::boolean(p12)];
    unsafe { native::invoke(0xEA47FE3719165B94, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_play_anim_advanced(ped: Ped, anim_dict: &str, anim_name: &str, pos_x: f32, pos_y: f32, pos_z: f32, rot_x: f32, rot_y: f32, rot_z: f32, speed: f32, speed_multiplier: f32, duration: i32, flags: i32, p13: f32, p14: i32, p15: i32, p16: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(anim_dict), NativeArg::string(anim_name), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(speed), NativeArg::float(speed_multiplier), NativeArg::int(duration), NativeArg::int(flags), NativeArg::float(p13), NativeArg::int(p14), NativeArg::int(p15), NativeArg::int(p16)];
    unsafe { native::invoke(0x83CDB10EA29B370B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_play_emote_with_hash(ped: Ped, emote_type: i32, playback_mode: i32, emote: Hash, is_secondary_task: bool, can_break_out: bool, disable_early_out_anim_tag: bool, ignore_invalid_main_task: bool, destroy_props: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(emote_type), NativeArg::int(playback_mode), NativeArg::hash(emote), NativeArg::boolean(is_secondary_task), NativeArg::boolean(can_break_out), NativeArg::boolean(disable_early_out_anim_tag), NativeArg::boolean(ignore_invalid_main_task), NativeArg::boolean(destroy_props)];
    unsafe { native::invoke(0xB31A277C1AC7B7FF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_play_upper_anim_facing_entity(ped: Ped, anim_dict: &str, anim_name: &str, entity: Entity, p4: i32, p5: f32, p6: f32, p7: i32, p8: f32, p9: bool, p10: bool, p11: f32, p12: &str, p13: i32, p14: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(anim_dict), NativeArg::string(anim_name), NativeArg::int(entity), NativeArg::int(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::int(p7), NativeArg::float(p8), NativeArg::boolean(p9), NativeArg::boolean(p10), NativeArg::float(p11), NativeArg::string(p12), NativeArg::int(p13), NativeArg::float(p14)];
    unsafe { native::invoke(0xAD67214236AB1CFE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_police(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x87BE56724650408E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_put_ped_directly_into_cover(ped: Ped, x: f32, y: f32, z: f32, timeout: i32, p5: bool, p6: f32, p7: NativeArg, p8: NativeArg, coverpoint: ScrHandle, p10: bool, p11: bool, p12: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(timeout), NativeArg::boolean(p5), NativeArg::float(p6), p7, p8, NativeArg::int(coverpoint), NativeArg::boolean(p10), NativeArg::boolean(p11), p12];
    unsafe { native::invoke(0x4172393E6BE1FECE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_put_ped_directly_into_grapple(ped: Ped, grapple_target: Ped, grapple_style: Hash, p3: f32, p4: f32, p5: bool, p6: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(grapple_target), NativeArg::hash(grapple_style), NativeArg::float(p3), NativeArg::float(p4), NativeArg::boolean(p5), NativeArg::int(p6)];
    unsafe { native::invoke(0xA05F3F20889D7A5B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_put_ped_directly_into_melee(ped: Ped, melee_target: Ped, melee_style: Hash, p3: f32, anim_blend_ratio: f32, p5: bool, p6: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(melee_target), NativeArg::hash(melee_style), NativeArg::float(p3), NativeArg::float(anim_blend_ratio), NativeArg::boolean(p5), NativeArg::int(p6)];
    unsafe { native::invoke(0x1C6CD14A876FFE39, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_react(ped: Ped, reacting_to: Entity, x: f32, y: f32, z: f32, reaction_name: &str, p6: f32, p7: f32, p8: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(reacting_to), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::string(reaction_name), NativeArg::float(p6), NativeArg::float(p7), NativeArg::int(p8)];
    unsafe { native::invoke(0xC4C32C31920E1B70, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_reload_weapon(ped: Ped, unused: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(unused)];
    unsafe { native::invoke(0x62D2916F56B9CD2D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_revive_target(ped: Ped, reviver: Ped, tool: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(reviver), NativeArg::hash(tool)];
    unsafe { native::invoke(0x356088527D9EBAAD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_ride_train(ped: Ped, train: Vehicle, scenario_point: i32, scenario_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(train), NativeArg::int(scenario_point), NativeArg::hash(scenario_hash)];
    unsafe { native::invoke(0x37FB1C870E2EC2C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_rob_ped(ped: Ped, target: Ped, p2: f32, flag: i32, p4: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target), NativeArg::float(p2), NativeArg::int(flag), NativeArg::int(p4)];
    unsafe { native::invoke(0x7BB967F85D8CCBDB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_scripted_animation(ped: Ped, args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(args)];
    unsafe { native::invoke(0x126EF75F1E17ABE5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_seek_clear_los_to_entity(ped: Ped, entity: Entity, p2: f32, p3: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4)];
    unsafe { native::invoke(0x8D7F2A63688C20A4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_seek_cover_from_ped(ped: Ped, from_ped: Ped, duration: i32, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(from_ped), NativeArg::int(duration), p3, p4, p5];
    unsafe { native::invoke(0x84D32B3BEC531324, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_seek_cover_from_pos(ped: Ped, x: f32, y: f32, z: f32, duration: i32, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(duration), p5, p6, p7];
    unsafe { native::invoke(0x75AC2B60386D89F2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_seek_cover_to_coords(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8, p9, p10];
    unsafe { native::invoke(0x39246A6958EF072C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_seek_cover_to_cover_point(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8];
    unsafe { native::invoke(0xD43D95C7A869447F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_set_blocking_of_non_temporary_events(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x90D2156198831D69, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_set_crouch_movement(ped: Ped, p1: bool, p2: NativeArg, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), p2, NativeArg::boolean(p3)];
    unsafe { native::invoke(0x17293C633C8AC019, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_set_sphere_defensive_area(ped: Ped, p1: f32, p2: f32, p3: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4)];
    unsafe { native::invoke(0x933C06518B52A9A4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_set_stealth_movement(ped: Ped, p1: bool, p2: NativeArg, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), p2, NativeArg::boolean(p3)];
    unsafe { native::invoke(0x4C3FA937B44A90FA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_shocking_event_react(ped: Ped, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2];
    unsafe { native::invoke(0x452419CBD838065B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_shoot_at_coord(ped: Ped, x: f32, y: f32, z: f32, duration: i32, firing_pattern: Hash, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(duration), NativeArg::hash(firing_pattern), p6];
    unsafe { native::invoke(0x46A6CC01E0826106, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_shoot_at_entity(entity: Entity, target_entity: Entity, duration: i32, firing_pattern: Hash, affect_cocked_state: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(target_entity), NativeArg::int(duration), NativeArg::hash(firing_pattern), NativeArg::boolean(affect_cocked_state)];
    unsafe { native::invoke(0x08DA95E8298AE772, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_shoot_with_weapon(ped: Ped, args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(args)];
    unsafe { native::invoke(0x08AA95E8298AE772, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_shuffle_to_next_vehicle_seat(ped: Ped, vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle)];
    unsafe { native::invoke(0x7AA80209BDA643EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_smart_flee_coord(ped: Ped, x: f32, y: f32, z: f32, distance: f32, time: i32, flee_type: i32, flee_speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(distance), NativeArg::int(time), NativeArg::int(flee_type), NativeArg::float(flee_speed)];
    unsafe { native::invoke(0x94587F17E9C365D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_smart_flee_ped(ped: Ped, flee_from_target: Ped, flee_distance: f32, flee_time: i32, flee_type: i32, flee_speed: f32, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flee_from_target), NativeArg::float(flee_distance), NativeArg::int(flee_time), NativeArg::int(flee_type), NativeArg::float(flee_speed), NativeArg::int(target_ped)];
    unsafe { native::invoke(0x22B0D0E37CCB840D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_stand_guard(ped: Ped, x: f32, y: f32, z: f32, heading: f32, scenario_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::string(scenario_name)];
    unsafe { native::invoke(0xAE032F8BBA959E90, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_stand_still(ped: Ped, time: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(time)];
    unsafe { native::invoke(0x919BE13EED931959, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_start_scenario_at_position(ped: Ped, scenario_hash: Hash, x: f32, y: f32, z: f32, heading: f32, duration: i32, sitting_scenario: bool, teleport: bool, p9: &str, p10: f32, p11: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(scenario_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::int(duration), NativeArg::boolean(sitting_scenario), NativeArg::boolean(teleport), NativeArg::string(p9), NativeArg::float(p10), NativeArg::boolean(p11)];
    unsafe { native::invoke(0x4D1F61FC34AF3CD1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_start_scenario_in_place_hash(ped: Ped, scenario_hash: Hash, duration: i32, play_enter_anim: bool, conditional_hash: Hash, heading: f32, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(scenario_hash), NativeArg::int(duration), NativeArg::boolean(play_enter_anim), NativeArg::hash(conditional_hash), NativeArg::float(heading), NativeArg::boolean(p6)];
    unsafe { native::invoke(0x524B54361229154F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_stay_in_cover(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xE5DA8615A6180789, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_stop_leading_horse(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xED27560703F37258, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_swap_fishing_bait(ped: Ped, bait: &str, without_buoy: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(bait), NativeArg::boolean(without_buoy)];
    unsafe { native::invoke(0x2C28AC30A72722DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_swap_weapon(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4];
    unsafe { native::invoke(0xA21C51255B205245, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_throw_projectile(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3];
    unsafe { native::invoke(0x7285951DBF6B5A51, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_turn_ped_to_face_coord(ped: Ped, x: f32, y: f32, z: f32, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(duration)];
    unsafe { native::invoke(0x1DDA930A0AC38571, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_turn_ped_to_face_entity(ped: Ped, target_entity: Entity, duration: i32, p3: f32, p4: f32, p5: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_entity), NativeArg::int(duration), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5)];
    unsafe { native::invoke(0x5AD23D40115353AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_turn_to_face_closest_ped(ped: Ped, p1: f32, p2: f32, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::float(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x84179419DBDD36F2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_use_nearest_scenario_chain_to_coord(ped: Ped, x: f32, y: f32, z: f32, distance: f32, p5: bool, p6: bool, p7: bool, p8: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(distance), NativeArg::boolean(p5), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0x9FDA1B3D7E7028B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_use_nearest_scenario_chain_to_coord_warp(ped: Ped, x: f32, y: f32, z: f32, distance: f32, p5: bool, p6: bool, p7: bool, p8: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(distance), NativeArg::boolean(p5), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::boolean(p8)];
    unsafe { native::invoke(0x97A28E63F0BA5631, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_use_nearest_scenario_to_coord_warp(ped: Ped, x: f32, y: f32, z: f32, distance: f32, duration: i32, p6: bool, p7: bool, p8: bool, p9: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(distance), NativeArg::int(duration), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::boolean(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x58E2E0F23F6B76C3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_use_nearest_train_scenario_to_coord_warp(ped: Ped, x: f32, y: f32, z: f32, distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(distance)];
    unsafe { native::invoke(0x3774B03456DD6106, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_use_random_scenario_in_group(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4];
    unsafe { native::invoke(0x14747F4A5971DE4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_use_scenario_point(ped: Ped, scenario: i32, conditional_anim: &str, p3: i32, p4: bool, p5: bool, p6: Hash, p7: bool, p8: f32, p9: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(scenario), NativeArg::string(conditional_anim), NativeArg::int(p3), NativeArg::boolean(p4), NativeArg::boolean(p5), NativeArg::hash(p6), NativeArg::boolean(p7), NativeArg::float(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0xCCDAE6324B6A821C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_aim_at_coord(ped: Ped, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x447C1E9EF844BC0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_aim_at_ped(ped: Ped, target: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target)];
    unsafe { native::invoke(0xE41885592B08B097, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_drive_straight_to_point(driver: Ped, vehicle: Vehicle, x: f32, y: f32, z: f32, p5: f32, p6: f32, flag: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(driver), NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p5), NativeArg::float(p6), NativeArg::int(flag)];
    unsafe { native::invoke(0x089FF2FB965F0A29, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_drive_to_coord(ped: Ped, vehicle: Vehicle, x: f32, y: f32, z: f32, speed: f32, driving_style: i32, vehicle_model: Hash, driving_mode: i32, stop_range: f32, straight_line_dist: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(speed), NativeArg::int(driving_style), NativeArg::hash(vehicle_model), NativeArg::int(driving_mode), NativeArg::float(stop_range), NativeArg::float(straight_line_dist)];
    unsafe { native::invoke(0xE2A2AA2F659D77A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_drive_to_destination(driver: Ped, vehicle: Vehicle, x: f32, y: f32, z: f32, speed: f32, driving_flags: i32, p7: i32, stopping_range1: f32, stopping_range2: f32, p10: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(driver), NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(speed), NativeArg::int(driving_flags), NativeArg::int(p7), NativeArg::float(stopping_range1), NativeArg::float(stopping_range2), NativeArg::boolean(p10)];
    unsafe { native::invoke(0x7F241A0D14354583, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_drive_wander(ped: Ped, vehicle: Vehicle, speed: f32, driving_style: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::float(speed), NativeArg::int(driving_style)];
    unsafe { native::invoke(0x480142959D337D00, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_escort(ped: Ped, vehicle: Vehicle, target_vehicle: Vehicle, mode: i32, speed: f32, driving_style: i32, min_distance: f32, p7: i32, no_roads_distance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::int(target_vehicle), NativeArg::int(mode), NativeArg::float(speed), NativeArg::int(driving_style), NativeArg::float(min_distance), NativeArg::int(p7), NativeArg::float(no_roads_distance)];
    unsafe { native::invoke(0x0FA6E4B75F302400, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_follow_waypoint_recording(ped: Ped, vehicle: Vehicle, waypoint_recording: &str, driving_mode: i32, p4: NativeArg, e_waypoint: i32, flag: i32, p7: f32, p8: bool, stopping_dist: f32, p10: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::string(waypoint_recording), NativeArg::int(driving_mode), p4, NativeArg::int(e_waypoint), NativeArg::int(flag), NativeArg::float(p7), NativeArg::boolean(p8), NativeArg::float(stopping_dist), p10];
    unsafe { native::invoke(0x3123FAA6DB1CF7ED, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_goto_navmesh(ped: Ped, vehicle: Vehicle, x: f32, y: f32, z: f32, speed: f32, behavior_flag: i32, stopping_range: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(speed), NativeArg::int(behavior_flag), NativeArg::float(stopping_range)];
    unsafe { native::invoke(0x195AEEB13CEFE2EE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_mission(driver: Ped, vehicle: Vehicle, vehicle_target: Vehicle, mission_type: i32, p4: f32, p5: NativeArg, p6: f32, p7: f32, drive_against_traffic: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(driver), NativeArg::int(vehicle), NativeArg::int(vehicle_target), NativeArg::int(mission_type), NativeArg::float(p4), p5, NativeArg::float(p6), NativeArg::float(p7), NativeArg::boolean(drive_against_traffic)];
    unsafe { native::invoke(0x659427E0EF36BCDE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_mission_ped_target(ped: Ped, vehicle: Vehicle, ped_target: Ped, mode: i32, max_speed: f32, driving_style: i32, min_distance: f32, p7: f32, drive_against_traffic: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::int(ped_target), NativeArg::int(mode), NativeArg::float(max_speed), NativeArg::int(driving_style), NativeArg::float(min_distance), NativeArg::float(p7), NativeArg::boolean(drive_against_traffic)];
    unsafe { native::invoke(0x9454528DF15D657A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_shoot_at_coord(ped: Ped, x: f32, y: f32, z: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p4)];
    unsafe { native::invoke(0x5190796ED39C9B6D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_shoot_at_ped(ped: Ped, target: Ped, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target), NativeArg::float(p2)];
    unsafe { native::invoke(0x10AB107B887214D8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_temp_action(driver: Ped, vehicle: Vehicle, action: i32, time: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(driver), NativeArg::int(vehicle), NativeArg::int(action), NativeArg::int(time)];
    unsafe { native::invoke(0xC429DCEEB339E129, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_walk_away(ped: Ped, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity)];
    unsafe { native::invoke(0x04ACFAC71E6858F9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_wander_and_converse_with_ped(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3];
    unsafe { native::invoke(0x8AC76D1408731732, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_wander_in_area(ped: Ped, x: f32, y: f32, z: f32, radius: f32, p5: f32, p6: f32, p7: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::float(p5), NativeArg::float(p6), NativeArg::int(p7)];
    unsafe { native::invoke(0xE054346CA3A0F315, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_wander_in_volume(ped: Ped, volume: Volume, p2: f32, p3: f32, p4: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(volume), NativeArg::float(p2), NativeArg::float(p3), NativeArg::int(p4)];
    unsafe { native::invoke(0x9FDA168777B28424, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_wander_standard(ped: Ped, p1: f32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xBB9CE077274F6A1B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_wander_swim(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0x527EA3DB8BC7F03B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_warp_ped_into_vehicle(ped: Ped, vehicle: Vehicle, seat: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::int(seat)];
    unsafe { native::invoke(0x9A7D091411C5F684, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_weapon(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7157B82D60E4BC46, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_whistle_anim(ped: Ped, aud_ped_whistle_type: Hash, p2: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(aud_ped_whistle_type), NativeArg::hash(p2)];
    unsafe { native::invoke(0xD6401A1B2F63BED6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uncuff_ped(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x67406F2C8F87FC4F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unhogtie_ped(ped: Ped, flags: i32, getup_set_hash: Hash, p3: &str, p4: &str, p5: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flags), NativeArg::hash(getup_set_hash), NativeArg::string(p3), NativeArg::string(p4), NativeArg::float(p5)];
    unsafe { native::invoke(0x79559BAD83CCD038, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_task_hands_up_duration(ped: Ped, duration: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(duration)];
    unsafe { native::invoke(0xA98FCAFD7893C834, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn use_waypoint_recording_as_assisted_movement_route(waypoint_recording: &str, p1: bool, p2: f32, p3: f32, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(waypoint_recording), NativeArg::boolean(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x5A353B8E6B1095B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn vehicle_waypoint_playback_get_is_paused(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x4D6D30AB18B0B089, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn vehicle_waypoint_playback_override_speed(vehicle: Vehicle, speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(speed)];
    unsafe { native::invoke(0x121F0593E0A431D7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn vehicle_waypoint_playback_pause(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x8A4E6AC373666BC5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn vehicle_waypoint_playback_resume(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xDC04FCAA7839D492, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn vehicle_waypoint_playback_use_default_speed(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x5CEB25A7D2848963, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_get_is_aiming(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xD73A5D1F0325C71C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_get_is_paused(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x701375A7D43F01CB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_get_is_shooting(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA5B94DF8AF058F46, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_override_speed(ped: Ped, speed: f32, p2: NativeArg, p3: f32, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(speed), p2, NativeArg::float(p3), p4];
    unsafe { native::invoke(0x7D7D2B47FA788E85, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_pause(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3];
    unsafe { native::invoke(0x0F342546AA06FED5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_resume(ped: Ped, p1: bool, p2: i32, p3: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x244F70C84C547D2D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_start_aiming_at_coord(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x8968400D900ED8B3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_start_aiming_at_entity(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x4F158205E0C74385, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_start_aiming_at_ped(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x20E330937C399D29, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_start_shooting_at_coord(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x057A25CFCC9DB671, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_start_shooting_at_entity(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x4AF458F71C1196D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_start_shooting_at_ped(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xE70BA7B90F8390DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_stop_aiming_or_shooting(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x47EFA040EBB8E2EA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_playback_use_default_speed(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6599D834B12D0800, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_recording_get_closest_waypoint(waypoint_recording: &str, x: f32, y: f32, z: f32, point: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(waypoint_recording), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(point)];
    unsafe { native::invoke(0xB629A298081F876F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_recording_get_coord(waypoint_recording: &str, point: i32, coord: &mut Vector3) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(waypoint_recording), NativeArg::int(point), NativeArg::in_out(coord)];
    unsafe { native::invoke(0x2FB897405C90B361, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_recording_get_num_points(waypoint_recording: &str, points: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(waypoint_recording), NativeArg::in_out(points)];
    unsafe { native::invoke(0x5343532C01A07234, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn waypoint_recording_get_speed_at_point(waypoint_recording: &str, point: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::string(waypoint_recording), NativeArg::int(point)];
    unsafe { native::invoke(0x005622AEBC33ACA9, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0000_a8_acdc2_e1_b6_a(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x0000A8ACDC2E1B6A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x00_ffe0_f85253_c572(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x00FFE0F85253C572, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0365000_d8_bf86531(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0365000D8BF86531, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x098036_cab8373_d36(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x098036CAB8373D36, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x098_caa6_dbe7_d8_d82(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x098CAA6DBE7D8D82, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_a98_a362_c5_a19_a43(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0A98A362C5A19A43, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_d322_aef8878_b8_fe(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0D322AEF8878B8FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_e184495_b27_bb57_d() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0E184495B27BB57D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_f4_f6_c4_ce471259_d(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0F4F6C4CE471259D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x10_adfdf07_b7_dffba(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x10ADFDF07B7DFFBA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x10_c44_f633_e2_d6_d9_e(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x10C44F633E2D6D9E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x11_c7_ce1_ae38911_b5(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x11C7CE1AE38911B5, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x152664_aa3188_b193(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x152664AA3188B193, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1632_eb9386_cdbe64(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x1632EB9386CDBE64, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x19_bc99_c678_fba139(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x19BC99C678FBA139, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_a7_d63_cb1_b0_bb223(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1A7D63CB1B0BB223, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_ac5_a8_ab50_cfaa33(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1AC5A8AB50CFAA33, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_f298_c7_bd30_d1240(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1F298C7BD30D1240, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_f7_a9_a9_c38_c13_a56(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x1F7A9A9C38C13A56, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2064_b33_f6_e6_b92_d4(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x2064B33F6E6B92D4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x22_cd2_c33_ed4467_a1(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x22CD2C33ED4467A1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x22_cdbf317_c40_a122(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x22CDBF317C40A122, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x23767_d80_c7_eed7_c6(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x23767D80C7EED7C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x28_ef780_bdea8_a639(ped: Ped, facing_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(facing_ped)];
    unsafe { native::invoke(0x28EF780BDEA8A639, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2948235_db2058_e99(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x2948235DB2058E99, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_a10538_d0_a005_e81(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x2A10538D0A005E81, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_b8_af29_a78024_bd3(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2B8AF29A78024BD3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_c497_bdef897_c6_df(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2C497BDEF897C6DF, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_d657_b10_f211_c572(ped: Ped, p1: f32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x2D657B10F211C572, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_e1_d6_d87346_bb7_d2(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x2E1D6D87346BB7D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_eb977293923_c723(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x2EB977293923C723, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x30146_c25686_b7836(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x30146C25686B7836, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x30_b391915538_ebe2(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x30B391915538EBE2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x31_bb338_f64_d5_c861(ped: Ped, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x31BB338F64D5C861, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x358_a1_a751_b335_a11(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x358A1A751B335A11, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x370_f57_c47_f68_ebca(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x370F57C47F68EBCA, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_bbeecc5_b8_f35318(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3BBEECC5B8F35318, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_f8387_db1_b9_f31_b7(script_struct: &mut Any, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(script_struct), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x3F8387DB1B9F31B7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_feb770_d8_ed9047_a(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x3FEB770D8ED9047A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x41323_f4_e0_c4_ae94_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x41323F4E0C4AE94B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4161648394262_fdf(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x4161648394262FDF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x41_d1331_afad5_a091(ped: Ped, p1: i32, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), p2];
    unsafe { native::invoke(0x41D1331AFAD5A091, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x450080_ddedb91258(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x450080DDEDB91258, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_a7_d73989_f52_eb37(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x4A7D73989F52EB37, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_ba972_d0_e5_ad8122(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x4BA972D0E5AD8122, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_e806_a395_d43_a458(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x4E806A395D43A458, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_f57397388_e1_dff8() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4F57397388E1DFF8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x50_aa09_a0_da64_e73_c(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x50AA09A0DA64E73C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5217_b7_b6_db78_e1_f3(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x5217B7B6DB78E1F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x59_ae5_ca4_ffb4_e378(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x59AE5CA4FFB4E378, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x59_aea4_dc640814_b9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x59AEA4DC640814B9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_b68_d0007_d9_c92_eb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x5B68D0007D9C92EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_d9_b0_baaf04_cf65_b(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x5D9B0BAAF04CF65B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x615_dc4_a82_e90_bb48(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x615DC4A82E90BB48, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x643_fd1556_f621772(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x643FD1556F621772, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x651_f0530083_c0_e5_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x651F0530083C0E5A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x65_d281985_f2_bdfc2(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x65D281985F2BDFC2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x673_a8779_d229_ba5_a(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x673A8779D229BA5A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x678_d3226_cf70_b9_c8(ped: Ped, p1: bool) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x678D3226CF70B9C8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c269_f673_c47031_e(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6C269F673C47031E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_dac799857_ef3_f11(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6DAC799857EF3F11, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x722_d6_a49200174_fe(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x722D6A49200174FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x748_d5_e0_d2_a1_a4_c61(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x748D5E0D2A1A4C61, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x74_f0209674864_cbd() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x74F0209674864CBD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x764_db5_a48390_fbad(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x764DB5A48390FBAD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x76610_d12_a838_ebde(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x76610D12A838EBDE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_cb99_fadde73_cd1_b(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7CB99FADDE73CD1B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_fb78_b2199_c10_e92(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7FB78B2199C10E92, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x801_bd27403_f3_cba0(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x801BD27403F3CBA0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x827_a58_ced9_d4_d5_b4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x827A58CED9D4D5B4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x82_ed59_f095056550(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x82ED59F095056550, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x865732725536_ee39(p0: NativeArg) -> Result<Vector3, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x865732725536EE39, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8798_cf6815_b8_fe0_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8798CF6815B8FE0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x885_d19_ac2_b6_fbff4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x885D19AC2B6FBFF4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x88_fd60_d846_d9_cd63(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x88FD60D846D9CD63, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_e1_dde26_d270_cc5_e(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8E1DDE26D270CC5E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_f8_c84363810691_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8F8C84363810691A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9050_df2_c53801208(ped: Ped, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(p1)];
    unsafe { native::invoke(0x9050DF2C53801208, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x90703_a8_f75_ee4_abd(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x90703A8F75EE4ABD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x908_bb14_bce85_c80_e(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x908BB14BCE85C80E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x91_cb5_e431_f579_ba1(p0: NativeArg) -> Result<Vector3, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x91CB5E431F579BA1, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x920684_be432875_b1(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x920684BE432875B1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x954451_ea2_d2120_fb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x954451EA2D2120FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9585_ff23_c4_b8_ede0(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9585FF23C4B8EDE0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9667_cce29_bfa0780(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9667CCE29BFA0780, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x974_da3408_dec4_e79(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x974DA3408DEC4E79, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_addbb9242179_d56(object: Object, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object), NativeArg::int(ped)];
    unsafe { native::invoke(0x9ADDBB9242179D56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_b6_a58_fdb0024_f12(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x9B6A58FDB0024F12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_c8_f42_a5_d1859_dc1(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9C8F42A5D1859DC1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_ebd34958_ab6_f824(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9EBD34958AB6F824, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_ff5_f9_b24_e870748(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9FF5F9B24E870748, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a052608_a12559_bbb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA052608A12559BBB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a21_aa2_f0_c2180125(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA21AA2F0C2180125, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a263_adbbc8056214(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA263ADBBC8056214, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a42_dc7919159_cccf(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA42DC7919159CCCF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a6_a76_d666_a281_f2_d(p0: NativeArg, item: Hash) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::hash(item)];
    unsafe { native::invoke(0xA6A76D666A281F2D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a7479_fb665361_edb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA7479FB665361EDB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a9_e7672_f8_c6_c6_f74(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA9E7672F8C6C6F74, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_adc45010_bc17_af0_e(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xADC45010BC17AF0E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_af2_ef28_ce3084505(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xAF2EF28CE3084505, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b2_d15_d3551_fe4_fae(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xB2D15D3551FE4FAE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b2_f47_a1_afdfcc595(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB2F47A1AFDFCC595, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b520_dbda7_fcf573_f(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB520DBDA7FCF573F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b79817_db31_ff72_b9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB79817DB31FF72B9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b8_e213_d02_f37947_d(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xB8E213D02F37947D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b8_e3486_d107_f4194(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB8E3486D107F4194, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_baab791_aa72_c2821(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xBAAB791AA72C2821, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bc3_f847_ae2_c3_dc65(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xBC3F847AE2C3DC65, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bd70108_d01875299(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xBD70108D01875299, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_bedbe39_b5_fd98_fd6(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBEDBE39B5FD98FD6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_beefbb608_d2_aa68_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xBEEFBB608D2AA68A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ce4_e669400_e5_f8_aa(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xCE4E669400E5F8AA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d0_abc4_ea3_b5_e21_a0(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD0ABC4EA3B5E21A0, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d508_fa229_f1_c4900(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xD508FA229F1C4900, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d999_e379265_a4501(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xD999E379265A4501, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_de0_c8_b145_ea466_ff(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xDE0C8B145EA466FF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_df56_a2_b50_c04_dea4(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDF56A2B50C04DEA4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_df94844_d474_f31_e5(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xDF94844D474F31E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e01_c8_dc8_edd28_d31(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE01C8DC8EDD28D31, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e01_f55_b2896_f6_b37(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE01F55B2896F6B37, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e05_a5_d39_be6_e93_af(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE05A5D39BE6E93AF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e116_f6_f2_da2_d777_e(p0: NativeArg) -> Result<Vector3, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE116F6F2DA2D777E, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e1_c105_e6_bba48270() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE1C105E6BBA48270, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e2_cf104_add49_d4_bf(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE2CF104ADD49D4BF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e55478_c5_edf70_ac2(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE55478C5EDF70AC2, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e5831_aa1_e2_fd147_c(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE5831AA1E2FD147C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e62754_d09354_f6_cf(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE62754D09354F6CF, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e69_fda40_aac3_efc0(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE69FDA40AAC3EFC0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e6_a151364_c600_b24(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE6A151364C600B24, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e9225354_fb7437_a7(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE9225354FB7437A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e9_a6400_d1_a0_e7_a55(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE9A6400D1A0E7A55, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eaf87_da2_be78_a15_b(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xEAF87DA2BE78A15B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec516_fe805_d2_cb2_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xEC516FE805D2CB2D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_efd875_c2791_ebefd(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xEFD875C2791EBEFD, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f3_c3503276_f4_a034(entity: Entity, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), p1];
    unsafe { native::invoke(0xF3C3503276F4A034, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f718931_a82_eeb898() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF718931A82EEB898, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f948_f4356_f010_f11(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xF948F4356F010F11, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f97_f462779_b31786(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF97F462779B31786, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa30_e2254461_adeb(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFA30E2254461ADEB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fdecca06_e8_b81346(ped: Ped) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xFDECCA06E8B81346, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fe5_d28_b9_b7837_cc1(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xFE5D28B9B7837CC1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ff8_afca532_b500_d4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xFF8AFCA532B500D4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_cover_blocking_volume(volume: Volume, p1: bool, p2: bool, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xEB2ED1DC3AEC0654, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_cover_point_for_entity(entity: Entity, x_offset: f32, y_offset: f32, z_offset: f32, heading: f32, p5: i32, p6: i32, p7: i32, p8: i32) -> Result<ScrHandle, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::float(heading), NativeArg::int(p5), NativeArg::int(p6), NativeArg::int(p7), NativeArg::int(p8)];
    unsafe { native::invoke(0x59872EA4CBD11C56, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_flee_target_coords(ped: Ped, x: f32, y: f32, z: f32, p4: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p4)];
    unsafe { native::invoke(0xE8F1A5B4CED3725A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn associate_prop_with_scenario(scenario: i32, entity: Entity, prop_name: &str, p3: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::int(entity), NativeArg::string(prop_name), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x8360C47380B6F351, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn calculate_waypoint_distance_from_start(waypoint_recording: &str, x: f32, y: f32, z: f32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::string(waypoint_recording), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x3ACC128510142B9D, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_vehicle_tasks(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x141BC64C8D7C5529, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_vehicle_tasks_secondary(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xEBA2081E0A5F4D17, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_herb_composites(asset: Hash, x: f32, y: f32, z: f32, heading: f32, ground_setting: i32, p6: &mut Any, p7: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(asset), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::int(ground_setting), NativeArg::in_out(p6), NativeArg::int(p7)];
    unsafe { native::invoke(0x5B4BBE80AD5972DC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_waypoint_path(path_name: &str, p1: &mut Any, nodes: i32, p3: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(path_name), NativeArg::in_out(p1), NativeArg::int(nodes), NativeArg::int(p3)];
    unsafe { native::invoke(0x5C885E0978B6AD60, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn cuff_ped(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x7981037A96E7D174, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_patch_objects_from_herb_composites(composite_id: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(composite_id), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x5758B1EE0C3FD4AC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_scenario_point(scenario: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0x81948DFE4F5A0283, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_carriable_ped(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x36D188AECB26094B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disassociate_prop_from_scenario(scenario: i32, prop_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::string(prop_name)];
    unsafe { native::invoke(0x6EF4E31B4D5D2DA0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_scenario_group_exist_hash(scenario_group: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(scenario_group)];
    unsafe { native::invoke(0x76E98B52369A289C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_scenario_point_have_props(scenario: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0xEA31F199A73801D3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn emit_ped_carriable_struggle_direction(ped: Ped, direction: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(direction)];
    unsafe { native::invoke(0x1ECF56C040FD839C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn emit_ped_carriable_struggle_intensity(ped: Ped, intensity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(intensity)];
    unsafe { native::invoke(0xBD1C3C0F271C39D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn evaluate_ped_carriable_struggle_available(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6AFD84AEAA3EA538, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn find_model_for_item(item: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(item)];
    unsafe { native::invoke(0xE47DD64B9F02677D, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn find_nearest_ped_around_animal(animal_ped: Ped, alive_only: bool, dead_only: bool, prefer_dead_predators: bool) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(animal_ped), NativeArg::boolean(alive_only), NativeArg::boolean(dead_only), NativeArg::boolean(prefer_dead_predators)];
    unsafe { native::invoke(0x244430C13BA5258E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_herb_composite_num_entities(composite_id: i32, out_entities: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(composite_id), NativeArg::in_out(out_entities)];
    unsafe { native::invoke(0x96C6ED22FB742C3E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_hogtie_escape_timer(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x4687E69D258BBE41, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_hold_to_reel_setting_enabled() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x5952DFA38FA529FE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_item_interaction_entity_from_ped(ped: Ped, item: Hash) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(item)];
    unsafe { native::invoke(0x05A0100EA714DB68, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_led_horse_from_ped(ped: Ped) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xED1F514AF4732258, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_linked_scenario_points(scenario_point: i32, out_points: &mut i32, max_points: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(scenario_point), NativeArg::in_out(out_points), NativeArg::int(max_points)];
    unsafe { native::invoke(0xE7BBC4E56B989449, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_is_ignoring_dead_bodies(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1948BBE561A2375A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_using_scenario_point(scenario: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0x5BA659955369B0E2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_writhe_break_free_progress(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x03D741CB4052E26C, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ransack_scenario_container_num_compartments(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x640A602946A8C972, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ransack_scenario_container_num_open_compartments(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x849791EBBDBA0362, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ransack_scenario_container_opening_state(entity: Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xB219612B5568E9EC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ransack_scenario_container_remaining_loot_count(entity: Entity) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x01AF8A3729231A43, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_revivable_horse() -> Result<Entity, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x351F74ED6177EBE7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_scenario_point_coords(scenario: i32, p1: bool) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xA8452DD321607029, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_scenario_point_entity(scenario: i32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0x7467165EE97D3C68, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_scenario_point_heading(scenario: i32, p1: bool) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xB93EA7184BAA85C3, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_scenario_point_ped_is_using(ped: Ped, p1: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xDF7993356F52359A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_scenario_point_radius(scenario: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0x6718F40313A2B5A6, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_scenario_point_type(scenario: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0xA92450B5AE687AAF, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_scenario_point_type_ped_is_using(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2D0571BB55879DA2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_script_task_action_time(ped: Ped, task: Hash) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(task)];
    unsafe { native::invoke(0xA710DC5D25F8B942, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_task_combat_ready_to_shoot(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5EA655F01D93667A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_task_fishing(ped: Ped, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xF3735ACD11ACD500, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_task_move_network_id(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xCACC2F9D994504B7, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_task_move_network_phase_float(ped: Ped, phase_name: &str) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(phase_name)];
    unsafe { native::invoke(0x844CEEE428EA35B0, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_task_ped_mount_leap_progress(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6BA606AB3A83BC4D, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_task_ped_mount_leap_state(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x9420FB11B8D77948, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_whistle_range_max_for_bonding_level(bonding_level: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(bonding_level)];
    unsafe { native::invoke(0x78D8C1D4EB80C588, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_whistle_range_min_for_bonding_level(bonding_level: i32) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(bonding_level)];
    unsafe { native::invoke(0xEB67D4E056C85A81, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_requested_carriable_config_loaded(carriable_config: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(carriable_config)];
    unsafe { native::invoke(0xB8F52A3F84A7CC59, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entity_revivable(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6C50B9DCCCA70023, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_hat_being_picked_up(hat_object: Object) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(hat_object)];
    unsafe { native::invoke(0x11CD066F54DA0133, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_hat_being_picked_up_2(hat_object: Object) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(hat_object)];
    unsafe { native::invoke(0x4ECCC2815CA79AE2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_arresting_any_ped(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xA9CC7856D52DBD25, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_being_led(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xAC5045AB7F1A34FD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_duelling(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC8B29D18022EA2B7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_leading_horse(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xEFC4303DDC6E60D3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_looking_at_coord(ped: Ped, x: f32, y: f32, z: f32, radius: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0x508F5053E3F6F0C4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_revivable_horse_prompt_visible(strict: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::boolean(strict)];
    unsafe { native::invoke(0x756C7B4C43DF0422, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scenario_group_enabled_hash(scenario_group: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(scenario_group)];
    unsafe { native::invoke(0xDCC374913DE6AAA6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scenario_in_use(scenario: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0x1ACBC313966C21F3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scenario_point_active(scenario: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0x0CC36D4156006509, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_scenario_point_flag_set(scenario: i32, flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::int(flag)];
    unsafe { native::invoke(0x8569C38D2FB80650, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn make_object_carriable(object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(object)];
    unsafe { native::invoke(0x78B4567E18B54480, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_fishingrod_hook_entity(ped: Ped, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity)];
    unsafe { native::invoke(0x1A52076D26E09004, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_fishingrod_hook_object(ped: Ped, object: Object) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(object)];
    unsafe { native::invoke(0xCE71C2F9BAA3F975, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ped_is_in_scenario_base(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x02EBBB3989B7E695, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_carriable_config(carriable_config: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(carriable_config)];
    unsafe { native::invoke(0x6AFDA2264925BD11, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_carriable_config(carriable_config: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(carriable_config)];
    unsafe { native::invoke(0xFF745B0346E19E2C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_herb_composite_asset(asset: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(asset)];
    unsafe { native::invoke(0x73F0D0327BFA0812, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_scenario_for_entity(scenario: i32, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::int(entity)];
    unsafe { native::invoke(0x2E20878FD208A68E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_scenario_script(scenario: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(scenario)];
    unsafe { native::invoke(0x5A40040BB5AE3EA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_aboard_ped_boat_pose(ped: Ped, boat: Vehicle, offset_x: f32, offset_y: f32, offset_z: f32, heading: f32, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(boat), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(heading), NativeArg::int(flags)];
    unsafe { native::invoke(0x517D01BF27B682D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_carriable_config_prompt_enabled(carriable_config: Hash, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(carriable_config), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x816A3ACD265E2297, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_fishing_bait(ped: Ped, bait: &str, without_buoy: bool, instantly: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(bait), NativeArg::boolean(without_buoy), NativeArg::boolean(instantly)];
    unsafe { native::invoke(0x9B0C7FA063E67629, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_hogtie_escape_timer(ped: Ped, time: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(time)];
    unsafe { native::invoke(0xAB591AE6B48B913E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_intimidated_facing_angle(ped: Ped, use_limits: bool, min_angle: f32, max_angle: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(use_limits), NativeArg::float(min_angle), NativeArg::float(max_angle)];
    unsafe { native::invoke(0x0FE797DD9F70DFA6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_item_interaction_state(ped: Ped, item_interaction_state: Hash, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(item_interaction_state), NativeArg::float(p2)];
    unsafe { native::invoke(0xB35370D5353995CB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_clear_aiming_in_the_air(ped: Ped, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1];
    unsafe { native::invoke(0x34C0010188D7C54A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_ignore_dead_bodies(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x013A7BA5015C1372, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_avoid_traffic(ped: Ped, avoid_traffic: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(avoid_traffic)];
    unsafe { native::invoke(0x7C015D8BCEC72CF4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_ladder_cost_modifier(ped: Ped, modifier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(modifier)];
    unsafe { native::invoke(0x70F7A1EAB1AE3AA8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_may_enter_deep_water(ped: Ped, may_enter_deep_water: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(may_enter_deep_water)];
    unsafe { native::invoke(0x9DE63896B176EA94, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_may_use_sliding_surfaces(ped: Ped, use_sliding_surfaces: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(use_sliding_surfaces)];
    unsafe { native::invoke(0x06ECF3925BC2ABAE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_never_use_interiors(ped: Ped, never_use_interiors: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(never_use_interiors)];
    unsafe { native::invoke(0x42CFD8FD8CC8DC69, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_prefer_horse_walkable(ped: Ped, prefer_horse_walkable: bool, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(prefer_horse_walkable), NativeArg::float(p2)];
    unsafe { native::invoke(0x216343750545A486, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_prefer_stay_in_water(ped: Ped, prefer_stay_in_water: bool, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(prefer_stay_in_water), NativeArg::float(p2)];
    unsafe { native::invoke(0xC6170856E54557B2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_prefer_to_avoid_foliage(ped: Ped, prefer_avoid_foliage: bool, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(prefer_avoid_foliage), NativeArg::float(p2)];
    unsafe { native::invoke(0x12990818C1D35886, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_path_prefer_to_avoid_mud(ped: Ped, prefer_avoid_mud: bool, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(prefer_avoid_mud), NativeArg::float(p2)];
    unsafe { native::invoke(0x8BB283A7888AD1AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ransack_scenario_container_opening_state(entity: Entity, open: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::boolean(open)];
    unsafe { native::invoke(0x188F8071F244B9B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_group_enabled_hash(scenario_group: Hash, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(scenario_group), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x9925EDDB6EAB88CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_point_active(scenario: i32, active: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::boolean(active)];
    unsafe { native::invoke(0xEEE4829304F93EEE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_point_coords(scenario: i32, x_pos: f32, y_pos: f32, z_pos: f32, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::float(x_pos), NativeArg::float(y_pos), NativeArg::float(z_pos), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x2056AB38DF06825C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_point_flag(scenario: i32, flag: i32, value: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::int(flag), NativeArg::boolean(value)];
    unsafe { native::invoke(0x5AF19B6CC2115D34, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_point_heading(scenario: i32, heading: f32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::float(heading), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xD3A0DA8F91612C6E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_point_radius(scenario: i32, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(scenario), NativeArg::float(radius)];
    unsafe { native::invoke(0xC47D9080A9A8856A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_scenario_type_enabled_hash(scenario_type: Hash, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(scenario_type), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xD00E50E673802D71, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_task_fishing(ped: Ped, p1: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xF3735ACD11ACD501, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_task_move_network_signal_float_2(ped: Ped, signal_name: &str, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(signal_name), NativeArg::float(value)];
    unsafe { native::invoke(0x099D4A855D53B03B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_task_move_network_signal_vector(ped: Ped, signal_name: &str, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::string(signal_name), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x4662BFE01938D98D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn swap_reins_for_peds(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xFC7F71CF49F70B6B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_animal_bleed_out(ped: Ped, killer: Ped, flee: bool, weapon_hash: Hash, p4: i32, bone_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(killer), NativeArg::boolean(flee), NativeArg::hash(weapon_hash), NativeArg::int(p4), NativeArg::int(bone_id)];
    unsafe { native::invoke(0x30A768C30D385EC5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_board_vehicle(ped: Ped, vehicle: Vehicle, p2: i32, p3: NativeArg, speed: i32, boarding_flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::int(p2), p3, NativeArg::int(speed), NativeArg::int(boarding_flags)];
    unsafe { native::invoke(0xE53D17AD837CBF7C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_board_vehicle_2(ped: Ped, p1: NativeArg, p2: i32, speed: f32, boarding_flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, NativeArg::int(p2), NativeArg::float(speed), NativeArg::int(boarding_flags)];
    unsafe { native::invoke(0xE41A09C8DDFF7AA4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_climb_2(ped: Ped, heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(heading)];
    unsafe { native::invoke(0xDF1D85BCAF60D537, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_combat_ped_at_coords(ped: Ped, x: f32, y: f32, z: f32, p4: i32, p5: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(p4), NativeArg::int(p5)];
    unsafe { native::invoke(0xC624414FA748B9BA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_cut_free_hogtied_target_ped(ped: Ped, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped)];
    unsafe { native::invoke(0x81D16C4FF3A77ADF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_cut_free_hogtied_target_ped_2(ped: Ped, target_ped: Ped, p2: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped), NativeArg::float(p2)];
    unsafe { native::invoke(0x525421A507216084, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_disembark_vehicle(p0: NativeArg, vehicle: Vehicle, p2: i32, p3: NativeArg, p4: f32, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::int(vehicle), NativeArg::int(p2), p3, NativeArg::float(p4), p5];
    unsafe { native::invoke(0xA7C6854BB5A4192A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_emote_action(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x6A1AF481407BF6E9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_emote_outro(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBDFEEB7600BCD938, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_equip_hat(hat_object: Object, ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(hat_object), NativeArg::int(ped)];
    unsafe { native::invoke(0xAA0AF6025160243A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_flee_from_coord(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11];
    unsafe { native::invoke(0x6879FF208ED87F2A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_flee_from_ped(ped: Ped, flee_from_target: Ped, x: f32, y: f32, z: f32, distance: f32, p6: i32, p7: i32, p8: f32, target_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flee_from_target), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(distance), NativeArg::int(p6), NativeArg::int(p7), NativeArg::float(p8), NativeArg::int(target_ped)];
    unsafe { native::invoke(0x7B74D8EEDE9B5727, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_force_fire_at_entity_while_aiming(ped: Ped, target_entity: Entity, duration_ms: i32, p3: NativeArg, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_entity), NativeArg::int(duration_ms), p3, NativeArg::boolean(p4)];
    unsafe { native::invoke(0x2416EC2F31F75266, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_guard_assigned_defensive_area_2(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0x1FC9B33976BACD6C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_intimidated(p0: NativeArg, ped: Ped, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<bool, NativeError> {
    let arguments = [p0, NativeArg::int(ped), p2, p3, p4];
    unsafe { native::invoke(0x648B75D44930D6BD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_intimidated_2(victim: Ped, attacker: Ped, p2: i32, p3: bool, p4: bool, every_frame: bool, p6: bool, p7: bool, flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(victim), NativeArg::int(attacker), NativeArg::int(p2), NativeArg::boolean(p3), NativeArg::boolean(p4), NativeArg::boolean(every_frame), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::int(flag)];
    unsafe { native::invoke(0x933ACC1A1771A288, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_item_interaction_2(ped: Ped, prop_name_gxt: Hash, prop: Object, prop_id: Hash, item_interaction_state: Hash, p5: i32, p6: NativeArg, p7: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(prop_name_gxt), NativeArg::int(prop), NativeArg::hash(prop_id), NativeArg::hash(item_interaction_state), NativeArg::int(p5), p6, NativeArg::float(p7)];
    unsafe { native::invoke(0x72F52AA2D2B172CC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_item_interaction_3(ped: Ped, item: Hash, guid: &mut Any, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(item), NativeArg::in_out(guid), p3, p4, p5, NativeArg::float(p6)];
    unsafe { native::invoke(0xD61D5E1AD9876DEB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_jump_2(ped: Ped, x: f32, y: f32, z: f32, entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(entity)];
    unsafe { native::invoke(0x91083103137D7254, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_knocked_out_set_duration(ped: Ped, ko_time_duration: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(ko_time_duration)];
    unsafe { native::invoke(0xFFB520A3E16F7B7B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_knocked_out_set_tuning(ped: Ped, tuning: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(tuning)];
    unsafe { native::invoke(0x8B1FDF63C3193EDA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_patrol_2(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0x964B06C88E4C86DB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_perform_sequence_2(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x4FC0AF869D6E309D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_play_emote(ped: Ped, emote_type: i32, playback_mode: i32, emote: Hash, is_secondary_task: bool, can_break_out: bool, disable_early_out_anim_tag: bool, ignore_invalid_main_task: bool, destroy_props: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(emote_type), NativeArg::int(playback_mode), NativeArg::hash(emote), NativeArg::boolean(is_secondary_task), NativeArg::boolean(can_break_out), NativeArg::boolean(disable_early_out_anim_tag), NativeArg::boolean(ignore_invalid_main_task), NativeArg::boolean(destroy_props)];
    unsafe { native::invoke(0x884E3436CC1F41DD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_point_at_entity(ped: Ped, target_entity: Entity, duration_ms: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_entity), NativeArg::int(duration_ms)];
    unsafe { native::invoke(0xF40A109B4B79A848, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_put_ped_directly_into_cover_from_coords(ped: Ped, x: f32, y: f32, z: f32, from_x: f32, from_y: f32, from_z: f32, timeout: i32, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg, p15: NativeArg, p16: NativeArg, p17: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(from_x), NativeArg::float(from_y), NativeArg::float(from_z), NativeArg::int(timeout), p8, p9, p10, p11, p12, p13, p14, p15, p16, p17];
    unsafe { native::invoke(0xDF8A5855B9F9A97B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_start_scenario_in_place_2(ped: Ped, p1: NativeArg, p2: &str, p3: i32, p4: bool, p5: f32, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, NativeArg::string(p2), NativeArg::int(p3), NativeArg::boolean(p4), NativeArg::float(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0xA917E39F2CEFD215, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_throw_projectile_2(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x7282356DFF6B5A51, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_use_nearest_scenario_to_coord(ped: Ped, x: f32, y: f32, z: f32, distance: f32, duration: i32, p6: bool, p7: bool, p8: bool, p9: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(distance), NativeArg::int(duration), NativeArg::boolean(p6), NativeArg::boolean(p7), NativeArg::boolean(p8), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x322BFDEA666E2B0E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_use_scenario_point_2(ped: Ped, ped2: Ped, p2: NativeArg, p3: &str, p4: i32, p5: Hash, p6: f32, p7: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(ped2), p2, NativeArg::string(p3), NativeArg::int(p4), NativeArg::hash(p5), NativeArg::float(p6), NativeArg::boolean(p7)];
    unsafe { native::invoke(0x0F6641449DD86FBE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_add_next_destination(vehicle: Vehicle, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x1D125814EBC517EB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_drive_to_coord_2(ped: Ped, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), p1, p2, p3, p4, p5, p6, p7, p8];
    unsafe { native::invoke(0xF0108F01FB105DA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_drive_to_destination_2(vehicle: Vehicle, x: f32, y: f32, z: f32, speed: f32, p5: i32, p6: i32, p7: f32, p8: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(speed), NativeArg::int(p5), NativeArg::int(p6), NativeArg::float(p7), NativeArg::float(p8)];
    unsafe { native::invoke(0x391073B9D3CCE2BA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_drive_to_point_2(vehicle: Vehicle, x: f32, y: f32, z: f32, p4: f32, p5: f32, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(p4), NativeArg::float(p5), p6];
    unsafe { native::invoke(0x6524A8981E8BE7C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_flee_on_cleanup(vehicle: Vehicle, p1: f32, p2: f32, p3: f32, speed: f32, type_: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(speed), NativeArg::hash(type_)];
    unsafe { native::invoke(0x55CD5FDDD4335C1E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_follow_waypoint_recording_2(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9];
    unsafe { native::invoke(0x041D17A9E221AE30, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn task_vehicle_is_heading_to_coords(vehicle: Vehicle, x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x583AE9AF9CEE0958, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn transition_scenario_to_conditional_anim(ped: Ped, scenario_point: i32, clipset_dict: &str, clip_name: &str, from_conditional_anim: &str, flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(scenario_point), NativeArg::string(clipset_dict), NativeArg::string(clip_name), NativeArg::string(from_conditional_anim), NativeArg::int(flags)];
    unsafe { native::invoke(0x79197F7D2BB5E73A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn update_task_go_to_coord_with_offset(ped: Ped, target_x: f32, target_y: f32, target_z: f32, offset_x: f32, offset_y: f32, offset_z: f32, speed: f32, tolerance: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::float(target_x), NativeArg::float(target_y), NativeArg::float(target_z), NativeArg::float(offset_x), NativeArg::float(offset_y), NativeArg::float(offset_z), NativeArg::float(speed), NativeArg::float(tolerance)];
    unsafe { native::invoke(0x3FFCD7BBA074CC80, &arguments) }.map(|_| ())
}

// ============================================================================
// TELEMETRY
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn analytics_playtime_freemode_end() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3180E991D4B8F248, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn analytics_playtime_freemode_start() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE9F24081D84931B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_camp_donate(transaction_id: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, slot_id: Hash, p6: Hash, p7: NativeArg, p8: bool) -> Result<(), NativeError> {
    let arguments = [transaction_id, p1, p2, p3, p4, NativeArg::hash(slot_id), NativeArg::hash(p6), p7, NativeArg::boolean(p8)];
    unsafe { native::invoke(0xDF516E598D966D06, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_personal_vehicle_mount(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xFF9052BC7A3B7D33, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_player_menu_pin(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x076C5843371EB889, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_f5_bc5_c4_eab42_b15(link_id: i32, type_: i32, content_id: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(link_id), NativeArg::int(type_), NativeArg::string(content_id)];
    unsafe { native::invoke(0x6F5BC5C4EAB42B15, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec0_bd8736_dcaf841(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xEC0BD8736DCAF841, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_telemetry_shop_ui() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x32D5898C4898CD95, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_ambient_vignette(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x3145044F3990D321, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_animal_skinned(type_: Hash, items: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(type_), NativeArg::in_out(items)];
    unsafe { native::invoke(0x7581972ADF5D699A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_bounty_target(data: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(data)];
    unsafe { native::invoke(0x52FA31DB8F3AD25D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_camp_created(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x565EAA726B2CE3B7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_camp_supplies(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x217F47761376E16E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_char_creator(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x7207AD471BC9278C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_collect(transaction_id: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [transaction_id, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xD6CB05DDAEE43AFD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_coupon(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x621D719C4836292B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_craft_item(p0: NativeArg, p1: NativeArg, p2: NativeArg, quantity: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, quantity];
    unsafe { native::invoke(0x78C2E029DB205A3A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_create_uuid(uuid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(uuid)];
    unsafe { native::invoke(0xE692D336F8A2A97F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_custom(args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0x40914CCF2A1AB531, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_defensive(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xE57529D23541D2DD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_discoverable(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF5EAD898EF387E73, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_emote_add_category_to_save(p0: NativeArg, p1: NativeArg, emote: Hash) -> Result<(), NativeError> {
    let arguments = [p0, p1, NativeArg::hash(emote)];
    unsafe { native::invoke(0x2C24AF8EEEEF8A55, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_fast_travel(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x7CEF4AC79F7E7FAD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_favor_emote(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x16B23D4F7A1F50D9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_game_progress(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x51EC204A6E5B5A1A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_gang_shares(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xE6DC9B21AC7A8729, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_gold_store(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x536B6025E94AC48F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_gun_locker() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x415FE28ED44BFF14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_gun_locker_weapon_removed(p0: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x317D9C9560529CC2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_gun_locker_weapon_stored(p0: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0xC3ADF4880784FA9C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_herb_picked(herb_type: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(herb_type)];
    unsafe { native::invoke(0xAE693EC3A178F6C2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_honor(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xE6B763C7F4902201, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_hub_navigation(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x25CC50EC1A6F3A96, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_hub_offers(coupon_item: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [coupon_item, p1];
    unsafe { native::invoke(0x37AA282163B0D2C4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_intro_skip(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x1B554723799245F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_lobby_progression(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xECD67E9FA677CCCF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_loot(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xCF63EF77B0DF0397, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_match_nomination(args: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0x330029E121380CEB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_match_over(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xA2058154357726BB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_match_queue(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x4C08D2B6D8BE17E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_match_started(p0: &mut Any, p1: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xF620F47B4F4A78C4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_match_vote(p0: &mut Any, p1: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1)];
    unsafe { native::invoke(0xEF3C68F56BAD7B69, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_menu_navigation(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x3255D4D2082C6339, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_mission_checkpoint(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x8EC7890D446BD9C1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_mission_failed_to_launch(p0: NativeArg, p1: NativeArg, x: f32, y: f32, z: f32, reason: i32) -> Result<(), NativeError> {
    let arguments = [p0, p1, NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(reason)];
    unsafe { native::invoke(0x6571E4327390EC0B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_mission_ilo_option(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xEA323F5E1A4DA2F1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_mission_over(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD894437E12C17AEC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_mission_started(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x15B0CC1B36F1DE29, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_moonshine_brew(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10];
    unsafe { native::invoke(0xB5013EFBB5516867, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_net_camp(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xA72773C3134F9A57, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_notoriety(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xE26970A7AE0F28E9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_parley_feud(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xF37A2149BC9A8A27, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_personal_vehicle_wagon(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xE67AF24C5A3B6058, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_photo(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xED22BE4C5A399E63, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_photo_cam(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8];
    unsafe { native::invoke(0x0777D65EE8A17517, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_player_spawned(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x5DA4718DF897EB25, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_poker_over(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9];
    unsafe { native::invoke(0x8127C5AA05C5A210, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_prison(transaction_id: NativeArg, bounty_amount: NativeArg, ped: Ped, completion_type: NativeArg, jail_time_served: NativeArg, jail_time_left: NativeArg, posse_role: NativeArg) -> Result<(), NativeError> {
    let arguments = [transaction_id, bounty_amount, NativeArg::int(ped), completion_type, jail_time_served, jail_time_left, posse_role];
    unsafe { native::invoke(0xB204BF9F30298D77, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_region(region_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(region_hash)];
    unsafe { native::invoke(0xCD6F8A0335D821F9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_role_bounty(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xAB43D1C80B5E9500, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_role_collector(transaction_id: NativeArg, collectible: NativeArg, category: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<(), NativeError> {
    let arguments = [transaction_id, collectible, category, p3, p4, p5, p6];
    unsafe { native::invoke(0x4AC38DFD286DAD14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_role_moonshiner(p0: NativeArg, transaction_id: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, transaction_id];
    unsafe { native::invoke(0x99D40C5D74BC88E9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_role_naturalist(transaction_id: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg) -> Result<(), NativeError> {
    let arguments = [transaction_id, p1, p2, p3, p4, p5, p6, p7, p8, p9];
    unsafe { native::invoke(0x6FB9EA308F302922, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_role_token_transaction(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x32C2939564D74BFF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_role_trader(p0: NativeArg, transaction_id: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, transaction_id];
    unsafe { native::invoke(0x476038B5A0734C10, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_rpg_global_calculate_attribute_core_delta() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x7E002A36AEFCFB55, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_sample(transaction_id: NativeArg, animal: NativeArg, p2: NativeArg, b_sampled: NativeArg, b_tranq: bool) -> Result<(), NativeError> {
    let arguments = [transaction_id, animal, p2, b_sampled, NativeArg::boolean(b_tranq)];
    unsafe { native::invoke(0x61559675D23D8BD1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_set_is_flow(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x9BEE018A63FFFAD9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_set_shop_for_transaction(transaction_id: i32, p1: Hash, p2: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(transaction_id), NativeArg::hash(p1), NativeArg::hash(p2)];
    unsafe { native::invoke(0xCA9E42F437625A85, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_shop_cutscene(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xB0B19B56697836F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_shop_entry(shop_type: NativeArg, shop_region: NativeArg, region: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [shop_type, shop_region, region, p3, p4, p5];
    unsafe { native::invoke(0x775B2ED944E44973, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_shop_exit(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xF78E669FDC202E73, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_shop_purchase(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x2A374E6F0075EE81, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_shop_sell(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, cent_sale_price: i32) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, NativeArg::int(cent_sale_price)];
    unsafe { native::invoke(0x9BD8A9D0A774A6F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_sleep(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF9F14080D80937BD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_start_gun_locker_interaction() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF0D54E0651DD7E07, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn telemetry_trigger_transaction_request(request_id: &mut Any, transaction_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(request_id), NativeArg::in_out(transaction_id)];
    unsafe { native::invoke(0x80A02D9F948A8BCA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn try_get_telemetry_id_from_transaction_id(transaction_id: &mut Any, request_id: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(transaction_id), NativeArg::in_out(request_id)];
    unsafe { native::invoke(0xF184B3ECE36219CF, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// TXD
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_streamed_texture_dict_exist(texture_dict: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(texture_dict)];
    unsafe { native::invoke(0x7332461FC59EB7EC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_streamed_txd_exist(txd_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(txd_hash)];
    unsafe { native::invoke(0xBA0163B277C2D2D0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_streamed_texture_dict_loaded(texture_dict: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(texture_dict)];
    unsafe { native::invoke(0x54D6900929CCF162, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_streamed_txd_loaded(txd_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(txd_hash)];
    unsafe { native::invoke(0xBE72591D1509FFE4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_streamed_texture_dict(texture_dict: &str, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(texture_dict), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC1BA29DF5631B0F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_streamed_txd(txd_hash: Hash, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(txd_hash), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xDB1BD07FB464584D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_streamed_texture_dict_as_no_longer_needed(texture_dict: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(texture_dict)];
    unsafe { native::invoke(0x4ACA10A91F66F1E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_streamed_txd_as_no_longer_needed(txd_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(txd_hash)];
    unsafe { native::invoke(0x8232F37DF762ACB2, &arguments) }.map(|_| ())
}

// ============================================================================
// UIAPPS
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_launch_uiapp_by_hash(app_name_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(app_name_hash)];
    unsafe { native::invoke(0xE555EC27D65EDE80, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_launch_uiapp_by_hash_with_entry(app_name_hash: Hash, entry_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(app_name_hash), NativeArg::hash(entry_hash)];
    unsafe { native::invoke(0x16F47D434B6086BF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_uiapp_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAC959AB99AAF3D9F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_uiapp_running() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDB30BEC7A7A5CBD3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_uiapp_active_by_hash(app_name_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(app_name_hash)];
    unsafe { native::invoke(0x25B7A0206BDFAC76, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_uiapp_running(app_name: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::string(app_name)];
    unsafe { native::invoke(0xDE4A9B35D028979F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_uiapp_running_by_hash(app_name_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(app_name_hash)];
    unsafe { native::invoke(0x4E511D093A86AD49, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_uiapp_transitioning_by_hash(app_name_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(app_name_hash)];
    unsafe { native::invoke(0x42095B886D30DE66, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn launch_uiapp_by_hash(app_name_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(app_name_hash)];
    unsafe { native::invoke(0xC8FC7F4E4CF4F581, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn launch_uiapp_by_hash_with_entry(app_name_hash: Hash, entry_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(app_name_hash), NativeArg::hash(entry_hash)];
    unsafe { native::invoke(0xC1BCF31E975B3195, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn launch_uiapp_with_entry(app_name: &str, entry: &str) -> Result<i32, NativeError> {
    let arguments = [NativeArg::string(app_name), NativeArg::string(entry)];
    unsafe { native::invoke(0x7B2027BAC5C8EC89, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_uiapp_transition_by_hash(app_name_hash: Hash, transition_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(app_name_hash), NativeArg::hash(transition_hash)];
    unsafe { native::invoke(0x7689CD255655BFD7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn close_all_uiapps() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xAD7B70F7230C5A12, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn close_all_uiapps_immediate() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x12769EEB8DBD7A7B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn close_uiapp(app_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(app_name)];
    unsafe { native::invoke(0x818C6CA9B659E8EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn close_uiapp_by_hash(app_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(app_name_hash)];
    unsafe { native::invoke(0x2FF10C9C3F92277E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn close_uiapp_by_hash_immediate(app_name_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(app_name_hash)];
    unsafe { native::invoke(0x04428420A248A354, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn close_uiapp_immediate(app_name: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(app_name)];
    unsafe { native::invoke(0x3015635426D1B17C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_uiapp_current_activity_by_hash(app_name_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(app_name_hash)];
    unsafe { native::invoke(0x96FD694FE5BE55DC, &arguments) }.map(|result| result.as_u32())
}

// ============================================================================
// UIDEBUG
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_display_text(text: &str, x: f32, y: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(text), NativeArg::float(x), NativeArg::float(y)];
    unsafe { native::invoke(0x16794E044C9EFB58, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_set_text_color(red: i32, green: i32, blue: i32, alpha: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(red), NativeArg::int(green), NativeArg::int(blue), NativeArg::int(alpha)];
    unsafe { native::invoke(0x16FA5CE47F184F1E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bg_set_text_scale(scale_x: f32, scale_y: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(scale_x), NativeArg::float(scale_y)];
    unsafe { native::invoke(0xA1253A3C870B6843, &arguments) }.map(|_| ())
}

// ============================================================================
// UIEVENTS
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn events_ui_get_message(hash: Hash, event_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash), NativeArg::in_out(event_data)];
    unsafe { native::invoke(0xE24E957294241444, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn events_ui_is_pending(hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0x67ED5A7963F2F722, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn events_ui_peek_message(hash: Hash, event_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(hash), NativeArg::in_out(event_data)];
    unsafe { native::invoke(0x90237103F27F7937, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn events_ui_pop_message(hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0x8E8A2369F48EC839, &arguments) }.map(|_| ())
}

// ============================================================================
// UIFEED
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_clear_channel(feed_channel: i32, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(feed_channel), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xDD1232B332CBB9E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_get_current_message(feed_channel: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(feed_channel)];
    unsafe { native::invoke(0xC17F69E1418CD11F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_fd07141_ad048_aae(p0: &mut Any, p1: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x0FD07141AD048AAE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x18_d6869_fbffec0_f8(p0: &mut Any, p1: &mut Any, p2: bool, p3: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x18D6869FBFFEC0F8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_e88_a65968_a55_c78(p0: &mut Any, p1: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x4E88A65968A55C78, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_d85126_f6_ccf02_c9(feed_channel: i32, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(feed_channel), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x6D85126F6CCF02C9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_aff5_be9_ba496_ce40(p0: &mut Any, p1: &mut Any, p2: bool, p3: bool, collectable_category: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::hash(collectable_category)];
    unsafe { native::invoke(0xAFF5BE9BA496CE40, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b7223_b91_cd6_b7_e07(feed_channel: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(feed_channel)];
    unsafe { native::invoke(0xB7223B91CD6B7E07, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_clear_all_channels() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x6035E8FBCA32AC5E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_clear_help_text_feed(feed_message: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(feed_message), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x2F901291EF177B02, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_get_message_state(feed_message: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(feed_message)];
    unsafe { native::invoke(0x59FA676177DBE4C9, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_feed_ticker(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xB2920B9760F0F36B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_game_update_shard(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x8D1249BD28791878, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_help_text(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x049D5C615BD38BAD, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_location_shard(duration: &mut Any, data: &mut Any, p2: bool, p3: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(duration), NativeArg::in_out(data), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xD05590C1AB38F068, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_mission_name(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x2024F4F333095FB1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_objective(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xCEDBF17EFCC0E4A4, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_one_text_shard(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x860DDFE97CC94DF0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_rankup_toast(p0: &mut Any, p1: &mut Any, p2: i32, p3: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0x3F9FDDBA79117C69, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_reticle_message(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x893128CDB4B81FBB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_sample_notification(p0: &mut Any, p1: &mut Any, p2: i32, p3: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::int(p2), NativeArg::int(p3)];
    unsafe { native::invoke(0xC927890AA64E9661, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_sample_toast(p0: &mut Any, p1: &mut Any, p2: bool, p3: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x26E87218390E6729, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_sample_toast_right(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xB249EBCB30DD88E0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_sample_toast_with_app_link(p0: &mut Any, p1: &mut Any, p2: bool, p3: bool, p4: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x38838A646FB30AAE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_three_text_shard(p0: &mut Any, p1: &mut Any, p2: bool, p3: bool, p4: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x02BCC0FE9EBA3529, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_two_text_shard(p0: &mut Any, p1: &mut Any, p2: bool, p3: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xA6F4216AB10EB08E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_feed_post_voice_chat_feed(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xC48152BC6B3E821C, &arguments) }.map(|result| result.as_i32())
}

// ============================================================================
// UILOG
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_a4765812202_e671() -> Result<Any, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2A4765812202E671, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x763637_f9_b838_b0_a7(p0: i32, p1: Hash, p2: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0x763637F9B838B0A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a20398536_b7_f1134(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xA20398536B7F1134, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a49_d6_d503_e3_ea847(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xA49D6D503E3EA847, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_da0_a30153_fcc0_ffd() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDA0A30153FCC0FFD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_add_entry_hash(p0: i32, p1: i32, x: f32, y: f32, z: f32, p5: Hash, p6: Hash, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::int(p1), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::hash(p5), NativeArg::hash(p6), p7];
    unsafe { native::invoke(0x69D5479982355D8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_add_item_to_task_list(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7];
    unsafe { native::invoke(0x49C63FDF69744A27, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_add_or_update_objective(p0: i32, p1: Hash, p2: Hash, p3: &str, p4: bool, p5: bool, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1), NativeArg::hash(p2), NativeArg::string(p3), NativeArg::boolean(p4), NativeArg::boolean(p5), NativeArg::boolean(p6)];
    unsafe { native::invoke(0xB43163388484CC87, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_add_total_take_entry(p0: Hash, p1: Hash, p2: &str, p3: &str, p4: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0), NativeArg::hash(p1), NativeArg::string(p2), NativeArg::string(p3), NativeArg::hash(p4)];
    unsafe { native::invoke(0x60C59968E8E87E6B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_clear_all_entries() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB95B4EA6B1EDF035, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_clear_cached_objective() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xDFF0D417277B41F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_clear_has_displayed_cached_objective() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA3108D6981A5CADB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_get_cached_objective() -> Result<NativeString, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x15A4461BEB788096, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_has_displayed_cached_objective() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCC48FFBB45B54F71, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_is_entry_registered(p0: i32, p1: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1)];
    unsafe { native::invoke(0xB8188CCF52202475, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_mark_all_entries_availability(p0: i32, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0x3920574CF0A2B7B6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_mark_entry_availability(p0: i32, p1: Hash, p2: i32, p3: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1), NativeArg::int(p2), NativeArg::string(p3)];
    unsafe { native::invoke(0x13E8D7DD08543482, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_mark_mission_completed(p0: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0xDE31D66D1E54C471, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_post_notification(data: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(data)];
    unsafe { native::invoke(0x49E58FE6EF40B987, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_print_cached_objective() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE9990552DEC71600, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_remove_entry(p0: i32, p1: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1)];
    unsafe { native::invoke(0xD594A19BE09A75C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_set_cached_objective(p0: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0)];
    unsafe { native::invoke(0xFA233F8FE190514C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_set_display_completion_rating(log_entry_type: i32, p1: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(log_entry_type), NativeArg::hash(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xA31013798FADCADC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_set_entry_brief_texture(p0: i32, p1: Hash, texture: Hash, texture_dictionary: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1), NativeArg::hash(texture), NativeArg::hash(texture_dictionary)];
    unsafe { native::invoke(0x69684D9936958D8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_set_entry_icon_texture(p0: i32, p1: Hash, icon: Hash, icon_dictionary: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1), NativeArg::hash(icon), NativeArg::hash(icon_dictionary)];
    unsafe { native::invoke(0x6965469934958D8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_set_entry_pinned(p0: i32, p1: Hash, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x72A5CD214B342568, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_set_has_displayed_cached_objective() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA3108D6981A5CADC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_set_pending_details_id(p0: i32, p1: Hash) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1)];
    unsafe { native::invoke(0x136A027CF37B0A4F, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_set_total_take_summary(p0: &str, p1: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::string(p0), NativeArg::string(p1)];
    unsafe { native::invoke(0xD106B211EF1B8F04, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uilog_update_entry_subheader(p0: i32, p1: Hash, p2: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0), NativeArg::hash(p1), NativeArg::string(p2)];
    unsafe { native::invoke(0x80D6524190258C3E, &arguments) }.map(|_| ())
}

// ============================================================================
// UIPINNING
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uipinning_get_tooltip_text(hash: Hash) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(hash)];
    unsafe { native::invoke(0x3138582E0A13BFAB, &arguments) }.map(|result| result.as_string())
}

// ============================================================================
// UISTATEMACHINE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_state_machine_can_request_transition(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF7C180F57F85D0B8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_state_machine_create(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x4C6F2C4B7A03A266, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_state_machine_destroy(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x4EB122210A90E2D8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_state_machine_destroy_and_clear(p0: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(p0)];
    unsafe { native::invoke(0x2738D68D2B4E09E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_state_machine_exists(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x5D15569C0FEBF757, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_state_machine_request_exit(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6B9FE4F0BA521A19, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_state_machine_request_transition(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x7EA9C3547E80350E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uiflowblock_enter(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3B7519720C9DCB45, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uiflowblock_is_loaded(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x10A93C057B6BD944, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uiflowblock_release(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF320A77DD5F781DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uiflowblock_request(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC0081B34E395CE48, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_state_machine_is_exited(p0: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(p0)];
    unsafe { native::invoke(0x11E73195E735B25B, &arguments) }.map(|result| result.as_bool())
}

// ============================================================================
// UISTICKYFEED
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_sticky_feed_clear_message(msg_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(msg_id)];
    unsafe { native::invoke(0x00A15B94CBA4F76F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_sticky_feed_create_death_fail_message(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x815C4065AE6E6071, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_sticky_feed_create_error_message(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x9F2CC2439A04E7BA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_sticky_feed_create_warning_message(p0: &mut Any, p1: &mut Any, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(p0), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x339E16B41780FC35, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_sticky_feed_get_message_state(msg_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(msg_id)];
    unsafe { native::invoke(0x07954320D77F6A3D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_sticky_feed_is_alert_screen_active() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xF8806EC3FF840FDC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_sticky_feed_is_channel_active(sticky_feed_channel: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(sticky_feed_channel)];
    unsafe { native::invoke(0xC5C395C60B542A3C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn ui_sticky_feed_update_message(msg_id: i32, p1: &mut Any, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(msg_id), NativeArg::in_out(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xBC6F454E310124DA, &arguments) }.map(|_| ())
}

// ============================================================================
// UITUTORIAL
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uitutorial_get_is_threat_indicator_capable_radar_shown() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2CC24A2A7A1489C4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uitutorial_get_is_threat_indicator_on() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFC2E0A5E9ED4E1B4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn uitutorial_set_rpg_icon_visibility(rpg_icon: i32, visibility: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(rpg_icon), NativeArg::int(visibility)];
    unsafe { native::invoke(0xC116E6DF68DCE667, &arguments) }.map(|_| ())
}

// ============================================================================
// UNLOCK
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_is_unlocked(unlock_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(unlock_hash)];
    unsafe { native::invoke(0xC4B660C7B6040E75, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_is_visible(unlock_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(unlock_hash)];
    unsafe { native::invoke(0x8588A14B75AF096B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_set_unlocked(unlock_hash: Hash, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(unlock_hash), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x1B7C5ADA8A6910A0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_set_visible(unlock_hash: Hash, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(unlock_hash), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x46B901A8ECDB5A61, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_get_item_role_unlock_info(unlock_hash: Hash, out_data: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(unlock_hash), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0x7C1C2062CFAD06FE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_is_lootable(unlock_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(unlock_hash)];
    unsafe { native::invoke(0x66BF197E066050DE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_is_new(unlock_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(unlock_hash)];
    unsafe { native::invoke(0x644166BA7AA49DEA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_is_unlock_flag_set(unlock_hash: Hash, flag: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(unlock_hash), NativeArg::int(flag)];
    unsafe { native::invoke(0x6B6369647F26F09F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn unlock_set_new(unlock_hash: Hash, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(unlock_hash), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xA6D79C7AEF870A99, &arguments) }.map(|_| ())
}

// ============================================================================
// VEHICLE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_road_node_speed_zone(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg) -> Result<i32, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10];
    unsafe { native::invoke(0x4C221BAC54D735C3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_any_vehicle_seats_free(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xA0A424505A1B6429, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn bring_vehicle_to_halt(vehicle: Vehicle, distance: f32, duration: i32, unknown: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(distance), NativeArg::int(duration), NativeArg::boolean(unknown)];
    unsafe { native::invoke(0x260BE8F09E326A20, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_anchor_boat_here(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xC075176CFB8B4128, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_shuffle_seat(vehicle: Vehicle, seat_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(seat_index)];
    unsafe { native::invoke(0xF8B2D32A2231FD24, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_last_driven_vehicle() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x0EFC5DC62E67609B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn copy_vehicle_damages(source_vehicle: Vehicle, target_vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(source_vehicle), NativeArg::int(target_vehicle)];
    unsafe { native::invoke(0xDBC28A8C683CD80B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_vehicle(model_hash: Hash, x: f32, y: f32, z: f32, heading: f32, is_network: bool, b_script_host_veh: bool, b_dont_auto_create_draft_animals: bool, p8: bool) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::boolean(is_network), NativeArg::boolean(b_script_host_veh), NativeArg::boolean(b_dont_auto_create_draft_animals), NativeArg::boolean(p8)];
    unsafe { native::invoke(0xAF35D0D2583051B0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_all_trains() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA3120A1385F17FF7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_mission_train(train: &mut Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(train)];
    unsafe { native::invoke(0x0D3630FB07E8B570, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_vehicle(vehicle: &mut Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(vehicle)];
    unsafe { native::invoke(0xE20A909D8C4A70F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_vehicle_weapon(disabled: bool, weapon_hash: Hash, vehicle: Vehicle, owner: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(disabled), NativeArg::hash(weapon_hash), NativeArg::int(vehicle), NativeArg::int(owner)];
    unsafe { native::invoke(0x94B1E71B144356A5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_extra_exist(vehicle: Vehicle, extra_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(extra_id)];
    unsafe { native::invoke(0xAF5E7E9A7620FFB5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn explode_vehicle(vehicle: Vehicle, is_audible: bool, is_invisible: bool, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(is_audible), NativeArg::boolean(is_invisible), p3, p4];
    unsafe { native::invoke(0x75DCED9EEC5769D7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn force_playback_recorded_vehicle_update(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x59ECA796021B0539, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_closest_vehicle(x: f32, y: f32, z: f32, radius: f32, model_hash: Hash, flags: i32) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::hash(model_hash), NativeArg::int(flags)];
    unsafe { native::invoke(0x52F45D033645181B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_station_for_train(train: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0x86FA6D8B48667D75, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_draft_animal_count(vehicle: Vehicle, expected: &mut i32, actual: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::in_out(expected), NativeArg::in_out(actual)];
    unsafe { native::invoke(0xA19447D83294E29F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_driver_of_vehicle(vehicle: Vehicle) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x2963B5C1637E8A27, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_last_driven_vehicle() -> Result<Vehicle, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA94F3E0AB9695E19, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_last_ped_in_vehicle_seat(vehicle: Vehicle, seat_index: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(seat_index)];
    unsafe { native::invoke(0x74583B19FEEAFDA7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_in_vehicle_seat(vehicle: Vehicle, seat_index: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(seat_index)];
    unsafe { native::invoke(0xBB40DD2270B65366, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_position_of_vehicle_recording_at_time(recording: i32, time: f32, script: &str) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(recording), NativeArg::float(time), NativeArg::string(script)];
    unsafe { native::invoke(0x1A00961A1BE94E5E, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rotation_of_vehicle_recording_at_time(recording: i32, time: f32, script: &str) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(recording), NativeArg::float(time), NativeArg::string(script)];
    unsafe { native::invoke(0x61787DD28B8CC0D5, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_time_position_in_recording(vehicle: Vehicle) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x233B51C7913FA031, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_track_index_of_train(train: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0x865FEC2FA899F29C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_carriage(train: Vehicle, trailer_number: i32) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::int(trailer_number)];
    unsafe { native::invoke(0xD0FB093A4CDB932C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_body_health(vehicle: Vehicle) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x42113B857E33C16E, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_doors_locked_for_player(vehicle: Vehicle, player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(player)];
    unsafe { native::invoke(0xFA2CDDFEB8BC898B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_door_lock_status(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xC867FD144F2469D3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_engine_health(vehicle: Vehicle) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x90DBFFAC43B22081, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_estimated_max_speed(vehicle: Vehicle) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xFE52F34491529F0B, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_max_number_of_passengers(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xA9C55F1C15E62E06, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_model_number_of_seats(model_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(model_hash)];
    unsafe { native::invoke(0x9A578736FF3A17C3, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_number_of_passengers(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x59F3F16577CD79B2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_petrol_tank_health(vehicle: Vehicle) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x1E5A9B356D5098BE, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_trailer_vehicle(vehicle: Vehicle, trailer: &mut Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::in_out(trailer)];
    unsafe { native::invoke(0xCF867A239EC30741, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_instant_fill_vehicle_population_finished() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2701D01D5E18FC31, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_vehicle_asset_loaded(vehicle_asset: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(vehicle_asset)];
    unsafe { native::invoke(0xB935F3154BC913C8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_vehicle_recording_been_loaded(recording: i32, script: &str) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(recording), NativeArg::string(script)];
    unsafe { native::invoke(0xBA9325BE372AB6EA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn instantly_fill_vehicle_population() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1FF00DB43026B12F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_any_vehicle_near_point(x: f32, y: f32, z: f32, radius: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius)];
    unsafe { native::invoke(0x5698BA4FD04D39C4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_draft_vehicle(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xEA44E97849E9F3DD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_entry_point_for_seat_clear(ped: Ped, vehicle: Vehicle, seat_index: i32, side: bool, on_enter: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::int(seat_index), NativeArg::boolean(side), NativeArg::boolean(on_enter)];
    unsafe { native::invoke(0x80DDCCB2F4A3EB57, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_playback_going_on_for_vehicle(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x02774B3A9034278F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_playback_using_ai_going_on_for_vehicle(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x5A7472606EC5B7C1, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_seat_warp_only(vehicle: Vehicle, seat_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(seat_index)];
    unsafe { native::invoke(0x7892685BF6D9775E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_this_model_a_boat(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0x799CFC7C5B743B15, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_this_model_a_train(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0xFC08C8F8C1EDF174, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_train_waiting_at_station(train: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0xE887BD31D97793F6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_door_fully_open(vehicle: Vehicle, door_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_id)];
    unsafe { native::invoke(0x7AE191143C7A9107, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_driveable(vehicle: Vehicle, p1: bool, p2: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xB86D29B10F627379, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_extra_turned_on(vehicle: Vehicle, extra_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(extra_id)];
    unsafe { native::invoke(0xFA9A55D9C4351625, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_in_burnout(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x3F5029A8FC060C48, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_model(vehicle: Vehicle, model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::hash(model)];
    unsafe { native::invoke(0x0045A54EC7A22455, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_on_all_wheels(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x0D5D119529654EE0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_seat_free(vehicle: Vehicle, seat_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(seat_index)];
    unsafe { native::invoke(0xE052C1B1CAA4ECE4, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_stopped(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x78C3311A73135241, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_stuck_timer_up(vehicle: Vehicle, stuck_type: i32, ms: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(stuck_type), NativeArg::int(ms)];
    unsafe { native::invoke(0x1ABA9753939503C5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_visible(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x424910CD5DE8C246, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_window_intact(vehicle: Vehicle, window_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(window_index)];
    unsafe { native::invoke(0x0E7910A63E05B12C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_wrecked(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xDDBEA5506C848227, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn lock_doors_when_no_longer_needed(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x1EF36558FBDE2DAA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn modify_vehicle_top_speed(vehicle: Vehicle, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(value)];
    unsafe { native::invoke(0x35AD938C74CACD6A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_road_node_speed_zone(speedzone: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(speedzone)];
    unsafe { native::invoke(0xFE9AB3354ACE6C9C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_vehicles_from_generators_in_area(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xC619A44639BC0CB4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_vehicle_asset(vehicle_asset: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(vehicle_asset)];
    unsafe { native::invoke(0x888A4E675B38F5AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_vehicle_recording(p0: NativeArg, p1: &mut Any) -> Result<(), NativeError> {
    let arguments = [p0, NativeArg::in_out(p1)];
    unsafe { native::invoke(0x139E35755418F6AA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_vehicle_window(vehicle: Vehicle, window_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(window_index)];
    unsafe { native::invoke(0x745F15A215F2DDF1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_vehicle_asset(vehicle_hash: Hash, vehicle_asset: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(vehicle_hash), NativeArg::int(vehicle_asset)];
    unsafe { native::invoke(0x81A15811460FAB3A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_vehicle_high_detail_model(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x84B81EF78BD22357, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_vehicle_recording(recording: i32, script: &str) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(recording), NativeArg::string(script)];
    unsafe { native::invoke(0xC474CF16EDA45DC9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_vehicle_stuck_timer(vehicle: Vehicle, null_attributes: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(null_attributes)];
    unsafe { native::invoke(0x23298B468F7D88B6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_allow_vehicle_explodes_on_contact(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x8D3230A0ED7DE39F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_vehicle_generators_active() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x3D596E6E88A02C24, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_vehicle_generators_active_in_area(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, p6: bool, p7: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::boolean(p6), NativeArg::boolean(p7)];
    unsafe { native::invoke(0xBBB134FB9D50C0CC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_boat_anchor(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xAEAB044F05B92659, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_boat_low_lod_anchor_distance(vehicle: Vehicle, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(value)];
    unsafe { native::invoke(0xE3261532550D6A9F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_boat_remains_anchored_while_player_is_driver(vehicle: Vehicle, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x286771F3059A37A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_boat_sinks_when_wrecked(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x62A6D317A011EA1D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_breakable_vehicle_locks_unbreakable(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xBC4735F48CD983EF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_random_trains_this_frame(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xD4288603E8766FF7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_superdummy(vehicle: Vehicle, disable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(disable)];
    unsafe { native::invoke(0x1716D787D9B94202, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_vehicle_engine_fires(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xD146EE5F2B06B95E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_vehicle_petrol_tank_damage(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x5795FBE7A2001C14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_disable_vehicle_petrol_tank_fires(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xB70986AB19B04AFF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_dont_allow_player_to_enter_vehicle_if_locked_for_player(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x63DC1F22C903B709, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_door_allowed_to_be_broken_off(vehicle: Vehicle, door_id: i32, is_breakable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_id), NativeArg::boolean(is_breakable)];
    unsafe { native::invoke(0x081FB9D6422F804C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_enable_vehicle_slipstreaming(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x73F1E4F6DF26FE30, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_force_hd_vehicle(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x373CB1283308BD7B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_force_low_lod_anchor_mode(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x75B49ACD73617437, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_force_vehicle_engine_damage_by_bullet(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x7F8E2B131E1DCA6C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mission_train_as_no_longer_needed(train: &mut Vehicle, flags: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(train), NativeArg::int(flags)];
    unsafe { native::invoke(0xBBE7648349B49BE8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mission_train_coords(train: Vehicle, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x7632755962AB9922, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_parked_vehicle_density_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0xFEDFA97638D61D4A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_owns_vehicle(ped: Ped, vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle)];
    unsafe { native::invoke(0x838C216C2B05A009, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_playback_speed(vehicle: Vehicle, speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(speed)];
    unsafe { native::invoke(0xD78084EED4CD94C6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_random_boats(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xF44D446D4E36DB87, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_random_trains(toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x1156C6EE7E82A98A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_random_vehicle_density_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0x1F91D44490E1EA0C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_train_cruise_speed(train: Vehicle, speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::float(speed)];
    unsafe { native::invoke(0x01021EB2E96B793C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_train_offset_from_station(train: Vehicle, offset: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::float(offset)];
    unsafe { native::invoke(0x8EC47DD4300BF063, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_train_speed(train: Vehicle, speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::float(speed)];
    unsafe { native::invoke(0xDFBA6BBFF7CCAFBB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_ai_can_use_exclusive_seats(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x0893DAFBFA67110E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_allow_homing_missle_lockon(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x1240E8596A8308B9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_allow_no_passengers_lockon(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xECB9E9BC887E8060, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_automatically_attaches(vehicle: Vehicle, p1: bool, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1), p2];
    unsafe { native::invoke(0x501354951CD942DE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_body_health(vehicle: Vehicle, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(value)];
    unsafe { native::invoke(0x55CCAAE4F28C67A0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_broken_parts_dont_affect_ai_handling(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xCEC4CA2CAB8FA98C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_can_be_targetted(vehicle: Vehicle, state: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(state)];
    unsafe { native::invoke(0x05254BA0B44ADC16, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_can_be_used_by_fleeing_peds(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE42952510F84AFDB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_can_be_visibly_damaged(vehicle: Vehicle, state: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(state)];
    unsafe { native::invoke(0x4BF8131AE811541C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_can_break(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC5ED9D59B4646611, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_can_eject_passengers_if_locked(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x065D03A9D6B2C6B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_damage(vehicle: Vehicle, x_offset: f32, y_offset: f32, z_offset: f32, damage: f32, radius: f32, p6: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::float(damage), NativeArg::float(radius), NativeArg::boolean(p6)];
    unsafe { native::invoke(0x1D7678F81452BB41, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_density_multiplier_this_frame(multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(multiplier)];
    unsafe { native::invoke(0x606374EBFC27B133, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_dirt_level(vehicle: Vehicle, dirt_level: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(dirt_level)];
    unsafe { native::invoke(0x758C3460EE915D0A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_doors_locked(vehicle: Vehicle, door_lock_status: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_lock_status)];
    unsafe { native::invoke(0x96F78A6A075D55D9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_doors_locked_for_all_players(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x2381977DA948F8DC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_doors_locked_for_player(vehicle: Vehicle, player: Player, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(player), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x359A8EA1FB8D6F0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_doors_locked_for_team(vehicle: Vehicle, team: i32, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(team), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xE712BC978770F105, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_doors_shut(vehicle: Vehicle, close_instantly: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(close_instantly)];
    unsafe { native::invoke(0xA4FFCD645B11F25A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_doors_to_open_at_any_distance(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x362CEDD2A41E0747, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_door_broken(vehicle: Vehicle, door_id: i32, delete_door: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_id), NativeArg::boolean(delete_door)];
    unsafe { native::invoke(0x9666CF20A1C6D780, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_door_control(vehicle: Vehicle, door_id: i32, speed: i32, angle: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_id), NativeArg::int(speed), NativeArg::float(angle)];
    unsafe { native::invoke(0xD57F10EBBA814ECF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_door_latched(vehicle: Vehicle, door_id: i32, p2: bool, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_id), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x06F8A202EB312A3C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_door_open(vehicle: Vehicle, door_id: i32, loose: bool, open_instantly: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_id), NativeArg::boolean(loose), NativeArg::boolean(open_instantly)];
    unsafe { native::invoke(0x550CE392A4672412, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_door_shut(vehicle: Vehicle, door_id: i32, close_instantly: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_id), NativeArg::boolean(close_instantly)];
    unsafe { native::invoke(0x6A3C24B91FD0EA09, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_engine_can_degrade(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x48E4C137A71C2688, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_engine_health(vehicle: Vehicle, health: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(health)];
    unsafe { native::invoke(0x8BDC5B998B4654EF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_engine_on(vehicle: Vehicle, value: bool, instantly: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(value), NativeArg::boolean(instantly)];
    unsafe { native::invoke(0xB64CFA14CB9A2E78, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_exclusive_driver(vehicle: Vehicle, ped: Ped, index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(ped), NativeArg::int(index)];
    unsafe { native::invoke(0xC6B9BF123B9463B6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_explodes_on_high_explosion_damage(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xA402939C6761E1A3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_extra(vehicle: Vehicle, extra_id: i32, disable: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(extra_id), NativeArg::boolean(disable)];
    unsafe { native::invoke(0xBB6F89150BC9D16B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_fixed(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x79811282A9D1AE56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_forward_speed(vehicle: Vehicle, speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(speed)];
    unsafe { native::invoke(0xF9F92AF49F12F6E7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_handbrake(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x91BE51AEC4E99710, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_has_been_owned_by_player(vehicle: Vehicle, owned: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(owned)];
    unsafe { native::invoke(0xBB5A3FA8ED3979C5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_has_strong_axles(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x252253C8A45AA1FC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_has_unbreakable_lights(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC903855E028A05F2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_inactive_during_playback(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x4EA71B4C9DB3C3F1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_individual_doors_locked(vehicle: Vehicle, door_id: i32, door_lock_status: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_id), NativeArg::int(door_lock_status)];
    unsafe { native::invoke(0xA9F1D75195CC40F6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_influences_wanted_level(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC1842F40FD501DA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_is_considered_by_player(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x54800D386C5825E5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_is_stolen(vehicle: Vehicle, is_stolen: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(is_stolen)];
    unsafe { native::invoke(0x6C32FC81DFF25C9A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_keep_engine_on_when_abandoned(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x1549BA7FE83A2383, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_lights(vehicle: Vehicle, state: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(state)];
    unsafe { native::invoke(0x629F0A0E952CAE7D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_limit_speed_when_player_inactive(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x8F75941C86EEBFCA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_lod_multiplier(vehicle: Vehicle, multiplier: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(multiplier)];
    unsafe { native::invoke(0x5F5E2B1B9EAECC0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_may_be_used_by_goto_point_any_means(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x7549B9E841940695, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_not_stealable_ambiently(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x09C970AE59ABF6B2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_on_ground_properly(vehicle: Vehicle, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x7263332501E07F52, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_petrol_tank_health(vehicle: Vehicle, health: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(health)];
    unsafe { native::invoke(0x6AB2918EE3BEC94C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_provides_cover(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x652712478F1721F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_respects_locks_when_has_driver(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x33992A808DF1C1BA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_shoot_at_target(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xB79BE78C665B3E6D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_stays_frozen_when_cleaned_up(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x23A3AB86E0807721, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_steer_bias(vehicle: Vehicle, value: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(value)];
    unsafe { native::invoke(0x84DAAE11E9EE4FC3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_stop_instantly_when_player_inactive(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC84E138448507567, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_strong(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xAB315515C9F8803D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_tyres_can_burst(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xEBD0A4E935106FE5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_undriveable(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6E884BAB713A2A94, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_wheels_can_break(vehicle: Vehicle, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x839137C40275FB77, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_wheels_can_break_off_when_blow_up(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xC462C79379ABBCB1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn skip_time_in_playback_recorded_vehicle(vehicle: Vehicle, time: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(time)];
    unsafe { native::invoke(0x5F5E6379C59EFC56, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_playback_recorded_vehicle(vehicle: Vehicle, recording: i32, script: &str, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(recording), NativeArg::string(script), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x4932B84E3276508E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn start_vehicle_horn(vehicle: Vehicle, duration: i32, mode: Hash, forever: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(duration), NativeArg::hash(mode), NativeArg::boolean(forever)];
    unsafe { native::invoke(0xB4E3BFC39CA16057, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_bringing_vehicle_to_halt(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x7C06330BFDDA182E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn stop_playback_recorded_vehicle(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xBF9B4D6267E8C26D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn track_vehicle_visibility(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x1F3969B140DEE157, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x012701_ed938_b85_de(p0: f32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(p0), NativeArg::float(p1)];
    unsafe { native::invoke(0x012701ED938B85DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0355_fe37240_e2_c77(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0355FE37240E2C77, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x04_f0579_dbdd32_f34(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x04F0579DBDD32F34, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0516_fae561276_efc(track_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(track_index)];
    unsafe { native::invoke(0x0516FAE561276EFC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x06_a09_a6_e0_c6_d2_a84(train: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x06A09A6E0C6D2A84, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0794199_b25_e499_e1(wagon: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(wagon), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x0794199B25E499E1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_cd7914_d17_a970_ab(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x0CD7914D17A970AB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_d5_fdf0_d36_fa10_cd(track_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index)];
    unsafe { native::invoke(0x0D5FDF0D36FA10CD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_f7_f603_bde08_c4_d3(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0F7F603BDE08C4D3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_fddee66_e3465726(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x0FDDEE66E3465726, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x104_d9_a7_b1_c0_d0783(vehicle: Vehicle, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(p1)];
    unsafe { native::invoke(0x104D9A7B1C0D0783, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1180_a2974_d251_b7_b(train: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0x1180A2974D251B7B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x12_f6_c6_ed3_eff42_de(vehicle: Vehicle, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x12F6C6ED3EFF42DE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x13_eb275_bf81636_d1(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x13EB275BF81636D1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x14_da8_c4_bc2_ccd90_a(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x14DA8C4BC2CCD90A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x15206_e88_ff7617_df(track_index: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::float(p1)];
    unsafe { native::invoke(0x15206E88FF7617DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x15_cc8_c33_d7_ffcc4_a(vehicle: Vehicle, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(p1)];
    unsafe { native::invoke(0x15CC8C33D7FFCC4A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x160_c1_b5_ab48_ab87_c(train: Vehicle, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::float(p1)];
    unsafe { native::invoke(0x160C1B5AB48AB87C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x165_be2001_e5_e4_b75(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x165BE2001E5E4B75, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x16_b86_a49_e072_aa85() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x16B86A49E072AA85, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x172_e9_dd35858_dcd7(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x172E9DD35858DCD7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_a861_f899_ebbe17_c(train: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x1A861F899EBBE17C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2045429505158_d1_a(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x2045429505158D1A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2200_ab13_cbd10_f4_e(vehicle: Vehicle, x: f32, y: f32, z: f32, p4: bool, p5: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(p4), NativeArg::float(p5)];
    unsafe { native::invoke(0x2200AB13CBD10F4E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x23_f66_c36_f8_e5_eaab(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x23F66C36F8E5EAAB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x27_e3_f2_b57209_fa54(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x27E3F2B57209FA54, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_a7413168_f6_cd5_a8() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x2A7413168F6CD5A8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_bb2_b5_bcf0_df8008(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x2BB2B5BCF0DF8008, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_c46_d2_a591_d8_c322(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x2C46D2A591D8C322, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3053064_f909_b5_f42(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3053064F909B5F42, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3137_edc899_e6_dae4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3137EDC899E6DAE4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x331_cbd247_fc5_daa8(config_hash: Hash, x: f32, y: f32, z: f32, direction: bool, p5: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(config_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(direction), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x331CBD247FC5DAA8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x34_bcf6209_b9668_a7(track_index: i32, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), p1];
    unsafe { native::invoke(0x34BCF6209B9668A7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x37_d238_be69_f7378_a(track_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(track_index)];
    unsafe { native::invoke(0x37D238BE69F7378A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x38_e7_dd70_a242_d5_cb(track_index: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::int(p1)];
    unsafe { native::invoke(0x38E7DD70A242D5CB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_abfa128_f5_bf5_a70(train_track: Hash, junction_index: i32, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(train_track), NativeArg::int(junction_index), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x3ABFA128F5BF5A70, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_d86997_a86_feef0_d(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x3D86997A86FEEF0D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x41365_db586_cd9_e8_e(track_index: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::float(p1)];
    unsafe { native::invoke(0x41365DB586CD9E8E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x41_f0_b254_ddf71473(wagon: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(wagon)];
    unsafe { native::invoke(0x41F0B254DDF71473, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x427_c919_e9809_e370(track_index: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::int(p1)];
    unsafe { native::invoke(0x427C919E9809E370, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x485_b05_ef05_b9_aee9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x485B05EF05B9AEE9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_c05_b42_a8_d937796() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x4C05B42A8D937796, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_c60_c333_f9_cca2_b6(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x4C60C333F9CCA2B6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x51_c7694_e140_fae43(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x51C7694E140FAE43, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_aadc7_bbbb1_bceeb(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0x5AADC7BBBB1BCEEB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x615_b3_b8_e73634509(track_index: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::float(p1)];
    unsafe { native::invoke(0x615B3B8E73634509, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x63509_ddf102_e08_e8(track_index: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::int(p1)];
    unsafe { native::invoke(0x63509DDF102E08E8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6355602_c02_edc6_df(entity: Entity, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), p1];
    unsafe { native::invoke(0x6355602C02EDC6DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6703872_ec09_bc158(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6703872EC09BC158, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6835_afea10_e186_f4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6835AFEA10E186F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x68830738_a6_bfb370(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x68830738A6BFB370, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x697_df68_f3_a761_a50(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x697DF68F3A761A50, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_b34_be961_f639_e21(track_index: i32, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::int(p1)];
    unsafe { native::invoke(0x6B34BE961F639E21, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_b53_f4_b811_e583_d2(vehicle: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6B53F4B811E583D2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_c87_f49_bfa181_db5(x: f32, y: f32, z: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x6C87F49BFA181DB5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_de072_ac8_a95_ffc1(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x6DE072AC8A95FFC1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_ea1273_d525427_f4(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x6EA1273D525427F4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_fd7_bdf10304363_a(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x6FD7BDF10304363A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x703_d4_fb366_da4452(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x703D4FB366DA4452, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x718_eb706_b6_e998_a0(track_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index)];
    unsafe { native::invoke(0x718EB706B6E998A0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x73118_a3_ee9_c9_b6_db(wagon: Vehicle, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(wagon), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x73118A3EE9C9B6DB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7408_b5_c66_ba31_adb(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10];
    unsafe { native::invoke(0x7408B5C66BA31ADB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x750_d42_c013_f64_ae7(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x750D42C013F64AE7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x762_fdc4_c19_e5_a981(train_carriage: Entity, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train_carriage), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x762FDC4C19E5A981, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7840576_c50_a13_dba(train: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x7840576C50A13DBA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_be0746539_def0_c8(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x7BE0746539DEF0C8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8379_e05871_ad24_e0() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x8379E05871AD24E0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x850_ce59_dec2028_f3(vehicle: Vehicle, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), p1];
    unsafe { native::invoke(0x850CE59DEC2028F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x873_aaf600_cc36_dac(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x873AAF600CC36DAC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x877_ea24_eb1614495(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x877EA24EB1614495, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x87_b974_e54_c71_ba7_b(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x87B974E54C71BA7B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8878_ff3_eee2868_a9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8878FF3EEE2868A9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_decd262602548_b9(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x8DECD262602548B9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9868_c0_d0134855_f7(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9868C0D0134855F7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x98_a7598_c579_ee871(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x98A7598C579EE871, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_d12796_ef4_bf9_ea9(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x9D12796EF4BF9EA9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_e8711_c81_aa17876(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x9E8711C81AA17876, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a13028_e22564_a1_bd(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA13028E22564A1BD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a230_a5_dde12_ed374(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xA230A5DDE12ED374, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a72_b1_bf3857_b94_d7(train: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xA72B1BF3857B94D7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a7966807953_a18_ee(track_index: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::float(p1)];
    unsafe { native::invoke(0xA7966807953A18EE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a9_e185_d498_b9_ac67(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA9E185D498B9AC67, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ae7_e66_a61_e7_c17_a5(train: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xAE7E66A61E7C17A5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b4241_ad8_f5_aee9_ed(track_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(track_index)];
    unsafe { native::invoke(0xB4241AD8F5AEE9ED, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b42_c87521_d1_bdd2_f(vehicle: Vehicle, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xB42C87521D1BDD2F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b961_dd799_a837_bd7() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB961DD799A837BD7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c325_a6_baa62_cf8_a2(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC325A6BAA62CF8A2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c351394_b932_a6_a50(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC351394B932A6A50, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c399_cc89_fba05_da0(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC399CC89FBA05DA0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c4_a2_c11_fc0_d41916(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC4A2C11FC0D41916, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cacab2_b123_bbdbd6(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xCACAB2B123BBDBD6, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_caff2_c9747103_c02(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xCAFF2C9747103C02, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cbc7_b6_f9_a56_b79_f6(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCBC7B6F9A56B79F6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cbf88256_e44_d5_d39(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xCBF88256E44D5D39, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ceb1_f1_eed484_a5_b4(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCEB1F1EED484A5B4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_cf342503_ca4_c8_df1(vehicle: Vehicle, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(p1)];
    unsafe { native::invoke(0xCF342503CA4C8DF1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d0116_df21_e6_c7_b36(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD0116DF21E6C7B36, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d0_aabe5_b9_f8_fa589(track_index: i32, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::float(p1)];
    unsafe { native::invoke(0xD0AABE5B9F8FA589, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d0_ba1853_d76683_c8(track_index: i32, x: f32, y: f32, z: f32, p4: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), p4];
    unsafe { native::invoke(0xD0BA1853D76683C8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d1_df5_e54_f4_acbe1_a(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0xD1DF5E54F4ACBE1A, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d21_a3_d421_e7_f09_f7(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD21A3D421E7F09F7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d4907_ef4334_c7602(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD4907EF4334C7602, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d826690_b5_cf3_beff(vehicle: Vehicle, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), p1];
    unsafe { native::invoke(0xD826690B5CF3BEFF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d9_bf3_ed8_efb67_ea3(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xD9BF3ED8EFB67EA3, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc0556_d0_f484_ecaa(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xDC0556D0F484ECAA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc69_f6913_cca0_b99(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDC69F6913CCA0B99, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dd100_ce1_ebbf37_e3(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xDD100CE1EBBF37E3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_de8_c5_b9_f65017_fa1(train: Vehicle) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0xDE8C5B9F65017FA1, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e12_f5_ed49_f44_d40_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE12F5ED49F44D40D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e1_c0_f8781_bf130_c2(wagon: Vehicle, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(wagon), NativeArg::int(p1)];
    unsafe { native::invoke(0xE1C0F8781BF130C2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e682002_db1_f30669(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE682002DB1F30669, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e6_bd7_dd3_fd474415(train: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xE6BD7DD3FD474415, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e777_ddf3_e78397_e8(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE777DDF3E78397E8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e78993_ff9022_c064(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xE78993FF9022C064, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ef28_a614_b4_b264_b8(train: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xEF28A614B4B264B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f05_dfaf1_adfef2_cd(train_config: Hash, x: f32, y: f32, z: f32, direction: bool, p5: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(train_config), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(direction), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xF05DFAF1ADFEF2CD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f57_db8_e83_dcd8349(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF57DB8E83DCD8349, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f5_ea41_c1408695_fb(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xF5EA41C1408695FB, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f6_e3_d38869_d0_f7_ad(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF6E3D38869D0F7AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f8_f7_da13_cfbd4532(track_index: i32, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xF8F7DA13CFBD4532, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fc4_f15_a7_dddc47_b1(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xFC4F15A7DDDC47B1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ff2_b1_f59_fb892_f14(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xFF2B1F59FB892F14, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fffe15_b433300_b8_c(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xFFFE15B433300B8C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_train_temporary_stop(train: Vehicle, track_index: i32, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::int(track_index), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x41503629D1139ABC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn are_any_vehicle_wheels_destroyed(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x18714953CCED17D3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_draft_vehicle_harness_ped(mount: Ped, draft: Vehicle, harness_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(mount), NativeArg::int(draft), NativeArg::int(harness_id)];
    unsafe { native::invoke(0x316CDB5B6E8F4110, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn break_off_draft_wheel(vehicle: Vehicle, wheel_index: i32, destroying_force: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(wheel_index), NativeArg::float(destroying_force)];
    unsafe { native::invoke(0xC372B6A88F6E4AD8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn break_off_vehicle_wheel(vehicle: Vehicle, wheel_index: i32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(wheel_index)];
    unsafe { native::invoke(0xD4F5EFB55769D272, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn break_vehicle_straps(vehicle: Vehicle, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xD1EFA8D68BF5D63D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_draft_vehicle(model_hash: Hash, x: f32, y: f32, z: f32, heading: f32, is_network: bool, b_script_host_veh: bool, b_dont_auto_create_draft_animals: bool, draft_animal_pop_group: Hash, p9: bool) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::hash(model_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(heading), NativeArg::boolean(is_network), NativeArg::boolean(b_script_host_veh), NativeArg::boolean(b_dont_auto_create_draft_animals), NativeArg::hash(draft_animal_pop_group), NativeArg::boolean(p9)];
    unsafe { native::invoke(0x214651FB1DFEBA89, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_mission_train(config_hash: Hash, x: f32, y: f32, z: f32, direction: bool, passengers: bool, p6: bool, conductor: bool) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::hash(config_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(direction), NativeArg::boolean(passengers), NativeArg::boolean(p6), NativeArg::boolean(conductor)];
    unsafe { native::invoke(0xC239DBD9A57D2A71, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_vehicle_lanterns(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xE1A83D4A3B5D7938, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_draft_vehicle_harness_from_index(draft: Vehicle, harness_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(draft), NativeArg::int(harness_id)];
    unsafe { native::invoke(0x4402960666000E62, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_draft_vehicle_harness_ped(draft: Vehicle, ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(draft), NativeArg::int(ped)];
    unsafe { native::invoke(0xB36D3EC70963BE60, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn detach_wagon_entity_from_train(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0x54CBDD6E1B4CB4DF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_train_exist_on_track(track_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(track_index)];
    unsafe { native::invoke(0xC29996A337BDD099, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn fade_and_destroy_vehicle(vehicle: &mut Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(vehicle)];
    unsafe { native::invoke(0x35DC1877312FBA0F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_all_wagon_passengers(wagon: Vehicle, item_set: ItemSet) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(wagon), NativeArg::int(item_set)];
    unsafe { native::invoke(0x0E558D3A49D759D6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_balloon_object_from_vehicle(vehicle: Vehicle) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x0BA4250D20007C2E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_breakable_vehicle_locks_state(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xE015CF1F2C0959D8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_breakable_vehicle_lock_object(vehicle: Vehicle, index: i32) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(index)];
    unsafe { native::invoke(0x58F2244C1286D09A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_checkpoint_train_spawn_location(track_index: i32, x: f32, y: f32, z: f32, distance: f32, direction: bool) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(distance), NativeArg::boolean(direction)];
    unsafe { native::invoke(0x35D302397E524939, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_track_for_train(train: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0xAF787E081AC4A8EE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_draft_vehicle_desired_speed(vehicle: Vehicle) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xC6D7DDC843176701, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_junction_coords_for_train_track(train_track: Hash, junction_index: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::hash(train_track), NativeArg::int(junction_index)];
    unsafe { native::invoke(0x785639D89F8451AB, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_nearest_train_track_position(x: f32, y: f32, z: f32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x6DE03BCC15E81710, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_breakable_vehicle_lock_objects(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x2FA86833E3617E2D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_cars_from_train_config(train_config: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(train_config)];
    unsafe { native::invoke(0x635423D55CA84FC8, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_draft_vehicle_harness_ped(model_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(model_hash)];
    unsafe { native::invoke(0x5B1A26BB18E7D451, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_draft_vehicle_logs(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x288CBB414C3C2FBB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_draft_vehicle_straps(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x1121B07088ED3013, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_in_draft_harness(vehicle: Vehicle, harness_id: i32) -> Result<Ped, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(harness_id)];
    unsafe { native::invoke(0xA8BA0BAE0173457B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_rowing_oars(vehicle: Vehicle, left: &mut Entity, right: &mut Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::in_out(left), NativeArg::in_out(right)];
    unsafe { native::invoke(0xA6E210FB4283B767, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_station_at_index(track_index: i32, station_index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::int(station_index)];
    unsafe { native::invoke(0x9CC94A948EAF5372, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_station_coords_from_train_station_data(track_index: i32, station_index: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(track_index), NativeArg::int(station_index)];
    unsafe { native::invoke(0xBA958F68031DDBFC, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_track_amount_of_visible_pixels(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x13C190302369308B, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_track_index_from_coords(x: f32, y: f32, z: f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x85D39F5E3B6D7EB0, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_car(train: Vehicle) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0x671A07C9A1CD50A5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_carriage_trailer_number(train: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0x60B7D1DCC312697D, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_direction(train: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0x3C9628A811CBD724, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_direction_from_index(track_index: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(track_index)];
    unsafe { native::invoke(0x67995318F5FAA496, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_model_from_train_config_by_car_index(train_config: Hash, train_car_index: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(train_config), NativeArg::int(train_car_index)];
    unsafe { native::invoke(0x8DF5F6A19F99F0D5, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_position_on_track(track_index: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(track_index)];
    unsafe { native::invoke(0x1E8A921112891651, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_track_from_train_vehicle(train: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0x45853F4E17D847D5, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_track_junction_at_coords(train_track: Hash, x: f32, y: f32, z: f32, junction_index: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(train_track), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(junction_index)];
    unsafe { native::invoke(0x86AFC343CF7F0B34, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_train_vehicle_from_track_index(track_index: i32) -> Result<Vehicle, NativeError> {
    let arguments = [NativeArg::int(track_index)];
    unsafe { native::invoke(0x6E585A616ABB8401, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_doors_locked_for_team(vehicle: Vehicle, team: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(team)];
    unsafe { native::invoke(0xDD1E1393D966D39A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_is_prop_set_applied(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xD798DF5DB67B1659, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_livery(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xBB765B8FD49A796C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_owner(vehicle: Vehicle) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xB729679356A889AE, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_tint(vehicle: Vehicle) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0xA44D65E6C624526F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_vehicle_turret_seat(vehicle: Vehicle, seat_index: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::in_out(seat_index)];
    unsafe { native::invoke(0xFF5791B7639C2A46, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_train_loaded(train: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0xBD3C4A2ED509205E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hide_horse_reins(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x201B8ED4FF7FE9F5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_boat_grounded(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x30D86B2B7622D0EB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_exclusive_driver_of_vehicle(ped: Ped, vehicle: Vehicle, out_index: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(vehicle), NativeArg::in_out(out_index)];
    unsafe { native::invoke(0xB213D2A560B2E48B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_this_model_a_draft_vehicle(model: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(model)];
    unsafe { native::invoke(0xB9D5BDDA88E1BB66, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_brought_to_halt(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x404527BC03DA0E6C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_door_broken(vehicle: Vehicle, door_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(door_id)];
    unsafe { native::invoke(0xE979BB5602AD3402, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_fading_out(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x5136B284B67B35C7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_on_fire(vehicle: Vehicle) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x0E3BF7ED4169EC43, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_vehicle_wheel_destroyed(vehicle: Vehicle, wheel: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(wheel)];
    unsafe { native::invoke(0xCB2CA620C48BC875, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn recover_draft_vehicle_falling_log(vehicle: Vehicle) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x42404D57D621601A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_vehicle_asset_anims(ped: Ped, entity: Entity, vehicle_asset: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(entity), NativeArg::int(vehicle_asset)];
    unsafe { native::invoke(0xCF9DA72002FC16BF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn return_train_info_from_handle(train: Vehicle, train_track: &mut Hash, junction_index: &mut i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::in_out(train_track), NativeArg::in_out(junction_index)];
    unsafe { native::invoke(0x09034479E6E3E269, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_junctions_cleared() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x138398153824E332, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_all_vehicle_generators_disabled_for_volume(volume: Volume, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x424FFCB9F0D2D4B5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_balloon_hover_state(balloon: Vehicle, p1: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(balloon), NativeArg::float(p1)];
    unsafe { native::invoke(0x7C9E45A4CED2E8DA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_batch_tarp_height(vehicle: Vehicle, height: f32, immediately: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(height), NativeArg::boolean(immediately)];
    unsafe { native::invoke(0x31F343383F19C987, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_draft_animal_random_seed(vehicle: Vehicle, seed: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(seed)];
    unsafe { native::invoke(0x8C6D9A399126C194, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_draft_vehicle_allow_draft_animal_auto_creation(vehicle: Vehicle, allow: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(allow)];
    unsafe { native::invoke(0x87344305778E5415, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_draft_vehicle_animals_can_detach(draft: Vehicle, can_detach: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(draft), NativeArg::boolean(can_detach)];
    unsafe { native::invoke(0x6090A031C69F384E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_draft_vehicle_desired_speed(vehicle: Vehicle, speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(speed)];
    unsafe { native::invoke(0x0C3F0F7F92CA847C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_draft_vehicle_yoke_can_break(draft: Vehicle, can_break: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(draft), NativeArg::boolean(can_break)];
    unsafe { native::invoke(0x226C6A4E3346D288, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_force_coach_robbery_loot(vehicle: Vehicle, coachrobbery_loot: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::hash(coachrobbery_loot)];
    unsafe { native::invoke(0xF489F94BFEE12BB0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_force_high_lod_vehicle(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x1098CDA477890165, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_horse_traffic_grouping_distribution(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xF5FFB08976911B50, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_mission_train_warp_to_coords(train: Vehicle, x: f32, y: f32, z: f32, direction: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(direction)];
    unsafe { native::invoke(0xC9EA26893C9E4024, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_random_trains_whistle_enabled(train: Vehicle, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x1BFBAFCC6760FF02, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_train_destruction_enabled(train: Vehicle, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x07E2E21E799080A0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_train_halt(train: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0x3660BCAB3A6BB734, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_train_leave_station(train: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train)];
    unsafe { native::invoke(0x787E43477746876F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_train_max_speed(train: Vehicle, speed: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::float(speed)];
    unsafe { native::invoke(0x9F29999DFDF2AEB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_train_stops_for_stations(train: Vehicle, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x4182C037AA1F0091, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_train_track_junction_switch(train_track: Hash, junction_index: i32, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(train_track), NativeArg::int(junction_index), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0xE6C5E2125EB210C1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_deterioration(vehicle: Vehicle, amount: f32, p2: i32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(amount), NativeArg::int(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x8E5DA070BAD3279E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_dirt_level_2(vehicle: Vehicle, dirt_level: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(dirt_level)];
    unsafe { native::invoke(0xBAE0EEDF93F05EAA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_is_in_hurry(vehicle: Vehicle, enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(enabled)];
    unsafe { native::invoke(0xCE1531927AD6C9F8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_livery(vehicle: Vehicle, livery_index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(livery_index)];
    unsafe { native::invoke(0xF89D82A0582E46ED, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_lod_level(vehicle: Vehicle, lod_level: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(lod_level)];
    unsafe { native::invoke(0x3FA7D7D1E0EA809E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_mud_level(vehicle: Vehicle, mud_level: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(mud_level)];
    unsafe { native::invoke(0x4D15E49764CB328A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_road_link_forced(vehicle: Vehicle, start_x: f32, start_y: f32, start_z: f32, end_x: f32, end_y: f32, end_z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(start_x), NativeArg::float(start_y), NativeArg::float(start_z), NativeArg::float(end_x), NativeArg::float(end_y), NativeArg::float(end_z)];
    unsafe { native::invoke(0xC2E62678D602853C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_snow_level(vehicle: Vehicle, snow_level: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(snow_level)];
    unsafe { native::invoke(0x6F73EFAB11651D7F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_tint(vehicle: Vehicle, tint_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(tint_id)];
    unsafe { native::invoke(0x8268B098F6FCA4E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_wet_level(vehicle: Vehicle, wet_level: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::float(wet_level)];
    unsafe { native::invoke(0x5AABB09F6FBD1F87, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn show_horse_reins(vehicle: Vehicle) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle)];
    unsafe { native::invoke(0x41CDA90EE3450921, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn trigger_train_whistle(train: Vehicle, whistle_sequence: &str, p2: bool, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(train), NativeArg::string(whistle_sequence), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xCFE122EC635CC2B2, &arguments) }.map(|_| ())
}

// ============================================================================
// VOICE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x08797_a8_c03868_cb8(threshold: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(threshold)];
    unsafe { native::invoke(0x08797A8C03868CB8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_ded260_a1958_a82_e(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x0DED260A1958A82E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_c38_c3577901_af1_f() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x1C38C3577901AF1F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x1_fbf7_f5_ba7_e4_be3_a(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x1FBF7F5BA7E4BE3A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x2_f82_cab262_c8_ae26(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x2F82CAB262C8AE26, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x356135_b9_b10_a2_a82(gamer_handle: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle)];
    unsafe { native::invoke(0x356135B9B10A2A82, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4791899615_d70_fa2(player: Player, p1: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0x4791899615D70FA2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x49623_bcfc3_a3_d829(player: Player, muted: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::boolean(muted)];
    unsafe { native::invoke(0x49623BCFC3A3D829, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x58125_b691_f6827_d5(proximity: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(proximity)];
    unsafe { native::invoke(0x58125B691F6827D5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x5_ca7_fb7_d6_de49_dcc(player: Player) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x5CA7FB7D6DE49DCC, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x767931_c727_df2_ed7(player: Player, p1: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(player), NativeArg::int(p1)];
    unsafe { native::invoke(0x767931C727DF2ED7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x79_f478_ff5_f9_f4_f05(enabled: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(enabled)];
    unsafe { native::invoke(0x79F478FF5F9F4F05, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x8_e462_db1_eaa9_c47_c(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x8E462DB1EAA9C47C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x919_af2_d93_e9_aa89_d(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0x919AF2D93E9AA89D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_aa35_fd9_abab490_a3(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xAA35FD9ABAB490A3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b3_e8841_f6_bdaf83_e() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xB3E8841F6BDAF83E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b6_e79850_b759_a30_e(team_id: i32, allow: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(team_id), NativeArg::boolean(allow)];
    unsafe { native::invoke(0xB6E79850B759A30E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b779_f4_fa19269_aec(flag: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(flag)];
    unsafe { native::invoke(0xB779F4FA19269AEC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ccf71_fcfa0070_b1_a() -> Result<bool, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xCCF71FCFA0070B1A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_db622_ecd3_dcbe078(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xDB622ECD3DCBE078, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_dc9_b361_cb7776673(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xDC9B361CB7776673, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ec8703_e4536_a9952() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xEC8703E4536A9952, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ef6_f2_a35_faaf2_ed7(player: Player) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xEF6F2A35FAAF2ED7, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f8938_cf3984092_a5(player: Player) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(player)];
    unsafe { native::invoke(0xF8938CF3984092A5, &arguments) }.map(|_| ())
}

// ============================================================================
// VOLUME
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_aggregate() -> Result<Volume, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x59F6F5C1D129F106, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_box(x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z)];
    unsafe { native::invoke(0xDF85637F22706891, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_cylinder(x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z)];
    unsafe { native::invoke(0x0522D4774B82E3E6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_sphere(x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z)];
    unsafe { native::invoke(0xB3FB80A32BAE3065, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_volume(volume: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x43F867EF5C463A53, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_volume_collide_with_any_volume_lock(x: f32, y: f32, z: f32, radius: f32, p4: bool, p5: i32, p6: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::boolean(p4), NativeArg::int(p5), NativeArg::int(p6)];
    unsafe { native::invoke(0x397769175A7DBB30, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_volume_exist(volume: Volume) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x92A78D0BEDB332A3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_volume_coords(volume: Volume) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0xF70F00013A62F866, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_volume_lock_request_status(vol_lock_request_id: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(vol_lock_request_id)];
    unsafe { native::invoke(0xB33A604345F58202, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_volume_rotation(volume: Volume) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x18675BC914891122, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_volume_scale(volume: Volume) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x3E2A25B2416DD67E, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_point_in_volume(volume: Volume, x: f32, y: f32, z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xF256A75210C5C0EB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_volume_lock_request_valid(vol_lock_request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vol_lock_request_id)];
    unsafe { native::invoke(0xA4A4359320345B34, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_volume_lock(x: f32, y: f32, z: f32, radius: f32, p4: i32, p5: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(p4), NativeArg::int(p5)];
    unsafe { native::invoke(0xF14BCEF290F869E1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_volume_lock_with_args(args: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0x183C0B6CFEFFCAE4, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_volume_coords(volume: Volume, pos_x: f32, pos_y: f32, pos_z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::float(pos_x), NativeArg::float(pos_y), NativeArg::float(pos_z)];
    unsafe { native::invoke(0x541B8576615C33DE, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_volume_owner_persistent_character(volume: Volume, pers_char: PersChar, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(pers_char), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xE2BE6FFA4A13CBB0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_volume_rotation(volume: Volume, rot_x: f32, rot_y: f32, rot_z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z)];
    unsafe { native::invoke(0xA07CF1B21B56F041, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_volume_scale(volume: Volume, scale_x: f32, scale_y: f32, scale_z: f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z)];
    unsafe { native::invoke(0xA46E98BDC407E23D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x128_fc3_a893_bf853_a(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x128FC3A893BF853A, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x351_d71_b8_b72_b858_b(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x351D71B8B72B858B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x3_efabb21_e14_a6_bd1(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x3EFABB21E14A6BD1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x40_f769_d31_a00_d5_a0(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x40F769D31A00D5A0, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x4_a8_fefc43_fd8_ac9_b(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x4A8FEFC43FD8AC9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x51_e52_c9687_fcdeec(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6];
    unsafe { native::invoke(0x51E52C9687FCDEEC, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x52572_b331_e693_aed(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x52572B331E693AED, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x53_d05_d60_e5_f5_b40_c(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0x53D05D60E5F5B40C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x695_dac2_db928_f308(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x695DAC2DB928F308, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x6_d5_f9_e69_ba1_be783(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x6D5F9E69BA1BE783, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x748_c5_f51_a18_cb8_f0(p0: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(p0)];
    unsafe { native::invoke(0x748C5F51A18CB8F0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x7_fd78_dfd0_c5_d7_b9_b(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x7FD78DFD0C5D7B9B, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x870_e9981_ed27_c815(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0x870E9981ED27C815, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x998202_b206872672(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x998202B206872672, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_aa9_ee2_aafc717623(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5];
    unsafe { native::invoke(0xAA9EE2AAFC717623, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ac355980681_a7_f89(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xAC355980681A7F89, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b440_f4_e35393_fc39(volume: Volume, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), p1];
    unsafe { native::invoke(0xB440F4E35393FC39, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b469_cfd9_e065_eb99(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xB469CFD9E065EB99, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c4019_cf9_ae8_e931_a(vol_lock_request_id: i32) -> Result<Vector3, NativeError> {
    let arguments = [NativeArg::int(vol_lock_request_id)];
    unsafe { native::invoke(0xC4019CF9AE8E931A, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c61_e2_fd926_dbb406() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC61E2FD926DBB406, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ca5_c90_d40665_d5_ce(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xCA5C90D40665D5CE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d460135_c98940274(volume: Volume, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), p1];
    unsafe { native::invoke(0xD460135C98940274, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d4_fa73_fe628_fec63(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xD4FA73FE628FEC63, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d52_df30355_ea7_c8_e(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xD52DF30355EA7C8E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d882_c5_b3991575_b7(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg) -> Result<Vector3, NativeError> {
    let arguments = [p0, p1, p2, p3, p4];
    unsafe { native::invoke(0xD882C5B3991575B7, &arguments) }.map(|result| result.as_vector3())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_eba87_b9273835_cf3(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xEBA87B9273835CF3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ee1_d6_ff54_caf7714(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xEE1D6FF54CAF7714, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f3_a2_fba5985_c8_cd5(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2, p3];
    unsafe { native::invoke(0xF3A2FBA5985C8CD5, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f6_ce6_f9_c3897804_e(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF6CE6F9C3897804E, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f6_f5447_d418_daa82(p0: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xF6F5447D418DAA82, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_fa15_c9_a320_e707_b0() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xFA15C9A320E707B0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_bounds_to_aggregate_volume(volume: Volume, aggregate: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(aggregate)];
    unsafe { native::invoke(0x6E0D3C3F828DA773, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_box_volume_to_volume_aggregate(aggregate: Volume, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(aggregate), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), NativeArg::float(p9)];
    unsafe { native::invoke(0x39816F6F94F385AD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_cylinder_volume_to_volume_aggregate(aggregate: Volume, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(aggregate), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), NativeArg::float(p9)];
    unsafe { native::invoke(0xBCE668AAF83608BE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_entry_volume_lock(args: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0x58D3803FA639A3BB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_sphere_volume_to_volume_aggregate(aggregate: Volume, p1: f32, p2: f32, p3: f32, p4: f32, p5: f32, p6: f32, p7: f32, p8: f32, p9: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(aggregate), NativeArg::float(p1), NativeArg::float(p2), NativeArg::float(p3), NativeArg::float(p4), NativeArg::float(p5), NativeArg::float(p6), NativeArg::float(p7), NativeArg::float(p8), NativeArg::float(p9)];
    unsafe { native::invoke(0x5B7D7BF36D2DE18B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_volume_to_volume_aggregate(aggregate: Volume, type_hash: Hash, x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(aggregate), NativeArg::hash(type_hash), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z)];
    unsafe { native::invoke(0x12FCAA23F2320422, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_anti_grief_volume(volume_type: Hash, x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::hash(volume_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z)];
    unsafe { native::invoke(0x0EB78C2B156635B1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_speed_volume(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg, p13: NativeArg, p14: NativeArg) -> Result<Volume, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13, p14];
    unsafe { native::invoke(0xBBE768E3AE76E07C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_aggregate_with_custom_name(name: &str) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::string(name)];
    unsafe { native::invoke(0x5D580DE6398BB162, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_box_with_custom_name(x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32, name: &str) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z), NativeArg::string(name)];
    unsafe { native::invoke(0xF68485C7495D848E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_by_hash(volume_type: Hash, x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::hash(volume_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z)];
    unsafe { native::invoke(0x502022FA1AF9DC86, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_by_hash_with_custom_name(volume_type: Hash, x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32, name: &str) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::hash(volume_type), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z), NativeArg::string(name)];
    unsafe { native::invoke(0x1F85E4AC774A201E, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_cylinder_with_custom_name(x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32, name: &str) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z), NativeArg::string(name)];
    unsafe { native::invoke(0xDF1E350EDDF06E59, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_lock(x: f32, y: f32, z: f32, radius: f32, flag: i32, p5: NativeArg) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(flag), p5];
    unsafe { native::invoke(0x00BBF7CEAE8C666A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_lock_attached_to_entity(entity: Entity, radius: f32, flag: i32, p3: NativeArg) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::float(radius), NativeArg::int(flag), p3];
    unsafe { native::invoke(0xF383E96C4904DF0C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_volume_sphere_with_custom_name(x: f32, y: f32, z: f32, rot_x: f32, rot_y: f32, rot_z: f32, scale_x: f32, scale_y: f32, scale_z: f32, name: &str) -> Result<Volume, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(rot_x), NativeArg::float(rot_y), NativeArg::float(rot_z), NativeArg::float(scale_x), NativeArg::float(scale_y), NativeArg::float(scale_z), NativeArg::string(name)];
    unsafe { native::invoke(0x10157BC3247FF3BA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_walk_and_talk_volume(p0: NativeArg, p1: NativeArg, p2: NativeArg, p3: NativeArg, p4: NativeArg, p5: NativeArg, p6: NativeArg, p7: NativeArg, p8: NativeArg, p9: NativeArg, p10: NativeArg, p11: NativeArg, p12: NativeArg) -> Result<Volume, NativeError> {
    let arguments = [p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12];
    unsafe { native::invoke(0xFD0E389CD44434B6, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn find_volume_lock_request_id_with_args(args: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::in_out(args)];
    unsafe { native::invoke(0x77A6E4AD0C496F81, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_volume_amount_of_indexed_items(x: f32, y: f32, z: f32, radius: f32, item_set: ItemSet) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(item_set)];
    unsafe { native::invoke(0x2B32B11520626229, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_volume_bounds(volume: Volume, min: &mut Vector3, max: &mut Vector3) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::in_out(min), NativeArg::in_out(max)];
    unsafe { native::invoke(0x5737199AF2DC609F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_volume_relationship(volume: Volume) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0x666C2F53ABEFC952, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_aggregate_volume(volume: Volume) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume)];
    unsafe { native::invoke(0xFEFF01B5725BCD22, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_point_near_volume_lock_center(x: f32, y: f32, z: f32, radius: f32, p4: i32, p5: i32, flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::float(radius), NativeArg::int(p4), NativeArg::int(p5), NativeArg::int(flags)];
    unsafe { native::invoke(0x769BB7626B8CDB06, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_volume_lock_request_valid_2(vol_lock_request_id: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(vol_lock_request_id)];
    unsafe { native::invoke(0xF6A8A652A6B186CD, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn modify_volume_lock_location(vol_lock: i32, x: f32, y: f32, z: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vol_lock), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0xEC43C2FFB70E3F30, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn release_lock_volume(vol_lock_request_id: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vol_lock_request_id)];
    unsafe { native::invoke(0xFDFECC6EE4491E11, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_bounds_from_aggregate_volume(volume: Volume, aggregate: Volume) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::int(aggregate)];
    unsafe { native::invoke(0xF92FA8890DECECF6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anti_grief_volume_blocks_horse(volume: Volume, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xBE551C2CC421185D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_anti_grief_volume_blocks_player(volume: Volume, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x5B23DFF8E0948BB2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_volume_relationship(volume: Volume, relationship_group: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::hash(relationship_group)];
    unsafe { native::invoke(0xFD010A2154B40676, &arguments) }.map(|_| ())
}

// ============================================================================
// WATER
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_water_lookup() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x754616EC6965D1FB, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_water_lookup() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x754616EC6965D1BF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_water_height(x: f32, y: f32, z: f32, height: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(height)];
    unsafe { native::invoke(0xFCA8B23F28813F69, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_water_height_no_waves(x: f32, y: f32, z: f32, height: &mut f32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::in_out(height)];
    unsafe { native::invoke(0xDCF3690AA262C03F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_extra_calming_quad(index: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(index)];
    unsafe { native::invoke(0x4BEF8DD75AF6C71C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn test_probe_against_all_water(x1: f32, y1: f32, z1: f32, x2: f32, y2: f32, z2: f32, flags: i32, intersection_pos: &mut Vector3) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x1), NativeArg::float(y1), NativeArg::float(z1), NativeArg::float(x2), NativeArg::float(y2), NativeArg::float(z2), NativeArg::int(flags), NativeArg::in_out(intersection_pos)];
    unsafe { native::invoke(0x8974647ED222EA5F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn test_vertical_probe_against_all_water(x: f32, y: f32, z: f32, flags: i32, water_height: &mut f32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(flags), NativeArg::in_out(water_height)];
    unsafe { native::invoke(0x2B3451FA1E3142E2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x09_a1_c7_dfdce54_fbc(p0: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(p0)];
    unsafe { native::invoke(0x09A1C7DFDCE54FBC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x0_dcec6_a92_e497_e17(entity: Entity, p1: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::int(p1)];
    unsafe { native::invoke(0x0DCEC6A92E497E17, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a33_f5069_b0_cb89_b8() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xA33F5069B0CB89B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b34_a6009_a0_db80_b8(entity: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity)];
    unsafe { native::invoke(0xB34A6009A0DB80B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e8126623008372_aa() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xE8126623008372AA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f0_fbf193_f1_f5_c0_ea(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xF0FBF193F1F5C0EA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_world_water_type() -> Result<i32, NativeError> {
    let arguments = [];
    unsafe { native::invoke(0x189739A7631C1867, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn reset_guarma_water_state() -> Result<(), NativeError> {
    let arguments = [];
    unsafe { native::invoke(0xC63540AEF8384769, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ocean_guarma_water_quadrant(waves_height: f32, p1: f32, waves_direction: i32, p3: f32, waves_amount: f32, p5: f32, waves_speed: f32, waves_strength: f32, ignore_height: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::float(waves_height), NativeArg::float(p1), NativeArg::int(waves_direction), NativeArg::float(p3), NativeArg::float(waves_amount), NativeArg::float(p5), NativeArg::float(waves_speed), NativeArg::float(waves_strength), NativeArg::boolean(ignore_height)];
    unsafe { native::invoke(0xC63540AEF8384732, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_world_water_type(water_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(water_type)];
    unsafe { native::invoke(0xE8770EE02AEE45C2, &arguments) }.map(|_| ())
}

// ============================================================================
// WEAPON
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_allow_dual_wield(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x918990BD9CE08582, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ammo_in_clip(ped: Ped, ammo: &mut i32, weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(ammo), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x2E1202248937775C, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ammo_in_ped_weapon(ped: Ped, weapon_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x015A522136D7F951, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_best_ped_shortarm_guid(ped: Ped, out_guid: &mut Any, p2: bool, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(out_guid), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xF52BD94B47CCF736, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_best_ped_weapon(ped: Ped, p1: bool, p2: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x8483E98E8B888AE2, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_ped_vehicle_weapon(ped: Ped, weapon_hash: &mut Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(weapon_hash)];
    unsafe { native::invoke(0x1017582BCD3832DC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_ped_weapon(ped: Ped, weapon_hash: &mut Hash, p2: bool, attach_point: i32, p4: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(weapon_hash), NativeArg::boolean(p2), NativeArg::int(attach_point), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x3A87E44BB9A01D54, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_ped_weapon_entity_index(ped: Ped, attach_point: i32) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attach_point)];
    unsafe { native::invoke(0x3B390A939AF0B5FC, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_ammo(ped: Ped, ammo: &mut i32, weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(ammo), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xDC16122C7A20C933, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_ammo_in_clip(ped: Ped, weapon_hash: Hash, p2: bool) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xA38DCFFCEA8962FA, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_ammo_by_type(ped: Ped, ammo_type: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(ammo_type)];
    unsafe { native::invoke(0x39D22031557946C1, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_ammo_type_from_weapon(ped: Ped, weapon_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x7FEAD38B326B9F74, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_backup_weapon(ped: Ped, p1: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC71FE230A513C30F, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_last_weapon_impact_coord(ped: Ped, coords: &mut Vector3) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(coords)];
    unsafe { native::invoke(0x6C4D0409BA1A2BC2, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_weapon_guid_at_attach_point(ped: Ped, attach_point: i32, weapon_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attach_point), NativeArg::in_out(weapon_guid)];
    unsafe { native::invoke(0x6929E22158E52265, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapontype_group(weapon_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xEDCA14CA5199FF25, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_clip_size(weapon_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xD3750CCC00635FC2, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_degradation(weapon_object: Object) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(weapon_object)];
    unsafe { native::invoke(0x0D78E1097F89E637, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_permanent_degradation(weapon_object: Object) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(weapon_object)];
    unsafe { native::invoke(0xD56E5F336C675EFA, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_delayed_weapon_to_ped(ped: Ped, weapon_hash: Hash, ammo_count: i32, p3: bool, add_reason: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::int(ammo_count), NativeArg::boolean(p3), NativeArg::hash(add_reason)];
    unsafe { native::invoke(0xB282DC6EBD803C75, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_weapon_to_ped(ped: Ped, weapon_hash: Hash, ammo_count: i32, b_force_in_hand: bool, b_force_in_holster: bool, attach_point: i32, b_allow_multiple_copies: bool, p7: f32, p8: f32, add_reason: Hash, b_ignore_unlocks: bool, permanent_degradation: f32, p12: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::int(ammo_count), NativeArg::boolean(b_force_in_hand), NativeArg::boolean(b_force_in_holster), NativeArg::int(attach_point), NativeArg::boolean(b_allow_multiple_copies), NativeArg::float(p7), NativeArg::float(p8), NativeArg::hash(add_reason), NativeArg::boolean(b_ignore_unlocks), NativeArg::float(permanent_degradation), NativeArg::boolean(p12)];
    unsafe { native::invoke(0x5E3BDDBCB83F3D84, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_weapon_to_ped_with_options(ped: Ped, in_data: &mut Any, out_data: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(in_data), NativeArg::in_out(out_data)];
    unsafe { native::invoke(0xBE7E42B07FD317AC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_ped_got_weapon(ped: Ped, weapon_hash: Hash, p2: i32, only_check_player_inventory: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::int(p2), NativeArg::boolean(only_check_player_inventory)];
    unsafe { native::invoke(0x8DECB02F88F428BC, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_weapon_got_weapon_component(weapon: Object, addon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(weapon), NativeArg::hash(addon_hash)];
    unsafe { native::invoke(0x76A18844E743BF91, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hide_ped_weapon_for_scripted_cutscene(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x6F6981D2253C208F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_armed(ped: Ped, flags: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(flags)];
    unsafe { native::invoke(0xCB690F680A3EA971, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_carrying_weapon(ped: Ped, weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xF29A186ED428B552, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_weapon_ready_to_shoot(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xB80CA294F2F26749, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_a_gun(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x705BE297EEBDB95D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_bow(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xC4DEC3CA8C365A5D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_melee_weapon(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x959383DCD42040DA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_pistol(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xDDC64F5E31EEDAB6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_repeater(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xDDB2578E95EF7138, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_revolver(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xC212F1D05A8232BB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_rifle(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x0A82317B7EBFC420, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_shotgun(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xC75386174ECE95D5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_valid(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x937C71165CF334B3, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn make_ped_drop_weapon(ped: Ped, p1: bool, attach_point: i32, p3: bool, p4: bool) -> Result<Entity, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::int(attach_point), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0xCEF4C65DE502D367, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_all_ped_weapons(ped: Ped, p1: bool, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xF25DF915FA38C5F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_weapon_component_from_weapon_object(weapon_object: Object, component: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(weapon_object), NativeArg::hash(component)];
    unsafe { native::invoke(0xF7D82B0D66777611, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_weapon_from_ped(ped: Ped, weapon_hash: Hash, p2: bool, remove_reason: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::boolean(p2), NativeArg::hash(remove_reason)];
    unsafe { native::invoke(0x4899CB088EDF59B8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_allow_any_weapon_drop(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x78030C7867D8B9B6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ammo_in_clip(ped: Ped, weapon_hash: Hash, ammo: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::int(ammo)];
    unsafe { native::invoke(0xDCD2A934D65CB497, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_current_ped_vehicle_weapon(ped: Ped, weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x75C55983C2C39DAA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_current_ped_weapon(ped: Ped, weapon_hash: Hash, equip_now: bool, attach_point: i32, p4: bool, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::boolean(equip_now), NativeArg::int(attach_point), NativeArg::boolean(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0xADF692B254977C0C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_current_ped_weapon_by_guid(ped: Ped, weapon_uid: &mut Any, p2: bool, p3: bool, p4: bool, p5: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(weapon_uid), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(p4), NativeArg::boolean(p5)];
    unsafe { native::invoke(0x12FB95FE3D579238, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_instantly_equip_weapon_pickups(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x739B9C6D0E7F7F93, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_ammo(ped: Ped, weapon_hash: Hash, ammo: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::int(ammo)];
    unsafe { native::invoke(0x14E56BC5B5DB6A19, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_ammo_by_type(ped: Ped, ammo_type: Hash, ammo: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(ammo_type), NativeArg::int(ammo)];
    unsafe { native::invoke(0x5FD1E1F011E76D7E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_ammo_to_drop(ped: Ped, p1: i32, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1), NativeArg::int(p2)];
    unsafe { native::invoke(0xA4EFEF9440A5B0EF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_current_weapon_visible(ped: Ped, visible: bool, deselect_weapon: bool, p3: bool, p4: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(visible), NativeArg::boolean(deselect_weapon), NativeArg::boolean(p3), NativeArg::boolean(p4)];
    unsafe { native::invoke(0x0725A4CCFDED9A70, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_drops_inventory_weapon(ped: Ped, weapon_hash: Hash, x_offset: f32, y_offset: f32, z_offset: f32, ammo_count: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::float(x_offset), NativeArg::float(y_offset), NativeArg::float(z_offset), NativeArg::int(ammo_count)];
    unsafe { native::invoke(0x208A1888007FC0E6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_drops_weapons_when_dead(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x476AE72C1D19D1A8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_infinite_ammo(ped: Ped, toggle: bool, weapon_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x3EDCB0505123623B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_player_ped_quick_swap_weapon_by_guid(ped: Ped, guid_primary: &mut Any, guid_secondary: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(guid_primary), NativeArg::in_out(guid_secondary)];
    unsafe { native::invoke(0xEC1F85DA51D3D6C4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_weapon_heading(vehicle: Vehicle, seat_index: i32, heading: f32, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(seat_index), NativeArg::float(heading), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x194D877FC5597B7D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn should_weapon_be_discarded_when_swapped(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x2C83212A7AA51D3D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x000_fa7_a4_a8443_af7(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x000FA7A4A8443AF7, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x07_e1_c35_f0078_c3_f9(ped: Ped, weapon: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon)];
    unsafe { native::invoke(0x07E1C35F0078C3F9, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x16_d9841_a85_fa627_e(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x16D9841A85FA627E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x183_ce355115_b6_e75(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x183CE355115B6E75, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x431240_a58484_d5_d0(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x431240A58484D5D0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x457_b16951_ad77_c1_b(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x457B16951AD77C1B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x45_e57_fdd531_c9477(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0x45E57FDD531C9477, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x486_c96_a0_dcd2_bc92(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x486C96A0DCD2BC92, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x63_b83_a526329_afbc(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0x63B83A526329AFBC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x641351_e9_ad103890(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x641351E9AD103890, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x74_c2365_fdd1_bb48_f(p0: NativeArg, p1: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0x74C2365FDD1BB48F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9409_c62504_a8_f9_e9(vehicle: Vehicle, p1: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x9409C62504A8F9E9, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x9_cca3131_e6_b53_c68(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0x9CCA3131E6B53C68, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a2091482_ed42_ef85(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xA2091482ED42EF85, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a3716_a77_dcf17424(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xA3716A77DCF17424, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_a769_d753922_b031_b(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xA769D753922B031B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_b0_fb9_b196_a3_d13_f0(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xB0FB9B196A3D13F0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_c5899_c4_cd2_e2495_d(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xC5899C4CD2E2495D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_d53846_b9_c931_c181(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xD53846B9C931C181, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_e9_b3_fec825668291(p0: NativeArg, p1: NativeArg, p2: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0, p1, p2];
    unsafe { native::invoke(0xE9B3FEC825668291, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_ecbb26529_a737_ef6(p0: NativeArg) -> Result<(), NativeError> {
    let arguments = [p0];
    unsafe { native::invoke(0xECBB26529A737EF6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f08_d8_feb455_f2_c8_c(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xF08D8FEB455F2C8C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f252_a85_b8_f3_f8_c58(weapon_collection: Hash, dualwield_variant: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_collection), NativeArg::hash(dualwield_variant)];
    unsafe { native::invoke(0xF252A85B8F3F8C58, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f2_f585411_e748_b9_c(p0: NativeArg, p1: NativeArg) -> Result<Any, NativeError> {
    let arguments = [p0, p1];
    unsafe { native::invoke(0xF2F585411E748B9C, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn native_0x_f8204_ef17410_bf43(weapon_group_hash: Hash, p1: f32, p2: f32, p3: NativeArg) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(weapon_group_hash), NativeArg::float(p1), NativeArg::float(p2), p3];
    unsafe { native::invoke(0xF8204EF17410BF43, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ammo_to_ped(ped: Ped, weapon_hash: Hash, amount: i32, add_reason: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::int(amount), NativeArg::hash(add_reason)];
    unsafe { native::invoke(0xB190BCA3F4042F95, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn add_ammo_to_ped_by_type(ped: Ped, ammo_type: Hash, amount: i32, add_reason: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(ammo_type), NativeArg::int(amount), NativeArg::hash(add_reason)];
    unsafe { native::invoke(0x106A811C6D3035F3, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn attach_weapon_to_horse_holster(horse: Ped, weapon_hash: Hash, owner_ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse), NativeArg::hash(weapon_hash), NativeArg::int(owner_ped)];
    unsafe { native::invoke(0x14FF0C2545527F9B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn can_ped_access_mount_weapons(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x23BF601A42F329A0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn clear_ped_last_weapon_damage(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x087D8F4BC65F68E4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn create_weapon_object(weapon_hash: Hash, ammo_count: i32, x: f32, y: f32, z: f32, show_world_model: bool, scale: f32) -> Result<Object, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash), NativeArg::int(ammo_count), NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::boolean(show_world_model), NativeArg::float(scale)];
    unsafe { native::invoke(0x9888652B8BA77F73, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn delete_weapon_objects_on_horse(horse: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(horse)];
    unsafe { native::invoke(0xD4C6E24D955FF061, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_all_special_ammo_for_ped(ped: Ped, weapon_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xD63B4BA3A02A99E0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_ammo_type_for_ped(ped: Ped, ammo_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0xAA5A52204E077883, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn disable_ammo_type_for_ped_weapon(ped: Ped, weapon_hash: Hash, ammo_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0xF0D728EEA3C99775, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_ped_have_pistol(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xBFCA7AFABF9D7967, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_ped_have_repeater(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x495A04CAEC263AF8, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_ped_have_revolver(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x5B235F24472F2C3B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_ped_have_rifle(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x95CA12E2C68043E5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_ped_have_shotgun(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xABC18A28BAD4B46F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn does_ped_have_sniper(ped: Ped, p1: bool) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x80BB243789008A82, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_all_special_ammo_for_ped(ped: Ped, weapon_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x404514D231DB27A0, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_ammo_type_for_ped(ped: Ped, weapon_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x3B7B7908B7ADFB4B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_ammo_type_for_ped_weapon(ped: Ped, weapon_hash: Hash, ammo_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0x23FB9FACA28779C1, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn enable_weapon_restore(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xC395355843BE134B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn explode_ped_ammo_type(ped: Ped, ammo_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0x44C8F4908F1B2622, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ammo_in_clip_by_inventory_uid(ped: Ped, ammo: &mut i32, inventory_uid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(ammo), NativeArg::in_out(inventory_uid)];
    unsafe { native::invoke(0x678F00858980F516, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ammo_in_ped_weapon_from_guid(ped: Ped, guid: &mut Any) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(guid)];
    unsafe { native::invoke(0x4823F13A21F51964, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ammo_recommended_type_for_weapon(weapon_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xEC97101A8F311282, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ammo_type_for_weapon(weapon_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x5C2EA6C44F515F34, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_best_ped_weapon_in_group(ped: Ped, weapon_group: Hash, p2: bool, p3: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_group), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x9F67929D98E7C6E8, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_best_ped_weapon_in_inventory(ped: Ped, p1: NativeArg, guid_primary: &mut Any) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), p1, NativeArg::in_out(guid_primary)];
    unsafe { native::invoke(0x7B98500614C8E8B8, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_can_switch_weapon(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBC9444F2FF94A9C0, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_can_twirl_weapon(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x6554ECCE226F2A2A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_correct_kit_emote_twirl_gun(ped: Ped, weapon_guid: &mut Any) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(weapon_guid)];
    unsafe { native::invoke(0xCD356B42C57BFE01, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_ammo_type_from_guid(ped: Ped, weapon_guid: &mut Any) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(weapon_guid)];
    unsafe { native::invoke(0xAF9D167A5656D6A6, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_current_ped_weapon_ammo_type(ped: Ped, weapon_object: Object) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(weapon_object)];
    unsafe { native::invoke(0x7E7B19A4355FEE13, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_default_ped_weapon_collection(ped_model: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(ped_model)];
    unsafe { native::invoke(0xD42514C182121C23, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_default_unarmed_weapon_hash(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x08FF1099ED2E6E21, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_default_weapon_attach_point(weapon_hash: Hash) -> Result<i32, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x65DC4AC5B96614CB, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_is_ped_taking_poison_gas_damage(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0DE0944ECCB3DF5D, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_lockon_range_current_weapon(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x3799EFCC3C8CD5E1, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_longarms_instantly_store_on_dismount(ped: Ped, p1: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p1)];
    unsafe { native::invoke(0x5A695BD328586B44, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_max_lockon_distance_of_current_ped_weapon(ped: Ped) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x79B1A6E780266DB0, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_num_peds_restrained_from_bolas(ped: Ped) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x46D42883E873C1D7, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_current_held_weapon(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x8425C5F057012DAB, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_gun_spinning_equipped_kit_emote_twirl(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2C4FEC3D0EFA9FC0, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_gun_spinning_hash_from_weapon_emote_variation(ped: Ped, weapon_emote_variation: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(weapon_emote_variation)];
    unsafe { native::invoke(0xF3B1620B920D1708, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_hogtie_weapon(ped: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x90EB1CB189923587, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_weapon_in_slot(ped: Ped, slot_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(slot_hash)];
    unsafe { native::invoke(0xDBC4B552B2AE9A83, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_weapon_object(ped: Ped, p1: bool) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0x6CA484C9A7377E4F, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_ped_worst_weapon(ped: Ped, p1: bool, p2: bool, p3: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3)];
    unsafe { native::invoke(0xDA37A053C1522F5D, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_player_ped_quick_swap_weapon_by_guid(ped: Ped, guid_primary: &mut Any, guid_secondary: &mut Any) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(guid_primary), NativeArg::in_out(guid_secondary)];
    unsafe { native::invoke(0xB7E52A058B07C7E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_projectile_ignited_in_volume(volume: Volume, out_entity: &mut Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::in_out(out_entity)];
    unsafe { native::invoke(0x74C8000FDD1BB111, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_projectile_in_volume(volume: Volume, out_entity: &mut Entity) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(volume), NativeArg::in_out(out_entity)];
    unsafe { native::invoke(0x74C8000FDD1BB222, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapontype_model(weapon_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xF70825EB340E7D15, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapontype_slot(weapon_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x46F032B8DDF46CDE, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_attach_point(ped: Ped, attach_point: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attach_point)];
    unsafe { native::invoke(0xCAD4FE9398820D24, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_component_type_model(component_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(component_hash)];
    unsafe { native::invoke(0x59DE03442B6C9598, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_damage(weapon_object: Object) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(weapon_object)];
    unsafe { native::invoke(0x904103D5D2333977, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_dirt(weapon_object: Object) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(weapon_object)];
    unsafe { native::invoke(0x810E8AE9AFEA7E54, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_emote_variation(ped: Ped, variation: i32) -> Result<i32, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(variation)];
    unsafe { native::invoke(0x86147D05FA831D3A, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_from_default_ped_weapon_collection(weapon_collection: Hash, weapon_group: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(weapon_collection), NativeArg::hash(weapon_group)];
    unsafe { native::invoke(0x9EEFD670F10656D7, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_from_horse_holster(horse: Ped) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::int(horse)];
    unsafe { native::invoke(0xAFFD0CCF31F469B8, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_gun_spinning_weapon_emote_trick_type_hash(emote: Hash, weapon_emote_trick_type: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(emote), NativeArg::int(weapon_emote_trick_type)];
    unsafe { native::invoke(0xF4601C1203B1A78D, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_has_multiple_ammo_types(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x58425FCA3D3A2D15, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_name(weapon_hash: Hash) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x89CF5FF3D363311E, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_name_2(weapon_hash: Hash) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x6D3AC61694A791C5, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_name_with_permanent_degradation(weapon_hash: Hash, permanent_degradation_level: f32) -> Result<NativeString, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash), NativeArg::float(permanent_degradation_level)];
    unsafe { native::invoke(0x7A56D66C78D8EF8E, &arguments) }.map(|result| result.as_string())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_object_from_ped(ped: Ped, p1: bool) -> Result<Object, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1)];
    unsafe { native::invoke(0xC6A6789BB405D11C, &arguments) }.map(|result| result.as_i32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_replaced_hash(clear: bool) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::boolean(clear)];
    unsafe { native::invoke(0x9F0E1892C7F228A8, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_scale(weapon_object: Object) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(weapon_object)];
    unsafe { native::invoke(0x22084CA699219624, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_soot(weapon_object: Object) -> Result<f32, NativeError> {
    let arguments = [NativeArg::int(weapon_object)];
    unsafe { native::invoke(0x4BF66F8878F67663, &arguments) }.map(|result| result.as_f32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_stat_id(weapon_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x8EC44AE8DECFF841, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_type_from_ammo_type(ammo_type: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(ammo_type)];
    unsafe { native::invoke(0x7AA043F6C41D151E, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_weapon_unlock(weapon_hash: Hash) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x865F36299079FB75, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_weapon_collection_to_ped(ped: Ped, weapon_collection: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_collection)];
    unsafe { native::invoke(0x899A04AFCC725D04, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_weapon_component_to_entity(entity: Entity, component_hash: Hash, weapon_hash: Hash, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(component_hash), NativeArg::hash(weapon_hash), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x74C9090FDD1BB48E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn give_weapon_component_to_weapon_object(weapon_object: &mut Object, ped: Ped, component_hash: Hash, p3: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(weapon_object), NativeArg::int(ped), NativeArg::hash(component_hash), NativeArg::boolean(p3)];
    unsafe { native::invoke(0x1A47699E8D533E8F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_been_damaged_by_weapon(entity: Entity, weapon_name: Hash, weapon_type: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(weapon_name), NativeArg::int(weapon_type)];
    unsafe { native::invoke(0xDCF06D0CDFF68424, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_entity_been_damaged_by_weapon_recently(entity: Entity, weapon_hash: Hash, ms: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(entity), NativeArg::hash(weapon_hash), NativeArg::int(ms)];
    unsafe { native::invoke(0x9E2D5D6BC97A5F1E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_ped_got_weapon_component(ped: Ped, component_hash: Hash, weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component_hash), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xBBC67A6F965C688A, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn has_weapon_asset_loaded(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xFF07CF465F48B830, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn hide_ped_weapons(ped: Ped, p0: i32, immediately: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(p0), NativeArg::boolean(immediately)];
    unsafe { native::invoke(0xFCCC886EDE3C63EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn holster_ped_weapons(ped: Ped, p1: bool, p2: bool, p3: bool, immediately: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(p1), NativeArg::boolean(p2), NativeArg::boolean(p3), NativeArg::boolean(immediately)];
    unsafe { native::invoke(0x94A3C1B804D291EC, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ammo_silent(ammo_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0xD2866CBA797E872E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ammo_silent_2(ammo_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0x7EFACC589B98C488, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ammo_type_valid_for_weapon(weapon_hash: Hash, ammo_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash), NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0xC570B881754DF609, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ammo_valid(ammo_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0x1F7977C9101F807F, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_carrying_weapon_sniper_at_attach_point(ped: Ped, attach_point: i32) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attach_point)];
    unsafe { native::invoke(0xD2209866B0CB72EA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_ped_current_weapon_holstered(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0xBDD9C235D8D1052E, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_target_ped_constrained_by_ped_using_bolas(ped: Ped, target_ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(target_ped)];
    unsafe { native::invoke(0x8D50F43298AB9545, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_binoculars(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xC853230E76A152DF, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_close_range(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xEA522F991E120D45, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_holster_state_changing(ped: Ped) -> Result<bool, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x2387D6E9C6B478AA, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_kit(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x6ABAD7B0A854F8FB, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_kit_2(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x49E40483948AF062, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_knife(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x792E3EF76C911959, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_lantern(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x79407D33328286C6, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_lasso(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x6E4E1A82081EABED, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_one_handed(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xD955FEE4B87AFA07, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_silent(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x5809DBCA0A37C82B, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_sniper(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x6AD66548840472E5, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_throwable(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x30E7C16B12DA8211, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_torch(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x506F1DE1BFC75304, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn is_weapon_two_handed(weapon_hash: Hash) -> Result<bool, NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x0556E9D2ECF39D01, &arguments) }.map(|result| result.as_bool())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn listen_projectile_hit_events(listen: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::boolean(listen)];
    unsafe { native::invoke(0xDA5D3F2C6DD5B5D4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn make_ped_reload(ped: Ped) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x79E1E511FF7EFB13, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn refill_ammo_in_clip(ped: Ped, clip_inventory_uid: &mut Any, p2: i32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(clip_inventory_uid), NativeArg::int(p2)];
    unsafe { native::invoke(0xDF4A3404D022ADDE, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn refill_ammo_in_current_ped_weapon(ped: Ped) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x0A2AB7B7ABC055F4, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn register_weapon_object_for_ignition(weapon_object: Entity) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(weapon_object)];
    unsafe { native::invoke(0x74C90AAACC1DD48F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_all_ped_ammo(ped: Ped) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped)];
    unsafe { native::invoke(0x1B83C0DEEBCBB214, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ammo_from_ped(ped: Ped, weapon_hash: Hash, amount: i32, remove_reason: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::int(amount), NativeArg::hash(remove_reason)];
    unsafe { native::invoke(0xF4823C813CB8277D, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_ammo_from_ped_by_type(ped: Ped, ammo_hash: Hash, amount: i32, remove_reason: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(ammo_hash), NativeArg::int(amount), NativeArg::hash(remove_reason)];
    unsafe { native::invoke(0xB6CFEC32E3742779, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_weapon_asset(weapon_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xC3896D03E2852236, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_weapon_component_from_ped(ped: Ped, component_hash: Hash, weapon_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(component_hash), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0x19F70C4D80494FF8, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn remove_weapon_from_ped_by_guid(ped: Ped, weapon_guid: &mut Any, remove_reason: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(weapon_guid), NativeArg::hash(remove_reason)];
    unsafe { native::invoke(0x51C3B71591811485, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn request_weapon_asset(weapon_hash: Hash, p1: i32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::hash(weapon_hash), NativeArg::int(p1), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x72D4CB5DB927009C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn send_weapon_to_inventory(ped: Ped, weapon_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash)];
    unsafe { native::invoke(0xE9BD19F8121ADE3E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_active_gun_spinning_equip_kit_emote_twirl(ped: Ped, emote: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(emote)];
    unsafe { native::invoke(0xCBCFFF805F1B4596, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_active_gun_spinning_kit_emote_twirl(ped: Ped, weapon_emote_trick_type: i32, spin: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(weapon_emote_trick_type), NativeArg::hash(spin)];
    unsafe { native::invoke(0x01F661BB9C71B465, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_allow_dual_wield(ped: Ped, allow: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(allow)];
    unsafe { native::invoke(0x83B8D50EB9446BBA, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ammo_in_turret(vehicle: Vehicle, turret_hash: Hash, ammo: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::hash(turret_hash), NativeArg::int(ammo)];
    unsafe { native::invoke(0xBDDA0C290C228159, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ammo_type_for_ped_weapon(ped: Ped, weapon_hash: Hash, ammo_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(weapon_hash), NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0xCC9C4393523833E2, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ammo_type_for_ped_weapon_inventory(ped: Ped, weapon_inventory_uid: &mut Any, ammo_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::in_out(weapon_inventory_uid), NativeArg::hash(ammo_hash)];
    unsafe { native::invoke(0xEBE46B501BC3FBCF, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_arrow_trail_fx(ped: Ped, trail_hash: Hash) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::hash(trail_hash)];
    unsafe { native::invoke(0x2EBF70E1D8C06683, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_force_auto_equip(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xBE711B14A159E84F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_force_current_weapon_into_cocked_state(ped: Ped, attach_point: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attach_point)];
    unsafe { native::invoke(0x5230D3F6EE56CFE6, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_gun_spinning_inventory_slot_id_activate(ped: Ped, emote_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(emote_type)];
    unsafe { native::invoke(0x408CF580C5E96D49, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_instantly_store_longarms_on_dismount(ped: Ped, store_longarms: bool, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(store_longarms), NativeArg::int(p2)];
    unsafe { native::invoke(0xB832F1A686B9B810, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_all_weapons_visibility(ped: Ped, visible: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(visible)];
    unsafe { native::invoke(0x4F806A6CFED89468, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_infinite_ammo_clip(ped: Ped, toggle: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(toggle)];
    unsafe { native::invoke(0xFBAA1E06B6BCA741, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_ped_weapon_attach_point_visibility(ped: Ped, attach_point: i32, visible: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::int(attach_point), NativeArg::boolean(visible)];
    unsafe { native::invoke(0x67E21ACC5C0C970C, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_projectile_effect_radius(projectile: Entity, radius: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(projectile), NativeArg::float(radius)];
    unsafe { native::invoke(0x74C9080FDD1BB48F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_projectile_fuse_time(projectile: Entity, time: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(projectile), NativeArg::float(time)];
    unsafe { native::invoke(0x74C9080FDD1BB48E, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_weapon_heading_limits(vehicle: Vehicle, p1: i32, min_heading: f32, max_heading: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(p1), NativeArg::float(min_heading), NativeArg::float(max_heading)];
    unsafe { native::invoke(0x56CB3B4305A4F7CE, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_weapon_heading_limits_2(vehicle: Vehicle, p1: i32, min_heading: f32, max_heading: f32) -> Result<Any, NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::int(p1), NativeArg::float(min_heading), NativeArg::float(max_heading)];
    unsafe { native::invoke(0xBF5987E1CDE63501, &arguments) }.map(|result| result.as_i64())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_vehicle_weapon_reload_mode(vehicle: Vehicle, no_reload: bool, p2: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(vehicle), NativeArg::boolean(no_reload), NativeArg::int(p2)];
    unsafe { native::invoke(0x8A779706DA5CA3DD, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weapon_damage(weapon_object: Object, level: f32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(weapon_object), NativeArg::float(level), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xE22060121602493B, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weapon_degradation(weapon_object: Object, level: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(weapon_object), NativeArg::float(level)];
    unsafe { native::invoke(0xA7A57E89E965D839, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weapon_dirt(weapon_object: Object, level: f32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(weapon_object), NativeArg::float(level), NativeArg::boolean(p2)];
    unsafe { native::invoke(0x812CE61DEBCAB948, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weapon_holstered(ped: Ped, disable_anim: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(ped), NativeArg::boolean(disable_anim)];
    unsafe { native::invoke(0x4820A6939D7CEF28, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weapon_level_threshold(weapon_object: Object, threshold: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(weapon_object), NativeArg::float(threshold)];
    unsafe { native::invoke(0xD4071EFC83794B2F, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weapon_scale(weapon_object: Object, scale: f32) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(weapon_object), NativeArg::float(scale)];
    unsafe { native::invoke(0xC3544AD0522E69B4, &arguments) }.map(|_| ())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn set_weapon_soot(weapon_object: Object, level: f32, p2: bool) -> Result<(), NativeError> {
    let arguments = [NativeArg::int(weapon_object), NativeArg::float(level), NativeArg::boolean(p2)];
    unsafe { native::invoke(0xA9EF4AD10BDDDB57, &arguments) }.map(|_| ())
}

// ============================================================================
// ZONE
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_map_zone_at_coords(x: f32, y: f32, z: f32, type_: i32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z), NativeArg::int(type_)];
    unsafe { native::invoke(0x43AD8FC02B429D33, &arguments) }.map(|result| result.as_u32())
}

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn get_water_map_zone_at_coords(x: f32, y: f32, z: f32) -> Result<Hash, NativeError> {
    let arguments = [NativeArg::float(x), NativeArg::float(y), NativeArg::float(z)];
    unsafe { native::invoke(0x5BA7A68A346A5A91, &arguments) }.map(|result| result.as_u32())
}

// ============================================================================
// _NAMESPACE4
// ============================================================================

/// Invoke the corresponding RDR2 game native.
///
/// # Safety
/// The caller must satisfy the game native's handle and pointer contract.
pub unsafe fn report_player_bad_sport_behavior(gamer_handle: &mut Any, bad_sport_behavior_type: i32) -> Result<(), NativeError> {
    let arguments = [NativeArg::in_out(gamer_handle), NativeArg::int(bad_sport_behavior_type)];
    unsafe { native::invoke(0xC31C44C43B48FDE3, &arguments) }.map(|_| ())
}

// 7132 named natives generated.